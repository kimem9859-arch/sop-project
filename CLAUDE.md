# CLAUDE.md — SOP 가디언 작업 지침

> 데스크톱·라즈베리파이 양쪽 Claude Code가 자동 로드하는 **공유 두뇌**.
> 설계 사양은 여기서 정의하지 않는다 — 정본은 `docs/통합수행설계문서_전체_섹션1-15.md`.
> Claude Code 작업은 프로젝트 작업과 분리 기록: `docs/claude-code-작업로그.md`(CC 시간순 로그) + `docs/claude-code-작업문서.md`(CC 설명서·원칙). 프로젝트 작업 로그는 `docs/작업로그.md`.

## 프로젝트
**1인칭 Vision AI × 웨어러블 기반 작업자 SOP 순서 위반 실시간 감지·차단 시스템.** PECVD 정비(PM)에서 작업자의 순서 위반(휴먼 에러)을 버튼 누르기 직전에 사전 감지·차단. 콘솔은 **시연용 모조품**(실제 PECVD 기능 없음·타워램프 시각화는 부차적) — 핵심은 **비전 AI 순서위반 감지·차단**. (안전은 효과 → 프레이밍 B)

## 정본 (Source of Truth)
- **설계·사양·배경·SOP·하드웨어·KPI = `docs/통합수행설계문서_전체_섹션1-15.md`** (단일 정본). 작업 시 원문을 읽고 진행.
- 폴더 지도·실행법 = `README.md`. 트랙별 상세 = `dev/*/README.md`.
- 이 CLAUDE.md = "어떻게 일하나"(규칙·흐름·현황)만. 사양은 통합문서로 위임.

## 두 환경의 역할 (런타임 분리 / 지식 공유)
| | 🖥️ 데스크톱 | 🍓 라즈베리파이 (Pi#1 = 주 작업기) | 🍓 sop-pi-2 |
|---|---|---|---|
| 역할 | 설계·통합문서 편집 · PoC(x86) · 발표 제작 | Rpi5 코드 실행·실HW 테스트·Hailo · 통합문서 편집 | Pi#1 복제본(예비/병행). `ssh pi2`(공유기 192.168.1.9) |
| 클론 | `sop-project` | `sop-project` + 그 안 `Rpi5/`(엄브렐러) | Pi#1과 동일 구성 |

## ⭐ 통합문서 = 단일 정본, 양방향 동기화
파이(개발·테스트 중 수정)와 데스크톱(발표 제작)에서 둘 다 편집·사용. **사본 금지**(병합 충돌).
- 파이: 수정 → `git commit && push` (sop-project)
- 데스크톱: `git pull` → 최신본으로 발표 제작

## Rpi5 = 별도 repo (파이 런타임 코드)
- `github.com/kimem9859-arch/Rpi5.git`, 작업 브랜치 **`feature/fsm-interlock`** (최신 본류). 구 브랜치 정리 완료.
- 파이 셋업(엄브렐러 재현):
  ```bash
  git clone https://github.com/kimem9859-arch/sop-project.git ~/sop-project
  cd ~/sop-project
  git clone -b feature/fsm-interlock https://github.com/kimem9859-arch/Rpi5.git Rpi5
  cd ~/sop-project && claude        # 통합문서 + 코드 한자리
  ```
- `Rpi5/`는 sop-project에서 gitignore(별도 repo). 런타임 맥락은 `Rpi5/CLAUDE.md`.

## 모델 (네이밍 규약 정본 §7.1, 3단계)
- `person_v1.pt` 초기 검증(완료) → **`console_v1`** 버튼검출 **1차 검증 테스트**(5클래스 B1~B4+EMO, .hef 빌드완료·파이 실추론·벤치마크 완료 — B1·B2·B3·EMO 검출 동작, **B4 미탐지** 확인) → **`console_v2` 최종 — 학습·`.hef` 변환·파이 배포 완료(2026-07-16, §10.14·§10.15). 🔴 B4 해결 여부는 미판정**(판정 기준 = §10.16, 최종 근거 = 미촬영 test 세션).
- 버튼 클래스맵·Pi 추론 규격(uint8 640·HailoRT NMS·4.x): `dev/ai_model/README.md`.
- ※ **측정 수치 정본 = 통합문서 §10**(학습 mAP·실추론 FPS 등). 여기·dev/README는 상태·결정만, 수치는 §10 참조.

## PoC 실행 (Step1, 데스크톱)
루트에서: `./dev/poc/run.sh poc_data/clips/ --rois dev/poc/rois.json --out dev/poc/out`
- 머신별 차이는 `dev/poc/SETUP.md`. 환경자산 `.poc_venv`·`poc_data`·`.syslibs`는 gitignore → 머신마다 재구축.

## 작업·커밋 규칙
- **⭐ 세션 시작 = 원격 먼저**: 양 머신이 같은 repo를 편집하므로, 로컬이 최신이라 가정 금지. 작업 전 `git fetch`로 ahead/behind 확인 후 필요시 `git pull`. (SessionStart 훅 `.claude/hooks/session-sync-check.sh`가 sop-project·Rpi5 양쪽을 자동 점검·보고 — 훅이 안 돌면 수동으로.)
- **자동 점검 인프라(훅)**: SessionStart ① `session-worklog-brief.sh`(**세션 시작 표 배너** = 원격 동기화 + ⏸중단·▶다음 이어하기 + 현재 session_id 주입을 한 표로 표시; 내부에서 `session-sync-check.sh`를 **순차 호출**[SessionStart 병렬실행 경쟁 회피]하고 `_banner.py`로 CJK정렬 표+systemMessage 렌더) ② `session-prune-stubs.sh`(resume 목록의 tiny/원격제어 스텁 세션을 `.trash`로 정리, systemMessage 배너). **①② 뿐**(`.claude/settings.json` 등록) — **클론 후 별도 설정 불필요**(`git pull`이면 적용). 권한 허용목록도 `.claude/settings.json`. **전부 repo 공유**(양 머신 동일).
  - 🗑️ **2026-07-17 폐기 2건**(실증 검증 후 — 경위·수치는 `docs/claude-code-작업로그.md`): ③ SessionEnd `session-worklog-commit.sh`(세션→커밋 자동매핑 — 산출 45건 중 고유정보 6건뿐·나머지는 사람 기록과 중복이거나 자기참조. **"세션마무리 누락을 구제한 사례 0건"** → 역할을 「세션 마무리」가 이미 전부 수행) / ④ pre-commit `.githooks/pre-commit`(문서 정합성 LLM 점검 — **12회 실행·적발 0건**, 매 호출이 세션 스텁을 흘려 ②가 뒤처리하는 닫힌 루프까지 형성). ④는 구 `doc-consistency-check.sh`(2026-07-01 제거)에 이은 **같은 목표의 두 번째 실패** → **문서 drift 방어는 훅이 아니라 아래 단일정본 규칙 + 편집 규율이 전담**(원래도 1차 방어는 이것이었고, 훅은 값을 못 한 안전망이었음).
- **⭐ "세션 마무리" 의식** (슬래시 `/세션마무리` 또는 스킬 `session-wrap`으로 절차화): 사용자가 **`/세션마무리`** 를 치거나 **"세션 마무리"**(또는 "세션 정리")라고 말하면 이번 세션 작업을 4분류(✅완료/⏸중단/▶다음/🔗커밋)로 정리해 **현재 session_id 태그와 함께** 기록한다(브리핑 훅 ID 사용). ※**정의 = 하루의 끝·작업 종료가 아니라 "한 세션/작업 단위(A작업)가 논리적으로 끝난 시점"** — 하루에 여러 번 가능(A작업 끝 → 마무리, B작업 시작). **프로젝트 작업 → `docs/작업로그.md`, Claude Code 작업(훅·도구·인프라) → `docs/claude-code-작업로그.md`** 로 나눠 적는다(둘 다면 양쪽에). (SessionEnd 자동 커밋매핑은 성격 구분 못 해 `작업로그.md` 하단에 전체 무분류로 쌓임 — 사람 기록만 분리.) 로그=시간순 저널이라 CLAUDE.md「현재 상태」(스냅샷)와 역할 분리·공존. **마무리 시 이전 세션의 미기록 커밋을 소급 점검**해 있으면 사용자 확인 후 별도 소급 블록으로 기록(2026-07-18 신설 — SessionEnd 자동매핑 폐기 후의 유일한 누락 안전망). 상세 절차는 `.claude/skills/session-wrap/SKILL.md`.
- 사양은 통합문서에만(README/CLAUDE 복붙 금지 → drift). 폴더·경로 바꾸면 README·CLAUDE 같이 갱신.
- **⭐ 측정·사양 수치 = 통합문서 단일 정본**: mAP·FPS·해상도 등 측정값은 통합문서 §10(설계값은 해당 §)에만 두고, 운영문서(CLAUDE·기준문서 제외 dev/README)는 **"§X 참조" 포인터만**(복제 금지). 수치가 바뀌면 통합문서만 고친다. (기준문서는 나침반이라 요약 보유 허용)
- **⭐ brainstorming 먼저(설계→승인→구현) = 기본 작업 방식**(2026-07-19 전환, 사용자 지시 — 이전의 "즉각·바로 구현" 습관이 계획 없이 기능을 만들어 부작용을 낳던 것을 교정): **구현·기능·스킬·훅·코드 수정·다중파일·구조(번호·파일명) 변경·삭제·외부 전송 등 비자명 작업은 곧장 구현하지 말고 먼저 설계를 제시·승인받는다**(superpowers `brainstorming` 스킬 활용 — 의도·요구·제약·성공기준 탐색 → 2~3안 트레이드오프 → 설계 제시 → 승인). **승인 전 구현·코드 금지**(HARD-GATE). 설계 깊이는 복잡도에 비례(단순하면 몇 문장). **자명한 것(문서 오타·날짜/해시 정정·작업로그 기입)만 바로 진행.** 설계 문서 = `docs/superpowers/specs/YYYY-MM-DD-<주제>-design.md`에 저장·커밋 — 단 **설계문서엔 논의·미결만, 확정 측정·사양 수치는 여전히 통합문서 §10에만**(drift 방지, 위 단일정본 규칙 유지). ※플러그인(superpowers 등)은 참고 도구함이나 **이 brainstorming 방식만은 프로젝트 채택 규칙**이다.
- **⭐ 수술적·최소 구현 + 자기검열**(Karpathy·Boris 교훈 선별 흡수 — 메뉴, 플러그인 아님): 설계 승인 후 **구현은 요청 범위만·최소로** — 투기적 기능·성급한 추상화 금지, **멀쩡히 도는 코드는 리팩터링하지 않는다**, 기존 스타일에 맞춘다(생각은 충분히·만드는 건 최소). **근본원인 우선·임시방편 금지**(systematic-debugging). **비자명 변경은 제출 전 "더 우아한 방법 없나" 1회 자기검열**(자명한 건 스킵 — 과설계 금지). **틀어지면 멈추고 재계획.**
  - ⛔ **채택 안 함**(Boris 원문 중 환경·방침 충돌): subagent 자유 남용·auto mode(비용 규율·"묻지 않으면 agent 금지") / 자동 버그수정 즉시착수(brainstorming-first와 충돌) / `tasks/todo.md`·`lessons.md` 신설(memory·작업로그·Task 도구가 상위).
- **커밋 구분**: 코드 수정 → 해당 repo(파이 코드는 `Rpi5`로) / 통합문서·설계 → `sop-project`로.
- 커밋 끝에 `Co-Authored-By: Claude <실제 작업 모델명> <noreply@anthropic.com>`(예: Opus 4.8, Fable 5 — **고정값 아님, 그 커밋을 만든 모델명**). push는 사용자 요청 시. (2026-07-14 Fable 점검: 모델별 작업 추적 위해 규칙화)

## 현재 상태 · 다음 작업 (2026-07-22)
- ✅ **console_v2 정량 채점 합격 — §10.16의 최종 판정 근거 확보(2026-07-21, 수치 정본 §10.20)**. test 113장(클린룸 형광등·전량 수동 라벨·**누출 0**) 채점 결과 **mAP50 0.992 · recall 0.993 · precision 0.998**, 사전 합의 기준(mAP50 ≥0.85·recall ≥0.90) 통과.
  - ⭐ **B4 AP50 0.997** — v1에서 실추론 0회였던 클래스가 사실상 만점. 파랑 스티커 재학습 효과가 정량 확정.
  - 클래스 간 오분류 **1건**·배경 오검출 **0건**. 옐로우등의 B2→B1 붕괴는 형광등에서 미재현. 운용 임계 0.65가 최적점(변경 근거 없음).
  - ⚠️ mAP50-95 0.592는 **성능 저하가 아니라 박스 규칙 차이** — v2 예측 박스가 정답보다 넓이 14% 큼(test 라벨링이 더 타이트). §10.14의 Amodal→Modal 때와 같은 현상. FSM은 ROI 겹침 판정이라 실사용 영향 작음.
  - ⭐ **결정: 재학습(v3) 보류**(§10.20) — 개선 여지 0.008 · **v2가 누출-프리라 v3보다 점수 신뢰도가 높음** · 더 급한 미지(실콘솔·HOI·저조도)가 있음. 데이터는 Roboflow `console_v3`(버전 2, 1104장)에 준비 완료. 착수 조건 = 실콘솔 저하 / 저조도·자연광 붕괴 / 박스 정밀도 실문제.
  - 🔴 **과장 금지** — "v2 검증 완료"가 아니라 **"클린룸 형광등·모조 콘솔에서 mAP50 0.992"**. 실콘솔(포맥스판) 2차 test가 §10.16의 최종 판정.
- ✅ **HOI 블로커 실증 해소(2026-07-21, 설계·근거 = `docs/superpowers/specs/2026-07-21-HOI-경로-design.md`)** — MediaPipe 손 모델이 이미 Hailo로 포팅돼 있고 `hailo_platform`+numpy+cv2만 쓴다 → **Python 3.13/aarch64 문제 원천 소멸**. 21점 랜드마크·**클린룸 장갑 착용 손 검출**·좌표계 정합·3모델 동시 55.2fps(NFR-1의 3.7배) 확인.
  - 🔴 **통합 제약**: `VDevice`는 장치당 1개(`HAILO_OUT_OF_PHYSICAL_DEVICES`). `detector.py`가 자체 VDevice를 영구 점유 → **공유 구조 변경 필요**(승인 사안, 미착수·런타임 코드 미변경).
  - ⚠️ **손끝 정확도 = 조건부** — 기하학적으로는 정확하나, 손을 편 채 지나가도 버튼에 들어가고 연속 프레임에서 최근접 버튼이 5프레임 만에 바뀐다. **접촉 임계 설계엔 "실제 버튼 누르는 동작" 촬영이 선행 조건**.
- ✅ **VDevice 공유 구조 변경 완료(2026-07-22, 설계 = `docs/superpowers/specs/2026-07-22-detector-vdevice-공유-design.md`)** — `hailo_device.py` 신설 + `detector.py` 교체. **버튼·팜·핸드 3모델이 한 VDevice에서 동시 구동** 확인(변경 전엔 `HAILO_OUT_OF_PHYSICAL_DEVICES`로 불가). 회귀 없음: 검출 60건 전부 동일 · test 113장 재채점 mAP50 0.992로 §10.20과 일치 · 성능 +0.8%.
  - 🔴 **시연 경로(`run_demo.sh` → GUI)는 미검증** — 카메라 미연결. **카메라 가능해지면 가장 먼저 확인**(GUI 기동·영상·검출 오버레이·정상 종료). 실패 시 단일 커밋이라 `git revert` 한 번으로 롤백.
  - ⚠️ ROI 판정·FSM 배선은 **이미 구현돼 있었다**(`camera_thread.roi_at_point`·`roi_signal`) — 비어 있는 건 **손 좌표 공급 하나뿐**. 통합 범위가 당초 예상보다 작다.
- ✅ **GPIO 눌림 로깅 구현(2026-07-22, 설계 = `…2026-07-22-GPIO-눌림-로깅-design.md`)** — `bench_detector.py --gpio` + `dwell_probe.py --gpio-log`. 선행시간(t_눌림−t_도착)·사전 감지율·오경보 후보 산출. 🔴 **실촬영 미검증**(ESP32 전원 미연결) · **선행 확인 필수 = 카메라에 보이는 버튼 = GPIO에 물린 버튼인가**.
- ✅ **손 검출을 `camera_thread`에 연결 완료(2026-07-22)** — `hand_tracker.py` 신설(Hailo 팜+핸드 캡슐화) + `camera_thread.py`의 MediaPipe 블록 4곳 교체(**81줄 삭제·15줄 추가**). ESP32·USB 양쪽 클래스 모두 적용. **손끝 → `roi_at_point` → `roi_signal` → FSM 경로가 이어졌다**(정지 이미지로 검증: 검지끝 (124,293) → ROI B1).
  - ⭐ **안전 설계**: 모델·소스가 없거나 `HAND_ENABLED=False`면 **조용히 비활성**되고 `detect()`가 None을 반환 → 손 검출이 없던 종전과 **정확히 동일 동작**. 두 비활성 경로 모두 실측 확인.
  - 기존 테스트 **15개 전부 통과**(`test_fsm`·`test_hoi_sim`).
  - 🔴 **모델·blaze 소스가 repo 밖(`~/hoi_probe/`)** — 클론·sop-pi-2에선 자동 비활성된다. **vendoring 여부는 별도 결정 사항**(모델 7MB·소스 228K).
- ✅ **Phase 0~3 완주 — 실HW 첫 측정(2026-07-22, 수치 정본 §10.21)**. 카메라·GPIO·인터락 전부 연결한 첫 세션.
  - 🔴 **시연 경로가 깨져 있었다** — `safety_console`이 손검출 통합(`0953294`)에서 사라진 `MEDIAPIPE_AVAILABLE`을 계속 import해 `./run_demo.sh`가 `ImportError`로 죽었다. 수정 = Rpi5 `5f57f86`. **놓친 이유**: 당시 검증이 `camera_thread` 직접 import + `test_fsm`·`test_hoi_sim`뿐이었는데 **셋 다 `safety_console`·`main`을 import하지 않는다** → 스모크 테스트 `test_imports.py` 신설로 사각지대 차단.
  - ✅ 고친 뒤 전 구간 동작: GUI 기동 · 5클래스 동시 검출 · 손 검출 활성 · GPIO B1~B4·EMO · 인터락 `RUN(ACK)` · 정상 종료.
  - ⭐ **§10.11 "ESP32 부적합" 근거 둘 중 B4는 완전 무효** — 실시간 3세션 전부 B4 median conf 0.88~0.89 · **B4↔EMO 오인 0회**. **FPS 근거만 남았고, 그 원인이 전송으로 특정됐다**(추론 32ms=이론상 31fps인데 실효 10.1fps · 추론 없는 원시 전송도 8~11fps).
  - 🔴 **NFR-1 미달** — HOI 포함 **11.0fps**(손검출 OFF 12.8). "달성"으로 쓰지 말 것.
  - 🔴 **사전 감지율 56%(9/16)** — 프로젝트 핵심 명제를 처음 직접 잰 수치. **절반 가까이 놓친다.** ⭐본 경우엔 **ROI 오분류 0**.
- ✅ **손 검출률 개선 — 오프라인 선검증까지 완료, 원인 규명·구현 보류(2026-07-22, 상세 = `docs/superpowers/specs/2026-07-22-손검출률-개선-design.md` §6)**
  - 🔴 **A(프레임 간 트래킹·ROI 유도) 폐기** — 설계에 미리 둔 관문이 발동했다. 어떤 계수·임계 조합도 기준선 9/16을 못 넘었고 **실패 프레임이 87개로 항상 동일**했다 → 팜 검출이 실패한 프레임에선 ROI 유도도 실패한다 = **검출기가 손을 놓친 게 아니었다.** ⭐**런타임 코드 미변경**(관문의 목적).
  - ⭐ **진짜 원인 = 신뢰도 게이트가 정확한 검출을 버린다.** 놓친 눌림 직전 프레임에서 **손끝이 버튼 중심 6~10px(박스 안)인데 `flag`가 0.11~0.13**이라 `HAND_MIN_SCORE=0.5`에 걸려 버려졌다. 좌표가 부드럽게 이어지므로 잡음이 아니다. `flag`는 **손이 버튼을 누르느라 가려질 때 떨어지는데 그때가 관측이 필요한 순간**이다.
  - ⭐ **`HAND_MIN_SCORE` 0.5 → 0.2 한 줄로 56% → 75%, 오경보 불변**(0.2 아래는 포화). ✅ **반영 완료** — 도넛 커밋(Rpi5 `d7e7f1c`)이 config 4값을 함께 바꾸며 적용했고 홀드아웃 29회로 검증됐다. (2026-07-24 정정: 종전 "🔴 미반영" 표기는 반영 시점을 놓친 stale 기술이었다.)
  - ~~목표 90% 도달 불가~~ → **아래 도넛 2단계 ROI가 뒤집었다(92%)**. 정확히는 **"손 검출 파라미터만으로는 불가"**였고, 남은 4회의 물리적 원인(누르는 순간 손이 버튼을 덮음)은 **판정 구조를 바꿔** 우회했다.
  - ⚠️ **단순 margin 확대는 채택 안 함** — 넓힌 구역을 안쪽과 **똑같이** 취급해 오경보가 1→3개가 된다. 도넛은 **넓히되 단계를 나눠** 이 대가를 없앤 것이다.
- ✅ **⭐ 사각 도넛 2단계 ROI — 개입 감지 92%로 돌파(2026-07-22, 수치 정본 §10.22, 설계 = `docs/superpowers/specs/2026-07-22-도넛-2단계-ROI-design.md`)**. **사용자 아이디어**(버튼 크기만큼=2단계 위험 / 바깥 테두리=1단계 경고)가 출발점.
  - ⭐ **핵심 발견: 짧았던 선행시간은 사람이 빨라서가 아니라 검출이 끊겨서였다.** 갭메우기 0.3초만 넣어도 선행 median이 **0.38s → 0.55s**로 뛴다. 그 0.3초는 **§9.4 정본값인데 여태 미구현**이었다.
  - **개입 감지 62% → 92%**, 오경보 **분당 1.1회**(링 25px). 링 40px는 감지가 안 늘고 오경보만 2배 → **25px 무릎점**.
  - **발화는 C1 확정**(임계 도달 + **안쪽일 때만** 차단, 링은 예열·경고만) — C2는 오경보 2배 이상.
  - 🔴 **§10.21③ "갭메우기 효과 0"과 모순 아님** — 그건 **링 없이 "눌리는 그 프레임"** 지표. **세 지표(체류 구간/눌림 프레임/선행시간)가 다른 것을 잰다** — 인용 시 지표 명시 필수.
  - 재촬영 3세션(①튜닝 26회 ②**정상 순서만**=진짜 오경보 23회 ③🔒홀드아웃 29회 미개봉). ⚠️**행동을 천천히 하게 하려던 시도는 실패**(선행 0.37→0.38s) — 개선은 행동이 아니라 **판정 방식**에서 나왔다.
  - ✅ **구현·검증 완료(Rpi5 `d7e7f1c`)** — 🔒**홀드아웃 29회 개봉: 24/29(83%) → 29/29(100%)**, **ROI 오분류 0**, 오경보 0→1. 튜닝 세션 재현 92%. GUI 실기동에서 `손 접근:(링)`→`손 진입:(박스 안)` 확인.
  - 구조: **`roi_zones.py` 신설 = 판정 규칙 단일 출처**(런타임·측정도구가 같이 import — 복제 drift 구조적 차단) · `fsm.update_vision(roi, now, level=2)`(기본값으로 하위 호환) · `HAND_ROI_RING_PX=0`이면 **도넛 꺼짐 = 롤백 스위치**.
  - ⚠️ **구현 중 GUI가 기동 즉시 죽었다** — `_on_roi`의 `@pyqtSlot(str)`이 시그널 `(str,int)`과 어긋남. **import 검사로 못 잡는 런타임 불일치** → **`run_demo.sh` 실기동 확인은 빼지 말 것**.
  - ⚠️ 재현이 두 번 어긋났는데 **둘 다 측정 도구 문제**였다(`dwell_probe`의 conf 고정·갭메우기 미적용). 이후 도구는 **config에서 값을 읽는다**.
- 🔴 **정정(2026-07-22): "차단 타이밍이 늦다"는 성립하지 않는다**(§10.22-정정) — **체류 타이머는 오답 ROI에서만 돈다**(`fsm.py`). 그런데 잰 선행시간은 **정답 버튼을 누른** 경우라 애초에 경고 대상이 아니다. **재본 적 없는 것을 추론으로 단정했던 것.**
  - ⭐ **"감지 ≠ 차단"의 실제 구조 = 층 2개**: ①감지→**WARNING**(경고만, 전기 안 끊음) ②경고→**BLOCK**(인터락)인데 **BLOCK은 오답 버튼이 실제로 눌린 뒤에만 발생**한다(§9.3-5·6). → **현재 인터락은 누른 뒤에 걸린다.** 누르기 전 물리 차단을 원하면 **WARNING에서 인터락** 설계 변경 필요(미결).
  - 🔴 **핵심 명제가 아직 한 번도 실측 안 됐다** — 오답 왕복은 **GPIO 이벤트를 안 남기고**(누르지 않았으므로), **오답 버튼을 실제로 누른 사례가 데이터에 0건**이다. §10.21·§10.22가 잰 것은 전부 **정상 작업**이다.
- ✅ **⭐ 위반 실측 완료 + 체류 임계 0.5→0.3초 재설정(2026-07-22, 수치 정본 §10.23, §9.4 정본 갱신)**
  - **처음으로 "위반"을 찍었다** — 기대 `B1`인데 `B2`·`B3`·`B4`를 **실제로 누른** 2세션 **37건**. 지금까지의 모든 촬영은 정상 순서라 **체류 타이머(오답 ROI에서만 돎)를 평가할 수 없었다.**
  - 🔴 **구 정본 0.5초는 위반의 14%만 잡았다** → 0.3초에서 **57%**. 0.25 이하는 2%p만 늘고 헛경고만 늚 → **0.3이 적정점**. 오경보 2개 → 7개(57초).
  - ⭐ **사람은 천천히 누르려 해도 못 누른다** — 체류 median 0.39↔0.40초, **3회 모두 재현**. 0.4초가 물리적 한계 → 임계 결정이 한 세션 우연에 안 기댄다.
  - ✅ **실시간 첫 발동 확인** — GUI에서 `B2` 체류 → **1초 안에 ⚠ 경고 → WARNING → 인터락 WARN(ACK)**, 해제도 동작. 도넛·체류·FSM·인터락 **전 구간 연결**.
  - ⚠️ **도구 기본값이 config를 안 따라 4번 물렸다**(conf·ring·dwell·gap) → `dwell_probe` 전부 config에서 읽도록 수정. **런타임이 바뀌면 측정 도구도 같이 바뀌어야 한다.**
- ✅ **기능 검증 촬영 도구 `run_scenario.sh`(2026-07-22)** — GUI+화면녹화+웹캠(3인칭)녹화를 한 번에 띄워 **HOI·FSM이 의도대로 도는지 눈으로 확인**하는 용도. 🔴**발표용 시연 기록이 아니다**(HOI 미완성). 상세·함정 2개 = `Rpi5/CLAUDE.md`. 산출물 `Demo/scenario/`는 gitignore.
- **▶ 다음 = 🔴 손 검출률 (이제 임계보다 큰 병목)**
  - 비전이 손을 본 비율이 **80% / 59%** 로 흔들리고 **신중히 움직일 때 오히려 더 못 봤다.** 못 본 프레임은 판정 자체가 불가 → **천장이 57~65%**.
  - 못 잡은 위반 = **미검출 4건 + 체류 0.06~0.14초 3건**(어떤 임계로도 불가). 후자는 **경고로는 원리적으로 불가능** → 층2 필요.
  - **층2(WARNING에서 인터락)** — 현재 인터락은 **눌린 뒤에만** 걸린다. 누르기 전 물리 차단은 미설계. 도입 시 **오경보 1건마다 전기가 끊기므로 오경보율이 훨씬 중요해진다.**
- **▶ 그 다음(별개 축)**: 가림 문제(카메라 각도) · 카메라 전송 속도(NFR-1·JPEG/해상도/USB 전환) · A7(웹캠 polling) · 실콘솔 2차 test(§10.16 최종 판정) · 손 모델·소스 vendoring(현재 repo 밖 `~/hoi_probe/`) · 저조도·자연광

### (이전) 2026-07-20 시점
- 🔴 **test 1차(모조) 실행 — 옐로우등에서 실패, 중단(2026-07-20, 수치 정본 §10.18)**. 12세션 촬영(`fluorescent` 2 · `cleanroom-fluorescent` 5 · `cleanroom-yellow` 5).
  - ✅ **장소 일반화 통과** — 클린룸 형광등에서 5클래스 정상(§10.18). 장소·배경이 바뀌어도 견뎠다.
  - 🔴 **옐로우등에서 B4 붕괴(미검출 95.8%)·B2가 B1으로 오분류** — 5세션 전부 재현. **오분류는 미검출보다 위험**(FSM이 순서 위반을 통과시킴 §9). 중단 규칙 적용해 조건2(저조도)·조건3(자연광)은 **미실시**.
  - 원인 가설(미검증) = **조명 색에 의한 색 정보 붕괴**. §10.13 증강이 `hsv_h=0`로 **유색 조명 내성을 0으로** 두었던 사각지대(§10.18).
  - ⭐ **결정(2026-07-20): 옐로우등을 프로젝트 범위에서 제외, 운용 조건을 형광등(일반+클린룸)으로 고정**(§10.18). 재학습 사이클 시간이 없어 범위를 좁힌 것 — **해결 아님**. 🔴 **시연·운용은 형광등에서만 유효**하며, 유색 조명에선 안전 기능이 **조용히 무력화**된다(오분류라 정상으로 보임). `cleanroom-yellow` raw 1500장은 **증거로 보존**(삭제·학습 사용 금지).
- ✅ **정량 채점 준비 완료(2026-07-20, §10.19)** — `cleanroom-fluorescent` 1500장 → 중복제거 **고유 495장** → **test 130 / train 292 / valid 73** 배분 → Roboflow `console_v2-mjefr`에 split 명시 업로드(중복·오류 0). 원본 raw 보존. 배분 상세 = `Rpi5/Demo/dataset/cleanroom_eval_20260720/ALLOCATION.md`.
  - ⭐ **순서 결정: v2 채점 먼저 → 재학습(v3)은 그 결과 보고 판단**(§10.19). 채점이 좋으면 재학습 자체가 불필요할 수 있고, **v2는 클린룸 이미지를 한 장도 학습 안 해 누출-프리**라 점수가 v3보다 깨끗하다.
  - ⚠️ **test는 프리라벨 금지**(평가 대상 모델로 정답을 만들면 순환논리) — 전량 수동.
  - **▶ 다음 = ①test 130장 수동 라벨링(임계 경로) → ②채점 스크립트(`.hef` 대상, mAP·운용점 P/R·혼동행렬) → ③v2 채점 → 재학습 여부 결정.** ⏰ Roboflow 만료 **7/27**.
  - 인프라: 산출물 파일명에 조건·모델 기재(Rpi5 `caed0e9`) · 클릭 실행 런처+자동판정 `run_bench_test.sh`·`test/verdict.py`(`9fe57a3`) · ESP32 WiFi 1순위를 Eung Min으로(`17cd4bb`).

### (이전) 2026-07-19 시점
- ✅ **console_v2 학습(§10.14)·`.hef` 변환(§10.15) 완료(2026-07-16)** — 데스크톱 RTX 5060+WSL2. 파랑 B4 데이터셋 652장 재학습(소요·epoch 수치 §10.14) → DFC **level 1**·캘리브 **652**(v1은 level 0·64) → `console_v2.hef` **Rpi5 `Demo/models/` 배포 완료**. HAR 검증(uint8·NMS·5클래스) 통과. 절차 정본 = `dev/ai_model/console_v2_학습가이드.md`.
  - ✅ `config.HEF_MODEL_PATH` **v2로 전환됨**(2026-07-16) — `bench_detector.py`·`run_demo.sh`는 `--hef` 옵션이 없어 config가 유일한 모델 선택 수단이라, 실테스트하려면 전환이 필요했다. 🔴 **전환 = 검증 아님**(B4 미판정). `replay_raw.py`만 `--hef`로 런타임 지정 가능.
  - 🔴 **RTX 5060(Blackwell)은 DFC 사용 불가**(TF 2.18에 sm_120 커널 없음). 학습엔 정상 사용. **GPU 구할 이유 없음**(§10.7이 양자화를 B4 원인에서 배제).
- ✅ **⑤ replay 평가 완료(2026-07-17, 파이 — 수치 정본 §10.17)** — 5세션 전량. **판정 기준 = §10.16**(“v1 대비 상승”은 쓰지 않음). ①**하한선 통과**(B4 전 세션 검출, 롤백 사유 없음) ②⭐**저조도 붕괴 소멸** — v1에서 B3 완전 미검출·EMO 반감이던 저조도(`180016`)가 v2에선 **기준선보다 오히려 높음** → §10.13 "저조도 파랑 B4 생존 미측정" 플래그 **해소** ③누출-프리 세션(`173728`)도 정상 검출. ⚠️ 남는 신호: **B4 confidence 마진이 저조도에서 최저**(§10.17). ③`.hef` vs `.pt` 대조는 파이 불가 → 미실시.
- 🔴 **그래도 "B4 해결" 미선언** — ⑤ 통과는 **약한 증거**(학습에 쓴 장면·`replay_raw`엔 정답 없음). v1도 mAP·HAR은 통과했으나 실추론 0회였다(§10.5~§10.7).
- 🔴 **▶ test 세션 = 2단계로 분리(2026-07-17 정정, §10.13·§10.16)** — 실콘솔 제작이 길어져 "실콘솔로만"을 고수하면 그동안 **정직한 평가가 완전 공백**이라, **1차=모조**(다른 날·장소·조명, 저조도 포함 → 시간/조명/장소 일반화 + 누출-프리 정량 채점) → **2차=실콘솔**(포맥스판 재질 정반사·EMO 위치 → **최종 판정**). 🔴**파랑 스티커 동일 사양 필수**·**절반은 train 보충 / 절반은 손대지 않는 test로 미리 분리**·⏰라벨링은 Roboflow 체험 **7/27** 전.
- ⏸ **▶ 실테스트(실시간 영상) = 연기(2026-07-17)** — 사용자 환경상 **하드웨어(카메라·버튼·배선) 조작 불가**. HW 가능해지면 수행. **test 이미지와 무게중심이 반대**다: 실테스트가 답하는 **FPS·트래킹 연속성·파이프라인 안정성은 콘솔 종류와 무관**(병목은 ESP32 TCP 경로 §10.11, 콘솔은 피사체일 뿐)이라 **모조로 한 번이 본편**이고, 실콘솔에선 **시연 리허설 + AE×포맥스 재질 반응**만 가볍게 보면 된다(반대로 test 이미지는 **실콘솔이 본편**). 같이 볼 것: 🔴**FPS 재측정** · **A7**(USB 웹캠 상시 polling 개선, 보류 건) · 저조도 B4 마진(§10.17).
- 🔴 **HOI(손 검출) 보류 — 그런데 이것이 실테스트의 선행조건이다(2026-07-17 발견)** — `hailo_platform`은 Python 3.13 전용인데 **mediapipe가 aarch64/3.13 미지원**. §4 NFR-1·§10.6이 요구하는 건 **"console_v2·MediaPipe 포함 FPS 재측정"**인데 **MediaPipe가 안 돌면 그 측정 자체가 불가**하고, 손-버튼 접촉 판정도 못 돈다 → **지금 실테스트를 해도 반쪽**. 비전 기반 순서위반 감지의 한 축이라 **별도 해결 과제**. **▶ HW 불필요 후보(미확정 — 새 세션에서 검토)**: 별도 venv/프로세스+IPC / Hailo에 손 검출 모델 올리기 / 최신 mediapipe 지원 여부 재확인.

### 지난 이력·세부 함정 = 각 원본 참조 (2026-07-19 슬림화 — 복제 제거)
완료 이력은 시간순 저널 `docs/작업로그.md`에, 살아있는 규칙·함정(gotcha)은 각 정본/가이드에 있다(CLAUDE.md "복제 금지 → drift" 규칙 준수 — 아래는 포인터만):
- 라벨링 기준(진짜 버튼 정체·애매하면 안 그림·Modal)·B3↔EMO 위치 구분·색기반 자동라벨러 배제 = `Rpi5/Demo/labeling_guide.md`
- 데이터 파이프라인·Roboflow 함정 2개(`annotation_labelmap`·`annotation_overwrite` 캐시) = `Rpi5/Demo/dataset_pipeline.md`
- 증강 스펙(파랑=저조도 주력·정반사 보험·`hsv_h=0`·기하 ±15°) = `Rpi5/Demo/augmentation_plan.md`
- 학습 절차·DFC 변환·albumentations 2.x 인자 함정 = `dev/ai_model/console_v2_학습가이드.md`
- 측정·원인 분석(B4 미탐지·정반사·저조도 B3 사멸·파랑 스티커) = 통합문서 §10.5~§10.17
- 트랙 A 인터락 코드·전장 E2E·EMO 해제 결함 = §12 · Rpi5 `feature/fsm-interlock` · `dev/interlock/README.md`
- 목적 프레이밍 B·발표 역반영 = §1·§3 · 발표자료는 데스크톱 보관(repo 미반영)
