# CLAUDE.md — SOP 가디언 작업 지침

> 데스크톱·라즈베리파이 양쪽 Claude Code 가 자동 로드하는 **공유 두뇌**.
>
> 🧭 **이 문서는 매 세션 알아야 하는 것만 담는다.** 특정 파일·작업에만 필요한 규칙은 **`.claude/rules/`**(그 파일을 열면 자동으로 붙는다)와 **`.claude/skills/`**(그 작업을 시작하면 붙는다)에 있다. 어디에 무엇을 두는지는 **`.claude/rules/규칙배치.md`** 가 정본이다.
>
> 🔴 **작업 기록·현황·수치를 여기 쓰지 않는다.** 2026-06~07 에 「현재 상태」 절이 7줄 → 163줄로 자라 이 문서를 헌법이 아니라 저널로 만든 이력이 있다. 적고 싶어지면 그것은 작업로그 항목이다.

## 프로젝트
**1인칭 Vision AI × 웨어러블 기반 작업자 SOP 순서 위반 실시간 감지·차단.** **반도체 장비** 정비(PM)에서 순서 위반(휴먼 에러)을 버튼 누르기 직전에 사전 감지·차단한다. 콘솔은 **시연용 모조품** — 핵심은 비전 AI 순서위반 감지·차단이고 안전은 그 효과다(프레이밍 B). 시연 시나리오는 **PECVD 정비로 가정**한다. 상세 = 통합문서 §1·§3.

> 더 자세한 위치·환경 구성은 [`README.md`](README.md) 가 답한다.

### 명령

| 무엇 | 명령 |
| --- | --- |
| 데모 실행(파이) | `bash Rpi5/Demo/run_demo.sh` — 🔴 `Demo/` 안에서 돌아야 상대 import 가 맞는다(런처가 `cd` 한다) |
| 촬영 시나리오 | `bash Rpi5/Demo/run_scenario.sh` (함정 = `촬영` 스킬) |
| 벤치 | `bash Rpi5/Demo/run_bench_test.sh` (함정 = `측정도구` 스킬) |
| 자가 테스트 | `python3 Rpi5/Demo/selftest/test_fsm.py` — ⚠️ **일괄 러너가 없다.** 24개를 개별 실행한다 |
| SOP 문서 빌드 | `bash docs/SOP-빌드.sh` → `docs/SOP-PECVD정비-표준작업절차서.html`·`.pdf` |

### 구조

`docs/` 문서(정본 = `통합문서.md`) · `dev/` 개발(poc·interlock·fsm·ai_model) · `captures/`·`media/` 자산 · **`Rpi5/` = 파이 런타임 (별도 git repo)**.
형제 폴더 `../hanium-docs` = 공모전 제출문서 — **규칙 정본은 그쪽 `.claude/rules/제출문서.md`** 이고 그 폴더에서 세션을 열 때만 붙는다. 🔴 **참고 금지 대상은 그 저장소의 `CLAUDE.md` 하나다**(그쪽 규칙 정본이 그렇게 정했다). ⚠️ **다만 그 저장소는 `.claude/rules/제출문서.md`(09-21) 말고 전부 8월 말에 멈춰 있다** — 특히 **판정표의 근거 참조 21건이 옛 `§10.x`** 이고 지금 §10 은 다른 장(「범위와 향후 확장」)이라 **가리킬 곳이 없다.** 쓰기 전에 `§12` 로 대조한다. ⚠️ **`.docx`·`.pptx` 편집은 데스크톱 전용** — 파이는 md 변환본 `docs/개발보고서-v4-본문.md` 로 본문을 읽는다.

---

## 1. 작업 착수·진행

- **⭐ 세션 시작 = 원격 먼저.** 양 머신이 같은 repo 를 편집하므로 로컬이 최신이라 가정하지 않는다. SessionStart 훅이 자동 점검·보고하지만 **훅이 안 돌면 `git fetch` 로 직접** 확인한다.
- **⭐ 착수 = 자명/비자명 판단부터.** 자명한 것(문서 오타·날짜/해시 정정·로그 기입)은 바로 진행한다. **비자명(구현·기능·훅·코드 수정·다중파일·구조 변경·삭제·외부 전송)이면 `superpowers:brainstorming` 으로 시작한다.** 갈래·관문·병렬성 판단은 **`작업갈래` 스킬**이 정본이다.
  - 🔴 **spec 에서 멈추지 않는다** — 설계만 하고 끝난 것이 8건 있었다.
  - 🔴 **`subagent-driven-development` 는 기본이 아니다** — 파일·데이터가 겹치지 않는 병렬성이 실제로 있을 때만 쓴다.
  - 🔴 **멀티에이전트·자율 루프도 기본이 아니다.**
- **⭐ 수술적·최소 구현 + 자기검열.** 설계 승인 후 **구현은 요청 범위만·최소로** — 투기적 기능·성급한 추상화 금지, **멀쩡히 도는 코드는 리팩터링하지 않는다.** **근본원인 우선·임시방편 금지.** 비자명 변경은 제출 전 「더 우아한 방법 없나」 **1회** 자기검열. **틀어지면 멈추고 재계획.**
- **⭐ 산출물에 공들이기 전에 쓰임·상위 결정을 먼저 확인한다.**
- **⭐ 역할 분담** — 구상 = 사용자 / 계획·구현·검토 = Claude / **검증 결과·최종 구현 확인 = 공동**. 🔴 **Claude 단독으로 「완료」를 선언하지 않는다** — 완료에는 **관문 결과 출처**를 붙이고, 실HW 미검증은 `⏸` 로 남긴다.
- **⭐ 사용자 프롬프트도 틀릴 수 있다** — 수치·경로·상태·버전·날짜·인용·「완료」 전제는 **착수 전 대조**하고, 어긋나면 **정정안+근거를 말하고 진행**한다. 구상은 충돌할 때만 확인하고, 물리 세계의 사실은 **증거를 요청**한다.
- **⭐ 선택지를 골랐다 ≠ 방향이 맞다** — 선택지를 낼 때 **프로젝트 방향과의 정합**을 함께 밝히고, 문서에 없는 사용자 머릿속 아이디어를 끌어낸다.
- **⭐ 사용자 검토는 파일을 열게 하지 않는다** — 고정 형식 요약으로 대화에 보고한다(①절마다 한 줄 ②버린 대안 ③확인 질문). **요약을 빠뜨리면 검토 관문을 우회한 것이다.**
- **⭐ 실HW 이상은 되묻지 말고 증거로** — 로그·영상·입력 프레임을 남겨 **직접 대조·오프라인 재현**으로 규명한다.
- **⭐ CC 인프라 작업은 공식 문서 원문을 본다** — `code.claude.com/docs`. **기억·추측 금지.**

### 도구 — 이 순간이 오면 알아서 쓰고, 썼으면 한 줄로 밝힌다

| 이런 순간이면 | 이 도구 |
|---|---|
| 라이브러리 함수를 쓰거나 고치기 직전 | context7 |
| 「완료」라고 쓰기 직전 | `superpowers:verification-before-completion` |
| 기능 구현을 넘기기 전 | `superpowers:requesting-code-review` |
| 토큰·비용·세션 효율을 묻거나 잴 때 | session-report |
| 완료를 기계가 판정할 수 있는 반복 작업 | ralph-loop (승인 후 · **횟수 상한 필수**) |
| 공동 확인 직후 · 실패·중단을 합의한 직후 | `work-close` |

**바로 쓰는 것**(조회·보고)과 **승인받는 것**(설정 파일 생성·장시간 실행)을 가른다.
> 🔑 **이 표가 여기 있는 이유** — 보존 세션 실측에서 스킬 호출이 0회였다. **매 세션 읽히는 곳에 있어야 떠오른다.**

- **⭐ 「세션 마무리」·「작업 마무리」** — `/작업마무리` 또는 **"세션 마무리"·"작업 마무리"** 라고 하면 `work-close` 를 호출한다. 정의 = 하루의 끝이 아니라 **한 작업 단위가 논리적으로 끝난 시점**.
- **⭐ 「이어하기」** — `/이어하기` 또는 **"이어서 하자"** 라고 하면 `session-resume` 을 호출한다. 보고하고 **멈춘다.**

## 2. 문서·커밋

- **⭐ 통합문서 계열을 열면 `.claude/rules/정본편집.md` 가 자동으로 붙는다.** 단일정본 배치·기록 형식·표기는 거기가 정본이다.
- 🔴 **결정 안 된 옵션·과장 표현을 문서에 임의로 넣지 않는다.**
- 파이·데스크톱 양쪽에서 **같은 파일**을 편집한다 — 수정 후 `git commit && push` → 반대편에서 `git pull`.
- 폴더·경로를 바꾸면 **README 를 같이 갱신**한다.
- **커밋 구분** — 코드 수정 → 해당 repo(파이 코드는 `Rpi5`) / 문서·설계 → `sop-project`.
- 커밋 끝에 `Co-Authored-By: Claude <실제 작업 모델명> <noreply@anthropic.com>` — **고정값이 아니라 그 커밋을 만든 모델명**이다. ⚠️ **과거 커밋의 서명은 고치지 않는다.**
- push 는 **`work-close` 마지막에 한 번** — 그 밖에는 사용자 요청 시.

## 3. ⚠️ 함정 — 매 세션 필요한 것만

> 영역별 함정은 그 작업을 시작하면 붙는다 — 측정 인용 = `rules/수치인용.md` · 촬영 = `촬영` 스킬 · 측정 도구 = `측정도구` 스킬 · 데이터셋 = `데이터셋` 스킬 · 훅 = `rules/훅설계.md` · 제출문서 = `hanium-docs/CLAUDE.md`.

- 🔴 **「상한을 측정했다」와 「원인을 규명했다」를 혼동하지 말 것.** FPS 저하가 관측되자 원인을 TCP 전송으로 지목하고 *"하드웨어 한계·소프트웨어로 해결 불가"* 까지 단정해 **"카메라 교체 필요"** 라는 결론을 두 달간 유지했다. 실제 병목은 **ESP32 내부 프레임 버퍼**였고 펌웨어 3줄로 해결됐다. **자원 사용률을 먼저 확인한다** — 당시 대역폭은 1.2% 만 쓰이고 있었다.
- 🔴 **test 세트에 모델 프리라벨 금지** — 평가 대상 모델로 정답을 만들면 채점이 순환논리가 된다.
- 🔴 **시뮬레이터 산출 수치는 시뮬레이션 정책 4종을 병기**한다 — 실제 GUI 운용과 다르다.
- ⚠️ **이 파이에 `sqlite3`·`jq` CLI 가 없다** — DB·JSON 질의는 `python3 -c` 로.
- ⚠️ **`git add -A`·`--all`·`.` 는 권한 deny 가 막는다**(`add -u`·`commit -a` 는 안 막힌다). **경로를 지정해 add 한다.**
- 🔴 **하드웨어를 빌려 쓰면 바이트 백업·검증 뒤에만 손대고 끝나면 되돌린다.** ESP32 는 **시리얼번호로 식별**한다(`ttyACM` 번호는 꽂는 순서로 뒤바뀌고 **MAC 마지막 바이트만 다르다**) — **잘못 구우면 다른 작업 세션의 펌웨어가 통째로 날아간다.** 절차 = `~/lab/esp32-link/RESTORE.md`.
- 🔴 **GitHub 제출 저장소에 학교명·이름·소속을 쓰지 않는다** — 본문뿐 아니라 **git 이력·계정명까지** 범위다.
