# CLAUDE.md — SOP 가디언 작업 지침

> 데스크톱·라즈베리파이 양쪽 Claude Code가 자동 로드하는 **공유 두뇌**.
>
> 🧭 **정보 위치는 [`README.md`](README.md)가 답한다** — 어느 문서·폴더에 무엇이 있는지, 문서별 역할·경계, 폴더 지도, 실행법, 환경 구성. **찾을 것이 있으면 먼저 README로 간다.**
>
> **이 문서는 「어떻게 일하나」만 담는다** — 규칙·절차·금지·함정. 🔴 **사양·측정 수치·작업 기록·현황을 여기 쓰지 않는다.** 2026-06-11~07-30에 「현재 상태」 섹션이 7줄 → 163줄(문서의 67%)로 자라 이 문서를 헌법이 아니라 저널로 만든 이력이 있다(2026-07-30에 걷어냄). 적고 싶어지면 그것은 작업로그 항목이다. **이 문서는 규칙이 바뀌었을 때만 고친다.**

## 프로젝트
**1인칭 Vision AI × 웨어러블 기반 작업자 SOP 순서 위반 실시간 감지·차단.** PECVD 정비(PM)에서 순서 위반(휴먼 에러)을 버튼 누르기 직전에 사전 감지·차단한다. 콘솔은 **시연용 모조품** — 핵심은 비전 AI 순서위반 감지·차단이고 안전은 그 효과다(프레이밍 B). 상세 = 통합문서 §1·§3.

---

## 1. 답변 방식 — 사용자 이해 우선

> 답변 수준이 높아 "이해하기 쉽게 다시"를 되묻는 일이 잦았다. 원인은 **지식의 저주**(알고 나면 모르던 때를 떠올리지 못하는 편향)다.

- ① **결론을 맨 앞에 2~3줄로** 축약한다. 근거·배경은 그 뒤에.
- ② **새 용어는 반드시 풀어 쓴다** — 영어 용어는 우리말 뜻 병기 + 한 줄 설명.
- ③ **이어하기는 본론 전에 "지난번에 뭘 했고 지금 어디"를 3~4줄 요약**부터. 로그는 기록엔 정확하나 다시 떠올리기엔 어렵다.
- ④ **어려운 개념은 비유 → 비교 → 실제 사례 순**으로 푼다. 일상 사물에 빗대고, 기존/변경을 나란히 놓고, 이 프로젝트의 실제 작업으로 바꿔 보인다. **원문·정의를 그대로 붙여넣지 않는다.**
- ⑤ **결정 요청은 한 번에 하나** — 추천안을 함께 제시하고 나머지는 예고만. 얽혀서 따로 못 정하는 것은 **조합(A안/B안)으로 묶어** 하나의 질문으로.

## 2. 작업 착수·진행

- **⭐ 세션 시작 = 원격 먼저.** 양 머신이 같은 repo를 편집하므로 로컬이 최신이라 가정하지 않는다. `git fetch`로 ahead/behind를 확인하고 필요하면 `git pull`. SessionStart 훅이 자동 점검·보고하지만 **훅이 안 돌면 수동으로** 한다.
- **⭐ 착수 = 자명/비자명 판단부터.** 자명한 것(문서 오타·날짜/해시 정정·로그 기입)은 바로 진행한다. **비자명(구현·기능·훅·코드 수정·다중파일·구조 변경·삭제·외부 전송)이면 `superpowers:brainstorming`으로 시작하고, 이 프로젝트의 표준 갈래·제약을 CC작업문서 §2에서 먼저 읽는다**(경로는 README).
  - 🔴 **spec에서 멈추지 않는다** — 2026-07-26 이전 설계 8건이 전부 거기서 멈췄다.
  - 🔴 **`subagent-driven-development`는 기본이 아니다** — 파일·데이터가 겹치지 않는 병렬성이 실제로 있을 때만 쓴다.
- **⭐ 수술적·최소 구현 + 자기검열.** 설계 승인 후 **구현은 요청 범위만·최소로** — 투기적 기능·성급한 추상화 금지, **멀쩡히 도는 코드는 리팩터링하지 않는다**, 기존 스타일에 맞춘다(생각은 충분히·만드는 건 최소). **근본원인 우선·임시방편 금지.** 비자명 변경은 제출 전 "더 우아한 방법 없나" **1회** 자기검열(자명한 건 스킵 — 과설계 금지). **틀어지면 멈추고 재계획.**
- **⭐ 「세션 마무리」** — 사용자가 `/세션마무리`를 치거나 **"세션 마무리"·"세션 정리"** 라고 하면 `session-wrap` 스킬을 호출한다. 정의 = 하루의 끝이 아니라 **한 작업 단위(A작업)가 논리적으로 끝난 시점**(하루에 여러 번 가능). 절차 정본은 그 스킬이다.

## 3. 문서 편집 규율

> 문서 drift 방어는 훅이 아니라 **아래 단일정본 규칙 + 편집 규율이 전담**한다. 같은 목표의 훅이 세 번 실패했다(경위 = CC작업로그).

- **⭐ 측정·사양 수치 = 통합문서 단일 정본.** mAP·FPS·해상도 등은 통합문서 §10(설계값은 해당 §)에만 두고, 다른 문서는 **"§X 참조" 포인터만** 둔다(복제 금지). 수치가 바뀌면 통합문서만 고친다.
- **⭐ 통합문서 읽는 법 — 통독 금지, `grep`으로 필요한 절만 범위 읽기.** 2,800여 줄이라 Read 도구 한계로 3회 이상 끊긴다. 통독은 컨텍스트 낭비다.
  - ⭐ **현행 측정값은 §10 머리의 `[CURRENT]` 표에서 먼저 찾는다.** 개별 §10.x를 직접 뒤지면 같은 지표의 옛 값·폐기값을 집을 수 있다.
  - ⭐ **§10 절을 인용하기 전에 그 절의 유효성 마커(제목 다음 줄)를 먼저 본다** — `✅ CURRENT-AS-OF` / `⚠️ PARTIALLY-SUPERSEDED-BY` / `⛔ SUPERSEDED-BY`. **§10은 시간순 저널이라 폐기된 결론이 현행처럼 읽히는 것이 가장 큰 함정이다.**
  - 🔴 **인용 시 조건 열을 반드시 함께 옮긴다.** 조건 없는 수치 인용 금지.
  - 🔴 **새 측정을 §10.x에 기록하면 `[CURRENT]` 표도 같이 고친다** — 표를 갱신하지 않은 측정은 확정값이 아니다. **§10에 새 절을 추가하면 유효성 마커도 함께 단다.**
- **통합문서 사본을 만들지 않는다**(병합 충돌). 파이·데스크톱 양쪽에서 같은 파일을 편집하고, 수정 후 `git commit && push` → 반대편에서 `git pull`.
- 폴더·경로를 바꾸면 **README를 같이 갱신**한다(지도가 README에 있다).
- 🔴 **작업 기록·현황·수치를 이 문서에 쓰지 않는다.** 갈 곳은 README의 「문서별 역할·경계」가 답한다.

## 4. 커밋

- **커밋 구분**: 코드 수정 → 해당 repo(파이 코드는 `Rpi5`) / 통합문서·설계 → `sop-project`.
- 커밋 끝에 `Co-Authored-By: Claude <실제 작업 모델명> <noreply@anthropic.com>` — **고정값이 아니라 그 커밋을 만든 모델명**이다. 🆕 **2026-07-26부터 주 작업 모델 = `Opus 5`**(그 이전은 `Opus 4.8`). ⚠️ **과거 커밋의 서명은 고치지 않는다** — 그때 그 모델이 만든 것이 사실이고, 모델별 추적이 이 규칙의 목적이다.
- push는 사용자 요청 시.

## 5. ⚠️ 함정·금지 (재발 방지 — 실제로 물린 것만)

> 전부 **한 번 이상 실제로 물렸던 것**이다. 수치는 담지 않는다(→ 통합문서 §10 `[CURRENT]` 표). 경위·이력은 작업로그. **영역별 세부 함정의 원본 위치는 README**에 있다.

### 측정·인용
- 🔴 **프레임 단위 손 검출률을 성능 지표로 쓰지 말 것** — 손이 없는 프레임이 분모에 섞여 값이 오염된다. 의미 있는 값은 **눌림 근처** 기준이다.
- 🔴 **지표 이름을 혼용하지 말 것** — 체류 구간 수 / 눌림 프레임 적중 / 선행시간 / 사전 감지율 / 개입 감지율은 **서로 다른 것을 잰다.** 인용 시 어느 지표인지 반드시 명시.
- 🔴 **사후 층화(post-hoc stratification) 값을 성능으로 쓰지 말 것** — 데이터를 보고 구간을 좁혀 얻은 값은 낙관 편향이 있다. 새 촬영으로 확인해야 성능 주장이 된다.
- 🔴 **단일 세션 값 금지 — 통합값으로 인용한다.** 같은 구도를 반복해도 세션 간 편차가 크다.
- ⚠️ **임계 효과는 임계를 실제로 바꿔가며 파이프라인 그대로 측정한다** — 원점수를 보려고 임계를 0.01로 열면 **가중 NMS가 저점수 앵커를 섞어 점수를 끌어내려** 반대 결론이 난다.
- ⚠️ **`cam_probe` 10프레임 표본은 게이트로 쓰지 말 것** — 분산이 커 세션 평균과 어긋난다.
- 🔴 **"상한을 측정했다"와 "원인을 규명했다"를 혼동하지 말 것.** FPS ~13이 관측되자 **원인을 TCP 전송으로 지목**하고 *"하드웨어 한계·소프트웨어로 해결 불가"*까지 단정해 **"카메라 교체 필요"**라는 결론을 두 달간 유지했다. 실제 병목은 **ESP32 내부 프레임 버퍼**였고 펌웨어 3줄로 2.4배가 됐다(§10.34). **자원 사용률을 먼저 확인한다** — 당시 대역폭은 1.2%만 쓰이고 있었다.

### 도구·구현
- 🔴 **런타임이 바뀌면 측정 도구도 같이 바꾼다** — 도구 기본값이 config를 안 따라 **4번 물렸다**(conf·ring·dwell·gap). 이후 도구는 **config에서 읽는다**.
- 🔴 **`run_demo.sh` 실기동 확인을 검증에서 빼지 말 것** — `@pyqtSlot` 시그니처 불일치처럼 **import 검사로 못 잡는 런타임 오류**가 있다. 실제로 GUI가 기동 즉시 죽은 적이 있다.
- ⚠️ **GPIO 로그가 정본이다** — 촬영 주기·횟수에 대한 기억·기대값이 틀린 적이 있다.
- ⚠️ **`sqlite3` CLI가 이 파이에 없다** — DB 질의는 `python3 -c`로.
- ⚠️ **촬영 지시는 "주기를 끝까지 반복"으로** — "횟수 지정"은 혼선을 낳아 재촬영을 부른다.

### 과장 금지 (조건 없는 단정 금지)
- 🔴 **"console_v2 검증 완료"로 쓰지 말 것** — 정확히는 **"클린룸 형광등·모조 콘솔 조건에서"**. 실콘솔 2차 test가 §10.16의 최종 판정이며 미실시.
- 🔴 **감지율을 성능 상수로 쓰지 말 것** — **착용 위치에 따라 크게 흔들린다.** 구도 조건을 함께 밝힌다.
- ⚠️ **NFR-1은 충족됐으나 "안정 처리"까지 단정하지 말 것** — 23.96fps(2026-08-10, §10.34)는 **손 없는 정지 장면·단일 조건**이고 **최저 5.1fps 구간**이 남아 있다. 조건을 병기한다.
- 🔴 **E2E 오경보 인용 시 시뮬레이션 정책 4종을 병기**한다(§10.31) — 실제 GUI 운용과 다르다.
- 🔴 **test 세트에 모델 프리라벨 금지** — 평가 대상 모델로 정답을 만들면 채점이 순환논리가 된다. 전량 수동.

### 문서 편집 (Word·PPT) — 2026-08-21 신설
- ⭐ **공모전 제출문서(개발보고서·제작설계서·시연영상) 작업은 이 저장소가 아니다** — 형제 폴더 **`../hanium-docs`**(데스크톱 전용, 파이 미동기화)에서 한다. **Word·PPT 편집 규율·3단 관문·그림 규격의 정본은 그쪽 `CLAUDE.md` §3~§4.**
- 🔴 **파이에는 `../hanium-docs` 가 없다 — 그것이 정상이다.** 원격이 없어 `pull` 로 오지 않는다. **없다고 만들지 말고, 그쪽 규율 정본을 찾아 헤매지 말 것.** 파이에서 할 수 있는 일이 아니다(문서 작업은 전부 데스크톱).
- 🔴 **그쪽에서 이 저장소를 고치지 않는다** — 문서 작업은 통합문서 §10 `[CURRENT]` 표를 **읽기만** 한다. 사실이 바뀌면 여기서 먼저 고친다.
- ⚠️ **`git add -A` 금지** — 백업본 docx 29MB를 통째로 커밋한 적이 있다(2026-08-21, 푸시 전 회수). 경로를 지정해 add 한다.

### 운용 지식 (직관과 어긋나는 것)
- ⚠️ **카메라를 낮게 두면 콘솔이 화면 위쪽에** 잡힌다(높게 두면 아래쪽). 직관과 반대다.
- ⚠️ **놓친 눌림은 세 종류다** — ①손 자체 미검출(구도) ②손끝이 ROI 밖(구도·판정) ③**구역 안에 있었는데 체류 임계 미달(시간)**. **대응이 다르므로 한 덩어리로 세지 말 것.**
  - 🔴 **③이 지배 원인이다** — 실측(§10.47)에서 ①+②는 46건 중 6건(13%)뿐이고 **③이 65%**였다. ①만 떠올리고 「구도 문제」로 결론내는 것이 이 함정의 실제 형태다.
