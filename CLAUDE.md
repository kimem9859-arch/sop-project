# CLAUDE.md — SOP 가디언 작업 지침

> 데스크톱·라즈베리파이 양쪽 Claude Code가 자동 로드하는 **공유 두뇌**.
> 설계 사양은 여기서 정의하지 않는다 — 정본은 `docs/통합문서.md`.
> Claude Code 작업은 프로젝트 작업과 분리 기록: `docs/claude-code-작업로그.md`(CC 시간순 로그) + `docs/claude-code-작업문서.md`(CC 설명서·원칙). 프로젝트 작업 로그는 `docs/작업로그.md`.

## 프로젝트
**1인칭 Vision AI × 웨어러블 기반 작업자 SOP 순서 위반 실시간 감지·차단 시스템.** PECVD 정비(PM)에서 작업자의 순서 위반(휴먼 에러)을 버튼 누르기 직전에 사전 감지·차단. 콘솔은 **시연용 모조품**(실제 PECVD 기능 없음·타워램프 시각화는 부차적) — 핵심은 **비전 AI 순서위반 감지·차단**. (안전은 효과 → 프레이밍 B)

## 정본 (Source of Truth)
- **설계·사양·배경·SOP·하드웨어·KPI = `docs/통합문서.md`** (단일 정본).
  - ⭐ **읽는 법 = 통독 금지, `grep`으로 필요한 절을 찾아 범위 읽기.** 전체 2,600여 줄 ≈ 60~70k 토큰이라 Read 도구 한계(25k)로 **3회 이상 끊긴다.** 통독은 컨텍스트 낭비다.
  - ⭐ **현행 측정값은 §10 머리의 `[CURRENT]` 표에서 먼저 찾는다** — `grep "\[CURRENT\]"` 한 번이면 확정값 전체가, `grep "\[CURRENT\] <지표명>"` 이면 값·조건·근거가 **한 줄로** 나온다. 개별 §10.x 절을 직접 뒤지면 같은 지표의 옛 값·폐기값을 집을 수 있다(예: "사전 감지율"은 30히트에 서로 다른 값이 12개).
  - 🔴 **인용 시 조건 열을 반드시 함께 옮긴다.** 조건 없는 수치 인용 금지.
  - 🔴 **새 측정을 §10.x에 기록하면 `[CURRENT]` 표도 같이 고친다** — 표를 갱신하지 않은 측정은 확정값이 아니다.
  - ⭐ **§10 절을 인용하기 전에 그 절의 유효성 마커(제목 바로 다음 줄)를 먼저 본다** — `✅ CURRENT-AS-OF` / `⚠️ PARTIALLY-SUPERSEDED-BY §X` / `⛔ SUPERSEDED-BY §X`. **§10은 시간순 저널이라 폐기된 결론이 현행처럼 읽히는 것이 가장 큰 함정이다.** `grep "SUPERSEDED"` 로 대체된 절을 한 번에 볼 수 있다. 🔴 **§10에 새 절을 추가하면 마커도 함께 단다.**
- 폴더 지도·실행법 = `README.md`. 트랙별 상세 = `dev/*/README.md`.

## 🚧 문서간 경계 (재유입 방지 — 2026-07-30 신설)

> **이 문서는 「어떻게 일하나」(규칙·절차·금지·함정)만 담는다. 작업 기록은 절대 여기 쓰지 않는다.**

| 쓸 내용 | 갈 곳 |
| --- | --- |
| 규칙·절차·금지사항·**함정** | **CLAUDE.md**(이 문서) |
| 무엇을 했나 — 완료·경위·판정 | `docs/작업로그.md` |
| CC 인프라 작업(훅·스킬·도구) | `docs/claude-code-작업로그.md` |
| 측정 수치 | `docs/통합문서.md` §10 머리 `[CURRENT]` 표 |
| 설계·사양·SOP·하드웨어 | `docs/통합문서.md` |
| **지금 어디까지 · 다음 할 일** | `docs/작업로그.md`의 `- ⏸`/`- ▶` → **SessionStart 훅이 배너로 표시** |

🔴 **「현재 상태」·「(이전) …시점」 같은 섹션을 다시 만들지 말 것.** 2026-06-11~07-30에 그 섹션이 **7줄 → 163줄(문서의 67%)** 로 자라 이 문서를 헌법이 아니라 저널로 만들었다. 원인은 **「스냅샷」의 경계가 없어 덜어내는 규칙도 없었던 것**이다(2026-07-01 작업로그 신설 때 "역할분리 공존"으로만 정하고 경계를 정의하지 않았다). 2026-07-30에 155줄을 걷어냈고, 그 내용은 전부 작업로그·통합문서에 더 상세히 있다.

🔴 **세션 성과를 여기 적고 싶어지면 그것은 작업로그 항목이다.** 이 문서는 **규칙이 바뀌었을 때만** 고친다.

## 두 환경의 역할 (런타임 분리 / 지식 공유)
| | 🖥️ 데스크톱 | 🍓 라즈베리파이 (Pi#1 = 주 작업기) | 🍓 sop-pi-2 |
|---|---|---|---|
| 역할 | 설계·통합문서 편집 · PoC(x86) · 발표 제작 | Rpi5 코드 실행·실HW 테스트·Hailo · 통합문서 편집 | Pi#1 복제본(예비/병행). `ssh pi2`(공유기 192.168.1.9) |
| 클론 | `sop-project` | `sop-project` + 그 안 `Rpi5/`(엄브렐러) | Pi#1과 동일 구성 |

## ⭐ 통합문서 = 단일 정본, 양방향 동기화
파이(개발·테스트 중 수정)와 데스크톱(발표 제작)에서 둘 다 편집·사용. **사본 금지**(병합 충돌).
- 파이: 수정 → `git commit && push` (sop-project)
- 데스크톱: `git pull` → 최신본으로 발표 제작

## Rpi5 = 별도 repo (파이 런타임 코드)
- `github.com/kimem9859-arch/Rpi5.git`, 작업 브랜치 **`feature/fsm-interlock`** (최신 본류). 구 브랜치 정리 완료.
- 파이 셋업(엄브렐러 재현):
  ```bash
  git clone https://github.com/kimem9859-arch/sop-project.git ~/sop-project
  cd ~/sop-project
  git clone -b feature/fsm-interlock https://github.com/kimem9859-arch/Rpi5.git Rpi5
  cd ~/sop-project && claude        # 통합문서 + 코드 한자리
  ```
- `Rpi5/`는 sop-project에서 gitignore(별도 repo). 런타임 맥락은 `Rpi5/CLAUDE.md`.

## 모델 (네이밍 규약 정본 §7.1, 3단계)
- `person_v1.pt` 초기 검증(완료) → **`console_v1`** 버튼검출 **1차 검증 테스트**(5클래스 B1~B4+EMO, .hef 빌드완료·파이 실추론·벤치마크 완료 — B1·B2·B3·EMO 검출 동작, **B4 미탐지** 확인) → **`console_v2` 최종 — 학습·`.hef` 변환·파이 배포 완료(2026-07-16, §10.14·§10.15). 🔴 B4 해결 여부는 미판정**(판정 기준 = §10.16, 최종 근거 = 미촬영 test 세션).
- 버튼 클래스맵·Pi 추론 규격(uint8 640·HailoRT NMS·4.x): `dev/ai_model/README.md`.
- ※ **측정 수치 정본 = 통합문서 §10**(학습 mAP·실추론 FPS 등). 여기·dev/README는 상태·결정만, 수치는 §10 참조.

## PoC 실행 (Step1, 데스크톱)
루트에서: `./dev/poc/run.sh poc_data/clips/ --rois dev/poc/rois.json --out dev/poc/out`
- 머신별 차이는 `dev/poc/SETUP.md`. 환경자산 `.poc_venv`·`poc_data`·`.syslibs`는 gitignore → 머신마다 재구축.

## 작업·커밋 규칙
- **⭐ 세션 시작 = 원격 먼저**: 양 머신이 같은 repo를 편집하므로, 로컬이 최신이라 가정 금지. 작업 전 `git fetch`로 ahead/behind 확인 후 필요시 `git pull`. (SessionStart 훅 `.claude/hooks/session-sync-check.sh`가 sop-project·Rpi5 양쪽을 자동 점검·보고 — 훅이 안 돌면 수동으로.)
- **자동 점검 인프라(훅)**: SessionStart ① `session-worklog-brief.sh`(**세션 시작 표 배너** = 원격 동기화 + ⏸중단·▶다음 이어하기 + 현재 session_id 주입을 한 표로 표시; 내부에서 `session-sync-check.sh`를 **순차 호출**[SessionStart 병렬실행 경쟁 회피]하고 `_banner.py`로 CJK정렬 표+systemMessage 렌더) ② `session-prune-stubs.sh`(resume 목록의 tiny/원격제어 스텁 세션을 `.trash`로 정리, systemMessage 배너). **①② 뿐**(`.claude/settings.json` 등록) — **클론 후 별도 설정 불필요**(`git pull`이면 적용). 권한 허용목록도 `.claude/settings.json`. **전부 repo 공유**(양 머신 동일).
  - 🗑️ **2026-07-17 폐기 2건**(실증 검증 후 — 경위·수치는 `docs/claude-code-작업로그.md`): ③ SessionEnd `session-worklog-commit.sh`(세션→커밋 자동매핑 — 산출 45건 중 고유정보 6건뿐·나머지는 사람 기록과 중복이거나 자기참조. **"세션마무리 누락을 구제한 사례 0건"** → 역할을 「세션 마무리」가 이미 전부 수행) / ④ pre-commit `.githooks/pre-commit`(문서 정합성 LLM 점검 — **12회 실행·적발 0건**, 매 호출이 세션 스텁을 흘려 ②가 뒤처리하는 닫힌 루프까지 형성). ④는 구 `doc-consistency-check.sh`(2026-07-01 제거)에 이은 **같은 목표의 두 번째 실패** → **문서 drift 방어는 훅이 아니라 아래 단일정본 규칙 + 편집 규율이 전담**(원래도 1차 방어는 이것이었고, 훅은 값을 못 한 안전망이었음).
- **⭐ "세션 마무리" 의식** (슬래시 `/세션마무리` 또는 스킬 `session-wrap`으로 절차화): 사용자가 **`/세션마무리`** 를 치거나 **"세션 마무리"**(또는 "세션 정리")라고 말하면 이번 세션 작업을 4분류(✅완료/⏸중단/▶다음/🔗커밋)로 정리해 **현재 session_id 태그와 함께** 기록한다(브리핑 훅 ID 사용). ※**정의 = 하루의 끝·작업 종료가 아니라 "한 세션/작업 단위(A작업)가 논리적으로 끝난 시점"** — 하루에 여러 번 가능(A작업 끝 → 마무리, B작업 시작). **프로젝트 작업 → `docs/작업로그.md`, Claude Code 작업(훅·도구·인프라) → `docs/claude-code-작업로그.md`** 로 나눠 적는다(둘 다면 양쪽에). (SessionEnd 자동 커밋매핑은 성격 구분 못 해 `작업로그.md` 하단에 전체 무분류로 쌓임 — 사람 기록만 분리.) 로그=시간순 저널이라 CLAUDE.md「현재 상태」(스냅샷)와 역할 분리·공존. **마무리 시 이전 세션의 미기록 커밋을 소급 점검**해 있으면 사용자 확인 후 별도 소급 블록으로 기록(2026-07-18 신설 — SessionEnd 자동매핑 폐기 후의 유일한 누락 안전망). 상세 절차는 `.claude/skills/session-wrap/SKILL.md`.
- 사양은 통합문서에만(README/CLAUDE 복붙 금지 → drift). 폴더·경로 바꾸면 README·CLAUDE 같이 갱신.
- **⭐ 측정·사양 수치 = 통합문서 단일 정본**: mAP·FPS·해상도 등 측정값은 통합문서 §10(설계값은 해당 §)에만 두고, 운영문서(CLAUDE·기준문서 제외 dev/README)는 **"§X 참조" 포인터만**(복제 금지). 수치가 바뀌면 통합문서만 고친다. (기준문서는 나침반이라 요약 보유 허용)
- **⭐ 계획·구현 절차 = superpowers 스킬이 정본**(2026-07-26 전환 → **2026-07-28 실행 갈래 조정**, 둘 다 사용자 지시): 비자명 작업(구현·기능·훅·코드 수정·다중파일·구조 변경·삭제·외부 전송)은 `superpowers:brainstorming`으로 시작한다. 🔴 **spec에서 멈추지 않는다** — 2026-07-26 이전 설계 8건이 전부 거기서 멈춰 `writing-plans`를 한 번도 호출하지 않았고, 그 결과 실행·검증 기록이 spec으로 흘러들었다. **절차·문서 형식·테스트 방식은 스킬 원문에만 두고 여기 옮겨 적지 않는다**(복제 → drift).
  - **⭐ 이 프로젝트의 표준 갈래 (2026-07-28 확정)** — `brainstorming` → `writing-plans` → **`executing-plans`(인라인)** → **최종 리뷰 1회**(`requesting-code-review`). 매 단계 전 `verification-before-completion`(주장 전에 검증 명령을 돌려 출력으로 확인).
    - 🔴 **`subagent-driven-development`는 기본이 아니다** — `writing-plans`가 제시하는 **두 실행 갈래 중 1번**일 뿐이고, 2번(`executing-plans`)도 정식 갈래다. **파일·데이터가 겹치지 않는 병렬성이 실제로 있을 때만** SDD를 쓴다.
    - 근거(2026-07-27~28 창 판정 실측): 설계+계획은 **14분·출력의 약 5%**였는데 SDD 실행이 **18시간 30분·에이전트 21개·API 한도 2회 소진**. 그중 **리뷰가 52%**를 먹고 수확은 Critical 1·Important 2. 반면 **작업을 구한 관문 3개(중단 규칙·홀드아웃·검증 관문)는 전부 계획서 문장**이었다. → **자를 곳은 계획이 아니라 실행 의식.** 상세 = `docs/claude-code-작업로그.md`
    - 🔴 **계획서는 짧게** — 창 판정 계획서 1,325줄은 과했다(코드 전문 복붙). 그중 하나가 **수학적으로 불가능한 테스트**여서 구현자가 16만 토큰을 태워 발견했다. **관문·중단 규칙·홀드아웃·태스크 경계 중심 150~250줄**이면 된다.
    - ⭐ **관문은 계획서에 반드시 박는다** — 중단 규칙(어떤 조합으로도 목표 미달이면 폐기) · 🔒홀드아웃 분리(확정 후 1회만 개봉) · 검증 관문(통과 못 하면 어떤 수치도 안 씀). 이 셋이 이번에 전부 발동했다.
  - **스킬이 알 수 없는 이 프로젝트 단서 3개**:
    - **자명한 것**(문서 오타·날짜/해시 정정·작업로그 기입)은 스킬 없이 바로 진행.
    - **spec·plan에 확정 측정·사양 수치 금지** — §10 포인터만(위 단일정본 규칙 유지).
    - **워크트리 미사용 · subagent 병렬 제한** — 코드가 별도 repo(`Rpi5`)라 `sop-project` 워크트리에 안 따라오고, Hailo `VDevice`·카메라·GPIO가 각 1개라 실HW 검증은 직렬만 된다. ⚠️ **병렬을 막는 건 하드웨어만이 아니다** — 창 판정에서 태스크들이 **DB·모듈 의존으로 사슬**을 이뤄 7태스크가 5웨이브로만 갈렸고, 그렇게 앞당긴 것이 **관문보다 먼저 착지해 Critical을 만들었다**.
- **⭐ 수술적·최소 구현 + 자기검열**(Karpathy·Boris 교훈 선별 흡수 — 메뉴, 플러그인 아님): 설계 승인 후 **구현은 요청 범위만·최소로** — 투기적 기능·성급한 추상화 금지, **멀쩡히 도는 코드는 리팩터링하지 않는다**, 기존 스타일에 맞춘다(생각은 충분히·만드는 건 최소). **근본원인 우선·임시방편 금지**(systematic-debugging). **비자명 변경은 제출 전 "더 우아한 방법 없나" 1회 자기검열**(자명한 건 스킵 — 과설계 금지). **틀어지면 멈추고 재계획.**
  - ⛔ **채택 안 함**: `tasks/todo.md`·`lessons.md` 신설(memory·작업로그·Task 도구가 상위).
- **⭐ 답변 방식 = 사용자 이해 우선**(2026-07-26 신설, 사용자 지시 — 답변 수준이 높아 "이해하기 쉽게 다시"를 되묻는 일이 잦았다. 원인 = **지식의 저주**[알고 나면 모르던 때를 떠올리지 못하는 편향]):
  - ① **결론을 맨 앞에 2~3줄로** — 핵심만 축약. 근거·배경은 그 뒤.
  - ② **새 용어는 반드시 풀어 쓴다** — 영어 용어는 우리말 뜻 병기 + 한 줄 설명.
  - ③ **이어하기 = 본론 전에 "지난번에 뭘 했고 지금 어디"를 3~4줄 요약**부터(로그는 기록엔 정확하나 다시 읽고 떠올리기엔 어렵다).
  - ④ **어려운 개념은 비유 → 비교 → 실제 사례 순으로 푼다** — 일상 사물에 빗대고(비유), 기존/변경을 나란히 놓고(비교), 이 프로젝트의 실제 작업으로 바꿔 보인다(사례). **원문·정의를 그대로 붙여넣지 않는다.**
  - ⑤ **결정 요청은 한 번에 하나** — 추천안을 함께 제시하고, 나머지는 예고만. 얽혀서 따로 못 정하는 것은 **조합(A안/B안)으로 묶어** 하나의 질문으로.
- **커밋 구분**: 코드 수정 → 해당 repo(파이 코드는 `Rpi5`로) / 통합문서·설계 → `sop-project`로.
- 커밋 끝에 `Co-Authored-By: Claude <실제 작업 모델명> <noreply@anthropic.com>`(예: Opus 5, Opus 4.8, Fable 5 — **고정값 아님, 그 커밋을 만든 모델명**). push는 사용자 요청 시. (2026-07-14 Fable 점검: 모델별 작업 추적 위해 규칙화)
  - 🆕 **2026-07-26부터 주 작업 모델 = `Opus 5`** (그 이전은 `Opus 4.8`). 사용자 결정으로 고정 — 이후 커밋 서명은 `Claude Opus 5`. ⚠️ **과거 커밋의 서명은 고치지 않는다**(그때 그 모델이 만든 것이 사실이고, 모델별 추적이 이 규칙의 목적이다).

## ⚠️ 함정·금지 (재발 방지 — 실제로 물린 것만)

> 전부 **한 번 이상 실제로 물렸던 것**이다. 수치는 담지 않는다(→ 통합문서 §10 `[CURRENT]` 표). 경위·이력은 `docs/작업로그.md`.

### 측정·인용
- 🔴 **프레임 단위 손 검출률을 성능 지표로 쓰지 말 것** — 손이 없는 프레임이 분모에 섞여 값이 오염된다. 의미 있는 값은 **눌림 근처** 기준이다.
- 🔴 **지표 이름을 혼용하지 말 것** — 체류 구간 수 / 눌림 프레임 적중 / 선행시간 / 사전 감지율 / 개입 감지율은 **서로 다른 것을 잰다.** 인용 시 어느 지표인지 반드시 명시.
- 🔴 **사후 층화(post-hoc stratification) 값을 성능으로 쓰지 말 것** — 데이터를 보고 구간을 좁혀 얻은 값은 낙관 편향이 있다. 새 촬영으로 확인해야 성능 주장이 된다.
- 🔴 **단일 세션 값 금지 — 통합값으로 인용한다.** 같은 구도를 반복해도 세션 간 편차가 크다.
- ⚠️ **임계 효과는 임계를 실제로 바꿔가며 파이프라인 그대로 측정한다** — 원점수를 보려고 임계를 0.01로 열면 **가중 NMS가 저점수 앵커를 섞어 점수를 끌어내려** 반대 결론이 난다.
- ⚠️ **`cam_probe` 10프레임 표본은 게이트로 쓰지 말 것** — 분산이 커 세션 평균과 어긋난다.

### 도구·구현
- 🔴 **런타임이 바뀌면 측정 도구도 같이 바꾼다** — 도구 기본값이 config를 안 따라 **4번 물렸다**(conf·ring·dwell·gap). 이후 도구는 **config에서 읽는다**.
- 🔴 **`run_demo.sh` 실기동 확인을 검증에서 빼지 말 것** — `@pyqtSlot` 시그니처 불일치처럼 **import 검사로 못 잡는 런타임 오류**가 있다. 실제로 GUI가 기동 즉시 죽은 적이 있다.
- ⚠️ **GPIO 로그가 정본이다** — 촬영 주기·횟수에 대한 기억·기대값이 틀린 적이 있다.
- ⚠️ **`sqlite3` CLI가 이 파이에 없다** — DB 질의는 `python3 -c`로.
- ⚠️ **촬영 지시는 "주기를 끝까지 반복"으로** — "횟수 지정"은 혼선을 낳아 재촬영을 부른다.

### 과장 금지 (조건 없는 단정 금지)
- 🔴 **"console_v2 검증 완료"로 쓰지 말 것** — 정확히는 **"클린룸 형광등·모조 콘솔 조건에서"**. 실콘솔 2차 test가 §10.16의 최종 판정이며 미실시.
- 🔴 **감지율을 성능 상수로 쓰지 말 것** — **착용 위치에 따라 크게 흔들린다.** 구도 조건을 함께 밝힌다.
- 🔴 **NFR-1을 "달성"으로 쓰지 말 것** — 미달이다.
- 🔴 **E2E 오경보 인용 시 시뮬레이션 정책 4종을 병기**한다(§10.31) — 실제 GUI 운용과 다르다.
- 🔴 **test 세트에 모델 프리라벨 금지** — 평가 대상 모델로 정답을 만들면 채점이 순환논리가 된다. 전량 수동.

### 운용 지식 (직관과 어긋나는 것)
- ⚠️ **카메라를 낮게 두면 콘솔이 화면 위쪽에** 잡힌다(높게 두면 아래쪽). 직관과 반대다.
- ⚠️ **놓친 눌림은 두 종류다** — 손 자체 미검출(구도 문제) vs 손끝이 ROI 밖(판정 문제). **대응이 다르므로 한 덩어리로 세지 말 것.**

## 📎 세부 함정 = 각 원본 참조 (복제 금지 → 포인터만)

영역별 세부 함정(gotcha)은 각 정본·가이드에 있다. **완료 이력은 `docs/작업로그.md`**(위 「문서간 경계」):
- 라벨링 기준(진짜 버튼 정체·애매하면 안 그림·Modal)·B3↔EMO 위치 구분·색기반 자동라벨러 배제 = `Rpi5/Demo/docs/labeling_guide.md`
- 데이터 파이프라인·Roboflow 함정 2개(`annotation_labelmap`·`annotation_overwrite` 캐시) = `Rpi5/Demo/docs/dataset_pipeline.md`
- 증강 스펙(파랑=저조도 주력·정반사 보험·`hsv_h=0`·기하 ±15°) = `Rpi5/Demo/docs/augmentation_plan.md`
- 학습 절차·DFC 변환·albumentations 2.x 인자 함정 = `dev/ai_model/console_v2_학습가이드.md`
- 측정·원인 분석(B4 미탐지·정반사·저조도 B3 사멸·파랑 스티커) = 통합문서 §10.5~§10.17
- 트랙 A 인터락 코드·전장 E2E·EMO 해제 결함 = §12 · Rpi5 `feature/fsm-interlock` · `dev/interlock/README.md`
- 목적 프레이밍 B·발표 역반영 = §1·§3 · 발표자료는 데스크톱 보관(repo 미반영)
