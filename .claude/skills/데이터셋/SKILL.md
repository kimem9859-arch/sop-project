---
name: 데이터셋
description: 라벨링·Roboflow 업로드·데이터셋 분할의 함정과 규칙 — 클래스맵·덮어쓰기·분할 누출·프리라벨 소스·자동라벨러 미채택 근거
when_to_use: 데이터셋을 만들거나 Roboflow 에 업로드할 때 · 프리라벨을 만들 때 · 학습·평가 셋을 나눌 때
---

# 데이터셋·라벨링

파이프라인 전체 = `Demo/docs/dataset_pipeline.md` · 라벨링 기준 = `Demo/docs/labeling_guide.md`.

## 🔴 Roboflow 함정 2개 (둘 다 물렸다)

1. **`annotation_labelmap` 이 없으면 클래스가 숫자("0","1")로 올라간다.**
2. **`annotation_overwrite=True` 가 없으면** 이미지 해시 캐시 때문에 `already annotated` 로 넘어간다.

## 분할

- ⚠️ **분할은 업로드 시점에 명시한다** — Roboflow 자동 랜덤분할 금지. 프레임을 섞으면 **누출 → mAP 거짓 상승**이다. 세션 단위 분할이 기본이나 **세션마다 촬영 변화 축이 다르므로** 무엇을 기준으로 나눴는지 함께 적는다.
- 🔴 **평가용 test 셋에는 프리라벨을 쓰지 않는다** — 평가 대상 모델로 정답을 만들면 채점이 **순환논리**가 된다. 전량 수동.

## 프리라벨

- **소스는 로그를 만든 모델**이다(`export_labels.py` 는 모델을 가리지 않는다). 달성률 수치는 통합문서 §12 가 정본이다.
- ❌ **색 기반 자동라벨러(`test/autolabel.py`)는 채택하지 않았다** — 달성률은 오르나 **EMO↔B3 오분류**가 났다(Hue 인접). 🔑 **틀린 라벨은 없는 라벨보다 해롭다.** 참고용으로만 보존.
