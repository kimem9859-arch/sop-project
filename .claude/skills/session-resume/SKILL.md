---
name: session-resume
description: 새 세션에서 이전 세션의 미완 작업을 이어받는다. 원격 동기화 · 두 작업로그 최신 블록의 ⏸/▶ · 보존 색인의 미기재/진행중 세션 · 커밋 안 된 변경을 모아 실제 상태와 대조한 뒤, 「지난번 요약 + 남은 작업 + 추천 하나」를 보고하고 멈춘다.
when_to_use: 사용자가 /이어하기 를 치거나 "이어서 하자", "이전 작업 이어받아", "지난 세션 이어서", "어디까지 했지" 라고 할 때. 세션의 첫 요청이 이전 작업을 가리킬 때.
---

# 이어하기 (session-resume)

> 설계 근거 = 2026-09-13 세션 `d44c5db2`·`f41da8bc` (CC 작업로그). 사용자는 새 세션을 열 때 무슨 말로 이어받게 할지 매번 몰랐다 — 이 스킬 하나로 고정한다.

## 원칙

- **읽기·대조·보고만 한다.** 작업 착수 · 작업로그 수정 · 소급 기록은 하지 않는다(기록은 `session-wrap`).
- **배너가 주입한 ⏸·▶ 는 낡을 수 있다** — 반드시 실제 상태로 대조한다(2026-09-13 배너가 이미 끝난 디스크 정리를 대기로 띄웠다).
- **다른 세션이 진행 중이면 그 작업은 가져가지 않는다** — 두 세션이 같은 기록을 쓰면 겹친다.

## 절차

### 1. 원격 확인

```bash
cd "$CLAUDE_PROJECT_DIR" && for r in . Rpi5; do git -C "$r" fetch -q 2>/dev/null; echo "$r: $(git -C "$r" status -sb | head -1)"; done
```

뒤처졌으면(`behind`) 보고에 적고 pull 을 추천에 포함한다.

### 2. 지난 작업 모으기

**두 작업로그 최신 블록의 ⏸·▶** — 마무리한 세션의 공식 기록:

```bash
cd "$CLAUDE_PROJECT_DIR" && for f in docs/작업로그.md docs/claude-code-작업로그.md; do echo "== $f"; awk '/^## /{c++} c==1{print} c>=2{exit}' "$f" | grep -E '^## |^-? *(⏸|▶)'; done
```

**보존 색인의 최근 7일 미기재·진행중 세션** — 마무리 없이 끝났거나 아직 도는 세션:

```bash
python3 "$CLAUDE_PROJECT_DIR/.claude/hooks/session_archive.py" --unlogged 7
awk -F'\t' -v me="${CLAUDE_CODE_SESSION_ID:0:8}" 'NR>1 && $6=="진행중" && substr($2,1,8)!=me {print $2, $1, $3}' ~/lab/session-archive/INDEX.tsv | while read -r sid day first; do
  if grep -q "$sid" "$CLAUDE_PROJECT_DIR"/docs/작업로그.md "$CLAUDE_PROJECT_DIR"/docs/claude-code-작업로그.md; then echo "최근 종료·기록 있음 ${sid:0:8} $day"; else echo "⚠️ 진행 중·기록 없음 ${sid:0:8} $day $first"; fi
done
```

🔑 **「진행중」만으로 경고하지 않는다.** 색인은 원본이 60분 안에 바뀐 세션을 모두 진행중으로 표시하므로, **방금 닫은 세션**도 여기 걸린다. 전체 세션 번호가 작업로그에 있으면 「최근 종료」로 보고 경고하지 않는다 — 기록이 없을 때만 경고한다(2026-09-13 시험 실행에서 발견).

**그 세션들이 멈춘 지점** — 보존본의 마지막 발화 2개와 응답 꼬리(`<ID8>` 를 채운다):

```bash
f=$(ls ~/lab/session-archive/*_<ID8>.md); awk '/^## 사용자 발화/{g=1;next} /^## 편집 파일/{g=0} g' "$f" | grep '^\[' | tail -2 | cut -c1-200; awk '/^## 마지막 응답 꼬리/{g=1;next} g' "$f"
```

**커밋 안 된 변경:**

```bash
cd "$CLAUDE_PROJECT_DIR" && git status --short && git -C Rpi5 status --short
```

### 3. 실제 상태와 대조

⏸·▶ 항목마다 **끝났다는 흔적**을 찾는다 — 최신 블록 날짜 이후의 커밋 제목(`git log --since=<블록 날짜> --oneline`, 두 저장소), 항목이 가리키는 파일·설정의 현재 상태. 판정은 넷 중 하나:

| 판정 | 뜻 |
|---|---|
| 대기 | 끝난 흔적이 없다 |
| 실물 준비 필요 | 결선·촬영·실HW 관문처럼 사용자가 현장에서 해야 한다 |
| 낡음 | 이미 끝났다 — **근거 한 줄**(커밋 해시나 파일 상태) |
| 미대조 | 확인 비용이 크다 — 추측으로 「낡음」 판정 금지 |

### 4. 보고하고 멈춘다 (형식 고정)

CLAUDE.md §1 답변 방식을 따른다(결론 먼저 · 새 용어 풀기).

```
지난번 — <3~4줄: 무엇을 했고 지금 어디. 기준 블록 날짜 명시>

남은 작업
| 항목 | 상태 | 근거 |

⚠️ 진행 중인 다른 세션 — <ID8 · 첫 지시>  (「진행 중·기록 없음」일 때만. 그 작업은 가져가지 않는다)
🔄 원격 — <동기화 / behind N / 로컬변경>

추천 — <하나> · 이유 <한 줄> · 실물 준비 <필요 / 불필요>
```

**그리고 멈춘다.** 사용자가 고르면 그때 시작한다 — 비자명 작업이면 `superpowers:brainstorming` 부터(CLAUDE.md §2).

## 주의

- 보존 색인은 세션 시작 스캔 기준이라 **최대 30분 늦을 수 있다.** 방금 끝난 세션이 안 보이면 그 때문이다.
- 최신 블록이 어제 이전 것일 수 있다 — 보고에 **블록 날짜**를 적어 기준을 드러낸다.
- 추천은 **하나만** 한다(CLAUDE.md §1⑤). 나머지는 남은 작업 표로 충분하다.
