# Claude Code 작업로그 (CC 작업 시간순)

> **Claude Code 관련 작업(훅·도구·인프라·작업방식 개선)** 을 시간순으로 남기는 로그.
> 프로젝트 작업(`작업로그.md`)과 **분리**. 설명서·원칙은 `claude-code-작업문서.md` 참조.
> **표기**: ✅ 완료 / ⏸ 중단 / ▶ 다음 / 🔗 커밋 · 세션 ID = 전체 UUID.

---

## 2026-08-29 · session 미상(소급 — 커밋 기반) (`hanium-docs` 접근 규칙 변경 — 파이도 읽는다)

> ⚠️ **커밋 메시지 기반 소급이다** — 무커밋 작업·판단 근거는 유실됐다. 2026-09-01 마무리 점검에서 미기록으로 검출돼 사용자 승인 후 기록했다.

- ✅ **CLAUDE.md §5 규칙 변경**(`74e3243`) — 종전 「`hanium-docs` 는 파이에 없는 것이 정상」을 **폐기**하고 **파이도 private 저장소를 clone 해 읽는다**로 바꿨다. 파이는 집필규칙·판정표·집필설계를 참조해 **소재를 정리**하고, **Word·PPT 편집은 데스크톱에서** 한다는 분업은 유지.
- 🔗 커밋: `74e3243`

---

## 2026-08-24 · session 8ef4918f-4e7e-4f1d-af92-4ce3827eb664 (⭐ 문서편집 3단 관문 신설 + `hanium-docs` 저장소 분리 — 「모양」을 검사하지 않던 문제)

- ⏸ 없음 — 관문·저장소 분리 모두 실동작 확인 완료
- ▶ 다음: 제작설계서(pptx)에서 같은 관문을 쓴다. `pptx` 스킬도 같은 자리에 있다
- 🔗 커밋: `sop-project` `bd5b5ee`·`3464328`·`3ef7f07`(세션마무리) / `hanium-docs` `0150f27`·`99fa481`·`a111610`

### 🔴 무엇이 문제였나 — 결함 14건이 관문을 그냥 통과했다

개발보고서 개정에서 사용자가 잡은 결함이 14건. 원인은 도구 부족이 아니었다:

1. **관문이 「내용」만 봤다** — 내가 만든 `check_docx.py` 는 단락·표·이미지 수와 금지 문구만 검사했다.
   **글꼴·크기·정렬·페이지 맞음은 검사 대상이 아니었다.** 사용자가 지적한 문제가 전부 이 바깥에 있었다.
2. **`document-skills@anthropic-agent-skills` 가 처음부터 활성화돼 있었는데 `docx` 스킬을 한 번도 안 불렀다.**
   그 안에 XSD 검증기·렌더러·run 병합기가 있었다. **이번 작업 최대의 실수.**
3. **눈이 없었다** — `libreoffice-writer` 미설치라 PDF 로 문서를 한 번도 못 봤다.
   설치 직후 **몇 분 만에** 그림 순서 뒤바뀜·번호 불일치가 나왔다. 계산으로는 안 나오는 것들이다.

웹 조사에서도 같은 결론이 나왔다 — Anthropic `docx` 스킬의 *"render it and look at it"*,
DocxEngine·docx-cli 의 시각 검증 내장, Textual-to-Visual Iterative Self-Verification 논문의
**넘침·겹침·빈 공간** 실패 유형과 렌더-확인 반복 루프. **업계 해법이 한 방향으로 수렴한다.**

### ✅ 완료 ① — 3단 관문 (`docs/개발보고서/` → 분리 후 `hanium-docs/도구/`)

| 단계 | 도구 | 잡는 것 |
|---|---|---|
| ① 스키마 | 공식 `validate.py` | OOXML XSD. **실제로 `tcPr` 자식 순서 위반 1건 적발** — Word 「복구」 유발 가능, 내 검사기로는 원리적으로 못 잡는다 |
| ② 내용 | `check_docx.py` | 구조 수·금지 문구 |
| ② 서식 | **`check_format.py`(신설)** | 글꼴 설치여부·**인쇄 글자크기**·페이지 맞음·**그림 번호 순서**·표 셀 양쪽맞춤/폭 |
| ② 회귀 | **`diff_format.py`(신설)** | **원본 대조** — 의도하지 않은 서식 변경. 요약표 라벨 칸 7개가 가운데→왼쪽으로 바뀐 것을 이걸로 잡았다 |
| ③ 렌더 | `gate.sh` | PDF 변환 → 쪽수·빈 페이지 → **페이지 PNG 를 눈으로 본다** |

성과: 그림 인쇄 글자 크기가 **6.5~16.3pt 제각각 → 13.7~14.5pt** 로 정렬됐다(본문 12pt +2~3).

### ✅ 완료 ② — `hanium-docs` 저장소 분리

**미푸시 16커밋이 34.3MB 바이너리를 더하고 있었다**(백업본 docx 7개가 29MB — `git add -A` 로 딸려 들어감).
푸시 전이라 원격 이력을 건드리지 않고 회수했다.

- `sop-project` 를 원격 지점으로 되돌리고 **포인터만** 남김(CLAUDE.md §5 · README 「형제 저장소」)
- `hanium-docs` 신설 → 비공개 원격 `github.com/kimem9859-arch/hanium-docs`
- 의존은 **한 방향** — hanium-docs 가 `../projects` 를 읽기만 한다. 역참조 0건
- 🔴 **파이에는 없는 것이 정상**임을 양쪽 문서에 명시(파이 Claude 가 폴더를 만들거나 규율 정본을 찾아 헤매지 않도록)

### ⚠️ 함정 (다음에 또 물릴 것 — CLAUDE.md §5 · hanium-docs/CLAUDE.md §3 에 등재)

- 🔴 **`doc.paragraphs` 는 표 안을 담지 않는다** — 문서 절반이 조용히 빠진다. **두 번** 물렸다(요약표 단문화 누락·글꼴 교체 누락)
- 🔴 **넣기 전에 잰다** — 본문 폭 17.0cm 를 8번째 작업에서야 쟀고, 셀 폭 14.28cm 는 사용자 지적 **뒤에** 쟀다. 순서가 거꾸로였다
- ⚠️ **OOXML 은 자식 요소 순서를 스키마로 강제한다** — `tcPr` 에 `w:shd` 를 append 하면 안 된다
- ⚠️ **표 셀에 수동 줄바꿈 + 양쪽맞춤 금지** — Word 가 줄을 셀 폭에 맞춰 늘려 글자가 벌어진다
- ⚠️ **글꼴이 실제로 설치돼 있는지 확인** — 본문 335 run 이 이 PC 에 없는 글꼴(휴먼명조)이었고 문서에 내장도 안 됐다. 맑은 고딕으로 통일
- 🔑 **안전장치가 값을 했다** — 분리 전 태그(`백업_분리전_20260821`)를 걸어둔 덕에, 복사 목록에서 빠진 7월 백업본 4개(14MB)를 `reset --hard` 후 복구할 수 있었다

---

## 2026-08-13 · session bbe6055e-013b-4c6e-91a5-a2a54835afd7 (🚀 Colab CLI 연결 — 파이 터미널에서 GPU 학습)

> 같은 세션의 **데이터셋 병합·`tool_v3` 학습·판정**은 프로젝트 작업 → `작업로그.md`.
> 🔴 **함정 상세는 통합문서 §10.41-(6)이 정본** — 여기 복제하지 않는다.

- ✅ **`google-colab-cli` 를 파이(aarch64)에 설치·검증** — 구글이 2026-06 출시한 공식 CLI. 이제 **브라우저·Drive 없이 파이 터미널에서 GPU 학습**을 돌린다. OAuth 1회 승인 · T4 할당 · 원격 실행 · 파일 회수 · VM 반납 전부 실측 확인.
  - 🔴 **설치는 반드시 `uv tool install google-colab-cli --with "jupyter-kernel-client<1.0"`** — CLI 0.6.0 이 의존성을 고정하지 않아 최신 1.0.x 가 딸려오면 `colab exec` 가 `AttributeError: KernelClient` 로 죽는다(1.0 에서 개명). **재설치할 때 재발한다.**
  - ℹ️ CLI 가 에이전트용 안내문을 내장한다 — `colab skill`.
- ✅ **장시간 작업의 함정 4건을 실측으로 규명**(정본 §10.41-(6)) — 요약만: 프록시 토큰 **1시간** 만료 후 파일·커널 API 전부 404 / ultralytics 저장 경로가 문서와 다름 / **`colab exec` 는 커널 예외에도 종료코드 0**(성패를 종료코드로 판단 금지) / keep-alive 는 SSH 와 분리된 프로세스라 접속이 끊겨도 학습이 계속 돈다.
- ✅ **에이전트 병렬 구현 방식 실전 적용** — 태스크 9개를 태스크별 에이전트 TDD + 리뷰 13라운드로 진행. **리뷰가 계획서 자체의 결함 5건을 잡았다**(상세는 프로젝트 로그). 리뷰어에게 "이 테스트가 옛 구현에서 실제로 실패하는가"를 검증시키는 것이 반복적으로 값을 했다.
- ⏸ 중단: (없음)
- ▶ 다음: (없음 — Colab 연동은 완결. 다음 학습 시 위 함정 목록을 먼저 볼 것)
- 🔗 커밋: 프로젝트 로그 참조(코드·문서 커밋은 전부 그쪽)

---

## 2026-07-30 · session c6c71d7d-67f2-47a2-bce7-0c5ff8916a3a (⭐ CLAUDE.md=지시 / README=포인터 성격 분리 — 11,115 → 4,988자)

> 같은 세션의 **기준문서·타임라인 폐기, 통합문서 정본화**는 프로젝트 작업 → `작업로그.md`.
> 설계 = `docs/superpowers/specs/2026-07-30-문서-역할경계-재정립-design.md`

- ⏸ **중단** — 없음(작업 단위 완결).
- ▶ **다음** — CC 인프라 대기 항목 없음. 프로젝트 본류는 `작업로그.md` 참조.

### ✅ 완료

**1. 사용자가 문서 성격 원칙을 정식화 — 중복은 CLAUDE.md 쪽을 덜어낸다**
초안은 `claude-code-작업문서.md` §1·§2가 CLAUDE.md와 중복이니 **작업문서를 지우는** 방향이었다. 사용자가 뒤집었다 — **내용은 원본 문서로 옮기고 CLAUDE.md를 비운다.** 이유: 한 문서에 핵심을 다 몰아넣으려 하면 2026-06~07의 비대화(「현재 상태」 163줄)가 반복된다. memory에 저장(`claude-md-is-router-not-content`).

**2. 2차 교정 — CLAUDE.md에 문서별 포인터를 남기는 것도 안 된다**
내가 `훅 구성 = 작업문서 §1`·`세션마무리 = SKILL.md` 같은 포인터를 남기려 했는데 사용자가 다시 교정: **그것은 여전히 CLAUDE.md가 README 역할을 겸하는 것**이다. → **CLAUDE.md = 지시사항 / README = 유일한 포인터**로 성격을 분리하고, CLAUDE.md가 정보 위치를 알려야 할 때는 README로만 보낸다.

**3. 🔴 3차 교정 — 판별 기준의 구멍을 사용자가 짚어냄**
내가 세운 1차 기준(*"~하라 / 하지 말 것" 형이면 CLAUDE.md*)에는 구멍이 있었다 — 그 기준이면 **작업문서 §3.0**(*"훅을 만들기 전에 물어라"*)까지 CLAUDE.md로 끌려온다. 사용자가 "superpowers 절차도 작업문서에 넣어도 되지 않나, 계기판 스티커가 글러브박스를 가리키게 하면"이라고 제시해 기준을 다시 세웠다:

| 기준 | CLAUDE.md | 원본 문서 |
|---|---|---|
| **판단하는 순간 눈앞에 있어야 하나** | ✅ CLAUDE.md만 자동 로드되므로 이 위치로만 보장 | ❌ 그 일을 하기로 정한 뒤 열면 됨 |

내 반론("판단하려면 규칙을 알아야 하는 순환")은 **판단 지시 자체를 CLAUDE.md에 남기면 끊긴다** — 사용자 구조가 그것이었다. → **트리거는 CLAUDE.md, 절차는 원본 문서.**

**4. `CLAUDE.md` 축약 — 135줄 11,115자 → 81줄 4,988자** (`2a9cb64`)
사실·포인터 6섹션(프로젝트 정의·두 환경·Rpi5 셋업·모델 3단계·PoC 실행·📎세부함정) → README / 훅 현황 → 작업문서 §1 / superpowers 갈래 상세 → 작업문서 §2 / 세션마무리 절차 → `session-wrap` 스킬(이미 100% 커버).
- **핵심 기법: 문서를 경로가 아니라 이름으로 부른다** — "CC작업문서 §2에서 먼저 읽는다(경로는 README)". 경로 열거가 README 소관이라 CLAUDE.md에 남은 `.md` 경로는 **19개 → 2개**(README.md·자기 언급)가 됐다.
- 자기모순 3건 제거: 71행(「현재 상태」와 공존 — 32행이 그 절을 금지) · 73행(기준문서 나침반 문구) · ⛔채택 안 함(`tasks/todo.md`·`lessons.md` — 2026-07-19 거부한 대안의 기록이라 정본은 그 spec).
- 🔴 **함정·금지 18항목은 한 줄도 깎지 않았다.** 축약 후 문서의 약 35%를 차지하는데 정상 — 전부 "실제로 물린 것"이고 CLAUDE.md가 원본이라 나갈 곳이 없다.

**5. `claude-code-작업문서.md` 승격 — 제거가 아니라 CC 설명서 정본으로** (`4f2d274`)
- §1: 등록 훅이 **①② 둘뿐**임을 명시. `session-prune-stubs.sh` 누락 보완, **`session-sync-check.sh`가 독립 훅이 아니라 brief의 순차 호출 하위 스크립트**임을 정정(7/17 이후 stale이던 부분).
- §2: 표준 갈래 4단계 · SDD 비기본 조건 · 계획서 150~250줄 · 관문 3종 · 워크트리 미사용·병렬 제한 사유 흡수. "큰 변경 → plan mode" 폐기(7/26 전환 미반영이던 것).
- §3(훅 설계 원칙 44줄)은 **손대지 않음** — 유일본이고 원칙이라 시간이 지나도 썩지 않는다. 이 문서가 정체된 원인은 §1·§2가 "현황"을 서술했기 때문이고, 앞으로 인프라가 바뀌면 **고칠 곳이 여기 하나로 확정**된다.

### ⚠️ 절차 이탈 (의식적 예외 — 실수 아님)
**`writing-plans`를 생략하고 `plan mode`로 대체했다.** 사용자 판단(사용량 80% 도달). 근거: 계획서가 역사적으로 값을 한 부분(**관문·중단 규칙**)이 이미 spec §6에 있어 공백은 **순서 하나**였다. 과거 "plan mode 중심안 기각"(spec이 안 남아 참조가 끊긴다)은 이번에 해당하지 않는다 — spec은 이미 커밋돼 있고 plan mode가 대체한 것은 계획서 쪽이다.
대신 **add-before-remove 순서 + 단계별 커밋**을 안전망으로 썼다: 채우기(1) → 정본화(2) → 비우기(3) → 폐기(4). 어느 지점에서 끊겨도 정보 유실 0이고, 단계 커밋이 진행 체크박스 역할을 한다.

### 🔍 자체 발견 (관문을 실제로 돌려서 나온 것)
- **⑥ 관문이 자기 발에 걸렸다** — `⛔ 채택 안 함: tasks/todo.md·lessons.md` 줄이 걸린다. **예외를 두지 않고 그 줄을 삭제**했다(예외를 두면 다음 사람이 예외를 늘린다).
- **자수 단위 오류** — `awk length()`가 C 로케일에서 **바이트**를 세는데 `wc -m`(문자)과 섞어 썼다. 한글 3바이트라 「작업·커밋 규칙」 비중을 84%로 보고했으나 실제는 **45.6%**, 함정·금지는 3,184자가 아니라 **1,571자**였다. 비율(바이트/바이트)은 유효했으나 전체값과 섞여 부풀려졌다.
- **사용자 커밋 덮어씀** — 3차 교정 편집이 `b9acd09`(사용자가 정식화한 「따름정리 2」)를 지웠다. 복원하고 행선지만 3차 결정으로 갱신(`90b6fe4`).

- 🔗 커밋: `8c8c2e0` · `25b746c` · `b9acd09`(사용자) · `42eff77` · `90b6fe4` · `4f2d274` · `2a9cb64`

---

## 2026-07-30 · session 9de4953a-b099-4501-b464-4b145ef2b942 (⭐ CLAUDE.md를 헌법으로 되돌림 — 작업기록 155줄 제거 + 경계 규칙)

- ⏸ **중단** — 없음(작업 단위 완결).
- ▶ **다음** — CC 인프라 쪽 대기 항목 없음. 프로젝트 본류는 `작업로그.md` 참조.

> 같은 세션의 **통합문서 정합성 점검**은 앞 블록(프로젝트 작업 = `docs/작업로그.md`)에 있다.

### ✅ 완료

**⭐ 출발점 — 사용자 지적: "CLAUDE.md에 작업 내용·기록이 들어 있다"**
- 내가 먼저 낸 처방은 **"중복 수치만 포인터화"** 였는데 **증상만 건드리는 것**이었다. 사용자가 *"이 문서는 CC가 작업하기 위한 **헌법·지시사항**인데 작업 기록이 섞여 있다"* 고 성격 자체를 지적했다.
- 🔴 **측정으로 확인**: 244줄 중 **작업 기록 155줄(63.5%)**, 규칙은 83줄(34%)뿐. 「현재 상태」 133줄 + 「(이전) 2026-07-19·20 시점」 22줄.

**⭐ 원인 규명 — git 이력으로 (사용자 질문 "왜 CLAUDE.md에 기록했나"에서 출발)**

| 시점 | 사건 |
|---|---|
| 2026-06-11 | CLAUDE.md 생성(63줄). 「현재 상태」 **7줄**. **작업로그가 없던 때** — 유일한 기록처였으니 정당한 위치 |
| 2026-07-01 | `작업로그.md` 신설. 커밋 메시지 = *"시간순 저널, **CLAUDE.md 현재상태와 역할분리 공존**"* → **경계를 정의하지 않고 "공존"으로 결정.** CLAUDE.md는 3줄만 수정(훅 명문화) |
| 이후 | 7 → 15 → 25 → 85 → 119 → **163줄**. **한 번도 줄어든 적 없음** |

- 🔴 **사용자 가설("7/1에 이전 안 해서 남았다")은 사실과 달랐다** — 당시 15줄의 내용은 **지금 하나도 없다**(4개 항목 전부 0회, 자연 교체). 현재 163줄은 **날짜가 전부 7/20 이후**, 즉 작업로그가 이미 있던 시기에 새로 쓰인 것이다.
- ⭐ **진짜 원인 = 경계 부재.** 아무도 규칙을 어기지 않았다 — 규칙이 "공존"이었다. 「스냅샷」의 정의가 없으니 **덜어내는 규칙도 없어** 단조 증가했다. → **일회성 정리로는 6주 뒤 다시 찬다**(7/1에 실제로 그랬다). 그래서 **기준을 먼저 세우고** 걸러냈다.

**⭐ 수정 전 전수 검증 — 이전(migration) 대상 0건**
- 사용자 지시가 *"삭제가 아니라 **이전** 중심"* 이라 먼저 대조했다. 결과: **최상위 완료 22건 전부 작업로그에 있음**(#6만 제목 표현이 달라 0으로 나왔으나 `hand_tracker`·`81줄 삭제`로 확인).
- ⭐ **내용 깊이도 작업로그가 더 상세** — 도넛 56줄 · 정량채점 113 · HOI DB 79 · 재현성 171 · 위반실측 102 vs CLAUDE.md 각 1~13줄. **삭제해도 정보가 준다기보다 늘어난다.**
- 「(이전)」 22줄도 전부 소재 확인(`test 2단계 분리`는 통합문서 3곳).
- ⚠️ **키워드 히트에 속을 뻔했다** — `실기동`은 작업로그에 **14회** 나오지만 전부 *"GUI 실기동 미확인(카메라 부재)"* 같은 **상태 서술**이고, *"검증에서 빼지 말라"는 교훈*은 없었다. **핵심 문구 단위로 재대조**해 잡았다. (그 2건은 함정이라 CLAUDE.md에 남으므로 결국 이전 0건)

**변경 4건**
- ① **`⚠️ 함정·금지` 섹션 신설 (27건, 4갈래)** — 측정·인용 / 도구·구현 / 과장 금지 / 운용 지식. **전부 실제로 물린 것만.** 🔴 **수치는 전부 제거하고 교훈만** 남겨 §10·작업로그와 역할을 갈랐다(사용자 선택 = (a)안: 함정은 지시사항이니 헌법에 두고 매 세션 자동 로드되게).
- ② **「현재 상태」133줄 + 「(이전)」 22줄 삭제 = −155**
- ③ **`🚧 문서간 경계` 신설** — 6행 라우팅 표 + 재발 방지 경고. **왜 그렇게 됐는지(경계 부재·성장 곡선)를 함께 적어** 다음 편집자가 "왜 여기 쓰면 안 되나"에 답을 얻게 했다.
- ④ `📎 세부 함정` 포인터 섹션 계층 정리(삭제 중 `###`로 딸려 들어간 것을 `##`로 승격).

**🔴 부수 발견 — SessionStart 훅이 ▶다음을 못 집어내고 있었다**
- 훅은 최신 블록에서 `^-? *(⏸|▶)` **평면 불릿**을 뽑는데(SKILL.md §5가 규정한 형식), **2026-07-28·07-30 블록이 `### ▶ 다음` 헤딩 + 중첩 불릿**으로 쓰여 배너에 **"미완 작업 없음"** 이 떴다. (내가 쓴 블록 포함)
- ⭐ **이것이 「현재 상태」가 자란 또 하나의 이유다** — 훅이 방향을 못 알려주니 **CLAUDE.md의 ▶다음이 실질적으로 유일한 방향 정보**였다. 따라서 훅을 고치지 않고 ▶다음만 지웠으면 방향을 잃었을 것.
- 조치: `작업로그.md` 최신 블록에 평면 `- ⏸`/`- ▶` 2줄 추가 → 배너 정상 표시 확인. **과거 블록은 저널 불변 원칙상 손대지 않음.**
- ⚠️ **처음엔 줄이 길어 배너가 250자로 벌어졌다** → 100자 안팎으로 압축.
- `SKILL.md` §5에 경고 추가 — 평면 불릿 필수 · **길면 배너가 그만큼 넓어짐** · 확인 명령(`awk … | grep -E '^-? *(⏸|▶)'`).

**부수 정리 2건**
- **점검 문서 폐기** — `통합문서_정합성점검_2026-07-29.md`(405줄). 32건 수정이 전부 반영돼 수명이 끝났고, 문서 스스로 머리말에 *"수정 반영 후 삭제한다"* 를 적어 뒀던 대로 폐기. 이력은 `503682b` 등에 보존.
- **`SKILL.md` §6 서명 stale 해소** — `Opus 4.8`이 박혀 있었다. 요청은 "Opus 5로 변경"이었으나 **모델명을 박으면 또 stale이 되므로** `CLAUDE.md`「작업·커밋 규칙」을 가리키게 했다(재발 방지 주석 포함). 🔴 **CLAUDE.md 쪽은 빼지 않았다** — 그쪽이 모든 커밋에 적용되는 정본이고, "왜"와 "과거 커밋은 고치지 않는다"가 거기에만 있다.

### 📊 결과

| | 전 | 후 |
|---|---:|---:|
| CLAUDE.md | 244줄 | **135줄** |
| 그중 작업 기록 | 155줄 (63.5%) | **0줄** |
| 측정 수치 복제 | 다수 | **0** |
| 함정·금지 섹션 | 없음 | **27건** |

검증: 작업기록 섹션 0 · 수치 잔존 0 · 삭제분 8개 표본 전부 타 문서 실재 · **훅 배너 ▶다음 정상 표시** · 표 무결성 · 참조 파일 5개 실재.

### ⚠️ 남긴 것

- 계획 목표(105줄)보다 **30줄 초과** — 함정을 27건으로 잡아서다. 쳐내려면 실제로 물린 항목을 버려야 해 남겼다.
- 삭제 중 *"이 CLAUDE.md = 어떻게 일하나(규칙·흐름·**현황**)만"* 한 줄이 사라졌다. 「문서간 경계」가 **현황을 명시적으로 제외**하며 더 정확히 대체하므로 되살리지 않았다.

### 🔗 커밋

`eda8abe`(점검 문서 폐기 + SKILL 서명) · `af3346d`(CLAUDE.md 재편 + 훅 형식 보정)

---

## 2026-07-30 · session 9de4953a-b099-4501-b464-4b145ef2b942 (⭐ 통합문서를 CC 정본으로 — 목적 선언·[CURRENT] 표·§10 유효성 마커)

> 같은 세션의 **통합문서 내용 수정**(정합성 32건)은 프로젝트 작업 → `docs/작업로그.md`.
> 이 블록은 **통합문서를 "CC가 grep으로 소비하는 정본"으로 만드는 구조 보강**만 다룬다.

### ✅ 완료

**⭐ 출발점 — 내가 목적을 잘못 잡고 답했다**
- 사용자 질문(*"§10 비중이 76%인데 문서의 역할이 달라진 건가"*)에 나는 **"읽기 어려우니 §10을 분리하자"**고 권고했다. 근거는 머리말의 *"📖 읽기 순서 설계"*(배경→요구사항→시연→기술 상세)였다.
- 🔴 **사용자 정정**: 이 문서의 소비자는 **사람이 아니라 Claude Code**다. 사람용 정리·발표 자료는 따로 있고, 이 문서는 **다른 문서를 만들고 고칠 때의 기준(정본)** 이다.
- 🔴 **확인해 보니 그 목적이 어디에도 적혀 있지 않았다** — 통합문서 머리말·CLAUDE.md·README 어디에도. CLAUDE.md의 *"작업 시 원문을 읽고 진행"* 이 가장 가까웠으나 "사람용이 아니다"라고 못박은 곳은 없었고, **머리말은 오히려 사람의 독해 흐름을 명시적으로 설계**해 뒀다. → **목적이 기록되지 않으면 편집자가 계속 엉뚱한 것을 최적화한다. 내가 그 사례.**

**⭐ 전제를 바꾸니 결론이 뒤집혔다 — §10 분리 권고 철회**
- 소비 방식이 **RAG가 아니라 `grep` + 범위 읽기**임을 확인했다(임베딩·벡터DB 없음). 그래서 판단 기준이 바뀐다:

  | | 사람 독자 기준 | **CC 정본 기준** |
  |---|---|---|
  | 읽기 흐름·분량 | 중요 | **무관** — 76%가 §10인 건 문제가 아님 |
  | **모순** | 사람이 보고 되물음 | 🔴 **치명적** — CC는 하나를 조용히 집어 쓴다 |
  | **폐기된 내용** | 맥락으로 걸러짐 | 🔴 **최대 위험** — 현행처럼 읽힘 |
  | 파일 분리 | 읽기 편해짐 | 🔴 **위험 증가** — 한쪽만 읽는 실패 |

- **분리는 grep 대상을 쪼개 "이력 문서만 읽고 정본을 안 읽는" 실패를 만든다** → 하지 않기로 확정.
- ⭐ 반대로 **구조는 이미 정본급**임도 실측으로 확인했다 — 헤딩(`## N.`/`### N.M`)·참조(`§X.Y`)·근거 raw 경로가 전부 일관되고, **"과장 금지 / 성능 지표로 쓰지 말 것" 가드레일 블록이 12건** 본문에 박혀 있다. CC 정본으로서 귀한 자산이라 **손대지 않기로** 했다.

**실측 — 무엇이 실제로 위험한가**
- 크기: 2,628줄 ≈ **59k~72k 토큰**. Read 도구 25k 한계로 **최소 3회** 분할. 이번 세션에서 실제로 `27561 tokens exceeds 25000` 실패를 겪었다. → **통독 금지, grep 기반으로.**
- 검색 정밀도: `사전 감지율` **30히트에 서로 다른 값 12개**(56%~100%). `임계` 96 · `FPS` 46 · `체류` 43 히트. **CC가 조건을 모른 채 하나를 집는다.**
- 폐기 판별: 정정 표기가 **8종**(`폐기`24/`무효`10/`정정(`7/`철회`3/`⛔`2/`🔴**정정`3/`⭐**재정정`1/`SUPERSEDED`0), 위치도 제목 **+0~+72행**. **grep으로 못 거른다.**

**③ 목적 선언 신설 (머리말)**
- *"이 문서는 Claude Code가 작업 시 참조하는 단일 정본이다. 사람이 읽기 좋게 정돈하는 것은 목표가 아니다."*
- 편집 우선순위 **①모순 없음 → ②폐기/현행 구분 → ③검색 가능성**, 서술의 매끄러움은 뒤로. **분량은 제약이 아니다**(이력은 지우지 말고 마커로 무효화).
- RAG가 아님을 명시하고 거기서 따라오는 규칙 3개(수치는 조건과 **같은 줄** / 값이 여러 개면 구분 키 / 폐기 표지는 리터럴 하나)를 기록.
- ⚠️ **내 잘못된 분리 권고를 재발 방지 사례로 본문에 남겼다.** 기존 `📖 읽기 순서 설계`는 지우지 않고 *(부차적 — 사람 통독용)* 으로 성격만 표시.

**① §10 「현재 확정값」 표 신설 — `[CURRENT]` 태그 20항목**
- §10 진입부(§10.1 앞)에 **값·조건·근거절**을 한 행에 담아 5개 묶음(정확도·감지차단·성능·운용 파라미터·구도 제약)으로. 끝에 **⛔ 확정값으로 쓰면 안 되는 것 5건**(100%/98.4%/"검증 완료"/30fps/옐로우등).
- ⭐ **갱신 관문 규칙**: *"이 표를 갱신하지 않은 측정은 확정값이 아니다. 표와 절이 어긋나면 **절이 옳고 표가 갱신 누락**이다"* — 표를 원천이 아니라 **관문**으로 둬 복제의 대가를 상쇄.
- ⚠️ **작업 중 잡은 문제 2건**: ⓐ태그가 머리말·절 제목으로 새어 grep을 오염 → 리터럴 제거 ⓑ**문서에 적어둔 grep 명령이 실제로 안 됐다** — 셀을 `` `[CURRENT]` `` 로 감싸 백틱이 끼는 바람에 `grep "\[CURRENT\] 사전 감지율"`이 0건. 백틱 20건 제거해 **문서 기재 명령과 실동작을 일치**시켰다.

**② §10 절 유효성 마커 31건 전건 부여 (B안)**
- 모든 §10.x 절의 **제목 바로 다음 줄** 고정: `✅ CURRENT-AS-OF` 13 · `⚠️ PARTIALLY-SUPERSEDED-BY §X` 17 · `⛔ SUPERSEDED-BY §X` 1.
- ⭐ **A안(폐기 절에만) 대신 B안(전 절)을 택한 이유** — 무표지가 "유효"인지 "미판정"인지 구분되지 않으면 **"모든 절이 마커를 갖는가"라는 검사를 만들 수 없다.** B안은 그 모호함이 구조적으로 사라진다.
- **PARTIALLY가 17건으로 다수** — §10 대부분이 "측정은 유효, 해석만 틀렸다" 유형이라 절을 통째로 버릴 수 있는 건 §10.24 하나뿐이었다.
- 가장 값이 큰 건 **§10.11** — 표지가 아예 없는데 본문 24행째에 *"ESP32는 주 카메라로 부적합"* 이 단정돼 있었다. 그 근거 둘 중 B4는 §10.21에서 무효화됐고 **남은 근거는 FPS뿐**임을 절 머리에 명시.
- 규약도 §10 머리와 CLAUDE.md에 기록 — *"새 절을 추가하면 마커도 함께 단다. 마커 없는 절은 미완성이다"*, 기존 산문 정정은 지우지 않는다(경위는 본문에).

**⭐ 살아 있는 drift를 잡았다 — 이 보강이 겨냥하는 실패의 실제 사례**
- `CLAUDE.md`가 위반 사전 차단율을 **78.2%**로 적고 있었는데 §10.31은 2026-07-28에 **75.9%**로 정정했다. CLAUDE.md만 갱신이 안 됐고, **내가 이 세션에서 그 stale 값을 재인용했다.**
- → CLAUDE.md에서 수치를 빼고 `[CURRENT]` 표를 가리키게 했다.

**CLAUDE.md 규칙 보강 4건**
- *"작업 시 원문을 읽고 진행"* → **"통독 금지, grep으로 필요한 절을 찾아 범위 읽기"**(근거: 토큰·Read 한계 실측)
- `[CURRENT]` 표 사용법 · 인용 시 조건 열 필수 · 갱신 관문 규칙
- **§10 절 인용 전 유효성 마커를 먼저 볼 것**

### ▶ 다음

- ⚠️ **`.claude/skills/session-wrap/SKILL.md` §6의 서명이 stale** — `Co-Authored-By: Claude Opus 4.8`로 적혀 있으나 CLAUDE.md는 **2026-07-26부터 주 작업 모델 = Opus 5**로 고정했다. 이번 마무리는 CLAUDE.md를 따랐다. **SKILL 수정 여부는 사용자 판단 대기.**
- ✅ 점검 문서(`통합문서_정합성점검_2026-07-29.md`) **삭제 완료**(2026-07-30) — 머리말에 스스로 적어 둔 수명대로. 이력은 커밋에 보존.

### 🔗 커밋

`503682b`(보강 ③① + CLAUDE.md) · `ef66362`(§10 유효성 마커 31건 + 규약)

---

## 2026-07-28 · session d96ff1e5-a309-477d-b4c4-46cd0fed3761 (⭐ superpowers 실행 갈래 조정 — SDD를 기본에서 내림, 실측 근거)

- ⭐ **문제 제기(사용자)**: superpowers를 작업 2건(폴더정리·창 판정)에 써본 뒤 **"토큰 사용량 대비 가성비가 있는지"** 확인 요청. 이어서 *"`writing-plans`가 한도를 빨리 태우니 일부만 쓰거나 plan mode로 방향을 다시 잡아야 할 것 같다"* 는 제안.
- 🔴 **측정으로 진단이 뒤집혔다 — 범인은 `writing-plans`가 아니었다.** 세션 JSONL에서 실사용량을 추출해 대조:

  | | 폴더정리 | 창 판정 |
  |---|---:|---:|
  | 메인 출력 | 345,192 | 634,918 |
  | **메인 캐시 읽기** | **51.17M** | **138.85M** |
  | 어시스턴트 턴 | 338 | 492 |
  | Agent 호출 | 6 | 23 |
  | 서브에이전트(태스크별 최종값 합) | — | **1,986,799** |

  - **커밋 시각으로 단계를 갈랐더니**: 설계(16:21) → 계획(16:32) → 사전점검(16:35) = **14분**. 그 뒤 SDD 실행이 **다음날 11:06까지 약 18시간 30분**. 세션 한도로 죽은 에이전트 2개(T4·T7)도 **둘 다 SDD 실행 중**.
  - 계획서 작성은 **전체 출력의 약 5%**. → **`writing-plans`는 범인이 아니다.**
  - ⚠️ **서브에이전트 집계 함정** — 재개된 에이전트는 알림마다 **누적값**을 보고한다. 단순 합산하면 541만으로 부풀고, **태스크별 최종값만 취해야** 199만이 맞다.
  - 🔴 **비용의 지배 항목은 서브에이전트가 아니라 메인 세션의 캐시 읽기**(1억 3,885만). 즉 **"세션이 길어진 것" 자체**가 비용이다.

- ⭐ **서브에이전트 안에서 리뷰가 52%**(12개·1,029,963) · 구현 48%(9개·956,836). 수확은 **Critical 1 · Important 2 · Minor 약 4**.
- ⭐ **그래도 절차가 값을 했다는 반사실** — 기본값 `M=3`의 차단율은 **71.8%**인데 종전 갭메우기는 **76.5%**였다. 스윕(중단 규칙)이 없었으면 **4.7%p 떨어뜨린 채 "개선했다"고 기록**했을 것이다. 최종 리뷰가 잡은 Critical(관문 FAIL인데 문서엔 PASS)은 **§10.31에 재현 불가능한 거짓 수치**를 남길 뻔했다.
- 🔴 **낭비였던 것도 명확** — 내 계획서에 결함 3건(사전점검 2건 + **수학적으로 통과 불가능한 테스트** 1건 → 구현자가 16만 토큰 태워 발견) · 소득 없는 리뷰 2건 · 병렬화가 만든 Critical 1건.

### ✅ 결정 — 표준 갈래 변경 (CLAUDE.md 반영)

- ⭐ **`subagent-driven-development`는 superpowers의 핵심이 아니다** — 스킬 원문으로 확인: `using-superpowers`는 핵심 process skill로 **brainstorming·systematic-debugging**만 꼽고 SDD는 언급조차 없다. `writing-plans`가 **실행을 두 갈래로 제시**(1. Subagent-Driven *recommended* / 2. Inline = `executing-plans`)하므로 **SDD는 옵션 1일 뿐**이다.
- → **표준 갈래 = `brainstorming` → `writing-plans`(경량) → `executing-plans`(인라인) → 최종 리뷰 1회.** 스킬 4개를 그대로 쓴다 — **"일부만 쓰는 것"이 아니라 "2번 갈래를 고르는 것"**.
- ⭐ **`verification-before-completion` 추가** — *"evidence before assertions"*. 이번 Critical(관문을 다시 안 돌리고 완료 선언)이 정확히 이 스킬이 막는 유형이고, **명령어 몇 줄로 리뷰 에이전트 12개가 하는 일 일부를 대신**한다.
- 🔴 **계획서는 150~250줄로** — 1,325줄은 과했고 코드 전문 복붙이 오히려 손해를 봤다.
- ⭐ **관문 3개는 계획서에 반드시 박는다** — 중단 규칙·🔒홀드아웃 분리·검증 관문. 이번에 셋 다 발동했고 **셋 다 계획서 문장 몇 줄**이었다.
- ⚠️ **병렬을 막는 건 하드웨어만이 아니다** — 태스크가 DB·모듈 의존으로 사슬을 이뤄 7태스크가 5웨이브로만 갈렸고, 앞당긴 것이 **관문보다 먼저 착지해 Critical을 만들었다**. CLAUDE.md의 "subagent 병렬 불가" 단서를 이 사유까지 포함하도록 갱신.

### ▶ 다음
- 다음 비자명 작업에서 **새 갈래를 실제로 적용**해 비용이 실제로 줄었는지 같은 방법(세션 JSONL 추출)으로 재측정한다.
- `plan mode` 중심안(B)은 **기각** — spec 문서가 안 남아 `docs/superpowers/specs/`를 참조하는 CLAUDE.md·§10 연결이 끊긴다.

### 🔗 커밋
- 🔗 sop-project `8ed1c6c`(CLAUDE.md 작업규칙 + 이 블록)

## 2026-07-26 · session c1abfd49-936b-4ec0-bab8-2809a9856462 (작업 단위 2 — ⭐ 답변 방식 규칙 신설, "이해하기 쉽게 다시"를 없앤다)

- ⭐ **문제 제기(사용자)**: 결정이 필요한 사항을 설명하는 답변의 수준이 높아 **"이해하기 쉽게 다시 알려줘"를 자주 되물어야 했다.** 추가 질문 없이 사용자 수준에 맞는 답변이 나오게 정립하기로.
- ⭐ **원인 = 지식의 저주**(curse of knowledge — 알고 나면 모르던 때가 어땠는지 떠올리지 못하는 인지 편향). 전문가는 상대가 안다고 가정하고 세부로 파고들며 맥락을 뺀다(MIT Sloan). **설명이 필요하다는 생각 자체를 못 하는 것**이라 "조심하자"로는 안 고쳐지고 **점검 항목**이 필요하다.
- ⭐ **진단 근거가 이 세션 안에 있었다** — 작업 단위 1에서 개정안을 **마크다운 원문 그대로** 보여드렸을 때 막혔고(`HARD-GATE`·`Global Constraints`·`verbatim`·`terminal state`를 설명 없이 사용), **기존/개정 대조표 + 도넛 ROI 작업으로 치환**하자 통과했다. **같은 내용, 형식만 바꿈.**
- ✅ **CLAUDE.md에 「답변 방식 = 사용자 이해 우선」 5항 신설**(`a43d0c7`):
  - ① 결론 맨 앞 2~3줄(핵심만 축약) ② 새 용어는 영어면 우리말 뜻 병기 + 한 줄 설명 ③ **이어하기 = 본론 전 "지난번에 뭘 했고 지금 어디" 3~4줄 요약** ④ **비유 → 비교 → 실제 사례 순**으로 풀고 원문·정의를 그대로 붙여넣지 않는다 ⑤ **결정 요청은 한 번에 하나**(추천안 동반, 얽힌 것은 A/B 조합으로 묶어 하나의 질문으로).
- ⭐ **③이 나온 경위 = 사용자 지적**: "어제 한 작업을 다음 날 이어서 하면 기억이 안 나 이해가 떨어진다." → 🔴 **작업로그가 이어하기용인데 그 역할을 못 하고 있다.** *"절벽 = y ≈ 337 (y336 66.7% → y338 18.2%)"* 같은 서술은 **기록으로는 정확하나 다시 읽고 떠올리기엔 어렵다.** ③은 그 간극을 사람 요약으로 메우는 장치다. **로그 형식 자체 개선은 별개 사안으로 미착수.**
- ⚠️ **⑤의 실증** — 작업 단위 1에서 내가 한 메시지에 **결정 4개**(자명한 것 예외 / 수치 처리 / plan mode / 기억 파일)를 몰아 물었고, 각각 배경 이해가 따로 필요했다. **그 답변에서 결정이 하나도 안 나왔고** 정리에 왕복이 더 들었다.
- ⚠️ **후보였다가 뺀 것 1건** — "세션이 바뀌면 용어를 다시 설명한다"(전에 설명해도 사용자에겐 안 남는다는 논리). **사용자 판단으로 제외.**
- ▶ 다음: **③(이어하기 요약)은 다음 세션에서야 효과 확인 가능** — 요약 길이·내용이 맞는지 사용자 피드백 받고 조정.
- 🔗 커밋: `a43d0c7`.

---

## 2026-07-26 · session c1abfd49-936b-4ec0-bab8-2809a9856462 (작업 단위 1 — ⭐ 계획 절차를 superpowers 스킬 원문에 위임, CLAUDE.md에서 절차 서술 제거)

- ⭐ **진단: 설계 8건이 전부 spec에서 멈췄다** — `brainstorming` 스킬의 종료 단계는 **`writing-plans` 호출**인데, 2026-07-19 채택 이후 8건 모두 spec을 쓰고 곧장 구현으로 갔다. 프로젝트 전체에서 `writing-plans` 언급이 **0건**(CLAUDE.md·작업로그 2종·설계문서 8개 grep). **검토 후 불채택이 아니라 선택지로 올라온 적이 없었다.**
  - 🔴 **결과 = spec이 plan의 역할을 흡수했다** — 실행 기록이 갈 곳이 없어 spec으로 흘러들었다(`vdevice #6-1 구현·검증 결과` · `손검출률 #6 오프라인 선검증` · `HOI-경로 ⚠️정정`). 그 김에 **확정 수치까지 딸려와** "설계문서엔 논의·미결만" 규칙도 같이 깨졌다.
  - 산출물 대조로 본 비중 = **superpowers 3 : CLAUDE.md 7**. superpowers가 이긴 건 spec 경로·2~3안·YAGNI뿐이고, spec 이후는 전부 CLAUDE.md/사용자 지시가 이겼다(검증이 "구현 후"에 배치 · `## 미결` 섹션 존치 · subagent 0회).
- ✅ **결정(사용자): superpowers 계획 방식을 원문 그대로 채택**하고, **CLAUDE.md에는 절차를 적지 않는다.** 스킬이 자동으로 작동하도록 **지시 한 줄 + 스킬이 알 수 없는 단서만** 남긴다.
  - **삭제** = brainstorming 절차 서술(스킬 본문과 중복 → 그 자체가 drift 원인) · HARD-GATE 문구 · spec 경로 · "설계문서엔 논의·미결만" · **subagent 자유 남용 불채택**(사용자 지시) · 자동 버그수정 즉시착수 불채택(`systematic-debugging`이 담당).
  - **유지** = ①**자명한 것 예외**(오타·날짜/해시·작업로그) ②**spec·plan에 확정 수치 금지**(§10 포인터만). 🔴 **둘 다 스킬 원문을 의도적으로 거스른다** — ①은 `brainstorming`의 *"단순해서 설계 불필요는 안티패턴"* 을, ②는 `writing-plans`의 *"Global Constraints에 값을 verbatim 복사"* 를. **스킬 원문이 CLAUDE.md 우선을 인정하므로 합법이나, 명시해야 override로 작동한다.**
  - **신설** = **워크트리 미사용 · subagent 병렬 불가**. 방침이 아니라 물리 — 코드가 별도 repo(`Rpi5`)라 `sop-project` 워크트리에 안 따라오고, Hailo `VDevice`·카메라·GPIO가 각 1개라 실HW 검증은 직렬만 된다(**순차 파견은 가능**).
  - 8줄 불릿 → 5줄, **절차 서술 0줄**.
- ⭐ **기억(memory)이 CLAUDE.md와 싸울 수 있다 — 규칙을 바꾸면 기억도 같이 본다.** `tests-after-implementation`("테스트는 단계마다 끼우지 말고 구현 끝낸 뒤 일괄")이 `writing-plans`의 태스크별 TDD와 **정면 충돌**했다. **CLAUDE.md는 이제 절차를 안 적으므로 반박 문장이 없어 기억이 이긴다** → 삭제. `brainstorm-first-workflow`도 본문이 "설계→승인→구현" 2단계를 서술해 갱신.
- ⚠️ **훅으로 막자는 안은 배제했다** — "이게 자명한 오타인가"는 의미 판정이라 LLM 훅이라야 하는데, 이 프로젝트엔 같은 목표의 실패가 **둘**(`doc-consistency-check.sh` 2026-07-01 제거 · `.githooks/pre-commit` 2026-07-17 폐기, 12회 실행·적발 0건) 있다. **세 번째를 만들 이유가 없다.**
- 🔴 **`writing-plans` 이후 경로는 이 프로젝트에서 미검증** — 8건 모두 spec에서 멈췄으므로 계획서 작성·태스크 분해·subagent 순차 파견은 **한 번도 돌려본 적이 없다.** 첫 실전에서 과중하면 조정 대상.
- ⚠️ **남긴 약한 충돌 1건** — `수술적·최소 구현`의 *"멀쩡히 도는 코드는 리팩터링하지 않는다"* vs `writing-plans`의 파일 분할 권장. 실질 위험이 작아 손대지 않았다.
- ▶ 다음: 첫 비자명 작업에서 **`brainstorming` → `writing-plans` → 실행 스킬**을 실제로 완주해 보고, 계획서 분량·태스크 크기가 이 프로젝트에 맞는지 확인. `docs/superpowers/plans/`는 그때 생성된다.
- 🔗 커밋: `00e73a4`. (기억 파일 2건은 repo 밖 — `~/.claude/projects/…/memory/`)

---

## 2026-07-26 · session 3817a9f6-9825-4bb6-b431-38891ea0a897 (주 작업 모델 전환 — Opus 4.8 → Opus 5)

- ✅ **2026-07-26부터 주 작업 모델을 `Opus 5`로 고정**(사용자 결정). 그 이전 작업은 `Opus 4.8`이었다.
  - 조치 = CLAUDE.md 커밋 서명 규칙에 전환 시점 명기. 이후 커밋은 `Co-Authored-By: Claude Opus 5`.
  - ⚠️ **과거 커밋 서명은 고치지 않는다** — 그때 그 모델이 만든 것이 사실이고, **모델별 작업 추적**이 이 규칙(2026-07-14 신설)의 목적이다.
  - 🔴 **문서에 모델명을 적어둘 곳은 이 규칙 한 곳뿐**이다 — 사양·측정 문서에는 모델명을 쓰지 않는다(작업 주체는 결과의 성질이 아니다). 전환을 기록하는 이유는 **어느 시점 커밋이 어느 모델 산출인지 나중에 판별**하기 위함이다.
- 🔗 커밋: `a865b1f`.

### 같은 날 — 분석 도구 교훈 3건 (HOI DB 구축 중)

> 프로젝트 작업(HOI DB · §10.30 재분석)은 `docs/작업로그.md`. 여기는 **도구·방법론**만.

- ⭐ **무거운 단계와 가벼운 단계를 분리해야 도구가 실제로 쓰인다** — 팜 추론(40분)과 DB 재구축(수 초)을 한 스크립트로 묶으면 **재구축마다 40분이 붙어 아무도 재구축하지 않는다.** 사이에 캐시를 두고, 캐시 스킵으로 **중단·재개**가 되게 했다(`.part` → `os.replace`로 반쪽 캐시 방지).
- ⭐ **검증은 "코드가 도는가"가 아니라 "기존 결과가 재현되는가"로** — §10.28·§10.29 값 4건과 대조했고, 그 대조가 **`_VIOLATION_RULE` 버그를 잡았다**(통합 지표로는 안 보이고 **세션별 기대값** 대조로만 드러났다). 반대로 **내 기대값이 틀린 경우도** 있었다(`far-high-r2`는 4주기가 아니라 5주기 — 원본 로그가 정본).
- 🔴 **통계 검정을 쓸 때 독립성을 먼저 확인할 것** — 프레임 단위 Fisher가 p=0.0136을 냈으나 **같은 눌림의 11프레임은 독립이 아니다.** 눌림 단위로는 표본이 2 대 4라 판정 불가였다. **이걸 안 짚었으면 "통계적으로 유의한 격차"로 기재할 뻔했다.**
- ⚠️ **파생 열을 스키마에 넣는 판단** — "원천만 담는다" 원칙에 `gap_frames`·`button_y`는 파생값이라 걸린다. **결정론적이고 규칙이 바뀔 일이 없어** 넣었고(그 덕에 곡선이 SQL 한 줄), 반면 **사전 감지율 같은 지표는 넣지 않았다**(판정 생산이 바뀌면 stale). **경계는 "규칙이 바뀔 수 있나"다.**

---

## 2026-07-24 · session 3817a9f6-9825-4bb6-b431-38891ea0a897 (작업방식 규칙 1건 + 측정 방법론 교훈)

> 프로젝트 작업(손 검출률 원인 규명 §10.24~§10.27)은 `docs/작업로그.md` 에 있다. 여기는 **작업방식·측정 방법론**만.

- ✅ **메모리 규칙 신설 — 무효 촬영 산출물은 "표시"가 아니라 "삭제"** (`memory/discard-invalid-capture-artifacts.md`). 사용자 정정("무효 표시가 아닌 제거 해줘")에서 나왔다. 이유 = 같은 `condition` slug 세션이 둘이 되면 분석 도구가 잘못 집고, raw는 세션당 약 100MB다. **단 증거 보존 결정분은 예외**(`cleanroom-yellow` 1500장 등).
- ⚠️ **측정 방법론 교훈 3건** (상세·수치는 §10.24~§10.27):
  1. **임계 효과는 임계를 실제로 바꿔 파이프라인을 그대로 돌려 잰다.** 게이트를 열고 내부 점수를 읽으면 **가중 NMS가 저점수 앵커를 섞어 점수를 끌어내려** 반대 결론이 나온다(실제로 날 뻔했다).
  2. **지표 이름을 겹쳐 쓰지 말 것.** "손을 본 비율"이 실은 사전 감지율이었고, 프레임 단위 검출률은 **분모에 손 없는 프레임이 섞여** 있었다. 오귀인의 직접 원인.
  3. **관문은 기준선을 확인하고 세울 것.** "버튼 검출률 ≥95%"를 관문으로 걸었는데 **대조군 자체가 87~88%** 라 무의미했다. `cam_probe`(10프레임)도 분산이 커 게이트로 부적합 — **파국 감지용**으로 격하하고 세션 평균으로 판정하도록 중간에 바꿨다(변경을 명시하고 진행).
- ⭐ **홀드아웃이 실제로 값을 했다** — ①②③(100·92·97%)만 보면 확신했을 자리에서 ④가 69%로 제동을 걸었고, 그 덕에 변수를 다시 잡았다. **찍고 열지 않는 세션을 두는 관행을 유지할 것.**
- ⚠️ **측정 방법론 교훈 3건 추가**(작업 단위 2에서 — 상세·수치는 §10.28·§10.29):
  4. **1세션 결과를 결론으로 쓰면 두 번 일한다.** `far-high` 100%(눌림 14회)를 두 문서에 기재한 뒤 재현에서 86~98%가 나와 정정했다. **"성능 상수로 쓰지 말 것"이라 적어둔 것이 그대로 실현**됐다 → **표본이 작으면 기재 전에 재현을 먼저.**
  5. **중간 지표를 최종 지표처럼 보고하지 말 것.** 팜 검출률 99.4%를 두 번 말했는데 프로젝트 명제(사전 감지율)는 **재지도 않은 상태**였다. **보고 시 어느 층의 지표인지 먼저 밝힐 것**(팜 → 손 → ROI → 사전 감지 → 차단).
  6. **교란을 빼야 신호가 보인다.** 속도-감지율 곡선을 전 세션(407건)으로 그리니 지오메트리 교란에 묻혔고(놓침 median이 오히려 더 길게 나옴), **절벽 눌림을 제외(365건)하자** 계단이 드러났다. **혼합 데이터에서 결론이 안 나오면 알려진 교란부터 뺄 것.**
- ⭐ **세션 간 조건 편차가 도구가 된 사례** — 세션 FPS가 7.6~13.5로 제각각이라 **"시간 경계인가 프레임 수 경계인가"를 추가 촬영 없이 분리**할 수 있었다. **조건이 흔들린 데이터도 버리지 말 것**(그 흔들림이 변수 분리를 준다).
- 🔗 커밋: 프로젝트 문서 커밋에 포함(`docs/작업로그.md` 참조). 이 블록 = 이 커밋.

---

## 2026-07-22 · session 69428f34-9852-4ec3-9917-fea4bf93373b (작업방식 규칙 1건 추가 — 테스트는 구현 완료 후)

> 프로젝트 작업(도넛 2단계 ROI 구현)은 `docs/작업로그.md` 에 있다. 여기는 그 과정에서 나온 **작업방식 규칙**만.

- ✅ **memory `tests-after-implementation.md` 신설** — 구현 계획에서 **테스트를 단계마다 끼우지 말고 구현을 전부 끝낸 뒤 일괄 실행**한다(사용자 지시). 의존이 얽힌 변경(공용 모듈 → 호출부 → 배선)에서는 중간 단계가 어차피 불완전해 테스트가 의미 있는 신호를 못 준다. 계획서를 「1단계=코드 작성(실행 없음) / 2단계=검증 일괄」로 나눈다. **검증 생략이 아니다** — brainstorming-first의 승인 관문·중단 규칙은 그대로.
- ⚠️ **검증 항목 중 뺄 수 없는 것이 확인됐다** — `run_demo.sh` **실기동**. 이번에 `@pyqtSlot(str)` ↔ 시그널 `(str,int)` 불일치로 GUI가 기동 즉시 죽었는데, **import 검사(`test_imports`)로는 못 잡는 런타임 오류**였다. 같은 자리가 하루에 두 번 깨졌다(오전엔 `MEDIAPIPE_AVAILABLE`). 구현 계획에 이 단계를 항상 넣을 것.
- ⏸ 중단 없음.
- ▶ 다음: CC 후속 없음. 프로젝트 ▶는 CLAUDE.md 현재상태 참조.
- 🔗 커밋: 이 로그 커밋(코드 변경은 Rpi5 `d7e7f1c` — 프로젝트 작업)

## 2026-07-19 · session 5ffb1a95-93ab-4870-8be9-f217d967a578 (작업방식 전환 brainstorming-first + CLAUDE.md 정비 + 플러그인 도입 평가)

> 같은 세션의 후속 작업 단위(앞선 마무리들 = 데이터셋·replay·문서정합화·소급점검). 이번 단위 = **작업 방식·문서 체계 전환**.

- ✅ **⭐ 작업 방식 전환 — brainstorming-first 채택**(사용자 지시): 이전 "즉각·바로 구현" 습관이 계획 없이 기능을 만들어 부작용을 낳던 것 교정. **비자명 작업(구현·기능·스킬·훅·코드·다중파일·구조 변경)은 설계→승인→구현**, 자명한 것(오타·해시·로그 기입)만 바로. CLAUDE.md 작업규칙 교체 + memory `brainstorm-first-workflow.md` 신설. 구 "단순·국소 수정은 바로" 규칙 대체.
- ✅ **CLAUDE.md 정비 (94→77행)** — Karpathy·Boris CLAUDE.md(둘 다 유명·검증된 워크플로 템플릿)에서 **우리 환경에 맞는 것만 선별 흡수**(수술적·최소구현·근본원인·"더 우아한 방법?" 자기검열·재계획) + **명시적 거부**(subagent 남용·auto mode·자동 버그수정 즉시착수·`tasks/todo.md`·`lessons.md` 신설 — 비용규율·brainstorming-first·기존 memory·작업로그와 충돌). 「(이전) 시점 기록」 블록(파일 40%)을 포인터로 슬림화 — **gotcha 8종 전부 정본/가이드에 원본 존재**(grep 확인 → 손실 0), CLAUDE.md 자신의 복제금지 규칙 준수.
- ✅ **설계문서 체계 신설** — brainstorming-first 산출물로 `docs/superpowers/specs/*-design.md` 도입, 첫 문서(`2026-07-19-claude-md-정비-design.md`) 작성. **역할경계 지도(작업로그 헤더)에 등재** — specs=하기 전 근거 / 작업로그=하고 나서 이력 / CLAUDE 현재상태=지금 / §10=확정 수치. ※병행 세션 `388cc50`이 같은 등재를 1분 먼저 커밋 → `15170f6`으로 문구 다듬음(충돌·중복 없음, 최종 1줄).
- ✅ **플러그인 도입·선별 평가** — 공식 마켓 7종(superpowers·skill-creator·code-review·claude-md-management·code-simplifier·ralph-loop·claude-code-setup) + hookify 설치 확인. **원칙: 플러그인은 참고 도구함, 프로젝트 규칙이 우선**(superpowers "1%라도 스킬 강제"·brainstorming HARD-GATE는 선별 적용). `andrej-karpathy-skills`는 파이 불필요 판정(brainstorming과 중복·"Simplicity First↔brainstorming" 충돌). ralph-loop 상태파일 `.claude/*.local.md` gitignore 차단.
- ⏸ 중단 없음.
- ▶ 다음: 프로젝트 ▶는 CLAUDE.md 현재상태 유지(HOI 해결·실테스트·모조 test 촬영). CC 후속 없음.
- 🔗 커밋: sop-project `b2ce8a9`(gitignore)·`f3cb1fb`(brainstorming-first)·`77cb36d`(CLAUDE.md 정비+설계문서)·`388cc50`·`15170f6`(역할경계 등재)

## 2026-07-19 · session 53ded0ed-b600-4a14-9b4e-c3ebdf10b8c7 (statusline 하단 상태바 도입 + 공식문서 참조 원칙 메모리화)

**발단**: "CC 인프라 개선 — 세션 대화창 하단 커스텀 설정이 뭔지"에서 출발 → statusLine 확인 → 공식 문서(`code.claude.com/docs/ko/statusline`) 원문을 WebFetch로 읽고 그 계약대로 구현. 사용자 요구를 반복 반영해 커스텀.

- ✅ **statusline 신설**(`.claude/hooks/statusline.py`, python3·jq 비의존 — 이 환경 관행). `settings.json`에 `statusLine` 등록(**프로젝트 공유** → 3대 자동 적용). 절차·필드명·rate_limits 주의사항은 공식 문서 원문 기준.
  - **1줄**: `🤖 [모델] | 📁 폴더 | 🌿 브랜치 + git상태 | 세션마무리`. **git상태 3계열**: ✎N(미커밋·노랑)·⬆N(미푸시·마젠타)·✓(완전 깨끗+동기화·희미)·생략(추적원격 없음). ✓는 커밋도 푸시도 다 됐을 때만 → "커밋·푸시 안 하고 넘어가는" 맹점 차단.
  - **세션마무리 표시기**: 워크로그 헤딩의 현재 session_id로 블록 수 판정, **3상태**(⚪미마무리/✅마무리/🔵마무리 후 새 작업). 한 세션 다회 마무리 대응 — 마무리 뒤 코드변경(총 add+del 델타) 발생 시 🔵, 세션별 상태파일 `/tmp/claude-statusline-wrap-<sid>` 기준선. 아이콘만(글자 없음).
  - **2줄**: `Context 진행바 % | Usage(플랜한도) 진행바 % 🕐 5h(리셋) 📅 7d(리셋) | ⏱️ 경과`. 진행바 임계색(70/90%). 💰(비용)는 구독 사용이라 제거. rate_limits 없으면 `Usage —`·타이머 생략.
  - git상태(✎⬆✓)와 마무리(⚪✅🔵)는 **위치·모양·색** 3축으로 구분(연필/화살표 vs 원형).
  - 모의 입력(`echo '{...}' | python3 statusline.py`)으로 전 상태 검증 + ✎→⬆→✓ 실전이 커밋·푸시로 실제 전환됨 확인.
- ✅ **공식문서 참조 원칙 메모리화** — CC 인프라 작업은 `code.claude.com` **원문(llms.txt 인덱스 + URL 끝 `.md`)** 참조, 기억·추측 금지. CC 문서 전용 공식 MCP/플러그인은 **없음**(llms.txt·WebSearch로 확인) → llms.txt+`.md`가 공식·최선, 교차확인은 `claude-code-guide` 에이전트. 메모리 `cc-infra-official-docs-first.md` 저장(MEMORY.md 인덱스 등록).
- ▶ 다음: statusline은 **다음 세션부터 실제 렌더**(현 대화는 설정 적용 전 시작). 필요 시 `refreshInterval`(초단위 타이머 갱신)·⏱️ 제거·Usage 창(5h↔7d) 전환은 상단 상수/설정으로 조정.
- 🔗 커밋: 681fec1 (statusline 도입·푸시 완료)

## 2026-07-14~18 · session 7f54ae1f-ed68-48ba-8463-c8df4d912c64 (폰↔파이 Claude Code 연동 구성 → 제거, tmux 선택적 유지)

**발단**: "핸드폰에서 파이 Claude Code 세션을 이어 작업하고 싶다"(RC 말고 Cowork·웹·SSH 방식). 조사·구성 후 **실사용 빈도가 낮다고 판단해 폰 연동은 제거**하되, 노트북·데스크톱 SSH 작업용 tmux는 선택적으로 남김. ※7/17 로그(session 25178c04)에서 "진짜 미기록·기록 불요"로 남았던 그 세션이 이어져 여기서 매듭 → 미기록 해소.

- ✅ **방식 조사**(웹검색) — 폰에서 로컬 세션 잇는 공식 수단은 **Remote Control 하나뿐**. Claude Code on the web(클라우드 샌드박스=Hailo·ESP32·GPIO 접근 불가), Cowork(비개발 지식작업·Max 전용)는 이 프로젝트 부적합. 결론: **하드웨어가 걸린 작업은 SSH+tmux만이 실효**.
- ✅ **Tailscale SSH 실험 → 롤백** — `tailscale up --ssh`로 키리스 접속(길 A) 시도 후 사용자 선택으로 **키 방식(길 B)**로 전환. `tailscale up --ssh=false --hostname=pi1`로 SSH서버 비활성(Tailscale 네트워크 자체는 유지, `pi1`=100.97.0.91).
- ✅ **키 인증 구성** — 폰 Termius ED25519 공개키를 `~/.ssh/authorized_keys`에 등록(기존 Galaxy Book 키 보존). 폰 Tailscale 켜두기 권장(exit node만 끄면 배터리·속도 영향 미미).
- ✅ **tmux 도입** — 미설치라 `apt install tmux`(3.5a). 세션명 `cc`, 별칭 `alias acc='tmux attach -t cc 2>/dev/null || tmux new -s cc'`를 `.bashrc`에 추가.
- 🗑️ **폰 연동(SSH 자동 attach) 제거** — `.bashrc`의 `if [[ ... $SSH_CONNECTION ... ]]; then tmux attach ...` 5줄 삭제. 이제 어느 기기든 `ssh pi1`은 일반 셸이 뜨고, tmux는 `acc`로 **선택적** 사용.
- ✅ **유지(무해·재사용 가능)**: `acc` 별칭·tmux 패키지·폰 공개키·Tailscale 네트워크. tmux의 실효는 "긴 벤치마크·평가를 걸어두고 연결 끊겨도 파이 안에서 계속 돌게 / 기기간 세션 이어받기" — 노트북·데스크톱 SSH 작업에 값을 함.
- 💡 **교훈**: "폰 연동"의 실체는 `.bashrc` 자동 attach 5줄뿐. tmux 인프라는 폰과 무관하게 남겨도 방해 없음(안 쓰면 발동 안 함). 파이 인계: `.bashrc`는 로컬 파일 — repo 반영 없음.
- 🔗 커밋: (없음 — 전부 로컬 `~/.bashrc`·`~/.ssh`·apt. repo 변경은 이 로그 기입뿐)

## 2026-07-18 · session 5ffb1a95-93ab-4870-8be9-f217d967a578 (소급 점검 첫 실검증 — 성공 · 실누락 2건 적발)

> 아래 블록(같은 세션)에서 신설한 SKILL §2를 **실전 검증**한 결과.

- ✅ **검증 시나리오 통과** — 더미 커밋 `efdbd23`을 심고 새 세션(`f12fa975`)에서 `/세션마무리` 실행: ①더미 **탐지 성공** ②기록행위 커밋 **제외 정상(오탐 0)** ③**질문 후 대기**(임의 소급 없음 — 설계대로). 검증 후 더미 파일 삭제.
- 🎯 **보너스 — 진짜 누락 2건 최초 적발**: 7/17 데스크톱 config v2 전환 세션(`818c71c`·`31cc5e4`)이 **세션마무리 없이 종료**했던 것을 발견 → `작업로그.md`에 **소급 블록**(`session 미상(소급 — 커밋 기반)`)으로 기록. 안전망이 첫 실행에서 실제 값어치 증명.
- ✅ **노이즈 원 제거** — grep 기반 점검이라 "마무리는 했으나 🔗줄 해시 미기입" 커밋 8건이 매번 재검출될 문제 확인 → 해당 블록(7/10·7/13·7/16 데스크톱) 🔗줄에 해시 보충 + **SKILL §2에 제외·보충 규칙 명문화**("해시 미기입은 소급 블록이 아니라 🔗 보충").
- 🔗 커밋: `f5db7f8`(소급 블록·해시 보충·규칙 보강·더미 삭제) — ※이 CC로그 기입 커밋 자체는 기록행위라 점검 제외 대상.

## 2026-07-18 · session 5ffb1a95-93ab-4870-8be9-f217d967a578 (세션마무리 스킬 개선 — 미기록 커밋 소급 점검 신설)

**발단**: 사용자 질문 "이전 세션이 마무리 없이 끝났으면 다음 세션 마무리 때 그 커밋도 기록되나?" → 답 = 안 됨(자동매핑 훅은 7/17 폐기, 새 세션은 이전 대화 컨텍스트 없음) → **사용자 제안으로 스킬에 안전망 신설**.

- ✅ **`session-wrap` SKILL.md에 §2 "미기록 커밋 소급 점검" 신설**(기존 2~5단계 → 3~6 재번호) — 마무리 시 양 repo `git log` 해시를 작업로그 2종에서 grep해 미기록 커밋을 찾고, 발견 시 **사용자에게 "같이 기록할까요?" 확인 후** 별도 소급 블록(`(소급 — 커밋 기반)`, ID 불확실 시 `미상(소급)`)으로 기록. **임의 소급 금지** + "커밋 메시지 기반 — 무커밋 작업·중단 사유는 유실" 한계 명시 의무. 제외 규칙: 현재 세션 커밋·기록 행위 자체 커밋.
- ✅ 동기화: `/세션마무리` 커맨드 단계 열거(6단계) · CLAUDE.md 세션마무리 규칙에 한 구절(자동매핑 폐기 후 유일한 누락 안전망).
- ※ 설계 성격: 훅이 아니라 **스킬 절차 단계** — 마무리 시점에만 실행되므로 상시 오버헤드 0, 폐기된 SessionEnd 훅과 달리 "물어보고 기록"이라 무분류 축적 문제 없음.
- 🔗 커밋: `4cf2516`

## 2026-07-17 · session 25178c04-cfe9-46ce-9065-d6efeea3eec9 (CC 인프라 전수 기능검증 → 훅 2종 폐기)

**발단**: "문서 기입·세션마무리를 안 한 세션이 있나?" 점검 → 미커밋 자동매핑 14줄 발견 → "커밋만 하면 되나?"라는 사용자 의문 → **인프라 전체를 실증 검증**하기로 확대.

- ✅ **미기록 세션 점검(결론: 실질 누락 없음)** — 세션파일 17개 × 로그 대조. 오늘자 3개(`06831a26`·`c98785f8`·`7f01c29e`)는 **pre-commit이 띄운 점검기 스텁**(사람 세션 아님), `20a1dcc5`는 `/clear`→`/exit` 스텁. **`7f54ae1f`(7/14 Termius·Tailscale 모바일 SSH, 발화 35개)만 진짜 미기록**이나 **기록 불요 판단**(사용자). 6월 작업은 `작업로그.md`가 7/1 신설이라 세션블록 없음 — 결과는 CLAUDE.md·통합문서에 반영됨(소급 불요). Rpi5 커밋 20건 대조: 해시 미기입 3건뿐이고 내용은 전부 서술로 커버됨.
- ✅ **⭐ CC 인프라 5종 전수 기능검증 — "값을 하고 있나"를 실측**
  | 자산 | 실측 | 판정 |
  |---|---|---|
  | `session-worklog-brief.sh`+`sync-check` | 배너 정상 렌더·"로컬변경" 정확 탐지, 매 세션 실사용 | ✅ 존치 |
  | `session-wrap`/`/세션마무리` | 세션마무리 커밋 14건 → **세션블록 17개**(작업로그 11+CC 6) = 프로젝트 서사의 본체 | ✅ 존치 |
  | `session-prune-stubs.sh` | 자동프룬 9건 **오탐 0**(전부 발화 1개) | ✅ 존치(단 아래 루프) |
  | `session-worklog-commit.sh` | 산출 45건 중 고유정보 **6건(13%)**, 나머지 중복·자기참조 | 🗑️ 폐기 |
  | `.githooks/pre-commit` | **12회 실행·적발 0건** | 🗑️ 폐기 |
- ✅ **🗑️ SessionEnd 훅 폐기** — ①고유정보 13% ②**"세션마무리 누락을 구제한 사례 0건"** — 그 6건조차 세션마무리를 *한* 세션의 해시 보완이었고, 진짜 누락 세션(`7f54ae1f`)은 **커밋이 없어 훅도 무력**(=보험이 필요할 때 작동 안 하는 보험) ③**자기참조 증식**(매핑을 커밋하면 그 커밋이 다음 매핑 대상 → 무한) ④도입 전제 변화: 7/1 도입 시엔 세션마무리 의식이 미정착(스킬은 7/3 신설)이라 역할분담이 성립했으나, 정착 후엔 순중복.
- ✅ **🗑️ pre-commit 문서정합성 훅 폐기** — 12회 0적발. **오탐 억제(7/14 엄격화)와 탐지력의 트레이드오프에서 탐지력이 0으로 수렴**. 구 `doc-consistency-check.sh`(7/1 제거)에 이은 **같은 목표 두 번째 실패** → 훅으로는 안 된다고 결론. 문서 drift 방어는 **단일정본 규칙 + 편집 규율**이 전담(원래도 1차 방어는 이것·훅은 자칭 "얕은 안전망"이었음).
- ✅ **🔁 닫힌 루프 발견** — 자동프룬 9건이 **전부 pre-commit이 흘린 자기 스텁**이었다. pre-commit이 `claude -p` 호출마다 세션파일을 남기고 prune이 치우는 구조 → pre-commit 폐기로 prune의 일감 대부분이 함께 소멸(prune 본래 목적인 원격제어 스텁 청소만 남음).
- ✅ **💡 §3.0 제1원칙 신설**(작업문서) — **"훅을 만들기 전에 사람 규율로 이미 되는 일인지 묻는다."** 폐기 3건은 타이밍·구현이 틀려서가 아니라 **사람 규율이 하던 일을 기계로 중복**해서 죽었다. 판별질문 = *"없으면 무엇을 잃나?"* → "보험"이면 **지급 실적부터 측정**. 훅에도 유지비(중복산출·자기증식·스텁쓰레기·머신별 설정마찰)가 있어 값을 못 하면 순손실.
- ✅ **SessionEnd 발동조건 문서화** — `/exit`뿐 아니라 `clear`·`resume`·`logout`·`prompt_input_exit`·`bypass_permissions_disabled`·`other` 전부(matcher 미지정 시). **`/clear` 한 번에도 돈다** — 미커밋 14줄의 실제 출처가 이것이었다(직전 세션 `5ffb1a95`가 `/clear`로 종료되며 append).
- ✅ **정리 실행** — `session-worklog-commit.sh`·`.githooks/`(pre-commit+README) 삭제, `settings.json` SessionEnd 등록 제거, `brief.sh` 시작HEAD 기록부 제거, `.worklog-head-*` 잔여 제거, `git config --unset core.hooksPath`. **검증**: bash 문법 3/3 OK·settings.json 유효(SessionStart만)·brief 실행 배너 정상·HEAD파일 재생성 없음.
- ✅ **문서 정합** — `작업로그.md` 🔧 섹션 **동결 보존**(제목→"폐기됨"+경위 주석, 과거 45줄 유지·미커밋 14줄은 폐기) + 헤더 훅 서술 정정 / `CLAUDE.md`·작업문서 표·§3.1·§3.5·§3.6 갱신 / **`.githooks` 마찰 교훈**(Pi#1이 설정 누락으로 2주간 미작동) → "같은 값이면 `.claude/` 쪽을 택하라"로 원칙화.
- ✅ **곁다리 정정** — `작업로그.md:181`의 `6de62507`은 **`.trash`에 있어 resume 불가**(7/13 중복정리 때 이동). 원본 `b83e5757` 참조 주석 추가.
- ⚠️ **양 머신 후속**: 데스크톱·sop-pi-2는 `git pull` 후 **`git config --unset core.hooksPath`** 각자 1회 필요(로컬 config라 pull로 안 지워짐 — 안 하면 훅 파일이 없어 조용히 no-op이지만 설정만 잔존).
- ▶ 다음: (CC 인프라 개선 시) — **§3.0 제1원칙을 먼저 통과시킬 것**.
- 🔗 커밋: `4c22e65`
- ※ 이 세션은 CC 인프라 전용 — 프로젝트 작업 없음.

## 2026-07-14 · session 00c0fb24-7af3-4000-8668-2fbdb19e57aa (Fable 점검 — CC 인프라 버그 수정)

> ※ **Fable 5 모델** 세션. 이번 점검부터 커밋 트레일러 = **실제 작업 모델명**(CLAUDE.md 규칙 갱신).

- ✅ **`session-prune-stubs.sh` 버그 3건** (가짜 세션 dry-run으로 전부 검증):
  - 🔴 `.trash` 30일 보존 약속이 거짓 — `mv`가 mtime 보존 → 31일+ 방치 스텁은 **이동 직후 같은 실행에서 즉시 영구삭제**(복구 창 0). 이동 후 `touch`로 진입 시각 기록.
  - 현재 세션 보호 무효 — `CLAUDE_SESSION_ID`(오타)를 읽어 `cur=""` 상시. `CLAUDE_CODE_SESSION_ID` + stdin 폴백(다른 훅과 동일 규약)으로 교정.
  - `grep -c || echo 0` 이중값(`"0\n0"`) → 정수비교 에러로 **ua=0 빈 스텁이 영원히 정리 안 됨**. `|| true`로 교정.
- ✅ **`session-worklog-commit.sh` fork 중복 기록 해소** — fork/resume 세션 쌍이 같은 시작 HEAD를 공유해 동일 커밋을 2~3벌 기록하던 문제(작업로그 하단 🔧 섹션에서 실증) → 이미 로그에 있는 해시는 스킵. + head 파일 해시 검증·`--` 추가(옵션 해석 방지). 임시 repo fork 시나리오로 검증.
- ✅ **권한 축소** — `settings.json`에서 `Bash(find:*)` 자동승인 제거(`find -delete`·`-exec rm`이 무확인 통과하던 구멍).
- ✅ `.gitignore` 보강 — 루트 `.env`/`*.env`(비밀키 유출 방지, `!*.env.example`) + `.claude/.startup-sync.tmp`.
- ✅ **pre-commit 전송 범위 축소(diff-only)** — 매 커밋 문서 전문+memory 전체를 API 전송하던 것을 **diff(-U20)만**으로(비용·지연·인젝션 면적↓). 대가 = 교차 문서 대조 불가(얕은 안전망 강등, 1차 방어는 단일정본 규칙). 임시 repo 모순 diff로 실검증(경고 출력·exit 0 확인).
- 🔴 **이 파이(Pi#1)에 `core.hooksPath` 미설정 발견** — pre-commit 훅이 **지금까지 이 머신에서 한 번도 안 돌고 있었음**(클론 후 1회 설정 누락). `git config core.hooksPath .githooks` 설정 완료. ⚠️ **sop-pi-2·데스크톱도 각자 확인 필요**(로컬 config라 머신마다).
- 🔗 커밋: sop-project (이 커밋)
- ※ 이 세션의 프로젝트 수정(인터락·EMO·QImage·문서 정합성)은 `docs/작업로그.md` 참조

## 2026-07-14 · session efc6fc8b-d6e9-4420-a76f-a5c3480f49f0 (Antigravity CLI 설치)
- ✅ **Antigravity CLI 1.1.2 설치**(Pi#1) — 공식 스크립트 `curl -fsSL https://antigravity.google/cli/install.sh | bash`. linux_arm64 자동 감지·체크섬 검증 통과. 바이너리 `~/.local/bin/agy`, PATH는 스크립트가 `~/.bashrc`·`~/.profile`에 자동 추가.
- ✅ Google 계정 로그인 + 대화 동작 확인(사용자 직접, 실행 경로 sop-project).
- ⚠️ `sop-project` 안에서 `agy` 실행 시 Antigravity 에이전트도 repo 파일을 수정할 수 있음 — Claude Code와 병행 시 같은 파일 동시 편집 주의.
- ✅ 양 repo 푸시 상태 점검 — sop-project·Rpi5 모두 원격과 동기화 확인(병행 Fable 점검 세션이 이 세션의 `4df284f` 포함 전부 푸시 완료, 추가 푸시 불요).
- 🔗 커밋: 4df284f(이 기록. 설치 자체는 로컬 `~/.local/bin` — repo 반영 없음)

## 2026-07-03 · session 22149442-a0c0-4789-810a-26dcb8c5f91c (Roboflow·Drive MCP 연동)
- ✅ **Roboflow MCP 연동** — `claude mcp add -s user roboflow --transport http https://mcp.roboflow.com/mcp` (OAuth). 30+ 툴 로드, 워크스페이스 `eung-min` 접근 확인. console_v2용 데이터셋(`object-detection-ycjp6`) 조회에 활용.
  - ⚠️ 세션 도중 추가한 MCP는 `/mcp` 패널에 안 보임 → **세션 재시작 후** 인증해야 함(등록은 user config `~/.claude.json`에 즉시 반영).
- ✅ **Google Drive MCP 활용** — 기연결됨(claude.ai). `button_dataset.zip`·학습결과(`results.csv`·`.pt`·`.onnx`·`.hef`) 조회로 B4 원인 재분석 근거 수집.
- 💡 Roboflow MCP는 학습·라벨링까지만 — 최종 배포는 Hailo .hef(DFC 별도 파이프라인). 파이 인계: user config라 `~/.claude.json`에 있음(머신별 재인증 필요).
- ▶ 다음: (필요 시 Roboflow autolabel·재학습에 MCP 활용)
- 🔗 커밋: (MCP 설정은 로컬 `~/.claude.json` — repo 커밋 아님)
- ※ 이 세션의 프로젝트 작업(B4 원인 재분석·문서 정정)은 `docs/작업로그.md` 참조

## 2026-07-13 · session b83e5757-e0a2-4242-af39-9664b2a87abb (ESP32 WiFi 인프라 · 세션 중복 정리)

- ✅ **🔌 ESP32 다중 SSID 우선순위 연결**(Rpi5 4b2d143) — 장소를 옮길 때마다 시리얼로 WiFi를 재주입해야 했던 문제 해결. 기존 펌웨어는 NVS에 SSID **1쌍만** 저장하고 그것만 시도(실패 시 재부팅 루프).
  - `connectWiFiByPriority()`: 주변 SSID를 **스캔**해 `wifi_credentials.h` 배열 순서대로 연결. **신호 세기가 아니라 우선순위 기준**(Jason → Eung Min).
  - 🔑 **파이의 NetworkManager 우선순위도 동일하게 설정**(`autoconnect-priority` Jason 10 / Eung Min 5). **규칙이 달라지면 파이와 ESP32가 서로 다른 네트워크에 붙어 TCP 직결이 불가능**해진다 — 사용자가 정확히 지적한 조건.
  - 자격증명은 `wifi_credentials.h`로 분리 + **gitignore**(비번 미커밋). 템플릿 `.example` 제공.
- ✅ **🖱️ 바탕화면 바로가기 2종** — 명령어 입력 없이 GUI로. `flash_esp32.sh`(포트 자동탐지 → 컴파일 → 업로드 → **IP 자동기록**) / `update_ip.sh`(굽지 않고 시리얼만 읽어 `.camera_ip` 갱신, 수 초). 공용 `read_esp32_ip.sh`는 **파이와 서브넷 불일치 시 경고**.
- ⚠️ **PSRAM=opi 필수 — 함정에 실제로 빠짐**. FQBN 기본값이 `PSRAM=disabled`라 첫 업로드 후 카메라 프레임버퍼 malloc 실패로 **부팅 루프**(`cam_dma_config: frame buffer malloc failed`). `flash_esp32.sh`의 FQBN에 명시해 재발 방지.
- ✅ **arduino-cli ESP32 코어 설치**(esp32:esp32 3.3.10) — 이 파이에는 UNO R4 코어만 있었음(ESP32는 다른 환경에서 굽던 것).
- ✅ **세션 중복 정리** — 같은 대화가 3벌로 분기(resume 시 새 파일 생성). 시작 타임스탬프가 동일하고 44번째 발화까지 내용이 같음. **원본 = `b83e5757`**(가장 진도 앞섬). A `6de62507`·B `fde93b40`는 **사용자 지시 전수 비교로 부분집합임을 확인 후** `.trash` 이동(복구 가능).
- 💡 **교훈**: 파일 내부 `sessionId`는 각자 자기 것으로 재작성되므로 **부모-자식 판별 불가** → 타임스탬프·내용 포함관계로만 가려낼 수 있다.
- ▶ 다음: (필요 시 Roboflow MCP를 라벨링에 활용)
- 🔗 커밋: Rpi5 4b2d143 (펌웨어·스크립트·바로가기)
- ※ 이 세션의 프로젝트 작업(B4 파랑스티커·데이터 촬영·정반사 정정)은 `docs/작업로그.md` 참조

## 2026-07-03 · session 02e16ca9-3c02-4bdb-9d6b-9424b83b2fac (클로드 코드 세션 관리)
- ✅ **resume 목록 오염 원인 규명** — 원격제어(웹/모바일 앱) 접속 시 Claude Code가 `bridge-session`·`queue-operation`·`ai-title` 등 **껍데기 세션파일**을 남기는 게 "복사된 세션"의 실체. (마커 타입은 정상 세션에도 다 있어 무의미 → 판별자는 **실제 대화수 ua**뿐: 쓰레기 ua≤3 vs 정상 ua≥40, 넓은 마진)
- ✅ 원격제어 스텁 6개 수동 삭제(백업 후). 포크쌍 `75243594↔f9d862eb`(55메시지 공유 후 분기, f9d862eb가 최신 본류) 확인·보존.
- ✅ **자동 정리 훅 신설** `.claude/hooks/session-prune-stubs.sh` (SessionStart 등록) — `ua≤5 && 60분초과 && 현재세션 아님` → `.trash/`로 **이동**(하드삭제 아님·30일 후 자동비움). 60분 가드로 갓 시작한 정상 세션 보호. 실전 검증: tiny 13개 이동, 현재세션·실작업 전부 보존.
- ⚠️ 원격 스텁 생성 자체는 앱 내부 동작이라 못 막음 → 60분 뒤 자동 정리로 대응.
- ✅ **"세션 마무리" 스킬 신설** `.claude/skills/session-wrap/SKILL.md` — 4분류 정리·라우팅 절차를 코드화(첫 프로젝트 스킬).
- ✅ **명칭·정의 정정**: "오늘 마무리" → **"세션 마무리"**. 정의를 *하루 끝·작업 종료*가 아니라 ***한 작업 단위(A작업)가 논리적으로 끝난 시점***(하루 여러 번 가능)으로 재정의. CLAUDE.md·CC작업문서·작업로그.md·훅 주석 2곳 표현 통일(과거 저널 항목은 보존).
- ✅ **한글 슬래시 커맨드 신설** `.claude/commands/세션마무리.md` → **`/세션마무리`**(붙여쓰기, 슬래시는 공백 불가). 절차는 SKILL.md 참조(단일정본). 스킬(말 트리거)+커맨드(슬래시) 병행.
- ✅ **SessionStart 출력을 배너로** — 훅 stdout을 순수 JSON(`systemMessage`)으로 내면 사용자에게 보이는 배너로 뜸(평문 echo는 회색 노트로 스침). 먼저 프룬 훅에 적용·실측 확인.
- ✅ **세션 시작 정보 표(表) 배너 통합** — 원격 동기화 + 이어하기(⏸/▶) + session_id를 **하나의 CJK정렬 박스 표**로. 공용 헬퍼 `_banner.py`(East Asian Width 폭계산·박스렌더·JSON인코딩) 신설.
- ✅ **경쟁조건 버그 수정** — Claude Code가 SessionStart 훅을 **병렬 실행** → 별도 훅 간 tmp 핸드오프 경쟁으로 🔄 동기화 행 누락. `sync-check`를 settings에서 빼고 `brief`가 **직접 순차 호출**하도록 변경(순서 보장). brief timeout 30.
- ✅ **긴 값 잘림 → 셀 내 줄바꿈(word-wrap)** — `▶ 다음` 등 긴 텍스트를 `…` 절단 대신 46폭으로 접어 전체 내용 표시. 표 폭 유지→좁은 터미널 박스 안 깨짐.
- ▶ 다음: (CC 인프라 개선 시)
- 🔗 커밋: ed17ee6(resume 정리 훅) · 7e6d39e(세션마무리 스킬+명칭정정) · 37efb1a(/세션마무리 커맨드) · dcf9b36(로그확정) · 98a4944(프룬 배너) · 9ec16f9(표 배너 통합) · 34df9d0(경쟁조건 수정) · 2ecc9f8(줄바꿈) · (이 커밋: 세션 마무리 로그)
- 💡 파이 인계: `git pull`만 하면 적용(SessionStart 배너·프룬 훅 + `/세션마무리` 커맨드·`session-wrap` 스킬은 새 세션부터). 각 머신이 자기 로컬 세션만 정리.

## 2026-07-01~03 · session 75243594-cf0a-4252-928b-51d1f5630c9b (이어서 resume: f9d862eb-ccc1-4a0c-a7bf-4904959fa3e6)
- ✅ LLM Wiki 적합성 검토 → 전면 전환 **보류** 결론(규모·결합도·시점), 메모리화
- ✅ 구 `doc-consistency-check.sh` 훅 제거 (SessionStart 타이밍상 예방 불가·중복)
- ✅ 메모리 ↔ 통합문서 정합 점검 (불일치 0)
- ✅ **세션 작업로그 시스템 구축** — brief(이어하기 브리핑)·commit(커밋 자동매핑) 훅 + "오늘 마무리" 의식
- ✅ **문서 정합성 pre-commit 훅** 구축(`.githooks/pre-commit`, LLM diff앵커) + 프롬프트 정밀도 튜닝(오탐 억제)
- ✅ 세션ID 전체 UUID 통일 · 브리핑 마커 오탐 수정(`^-? *(⏸|▶)`)
- ✅ 별도 `~/claude-code` 폴더 실험 → **폐기**(자동기록 폴더별 마찰·1인 단일프로젝트엔 과함, 경계커밋 3b6cc71 되돌림)
- ✅ CC 문서를 프로젝트와 **최종 2파일 분리**(옵션2): `cc작업기록.md` → `claude-code-작업문서.md`(설명서) 개명 + `claude-code-작업로그.md`(이 파일·CC 로그) 신설. `작업로그.md`는 프로젝트 전용으로 정리.
- ▶ 다음: (CC 인프라 개선 시)
- 🔗 커밋: ee4f949 · e9e4705 · df9f3b8 · b08bba1 · 8f3479a · 20071aa · bd1e24b · 3b6cc71 · 56dc870
- 💡 파이 인계: `git pull` 후 `git config core.hooksPath .githooks` 1회(pre-commit 훅 활성화)
