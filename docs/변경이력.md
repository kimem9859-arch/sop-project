# 변경이력 — 통합문서의 사실이 어떻게 바뀌어 왔나

> 🔴 **여기에는 폐기된 사실만 있다.** 지금 맞는 것은 [`통합문서.md`](통합문서.md) 가 정본이다. 이 문서에 현행 값은 적지 않는다 — 각 절 머리의 `> 현행 = §N` 을 따라간다.
> 🔴 **측정 수치는 여기 없다** — §12 절의 `⛔ SUPERSEDED-BY` 마커와 갈래 색인이 정본이다. 승계 사슬·색인을 여기 복제하지 않는다.
> **정렬은 주제순**이다. 그날 무엇을 했나(시간순) = [`작업로그.md`](작업로그.md) · 작업 전 설계·버린 대안 = [`superpowers/specs/`](superpowers/specs/).
>
> 🆕 **쓰는 법** — 통합문서의 현행 사실을 덮어쓸 때 **지우기 전에** 여기로 옮긴다(`work-close` 2.4). 주제 절은 **맨 뒤에 덧붙이기만** 하고 지운 번호는 재사용하지 않는다. 열은 5개 고정이며 「왜」는 한 줄, 상세는 근거로 넘긴다(R9).
> 🔑 **왜 나누나** — 현행과 폐기가 한 문서에 섞이면 `grep` 이 **폐기된 줄을 현행으로** 뱉는다(실제 사고 사례 있음). 현행은 통합문서, 폐기는 이 문서. 관련 규칙 = **R2**(폐기는 옮긴다 · §12 예외) · **R3**(나누는 축은 현행/폐기 하나) · **R12**(갈 곳 명시).
> spec: `docs/superpowers/specs/2026-09-18-변경이력-문서-design.md` · plan: `docs/superpowers/plans/2026-09-18-변경이력-문서.md`

---

## 1. 버튼 구성·수량

> 현행 = 통합문서 §14.1(수량·색) · §5.4(배치)

| 시점 | 폐기된 사실 | 폐기 시점 | 왜 | 근거 |
| --- | --- | --- | --- | --- |
| 2026-06-09 | 아케이드 버튼 **8개**(4색 ×2개씩) — 좌 2×2 사용(B1~B4) / 우 2×2 미사용(확장용) | 2026-08-06 | 실콘솔은 확장 2×2 를 두지 않기로 확정했다 | `65534cb` |

🔴 **이 줄이 2026-08-29 오정정의 원인이었다** — 실물은 2026-08-06 에 확정됐는데 통합문서 4곳이 3주간 옛 수량인 채였고, 제출문서 작업이 그 낡은 서술을 근거로 잘못 정정했다.

## 2. 버튼 배치

> 현행 = 통합문서 §5.4 「⭐ 버튼 배치도 — 실콘솔 전면부 포맥스판」

| 시점 | 폐기된 사실 | 폐기 시점 | 왜 | 근거 |
| --- | --- | --- | --- | --- |
| 2026-06-09 | 배치도 **Z자** — 상단열 `[B1 노랑][B2 흰]` · 하단열 `[B3 핑크][B4 검정]` | 2026-08-10 | 이 배치도가 **모조 콘솔 실물과 어긋나 있었다**(실물·학습 데이터가 정본) | §12.32 |
| 2026-08-06 | 포맥스판 제작 당시 배치 — 위 Z자를 그대로 따라 제작 | 2026-08-10 | `console_v2` 학습 분포와 **세 자리가 어긋나** B3→B1 오분류가 났다 | §12.33 |

## 3. 터치 디스플레이 입력

> 현행 = 통합문서 §14.1

| 시점 | 폐기된 사실 | 폐기 시점 | 왜 | 근거 |
| --- | --- | --- | --- | --- |
| 2026-06 | 제우스 Z10 = **출력 전용**(입력 아님), 해제 버튼 UI 만 여기 배치 | 2026-08-29 | 오기였다 — 경고·차단 해제를 작업자가 화면 터치로 한다 | `65534cb` |

## 4. 시연에 쓰는 콘솔

> 현행 = 통합문서 §5.4(실콘솔 전면부 포맥스판) · §13.1(구성 블록도)

🔴 **「시연용 모조품」과 「모조 콘솔」은 다른 말이다.** 실콘솔도 실제 PECVD 장비가 아니므로 **시연용 모조품**이라는 프로젝트 정의(CLAUDE.md)는 그대로다. 아래에서 폐기된 것은 **초기 2×2·8버튼 시제품(「모조 콘솔」)을 쓰는 것**이다.

| 시점 | 폐기된 사실 | 폐기 시점 | 왜 | 근거 |
| --- | --- | --- | --- | --- |
| 2026-06 | 시연·촬영에 **모조 콘솔**(2×2 8버튼 · 박스 + 검정 스프레이)을 쓴다 | 2026-09-04 | 실콘솔로 본격 전환했다 | 사용자 확인 2026-09-18 |
| 2026-08-06 | 실콘솔 전면부는 **프레임 조립 전**이다(버튼·배선만 완료) | 2026-09-04 이전(일자 미상) | 조립을 마치고 실사용에 들어갔다 | 〃 |

⚠️ **실콘솔 프레임 완성 일자는 기록이 없다** — 추측해 적지 않는다.
🔴 **§12 측정값은 아직 모조 콘솔 조건이다** — 실콘솔 재측정이 미실시라 폐기 표시하지 않는다. 재측정하면 R6대로 새 절 + `⛔ SUPERSEDED-BY` 로 처리한다(§12 🔓 미결).

## 5. 요구사항·목표 기재

> 현행 = 통합문서 §4.1(목표) · §4.2(기능 요구사항)

| 시점 | 폐기된 사실 | 폐기 시점 | 왜 | 근거 |
| --- | --- | --- | --- | --- |
| 2026-06 | FR-2 「위반 시 청각(타워램프·**부저**)」 | 2026-09-19 | 코드와 달랐다 — WARN 은 부저 OFF, 부저는 BLOCK 전용 | `console_interlock.ino` |
| 2026-06 | 시스템 요구사항 한 줄 「FPV 단일 비전 + FSM 6상태 + 인터락 아키텍처를 구현한다 … **위반 강행 시 인터락으로 전기 입력을 차단하는 구조를 갖춘다**」 | 2026-09-19 | 조건 없는 단정이었다(CH5 실물 미동작) · 내용은 §4.1 목표로 흡수 | §12 🔓 미결 |
| 2026-06 | 사용자 요구사항 한 줄 「작업자가 잘못된 순서를 시도하면 시스템이 이를 감지하고, 시청각으로 피드백하며, 필요 시 입력을 차단한다」 | 2026-09-19 | FR-1~4 의 되풀이였다 | — |
| 2026-06 | FR-3 안의 「시연용 모조품이라 시연에서는 타워램프로 시각화한다」 | 2026-09-19 | 요구사항이 아니라 구현 방식 — §7·§8 에 이미 있다 | — |
| 2026-06-13 | 비기능 요구사항을 **별도 장(§11)** 으로 둔다 | 2026-09-19 | 목표가 §4 와 §11 두 곳에 나뉘어 있었다 — §4.1 로 통합하고 §11 은 결번 | §16.5 |

⚠️ **NFR-1 칸에 있던 측정값**(추론+HOI fps·펌웨어 3줄 등)은 폐기가 아니라 **자리가 틀렸던 것**이다 — §12 `[CURRENT]` 가 정본이고 §4.1 에는 목표값만 둔다(R5).

## 6. 인식 파이프라인 기재

> 현행 = 통합문서 §6.1(스택·임계) · §6.2(HOI 명세)

| 시점 | 폐기된 사실 | 폐기 시점 | 왜 | 근거 |
| --- | --- | --- | --- | --- |
| 2026-06 | HOI 신뢰도 **이중 임계 0.70 / 0.55**(설계 초안값) | 2026-09-15 | 재정의하지 않기로 했다 — 운용 임계는 세 값(버튼 conf·손 랜드마크·팜 내부) | 사용자 결정 |
| 2026-06 | 검출 대상이 **person · 객체 바운딩 박스**다 | 2026-09-19 | 현행 클래스는 **버튼 5종(B1~B4·EMO)** 이고 person 은 없다 | `detector.py:103` |
| 2026-06-13 | QVGA(320) 다운 **발동 조건 충족**(HOI 포함 11.0fps) | 2026-09-19 | NFR-1(15fps)이 충족되어 발동하지 않는다 | §12 · §4.1 |

## 7. 판정(FSM) 기재

> 현행 = 통합문서 §7.1(상태) · §7.2(전이도) · §7.3(시나리오) · §7.4(임계)

| 시점 | 폐기된 사실 | 폐기 시점 | 왜 | 근거 |
| --- | --- | --- | --- | --- |
| 2026-06 | 해제 버튼은 **WARNING용·BLOCK용 별도의 두 버튼**이다 | 2026-09-19 | 화면의 해제 버튼은 **하나**이고 배너 상태로 복귀 지점이 갈린다 | `overlay.py` · `safety_console.py` |
| 2026-06 | READY → PROCESS RUN 전이 조건이 **「레시피 로드 완료」** 다 | 2026-09-19 | READY 는 머무는 상태가 아니라 **경유 지점**이다 | `fsm.py` `_step_complete` |
| 2026-06-07 | 타이머 근거의 **dwell 0.5초** | 2026-09-19 | 현행 값은 §7.4 가 정본이다 (갭메우기는 폐기가 아니라 현행 — 값 §7.4) | `config.py` |

## 8. 통신·확장 기재

> 현행 = 통합문서 §10.3(WiFi 접속 흐름) · §10.1(확장 항목)

| 시점 | 폐기된 사실 | 폐기 시점 | 왜 | 근거 |
| --- | --- | --- | --- | --- |
| 2026-06 | WiFi 접속 흐름 = **핫스팟 IP 고정 · 오류 시 1회 재접속** | 2026-09-19 | 펌웨어는 저장된 SSID 우선순위 스캔 → **실패 시 무한 재스캔**이고, 경로는 공유기다 | `glass_voice.ino` |
| 2026-06 | 작업 완료 제스처를 **MediaPipe `Thumb_Up`** 으로 한다 | 2026-09-19 | 현 스택에 MediaPipe 프레임워크가 없다(손 모델만 Hailo 포팅) | §6.1 |

## 9. 하드웨어·환경 기재

> 현행 = 통합문서 §14.1(확정 사양) · §14.2(환경) · §14.3(검증 진행 중)

| 시점 | 폐기된 사실 | 폐기 시점 | 왜 | 근거 |
| --- | --- | --- | --- | --- |
| 2026-08 | **sop-pi-2 가 LLM·STT·TTS 를 전담**한다 | 2026-09-19 | STT·TTS 는 **pi1** 의 `~/env/tts/.venv` 에서 돈다 — pi2 는 LLM 만 | `run_voice.sh` · `voice_assistant.py` |
| 2026-08-26 | 음성비서 하드웨어는 **발주 완료·미검증**이다 | 2026-09-19 | 입고됐고 V1·V3·V4 를 이 부품으로 수행했다 | §12.55 · §12.61 |
| 2026-08 | 글라스 본체 재고 「**메인 사용 중** + 1」(`44:1B` 지칭) | 2026-09-19 | 장착 유닛이 바뀌었다 — 현행 재고표는 §14.3 | §12.55-(5) |
| 2026-08-27 | 커패시터 **V1 미착수** | 2026-09-04 | V1 판정이 끝났다 — 결과는 §14.3 | §12.55-(3) |

## 10. 부록·BOM 기재

> 현행 = 통합문서 §16.2(BOM) · §16.4(라이선스)

| 시점 | 폐기된 사실 | 폐기 시점 | 왜 | 근거 |
| --- | --- | --- | --- | --- |
| 2026-08 | BOM 음성비서 행 「**DFR0954 ×2 / ada-3006 ×1** · 발주 완료」 | 2026-09-19 | 실제는 DFR0954 ×1 입고이고 ada-3006 은 없다 | §14.3 |
| 2026-09-14 | 「`console_v3` 공개 여부 **미확인**」 | 2026-09-19 | 같은 절 ② 가 이미 public 으로 확인했다 | §16.4 ② |


## 11. 규칙

> 현행 = [`CLAUDE.md`](../CLAUDE.md) · [`README.md`](../README.md) · [`claude-code-작업문서.md`](claude-code-작업문서.md) · 통합문서 「📐 문서 규칙」
> 🔴 **이 절은 통합문서가 아니라 「규칙 문서」에서 뺀 것을 담는다.**

| 시점 | 폐기된 규칙 | 폐기 시점 | 왜 | 근거 |
| --- | --- | --- | --- | --- |
| 2026-09-15 | **`CLAUDE.md` 「`../hanium-docs` = 참고 금지」(저장소 전체)** | 2026-09-22 | **범위가 과했다** — 금지 사유였던 「원치 않는 규칙·근거 없는 내용」은 그 저장소의 `CLAUDE.md` 하나에 있고, **2026-09-21 에 규칙 정본이 `.claude/rules/제출문서.md` 로 분리·신설**되면서 그 파일 자신이 금지 범위를 *「같은 저장소의 CLAUDE.md」* 로 좁혀 적었다(그쪽 `7e28f90` — 09-15 결정보다 나중). 장려상 제출물 2종(개발보고서·제작설계서)이 그 폴더의 작업 대상이라 전면 금지는 필수 경로를 막는다 | 실물 대조(2026-09-22) — `hanium-docs/.claude/rules/제출문서.md` 첫 줄 · 커밋 `7e28f90` 날짜 · 낡은 곳 4개(일정 표·데스크톱 경로·통합문서 §10→§12·산출물 상태) |
| 2026-09-21 | **출력 스타일 ⑤ 「결정 요청은 한 번에 하나 — 추천안을 함께 제시하고 나머지는 예고만. 얽혀서 따로 못 정하는 것은 조합(A안/B안)으로 묶어 하나의 질문으로」** | 2026-09-22 | **묻는 횟수를 줄였으나 묻는 종류를 가리지 않았다** — 매 답변마다 걸려 Claude 가 선택지를 계속 냈고, 사용자가 *"제안해준 사항에 대해 이해를 못한다"* 고 밝혔다. 원인은 이해력이 아니라 **답하려면 Claude 의 지식이 필요한 질문**을 사용자에게 넘긴 것이다(실측: 한 세션 질문 5개 중 3개). 이해→승인→진행 구조라 이해가 막히면 작업이 통째로 멈춘다 | 실측 분류(세션 c26d348e) · 대체 규칙 = 같은 파일 ⑤(사용자만 답할 수 있는 것 넷으로 한정 · 선택지 금지 · 되돌리는 법 병기) |
| 2026-09-22 | **`rules/규칙배치.md` 「`paths:` 의 첫 패턴에 와일드카드를 반드시 넣는다 — 리터럴이면 뒤에 글롭이 있어도 목록 전체가 안 걸린다」** | 2026-09-22 | **진단이 틀렸다** — 탐침 F(첫 패턴 리터럴 + 둘째 글롭)가 정상으로 붙었고 공식 문서에도 그런 제약이 없다. 실제 현상은 **`정본편집.md` 파일 하나가 한 세션 동안 규칙 목록에서 통째로 빠진 것**이고 **새 세션에서 정상 복귀**했다(원인 미규명). 규칙이 조용히 죽는다는 부분과 확인법은 살려 남겼다 | 탐침 6개(세션 89ca7ddc) · 재확인 `~/lab/rule-loads/2026-09.tsv` `01:23:26` `path_glob_match .claude/rules/정본편집.md`(세션 c26d348e) · `code.claude.com/docs/en/memory` §Path-specific rules |
| 2026-09-13 | **`CLAUDE.md` 도구 표 「같은 종류 실수가 두 번째 → hookify(규칙 제안 → 승인 후 생성)」** | 2026-09-21 | **도구 자체를 껐다** — 판정할 규칙 0개인 채로 약 2만 회 실행되고 적발 0건이었다(훅 제1원칙의 실적 관문). 문서만 「쓰라」고 남아 실태와 어긋나 있었다 | `rules/cc인프라.md` 플러그인 상태 절(되살리는 법 포함) · `rules/훅설계.md` · 원장 §16.4·§19.4 |
| 2026-09-21 | **`CLAUDE.md` §1 답변 방식 5항목** — 결론 먼저 · 용어 병기 · 이어하기 요약 · 비유→비교→사례 · 결정은 한 번에 하나 | 2026-09-21 | **폐기가 아니라 이관** — 공식이 *"Output styles change how Claude responds"* 로 응답 방식 전용 면을 둔다. `CLAUDE.md` 는 user message 층이라 *"a request, not a guarantee"* 이고 output style 은 시스템 지시 층이다 | → `.claude/output-styles/사용자이해우선.md` · 원장 `CM-02`~`CM-06` |
| 2026-09-21 | **`CLAUDE.md` §5 영역별 함정** — 측정·인용 8 · 과장 금지 4 · 촬영·운용 6 · 제출문서 5 | 2026-09-21 | **폐기가 아니라 이관** — 매 세션이 아니라 **그 작업을 할 때만** 필요하다. 공식 *"only matters for one part of the codebase → path-scoped rule"* · *"multi-step procedure → skill"* | → `rules/수치인용.md` · `촬영`·`측정도구`·`데이터셋` 스킬 · `hanium-docs/CLAUDE.md` · 원장 §10.1 |
| 2026-09-21 | **`CLAUDE.md` §3 문서 편집 규율** 중 「통합문서를 고치기 전에 📐 를 먼저 읽는다」 | 2026-09-21 | **폐기가 아니라 이관** — 「읽어 달라」는 부탁이었다. `paths:` 룰은 그 파일을 열면 **자동으로 붙고** 압축 후에도 다시 로드된다 | → `rules/정본편집.md` · 원장 `CM-23` |
| 2026-09-20 | **규칙 문서 지침 G1~G4**(종전 R13~R16) — ①규칙 문서에 경위 금지(근거는 남김) ②길이 한도(`CLAUDE.md` 200줄·`SKILL.md` 500줄·`MEMORY.md` 색인 200줄·25KB) ③규칙 중복 금지(한 곳 정본 + 포인터) ④뺀 규칙은 이 절로 이관 | 2026-09-21 | **기준과 설계가 틀렸다** — 적용 대상 정의에 정작 이 지침이 적힌 통합문서가 빠져 **자기 페이지에 안 걸렸고**, 길이 한도의 근거로 적은 「설명서도 매 세션 로드된다」가 **사실이 아니었다**(Claude Code 는 README·작업문서를 로드하지 않는다). 둘 자리도 정하지 못한 채였다 | 3중 검토(백지·자기준수·공식) · 원장 `R-공백-01~04` |
| 2026-08-29 | **파이도 `hanium-docs` 를 클론해 읽는다** — 집필규칙·판정표·집필설계를 참조하고, **편집 규율의 정본은 그쪽 `CLAUDE.md` §3~§4** 다 | 2026-09-15 | 그 저장소에 **원치 않는 규칙·근거 없는 내용이 섞여** 있어 정리 전까지 규칙·내용 모두 참고하지 않기로 했다. 사실은 통합문서에서 가져온다 | CLAUDE.md §5 · 원장 `RM-X04` |
