# 개발보고서 Ⅱ-4 「프로젝트 개발 환경」 — 최종본

> **✅ 파이 claude code가 확인함 (2026-08-29, sop-pi 실측)**
>
> 🔴 **이 파일은 정본이 아니다.** 개발환경 사양의 단일 정본은 **`docs/통합문서.md` §11.2** 이며,
> 이 파일은 개발보고서에 그대로 옮겨 붙이기 위한 **2026-08-29 시점 사본**이다.
> 사실이 바뀌면 **§11.2 를 먼저 고치고** 이 파일을 다시 뽑는다.
>
> ⚠️ 표시가 붙은 항목은 **파이에서 확인할 수 없어 데스크톱 확인이 필요**한 것이다.
>
> 🔴 **2026-08-29 데스크톱 정정 3건** — 버튼 수·디스플레이 터치·Cursor 제외. 부록 A ⑥⑦ 참조.
> 앞의 둘은 **§11.1 의 옛 서술을 근거로 삼아 생긴 오류**다. §11.1 은 모조 콘솔 기준이 남아 있어 그대로 인용하면 안 된다.

---

## [S/W 개발환경]

### OS
- Raspberry Pi OS 64-bit (Debian 13 trixie 기반, 커널 6.18) — 엣지 장치
- Ubuntu 24.04.4 LTS on WSL2 — 개발 PC (모델 학습 · NPU 변환)

### 개발환경 (IDE, CLI)
- Claude Code CLI · Antigravity CLI ⚠️버전 미확인
- VS Code 1.135.0
- arduino-cli 1.4.1 — 펌웨어 컴파일·업로드
- Google Colab (T4 GPU) — 공구 모델 학습
- google-colab-cli — 파이 터미널에서 T4 학습 실행

### 개발도구
- Ultralytics YOLOv8 8.4.117 — 모델 학습
- Hailo DFC 3.33.1 · hailo_model_zoo 2.18.0 — NPU 변환 / HailoRT 4.23.0 — NPU 실행
- PyQt6 6.11.0 — 작업자 화면
- OpenCV 4.10.0 · NumPy 2.2.4 — 영상·수치 처리
- pySerial 3.5 · gpiozero 2.0.1 — 제어 신호 전송 · 버튼 입력
- ESP32 Arduino core 3.3.10 · Arduino renesas_uno core 1.6.0 · WiFiManager 2.0.17
- AnyLabeling ⚠️버전 미확인 — 데이터셋 라벨링
- Ollama 0.21.2 — 소형 LLM 비교 실측 (음성비서 Pi에서 구동)

### 개발언어
- Python 3.13 (GUI·비전 런타임) / 3.12 (공구 추론) / 3.11 (모델 변환)
- C/C++ (Arduino · ESP32 펌웨어)

> **파이썬 3중 구조** — `run_demo.sh` 가 시스템 `python3` 를 호출하므로 GUI 는 3.13 에서 동작한다.
>
> | 인터프리터 | 버전 | 역할 | 주요 패키지 |
> | --- | --- | --- | --- |
> | 시스템 `python3` | 3.13.5 | GUI·비전 런타임 | PyQt6 · OpenCV · NumPy · pySerial · gpiozero · HailoRT |
> | `~/env/rfenv` | 3.12.13 | 공구 추론 워커(별도 프로세스) | Ultralytics · PyTorch · ONNX · ONNX Runtime |
> | `Rpi5/.venv` | 3.11.15 | 손 모델 변환·실험 | MediaPipe 0.10.18 |

### 기타사항
- PyTorch 2.13.0+cpu · ONNX 1.22.0 · ONNX Runtime 1.21.1 변환 파이프라인
- MediaPipe 손 모델(BlazePalm · BlazeHandLandmark) — Hailo `.hef` 변환 원본, 프레임워크는 런타임 미사용
- hailo_platform (HailoRT Python API) · HailoRT 내장 NMS
- Roboflow MCP · draw.io MCP — 데이터셋·도식 연동

---

## [H/W 구성장비]

### 디바이스
- Raspberry Pi 5 (8GB, Cortex-A76 2.4GHz) — 비전 추론
- Hailo-8 NPU (26 TOPS, INT8, M.2 PCIe)
- Arduino UNO R4 Minima (주) / UNO R3 (예비) — 인터락 제어
- Raspberry Pi 5 (8GB) — 음성 비서 연산용 별도 1대 *(검증 중)*
- 데스크톱 컴퓨터 (WSL2) — 버튼 모델 학습 · DFC 변환

### 센서
- XIAO ESP32-S3 Sense (OV3660 카메라) — 스마트 글라스
- 온보드 PDM 마이크 (ESP32-S3 Sense 내장)

### 통신
- WiFi TCP — 글라스에서 엣지 장치로 영상 전송
- Serial — 엣지 장치에서 제어 보드로 명령 전송
- GPIO — 물리 버튼 입력

### 언어
- C/C++ (Arduino · ESP32) / Python (Raspberry Pi 5)

### 기타사항
- 릴레이 4채널 (주) / 8채널 (예비)
- 큐라이트 타워램프 (부저 내장)
- 제우스 Z10 10.5″ 터치 디스플레이 — 화면 출력 · 해제 조작 입력
- 아케이드 버튼 4개(B1~B4) + 비상정지(EMO)
- I2S 앰프 DFR0954(MAX98357A) · 스피커 6종 — *입고·미검증*
- 알루미늄 프로파일 DF3030 + MDF/포맥스 외장
- 멀티탭 마스터 전원 (강원전자 NM-SF630)
- 전원 어댑터 — Pi 27W USB-C · 12V 2A (SZH-PSU03)

---

## [프로젝트 관리환경]

### 형상관리
- GitHub — 저장소 2개 (`sop-project` 문서·설계 / `Rpi5` 런타임 코드)
- Google Drive — 학습 결과 보관

### 의사소통관리
- 카카오톡 · Notion

### 기타사항
- Git
- Tailscale 1.102.2 (VPN) · NoMachine (원격 데스크톱) — 두 개발 환경 원격 연동
- Google Drive MCP · Notion MCP

---

## 부록 A — 초안(v4)에서 바뀐 것 10건

| # | 바뀐 내용 | 근거 |
| --- | --- | --- |
| 1 | Colab T4 → **공구 모델 학습**으로 한정, **데스크톱 컴퓨터** 추가 | 버튼 모델(console_v2)은 데스크톱 컴퓨터에서 학습. Colab 배제 결정 — DFC 가 x86 리눅스 전용 (작업로그 2026-07-17) |
| 2 | Python → **3개 버전 병기** | GUI(3.13)에 `ultralytics`·`torch` 가 없다. `tool_worker.py` 주석이 명시 |
| 3 | MediaPipe → **프레임워크 런타임 미사용** 명시 | 3.13/aarch64 휠 부재. 모델만 `.hef` (`~/lab/hoi/hailo8/`) |
| 4 | OS → **Debian 13 trixie · 커널 6.18** 병기 | `/etc/os-release` · `/etc/rpi-issue` |
| 5 | **Arduino 툴체인 4종 신규** | arduino-cli 1.4.1 · ESP32 core 3.3.10 · renesas_uno 1.6.0 · WiFiManager 2.0.17 |
| 6 | ~~아케이드 버튼 4개 → 8개(사용 4)~~ → 🔴 **철회, 4개 유지** | §11.1 이 **모조 콘솔 기준 옛 서술**이었다. 실콘솔 배치도(§5, 2026-08-10 확정·정본) = *"미사용 확장 버튼 없음 · 버튼 5개만 쓴다"* · 사용자 확인 2026-08-29 |
| 7 | ~~터치 디스플레이 → 출력 전용~~ → 🔴 **철회, 터치 입력 사용** | §11.1·§5 부품은 *"출력 전용(입력 아님), 단 해제 버튼 UI 는 여기 배치"* 로 **한 문장 안에서 모순**이다. §5 통신 경로는 *"출력 + 해제 입력"*. 사용자 확인 2026-08-29 — **해제는 터치로 한다** |
| 8 | I2S 앰프·스피커 → **미검증** 표시 | 통합문서 §11.3 — 입고만 되고 V0~V7 미수행 |
| 9 | Ollama → **음성비서 Pi 구동** 명시 | 메인 Pi 엔 바이너리만 있고 모델 0개. pi2 에 5종 적재 |
| 10 | Roboflow → 개발도구에서 **MCP 항목으로 이동** | 파이 `rfenv` 에 SDK 미설치 — 웹·MCP 로만 사용 |

## 부록 B — 데스크톱 확인 결과 (2026-08-29, 데스크톱 claude code)

| 항목 | 결과 | 확인 방법 |
| --- | --- | --- |
| Hailo DFC | ✅ **3.33.1** · `hailo_model_zoo` 2.18.0 · `~/hailo-venv`(Python 3.12.3) | `~/hailo-venv/bin/pip list` |
| Ubuntu on WSL2 | ✅ **24.04.4 LTS (noble)** | `lsb_release -a` |
| VS Code | ✅ **1.135.0** | `"…/Microsoft VS Code/bin/code" --version` |
| Antigravity CLI | ⚠️ **WSL PATH 에 없음** — 윈도우 쪽 설치로 보인다 | `which antigravity` |
| AnyLabeling | ⚠️ **미확인** — 앱에서 직접 봐야 한다 | 앱 정보 |

> ⚠️ **`code` 를 그냥 치면 VS Code 가 아니라 Cursor(3.7.42)가 잡힌다** — PATH 우선순위가 Cursor 다.
> 🔴 **Cursor 는 개발환경에 넣지 않는다** — 설치돼 있을 뿐 이 프로젝트에서 쓰지 않았다(사용자 확인 2026-08-29).

## 부록 C — 확인에 쓴 명령 (재현용)

```bash
cat /etc/os-release; cat /etc/rpi-issue; uname -srm
python3 -V                                   # 시스템 = GUI 런타임
python3 -c "import PyQt6.QtCore as q; print(q.PYQT_VERSION_STR)"
python3 -c "import cv2, numpy; print(cv2.__version__, numpy.__version__)"
python3 -c "import serial, gpiozero; print(serial.__version__)"
python3 -c "import mediapipe"                # 런타임엔 없어야 정상
~/env/rfenv/bin/python -c "import ultralytics, torch; print(ultralytics.__version__, torch.__version__)"
hailortcli fw-control identify | head -5
dpkg -l | grep -i hailo
~/bin/arduino-cli version
ls ~/.arduino15/packages/esp32/hardware/esp32/
ls ~/.arduino15/packages/arduino/hardware/renesas_uno/
ssh pi2 'ollama list; free -g | head -2; nproc'
tailscale version
```
