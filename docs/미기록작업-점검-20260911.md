# 미기록 작업 전수 점검 — 2026-09-11

> **무엇인가** — Claude Code 세션 기록 47개를 전수로 훑어, **저장소 문서(작업로그·통합문서·spec)에 반영되지 않은 채 대화에만 남아 있던 작업**을 찾아 모은 점검 보고서다.
> **왜 여기 있나** — 작업로그는 세션별 시간순 저널이고 통합문서는 설계·측정 정본이다. 이 문서는 둘 중 어디에도 속하지 않는 **일회성 감사 결과**이므로 별도로 둔다(사용자 지시, 2026-09-11).
> 🔴 **이 문서는 「발견 목록」이지 정본이 아니다.** 여기 적힌 사실을 인용하려면 근거(코드·세션·커밋)를 직접 확인한다. 처리(정본 반영·수정)는 **아직 하지 않았다.**
> **처리하면** 해당 항목에 완료 표시를 남기고, 사실은 통합문서로, 경위는 작업로그로 옮긴다.

---

## 0. 점검 방법과 한계

| 항목 | 내용 |
| --- | --- |
| 대상 | `~/.claude/projects/-home-pi-sop-project/` 세션 **47개** (2026-08-10 ~ 2026-09-11) |
| 방법 | 세션별 지시 발화 전수 추출(2,282줄) + 편집 파일 목록 + 커밋 대조 + 문서 `grep` |
| 깊이 | 전 세션 = 지시·편집 수준 / **인터락·정본·공구·네트워크·하드웨어·편집·운영 갈래는 본문 정독** |
| 🔴 한계 | 이 파이의 세션 기록은 **2026-08-10 이후만** 남아 있다. 그 이전은 커밋·문서로만 추적됨 |
| 🔴 한계 | 본문을 끝까지 읽은 것은 일부다. **판단 근거·기각한 대안처럼 본문에만 있는 것**은 더 나올 수 있다 |

---

## 1. 🔴 사실 어긋남 — 지금 문서와 실물이 다른 것

### 1.1 인터락 물리 차단 — 문서는 「동작」, 실물은 **결선 풀림**

| 층 | 상태 |
| --- | --- |
| 설계 | `specs/2026-09-05-인터록-물리차단-design.md` — 검증 7항목 명시 |
| 코드 | ✅ `console_interlock.ino` `PIN_CUT=3`(D3→IN5). 릴레이 CH5 자체는 명령에 반응(LED·딸깍 육안 확인) |
| **실물 결선** | 🔴 **풀려 있음** — 2026-09-05 결선 실패 후 **릴레이 우회·브레드보드 직결(종전 구조)로 되돌린 뒤 촬영**. 이후 복구 커밋 없음 |
| 제출문서 | 제작설계서 29·36·37쪽, 개발보고서 7곳이 **동작하는 기능으로 서술** |
| 통합문서 | CH5 **0회**. FR-3·§5.2·§6·§11·§12 는 「장비로 가는 전기 신호 차단」 |

**2026-09-05 실HW 디버깅 경위** (세션 `3e4f101a`, 작업로그 미기록)

1. 결선 후 **B1~B4 전부 무입력**. EMO 만 정상(릴레이를 안 거치는 경로)
2. 펌웨어 미업로드 가설 → **기각**(BLOCK 에 CH5 LED·딸깍 반응 확인)
3. 버튼·신호선·GPIO·풀업 → 전부 정상(B2 를 브레드보드 GND 직결 시 17회 정상 검출)
4. NC/NO 를 바꿔 끼워도 동일 → **「와고 → 릴레이 → Pi GND」 한 구간만 안 통함. 원인 미규명**
5. 촬영을 위해 **릴레이 우회**로 되돌림
6. 🔑 남은 유력 후보 = **와고 출력 가닥이 릴레이 단자에 미체결**(나사 조임·심선 삽입). 확인법 = 테스터 도통

🔴 **2026-09-07 정본 점검 세션도 「결선 ✅ 완료」로 잘못 알고 설계를 세웠다.** 그 설계의 §10.63 초안은 이 전제 위에 있다.

### 1.2 미수정 안전 결함 — 거짓 양성

- `Demo/precheck.py:73` 이 `getattr(gpio, "available", True)` 로 읽는데 **`Demo/gpio_input.py` 에 `available` 속성이 없다**
- 결과: **GPIO 초기화가 실패해도 콘솔 점검이 「준비됨」으로 통과**한다
- 같은 파일 61·121행의 손 검출은 기본값 `False` 라 **비대칭**이다
- 짝 결함(`interlock.connected` 부재 = 거짓 음성)은 2026-09-05 `fdf548b` 로 수정됨

### 1.3 버튼 채터링 — 대응 미적용

- 2026-09-05 실측: **한 번 누름이 3~4회로 기록**. 원신호 단계에서도 0.1초 간격 중복 관측
- `config.GPIO_BOUNCE_SEC` 현재 **0.05 그대로**(0.2 로 올리자는 제안이 승인 전에 종료)
- 위험: 시연 중 단계 건너뜀·오답 판정

### 1.4 통합문서 수치·서술 오류

| 대상 | 정본 현재 | 사실 | 근거 |
| --- | --- | --- | --- |
| FR-3 · §3 · §5.2 · §6 · §11 · §12 | 장비로 가는 **전기 신호** 차단 | **버튼 4개 공통 GND 1가닥** | `console_interlock.ino:16,36-44` |
| §5.3 · §11 BOM · §14.2 | 릴레이 **4채널**(주)/8채널(예비) | **8채널 모듈 · CH1~CH5 사용** | `.ino` PIN_RED~PIN_CUT |
| §10.61 · `[CURRENT]` | 답변 재생 **302~304ms** | **ACK 미수신 포기 시각**. 10회 전부 `재생성공:false`, 원값 3~1286ms(편차 400배) | `계측.jsonl` |
| §11.2 | Ollama **0.21.2** | **0.32.13** | §10.46 `[CURRENT]` 와도 불일치 |
| §11.1 머리말 | *"검증 V0~V7 을 **하나도 수행하지 않았다**"* | 2026-09-07 **관문 6개 통과**(§10.61) | §10.61 |
| 전역 | 인터**록** 41 / 인터**락** 37 | 사용자 확정 표기 = **인터락**(2026-09-07) | — |

**같이 낡은 문서** — `docs/개발보고서-개발환경.md`(릴레이 4채널·Ollama 옛값) · `docs/개발보고서-가치와제작노력.md`(「전기 신호」 1곳) · `README.md`(인터록 2곳) · `dev/interlock/결선도_초안.md`(**CH5 를 아직 「PoC 파일럿 램프」로 기재**, 2026-08-10 이후 미수정)

### 1.5 라이선스 표시 의무 미이행

- 공구 데이터셋 2종(`karthi-zqreo`·`ruri-binnw`)이 **CC BY 4.0** → ①저작자 표시 ②라이선스 명기 ③**변경 사실 표기**(병합·재분할했으므로 해당) 의무
- 🔴 **제출 PDF 2종에 `라이선스`·`저작권`·`CC BY`·`AGPL`·`karthi` 전부 0회**
- 부록 문안 `docs/제작설계서/개발보고서_부록_라이선스.md` 는 **제출 다음 날(9/8) 작성**이며 「이행함」으로 서술 → **그대로 정본화하면 정본이 거짓이 된다**

### 1.6 정본에 아예 없는 것

CH5 물리 차단 · 라이선스 감사(원문 대조 14종 · 상용화 리스크 2건: YOLOv8 AGPL · PyQt6 GPL) · `tool_v4` · 제출 사실과 시연영상 URL · `session_stats`

---

## 2. 갈래별 미기록 작업 (시간순)

### 2.1 공구 검출 — `tool_v4` 관문 3은 「미달」이 아니라 **무효**로 끝났다

세션 `15f41511`(09-04~05). 작업로그 09-04 블록은 **「관문 3 미실시」에서 멈춰 있어 아래가 통째로 빠짐**.

| # | 사건 | 값 |
| --- | --- | --- |
| 1 | 홀드아웃 개봉 (163장 · conf 0.65 · IoU 0.5) — `tool_v3` 기준선 먼저 채점 | — |
| 2 | **Colab VM 회수** — 52에폭에서 중단, `best.pt`·`last.pt` 소실 | 보유 = `tool_v4_partial_e38.pt`(mAP50 0.901) |
| 3 | e38 로 관문 3 개봉 → **미달** | 쥔 상태 **7% · 0% · 0%** (합격선 3종 모두 ≥40%) |
| 4 | 학습 valid 재확인 → **도메인 갭 확정** | 같은 모델이 학습 도메인에선 `wrench-in-hand` **76.4%** |
| 5 | 🔴 **판정 무효** — 홀드아웃 밖 잔여 프레임에선 conf 0.65 에 **9/33** 검출되는 모순 | 정답 박스가 렌치 **머리 조각만** → IoU 0.23~0.26 으로 전부 미적중 |
| 6 | 원인 | 가운데를 쥔 렌치는 위아래로 갈린다. **「양 끝을 감싸는 박스 하나」 지침이 라벨 작업자에게 전달되지 않음** |
| 7 | 범위 | `wrench-in-hand` **32개만**. driver·pliers 는 정상. 박스 면적비로도 드러남(쥠/놓임 = pliers 1.87 · driver 1.27 · **wrench 0.89**) |
| 8 | 중단 상태 | **「쥔 렌치 32개 재라벨 → 재채점」 요청이 나간 채 종료.** 재라벨·재채점 기록 없음 |
| 9 | 참고 예측 | 재라벨해도 합격 어려움 — 잔여 프레임 검출률 wrench 27% · driver 10% · **pliers 0%** |

🔴 **그러므로 관문 3 은 아직 판정되지 않았다.** `tool_v4` 는 통합문서 **0회**.

### 2.2 네트워크 — 파이 설정이 문서 밖에서 바뀌었다

| 날짜 | 무엇 | 현재 상태 |
| --- | --- | --- |
| 08-29 | `wifi-prefer-jason` 타이머 설치(Jason 강제 전환) | 09-03 삭제됨 |
| 09-03 | `Jason` 프로파일·타이머 삭제 · `Public WiFi Free` 가 우선순위 10 승계 · **`wifi-prefer-main` 타이머 신설**(Eung Min 강제 전환, 1분 주기) | ✅ **enabled · 1분마다 동작 중**(전환 로그 0건) |
| 09-03 | `Public WiFi Free` DHCP `no lease` → pi2 가 고정 IP 수동 지정 → DHCP 로 원복 | 현재 `ipv4.method: auto` |
| 09-03 | pi2 유선 IP **192.168.1.9 → 192.168.1.12** | `ssh pi2` 는 Tailscale(100.67.150.74) 경유라 영향 없음. 문서·메모리의 옛 주소는 낡음 |
| 09-04 | WiFi 절전 끄기(런타임) | 🔴 **재부팅으로 원복 — 현재 `Power save: on`**. 영구 적용 여부 미결 |
| 09-06 | 통신경로 재설계(폰을 영상 경로에서 제외) | 통합문서 §10.58·§10.60 ✅ / **작업로그 블록 없음** |

🔑 **`no lease` 의 진짜 원인은 우선순위가 아니라 「공유기가 이 기기에 IP 를 주지 않는 것」이었다.** 경쟁 SSID 가 하나도 없는 깨끗한 조건에서도 자동 연결이 안 됐다. 이 진단이 어느 문서에도 없다.

### 2.3 하드웨어 · 전원 — 결론까지 나온 검토가 문서 밖

**배터리 잔량 · 원격 전원 제어**(08-12 세션 `5a9084a0`, 문서 0회)

- 원격 **끄기는 가능, 켜기는 원리상 불가** — 전원을 끊으면 WiFi 가 함께 꺼져 명령을 받을 수단이 사라진다(WiFi 는 기상 소스가 아니다)
- 🔴 **이 보드는 카메라를 deinit 하지 않고 재우면 딥슬립에서도 약 20mA** 를 먹는다
- 권고 = **전원 차단 대신 카메라·스트림만 정지** — 즉시 재개 가능 · 추가 부품 0
- 잔량 자동 판독 = 저항 2개 분압 + ADC. 단 LiPo 는 전압-잔량이 비선형이라 **0.05V 에 10%p** 가 움직인다
- 판단 보류 조건 = **V1(전류 실측)·V7(배터리 지속시간)**. 🔴 **V7 은 지금까지 미실시**

**기타** — `battery_reader/` 가 저장소 루트에 **빈 폴더**로 남아 있다(08-19 생성, 산출물 없음)

> ℹ️ 1차 정리에서 「미기록」으로 적었던 **발열 테스트 결론**(가설 기각)과 **커패시터 100µF 채택**은 확인 결과 **작업로그에 이미 기록돼 있다.** 정정한다.

### 2.4 시연영상 편집 — 기준과 러프컷 3판이 문서 밖

- 러프컷 **v1 · v2(85.7초 · 12컷) · v3(GUI 만)** 제작. 소재·컷 구성표까지 확정
- 편집 기준: **총 90초 상한** · 오검출 프레임은 NG · 대기 구간 **4배속** · B1 UI **크롭 확대** · 오검출 장면은 다른 촬영본으로 교체
- 🔑 **「버튼 스침」 설명 컷 = `184102` 30~41초** — 경고가 **39.5초**에 떠서 38초로 자르면 결말이 잘린다
- 🔴 편집 중에도 **「전기 차단」 표현이 사실과 다르다고 두 번 지적**됐다(1.1 참조). 제출 영상의 최종 표현은 확인 필요
- 산출물 = `Rpi5/Demo/recordings/시연영상/` **1.7GB**(촬영본 · 채택 · NG보관 · 러프컷)

### 2.5 파이 운영 — 🔴 정전으로 저장소가 깨졌던 사건이 통째로 미기록

- **2026-09-03 22:12 정전** → 09-04 10:45 복구. 비정상 종료(ext4 dirty bit · orphan cleanup)
- **`sop-project` git 저장소 손상** — 0바이트 object 7개, 마지막 커밋 `75c0c24` 소실로 `git` 명령 전부 실패(`bad object HEAD`)
- 복구 절차: `.git` 백업 → `main` 을 `e518a26` 으로 되돌림 → 깨진 object 제거·index 재생성 → `fsck` 무오류 → 살아남은 3파일을 **3개 커밋으로 재작성**
- 하드웨어 이상 없음(`get_throttled` 0x0 · 49.4℃ · Hailo 정상). `Rpi5`·`hanium-docs` 무손상
- 부수: 정전으로 0바이트가 된 촬영 로그·`tool_probe` jsonl = 삭제 대상
- **작업로그에 「정전」·「손상」·복구 커밋이 한 번도 안 나온다**

### 2.6 나머지 미기록 (지시·편집 수준 확인)

| 갈래 | 미기록 항목 |
| --- | --- |
| 음성비서 | 구글 폼 채점 운영 경위(아티팩트 → 구글 폼 전환 사유 · 폼/시트 링크) |
| 제출문서 | 개발보고서 3~5장 전면 수정 이력 · 그림 제작 반복(제미나이) 경위 — 산출물은 `../hanium-docs` |
| 파이 운영 | 디스플레이 5분 잠금 해제(09-03) · SD카드·USB 허브 조사(08-13) · 정전 후 pi2 점검(09-04) · pi1·pi2 네트워크 재연결(09-10) |
| 인터락 | ✅ **2026-09-11 해소** — 결론이 없던 게 아니라 **사용자의 NO 결정(09-04 04:42)이 반영되지 않은 채 NC 로 구현**됐다. NO 로 확정하고 근거·고칠 곳을 `specs/2026-09-05-인터록-물리차단-design.md` §11 에 기록 |
| 작업로그 공백 | **09-05 · 09-06 블록 없음** |

---

## 3. 🔑 이번 점검이 드러낸 구조적 원인

1. **병행 세션이 많다** — 같은 날 3~5개 세션이 동시에 돌았고, 세션 마무리를 한 세션의 블록만 남는다. 다른 세션의 결론은 그대로 증발한다.
2. **「진행 중」으로 끝난 갈래가 기록되지 않는다** — tool_v4 관문 무효, 인터락 결선 실패처럼 **결론이 부정형일 때** 특히 안 남는다.
3. **실물 상태 변경에 기록 경로가 없다** — systemd 타이머·WiFi 설정·결선 되돌림은 커밋이 남지 않아 문서로 갈 길이 없다.
4. **제출문서가 정본을 앞질렀다** — `docs/제작설계서/작업규칙.md` §2 가 *"통합문서를 정본으로 삼지 않는다"* 로 명시했고, 그 상태로 2주가 지났다.

---

## 4. 처리 대기 목록 (사용자 결정 후 진행)

| # | 항목 | 성격 |
| --- | --- | --- |
| 1 | 인터락 **NO 개정** — 펌웨어 2줄 반전 → 가닥을 NC에서 NO로 이설 → 검증 7항목. 절차·근거 = `specs/2026-09-05-인터록-물리차단-design.md` **§11**(2026-09-11 기록) | 코드+실물 |
| 2 | `gpio_input.available` 결함 수정 → **일괄 처리 묶음**(4.5) | 코드 |
| 3 | 버튼 디바운스 **0.05 → 0.2 로 임시 확정** — 결선 후 실제로 눌러 보고 문제 없으면 확정(4.4) | 코드 |
| 4 | 통합문서 정정 6건 + 낡은 문서 4개(1.4) | 문서 |
| 5 | 라이선스 표시 의무 — **2차 제출 때 반영**(아래 4.1) + 정본 §14.4 신설 | 문서·대외 |
| 6 | ~~`tool_v4` 재라벨·재채점~~ → **실패 확정, 폐기**(아래 4.2) | 결정됨 |
| 7 | 작업로그 09-05·09-06 블록 소급 + 정전 사건 기록 → **일괄 처리 묶음**(4.5) | 문서 |
| 8 | **WiFi 설정 전면 재점검 — 별도 작업**(아래 4.3) | 실물+문서 |
| 9 | 2026-09-07 정본 정리 설계(A~H)를 spec 으로 복원 → **일괄 처리 묶음**(4.5) | 문서 |
| 10 | 🆕 **우리가 라벨링·학습한 모델 전수 검사** — 학습 코드가 우리 상황에 맞았는지(아래 4.2) | 측정 |

### 4.1 라이선스 — 2차 제출본에 반영한다 (2026-09-11 사용자 결정)

🔑 **지금 제출본은 1차 제출본이다.** 1차 평가에서 선정되면 **2차 평가 때 수정본을 다시 낸다.** 그때 반영한다.

- 대상 = 공구 데이터셋 2종 **CC BY 4.0** (`karthi-zqreo` · `ruri-binnw`)
- 넣을 것 세 가지 — ①저작자 표시 ②라이선스 명기 ③**변경 사실 표기**(병합·재분할·네거티브 보강을 했으므로 해당)
- 문안 초안은 이미 있다 = `docs/제작설계서/개발보고서_부록_라이선스.md`(원문 대조 14종 · 상용화 리스크 2건: YOLOv8 AGPL · PyQt6 GPL)
  🔴 **단 그 문안은 「이행함」으로 적혀 있다** — 2차 제출 전까지는 사실이 아니므로 정본에 옮길 때 **미이행 상태**로 고쳐 쓴다
- 넣을 자리 = 개발보고서 부록 · 제작설계서 말미. 1차 제출본에는 **두 문서 모두 0회**

### 4.2 공구 모델 — `tool_v4` 실패 확정, `tool_v3` 임시 사용 (2026-09-11 사용자 결정)

- **`tool_v4` 는 실패로 종료한다.** 재라벨·재채점을 하지 않으므로 **관문 3 은 판정 없이 닫힌다.** 정본에는 「미채택 · 판정 없이 종료」로 적고, 무효 경위(§2.1)를 함께 남긴다
- **`tool_v3` 를 임시로 쓴다** — 1차 제출본도 v3 기준으로 통일돼 있어 문서와 맞는다(§5.1)
- ⏸ **`tool_v5` 재학습은 추후 가능성으로 열어 둔다** (지금 착수하지 않음)
- 🆕 **선행 과제 — 우리가 직접 라벨링·학습한 모델을 전수 검사한다.** 학습 코드가 우리 상황(1인칭·저해상도·ESP32 프레임)에 맞게 짜였는지 확인이 필요하다. **지금은 하지 않고 추후 별도 작업**으로 둔다
  - 대상 후보 = `console_v1`·`console_v2`(버튼) · `tool_v1`~`tool_v4`(공구) · 학습 스크립트 `dev/ai_model/tool_v1/train_tool_v*.py`

### 4.3 WiFi 설정 — 전면 재점검이 필요하다 (2026-09-11 사용자 지적)

> 🔴 **문서화만으로 끝낼 일이 아니다.** 현재 설정에 문제가 많아 **현 상황에 맞게 다시 설계·설정해야 한다.** 별도 작업으로 분리한다.

| 확인·해결할 것 | 지금 상태 |
| --- | --- |
| 🔴 **공용 WiFi 자동 연결 안 됨** | 2026-09-03 규명 — 원인은 우선순위가 아니라 **공유기가 이 기기에 IP 를 안 주는 것**(`no lease` 반복). 경쟁 SSID 가 없는 깨끗한 조건에서도 재현 |
| `wifi-prefer-main` 타이머 | 2026-09-03 설치, **1분마다 동작 중**. 주 핫스팟이 보이면 강제 전환한다 — 지금 구성(영상은 `iptime`, 인터넷만 핫스팟)과 맞는지 재검토 필요 |
| WiFi 절전 | 껐다가 **재부팅으로 원복**, 현재 켜짐. 영구 적용 여부 미결 |
| 프로파일 우선순위 | `Public WiFi Free` 10 · `Eung Min` 20 · 나머지 0. `Jason` 은 삭제됨 |
| pi2 유선 IP | 192.168.1.9 → **192.168.1.12** 로 변경됨. 문서·메모리의 옛 주소는 낡음 |

---

## 5. 🆕 제출 최종본 대조 (2026-09-11)

> 대상 = `~/Documents/한이음-제출문서/` 의 **개발보고서 34쪽(09-08 14:12)** · **제작설계서 55쪽(09-08 14:13)**
> 방법 = 개발보고서는 글자 추출 전수, 제작설계서는 대부분 이미지라 핵심 쪽(27·34·43)을 눈으로 확인
> 🔑 **§1 의 어긋남 중 다섯 개가 최종본에서는 이미 고쳐져 있었다.** 제 2026-09-07 대조는 59쪽 판본 기준이었다.

### 5.1 ✅ 최종본에서 해소된 것

| # | §1 의 지적 | 최종본 |
|---|---|---|
| 1 | 「인터록」 표기 | ✅ **인터락으로 통일** — 「인터록」 0회 (개발 19회 · 설계 3회 모두 「인터락」) |
| 2 | 「전기 신호 차단」 | ✅ **「버튼 입력 차단」** — 「전기 신호」 0회 / 「버튼 입력」 개발 8회·설계 6회 |
| 3 | 릴레이 4채널 | ✅ **8채널** (개발 「릴레이 8 채널」 · 설계 34쪽 「릴레이 5 채널」 = D7~D3 → IN1~IN5) |
| 4 | 「답변 재생 약 0.3초」 | ✅ **삭제됨** — 남은 「0.3 초」 2회는 전부 **체류 임계** 맥락 |
| 5 | 공구 모델 세대 혼재(v3/v4) | ✅ **v3 로 통일** — 설계 27쪽이 **검지 끝이 공구 상자 안인지**로 판정(`wrench` 0.74 · `driver` 0.69, 평이름). in-hand 클래스 흔적 없음. 개발보고서도 「공개 데이터 2종 병합 약 2만 9천 장」 = `tool_v3`(§10.40 과 일치) |

### 5.2 🔴 최종본에도 남은 것

| # | 내용 | 근거 |
|---|---|---|
| 6 | **라이선스 표시 의무 미이행 확정** | 두 문서에 `라이선스`·`저작권`·`CC BY`·`AGPL`·`karthi`·`ruri`·`오픈소스` **전부 0회** |
| 7 | **진척도 표 「공구 검출 100%」** | 개발보고서 전체 기능 목록. 실측은 **쥔 상태 판정 관문 미판정**(§2.1) — 정본에 100% 로 옮기면 과장 |
| 8 | **시연영상 URL 이 바뀌었다** | 최종본 설계서 54쪽 = `https://youtu.be/uD0gHztG76E`. 2026-09-07 대조 때는 `KS8-xonXdsk` 였다. **둘 다 정본에 0회** |
| 9 | **설계서 43쪽에 개정 전 펌웨어가 실려 있다** | `digitalWrite(PIN_CUT, cut ? RELAY_ON : RELAY_OFF)` + 주석 `ON = 버튼이 끊긴다` = **NC 전제**. 🔴 NO 개정(§11) 후에는 이 슬라이드가 낡는다 — **슬라이드를 근거로 코드를 되돌리지 말 것** |
| 10 | 설계서 50쪽 「데이터셋 ② console_v3」 | `console_v3` 는 Roboflow 1104장으로 **재학습 보류** 상태다(통합문서 §10.20). 슬라이드에 그 조건이 있는지는 **미확인** |

### 5.3 결선에 쓸 값 (설계서 34쪽에서 확정)

- **공통 GND = 파이 40핀 헤더의 물리 25번** — NO 개정 시 CH5 COM 이 갈 곳이 여기다(§11.4)
- 버튼 = `GPIO5`(B1) · `GPIO6`(B2) · `GPIO13`(B3) · `GPIO19`(B4) · EMO `GPIO26`, 전부 `INPUT_PULLUP`
- **EMO 는 NC 쌍이며 버튼과 묶지 않는다** — 선이 끊겨도 비상으로 본다(fail-safe). 설계서에도 그렇게 적혀 있다
- 릴레이 코일 전원 = Arduino 5V, 남는 채널 = IN6~IN8

---

## 6. 🆕 2026-09-11 확정 사항 — 방침과 확인 결과

### 6.1 ⭐ 방침 — 정본은 사실 그대로 적는다

> 사용자 지시(2026-09-11). **모든 작업에 적용한다.**

제출본이 다르게 적었더라도 **정본은 실측·실물 그대로** 쓴다. 둘이 달라지는 것을 받아들인다.

| 예 | 제출본 | 정본에 쓸 것 |
| --- | --- | --- |
| 공구 검출 진척도 | **100%** | 쥔 상태 판정 **관문 미판정**(§2.1) · `tool_v3` 임시 사용 |
| 인터락 물리 차단 | 동작하는 기능 | **결선 풀림 · 실동작 미검증**(§1.1) |

🔴 제출본을 정본의 근거로 삼지 않는다. 근거는 코드·실측·실물이다.

### 6.2 ⭐ 방침 — 대조 기준은 **개발보고서**다

> 사용자 지시(2026-09-11).

**제작설계서는 개발보고서와 설계 문서를 바탕으로 만든 파생 문서**다. 둘이 어긋나면 **개발보고서를 본다.**
제작설계서만 가진 서술은 근거를 따로 확인한 뒤에 쓴다.

### 6.3 ✅ 확인 완료 — 시연영상

| 항목 | 결과 |
| --- | --- |
| 영상 속 표현 | ✅ **「입력 차단」 또는 「차단」으로 설명** — 「전기 차단」이라고 말하지 않았다. §2.4 에서 우려했던 어긋남은 **발생하지 않았다** |
| 주소 | ✅ **`https://youtu.be/uD0gHztG76E` 가 실제 제출본**(사용자 확인). 정본에 아직 0회 — 기재 대상 |

### 6.4 🆕 버튼 디바운스 — 0.2초 임시 확정

- `config.GPIO_BOUNCE_SEC` **0.05 → 0.2** 로 정해 두고, **인터락 결선 뒤 실제로 눌러 확인**해 문제 없으면 확정한다
- 근거 = 2026-09-05 실측에서 한 번 누름이 **3~4회**로 기록됐다(§1.3). 접점 튐이 0.05초보다 오래 이어진다
- 확인 방법 = 결선 후 B1~B4 를 각각 5회씩 눌러 로그에 **정확히 5줄**이 찍히는지 본다
- ⚠️ 결선을 새로 하면 접점 상태가 달라진다 — **결선 전에 값을 바꾸되 판정은 결선 후**에 한다

### 4.5 🆕 일괄 처리 묶음 — 미기록 항목 확인이 끝난 뒤 한 번에

> 사용자 지시(2026-09-11). **지금 하지 않는다.** 남은 갈래의 확인이 끝나면 사소한 처리와 함께 한 번에 처리한다.

| # | 항목 | 성격 |
| --- | --- | --- |
| a | `gpio_input.available` 결함 수정(거짓 양성) | 코드 |
| b | 2026-09-07 정본 정리 설계(A~H)를 spec 으로 복원 | 문서 |
| c | 작업로그 **09-05 · 09-06** 블록 소급 + **정전·저장소 복구** 사건 기록 | 문서 |
| d | 이 문서 §0 「한계」 절 수정 — 2026-08-10 이전 구간을 커밋·문서로 대조한 결과 반영(저널 시작 = 07-01, 실질 공백 없음) | 문서 |
| e | README 「문서별 역할·경계」에 이 문서 포인터 1줄 | 문서 |
| f | 미푸시 커밋 일괄 푸시 | 저장소 |


---

## 7. 🆕 3차 정독 — 세션 ID 전수 대조 (2026-09-11)

> 방법 = 세션 47개의 ID 를 `작업로그.md` · `claude-code-작업로그.md` 에서 전수 검색 → **어느 로그에도 ID 가 없는 25개**를 추려 결론 대목을 읽었다.
> 🔑 **ID 가 없다 ≠ 미기록** — 병행 세션은 다른 세션 블록이 내용을 흡수한 경우가 많다. 실제로 대부분은 내용이 남아 있었다.

### 7.1 🔴 새로 나온 정본 공백 — 카메라 세로 전환

**2026-08-26 세션 `d97f5518`**(작업로그 ID 없음). 커밋 `d381869`.

| 무엇 | 내용 |
| --- | --- |
| 계기 | ESP32 장착 구도가 바뀌어 화면이 **오른쪽으로 90° 돌아갔다** |
| 조치 | 파이프라인에 **반시계 90° 회전**을 추가. 순서 = 상하반전 → **왜곡보정** → 회전 (캘리브레이션 맵이 640×480 기준이라 회전을 뒤에 둬야 한다) |
| 결과 | 프레임이 **480×640 세로**가 됐고, UI 좌우 레터박스를 없애려 **표시 채움**을 켰다 |
| 현재 설정 | `CAMERA_FLIP_VERTICAL=True` · **`CAMERA_ROTATE_CCW90=True`** · **`CAMERA_DISPLAY_FILL=True`** (`Demo/config.py`), 구현 = `Demo/frame_orient.py` |
| 🔴 기록 | **통합문서에 `CCW90` 0회 · 「레터박스」 0회.** `Rpi5/CLAUDE.md` 에만 있다 |

🔑 **왜 중요한가** — 화면 비율이 가로에서 세로로 바뀐 것은 **검출 구도·UI 배치·화각에 모두 걸리는 구조 변경**이다. 정본의 카메라 절(§7·§11)이 아직 640×480 가로를 전제한다면 그대로 두면 안 된다.
⚠️ 같은 세션이 확인한 사실 하나 — **회전으로는 시야를 되찾을 수 없다.** 센서가 이미 눕혀져 담은 범위가 달라졌고, 픽셀을 돌리는 것으로는 복구되지 않는다.

### 7.2 카메라 모듈 조사 — 기록해 둘 값이 있다

**2026-09-05 세션 `9eb3cb46`**(조사만, 문서 0회). 카메라 교체가 다시 논의될 때 재조사를 막는다.

- XIAO ESP32-S3 Sense 확장보드의 카메라 단자 = **24핀 · 0.5mm 피치 FPC · DVP** → **ESP32-CAM 용 표준 24핀 모듈이 그대로 꽂힌다**
- 리본 길이(21 / 40 / 75mm)와 화각(66°~222°)을 **골라서 초소형 헤드로 교체 가능**
- `OV3660` = 1/5인치 3MP · 화소 **1.4µm** → `OV2640`(2.2µm)보다 **저조도에 불리**. 해상도는 1.5배지만 어두운 곳에서 손해
- 순수 TCP 소켓 전송은 이 생태계에서 **소수 사례**다(표준은 HTTP MJPEG). 우리 구조가 흔한 길이 아니라는 뜻

### 7.3 확인했으나 문제 없던 것

| 세션 | 내용 | 판정 |
| --- | --- | --- |
| 08-10 `53a7d512` | 공구 데이터셋 조사 · `tool_v1`·`tool_v2` 판정 | ✅ §10.35~§10.38 · 작업로그 블록 |
| 08-13 `aa9cec95` | 공구 입력 A-2 · 네거티브 보강 중단 판정 | ✅ §10.42·§10.43 · spec·plan |
| 09-01 `6d034bf1` | 오디오 다리 · STT 핫워드 | ✅ §10.51·§10.52 |
| 08-27 `1b03891d` | 인터락 작동 시점 | ✅ `specs/2026-08-28-인터락-작동시점-design.md` |
| 08-18 `1461df7a`·`42619f0d` | 홈 폴더 정리 · 자동 진행 | ✅ 내용이 08-18·08-19 블록에 |
| 08-25·08-31 소형 | 발표·보고서용 이미지 고르기 | 산출물 성격, 기록 불요 |
| 09-02~03 문서 세션 4개 | 개발보고서 본문·그림 제작 | 산출물이 `../hanium-docs` — 이 저장소 밖 |

### 7.4 ⚠️ 작은 공백 하나

`console_v2` **학습셋 652장의 최종 박스 수**가 어디에도 없다(Roboflow 안에만). test 113장은 **539개**로 §10.20 에 있다. 인용할 일이 생기면 Roboflow 를 봐야 한다.

---

## 8. ✅ 전수 확인 완료 (2026-09-11)

> **세션 47개를 전부 확인했다.** 아래가 무엇을 어느 깊이로 봤는지의 기록이다.

### 8.1 3차에 걸친 방법

| 차수 | 방법 | 산출 |
| --- | --- | --- |
| **1차** | 세션별 **지시 발화 전수 추출**(2,282줄) + 편집 파일 목록 + 커밋 날짜 대조 + 문서 `grep` | 갈래 6개의 미기록 항목 목록(§2) |
| **2차** | 인터락·정본·공구·네트워크·하드웨어·편집·운영 갈래의 **본문 정독** | 사실 어긋남 6건(§1) · tool_v4 무효 경위 · 정전 사건 |
| **3차** | 세션 47개 **ID 를 두 로그에서 전수 검색** → 어느 쪽에도 없는 25개의 **결론 대목 정독** | 카메라 세로 전환 공백(§7.1) · 카메라 모듈 조사(§7.2) |

### 8.2 확인 깊이 — 세션별

**본문** = 대화 본문을 읽음 · **결론** = 결론·판정 대목만 읽음 · **지도** = 지시 발화와 편집 파일로 주제·산출물 확인
**로그** = ✅ 세션 ID 가 로그에 있음 / ○ ID 는 없으나 내용 확인함 / — ID 없음(다른 블록이 흡수했거나 기록할 산출물 없음)

| 날짜 | 세션 | 확인 깊이 | 로그 | 주제 |
|---|---|:-:|:-:|---|
| 08-10 | `53a7d512` | 결론 | ✅ | 공구 데이터셋 조사 · tool_v1·v2 판정 |
| 08-12 | `5a9084a0` | **본문** | ✅ | 현재 프로젝트에 추가 아이디어가 생겼어 확인해줘 |
| 08-12 | `bbe6055e` | 지도 | ✅ | 데이터셋 병합 작업 브레인스토밍을 진행하기 앞써 작업  |
| 08-13 | `791095c2` | 지도 | — | SD카드·USB 허브 조사 |
| 08-13 | `aa9cec95` | 결론 | ✅ | 공구 입력 A-2 · 네거티브 보강 중단 |
| 08-15 | `0e0e8c17` | 지도 | ✅ | pi2에 사용할 sd카드를 pi1에 usb에 연결하였어 |
| 08-15 | `50891fa5` | 지도 | ✅ | UI 애니메이션 · 작업 초기화 |
| 08-15 | `9a8b3463` | 지도 | ✅ | 현재 HW를 사용가능한 환경이야 tool.v3와 sop |
| 08-17 | `efe6cca3` | 지도 | — | 공모전 파악 · 보고서 착수 |
| 08-18 | `1461df7a` | 지도 | — | 파이 홈 폴더 정리 |
| 08-18 | `42619f0d` | 지도 | — | - sop 프로그램 기능 중 다음작업시작 버튼을 제거하 |
| 08-23 | `65638bb6` | **본문** | ✅ | 통합 문서가 정본으로써 역할을 잘 하고 있는지 검토하고 |
| 08-24 | `5c97a44b` | 지도 | ✅ | 7월부터 지금까지 진행한 프로젝트 작업을 시간 순서대로 |
| 08-24 | `9801dc86` | 지도 | ✅ | git pull 건에 대해 확인만 해줘 pull 하지  |
| 08-25 | `e55e95ca` | 결론 | ○ | 발표 PPT 자료 점검 |
| 08-26 | `0f9bdbaf` | 지도 | ✅ | 음성비서 작업에 대해 현재 현황 파악해줘 다음 작업이  |
| 08-26 | `c814e9ec` | 지도 | ✅ | esp32 연결 상태 확인해줘 |
| 08-26 | `d97f5518` | 결론 | ○ | 카메라 90° 회전 보정 |
| 08-26 | `f8fcecdc` | 지도 | ✅ | esp32 연결 상태 확인해줘 |
| 08-27 | `1b03891d` | 결론 | ○ | 인터락 작동 시점 재정의 |
| 08-27 | `d269e6d5` | 결론 | ○ | 커패시터 3종 입수 · 노트북 환경 |
| 08-28 | `12ed2c28` | 지도 | — | 개발환경 표 · 가치와 제작노력 |
| 08-30 | `625ef30b` | 결론 | ○ | HW 없이 가능한 작업 정리 |
| 08-31 | `2c09d49e` | 결론 | ○ | 펌웨어 3줄 · 코드 이미지 |
| 08-31 | `35b266bc` | 결론 | ○ | 보고서용 이미지 선별 |
| 09-01 | `6d034bf1` | 결론 | ✅ | 음성 비서 작업을 이어 진행하자 현재 음성비서용 ESP |
| 09-01 | `dffb154d` | 지도 | — | pi2 안전 종료 |
| 09-02 | `030ca218` | 지도 | — | 개발보고서 개요 그림 제작 |
| 09-02 | `92229f89` | 지도 | ✅ | 밀린 작업 정리해서 알려줘 (현재 HW 사용 불가능) |
| 09-02 | `e38a44b3` | 지도 | — | 통합 프로세스 흐름도 제작 |
| 09-03 | `1b004b69` | 지도 | — | SW·HW 구성 문안 |
| 09-03 | `1e79f4c8` | **본문** | ○ | 개발보고서 3~5장 수정 |
| 09-03 | `3e4f101a` | **본문** | ✅ | 프로젝트 시연 영상 촬영을 위해 필용한 기능을 만들어야 |
| 09-03 | `75c0669f` | 지도 | ✅ | 버튼 입력과 인터락 확인하고 싶어 |
| 09-03 | `aa6fb348` | 지도 | — | 버튼 입력과 인터락 확인하고 싶어 |
| 09-03 | `cf516f07` | **본문** | ○ | 현재 네트워크 상태 확인해줘 |
| 09-04 | `11fd399e` | **본문** | ✅ | esp32 네트워크 성능 저하 문제 때문에 현재 시연  |
| 09-04 | `15f41511` | **본문** | ○ | 버튼 입력과 인터락 확인하고 싶어 |
| 09-04 | `730420c8` | **본문** | ○ | 어제 10시 10분 쯤 라즈베리파이 전원이 나가서 현재 |
| 09-04 | `c419d57e` | **본문** | ○ | 현재 파이 네트워크 상태 확인해줘 ssh로 연결한 노트 |
| 09-05 | `067ebb47` | 지도 | — | 제작설계서 착수 |
| 09-05 | `9eb3cb46` | 결론 | ○ | 카메라 모듈 조사 |
| 09-06 | `3ee3d7c9` | **본문** | ○ | 제작설계서 제작 |
| 09-07 | `60557982` | **본문** | ○ | 제출본 대조 · 정본 점검 |
| 09-07 | `df612aa6` | 지도 | — | 파이 안전 종료 |
| 09-10 | `0a1fab9f` | 지도 | — | pi1·pi2 네트워크 점검 |
| 09-11 | `17c2ff79` | 지도 | — | 이번 세션 — 전수 점검 |

**총 47 — 본문 11 · 결론 11 · 지도 25**

### 8.3 🔴 이 확인이 닿지 못하는 곳

| 한계 | 내용 |
| --- | --- |
| **2026-08-10 이전** | 세션 기록이 30일 보존 정책으로 삭제됐다(2026-09-11 에 50일로 늘렸으나 소급되지 않는다). 그 구간은 **커밋 날짜 ↔ 작업로그 블록 대조**로만 확인했다 — 저널이 2026-07-01 에 시작했고, 저널 이후 블록 없는 날 4일은 전부 다른 로그·본문에 내용이 있었다. **실질 공백 없음** |
| **커밋도 파일도 안 남은 변경** | 파이 설정·결선처럼 저장소 밖에서 일어난 것은 대화에 언급될 때만 잡힌다. 8월 29일 WiFi 타이머가 그 예다 — 9월 3일 대화 덕에 알았고, 그 세션 자체는 이미 삭제됐다 |
| **「지도」 깊이 25개** | 주제·산출물·커밋은 확인했으나 본문은 안 읽었다. 그 안의 **기각한 대안·판단 근거**는 더 나올 수 있다 |

### 8.4 결론

- **갈래별 미기록 항목은 §2 가, 사실 어긋남은 §1 이, 최종본 대조는 §5 가, 3차 발견은 §7 이 담고 있다.**
- 처리 대기 = §4(1~10) · 일괄 묶음 = §4.5
- 🔑 **8월 세션은 기록이 잘 돼 있었고, 공백은 9월에 몰려 있다.** 원인은 §3 의 넷 — 병행 세션, 부정형 결말, 저장소 밖 변경, 제출문서가 정본을 앞지른 것

---

## 9. 🆕 2026-09-11 추가 확인 3건

### 9.1 ✅ 「표시 화면 ↔ 판정 화면 분리」는 **이미 돼 있다**

사용자 질문(2026-09-11): *"디스플레이에 보여지는 것과 판정 화면을 따로 구분할 수 있나?"*

**이미 그렇게 만들어져 있다.** 2026-08-26 회전 보정 때 같은 목적으로 넣은 것이다.

| | 무엇을 쓰나 |
| --- | --- |
| **화면 표시** | `CAMERA_DISPLAY_FILL=True` → 세로 프레임을 가로 UI 에 **채우고 넘치는 만큼 잘라낸다**(좌우 검은 띠 제거). 자르는 곳 = `safety_console.py:621-630` |
| **검출 · ROI 판정 · 녹화** | **잘리지 않은 전체 프레임**(480×640) |

🔑 **그래서 화면 가장자리에서 검출 박스가 잘려 보여도 시스템이 못 본 것이 아니다.** `config.py:194-199` 주석이 이미 그렇게 못박고 있다.
🔴 다만 **통합문서에는 이 분리가 0회**다 — §7.1 의 회전과 함께 정본에 넣어야 한다.

⏸ **더 갈라야 할 것이 남았는지는 별개 문제다.** 지금은 「같은 프레임을 자르기만」 하는 분리다. 판정을 **다른 방향·다른 해상도**로 돌리는 것(예: 판정은 640×480 가로, 표시는 세로)은 지금 구조가 아니며, 그렇게 하면 왜곡보정 맵·모델 학습 도메인이 전부 걸린다.

### 9.2 카메라 모듈 교체 — `OV5640` 로 바꾸면 무엇이 달라지나

> 사용자 요청(2026-09-11): 바꿀 생각은 있으나 **무엇이 달라지는지만** 먼저 본다. 대상 = **Seeed 정품 OV5640 for XIAO ESP32S3 Sense**(방열판 포함, 드롭인 교체).

| | 현재 `OV3660` | `OV5640` |
| --- | --- | --- |
| 최대 해상도 | 3MP (2048×1536) | **5MP (2592×1944)** |
| 광학 포맷 | 1/5″ | **1/4″** |
| 화소 크기 | 1.4µm | **1.4µm (같다)** |
| 초점 | 고정 | 🔑 **오토포커스** |
| 프레임률(사양) | 720p 30 · 3MP 15 | 1080p 30 · 720p 60 · **VGA 90** |

**우리 조건에서 실제로 달라질 것 — 셋으로 갈린다.**

| | 판단 |
| --- | --- |
| ❌ **속도는 안 오른다** | 우리 병목은 센서가 아니다. DVP 8비트 병렬 버스(20MHz)·PSRAM 경합·무선 링크가 상한이고, 실측 **27.8fps**(§10.60)가 이미 그 근처다. 커뮤니티도 XIAO+OV5640 에서 **25fps 안팎**을 보고한다 |
| 🔑 **초점은 달라질 수 있다** | 오토포커스가 유일한 실질 차이다. 우리 미해결 중 **쥔 공구 검출 2%**·**원거리 재현율**이 선명도와 얽혀 있어(§10.9~§10.10 계열) **가설로는 값이 있다.** 단 1인칭 근접 구도에서 AF 가 계속 재초점을 잡으면 오히려 흔들릴 수 있다 — **재 봐야 아는 것** |
| ⚠️ **저조도는 나아지지 않는다** | 화소 크기가 **1.4µm 로 동일**하다. 포맷이 커진 만큼 화소 수가 늘 뿐이라, 같은 VGA 로 쓰면 픽셀당 수광은 그대로다 |

🔴 **교체하면 따라오는 비용 3가지**
1. **재캘리브레이션 필수** — 왜곡보정 맵은 현 렌즈 전용이다(`camera_calibration.npz`). 렌즈가 바뀌면 §10.57 의 미판정 항목이 다시 열린다
2. **모델 재검증** — `console_v2`·`tool_v3` 는 **현 카메라 화질로 학습·평가**됐다. §10.9~§10.10 이 *"화질이 B4 검출을 좌우한다"* 를 실증했으므로, 화질이 바뀌면 그 값들이 그대로 유효하지 않다
3. **펌웨어 여유** — `fb_count=2`(§10.34)로 얻은 2.4배가 메모리 여유에 기대고 있다. 더 큰 센서는 PSRAM 을 더 쓴다

▶ **권고 — 교체 전에 「초점」 하나만 재는 실험을 먼저 한다.** 지금 카메라로 **손에 쥔 공구 거리**에서 선명도(라플라시안 분산)를 재 두면, 교체 후 같은 자리에서 재어 **AF 가 실제로 무엇을 바꿨는지**가 한 숫자로 갈린다. 이 기준선이 없으면 교체 후에도 좋아졌는지 말할 수 없다.

### 9.3 ⏸ 버튼 학습셋 박스 수 — 조회 실패

- 로컬에 `console_v2` 데이터셋 사본이 **없다**(`~/data` 에는 공구 데이터셋만). Roboflow 안에만 있다
- **Roboflow MCP 토큰이 만료**됐다(`OAuth token is invalid or expired`). 파이에 API 키도 저장돼 있지 않다
- ▶ 재연결(`/mcp`) 하거나 키를 주시면 바로 조회한다. 급하지 않으면 두어도 된다 — test 113장 **539개**는 §10.20 에 있다
