# 글라스 UI 3차 수정 — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> ⚠️ **`subagent-driven-development`는 쓰지 않는다** — 태스크 대부분이 `overlay_menu.py` 한 파일로 수렴해 병렬성이 없다(CC작업문서 §2.2).

**Goal:** 배치·정렬 5건을 다듬고, 녹화 저장 폴더 열기와 CCTV 전환 확인 팝업을 더한다.

**Architecture:** 전부 **표시 계층**이다. `overlay_menu.py`(알림·메뉴·점검·녹화·설정)와 `overlay.py`(`ConnBar`)를 고치고, `safety_console.py`는 배선과 확인 팝업만 손댄다. 판정·통신 코드는 건드리지 않는다.

**Tech Stack:** PyQt6 6.11 · Python 3.13 · 기존 `theme.py`/`config.py`

**정본 문서:** `docs/superpowers/specs/2026-08-04-글라스-UI-3차수정-design.md`

## Global Constraints

- 🔴 **FSM·인터락·GPIO 배선을 바꾸지 않는다.** 이번 수정도 전부 표시 계층이다.
- 🔴 **안전 UI 3종을 건드리지 않는다** — BLOCK 해제 거부(EMO 미복귀) · 인터락 폴트 경고창 · 종료 확인창.
- 🔴 **메뉴·알림 창의 닫기 방식**(같은 버튼 재클릭)과 **항목 패널의 ✕**(2차 §7)를 그대로 둔다.
- **색·폰트 값을 코드에 박지 않는다** — `theme.C(token)` · `config.font(role, weight)`.
- 🔴 **크기가 고정된 라벨에는 `padding: 0` 을 명시한다** — 시트의 `padding` 이 자식에 상속돼 글리프가 사라진다(2차 Task 6 에서 물림).
- **테스트는 pytest 가 아니다** — `selftest/*.py` 는 `if __name__ == "__main__"` 단독 스크립트다.
- 작업 브랜치 = `feature/glass-ui`. 커밋 서명은 `CLAUDE.md` §4 를 따른다.

---

## 관문 (매 태스크 공통)

**하나라도 실패하면 커밋하지 않는다.**

1. 그 태스크의 셀프테스트 통과
2. `python3 selftest/test_imports.py` 통과
3. `python3 selftest/test_fsm.py` · `test_recipe.py` 통과 — 판정부 회귀 방지
4. 🔴 **`./run_demo.sh` 실기동** — 창이 뜨고 30초 유지

## 중단 규칙

- **실기동에서 GUI 가 죽으면** → 되돌리고 원인부터 찾는다.
- **안전 UI 3종의 동작이 하나라도 달라지면** → 즉시 중단.
- **원인이 계획서와 다르면** → 고치기 전에 멈추고 보고한다.

## 🔴 눈으로 확인하는 법

2차 계획서와 같다. 가상 디스플레이에 **실제로 그려** 캡처한다.

```bash
pgrep -f "Xvfb :77" > /dev/null || (Xvfb :77 -screen 0 1920x1080x24 -nolisten tcp &)
DISPLAY=:77 python3 <스크립트>
```

스크립트 뼈대 — **`QT_QPA_PLATFORM=xcb`** 라야 실제로 그려진다:

```python
import os, sys, time
_DEMO_DIR = "/home/pi/sop-project/Rpi5/Demo"
sys.path.insert(0, _DEMO_DIR); os.chdir(_DEMO_DIR)
os.environ["QT_QPA_PLATFORM"] = "xcb"
os.environ["SOP_USB_CAMERA"] = "0"

import config
config.RECORDING_ENABLED = False
config.CAMERA_TCP_HOST = "127.0.0.1"      # 즉시 거절 — 죽은 IP 는 10초씩 멈춘다
config.TCP_RECV_TIMEOUT_SEC = 0.3
config.TCP_RECONNECT_DELAY_SEC = 0.1

from PyQt6.QtWidgets import QApplication
app = QApplication.instance() or QApplication([])

def pump(sec=0.6):
    end = time.time() + sec
    while time.time() < end:
        app.processEvents(); time.sleep(0.02)

def shot(name):
    pump()
    QApplication.primaryScreen().grabWindow(0).save(f"/tmp/{name}.png")

from safety_console import SafetyConsole
win = SafetyConsole(); win.showMaximized(); pump(1.5)
# ... 확인할 화면을 만들고 shot("이름") ...
win.close(); pump(0.3)
```

⚠️ **Hailo 장치는 하나뿐**이다. 실기동 관문 전에 다른 데모를 먼저 죽인다.
🔴 **`pkill -f "main.py"` 를 쓰지 말 것** — 명령 문자열에 `main.py` 가 들어 있어 **자기 셸까지 죽는다**(2차에서 두 번 물렸다). `ps -eo pid,args | grep "[p]ython3 main.py"` 로 PID 를 찾아 `kill -9 <pid>` 한다.

---

## 파일 구조

| 파일 | 이번에 맡는 것 | 태스크 |
|---|---|---|
| `Demo/overlay.py` | `ConnBar` 를 영상 사각형 기준으로 | 1 |
| `Demo/overlay_menu.py` | 알림 아이콘 정렬 · 메뉴 들여쓰기 · 점검 버튼 · 녹화 패널 · 설정 라디오 | 2·3·4·5·7 |
| `Demo/safety_console.py` | 영상 사각형 전달 · 폴더 열기 · CCTV 확인 팝업 | 1·5·6 |
| `Demo/selftest/test_overlay.py` | `ConnBar` 배치 회귀 | 1 |
| `Demo/selftest/test_overlay_menu.py` | 아이콘 정렬 · 녹화 가로배치 · 설정 라디오 회귀 | 2·5·7 |
| `Demo/selftest/test_console_flow.py` | CCTV 확인 분기 회귀 | 6 |

---

## Task 1: 연결 상태바를 영상 영역으로

**Files:**
- Modify: `Demo/overlay.py` — `ConnBar.relayout` (479~480행)
- Modify: `Demo/safety_console.py` — `_relayout` 의 `self.conn_bar.relayout(r)` (463행 부근)
- Test: `Demo/selftest/test_overlay.py`

**Interfaces:**
- Consumes: `place(widget, parent_rect, right=..., bottom=...)` (overlay.py 44행)
- Produces: `ConnBar.relayout(parent_rect)` — **넘어온 사각형 안** 우하단에 앉는다. 규약은 그대로이고 **콘솔이 영상 사각형을 넘긴다.**

**원인:** `place(..., right=0.03, bottom=0.04)` 가 **창 전체**를 기준으로 삼는다. 영상은 가운데 4:3 이라 창 오른쪽 끝은 검정 레터박스다.

- [x] **Step 1: 실패 테스트를 쓴다**

`test_overlay.py` 의 `test_conn_bar` 뒤에 추가한다:

```python
def test_conn_bar_follows_given_rect():
    """🔴 상태바는 **넘어온 사각형** 안에 앉는다 — 콘솔이 영상 사각형을 넘긴다.

    종전에는 창 전체 기준이라 오른쪽 검정 레터박스에 앉았다(2026-08-04).
    """
    print("\n[N] 상태바 기준 사각형")
    from overlay import ConnBar
    host = QWidget()
    c = ConnBar(host)

    # 1920×1080 창 안의 4:3 영상 = 1440×1080, 좌우 240px 씩 레터박스
    video = QRect(240, 0, 1440, 1080)
    c.relayout(video)
    r = c.geometry()
    check(video.contains(r), f"영상 {video} 안: 상태바 {r}")
    check(r.right() < video.right(), f"영상 오른쪽 끝({video.right()}) 안쪽: {r.right()}")
    check(r.right() > video.center().x(), f"영상 우측 절반: {r.right()}")
```

- [x] **Step 2: 실행해 실패를 확인한다**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_overlay.py`
Expected: ❌ — `place()` 가 `parent_rect` 의 **폭만** 쓰고 `left`/`top` 오프셋을 더하지 않아, 상태바가 영상 왼쪽 밖(x<240)에 앉는다.

- [x] **Step 3: `place()` 가 사각형의 위치를 존중하게 한다**

`overlay.py` 의 `place()` 를 고친다. 지금은 `parent_rect.width()/height()` 만 쓰고 **`left()`/`top()` 을 무시**한다. 오프셋을 더한다:

```python
def place(widget, parent_rect, left=None, top=None, width=None,
          right=None, bottom=None, height=None):
    """부모 사각형 안에 비율로 배치한다. 픽셀 계산을 한 곳에 모은다.

    🔴 parent_rect 의 **좌상단 오프셋을 존중한다** — 창 전체가 아니라 영상 사각형
       처럼 화면 일부를 넘겨받는 경우가 있다(연결 상태바, 2026-08-04).
       종전에는 폭·높이만 써서 넘겨준 사각형 밖에 앉았다.
    """
    pw, ph = parent_rect.width(), parent_rect.height()
    w = int(pw * width) if width is not None else widget.sizeHint().width()
    h = int(ph * height) if height is not None else widget.sizeHint().height()
    if left is not None:
        x = int(pw * left)
    elif right is not None:
        x = pw - int(pw * right) - w
    else:
        x = (pw - w) // 2
    if top is not None:
        y = int(ph * top)
    elif bottom is not None:
        y = ph - int(ph * bottom) - h
    else:
        y = (ph - h) // 2
    widget.setGeometry(QRect(parent_rect.left() + x, parent_rect.top() + y, w, h))
```

⚠️ **다른 오버레이는 영향받지 않는다** — `_relayout` 이 넘기는 `self._root.rect()` 는 좌상단이 `(0, 0)` 이라 오프셋이 0 이다.

- [x] **Step 4: 콘솔이 영상 사각형을 넘긴다**

`safety_console.py` `_relayout` 에서 `self.conn_bar.relayout(r)` 을 바꾼다:

```python
        # 🔴 상태바만 **영상 사각형** 기준이다 — 창 기준이면 오른쪽 검정
        #    레터박스에 앉는다(2026-08-04).
        self.conn_bar.relayout(self.camera_label.geometry())
```

- [x] **Step 5: 테스트가 통과하는지 확인한다**

Run: `python3 selftest/test_overlay.py`
Expected: 전부 ✅ (기존 `test_conn_bar` 도 통과해야 한다 — `SCREEN` 은 좌상단이 `(0,0)` 이라 결과가 같다)

- [x] **Step 6: 눈으로 확인한다**

「눈으로 확인하는 법」 뼈대에 이어 붙인다:

```python
pump(2.0)
print("영상 :", win.camera_label.geometry())
print("상태바:", win.conn_bar.geometry())
shot("t1_connbar")
```

확인할 것 — 상태바가 **영상 안쪽** 우하단에 있는가(검정 레터박스가 아니라). 출력된 두 사각형으로 숫자도 대조한다.

- [x] **Step 7: 관문 1~4 → 커밋**

```bash
python3 selftest/test_overlay.py && python3 selftest/test_imports.py \
  && python3 selftest/test_fsm.py && python3 selftest/test_recipe.py
ps -eo pid,args | grep "[p]ython3 main.py"     # 있으면 kill -9 <pid>
DISPLAY=:0 ./run_demo.sh                        # 30초 유지 확인
git add Demo/overlay.py Demo/safety_console.py Demo/selftest/test_overlay.py
git commit -m "fix(ui): 연결 상태바를 영상 영역 안으로 (Task 1)"
```

---

## Task 2: 알림 아이콘을 제목 줄에 맞춘다

**Files:**
- Modify: `Demo/overlay_menu.py` — `NotifyPanel.push` (285~295행)
- Test: `Demo/selftest/test_overlay_menu.py`

**Interfaces:**
- Consumes: 없음
- Produces: 없음

**원인:** 아이콘에 `setAlignment(Qt.AlignmentFlag.AlignTop)` 을 줘서 행 **좌상단 꼭짓점**에 붙는다.

🔴 **행 전체의 세로 중앙에 맞추면 안 된다** — 부가 설명이 있는 2줄짜리 알림에서 아이콘이 제목과 설명 **사이**에 뜬다. **제목 줄**에 맞춘다.

- [x] **Step 1: 실패 테스트를 쓴다**

```python
def test_notify_icon_aligned_with_title():
    """🔴 아이콘은 **제목 줄**과 세로 중앙이 맞아야 한다 (2026-08-04).

    종전에는 AlignTop 이라 행 좌상단 꼭짓점에 붙어 제목과 어긋나 보였다.
    ⚠️ 행 전체의 중앙에 맞추면 2줄짜리 알림에서 제목과 설명 사이에 뜬다.
    """
    print("\n[21] 알림 아이콘 정렬")
    host = QWidget()
    host.setGeometry(SCREEN)
    pnl = NotifyPanel(host)
    pnl.relayout(SCREEN)
    pnl.push("warn", "순서가 다릅니다", "부가 설명 한 줄")
    pnl.show()
    pnl.layout().activate()

    row = pnl._rows[-1]
    row.layout().activate()
    lbls = row.findChildren(QLabel)
    icon, title = lbls[0], lbls[1]
    ic, tc = icon.geometry().center().y(), title.geometry().center().y()
    check(abs(ic - tc) <= 4, f"아이콘 중심 {ic} ≈ 제목 중심 {tc} (차이 {abs(ic - tc)}px)")
    pnl.hide()
```

- [x] **Step 2: 실행해 실패를 확인한다**

Run: `python3 selftest/test_overlay_menu.py`
Expected: `[21]` 에서 ❌ — 아이콘이 위로 붙어 제목 중심과 여러 px 어긋난다.

- [x] **Step 3: 제목 줄에 맞춘다**

아이콘 높이를 **제목 줄 높이로 고정**하고 그 안에서 중앙 정렬하면, 위에서부터 제목과 나란히 놓인다.

🔴 **순서가 중요하다** — 제목 라벨 `t` 의 높이를 써야 하므로 **`t` 를 만든 뒤에** 아이콘 높이를 정한다. 지금 코드는 아이콘을 먼저 만들고 있으므로, `push()` 의 해당 구간을 아래 순서로 바꾼다:

```python
        icon = QLabel(mark)
        # ⚠️ weight 800 으로 두면 이모지가 아예 안 그려진다(2026-08-04, 2차 Task 6)
        icon.setFont(config.font("body", 600))
        # 🔴 padding: 0 을 반드시 명시한다 — 시트의 padding 이 자식에 상속되면
        #    폭이 고정된 아이콘은 눌려 글리프가 사라진다(2차 Task 6 에서 물림)
        icon.setStyleSheet(theme.text_qss(token, 600) + "padding: 0;")
        icon.setFixedWidth(26)          # 이모지는 종전 기호보다 넓다(20 → 26)

        col = QVBoxLayout()
        col.setSpacing(1)
        t = QLabel(title)
        t.setFont(config.font("body", 600))
        t.setStyleSheet(theme.text_qss("text", 600))
        t.setWordWrap(True)
        col.addWidget(t)

        # 🔴 **제목 줄**에 맞춘다 — AlignTop 이면 행 꼭짓점에 붙어 어긋나고,
        #    행 전체 중앙에 맞추면 2줄짜리에서 제목과 설명 사이에 뜬다(2026-08-04).
        #    ⚠️ t 를 만든 **뒤**라야 제목 줄 높이를 알 수 있다.
        icon.setFixedHeight(t.sizeHint().height())
        icon.setAlignment(Qt.AlignmentFlag.AlignCenter)
```

즉 **종전의 `icon.setAlignment(Qt.AlignmentFlag.AlignTop)` 한 줄을 지우고**, 위 두 줄을 `col.addWidget(t)` 다음에 넣는다. 부가 설명(`if sub:` 블록)과 `col.addWidget(s)` 는 그대로 둔다.

그리고 행 레이아웃이 아이콘 칸을 위로 붙이게 한다 — 안 그러면 2줄일 때 아이콘 칸이 세로로 늘어나 다시 가운데로 내려간다:

```python
        rl.addWidget(icon, 0, Qt.AlignmentFlag.AlignTop)
        rl.addLayout(col, 1)
```

- [x] **Step 4: 테스트가 통과하는지 확인한다**

Run: `python3 selftest/test_overlay_menu.py`
Expected: `[21]` ✅

- [x] **Step 5: 눈으로 확인한다**

```python
for kind, title in (("check", "점검 완료"), ("work", "작업을 시작합니다"),
                    ("warn", "순서가 다릅니다"), ("danger", "전기 입력 차단됨")):
    win._notify(kind, title, "부가 설명 한 줄")
win._toggle_notify(True)
shot("t2_notify")
```

확인할 것 — 아이콘이 **제목 글자와 같은 높이**에 있는가. 1줄짜리(부가 설명 없는 것)도 함께 넣어 두 경우 모두 본다.

- [x] **Step 6: 관문 1~4 → 커밋**

```bash
git add Demo/overlay_menu.py Demo/selftest/test_overlay_menu.py
git commit -m "fix(ui): 알림 아이콘을 제목 줄에 맞춤 (Task 2)"
```

---

## Task 3: 메뉴 글자 들여쓰기

**Files:**
- Modify: `Demo/overlay_menu.py` — `_row_button`(86~95행) · `MenuPanel.__init__` 의 「＋」(197행 부근) · `MenuPanel.apply_theme`(215~226행)

**Interfaces:**
- Consumes: 없음
- Produces: `_MENU_PAD_LEFT = 12` — 메뉴 항목·제목·「＋」·종료가 **같은 값**을 쓴다.

**증상:** 폭을 1.5배로 넓혔더니(2차 §4) 글자가 왼쪽에 치우쳐 보인다. 지금 좌측 여백은 `padding: 9px 2px` 의 2px 다.

- [x] **Step 1: 상수를 만들고 항목에 적용한다**

🔴 **네 군데(항목·제목·「＋」·종료)가 같은 값을 써야 세로선이 하나로 보인다.** 값을 흩어 놓지 말고 상수로 둔다. `_MENU_POS` 아래에 추가한다:

```python
# 메뉴 항목의 좌측 들여쓰기 — 🔴 제목·「＋」·종료가 **같은 값**을 써야 글자
#    시작점이 세로로 한 줄이 된다(2026-08-04. 종전 2px 라 왼쪽에 치우쳐 보였다).
_MENU_PAD_LEFT = 12
```

`_row_button` 을 바꾼다:

```python
def _row_button(text, token="text"):
    b = QPushButton(text)
    b.setFont(config.font("body", 600))
    b.setCursor(Qt.CursorShape.PointingHandCursor)
    b.setFlat(True)
    b.setStyleSheet(
        f"QPushButton {{ color: {theme.C(token)}; background: transparent; border: none;"
        f" text-align: left; padding: 9px 2px 9px {_MENU_PAD_LEFT}px; }}"
        f"QPushButton:hover {{ color: {theme.C('info')}; }}")
    return b
```

- [x] **Step 2: `apply_theme` 의 사본도 맞춘다**

`MenuPanel.apply_theme` 은 항목·종료·「＋」의 스타일을 **다시 쓴다**(테마 전환 때 덮어쓰므로 여기도 고쳐야 한다):

```python
    def apply_theme(self):
        super().apply_theme()
        self._title.setStyleSheet(theme.text_qss("label", 700)
                                  + f"padding-left: {_MENU_PAD_LEFT}px;")
        for b in self._items:
            b.setStyleSheet(
                f"QPushButton {{ color: {theme.C('text')}; background: transparent;"
                f" border: none; text-align: left;"
                f" padding: 9px 2px 9px {_MENU_PAD_LEFT}px; }}"
                f"QPushButton:hover {{ color: {theme.C('info')}; }}")
        self._shutdown.setStyleSheet(
            f"QPushButton {{ color: {theme.C('danger')}; background: transparent;"
            f" border: none; text-align: left;"
            f" padding: 9px 2px 9px {_MENU_PAD_LEFT}px; font-weight: 700; }}")
        for f in self._free:
            f.setStyleSheet(f"color: {theme.C('todo')}; background: transparent;"
                            f" padding: 9px 2px 9px {_MENU_PAD_LEFT}px;")
        for line in self._lines:
            line.setStyleSheet(f"background-color: {theme.C('panel_border')}; border: none;")
```

「＋」의 생성 시 스타일(`MenuPanel.__init__`)도 같은 값으로 바꾼다:

```python
            f.setStyleSheet(f"color: {theme.C('todo')};"
                            f" padding: 9px 2px 9px {_MENU_PAD_LEFT}px;")
```

- [x] **Step 3: 셀프테스트가 여전히 통과하는지 확인한다**

Run: `python3 selftest/test_overlay_menu.py`
Expected: 전부 ✅ (기존 `test_menu_width_widened` 의 "가장 긴 항목 + 여백 ≤ 폭" 이 여전히 맞는지 본다 — 폭 374px 에 108px 항목이라 여유가 크다)

- [x] **Step 4: 눈으로 확인한다**

```python
win._toggle_menu(True)
pump(0.6)
shot("t3_menu")
```

확인할 것 — ①항목 글자가 왼쪽에 치우치지 않는가 ②**제목「메뉴」·항목·「＋」·「시스템 종료」의 글자 시작점이 세로로 한 줄인가** ③오른쪽이 잘리지 않는가.

- [x] **Step 5: 관문 1~4 → 커밋**

```bash
git add Demo/overlay_menu.py
git commit -m "fix(ui): 메뉴 글자 들여쓰기 — 시작점을 한 줄로 (Task 3)"
```

---

## Task 4: 점검 「재연결」 버튼을 내용 쪽으로

**Files:**
- Modify: `Demo/overlay_menu.py` — `CheckPanel.update_results` (523~535행)

**Interfaces:**
- Consumes: 없음
- Produces: 없음

**원인:** `rl.addLayout(col, 1)` 로 이름 칸이 남는 공간을 **모두** 가져가 버튼을 오른쪽 끝으로 민다.

- [x] **Step 1: 이름 칸이 공간을 독차지하지 않게 한다**

`update_results` 의 행 조립 부분을 바꾼다:

```python
            rl.addWidget(mark)
            # 🔴 stretch 를 이름 칸이 아니라 **이름과 버튼 사이**에 둔다 —
            #    이름 칸에 주면 남는 공간을 전부 먹어 버튼이 오른쪽 끝으로 밀린다
            #    (2026-08-04. 항목 이름과 버튼이 화면 폭만큼 벌어져 보였다).
            rl.addLayout(col)
            rl.addStretch(1)
```

버튼을 붙이는 부분은 그대로 두되, **버튼 뒤에 여백을 준다** — 패널 오른쪽 가장자리에 딱 붙지 않게:

```python
                btn.clicked.connect(lambda _=False, k=r.key: self.retry_requested.emit(k))
                rl.addWidget(btn)
                rl.addSpacing(4)
```

- [x] **Step 2: 셀프테스트가 여전히 통과하는지 확인한다**

Run: `python3 selftest/test_overlay_menu.py`
Expected: 기존 `test_check_panel` 전부 ✅ (행 수·버튼 유무·신호를 보므로 배치 변경에 영향받지 않는다)

- [x] **Step 3: 눈으로 확인한다**

```python
win._open_check()
pump(0.8)
shot("t4_check")
```

확인할 것 — 「재연결」 버튼이 **항목 이름 가까이** 왔는가, 패널 밖으로 나가지 않았는가, 실패 항목에만 붙어 있는가.

- [x] **Step 4: 관문 1~4 → 커밋**

```bash
git add Demo/overlay_menu.py
git commit -m "fix(ui): 점검 「재연결」 버튼을 내용 쪽으로 (Task 4)"
```

---

## Task 5: 녹화 패널 — 가로 배치 + 저장 폴더 열기

**Files:**
- Modify: `Demo/overlay_menu.py` — `RecordPanel.__init__`(578~600행) · `set_state` · `apply_theme`
- Modify: `Demo/safety_console.py` — `_init_ui` 의 `record_panel` 배선(392~394행 부근)
- Test: `Demo/selftest/test_overlay_menu.py`

**Interfaces:**
- Consumes: `config.RECORDING_SAVE_DIR` (config.py 142행)
- Produces:
  - `RecordPanel.open_folder_requested` — `pyqtSignal()`. 저장 폴더 줄을 누르면 나온다.
  - `RecordPanel.set_save_dir(path: str) -> None` — 화면에 보일 저장 경로를 넣는다.
  - `SafetyConsole._open_recordings_folder() -> None` — 파일관리자로 폴더를 연다. **실패해도 예외를 밖으로 내보내지 않는다.**

- [x] **Step 1: 실패 테스트를 쓴다**

```python
def test_record_buttons_side_by_side():
    """🔴 시작 버튼 2개는 가로로 나란히 (2026-08-04).

    세로로 쌓여 있어 패널이 가로로 지나치게 길었다.
    """
    print("\n[22] 녹화 버튼 가로 배치")
    from overlay_menu import RecordPanel
    host = QWidget()
    host.setGeometry(SCREEN)
    p = RecordPanel(host)
    p.relayout(SCREEN)
    p.show()
    p.layout().activate()

    a, b = p._btn_full.geometry(), p._btn_cam.geometry()
    check(abs(a.center().y() - b.center().y()) <= 2,
          f"두 버튼이 같은 줄: y={a.center().y()} / {b.center().y()}")
    check(a.right() <= b.left(), f"가로로 나란히: {a.right()} ≤ {b.left()}")
    p.hide()


def test_record_folder_row():
    """저장 경로를 보여 주고, 누르면 폴더 열기 신호가 나온다."""
    print("\n[23] 녹화 저장 폴더")
    from overlay_menu import RecordPanel
    host = QWidget()
    p = RecordPanel(host)
    p.set_save_dir("/home/pi/sop-project/Rpi5/Demo/recordings")
    check("recordings" in p._folder.text(), f"경로 표시: {p._folder.text()}")

    got = []
    p.open_folder_requested.connect(lambda: got.append(1))
    p._folder.click()
    check(got == [1], f"누르면 신호: {got}")
```

- [x] **Step 2: 실행해 실패를 확인한다**

Run: `python3 selftest/test_overlay_menu.py`
Expected: `[22]` 에서 ❌(두 버튼이 세로로 쌓여 y 가 다르다), `[23]` 에서 `AttributeError: 'RecordPanel' object has no attribute 'set_save_dir'`

- [x] **Step 3: 버튼을 가로로 놓고 폴더 줄을 만든다**

`RecordPanel` 에 시그널을 더한다(클래스 맨 위, `start_requested` 옆):

```python
    open_folder_requested = pyqtSignal()      # 저장 폴더를 열어 달라
```

`__init__` 의 버튼 부분을 바꾼다. 🔴 **글자를 짧게 줄인다** — 가로로 나란히 놓이므로 긴 글자는 안 들어간다:

```python
        # 🔴 시작 버튼 2개는 **가로로** 나란히 — 세로로 쌓으니 패널이 가로로
        #    지나치게 길었다(2026-08-04). 글자도 짧게 줄인다.
        self._btn_full = QPushButton("전체 화면")
        self._btn_cam = QPushButton("카메라 영역")
        self._btn_stop = QPushButton("녹화 중지")
        btn_row = QHBoxLayout()
        btn_row.setSpacing(8)
        for b, mode in ((self._btn_full, "full"), (self._btn_cam, "camera")):
            b.setFont(config.font("body", 700))
            b.setCursor(Qt.CursorShape.PointingHandCursor)
            b.clicked.connect(lambda _=False, m=mode: self.start_requested.emit(m))
            btn_row.addWidget(b)
        lay.addLayout(btn_row)
        self._btn_stop.setFont(config.font("body", 700))
        self._btn_stop.setCursor(Qt.CursorShape.PointingHandCursor)
        self._btn_stop.clicked.connect(lambda: self.stop_requested.emit())
        lay.addWidget(self._btn_stop)
```

`self._note` 를 만든 뒤(그 다음 줄)에 폴더 줄을 더한다:

```python
        # 저장 폴더 — 누르면 파일관리자로 연다(design §7)
        self._folder = QPushButton("")
        self._folder.setFont(config.font("small", 600))
        self._folder.setCursor(Qt.CursorShape.PointingHandCursor)
        self._folder.setFlat(True)
        self._folder.setFocusPolicy(Qt.FocusPolicy.NoFocus)
        self._folder.clicked.connect(lambda: self.open_folder_requested.emit())
        lay.addWidget(self._folder)
```

그리고 `set_save_dir` 를 더한다(`set_state` 옆):

```python
    def set_save_dir(self, path):
        """화면에 보일 저장 경로. 🔴 경로를 코드에 박지 않는다 — config 가 정본이다."""
        self._save_dir = path
        self._folder.setText(f"📁  {path}")
```

`apply_theme` 에 폴더 줄 색을 더한다(`self._title.setStyleSheet(...)` 다음):

```python
        self._folder.setStyleSheet(
            f"QPushButton {{ color: {theme.C('label')}; background: transparent;"
            f" border: none; text-align: left; padding: 4px 2px; }}"
            f"QPushButton:hover {{ color: {theme.C('info')}; }}")
```

- [x] **Step 4: 테스트가 통과하는지 확인한다**

Run: `python3 selftest/test_overlay_menu.py`
Expected: `[22]`·`[23]` 전부 ✅

- [x] **Step 5: 콘솔이 경로를 넣고 폴더를 연다**

`safety_console.py` `_init_ui` 의 `record_panel` 배선에 더한다:

```python
        self.record_panel.set_save_dir(RECORDING_SAVE_DIR)
        self.record_panel.open_folder_requested.connect(self._open_recordings_folder)
```

그리고 `_open_record` 근처에 메서드를 더한다:

```python
    def _open_recordings_folder(self):
        """저장 폴더를 파일관리자로 연다 (design §7).

        🔴 실패해도 GUI 가 멈추면 안 된다 — 파일관리자가 없거나 실행이 실패하면
           경로를 로그에 남기고 조용히 넘어간다. 시연 중에 예외로 죽는 쪽이 훨씬 나쁘다.
        """
        os.makedirs(RECORDING_SAVE_DIR, exist_ok=True)
        try:
            subprocess.Popen(["xdg-open", RECORDING_SAVE_DIR])
            self._append_log(f"[녹화] 저장 폴더 열기: {RECORDING_SAVE_DIR}")
        except Exception as e:
            self._append_log(f"[녹화] 폴더 열기 실패({type(e).__name__}) — 경로: {RECORDING_SAVE_DIR}")
```

⚠️ `os` · `subprocess` · `RECORDING_SAVE_DIR` 은 이미 import 돼 있다(`safety_console.py` 1·3·19행).

- [x] **Step 6: 눈으로 확인한다**

```python
win._open_record()
pump(0.8)
print("전체:", win.record_panel._btn_full.geometry())
print("카메라:", win.record_panel._btn_cam.geometry())
print("폴더:", win.record_panel._folder.text())
shot("t5_record")
```

확인할 것 — ①두 버튼이 **가로로 나란한가** ②글자가 버튼 안에 들어가는가(잘리면 더 줄인다) ③폴더 줄의 경로가 패널 폭 안에 들어가는가(길면 앞을 `…` 로 줄인다).

⚠️ **폴더가 실제로 열리는지는 실기동에서 마우스로 확인한다** — 가상 디스플레이에서 파일관리자를 띄우면 화면만 어지럽다.

- [x] **Step 7: 관문 1~4 → 커밋**

```bash
git add Demo/overlay_menu.py Demo/safety_console.py Demo/selftest/test_overlay_menu.py
git commit -m "feat(ui): 녹화 버튼 가로 배치 + 저장 폴더 열기 (Task 5)"
```

---

## Task 6: CCTV 전환 확인 팝업

**Files:**
- Modify: `Demo/safety_console.py` — `_toggle_camera_source` (581~584행)
- Test: `Demo/selftest/test_console_flow.py`

**Interfaces:**
- Consumes: `SafetyConsole._switch_camera(source, quiet=False)` (525행)
- Produces:
  - `SafetyConsole._confirm_camera_switch(target: str) -> bool` — 확인창을 띄우고 결과를 준다. 🔴 **여기서만 모달을 띄운다.**
  - `SafetyConsole._toggle_camera_source()` — 확인을 받으면 전환한다.

🔴 **묻는 부분과 바꾸는 부분을 분리한다** — 테스트가 `_toggle_camera_source()` 를 그대로 부르면 모달이 응답을 기다리며 **화면이 멎는다.** 테스트는 `_confirm_camera_switch` 를 가짜로 바꿔치기해 「예/아니요를 받은 뒤」의 동작만 본다.

- [x] **Step 1: 실패 테스트를 쓴다**

`test_console_flow.py` 에 추가한다:

```python
def test_cctv_switch_needs_confirm():
    """🔴 CCTV 전환은 확인을 받고 나서 바뀐다 (2026-08-04).

    실수로 카메라가 바뀌면 감지가 끊긴다. 종료 확인창과 같은 방어다.
    ⚠️ 모달을 실제로 띄우면 테스트가 응답을 기다리며 멎는다 — 확인 함수를
       가짜로 바꿔 「응답을 받은 뒤」만 검사한다.
    """
    print("\n[13] CCTV 전환 확인")
    win = make_console()
    before = win._active_camera

    win._confirm_camera_switch = lambda target: False      # 「아니요」
    win._toggle_camera_source()
    check(win._active_camera == before, f"거절하면 그대로: {win._active_camera}")

    asked = []
    win._confirm_camera_switch = lambda target: asked.append(target) or True   # 「예」
    win._toggle_camera_source()
    check(asked and asked[0] != before, f"전환할 카메라를 묻는다: {asked}")
    check(win._active_camera != before, f"승낙하면 바뀐다: {win._active_camera}")
    win.close()
```

- [x] **Step 2: 실행해 실패를 확인한다**

Run: `python3 selftest/test_console_flow.py`
Expected: `[13]` 첫 항목에서 ❌ — 지금은 묻지 않고 바로 바꾸므로 「아니요」에도 카메라가 바뀐다.

- [x] **Step 3: 확인 단계를 넣는다**

`_toggle_camera_source` 를 바꾼다:

```python
    def _confirm_camera_switch(self, target):
        """전환 확인창. 🔴 모달은 **여기서만** 띄운다 — 분리해 두면 테스트가
        이 함수를 가짜로 바꿔 「응답을 받은 뒤」만 검사할 수 있다.

        기본 선택은 「아니요」다. 실수로 카메라가 바뀌면 감지가 끊긴다
        (종료 확인창과 같은 방어).
        """
        name = "CCTV(USB 웹캠)" if target == "usb" else "초소형카메라(ESP32-S3)"
        reply = QMessageBox.question(
            self,
            "카메라 전환 확인",
            f"{name} 로 전환할까요?\n\n"
            "전환하는 동안 잠시 영상이 끊깁니다.",
            QMessageBox.StandardButton.Yes | QMessageBox.StandardButton.No,
            QMessageBox.StandardButton.No,   # 기본 선택 = 아니오 (오동작 방지)
        )
        return reply == QMessageBox.StandardButton.Yes

    def _toggle_camera_source(self):
        """메뉴의 「CCTV 전환」 — 두 카메라를 번갈아 쓴다. 🔴 확인을 받고 바꾼다."""
        target = "usb" if self._active_camera == "esp32" else "esp32"
        self._toggle_menu(False)
        if not self._confirm_camera_switch(target):
            self._append_log("[카메라] 전환 취소됨")
            return
        self._switch_camera(target)
```

⚠️ `QMessageBox` 는 이미 import 돼 있다(`safety_console.py` 11행).
⚠️ **메뉴를 먼저 닫는다** — 종전 코드는 전환 뒤에 닫았는데, 모달이 뜨는 동안 메뉴가 남아 있으면 지저분하다.

- [x] **Step 4: 테스트가 통과하는지 확인한다**

Run: `python3 selftest/test_console_flow.py`
Expected: `[13]` 전부 ✅

- [x] **Step 5: 관문 1~4 → 커밋**

🔴 **실기동에서 마우스로 확인한다** — 메뉴 → CCTV 전환 → 팝업이 뜨는가, 「아니요」면 그대로인가, 「예」면 바뀌는가.

```bash
git add Demo/safety_console.py Demo/selftest/test_console_flow.py
git commit -m "feat(ui): CCTV 전환 확인 팝업 (Task 6)"
```

---

## Task 7: 설정 라디오 글자 통일

**Files:**
- Modify: `Demo/overlay_menu.py` — `SettingsPanel.__init__`(테마·배경 라디오) · `set_tools` · `apply_theme`
- Test: `Demo/selftest/test_overlay_menu.py`

**Interfaces:**
- Consumes: 없음
- Produces: 없음

**증상:** 「없음 (글자만 — AR 글라스 느낌)」과 「있음」의 글자 위치·크기가 어긋나 보인다.

- [x] **Step 1: 실패 테스트를 쓴다**

```python
def test_settings_radios_uniform():
    """🔴 라디오 4개(테마 2 + 배경 2)의 글자 크기·시작점이 같아야 한다 (2026-08-04)."""
    print("\n[24] 설정 라디오 통일")
    host = QWidget()
    s = SettingsPanel(host)
    s.set_tools(["spanner", "driver"], "spanner")
    s.apply_theme()

    radios = (list(s._tool_buttons.values()) + list(s._theme_group.buttons())
              + list(s._bg_group.buttons()))
    sizes = {b.font().pointSize() for b in radios}
    check(len(sizes) == 1, f"글자 크기 하나로: {sizes}")

    qss = {b.styleSheet() for b in radios}
    check(len(qss) == 1, f"스타일 하나로: {len(qss)}가지")
    check(all("padding" in q for q in qss), "여백이 명시돼 있다")
```

- [x] **Step 2: 실행해 실패를 확인한다**

Run: `python3 selftest/test_overlay_menu.py`
Expected: `[24]` 통과 여부를 **먼저 눈으로 확인한다** — 이미 같은 값을 쓰고 있다면 이 테스트는 통과한다. 그 경우 실제 어긋남은 **글자 길이에 따른 줄바꿈**이므로 Step 3 에서 그것을 고친다.

- [x] **Step 3: 라디오 스타일을 한 곳에서 만든다**

🔴 **테마·배경 라디오는 `__init__` 에서 스타일 없이 만들어지고 `apply_theme` 에서만 스타일을 받는다.** 공구 라디오는 `set_tools` 에서 만들어진다. 세 군데가 어긋나지 않게 **모듈 수준 헬퍼**로 모은다. `_row_button` 아래에 추가한다:

```python
def _radio(text):
    """설정 패널의 라디오 — 🔴 네 개가 같은 크기·같은 시작점이어야 한다.
    스타일은 apply_theme 이 채운다(테마 색이 필요하므로)."""
    rb = QRadioButton(text)
    rb.setFont(config.font("body", 600))
    rb.setCursor(Qt.CursorShape.PointingHandCursor)
    return rb
```

`SettingsPanel.__init__` 의 테마·배경 라디오 생성과 `set_tools` 의 공구 라디오 생성에서 `QRadioButton(...)` + `setFont` + `setCursor` 3줄을 `_radio(...)` 한 줄로 바꾼다. 예를 들어 테마 쪽은:

```python
        for key, text in (("dark", "다크  (기본)"), ("light", "화이트")):
            rb = _radio(text)
            rb.setChecked(key == theme.current())
            rb.toggled.connect(lambda on, k=key: on and self.theme_changed.emit(k))
            self._theme_group.addButton(rb)
            lay.addWidget(rb)
```

배경 쪽·공구 쪽도 같은 모양으로 바꾼다(`rb.setChecked(...)` 이하 줄은 그대로).

그리고 `apply_theme` 의 `radio_qss` 에 **글자 시작점을 명시**한다:

```python
        radio_qss = (f"QRadioButton {{ color: {theme.C('text')}; background: transparent;"
                     f" padding: 3px 2px; spacing: 8px; }}"
                     f"QRadioButton:disabled {{ color: {theme.C('todo')}; }}")
```

⚠️ `spacing` 은 **동그라미와 글자 사이**를 정한다. 값이 없으면 Qt 기본값이 항목마다 달라 보일 수 있다.

- [x] **Step 4: 테스트가 통과하는지 확인한다**

Run: `python3 selftest/test_overlay_menu.py`
Expected: `[24]` 전부 ✅

- [x] **Step 5: 눈으로 확인한다**

```python
win._open_settings()
pump(0.8)
shot("t7_settings")
```

확인할 것 — 라디오 4개의 **동그라미가 세로로 한 줄**인가, **글자 시작점도 한 줄**인가, 글자 크기가 같은가. 「없음 (글자만 — AR 글라스 느낌)」이 길어 **줄바꿈되면** 문구를 줄인다(예: 「없음 (글자만)」).

- [x] **Step 6: 관문 1~4 → 커밋**

```bash
git add Demo/overlay_menu.py Demo/selftest/test_overlay_menu.py
git commit -m "fix(ui): 설정 라디오 글자 크기·시작점 통일 (Task 7)"
```

---

## Task 8: 문서 반영

**Files:**
- Modify: `docs/superpowers/specs/2026-08-03-uiux-글라스-design.md`
- Modify: `docs/superpowers/plans/2026-08-04-글라스-UI-3차수정.md`(이 파일)

- [x] **Step 1** 선행 design 의 §4.7(메뉴)·§4.8(알림)·§4.9(상태바)에 3차 변경을 한 줄씩 반영하고, **값은 복제하지 말고** 3차 design 을 가리킨다.
- [x] **Step 2** 이 계획서 끝에 「실행 결과 — 계획과 달랐던 것」을 쓴다. **계획이 맞았던 것은 적지 않는다** — 틀렸거나 예상 못 한 것만.
- [x] **Step 3: 커밋** (`sop-project` 쪽이다 — 코드는 `Rpi5`)

```bash
cd /home/pi/sop-project
git add docs/superpowers/
git commit -m "docs(설계·계획): 글라스 UI 3차 수정 반영"
```

---

## 이 계획이 다루지 않는 것

- **화이트 테마 글자 대비**·**패널 배경 기본값** — ESP32 연결 후 결정.
- **마우스·터치로 실제 클릭한 검증** — 관문 4의 실기동은 기동·생존만 본다. Task 5(폴더 열기)·Task 6(확인 팝업)은 **실기동에서 사람이 눌러 봐야** 완결된다.

---

# 실행 결과 (2026-08-04) — 계획과 달랐던 것

> 🔴 **Task 1~8 전부 완료.** 아래는 **계획이 틀렸거나 예상 못 한 것**만 남긴다.

## 원인이 계획과 달랐던 것 2건

1. **점검 「재연결」 버튼 (Task 4)** — 계획은 stretch 를 「이름과 버튼 **사이**」에 두라고 했는데, 그러면 **오히려 더 밀려난다.** 여백은 버튼 **뒤**에 둬야 버튼이 안쪽으로 당겨진다. 최종 = 이름 칸 3 : 오른쪽 여백 1.
2. **설정 라디오 (Task 7)** — 계획은 「글자 크기·시작점이 어긋난다」고 봤는데 **그 둘은 이미 같았다**(테스트로 확인). 실제 원인은 **Qt 기본 렌더가 선택 안 된 인디케이터를 거의 안 그리는 것**이었다. 선택된 줄만 동그라미가 보여 나머지와 달라 보였다. → 인디케이터를 스타일시트로 명시.

## 테스트가 결함을 못 잡은 일 2건 (둘 다 고쳐서 잡았다)

3. **Task 1 첫 테스트** — 「영상 사각형 **안에** 있는가」만 보면 창 기준으로 앉아도 **우연히 통과**한다. 여백을 영상 사각형 기준으로 재도록 고쳐 잡았다(오른쪽 여백 283px, 있어야 할 값 43px).
4. **Task 2 첫 테스트** — 패널만 `activate()` 하면 **스크롤 안쪽 행의 geometry 가 계산 전 값**으로 남아 아무것도 못 잡는다. `_body` 레이아웃까지 `activate()` 하고 `processEvents()` 를 거쳐야 좌표가 나온다. 되돌려 검증 — 수정 전 9px 어긋남, 수정 후 0px.

## 계획대로였지만 기록해 둘 것

5. **Task 1 은 `ConnBar` 가 아니라 `place()` 를 고쳤다** — `place()` 가 `parent_rect` 의 `left`/`top` 을 버리고 있었다. 계획 단계에서 미리 찾아 둔 것이라 구현 중 놀랄 일이 없었다.

## 남은 것 (실기동에서 사람이 눌러 봐야 완결)

- 🔴 **녹화 저장 폴더가 실제로 열리는가**(Task 5) — 가상 디스플레이에서는 확인하지 않았다.
- 🔴 **CCTV 전환 팝업이 실제로 뜨고 예/아니요가 먹는가**(Task 6) — 테스트는 확인 함수를 가짜로 바꿔 「응답 이후」만 봤다.

---

# 3차 이후 추가 수정 (2026-08-04~05, 사용자 재확인)

> 계획서 Task 1~8 을 마친 뒤 사용자가 실기동으로 다시 확인해 나온 것. **계획에 없던 것**이라 여기 따로 남긴다.

1. **알림 아이콘 정렬이 창 크기에 따라 달랐다** (`66f562d`) — Task 2 의 수정은 **1280 창에서만** 맞았고 1920 에서는 10px 어긋났다. 제목 라벨이 레이아웃에서 늘어나(높이 40, 필요 20) 글자가 그 중앙에 그려지는데 아이콘은 고정 높이라 위에 붙었다. `setSizePolicy(..., Fixed)` 로도 막히지 않았다(wordWrap 라벨은 height-for-width). → **구조로 해결**: 아이콘과 제목을 같은 `QHBoxLayout` 에 넣어 레이아웃이 세로 중앙을 맞추게 했다.
   - 🔴 **교훈 — 배치를 "계산해서 맞추면" 창 크기가 바뀔 때 깨진다. 같은 줄에 넣어 레이아웃에 맡긴다.**
2. **패널 배경을 켜면 공정 단계 글자가 밀렸다** (`ab72342`) — 행 높이 20 → 44px, 패널 140 → 284px. 원인은 2차 Task 6 과 **같은 스타일시트 상속**(selector 없는 규칙이 자식 라벨에도 적용돼 `panel_qss` 의 padding 이 행마다 붙음). → `QWidget#glassPanel { … }` 로 자기 위젯에만 건다.
   - ⚠️ **회귀 테스트는 배치가 아니라 원인(selector 유무)을 본다** — 이 결함은 `offscreen` 에서 재현되지 않아, 행 높이를 재는 테스트는 수정을 되돌려도 통과했다.
3. **여백을 반대쪽으로 통일했다** (`0c15aee`) — 2번으로 여백이 **늘 없는 쪽**이 됐는데 사용자가 원한 것은 **넉넉한 쪽**이었다. 행마다 여백을 명시(상하 12·좌우 14). 겸사겸사 현재 단계의 글자 시작점(혼자 7px 였다)과 상태·캡션의 좌우 여백도 맞췄다.
4. **알림 행 아이콘이 왼쪽 가장자리에 붙어 있었다** (`3b16824`) — 행 레이아웃에 좌우 여백 12px(`_NOTIFY_ROW_PAD`).

## 🔴 UI/UX 는 아직 완성이 아니다 (2026-08-05, 사용자 확인)

수정·보완·기능 추가가 **더 남아 있다.** 3차까지 끝났다고 해서 UI 작업이 닫힌 것으로 읽지 말 것.

**지금 시점에 알려진 남은 것**
- **화이트 테마 글자 대비** · **패널 배경 기본값** — ESP32 를 연결해 밝은/어두운 영상에서 결정(1차 계획서 Task 15·17).
- **마우스·터치로 실제 눌러 보는 검증** — 관문 4의 실기동은 기동·생존만 본다. 특히 **녹화 저장 폴더 열기**(3차 Task 5)와 **CCTV 전환 확인 팝업**(3차 Task 6)은 사람이 눌러 봐야 완결된다.
- **공구 감지** — 별도 작업 단위(데이터셋·학습·`.hef`). 지금은 「다음 단계 진행」 버튼이 폴백이다.
- 그 밖에 실기동에서 나올 것들 — 3차까지 **매번 새 항목이 나왔다.** 다음 회차를 예정에 넣어 둔다.
