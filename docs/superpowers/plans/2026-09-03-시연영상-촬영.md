# 시연영상 촬영 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** GUI 를 촬영 모드로 한 번 실행하면 5개 영상이 동시에 각각의 한국어 파일명으로 저장되고, 끝나면 채택/NG 로 갈라 보관한다.

**Architecture:** GUI 밖에서 `ffmpeg` 2개(①GUI 전체 화면크롭 · ④USB 웹캠 v4l2)를, GUI 안에서 15fps `QTimer` 1개가 3개(②UI만 · ③-a/③-b 1인칭)를 쓴다. 시작·종료는 GUI 가 쥔다 — ffmpeg 도 GUI 가 띄운다.

**Tech Stack:** PyQt6 · OpenCV `VideoWriter`(avc1/mp4) · ffmpeg(x11grab, v4l2) · bash 런처

**Spec:** `docs/superpowers/specs/2026-09-03-시연영상-촬영-design.md`

## Global Constraints

- 코드 저장소는 **`Rpi5`** 다(`sop-project` 아님). 커밋 서명 = `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.
- 작업 디렉터리는 항상 `Rpi5/Demo` — `main.py` 가 같은 폴더를 상대 import 한다.
- **pytest 가 없다.** 검증은 `python3 - <<'EOF'` 인라인 assert 와 실기동으로 한다.
- 🔴 **버벅임 대책을 넣지 않는다**(사용자 지시). 프레임 버리기·해상도 낮추기·워커 스레드 전부 금지. 부하는 Task 7 에서 재기만 한다.
- 🔴 **평소 실행 경로를 바꾸지 않는다.** `SOP_DEMO_CAPTURE=1` 일 때만 새 동작이 켜진다.
- 🔴 **기존 녹화 기능(메뉴 → 녹화)을 건드리지 않는다.**
- 규격: 영상 5개 전부 **1920×1080 · 15fps · `avc1`(.mp4)**. 저장 경로 `recordings/시연영상/`.
- 파일명: `<YYYYMMDD_HHMMSS>_<시나리오>_<종류>[_오버레이켬|끔].mp4` (한국어).

---

### Task 1: 촬영 모드 설정과 창 크기

**Files:**
- Modify: `Demo/config.py` (녹화 설정 절 끝)
- Modify: `Demo/main.py:22-27`

**Interfaces:**
- Produces: `config.DEMO_CAPTURE`(bool) · `DEMO_CAPTURE_DIR`(str) · `DEMO_CAPTURE_SIZE`(tuple) · `DEMO_CAPTURE_FPS`(float) · `DEMO_CAPTURE_START_TIMEOUT`(float) · `DEMO_SCENARIO`(str) · `DEMO_OVERLAY`(str)

- [ ] **Step 1: config 에 촬영 모드 설정 추가** (`RECORDING_EXT` 줄 바로 아래)

```python
# --- 시연영상 촬영 모드 -------------------------------------------------------
# 🔴 평소 실행과 완전히 분리된 경로다. SOP_DEMO_CAPTURE=1 일 때만 켜진다.
#    설계 = 상위 specs/2026-09-03-시연영상-촬영-design.md
DEMO_CAPTURE      = os.environ.get("SOP_DEMO_CAPTURE", "0") == "1"
DEMO_CAPTURE_DIR  = os.path.join(RECORDING_SAVE_DIR, '시연영상')
DEMO_CAPTURE_SIZE = (1920, 1080)      # 5개 영상 공통 규격(16:9)
DEMO_CAPTURE_FPS  = 15.0
# 첫 카메라 프레임을 이만큼 기다렸다 없으면 그냥 시작한다.
# ESP32 가 안 붙었을 때 영영 시작 못 하는 것을 막는다.
DEMO_CAPTURE_START_TIMEOUT = 10.0
DEMO_SCENARIO = os.environ.get("SOP_DEMO_SCENARIO", "정상")   # 정상 | 오답
DEMO_OVERLAY  = os.environ.get("SOP_DEMO_OVERLAY", "켬")      # 켬 | 끔
```

- [ ] **Step 2: main.py 창 배치** — `SOP_FULLSCREEN` 분기 **앞에** 넣는다

```python
    # 촬영 모드는 창을 16:9 로 고정한다 — 잘라낼 좌표가 딱 떨어지고 편집 규격 그대로다.
    # 🔴 최대화·전체화면을 쓰지 않는다. 최대화는 비율이 어중간하고, 전체화면은
    #    제목표시줄이 없어져 창을 닫을 수 없다(녹화 종료가 GUI 종료에 묶여 있다).
    if config.DEMO_CAPTURE:
        window.resize(*config.DEMO_CAPTURE_SIZE)
        window.move(0, 40)
        window.show()
    elif os.environ.get("SOP_FULLSCREEN", "0") == "1":
```

`import config` 를 main.py 상단에 추가한다(지금은 이름만 골라 import 한다).

- [ ] **Step 3: 검증**

```bash
cd ~/sop-project/Rpi5/Demo
python3 -c "import config; print(config.DEMO_CAPTURE, config.DEMO_CAPTURE_DIR, config.DEMO_CAPTURE_SIZE)"
SOP_DEMO_CAPTURE=1 python3 -c "import config; assert config.DEMO_CAPTURE; print('촬영 모드 ON')"
```
Expected: 첫 줄 `False .../recordings/시연영상 (1920, 1080)`, 둘째 줄 `촬영 모드 ON`

- [ ] **Step 4: 커밋** — `git add Demo/config.py Demo/main.py`

---

### Task 2: 오버레이 그리기를 화면 표시와 분리

**Files:**
- Modify: `Demo/camera_thread.py` — `CameraThread` 의 `__init__`, `_process_frame`

**Interfaces:**
- Produces: `CameraThread.set_frame_sink(fn)` — `fn(annotated_bgr, clean_bgr)`. `None` 이면 끈다.
- ⚠️ `UsbCameraThread` 는 건드리지 않는다. 촬영 모드에서는 ffmpeg 가 웹캠을 잡으므로 이 스레드가 아예 안 뜬다.

- [ ] **Step 1: `__init__` 에 sink 추가** (`self._draw_boxes = config.SHOW_DETECT_BOXES` 옆)

```python
        self._frame_sink = None      # 시연영상 촬영이 붙을 자리 (Task 3)
```

- [ ] **Step 2: setter 추가** (`set_draw_boxes` 옆)

```python
    def set_frame_sink(self, fn):
        """매 프레임 (오버레이 있는 것, 없는 것) 두 벌을 받을 함수. None 이면 끈다.

        🔴 sink 가 붙으면 **화면 표시 설정과 무관하게 오버레이를 그린다.**
           촬영은 오버레이 OFF 회차에서도 「오버레이 있는 1인칭 영상」을 남겨야 한다.
        """
        with self._lock:
            self._frame_sink = fn
```

- [ ] **Step 3: `_process_frame` 수정** — `draw = self._draw_boxes` 를 읽는 `with self._lock:` 블록에 `sink = self._frame_sink` 를 추가하고, `tool_frame` 를 만드는 자리 **바로 뒤**에 깨끗한 사본을 뜬다.

```python
        # 🔴 오버레이 없는 사본 — 촬영이 붙어 있을 때만. tool_frame 과 목적이 다르다
        #    (저쪽은 공구 추론용, 이쪽은 ③-b 1인칭 원본 영상용).
        clean_frame = frame.copy() if sink is not None else None
        draw_overlay = draw or sink is not None
```

이어서 `if draw:` 두 곳을 `if draw_overlay:` 로 바꾼다 — YOLO 박스(`self._draw_yolo`)와 손 랜드마크(`draw_on=frame if draw else None` → `draw_on=frame if draw_overlay else None`).

- [ ] **Step 4: 반환 직전에 sink 호출과 표시 프레임 선택**

`_process_frame` 이 `frame` 을 반환하는 자리를 다음으로 바꾼다.

```python
        if sink is not None:
            sink(frame, clean_frame)
            # 화면에는 회차 설정대로 보여준다 — 촬영 때문에 화면이 바뀌면 안 된다.
            return frame if draw else clean_frame
        return frame
```

- [ ] **Step 5: 검증** — sink 없이 기존 동작이 그대로인지 소스 수준에서 확인

```bash
cd ~/sop-project/Rpi5/Demo
python3 - <<'EOF'
import ast
src = open('camera_thread.py').read()
assert 'set_frame_sink' in src
assert 'draw_on=frame if draw_overlay else None' in src
assert src.count('clean_frame = frame.copy() if sink is not None else None') == 1
ast.parse(src)          # 문법 확인
print("camera_thread 구조 확인 통과")
EOF
```

- [ ] **Step 6: 실기동 회귀 확인** — 평소 실행이 그대로인지. `./run_demo.sh` 로 GUI 를 띄워 **영상이 나오고 박스가 보이는지** 눈으로 확인한 뒤 닫는다. (🔴 import 검사로 못 잡는 런타임 오류가 실제로 있었다.)

- [ ] **Step 7: 커밋** — `git add Demo/camera_thread.py`

---

### Task 3: DemoRecorder — GUI 안 3개 영상

**Files:**
- Create: `Demo/demo_recorder.py`

**Interfaces:**
- Consumes: `config.DEMO_CAPTURE_*`(Task 1) · `CameraThread.set_frame_sink`(Task 2)
- Produces:
  - `letterbox(frame_bgr, size) -> ndarray` — 비율 유지, 남는 곳 검정
  - `class DemoRecorder(QObject)`
    - `__init__(self, out_dir, stamp, scenario, overlay, parent=None)`
    - `start(self, grab_fn, camera_rect) -> None` — `grab_fn() -> QImage`(창 전체), `camera_rect` = 창 좌표계의 `QRect`
    - `submit_camera(self, annotated_bgr, clean_bgr) -> None` — **카메라 스레드가 부른다.** 최신 프레임만 보관
    - `stop(self) -> dict` — `{'files': [str], 'seconds': float}`

- [ ] **Step 1: 파일 작성**

```python
"""시연영상 촬영 — GUI 안에서 찍는 3개(②UI만 · ③-a/③-b 1인칭).

①GUI 전체·④USB 3인칭은 ffmpeg 가 맡는다(demo_ffmpeg.py).
설계 = 상위 specs/2026-09-03-시연영상-촬영-design.md
"""
import os
import threading
import time

import cv2
import numpy as np
from PyQt6.QtCore import QObject, QTimer, pyqtSlot
from PyQt6.QtGui import QImage

import config


def letterbox(frame, size):
    """비율을 지켜 size 캔버스 가운데 놓고 남는 곳은 검정.

    🔴 화면 표시(_fit_to_label)와 다르다 — 그쪽은 채우고 잘라내지만(FILL),
       촬영본은 **시야를 하나도 버리지 않아야** 하므로 레터박스다.
    """
    tw, th = size
    h, w = frame.shape[:2]
    s = min(tw / w, th / h)
    nw, nh = max(1, int(w * s)), max(1, int(h * s))
    canvas = np.zeros((th, tw, 3), dtype=np.uint8)
    x, y = (tw - nw) // 2, (th - nh) // 2
    canvas[y:y+nh, x:x+nw] = cv2.resize(frame, (nw, nh), interpolation=cv2.INTER_AREA)
    return canvas


def qimage_to_bgr(qt_image):
    img = qt_image.convertToFormat(QImage.Format.Format_RGB888)
    w, h = img.width(), img.height()
    ptr = img.bits(); ptr.setsize(h * w * 3)
    arr = np.array(ptr).reshape(h, w, 3)
    return cv2.cvtColor(arr, cv2.COLOR_RGB2BGR)


class DemoRecorder(QObject):
    """15fps 타이머 하나가 3개 파일을 쓴다.

    🔴 타이머가 「지금 최신 프레임」을 가져다 쓴다 — 새 프레임이 안 왔으면 직전 것을
       한 번 더 쓴다. ESP32 프레임 간격이 일정하지 않아, 오는 대로 쓰면 **영상 길이가
       실제 시간과 어긋난다.**
    """

    def __init__(self, out_dir, stamp, scenario, overlay, parent=None):
        super().__init__(parent)
        self._dir = out_dir
        self._suffix = f"_오버레이{overlay}"
        self._base = f"{stamp}_{scenario}"
        self._lock = threading.Lock()
        self._latest = None          # (annotated_bgr, clean_bgr)
        self._writers = {}
        self._grab = None
        self._camera_rect = None
        self._started = 0.0
        self._timer = QTimer(self)
        self._timer.setInterval(int(1000 / config.DEMO_CAPTURE_FPS))
        self._timer.timeout.connect(self._tick)

    def _path(self, kind, with_overlay_suffix=True):
        name = f"{self._base}_{kind}{self._suffix if with_overlay_suffix else ''}.mp4"
        return os.path.join(self._dir, name)

    def _open(self, key, path):
        fourcc = cv2.VideoWriter_fourcc(*config.RECORDING_CODEC)
        w = cv2.VideoWriter(path, fourcc, config.DEMO_CAPTURE_FPS, config.DEMO_CAPTURE_SIZE)
        if not w.isOpened():
            raise RuntimeError(f"VideoWriter 실패 ({config.RECORDING_CODEC}): {path}")
        self._writers[key] = (w, path)

    def start(self, grab_fn, camera_rect):
        os.makedirs(self._dir, exist_ok=True)
        self._grab = grab_fn
        self._camera_rect = camera_rect
        self._open('화면만', self._path('GUI화면만'))
        self._open('1인칭O', os.path.join(
            self._dir, f"{self._base}_1인칭풀_오버레이켬.mp4"))
        self._open('1인칭X', os.path.join(
            self._dir, f"{self._base}_1인칭풀_오버레이끔.mp4"))
        self._started = time.time()
        self._timer.start()

    def submit_camera(self, annotated_bgr, clean_bgr):
        with self._lock:
            self._latest = (annotated_bgr, clean_bgr)

    @pyqtSlot()
    def _tick(self):
        if not self._writers:
            return
        # ② GUI UI만 — 창을 떠서 카메라 영역만 검정으로 덧칠한다
        try:
            frame = qimage_to_bgr(self._grab())
            r = self._camera_rect
            frame[r.y():r.y()+r.height(), r.x():r.x()+r.width()] = 0
            self._writers['화면만'][0].write(
                cv2.resize(frame, config.DEMO_CAPTURE_SIZE))
        except Exception as e:
            print(f"[시연촬영] 화면만 프레임 오류: {e}")
        # ③ 1인칭 두 벌
        with self._lock:
            latest = self._latest
        if latest is not None:
            annotated, clean = latest
            for key, img in (('1인칭O', annotated), ('1인칭X', clean)):
                try:
                    self._writers[key][0].write(letterbox(img, config.DEMO_CAPTURE_SIZE))
                except Exception as e:
                    print(f"[시연촬영] {key} 프레임 오류: {e}")

    def stop(self):
        self._timer.stop()
        files = []
        for w, path in self._writers.values():
            w.release()
            files.append(path)
        self._writers = {}
        return {'files': files, 'seconds': time.time() - self._started if self._started else 0.0}
```

- [ ] **Step 2: `letterbox` 검증**

```bash
cd ~/sop-project/Rpi5/Demo
python3 - <<'EOF'
import numpy as np
from demo_recorder import letterbox
# 세로 프레임(480x640) → 16:9 캔버스. 가운데 810x1080, 좌우는 검정이어야 한다.
src = np.full((640, 480, 3), 255, np.uint8)
out = letterbox(src, (1920, 1080))
assert out.shape == (1080, 1920, 3), out.shape
assert out[540, 0].sum() == 0, "좌측이 검정이 아니다"
assert out[540, 960].sum() > 0, "가운데가 비었다"
assert out[0, 960].sum() > 0, "세로는 꽉 차야 한다"
print("letterbox 통과 —", out.shape)
EOF
```
Expected: `letterbox 통과 — (1080, 1920, 3)`

- [ ] **Step 3: 커밋** — `git add Demo/demo_recorder.py`

---

### Task 4: ffmpeg 2개 — GUI 전체와 USB 3인칭

**Files:**
- Create: `Demo/demo_ffmpeg.py`

**Interfaces:**
- Produces: `class FfmpegPair` — `start(self, screen_rect, screen_path, webcam_path) -> list[str]`(실패한 것들의 사유), `stop(self) -> None`
- `screen_rect` = **화면 절대 좌표**의 `(x, y, w, h)`

- [ ] **Step 1: 파일 작성**

```python
"""시연영상 촬영 — ffmpeg 가 맡는 2개(①GUI 전체 · ④USB 3인칭).

🔴 웹캠을 ffmpeg 가 직접 v4l2 로 연다. OpenCV 는 GStreamer 백엔드로 열려
   MJPG 지정이 무시되고 1080p 5fps 로 떨어진다(2026-07-22 실측).
🔴 시각 자막(drawtext)을 넣지 않는다 — run_scenario.sh 와 다른 점이다.
   저쪽은 대조용 기록이고 이쪽은 **제출 영상**이다.
"""
import os
import signal
import subprocess
import time

import config


class FfmpegPair:
    def __init__(self):
        self._procs = []

    def _spawn(self, args, path):
        return subprocess.Popen(
            ["ffmpeg", "-hide_banner", "-loglevel", "error", "-y"] + args + [path],
            stdin=subprocess.DEVNULL)

    def start(self, screen_rect, screen_path, webcam_path):
        problems = []
        x, y, w, h = screen_rect
        w -= w % 2; h -= h % 2          # x264 는 짝수 크기만 받는다
        display = os.environ.get("DISPLAY", ":0")
        self._procs.append(self._spawn([
            "-f", "x11grab",
            "-framerate", str(int(config.DEMO_CAPTURE_FPS)),
            "-video_size", f"{w}x{h}",
            "-i", f"{display}+{x},{y}",
            "-vf", f"scale={config.DEMO_CAPTURE_SIZE[0]}:{config.DEMO_CAPTURE_SIZE[1]}",
            "-c:v", "libx264", "-preset", "ultrafast", "-crf", "23",
            "-pix_fmt", "yuv420p",
        ], screen_path))
        if os.path.exists("/dev/video0"):
            self._procs.append(self._spawn([
                "-f", "v4l2", "-input_format", "mjpeg",
                "-video_size", "1920x1080", "-framerate", "30",
                "-i", "/dev/video0",
                "-c:v", "libx264", "-preset", "ultrafast", "-crf", "23",
                "-pix_fmt", "yuv420p",
            ], webcam_path))
        else:
            problems.append("USB 웹캠(/dev/video0)이 없어 3인칭 녹화를 건너뜁니다")
        time.sleep(1.0)
        for p in list(self._procs):
            if p.poll() is not None:
                problems.append(f"ffmpeg 가 즉시 종료됨 (코드 {p.returncode})")
                self._procs.remove(p)
        return problems

    def stop(self):
        # 🔴 SIGINT 여야 한다. kill 로 죽이면 mp4 무빙 헤더가 안 써져 파일이 깨진다.
        for p in self._procs:
            try:
                p.send_signal(signal.SIGINT)
            except Exception:
                pass
        for p in self._procs:
            try:
                p.wait(timeout=10)
            except Exception:
                p.kill()
        self._procs = []
```

- [ ] **Step 2: 단독 검증** — GUI 없이 3초만 찍어 두 파일이 재생 가능한지

```bash
cd ~/sop-project/Rpi5/Demo
python3 - <<'EOF'
import time, os, subprocess
from demo_ffmpeg import FfmpegPair
out = "/tmp/ff_screen.mp4"; cam = "/tmp/ff_cam.mp4"
f = FfmpegPair()
print("문제:", f.start((0, 40, 1280, 720), out, cam))
time.sleep(3)
f.stop()
for p in (out, cam):
    if os.path.exists(p):
        d = subprocess.run(["ffprobe","-v","error","-show_entries","format=duration",
                            "-of","default=nw=1:nk=1", p], capture_output=True, text=True)
        print(p, os.path.getsize(p), "bytes, 길이", d.stdout.strip())
EOF
```
Expected: 두 파일 모두 존재하고 길이가 **2~4초**. 길이가 안 나오면 파일이 깨진 것이다.

- [ ] **Step 3: 커밋** — `git add Demo/demo_ffmpeg.py`

---

### Task 5: safety_console 배선

**Files:**
- Modify: `Demo/safety_console.py` — `__init__`(촬영 객체 생성) · `_note_frame`(시작 트리거) · `closeEvent`(정리)

**Interfaces:**
- Consumes: `DemoRecorder`(Task 3) · `FfmpegPair`(Task 4) · `CameraThread.set_frame_sink`(Task 2)
- Produces: 세트 폴더와 `촬영정보.txt`

- [ ] **Step 1: import 와 초기화** — `__init__` 끝(`RECORDING_ENABLED` 분기 근처)

```python
        # --- 시연영상 촬영 -------------------------------------------------
        self._demo = None
        self._demo_ff = None
        self._demo_dir = ""
        if config.DEMO_CAPTURE:
            stamp = datetime.now().strftime("%Y%m%d_%H%M%S")
            folder = f"{stamp}_{config.DEMO_SCENARIO}_오버레이{config.DEMO_OVERLAY}"
            self._demo_dir = os.path.join(config.DEMO_CAPTURE_DIR, '촬영본', folder)
            self._demo = DemoRecorder(self._demo_dir, stamp,
                                      config.DEMO_SCENARIO, config.DEMO_OVERLAY, self)
            self._demo_ff = FfmpegPair()
            # 첫 프레임이 안 와도 이 시간이 지나면 시작한다(ESP32 미연결 대비).
            QTimer.singleShot(int(config.DEMO_CAPTURE_START_TIMEOUT * 1000),
                              self._start_demo_capture)
            self.camera_thread.set_draw_boxes(config.DEMO_OVERLAY == "켬")
```

- [ ] **Step 2: 시작 함수 추가** (`_start_recording` 위)

```python
    def _start_demo_capture(self):
        """5개 동시 시작. 🔴 첫 카메라 프레임이 온 뒤여야 1인칭 영상이 검은 화면으로
        시작하지 않는다. 두 번 불려도 한 번만 시작한다."""
        if self._demo is None or self._demo_dir == "" or getattr(self, "_demo_on", False):
            return
        self._demo_on = True
        self.raise_(); self.activateWindow()      # 창 앞으로 — 터미널이 겹치면 찍힌다
        QApplication.processEvents()
        os.makedirs(self._demo_dir, exist_ok=True)
        base = os.path.basename(self._demo_dir).split('_오버레이')[0]
        g = self.geometry()
        top_left = self.mapToGlobal(QPoint(0, 0))
        problems = self._demo_ff.start(
            (top_left.x(), top_left.y(), g.width(), g.height()),
            os.path.join(self._demo_dir, f"{base}_GUI전체_오버레이{config.DEMO_OVERLAY}.mp4"),
            os.path.join(self._demo_dir, f"{base}_3인칭웹캠.mp4"))
        self._demo.start(lambda: self.grab().toImage(), self.camera_label.geometry())
        self.camera_thread.set_frame_sink(self._demo.submit_camera)
        for p in problems:
            self._append_log(f"[시연촬영] ⚠️ {p}")
        self._append_log(f"[시연촬영] 시작 — {self._demo_dir}")
```

- [ ] **Step 3: 첫 프레임에서 시작** — `_note_frame` 끝에 한 줄

```python
        if config.DEMO_CAPTURE and not getattr(self, "_demo_on", False):
            self._start_demo_capture()
```

- [ ] **Step 4: 종료 정리** — `closeEvent` 맨 앞(`self._stop_recording()` 위)

```python
        self._stop_demo_capture()
```

그리고 함수를 추가한다.

```python
    def _stop_demo_capture(self):
        if self._demo is None or not getattr(self, "_demo_on", False):
            return
        self.camera_thread.set_frame_sink(None)
        info = self._demo.stop()
        self._demo_ff.stop()
        lines = [f"시나리오: {config.DEMO_SCENARIO}",
                 f"오버레이: {config.DEMO_OVERLAY}",
                 f"길이: {info['seconds']:.1f}초", ""]
        for f in sorted(os.listdir(self._demo_dir)):
            p = os.path.join(self._demo_dir, f)
            if os.path.isfile(p) and f.endswith('.mp4'):
                lines.append(f"{f}  {os.path.getsize(p)/1024/1024:.1f} MB")
        with open(os.path.join(self._demo_dir, '촬영정보.txt'), 'w') as fp:
            fp.write("\n".join(lines) + "\n")
        self._append_log(f"[시연촬영] 종료 — {info['seconds']:.1f}초")
```

⚠️ `QPoint`·`QApplication` import 가 이미 있는지 확인하고 없으면 추가한다.

- [ ] **Step 5: 검증 — 평소 실행 회귀** (촬영 모드 아님)

```bash
cd ~/sop-project/Rpi5/Demo && ./run_demo.sh
```
Expected: 지금과 똑같이 GUI 가 뜬다. `recordings/시연영상/` 이 **생기지 않는다.** 닫고 확인:
```bash
test -d ~/sop-project/Rpi5/Demo/recordings/시연영상 && echo "❌ 평소 실행에서 폴더가 생겼다" || echo "✅ 평소 실행 영향 없음"
```

- [ ] **Step 6: 커밋** — `git add Demo/safety_console.py`

---

### Task 6: 런처와 바탕화면 바로가기

**Files:**
- Create: `Demo/촬영런처.sh` (실행 권한)
- Create: `~/Desktop/시연영상-촬영.desktop`

- [ ] **Step 1: 런처 작성**

```bash
#!/usr/bin/env bash
# 시연영상 촬영 런처 — 5개 영상을 한 번에. 설계 = specs/2026-09-03-시연영상-촬영-design.md
cd "$(dirname "$(readlink -f "$0")")" || exit 1

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "  시연영상 촬영"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
FREE_MB=$(df -Pm . | awk 'NR==2{print $4}')
if [ "$FREE_MB" -lt 4096 ]; then
    echo "❌ 디스크 여유 ${FREE_MB}MB — 5개 동시 녹화에는 4GB 이상 필요합니다."
    read -rp "Enter 키를 누르면 닫힙니다..."; exit 1
fi

echo "  시나리오:  1) 정상   2) 오답(순서 위반)"
read -rp "  > " s
case "$s" in 2) SCEN="오답" ;; *) SCEN="정상" ;; esac
echo "  GUI 화면에 검출 오버레이를 표시할까요:  1) 켬   2) 끔"
read -rp "  > " o
case "$o" in 2) OVL="끔" ;; *) OVL="켬" ;; esac

echo
echo "  ▶ [$SCEN / 오버레이 $OVL] GUI 를 띄웁니다."
echo "    카메라 영상이 들어오면 5개 녹화가 동시에 시작됩니다."
echo "    끝나면 GUI 창을 닫으세요."
echo

# 🔴 웹캠은 ffmpeg 가 직접 연다 — GUI 의 UsbCameraThread 가 점유하면 안 된다.
SOP_DEMO_CAPTURE=1 SOP_DEMO_SCENARIO="$SCEN" SOP_DEMO_OVERLAY="$OVL" \
SOP_USB_CAMERA=0 python3 main.py
status=$?

SET_DIR="$(ls -dt recordings/시연영상/촬영본/*/ 2>/dev/null | head -1)"
if [ -z "$SET_DIR" ]; then
    echo "❌ 촬영본이 없습니다 (GUI exit code: $status)"
    read -rp "Enter 키를 누르면 닫힙니다..."; exit 1
fi

echo; echo "  ▪ 촬영본 — $SET_DIR"
cat "$SET_DIR/촬영정보.txt" 2>/dev/null | sed 's/^/    /'
echo
read -rp "  이번 테이크를 채택하시겠습니까? (y = 채택 / 그 외 = NG보관) " a
case "$a" in y|Y) DEST="recordings/시연영상/채택" ;; *) DEST="recordings/시연영상/NG보관" ;; esac
mkdir -p "$DEST" && mv "$SET_DIR" "$DEST/" && echo "  → $DEST/ 로 옮겼습니다"
read -rp "Enter 키를 누르면 닫힙니다..."
```

- [ ] **Step 2: 바로가기 작성** — `~/Desktop/시연영상-촬영.desktop`

```ini
[Desktop Entry]
Version=1.0
Type=Application
Name=🎬 SOP 가디언 — 시연영상 촬영
Comment=한 번 실행에 5개 영상 동시 녹화 (GUI전체·UI만·1인칭 2벌·3인칭)
Exec=lxterminal --title="시연영상 촬영" -e /home/pi/sop-project/Rpi5/Demo/촬영런처.sh
Path=/home/pi/sop-project/Rpi5/Demo
Icon=camera-video
Terminal=false
Categories=Utility;
```

- [ ] **Step 3: 권한과 문법 검증**

```bash
chmod +x ~/sop-project/Rpi5/Demo/촬영런처.sh
bash -n ~/sop-project/Rpi5/Demo/촬영런처.sh && echo "런처 문법 통과"
chmod +x ~/Desktop/시연영상-촬영.desktop
```

- [ ] **Step 4: 커밋** — `git add Demo/촬영런처.sh` (바로가기는 홈이라 저장소 밖이다)

---

### Task 7: 실기동 검증과 부하 실측

**Files:** 없음 (측정만)

- [ ] **Step 1: 짧은 테이크 한 번** — 바탕화면 `🎬 SOP 가디언 — 시연영상 촬영` 실행. 정상/켬 선택, **약 30초** 두고 GUI 를 닫는다. 채택 질문에는 `n`(NG보관).

- [ ] **Step 2: 5개 파일 확인**

```bash
cd ~/sop-project/Rpi5/Demo
D="$(ls -dt recordings/시연영상/NG보관/*/ | head -1)"; echo "$D"
for f in "$D"*.mp4; do
  printf "%-60s %6.1fMB  %ss\n" "$(basename "$f")" \
    "$(echo "scale=1; $(stat -c%s "$f")/1048576" | bc)" \
    "$(ffprobe -v error -show_entries format=duration -of default=nw=1:nk=1 "$f")"
done
```
Expected: **5개** 파일, 길이가 서로 **10% 이내**로 일치하고 실제 촬영 시간과 맞을 것.
🔒 **중단 규칙**: 하나라도 없거나 길이가 10% 넘게 어긋나면 **본 촬영을 시작하지 않는다.** 원인을 먼저 잡는다.

- [ ] **Step 3: 눈으로 확인** — 각 파일을 열어 본다.
  - `GUI전체` — **작업표시줄·제목표시줄이 안 보여야** 한다
  - `GUI화면만` — 카메라 자리가 검정, UI 는 정상 동작
  - `1인칭풀_오버레이켬` — 박스와 손 랜드마크가 보인다
  - `1인칭풀_오버레이끔` — 오버레이가 하나도 없다
  - `3인칭웹캠` — 박스·랜드마크·시각 자막이 **하나도 없어야** 한다

- [ ] **Step 4: 부하 실측** — 같은 촬영을 한 번 더 하면서 다른 터미널에서

```bash
top -b -n 30 -d 2 | grep -E "^%Cpu|python3|ffmpeg" > /tmp/촬영부하.txt
```
GUI 로그의 FPS 표시와 함께 기록한다. **여기서 나온 수치를 보고** 버벅임 대책이 필요한지 정한다(요구사항 9).

- [ ] **Step 5: 결과를 사용자에게 보고** — 파일 5개·길이 일치 여부·부하 수치. 통합문서 §10 기입 여부는 사용자 판단.
