# 런타임 XGA 전환 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 런타임이 첫 프레임 크기를 보고 보정 파일과 픽셀 값을 스스로 맞추게 하고, 펌웨어를 XGA + 노출 9ms 로 바꿔 실HW 관문 G1~G5 를 통과시킨다.

**Architecture:** 보정 파일 선택을 `frame_orient` 한 곳에 모은다(해상도별 파일 우선 → 기존 파일은 크기가 맞을 때만). 픽셀 값은 VGA 기준값 × `px_scale`(긴 변 ÷ 640). GUI 캘리브레이션 창·버튼은 제거한다.

**Tech Stack:** Python 3.13 · OpenCV · PyQt6 · Arduino CLI(ESP32-S3) · esptool

**Spec:** `docs/superpowers/specs/2026-09-23-XGA-런타임전환-design.md`

## Global Constraints

- 코드 저장소 = `/home/pi/sop-project/Rpi5`(별도 git) · 문서 = `/home/pi/sop-project`. 커밋은 **경로 지정 add** 만(`git add -A`·`.` 금지).
- 커밋 서명 = `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>` + `Claude-Session: https://claude.ai/code/session_01MVDAkWJDHbcnsoxEfVUfhx`.
- 측정 도구는 config 에서 읽는다 — 판정 규칙을 도구에 복제하지 않는다.
- 굽기 전 **시리얼번호 `3C:0F:02:DD:5E:58`** 확인. 아니면 멈춘다. 되돌리기 백업 = `~/lab/esp32-link/mainboard_full_20260923_153411.bin`(VGA·36ms).
- 자가 테스트는 **일괄 러너가 없다** — `selftest/test_*.py` 를 개별 실행한다.
- `camera_thread` 의 최상위 이름을 바꾸면 **`selftest/test_imports.py` 필수**.
- 링 기준값 `HAND_ROI_RING_PX_VGA = 25`(§12.22). `px_scale = max(w, h) / 640`.
- **USB 웹캠 기능은 전부 제거한다**(spec D5) — 백업 태그 `backup/webcam-before-removal-20260923` 를 **먼저** 원격에 올린다.

## 관문 (정본 = spec §3)

- **🔒 홀드아웃** — 합격선은 spec §3.1 표로 **측정 전에 고정**됐다. 측정 뒤 바꾸지 않는다. G3 사전 감지율은 **합산값**으로만 판정.
- **검증 관문** — G1(자가 테스트 전부 · Task 9 Step 4) · G2·G3(Task 8) · G4·G5(Task 9). 각 태스크 끝의 확인 단계는 그 태스크의 관문이다.
- **중단 규칙** — ①G2·G3 미달 → 펌웨어만 백업(VGA)으로 되돌리고 멈춘다 ②시리얼번호가 `3C:0F:02:DD:5E:58` 이 아니면 굽지 않는다 ③어느 태스크든 확인 단계가 실패하면 다음 태스크로 넘어가지 않는다 ④같은 수정을 3번 시도해도 안 되면 멈추고 보고한다.

## Review Focus

1. **보정 파일이 두 개 다 있을 때** — XGA 프레임에 `camera_calibration.npz`(VGA)가 잘못 붙지 않아야 한다 → Task 1 테스트 `test_xga_prefers_per_resolution`.
2. **해상도별 파일이 다른 크기로 저장된 경우**(이름은 1024x768 인데 `image_size` 가 다름) → `mismatch` 로 거부 → Task 1 `test_per_resolution_size_mismatch`.
3. **보정 파일이 하나도 없을 때** 런타임이 죽지 않고 보정 없이 돌며 안내 로그를 남긴다 → Task 2 확인 단계.
4. **GUI 에서 캘리브레이션 버튼을 없앤 뒤 메뉴 순서·다른 버튼 신호가 그대로** → Task 3 `test_overlay_menu.py`.
5. **측정 도구가 XGA 사진에 VGA 링(25px)을 쓰는 실수** → Task 5 `dwell_probe`·`hoi_probe_batch` 가 사진 크기로 환산.
6. **웹캠이 없는 장비에서 GUI·녹화·런처가 웹캠을 찾다 죽거나 경고를 띄움** → Task W1~W4 뒤 `grep` 으로 잔존 참조 0 · G4 실기동에서 로그 확인.

---

### Task 0: 백업 태그

- [ ] **Step 1:** 웹캠 제거 직전 상태에 태그를 붙여 원격에 올린다

```bash
cd /home/pi/sop-project/Rpi5 && git tag -a backup/webcam-before-removal-20260923 -m "USB 웹캠 기능 제거 직전(spec 2026-09-23 D5) — 꺼낼 때: git checkout <태그> -- <파일>" && git push origin backup/webcam-before-removal-20260923
```
Expected: `* [new tag] backup/webcam-before-removal-20260923`

---

### Task W1: 웹캠 제거 ① — GUI 스레드·CCTV·카메라 전환

**Files:**
- Modify: `Rpi5/Demo/camera_thread.py:646~`(`UsbCameraThread` 클래스 전체 삭제 · 243 줄 주석 정리)
- Modify: `Rpi5/Demo/safety_console.py:28`(import) · `:310-320`(USB 스레드 생성·`_switch_camera` 초기 호출) · `:436`(CCTV 메뉴 연결) · `:672-720`(`_switch_camera`·`_toggle_camera_source`·USB 프레임 처리 `_update_usb_frame`) · `:862`·`:890`·`:1003`·`:1632`·`:1803`(USB 스레드 참조)
- Modify: `Rpi5/Demo/overlay_menu.py:163`·`:193`(`cctv_clicked`·「CCTV 전환」)
- Modify: `Rpi5/Demo/config.py:381-392`(`USB_CAMERA_ENABLED`·`CAMERA_SOURCE`)
- Modify: `Rpi5/Demo/selftest/test_overlay_menu.py`(신호 목록에서 `cctv_clicked` 제거) · `Rpi5/Demo/selftest/test_console_flow.py`(웹캠 참조 4줄)

- [ ] **Step 1: 테스트 먼저** — `test_overlay_menu.py` 에 추가하고 신호 목록에서 `"cctv_clicked"` 를 뺀다

```python
def test_no_cctv_item():
    """USB 웹캠 기능 제거(spec 2026-09-23 D5)."""
    import overlay_menu
    assert not any(hasattr(c, "cctv_clicked") for c in vars(overlay_menu).values() if isinstance(c, type)), "CCTV 신호가 남아 있다"
```
Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_overlay_menu.py` · Expected: FAIL

- [ ] **Step 2: 제거** — 먼저 `safety_console.py:660-725`·`:850-895`·`:995-1005` 를 읽고, **ESP32 경로만 남기도록** 고친다.
  - `_switch_camera` 가 FPS 표본 초기화(`fps.py` 의 세 자리 중 하나)를 하고 있으면 **그 초기화는 ESP32 경로에 남긴다**(Rpi5/CLAUDE.md `fps_stale` 함정). 카메라가 하나뿐이면 전환 자체가 없어지므로, 기동 시 ESP32 스레드를 바로 `start()` 하고 `set_active(True)` 로 둔다.
  - `self.usb_camera_thread` 를 쓰는 모든 줄을 지우거나 `self.camera_thread` 단일 경로로 바꾼다.
  - `from camera_thread import CameraThread, UsbCameraThread, close_detector` → `UsbCameraThread` 제거.
  - `overlay_menu.py`: `cctv_clicked` 신호·「📹  CCTV 전환」 항목 삭제.
  - `config.py`: `USB_CAMERA_ENABLED`·`CAMERA_SOURCE` 와 그 주석 블록 삭제.
  - `test_console_flow.py`: 웹캠 참조 줄을 읽고 ESP32 단일 경로 기준으로 고친다.

- [ ] **Step 3: 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && for t in test_imports test_overlay_menu test_console_flow test_overlay; do python3 selftest/$t.py >/dev/null 2>&1 && echo "✅ $t" || echo "🔴 $t"; done; grep -rnE "UsbCameraThread|usb_camera_thread|cctv_clicked|USB_CAMERA_ENABLED|CAMERA_SOURCE|_switch_camera|_toggle_camera_source|_update_usb_frame" --include=*.py . | grep -v original/`
Expected: 4개 ✅ · grep 출력 없음

- [ ] **Step 4: 커밋** — `feat(런타임): USB 웹캠 제거 ① — GUI 스레드·CCTV·카메라 전환`

---

### Task W2: 웹캠 제거 ② — 시연 녹화의 3인칭

**Files:**
- Modify: `Rpi5/Demo/demo_ffmpeg.py:16`(머리말) · `:67`(경로 집합의 `'webcam'`) · `:93-107`(B. 웹캠 입력)
- Modify: `Rpi5/Demo/demo_recorder.py:84`(`'webcam'` 경로)
- Modify: `Rpi5/Demo/config.py:238-255`(`DEMO_WEBCAM_SIZE` · `DEMO_TARGETS` 의 `"webcam"` 회차 · `demo_wants` 설명)
- Modify: `Rpi5/Demo/safety_console.py:355`(`DEMO_TARGETS == "webcam"` 분기)

- [ ] **Step 1: 제거** — 먼저 각 구간을 읽고 `'webcam'` 경로·입력·회차를 지운다. `DEMO_TARGETS` 가 `"webcam"` 값을 받으면 **기동 시 오류로 알린다**(조용히 무시하지 않는다).
- [ ] **Step 2: 확인** — `python3 -c "import ast,sys;[ast.parse(open(f).read()) for f in sys.argv[1:]];print('OK')" demo_ffmpeg.py demo_recorder.py config.py safety_console.py && grep -rnE "webcam|웹캠|/dev/video|DEMO_WEBCAM" demo_ffmpeg.py demo_recorder.py config.py safety_console.py`
  Expected: `OK` · grep 출력 없음(과거 사실을 적은 주석이 남으면 「(2026-09-23 제거)」 한 줄로 줄인다)
- [ ] **Step 3: 커밋** — `feat(런타임): USB 웹캠 제거 ② — 시연 녹화의 3인칭`

---

### Task W3: 웹캠 제거 ③ — 촬영 런처

**Files:**
- Modify: `Rpi5/Demo/run_scenario.sh`(웹캠 녹화) · `Rpi5/Demo/촬영런처.sh:7-8`·`:25-58`·`:91`(웹캠 확인·「3인칭 웹캠만」 회차·`SOP_USB_CAMERA=0`)

- [ ] **Step 1: 제거** — 먼저 두 스크립트를 전부 읽는다. 웹캠 녹화 프로세스 시작·정리, `/dev/video0` 확인, 「웹캠만」 메뉴, `SOP_USB_CAMERA=0` 을 지우고 메뉴 번호를 다시 맞춘다.
- [ ] **Step 2: 확인** — `bash -n run_scenario.sh && bash -n 촬영런처.sh && grep -nE "webcam|웹캠|/dev/video|SOP_USB_CAMERA" run_scenario.sh 촬영런처.sh` · Expected: 문법 OK · grep 출력 없음
- [ ] **Step 3: 커밋** — `feat(런처): USB 웹캠 제거 ③ — 촬영 런처의 3인칭 녹화`

---

### Task W4: 웹캠 제거 ④ — 음성 데모 녹화

**Files:**
- Modify: `Rpi5/Demo/voice/record_voice_demo.py`(② 3인칭 · ③ 웹캠 마이크 · `--no-webcam`·`--preview` · `MIC`·`WEBCAM_*` 상수) · `Rpi5/Demo/voice/run_record.sh`(웹캠 3줄)

- [ ] **Step 1: 제거** — 먼저 파일 전체를 읽는다. 1인칭 오버레이·공구 워커·음성비서 데몬은 남기고, 웹캠 영상·웹캠 마이크 녹음·그것에 기대는 산출물(`마이크_전체.wav` 등)을 지운다. 머리말에 「🔴 2026-09-23 웹캠 제거 — 녹음 수단 없음. 다시 찍으면 다른 마이크 필요」 한 줄.
- [ ] **Step 2: 확인** — `python3 -c "import ast;ast.parse(open('voice/record_voice_demo.py').read());print('OK')" && bash -n voice/run_record.sh && grep -nE "webcam|웹캠|/dev/video|plughw|MIC\b" voice/record_voice_demo.py voice/run_record.sh`
  Expected: `OK` · grep 출력 없음(또는 제거 사실 한 줄)
- [ ] **Step 3: 커밋** — `feat(음성): USB 웹캠 제거 ④ — 음성 데모의 3인칭·웹캠 마이크`

---

### Task W5: 웹캠 제거 ⑤ — 측정 도구

**Files:**
- Modify: `Rpi5/Demo/test/bench_detector.py`(`--source usb`·`--usb-index`·`--lock-exposure`·`--exposure`·`--wb`·`_lock_usb_exposure`·USB 캡처 경로·manifest 의 `usb_index`·`lock_exposure`·`exposure`·`wb`)
- Modify: `Rpi5/Demo/test/anim_fps_bench.py`(`--camera` 의 `usb` 선택지 · 기본값을 `esp32` 로 · 머리말에 「USB 웹캠 기준 측정(§12.48)은 제거 전 기록」)
- Delete: `Rpi5/Demo/test/tune_exposure.py`
- Modify: `Rpi5/Demo/run_bench_test.sh`(`--source esp32` 줄은 옵션이 없어지면 삭제)

- [ ] **Step 1: 제거** — 먼저 `bench_detector.py` 의 USB 관련 구간을 전부 읽고, ESP32 경로만 남긴다(수신·방향 보정·검출 흐름은 바꾸지 않는다).
- [ ] **Step 2: 확인** — `cd /home/pi/sop-project/Rpi5/Demo && python3 test/bench_detector.py --help >/dev/null && python3 test/anim_fps_bench.py --help >/dev/null && bash -n run_bench_test.sh && grep -rnE "usb|USB|webcam|VideoCapture|tune_exposure" test/bench_detector.py test/anim_fps_bench.py run_bench_test.sh`
  Expected: 오류 없음 · grep 출력 없음(제거 사실 주석만 예외)
- [ ] **Step 3: 커밋** — `feat(측정도구): USB 웹캠 제거 ⑤ — bench·anim 의 웹캠 경로 · tune_exposure 삭제`

---

### Task 1: `frame_orient` — 해상도별 보정 파일 선택 + `px_scale`

**Files:**
- Modify: `Rpi5/Demo/frame_orient.py`
- Create: `Rpi5/Demo/selftest/test_frame_orient.py`

**Interfaces:**
- Produces:
  - `frame_orient.calibration_path(w, h, base_dir=None) -> tuple[str|None, str]` — 상태 `'ok'|'missing'|'mismatch'`
  - `frame_orient.load_undistort(w, h, base_dir=None) -> tuple[maps|None, str, str|None]` — (맵, 상태, 파일 경로)
  - `frame_orient.undistort_map(w, h)` — 기존 호출부 호환(맵만)
  - `frame_orient.px_scale(w, h) -> float` = `max(w, h) / 640`

- [ ] **Step 1: 실패하는 테스트 작성** — `selftest/test_frame_orient.py`

```python
"""frame_orient — 해상도별 보정 파일 선택·px_scale (spec 2026-09-23-XGA-런타임전환 §2.2·§2.4)."""
import os, sys, tempfile
import numpy as np
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import frame_orient as fo

K = np.array([[500., 0, 320], [0, 500., 240], [0, 0, 1]])
D = np.zeros((1, 5))

def _save(d, name, w, h):
    np.savez(os.path.join(d, name), camera_matrix=K, dist_coeffs=D, image_size=np.array([w, h]))

def test_vga_uses_legacy_file():
    with tempfile.TemporaryDirectory() as d:
        _save(d, "camera_calibration.npz", 640, 480)
        p, st = fo.calibration_path(640, 480, base_dir=d)
        assert st == "ok" and p.endswith("camera_calibration.npz")

def test_xga_prefers_per_resolution():
    with tempfile.TemporaryDirectory() as d:
        _save(d, "camera_calibration.npz", 640, 480)
        _save(d, "camera_calibration_1024x768.npz", 1024, 768)
        p, st = fo.calibration_path(1024, 768, base_dir=d)
        assert st == "ok" and p.endswith("camera_calibration_1024x768.npz")

def test_xga_without_file_is_mismatch():
    with tempfile.TemporaryDirectory() as d:
        _save(d, "camera_calibration.npz", 640, 480)
        assert fo.calibration_path(1024, 768, base_dir=d) == (None, "mismatch")

def test_missing():
    with tempfile.TemporaryDirectory() as d:
        assert fo.calibration_path(640, 480, base_dir=d) == (None, "missing")

def test_per_resolution_size_mismatch():
    with tempfile.TemporaryDirectory() as d:
        _save(d, "camera_calibration_1024x768.npz", 640, 480)   # 이름과 내용이 다름
        assert fo.calibration_path(1024, 768, base_dir=d) == (None, "mismatch")

def test_load_undistort_returns_path():
    with tempfile.TemporaryDirectory() as d:
        _save(d, "camera_calibration_1024x768.npz", 1024, 768)
        maps, st, p = fo.load_undistort(1024, 768, base_dir=d)
        assert maps is not None and st == "ok" and p.endswith("1024x768.npz")

def test_px_scale():
    assert fo.px_scale(640, 480) == 1.0
    assert fo.px_scale(480, 640) == 1.0          # 회전 뒤에도 같다
    assert fo.px_scale(1024, 768) == 1.6
    assert fo.px_scale(768, 1024) == 1.6

if __name__ == "__main__":
    for n, f in list(globals().items()):
        if n.startswith("test_"):
            f(); print("ok", n)
    print("ALL OK")
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_frame_orient.py`
Expected: `AttributeError: module 'frame_orient' has no attribute 'calibration_path'`

- [ ] **Step 3: 구현** — `frame_orient.py` 의 `undistort_map` 을 아래로 바꾸고 `px_scale` 추가(나머지 함수·머리말 유지, 머리말의 「640×480 전용」 문장은 「그 해상도 전용 — 해상도별 파일 `camera_calibration_<w>x<h>.npz`」로 고친다)

```python
def calibration_path(w, h, base_dir=None):
    """(w, h) 센서 원본 크기에 맞는 보정 파일 — (경로, 'ok'|'missing'|'mismatch').

    ① `camera_calibration_<w>x<h>.npz`(test/calib_capture.py 가 만든다) 를 먼저 찾고
    ② 없으면 기존 `camera_calibration.npz` 를 **image_size 가 맞을 때만** 쓴다.
    🔴 크기가 안 맞는 파일은 절대 쓰지 않는다 — 다른 해상도의 보정은 틀린 보정이다.
    """
    d = base_dir or os.path.dirname(config.YOLO_CALIBRATION_PATH)
    per = os.path.join(d, f"camera_calibration_{w}x{h}.npz")
    legacy = os.path.join(d, os.path.basename(config.YOLO_CALIBRATION_PATH))
    cand = [p for p in (per, legacy) if os.path.exists(p)]
    if not cand:
        return None, "missing"
    for p in cand:
        data = np.load(p)
        if "image_size" not in data or tuple(int(v) for v in data["image_size"]) == (w, h):
            return p, "ok"
    return None, "mismatch"


def load_undistort(w, h, base_dir=None):
    """→ (맵 또는 None, 상태, 쓴 파일 경로). 런타임·도구가 모두 이것을 쓴다."""
    path, status = calibration_path(w, h, base_dir)
    if path is None:
        return None, status, None
    data = np.load(path)
    cam_mat, dist = data["camera_matrix"], data["dist_coeffs"]
    new_mat, _ = cv2.getOptimalNewCameraMatrix(cam_mat, dist, (w, h), config.CALIB_ALPHA, (w, h))
    maps = cv2.initUndistortRectifyMap(cam_mat, dist, None, new_mat, (w, h), cv2.CV_16SC2)
    return maps, "ok", path


def undistort_map(w, h):
    """기존 호출부 호환 — 맵만. 없거나 크기가 다르면 None(호출부가 경고를 책임진다)."""
    return load_undistort(w, h)[0]


def px_scale(w, h):
    """픽셀 설정 배율 — VGA(640×480) 기준값에 곱한다. 긴 변 기준이라 회전 전후가 같다."""
    return max(w, h) / 640.0
```

그리고 `_selftest()` 를 있는 보정 파일마다 돌게 바꾼다:

```python
def _selftest() -> int:
    """있는 보정 파일마다 순서·크기 규약을 확인한다."""
    for (w, h) in ((640, 480), (1024, 768)):
        maps, st, p = load_undistort(w, h)
        if maps is None:
            print(f"  {w}×{h}: 보정 파일 없음({st}) — 건너뜀"); continue
        f = np.zeros((h, w, 3), np.uint8)
        out = apply_full(f, maps)
        exp = (h, w) if config.CAMERA_ROTATE_CCW90 else (w, h)
        assert (out.shape[1], out.shape[0]) == exp, out.shape
        print(f"  {w}×{h}: {os.path.basename(p)} OK")
    print("selftest OK")
    return 0
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_frame_orient.py && python3 -c "import frame_orient; frame_orient._selftest()"`
Expected: `ALL OK` · `640×480: camera_calibration.npz OK` · `1024×768: camera_calibration_1024x768.npz OK` · `selftest OK`

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/frame_orient.py Demo/selftest/test_frame_orient.py && git commit -m "feat(런타임): frame_orient — 해상도별 보정 파일 선택 + px_scale" -m "<서명 두 줄>"
```

---

### Task 2: `camera_thread` — 보정 선택을 `frame_orient` 로 · 창 전용 경로 제거 · 링 비례

**Files:**
- Modify(줄 번호는 Task W1 뒤라 달라졌을 수 있다 — 이름으로 찾는다): `Rpi5/Demo/camera_thread.py:144-177`(zone_at_point 기본값·`_load_undistort_map`), `:202-223`(신호·필드), `:313-341`(`_init_calibration`·`reload_calibration`·`_undistort`), `:461-470`(첫 프레임·`raw_frame_signal`), `:568`(ESP32 스레드 zone 호출)
- Modify: `Rpi5/Demo/config.py:367`(`HAND_ROI_RING_PX` → `HAND_ROI_RING_PX_VGA`)

**Interfaces:**
- Consumes: `frame_orient.load_undistort(w, h)`, `frame_orient.px_scale(w, h)`
- Produces: `CameraThread._ring_px: int` (첫 프레임에 결정) · 로그 `"[캘리브레이션] <파일명> 로드 (<w>×<h>)"` 또는 `"[캘리브레이션] <w>×<h> 보정 파일 없음(<상태>) — test/calib_capture.py 로 만들 것. 보정 없이 진행"`

- [ ] **Step 1: config** — `HAND_ROI_RING_PX = 25` 줄을 아래로 바꾼다(주석은 유지하고 첫 줄만 추가)

```python
# 🔴 VGA(640×480) 기준값이다. 런타임은 frame_orient.px_scale(프레임) 을 곱해 쓴다(XGA → 40px).
HAND_ROI_RING_PX_VGA = 25
```

- [ ] **Step 2: `camera_thread` 수정**
  - `_load_undistort_map` 함수(165~177)를 **삭제**하고, 상단 import 에서 `YOLO_CALIBRATION_PATH` 를 뺀다.
  - `zone_at_point` 기본 링: `ring = getattr(config, "HAND_ROI_RING_PX_VGA", 0)` (호출부가 링을 넘기지 않을 때의 기본값).
  - `raw_frame_signal`, `calibration_needed_signal`, `self._calibration_active` 를 **삭제**하고, 467~468 의 `if self._calibration_active: self.raw_frame_signal.emit(...)` 두 줄을 삭제.
  - `__init__` 에 `self._ring_px = config.HAND_ROI_RING_PX_VGA` 추가.
  - `_init_calibration`·`reload_calibration` 을 하나로:

```python
    def _init_calibration(self, w, h):
        """첫 프레임 크기로 보정 파일과 픽셀 배율을 정한다 (spec 2026-09-23 §2.2·§2.4)."""
        maps, status, path = frame_orient.load_undistort(w, h)
        self._undistort_map = maps
        self._ring_px = int(round(config.HAND_ROI_RING_PX_VGA * frame_orient.px_scale(w, h)))
        if maps is None:
            self.log_signal.emit(f"[캘리브레이션] {w}×{h} 보정 파일 없음({status}) — "
                                 f"test/calib_capture.py 로 만들 것. 보정 없이 진행")
        else:
            self.log_signal.emit(f"[캘리브레이션] {os.path.basename(path)} 로드 ({w}×{h}) · 링 {self._ring_px}px")
```
  - `reload_calibration` 메서드는 **삭제**(호출부는 Task 3 에서 없어진다).
  - 568 줄: `zone_at_point(*fingertip, self._tracks, ring=self._ring_px)`. (719 줄 USB 경로는 Task W1 에서 이미 없어졌다.)

- [ ] **Step 3: 테스트** — import·런타임 이름 확인

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_imports.py`
Expected: 통과. (Task 3 전이라 `safety_console` 이 `calibration_needed_signal`·`reload_calibration` 을 참조해 **실패할 수 있다** — 실패하면 그 이름만 확인하고 Task 3 뒤 다시 돌린다.)

Run: `grep -n "HAND_ROI_RING_PX\b\|_load_undistort_map\|raw_frame_signal\|calibration_needed_signal\|_calibration_active" camera_thread.py`
Expected: 출력 없음

- [ ] **Step 4: 커밋** (Task 3 과 함께 커밋 — 이 둘은 서로 없이 import 가 깨진다)

---

### Task 3: GUI 캘리브레이션 버튼·창 제거

**Files:**
- Modify: `Rpi5/Demo/safety_console.py:25`(BTN_CALIB import), `:50-178`(CalibrationDialog 전체), `:302`(신호 연결), `:435`(메뉴 연결), `:1646-1658`(`_on_calibration_needed`·`_open_calibration_dialog`)
- Modify: `Rpi5/Demo/overlay_menu.py:162`(`calibrate_clicked`), `:192`(메뉴 항목)
- Modify: `Rpi5/Demo/config.py:93`(`BTN_CALIB`)
- Modify: `Rpi5/Demo/selftest/test_overlay_menu.py:36-44`(메뉴 구성·신호 목록에서 캘리브레이션 제거)

- [ ] **Step 1: 테스트 먼저** — `test_overlay_menu.py` 의 신호 목록에서 `"calibrate_clicked"` 를 빼고, 구성 주석에서 「캘리브레이션」을 뺀다. 그리고 「메뉴에 캘리브레이션 항목이 없다」 확인을 추가:

```python
def test_no_calibration_item():
    """캘리브레이션은 test/calib_capture.py 로만 한다(spec 2026-09-23 D2)."""
    import overlay_menu
    assert not hasattr(overlay_menu.MenuPanel, "calibrate_clicked"), "캘리브레이션 신호가 남아 있다"
```
(클래스 이름이 `MenuPanel` 이 아니면 `overlay_menu.py:162` 가 속한 클래스 이름으로 쓴다.)

Run: `python3 selftest/test_overlay_menu.py` · Expected: 새 테스트 FAIL

- [ ] **Step 2: 제거**
  - `overlay_menu.py`: `calibrate_clicked = pyqtSignal()` 줄과 `("🎯  캘리브레이션", self.calibrate_clicked),` 항목 삭제.
  - `safety_console.py`: `CalibrationDialog` 클래스(50~178 블록 전체) · `BTN_CALIB` import · 302 줄 연결 · 435 줄 연결 · `_on_calibration_needed`·`_open_calibration_dialog`(1646~1658) 삭제. `cv2`·`np`·`QDialog` 등이 다른 곳에서 안 쓰이게 되면 import 도 정리(쓰이면 둔다 — `grep` 으로 확인).
  - `config.py`: `BTN_CALIB` 줄 삭제(다른 참조가 없음을 grep 으로 확인).

- [ ] **Step 3: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && for t in test_imports test_overlay_menu test_overlay test_theme; do python3 selftest/$t.py >/dev/null 2>&1 && echo "✅ $t" || echo "🔴 $t"; done && grep -rn "CalibrationDialog\|calibrate_clicked\|BTN_CALIB\|_open_calibration_dialog\|reload_calibration" --include=*.py . | grep -v "test/calib_capture.py"`
Expected: 4개 ✅ · grep 출력 없음(`calib_capture.py` 머리말의 설명만 예외)

- [ ] **Step 4: 커밋** (Task 2 와 함께)

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/camera_thread.py Demo/config.py Demo/safety_console.py Demo/overlay_menu.py Demo/selftest/test_overlay_menu.py && git commit -m "feat(런타임): 보정 선택을 frame_orient 로 · 링 해상도 비례 · GUI 캘리브레이션 버튼·창 제거" -m "<서명 두 줄>"
```

---

### Task 4: 보정 로더 세 번째 사본 정리 — `voice/record_voice_demo.py`

**Files:**
- Modify: `Rpi5/Demo/voice/record_voice_demo.py:228-250`(`load_undistort`)

- [ ] **Step 1:** `load_undistort(w, h)` 본문을 `frame_orient.load_undistort` 호출로 바꾼다(`frame_orient` 는 Qt·Hailo 를 끌어오지 않는 순수 모듈이라 import 해도 된다 — 머리말의 「camera_thread 를 import 하지 않는다」 이유와 충돌하지 않음):

```python
    def load_undistort(w, h):
        """왜곡보정 맵 — frame_orient.load_undistort 가 정본(해상도별 파일 선택 포함)."""
        maps, status, _ = frame_orient.load_undistort(w, h)
        return maps, status
```
(파일 상단에 `import frame_orient` 가 없으면 추가 — `sys.path` 에 Demo 가 이미 있는지 확인.)

- [ ] **Step 2: 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 -c "import ast;ast.parse(open('voice/record_voice_demo.py').read());print('OK')" && grep -n "YOLO_CALIBRATION_PATH" voice/record_voice_demo.py`
Expected: `OK` · grep 출력 없음

- [ ] **Step 3: 커밋**

---

### Task 5: 측정 도구 — 링·절벽 환산 · manifest 에 해상도

**Files:**
- Modify: `Rpi5/Demo/test/dwell_probe.py`(링 기본값), `Rpi5/Demo/test/hoi_probe_batch.py:127-134`(링 기본값), `Rpi5/Demo/test/hoi_metrics.py:32`(CLIFF_Y), `Rpi5/Demo/test/bench_detector.py:555-575`(manifest)

- [ ] **Step 1: 링** — 두 도구 모두 `--ring` 을 주지 않으면 **분석하는 사진 크기**로 환산:

```python
# dwell_probe.py / hoi_probe_batch.py — 링 기본값 결정부
import frame_orient
h0, w0 = cv2.imread(<세션 첫 PNG 경로>).shape[:2]
ring = args.ring if args.ring is not None else int(round(config.HAND_ROI_RING_PX_VGA * frame_orient.px_scale(w0, h0)))
```
`--ring` help 문구도 「기본 = VGA 25px × 사진 크기 배율(런타임과 동일)」로 고친다. `config.HAND_ROI_RING_PX` 참조가 남지 않게 한다.

- [ ] **Step 2: 절벽** — `hoi_metrics.py`:

```python
CLIFF_Y_VGA = 337     # 절벽 경계(VGA 480×640 회전 후 좌표) — §12.27. hoi.db 는 전부 VGA 세션이다.
CLIFF_Y = CLIFF_Y_VGA # XGA 세션이 hoi.db 에 들어오면 × frame_orient.px_scale 로 환산할 것
```

- [ ] **Step 3: manifest** — `bench_detector.py` 의 manifest dict 에 추가(값은 이미 있는 변수에서 — 첫 프레임 크기와 `frame_orient.load_undistort` 결과를 저장해 둔다):

```python
                "frame_size":       f"{w0}x{h0}" if w0 else None,
                "undistort":        bool(maps is not None),
                "calibration_file": os.path.basename(calib_path) if calib_path else None,
```
(manifest 는 첫 프레임 전에 쓰일 수 있다 — 그 경우 첫 프레임 뒤 manifest 를 **한 번 더 덮어쓴다**. 구현 전 `bench_detector.py:275-290`·`:553-580` 을 읽고 순서를 맞출 것.)

- [ ] **Step 4: 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && grep -rn "HAND_ROI_RING_PX\b" --include=*.py . ; python3 selftest/test_hoi_metrics.py && python3 test/dwell_probe.py test/raw/20260923_140210_esp32_s6-press-r1_console_v2 --gpio-log test/logs/20260923_140210_esp32_s6-press-r1_console_v2_gpio_log.csv 2>&1 | grep -E "사전 감지율|링"`
Expected: 첫 grep 출력 없음 · 테스트 통과 · 사전 감지율 **2/16 (12%)**(§12.67-(5) r1 과 같음 — VGA 사진이라 링 25px 그대로)

- [ ] **Step 5: 커밋**

---

### Task 6: 펌웨어 — XGA + 부팅 노출 9ms

**Files:**
- Modify: `Rpi5/arduino/camera_stream_tcp/camera_stream_tcp.ino:366`(frame_size), `:381-389`(부팅 노출), `:89-91`(「굽자마자 달라지는 것은 없다」 주석)

- [ ] **Step 1:** `config.frame_size = FRAMESIZE_XGA;` · 카메라 초기화 뒤 `printExposure("boot");` **앞**에:

```c
  // 노출 상한 밴드 = 2 → 최대 노출 약 9ms (상위 §12.66-(18) 채택 · 해상도와 무관 — 한 줄 읽는 시간이 같다 §12.68-(3))
  {
    sensor_t *se = esp_camera_sensor_get();
    if (se && se->set_reg) { se->set_reg(se, 0x3A0E, 0xFF, 0x02); delay(300); }
  }
```
89~91 주석을 「부팅 때 0x3A0E=2(9ms)를 건다 — 되돌리려면 시리얼 `W:3A0E:08`」로 고친다.

- [ ] **Step 2: 컴파일**

Run: `cd /home/pi/sop-project/Rpi5 && arduino-cli compile --fqbn "esp32:esp32:XIAO_ESP32S3:PSRAM=opi" arduino/camera_stream_tcp 2>&1 | tail -2`
Expected: 「프로그램 저장 공간 … 사용」 (오류 없음)

- [ ] **Step 3: 커밋** (굽기는 Task 8)

---

### Task 7: 문서

**Files:**
- Modify: `/home/pi/sop-project/Rpi5/CLAUDE.md`(CalibrationDialog 줄 → calib_capture 로 · 링 설명) · `/home/pi/sop-project/README.md`(해상도·보정 파일) · `/home/pi/sop-project/docs/통합문서.md`(§12 머리의 링 25px·절벽 y≈337·권장 버튼 y 에 「VGA 기준」)

- [ ] **Step 0:** `Rpi5/CLAUDE.md`·`.claude/skills/촬영/SKILL.md`·`.claude/skills/측정도구/SKILL.md`·README 에서 웹캠 관련 설명(`UsbCameraThread`·`run_scenario` 웹캠 함정 2개·`--source {esp32,usb}`·`anim_fps_bench --camera usb`)을 걷어내고, 필요한 곳에 「USB 웹캠 기능은 2026-09-23 제거 — 백업 태그 `backup/webcam-before-removal-20260923`」 한 줄.
- [ ] **Step 1:** `Rpi5/CLAUDE.md` 의 `CalibrationDialog` 두 줄을 「캘리브레이션 = `test/calib_capture.py` 만(GUI 창은 2026-09-23 제거) · 보정 파일은 `camera_calibration_<w>x<h>.npz` 우선 — `frame_orient.calibration_path`」로 바꾼다. 데이터 흐름의 `zone_at_point(roi_zones: 링1/안쪽2)` 옆에 「링 = VGA 25px × px_scale」.
- [ ] **Step 2:** 통합문서 — `grep -n "25px\|y≈337\|절벽 y\|권장 버튼 y" docs/통합문서.md` 로 나온 `[CURRENT]`·설계값 줄에 「(VGA 기준 · XGA 는 ×1.6)」을 덧붙인다. 측정 절(저널)은 건드리지 않는다.
- [ ] **Step 3: 커밋** (Rpi5 · sop-project 각각)

---

### Task 8: 실HW — 굽기 · G2 · G3

- [ ] **Step 1: 굽기** — 시리얼번호 확인 후 업로드, 부팅 로그 확인

```bash
PORT=$(python3 -c "from serial.tools import list_ports; l=[p.device for p in list_ports.comports() if p.serial_number=='3C:0F:02:DD:5E:58']; print(l[0] if l else '')"); [ -n "$PORT" ] || exit 1
cd /home/pi/sop-project/Rpi5 && arduino-cli upload -p "$PORT" --fqbn "esp32:esp32:XIAO_ESP32S3:PSRAM=opi" arduino/camera_stream_tcp
```
Expected: `Hash of data verified.` · 이어서 시리얼로 `EXP[run]: 196.0 row = 9.02 ms` 확인 · `read_esp32_ip.sh` 공유기 대역 · 첫 프레임 1024×768.

- [ ] **Step 2: G2** — 사용자가 카메라를 콘솔 정면에 고정. S1 정지 3회 → S2 좌우이동 3회

```bash
cd /home/pi/sop-project/Rpi5/Demo/test
for i in 1 2 3; do DISPLAY=:0 python3 bench_detector.py --frames 300 --no-video --hand --gpio --undistort --save-raw --raw-every 10 --condition XGA-rt-S1-r$i; done
for i in 1 2 3; do DISPLAY=:0 python3 bench_detector.py --frames 300 --no-video --hand --gpio --undistort --save-raw --raw-every 1 --condition XGA-rt-S2-r$i; done
```
판정(perf_log 원본에서): 평균 FPS ≥ 13.9 · 15 미만 연속 최장 ≤ 5프레임. **미달이면 중단 규칙**(Task 9 Step 3 로 가서 백업 복원 후 멈춤).

- [ ] **Step 3: G3** — S6 버튼 누르기 2회(B1→B4 7주기) → `dwell_probe` 로 사전 감지율 합산, 자리 기준 채점(§12.67-(2) 방식 · 반경 25×1.6=40px · score≥0.65)으로 B3→EMO 비율

```bash
for i in 1 2; do DISPLAY=:0 python3 bench_detector.py --frames 1700 --no-video --hand --gpio --undistort --save-raw --raw-every 1 --condition XGA-rt-S6-r$i; done
for r in 1 2; do python3 dwell_probe.py $(ls -d raw/*xga-rt-s6-r$r*) --gpio-log $(ls logs/*xga-rt-s6-r$r*gpio_log.csv) | grep "사전 감지율"; done
```
판정: 사전 감지율 합산 ≥ 34% · B3→EMO 확정 오분류(S1·S2·S6 전체) 비율 ≤ 57/8,453. **미달이면 중단 규칙.**

---

### Task 9: 실HW — G4 · G5 · 최종 상태

- [ ] **Step 1: G4** — `DISPLAY=:0 bash /home/pi/sop-project/Rpi5/Demo/run_demo.sh` 기동 → 로그에 `[캘리브레이션] camera_calibration_1024x768.npz 로드 (1024×768) · 링 40px` 확인 → **사용자가 GUI 에서 녹화 1회** → 녹화 파일의 1인칭 영상 크기가 768×1024 인지 `ffprobe` 로 확인.
- [ ] **Step 2: G5 되돌리기 시험** — 백업(VGA) 복원 → `run_demo.sh` 기동 → 로그 `[캘리브레이션] camera_calibration.npz 로드 (640×480) · 링 25px` 확인 → 종료.
- [ ] **Step 3: 최종 상태** — G2·G3 통과면 **XGA 펌웨어를 다시 굽는다**(Task 8 Step 1). 미달이면 VGA 백업 상태로 둔다.
- [ ] **Step 4: 자가 테스트 전부** — `for t in selftest/test_*.py; do python3 $t >/dev/null 2>&1 && echo "✅ $t" || echo "🔴 $t"; done` (G1)

---

### Task 10: work-close

- [ ] 저널 새 절(§12.70 — XGA 런타임 전환 관문 결과) · 통합문서 `[CURRENT]`(해상도 · 노출 채택값 행의 「펌웨어 미반영」 해소) · 🔓(XGA 채택 여부 · 노출 펌웨어 반영 해소 · VGA 보정 파일 ⏸ 유지) · 작업로그 · 두 저장소 push — `work-close` 스킬 절차 그대로.
