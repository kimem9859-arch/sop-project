# 공구 검출 모델 `tool_v1` — 데이터 준비 + 학습(.pt) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ARCAD 3클래스(spanner·driver·wrench)로 `yolov8n@640` 공구 검출 모델 `tool_v1.pt`를 Colab에서 학습하고, ARCAD 자동 mAP + 파이 영상 눈 확인으로 동작을 확인한다.

**Architecture:** ARCAD(12클래스, 이미 라벨됨)를 우리 Roboflow 워크스페이스로 fork → 전체 export → **로컬 필터 스크립트로 3클래스만 남김**(Roboflow MCP가 클래스 선택을 지원하지 않음) → Colab에서 Ultralytics로 학습(증강은 console과 반대로 색 켬) → ARCAD valid 자동 mAP + 파이 `tools-baseline` 눈 확인.

**Tech Stack:** Roboflow(MCP + SDK), Python 3.12, Ultralytics 8.4.x(YOLOv8n), Colab T4, pytest.

## Global Constraints

- 확정 측정 수치는 **통합문서 §10에만** 기록(설계문서·계획엔 복제 금지). 이 세션 결과 → **§10.36 신설**. (CLAUDE.md §3)
- 클래스명 정본: `names: ['spanner','driver','wrench']`, `nc: 3` — **숫자 문자열 아님**.
- ARCAD 원본 클래스 매핑: `Adjustable Spanner`→spanner · `ScrewDriver`→driver · `Wrench`→wrench.
- 데이터 라이선스 **CC BY 4.0** — 산출물·문서에 ARCAD 귀속 표기.
- 🔴 **과장 금지**: ARCAD mAP는 ARCAD 도메인 점수(우리 카메라 아님). 파이 영상엔 렌치 없음.
- 커밋 서명: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. push는 사용자 요청 시.
- 실행자 태그: **[Claude]** = 이 세션이 도구로 수행 / **[You]** = 사용자(Colab·파이·시각 확인) / **[Together]**.

---

## File Structure

- Create: `dev/ai_model/tool_v1/filter_tool_classes.py` — 12클래스 YOLO 데이터셋 → 3클래스 필터·리맵. **유일한 코드+테스트 산출물.**
- Create: `dev/ai_model/tool_v1/test_filter_tool_classes.py` — 위 스크립트 pytest.
- Create: `dev/ai_model/tool_v1/train_tool_v1.py` — Colab에서 실행하는 학습 스크립트(console_v2 패턴).
- Create: `dev/ai_model/tool_v1/README.md` — 이 모델 상태·실행 순서·귀속 표기(포인터만).
- Modify: `docs/통합문서.md` — §10.36 신설(결과 수치 정본).
- Modify: `docs/작업로그.md` — 세션 블록.

산출 가중치 `tool_v1.pt`는 Colab에서 생성 → 데스크톱/파이로 다운로드(repo 커밋 여부는 크기·정책 확인 후 결정, 기본 미커밋).

---

## Task 1: ARCAD 접근 확인 (fork 없이 공개 v9 직접 사용) [Claude] — ✅ 해소됨

> 🔴 **fork 경로 폐기 (2026-08-10, 실행 중 확인)** — `projects_fork`가 **크레딧 부족**으로 실패(6,028장 fork에 ~1.2 크레딧 필요, 워크스페이스 잔액 부족). 공개 Universe 데이터셋은 **fork 없이 Colab SDK로 직접 다운로드**되므로 fork·버전생성·export 단계를 전부 제거한다. Roboflow MCP 클래스 선택 미지원 문제도 함께 무의미해짐(어차피 Task 2 로컬 필터로 자름).

**Files:** (없음)

**Interfaces:**
- Produces: 다운로드 좌표 = 워크스페이스 `arcad` · 프로젝트 `tools-detection-b2xjk` · 버전 `9` · 포맷 `yolov8`. (Task 3 Colab 코드가 사용)

- [ ] **Step 1: 목표 3종 존재·버전 구성 확인 (완료)**

`versions_get(arcad/tools-detection-b2xjk, 9)` 확인 결과:
- 12클래스에 `Adjustable Spanner`·`ScrewDriver`·`Wrench` 모두 존재. ✅
- ⚠️ **v9 구성**: 15,300장(원본 3× 증강 구움: noise 0.54·exposure 14%·rotate 10°) · 전처리 = **Fit within 640(레터박스) + Histogram Equalization** · split = **train 13,908 / valid 1,392, test 없음**.
- 🔴 **한계(§10.36에 기록)**: v9엔 히스토그램 평활화·증강이 이미 구워져 있어 우리 ESP32 프레임과 전처리가 다르다. 크레딧 없이 얻는 유일한 12클래스 버전이라 이걸로 진행하되 성능 해석 시 감안. mAP는 **valid로 산정**(test 없음).

- [ ] **Step 2: 커밋 없음**

원격 상태 확인만. 다운로드는 Task 3(Colab)에서.

---

## Task 2: 3클래스 필터 스크립트 (TDD) [Claude]

**Files:**
- Create: `dev/ai_model/tool_v1/filter_tool_classes.py`
- Test: `dev/ai_model/tool_v1/test_filter_tool_classes.py`

**Interfaces:**
- Produces: `filter_dataset(src_dir: str, dst_dir: str, keep_names: list[str], new_names: list[str]) -> dict` — `src_dir`(YOLOv8 export 루트, `data.yaml` + `train/valid/test`) 를 읽어 `keep_names`(원본 대소문자 정확 일치)만 남기고 `new_names` 순서로 인덱스 리맵, `dst_dir`에 복제. 라벨이 없어진 이미지는 **네거티브(빈 라벨)로 유지**. 반환: `{"kept": {name:count}, "images": n, "empty": m}`.

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# dev/ai_model/tool_v1/test_filter_tool_classes.py
import os, textwrap
from filter_tool_classes import filter_dataset

def _write(p, s):
    os.makedirs(os.path.dirname(p), exist_ok=True)
    with open(p, "w") as f:
        f.write(s)

def test_filter_keeps_and_remaps(tmp_path):
    src = tmp_path / "src"; dst = tmp_path / "dst"
    # 원본 5클래스: 0=Hammer 1=Adjustable Spanner 2=ScrewDriver 3=Wrench 4=Plier
    _write(str(src/"data.yaml"),
        "train: ../train/images\nval: ../valid/images\n"
        "nc: 5\nnames: ['Hammer','Adjustable Spanner','ScrewDriver','Wrench','Plier']\n")
    # img A: spanner(1) + hammer(0)  → spanner만 남고 new idx 0
    _write(str(src/"train/images/a.jpg"), "x")
    _write(str(src/"train/labels/a.txt"), "1 0.5 0.5 0.2 0.2\n0 0.1 0.1 0.1 0.1\n")
    # img B: plier(4)만 → 라벨 비워짐(네거티브)
    _write(str(src/"train/images/b.jpg"), "x")
    _write(str(src/"train/labels/b.txt"), "4 0.5 0.5 0.2 0.2\n")
    # img C(valid): driver(2)+wrench(3) → new idx 1,2
    _write(str(src/"valid/images/c.jpg"), "x")
    _write(str(src/"valid/labels/c.txt"), "2 0.4 0.4 0.1 0.1\n3 0.6 0.6 0.1 0.1\n")

    r = filter_dataset(str(src), str(dst),
                       keep_names=['Adjustable Spanner','ScrewDriver','Wrench'],
                       new_names=['spanner','driver','wrench'])

    # data.yaml 재작성
    y = (dst/"data.yaml").read_text()
    assert "nc: 3" in y
    assert "['spanner', 'driver', 'wrench']" in y or '["spanner", "driver", "wrench"]' in y
    # a.txt: spanner만, idx 0
    a = (dst/"train/labels/a.txt").read_text().strip().splitlines()
    assert a == ["0 0.5 0.5 0.2 0.2"]
    # b.txt: 빈 파일(네거티브 유지)
    assert (dst/"train/labels/b.txt").read_text().strip() == ""
    assert (dst/"train/images/b.jpg").exists()
    # c.txt: driver→1, wrench→2
    c = sorted((dst/"valid/labels/c.txt").read_text().strip().splitlines())
    assert c == ["1 0.4 0.4 0.1 0.1", "2 0.6 0.6 0.1 0.1"]
    assert r["kept"]["spanner"] == 1 and r["kept"]["driver"] == 1 and r["kept"]["wrench"] == 1
    assert r["empty"] == 1
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd dev/ai_model/tool_v1 && python -m pytest test_filter_tool_classes.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'filter_tool_classes'`.

- [ ] **Step 3: 최소 구현**

```python
# dev/ai_model/tool_v1/filter_tool_classes.py
"""ARCAD 등 다클래스 YOLOv8 export를 지정 클래스만 남기고 인덱스 리맵.
없어진 라벨의 이미지는 네거티브(빈 라벨)로 유지한다."""
import os, re, shutil, glob

def _load_names(data_yaml):
    txt = open(data_yaml).read()
    m = re.search(r"names\s*:\s*(\[.*\])", txt, re.S)
    if m:
        items = re.findall(r"['\"]([^'\"]+)['\"]", m.group(1))
        return items
    # names 블록(list) 형식 대응
    names, cap = [], False
    for line in txt.splitlines():
        if line.strip().startswith("names:"):
            cap = True; continue
        if cap:
            mm = re.match(r"\s*-\s*(.+)\s*$", line)
            if mm: names.append(mm.group(1).strip().strip("'\""))
            else: break
    return names

def filter_dataset(src_dir, dst_dir, keep_names, new_names):
    assert len(keep_names) == len(new_names)
    src_names = _load_names(os.path.join(src_dir, "data.yaml"))
    # 원본 인덱스 → 새 인덱스
    remap = {}
    for new_i, kn in enumerate(keep_names):
        if kn not in src_names:
            raise ValueError(f"keep class '{kn}' not in source names {src_names}")
        remap[src_names.index(kn)] = new_i
    kept = {n: 0 for n in new_names}
    n_img, n_empty = 0, 0
    for split in ("train", "valid", "test"):
        img_dir = os.path.join(src_dir, split, "images")
        if not os.path.isdir(img_dir):
            continue
        for img in glob.glob(os.path.join(img_dir, "*")):
            base = os.path.splitext(os.path.basename(img))[0]
            src_lbl = os.path.join(src_dir, split, "labels", base + ".txt")
            out_lines = []
            if os.path.exists(src_lbl):
                for line in open(src_lbl):
                    parts = line.split()
                    if not parts:
                        continue
                    ci = int(parts[0])
                    if ci in remap:
                        ni = remap[ci]
                        out_lines.append(" ".join([str(ni)] + parts[1:]))
                        kept[new_names[ni]] += 1
            # 이미지·라벨 복제(라벨 없으면 빈 파일 = 네거티브)
            d_img = os.path.join(dst_dir, split, "images"); os.makedirs(d_img, exist_ok=True)
            d_lbl = os.path.join(dst_dir, split, "labels"); os.makedirs(d_lbl, exist_ok=True)
            shutil.copy(img, os.path.join(d_img, os.path.basename(img)))
            with open(os.path.join(d_lbl, base + ".txt"), "w") as f:
                f.write("\n".join(out_lines) + ("\n" if out_lines else ""))
            n_img += 1
            if not out_lines:
                n_empty += 1
    # data.yaml 재작성
    with open(os.path.join(dst_dir, "data.yaml"), "w") as f:
        f.write("train: ../train/images\nval: ../valid/images\ntest: ../test/images\n")
        f.write(f"nc: {len(new_names)}\n")
        f.write("names: [" + ", ".join(f"'{n}'" for n in new_names) + "]\n")
    return {"kept": kept, "images": n_img, "empty": n_empty}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd dev/ai_model/tool_v1 && python -m pytest test_filter_tool_classes.py -v`
Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git add dev/ai_model/tool_v1/filter_tool_classes.py dev/ai_model/tool_v1/test_filter_tool_classes.py
git commit -m "feat(tool_v1): 3클래스 필터·리맵 스크립트 + 테스트

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: Colab 학습 스크립트 + 학습 실행 [Claude 작성 / You 실행]

**Files:**
- Create: `dev/ai_model/tool_v1/train_tool_v1.py`

**Interfaces:**
- Consumes: Task 1의 `TOOL_PROJECT`·`V`·워크스페이스, Task 2의 `filter_dataset`.
- Produces: `tool_v1.pt`(Colab 산출), ARCAD valid mAP(콘솔·`runs/`).

- [ ] **Step 1: 학습 스크립트 작성**

증강은 **console과 반대(색 켬)**. console_v2가 `hsv_h=0`으로 껐던 것을 tool은 기본(0.015) 유지 + 상하뒤집기·회전 추가.

```python
# dev/ai_model/tool_v1/train_tool_v1.py
"""Colab T4에서 tool_v1 학습. 사용 전 sop-project를 clone 하고 아래 상수를 채운다.
데이터는 우리 Roboflow 프로젝트 버전에서 받아 3클래스로 필터 후 학습한다."""
import os, sys
sys.path.insert(0, os.path.dirname(__file__))
from filter_tool_classes import filter_dataset

# ── ARCAD 공개 데이터셋 직접 다운로드 (fork 없음, Task 1 참조) ──
RF_API_KEY   = os.environ["ROBOFLOW_API_KEY"]   # Colab secret (아무 유효 키)
RF_WORKSPACE = "arcad"
RF_PROJECT   = "tools-detection-b2xjk"
RF_VERSION   = 9

def main():
    from roboflow import Roboflow
    rf = Roboflow(api_key=RF_API_KEY)
    ds = rf.workspace(RF_WORKSPACE).project(RF_PROJECT).version(RF_VERSION).download("yolov8")
    src = ds.location            # 12클래스 export 루트
    dst = "/content/tool_v1_data"
    stat = filter_dataset(src, dst,
        keep_names=['Adjustable Spanner', 'ScrewDriver', 'Wrench'],
        new_names=['spanner', 'driver', 'wrench'])
    print("[filter]", stat)

    from ultralytics import YOLO
    model = YOLO("yolov8n.pt")
    model.train(
        data=os.path.join(dst, "data.yaml"),
        imgsz=640, epochs=100, patience=20, batch=16, workers=2,
        # ── 공구 증강: 색 ON(console과 반대) + 방향 자유 ──
        hsv_h=0.015, hsv_s=0.7, hsv_v=0.4,   # 색 흔들기 허용
        degrees=30.0,                         # 회전 넓게
        fliplr=0.5, flipud=0.5,               # 상하뒤집기까지
        mosaic=1.0,
        project="runs", name="tool_v1",
    )
    # ARCAD valid 자동 mAP는 학습 종료 시 출력·runs/tool_v1/에 저장된다.
    print("[done] best =", "runs/tool_v1/weights/best.pt")

if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 스모크 검증용 인자 확인 (로컬, 데이터 없이 구문만)**

Run: `python -c "import ast; ast.parse(open('dev/ai_model/tool_v1/train_tool_v1.py').read()); print('ok')"`
Expected: `ok` (구문 오류 없음). 실제 학습은 Colab에서.

- [ ] **Step 3: 커밋**

```bash
git add dev/ai_model/tool_v1/train_tool_v1.py
git commit -m "feat(tool_v1): Colab 학습 스크립트(색 증강 ON·상하뒤집기·회전)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

- [ ] **Step 4: [You] Colab 스모크 (1 epoch) 실행**

Colab T4 노트북에서:
```
!git clone https://github.com/kimem9859-arch/sop-project.git
%cd sop-project/dev/ai_model/tool_v1
!pip -q install roboflow ultralytics
import os; os.environ["ROBOFLOW_API_KEY"]="<키>"
# train_tool_v1.py의 RF_PROJECT/RF_VERSION을 Task1 값으로 수정한 뒤,
# 스모크: epochs=100 → 1 로 임시 변경해 한 번 실행
!python train_tool_v1.py
```
Expected: 다운로드 → `[filter] {...}`(kept 3종 count>0) → 1 epoch 학습이 에러 없이 끝남. **여기서 경로·필터·증강 수용을 확인**(console_v2 가이드 "스모크 먼저" 규율).

- [ ] **Step 5: [You] Colab 본 학습 실행**

`epochs=100`으로 되돌려 재실행. 종료 후 출력되는 **ARCAD valid mAP50/precision/recall**을 캡처(=§10.36 기록용). `runs/tool_v1/weights/best.pt`를 **`tool_v1.pt`로 다운로드**(데스크톱과 파이 양쪽에 저장).
Expected: 학습 완료, mAP 수치 확보, `tool_v1.pt` 확보.

---

## Task 4: 파이 영상 눈 확인 [You, 파이]

**Files:** (없음 — 명령 실행 + 시각 확인)

**Interfaces:**
- Consumes: `tool_v1.pt`(Task 3), 파이 `Demo/test/raw/20260810_154055_esp32_tools-baseline_console_v2/`(360장, §10.35).

- [ ] **Step 1: [You] 파이에 tool_v1.pt·ultralytics 준비**

파이에서 `tool_v1.pt`를 `~/`에 두고, ultralytics가 없으면 설치(`pip install ultralytics` — CPU 추론이라 Hailo 불필요).

- [ ] **Step 2: [You] 오버레이 생성**

```bash
yolo predict model=~/tool_v1.pt \
  source=~/sop-project/Rpi5/Demo/test/raw/20260810_154055_esp32_tools-baseline_console_v2/ \
  conf=0.65 save=True project=~/tool_eyeball name=v1
```
Expected: `~/tool_eyeball/v1/`에 박스가 그려진 이미지 생성. conf 0.65는 §10.35에서 오검출이 0.3%까지 떨어진 값.

- [ ] **Step 3: [You] 눈 확인**

생성된 오버레이에서 **스패너·(미니)드라이버가 옳은 박스로 잡히는지** 육안 확인. 몇 장 캡처.
Expected(판정 아님·관찰): 스패너는 대체로 잡힘. 미니드라이버는 §10.35대로 약할 수 있음(크기). **렌치는 영상에 없어 확인 대상 아님.**

- [ ] **Step 4: 관찰 결과를 Claude에 공유**

캡처·소감을 이 세션에 전달 → Task 5에서 기록.

---

## Task 5: 결과 기록 [Claude]

**Files:**
- Modify: `docs/통합문서.md` (§10.36 신설)
- Create: `dev/ai_model/tool_v1/README.md`
- Modify: `docs/작업로그.md`

**Interfaces:**
- Consumes: Task 3 ARCAD mAP, Task 4 눈 확인 결과.

- [ ] **Step 1: §10.36 신설**

§10.35 다음에 `### 10.36 🆕 공구 검출 tool_v1 학습 — ARCAD 3클래스 (Colab, 2026-08-10)` 추가. 유효성 마커 `✅ CURRENT-AS-OF 2026-08-10`, **조건 병기**(Colab·yolov8n@640·ARCAD 도메인·색 증강 ON), 표에 ARCAD valid mAP50/precision/recall + 클래스별 AP, 그리고 파이 눈 확인 소견. 🔴 "ARCAD 도메인 점수·우리 카메라 아님·렌치 미확인" 명시. **§10 머리 `[CURRENT]` 표도 함께 갱신**(CLAUDE.md §3).

- [ ] **Step 2: tool_v1 README 작성**

`dev/ai_model/tool_v1/README.md` — 상태·실행 순서(Task1~4)·ARCAD 귀속(CC BY 4.0)·수치는 "§10.36 참조" 포인터만.

- [ ] **Step 3: 작업로그 세션 블록**

`docs/작업로그.md` 최상단에 세션 블록(✅완료/⏸중단/▶다음/🔗커밋). ▶다음 = ①ONNX→DFC(.hef) ②파이 4번째 모델 배포 ③(필요시)정식 채점 라벨링 ④공구 판정 규칙 설계.

- [ ] **Step 4: 커밋**

```bash
git add docs/통합문서.md docs/작업로그.md dev/ai_model/tool_v1/README.md
git commit -m "docs(측정): tool_v1 ARCAD 학습 결과 — §10.36 신설 + 작업로그

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review (계획↔spec 대조)

- **spec §1 목표·성공기준** → Task 3(학습·mAP), Task 4(눈 확인). ✅
- **spec §2 클래스** → Task 2 매핑·리맵(테스트로 고정). ✅
- **spec §3 데이터 준비 A/B** → MCP가 클래스 선택 미지원 확인 → **B안(로컬 필터)** 확정, Task 1(fork+전체 export)+Task 2(필터). ✅
- **spec §4 증강(색 켬·상하뒤집기·회전)** → Task 3 하이퍼파라미터에 구체값. ✅
- **spec §5 학습(Colab·스모크 먼저)** → Task 3 Step 4(스모크)→Step 5(본). ✅
- **spec §6 검증(라벨링 0)** → Task 3(ARCAD 자동 mAP)+Task 4(눈 확인). ✅
- **spec §7 산출물(§10.36)** → Task 5. ✅
- **Placeholder scan**: `RF_PROJECT`·`RF_VERSION`은 Task 1이 산출하는 **인터페이스 값**(플레이스홀더 아님, Task 3에서 채움). mAP 수치는 측정 전이라 비움(§10.36에 기록). 그 외 TBD 없음. ✅
- **Type 일관성**: `filter_dataset(src_dir, dst_dir, keep_names, new_names)` 시그니처가 Task 2 정의 = Task 3 호출 동일. ✅
