# 하네스 구현 계획 — 보존·색인·배너·권한·규칙

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 병행 세션·실패 작업·저장소 밖 변경의 기록이 사라지지 않게 하고, 규칙이 깨진 곳에만 장치를 붙인다.
**Architecture:** 세션 시작 배너 훅이 배너를 만들기 전에 보존 스크립트를 순차 호출한다. 스크립트는 세션 기록을 저장소 밖에 보존하고 작업로그 기재 여부를 색인한다. 배너는 정렬 표를 버리고 출처·미기재 세션을 Claude 컨텍스트에 넣는다.
**Tech Stack:** Python 3 표준 라이브러리 · bash · Claude Code 훅/권한 설정
**Spec:** `docs/superpowers/specs/2026-09-13-하네스-design.md`

## Global Constraints

- 파이썬 **표준 라이브러리만**. pytest 없음 → 자체 점검 스크립트(`✅ … 통과` / `❌ 실패 N건`, exit 1)
- 훅은 **fail-open** — 실패해도 exit 0, `timeout` 사용(CC작업문서 §3.4)
- **저장소에 자동으로 쓰지 않는다** — 보존·색인 = `~/lab/session-archive/` (환경변수 `SESSION_ARCHIVE_DIR` 로 바꿀 수 있음)
- 보존 제외 = **현재 세션 · 껍데기(user+assistant ≤5) · 헤드리스(`entrypoint: sdk-cli`) · `.trash/`**
- 반복 스캔 간격 **1800초** · 동시 실행은 잠금으로 건너뜀
- `git add` 는 경로 지정 · 커밋 서명 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`
- 🔴 **어느 관문이든 미통과면 「완료」라고 쓰지 않는다.** 최종 확인은 사용자와 함께 선언한다(spec §3)

## 관문 · 중단 규칙

| 관문 | 위치 | 통과 기준 | 실패 시 |
|---|---|---|---|
| **G1 답을 아는 대조** | Task 1 끝 | 실데이터 색인의 기재/미기재가 아래 기대 집합과 일치 | **Task 2 착수 금지.** 불일치 줄을 열어 판정 로직 버그인지 설계 문제인지 가른다 |
| **G2 배너 수동 실행** | Task 2 끝 | JSON 유효 · 출처·미기재 행 존재 · 5초 안 | Task 2 커밋 되돌림 후 재계획 |
| **G3 금지 규칙** 🤝 | Task 3 끝 | 새 세션에서 `git add -A --dry-run` 거부 | 공식 문서 재확인. 추측으로 문법 바꾸지 않음 |
| **G4 중복 실행** 🤝 | Task 5 | `/clear` 2회 + 병행 세션 동시 시작 → `scan.log` 에 `scanned` 1줄, 나머지 skipped | 잠금·간격 로직 수정 |
| **G5 비정상 종료** 🤝 | Task 5 | 버림 세션을 강제 종료 → 다음 세션 시작 후 그 세션 보존본 존재 | 보존 트리거 재설계 |

🤝 = 사용자가 세션을 여닫아야 하는 공동 관문. Claude 는 절차와 확인 명령을 준비한다.

**G1 기대 집합**(`docs/미기록작업-점검-20260911.md` §8.2 「로그」 열 + 2026-09-13 소급·09-12 마무리 반영)
- 기재: `53a7d512 5a9084a0 bbe6055e aa9cec95 0e0e8c17 50891fa5 9a8b3463 65638bb6 5c97a44b 9801dc86 0f9bdbaf c814e9ec f8fcecdc 6d034bf1 92229f89 3e4f101a 75c0669f 11fd399e 730420c8 9eb3cb46 067ebb47 3ee3d7c9 17c2ff79`
- 미기재: `791095c2 efe6cca3 1461df7a 42619f0d e55e95ca d97f5518 1b03891d d269e6d5 12ed2c28 625ef30b 2c09d49e 35b266bc dffb154d 030ca218 e38a44b3 1b004b69 1e79f4c8 aa6fb348 cf516f07 15f41511 c419d57e 60557982 df612aa6 0a1fab9f`

---

### Task 1: 보존·색인 스크립트

**Files:** Create `.claude/hooks/session_archive.py` · Create `.claude/hooks/test_session_archive.py` · Create `~/lab/session-archive/README.md`(저장소 밖)

**Interfaces — Produces:**
- `sess_dir(project_dir) -> str` · `parse_session(path) -> dict | None`(키 `id prompts files commits ts tail ua`)
- `run(project_dir, current_id, now=None) -> str` — `"skipped-lock"` · `"skipped-recent"` · `"scanned N written M"`
- `unlogged(days, now=None) -> list[str]` — `"<ID8> <시작일> <첫지시>"`
- CLI `session_archive.py <project_dir> <current_id>` · `session_archive.py --unlogged <days>` · 산출물 `INDEX.tsv`(열: 시작일·세션·첫지시·편집·커밋·기재) · `<시작일>_<ID8>.md` · `.last-scan` · `scan.log`

- [ ] **Step 1: 점검 스크립트를 먼저 쓴다** — `test_session_archive.py`

```python
"""보존·색인 관문 — spec 2026-09-13-하네스-design.md §6.1·§6.2. 실행: python3 .claude/hooks/test_session_archive.py"""
import fcntl, json, os, sys, tempfile
sys.dont_write_bytecode = True
_tmp = tempfile.mkdtemp()
os.environ["HOME"] = _tmp
os.environ["SESSION_ARCHIVE_DIR"] = os.path.join(_tmp, "archive")
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
import session_archive as sa
_fails = []
def check(c, m):
    if not c: _fails.append(m)
PROJ = os.path.join(_tmp, "proj")
A, B, C, D, E = ("aaaaaaaa-0000-0000-0000-000000000001", "bbbbbbbb-0000-0000-0000-000000000002",
                 "cccccccc-0000-0000-0000-000000000003", "dddddddd-0000-0000-0000-000000000004",
                 "eeeeeeee-0000-0000-0000-000000000005")
def session(sid, turns, extra=(), sub=""):
    d = os.path.join(sa.sess_dir(PROJ), sub); os.makedirs(d, exist_ok=True)
    rows = []
    for i in range(turns):
        rows.append({"type": "user", "timestamp": "2026-09-13T01:00:0%dZ" % (i % 10), "message": {"content": "지시 %d" % i}})
        rows.append({"type": "assistant", "timestamp": "2026-09-13T01:00:0%dZ" % (i % 10), "message": {"content": [{"type": "text", "text": "응답 %d" % i}]}})
    rows += list(extra)
    open(os.path.join(d, sid + ".jsonl"), "w").write("\n".join(json.dumps(r, ensure_ascii=False) for r in rows))
def tool(name, inp):
    return {"type": "assistant", "timestamp": "2026-09-13T02:00:00Z", "message": {"content": [{"type": "tool_use", "name": name, "input": inp}]}}
T0 = 1_800_000_000
def test_1_parse():
    session(A, 3, [tool("Edit", {"file_path": "/x/a.py"}), tool("Bash", {"command": 'git commit -q -m "feat: 보존\n\n본문"'})])
    s = sa.parse_session(os.path.join(sa.sess_dir(PROJ), A + ".jsonl"))
    check(s and len(s["prompts"]) == 3, "사용자 발화 3건"); check(s and s["files"] == ["/x/a.py"], "편집 파일")
    check(s and s["commits"] == ["feat: 보존"], "커밋 제목")
def test_2_scope():
    session(B, 3); session(C, 2); session(D, 3); session(E, 3, sub=".trash")
    os.makedirs(os.path.join(PROJ, "docs"), exist_ok=True)
    open(os.path.join(PROJ, "docs", "작업로그.md"), "w").write("## 2026-09-13 · session %s (테스트)\n" % A)
    r = sa.run(PROJ, D, now=T0); arch = os.environ["SESSION_ARCHIVE_DIR"]
    check(r.startswith("scanned 2"), "대상 2건(A·B): " + r)
    idx = open(os.path.join(arch, "INDEX.tsv")).read()
    check("%s\t지시 0\t1\t1\t기재" % A in idx, "A 기재"); check(B in idx and "미기재" in idx, "B 미기재")
    for x in (C, D, E): check(x not in idx, "제외 실패: " + x[:8])
def test_3_recent_skip():
    check(sa.run(PROJ, D, now=T0 + 60) == "skipped-recent", "30분 안 재실행은 건너뛴다")
def test_4_lock_skip():
    lk = open(os.path.join(os.environ["SESSION_ARCHIVE_DIR"], ".lock"), "w"); fcntl.flock(lk, fcntl.LOCK_EX)
    check(sa.run(PROJ, D, now=T0 + 4000) == "skipped-lock", "잠금 중이면 건너뛴다"); lk.close()
def test_5_rewrite_newer():
    src = os.path.join(sa.sess_dir(PROJ), B + ".jsonl"); os.utime(src, (T0 + 9e8, T0 + 9e8))
    check("written 1" in sa.run(PROJ, D, now=T0 + 8000), "원본이 새로우면 다시 쓴다")
def test_6_unlogged():
    u = sa.unlogged(36500, now=T0 + 8000)
    check(len(u) == 1 and u[0].startswith("bbbbbbbb"), "미기재 목록 = B: %r" % u)
if __name__ == "__main__":
    for n, f in sorted(globals().items()):
        if n.startswith("test_"): f()
    print(("❌ 실패 %d건\n   - " % len(_fails) + "\n   - ".join(_fails)) if _fails else "✅ 보존·색인 관문 통과")
    sys.exit(1 if _fails else 0)
```

- [ ] **Step 2: 실패 확인** — `python3 .claude/hooks/test_session_archive.py` → `ModuleNotFoundError: session_archive`
- [ ] **Step 3: 구현** — `session_archive.py`

```python
#!/usr/bin/env python3
"""세션 보존·색인 — spec 2026-09-13-하네스-design.md §6.1·§6.2. 판단 없음. 실패해도 exit 0.
사용: session_archive.py <project_dir> <current_session_id> | session_archive.py --unlogged <days>"""
import fcntl, glob, json, os, re, sys, time
ARCHIVE = os.path.expanduser(os.environ.get("SESSION_ARCHIVE_DIR", "~/lab/session-archive"))
MIN_INTERVAL, STUB_MAX_UA = 1800, 5
LOGS = ("docs/작업로그.md", "docs/claude-code-작업로그.md")

def sess_dir(project_dir):
    return os.path.expanduser("~/.claude/projects/" + project_dir.replace("/", "-"))

def commit_subject(cmd):
    m = re.search(r'-m\s+"([^"\n]+)', cmd) or re.search(r"<<'?\w+'?\n([^\n]+)", cmd)
    return (m.group(1) if m else cmd.split("\n")[0])[:100]

def parse_session(path):
    s = {"id": os.path.basename(path)[:-6], "prompts": [], "files": [], "commits": [], "ts": [], "tail": "", "ua": 0}
    for line in open(path, errors="ignore"):
        try:
            e = json.loads(line)
        except ValueError:
            continue
        t = e.get("type")
        if t not in ("user", "assistant"):
            continue
        s["ua"] += 1
        if e.get("timestamp"):
            s["ts"].append(e["timestamp"])
        c = (e.get("message") or {}).get("content")
        if t == "user" and isinstance(c, str) and not c.startswith("<") and not e.get("isMeta"):
            s["prompts"].append(c)
        for b in (c if t == "assistant" and isinstance(c, list) else []):
            if not isinstance(b, dict):
                continue
            if b.get("type") == "text" and b.get("text"):
                s["tail"] = b["text"][-300:]
            inp = b.get("input") or {}
            if b.get("type") == "tool_use" and b.get("name") in ("Edit", "Write", "NotebookEdit"):
                fp = inp.get("file_path") or inp.get("notebook_path")
                if fp and fp not in s["files"]:
                    s["files"].append(fp)
            if b.get("type") == "tool_use" and b.get("name") == "Bash" and "git commit" in inp.get("command", ""):
                s["commits"].append(commit_subject(inp["command"]))
    return None if s["ua"] <= STUB_MAX_UA else s

def render(s):
    out = ["# " + s["id"], "- 시작 %s · 끝 %s" % ((s["ts"] or ["?"])[0], (s["ts"] or ["?"])[-1]), "", "## 사용자 발화"]
    out += ["\n[%d] %s" % (i + 1, p) for i, p in enumerate(s["prompts"])]
    out += ["", "## 편집 파일"] + ["- " + f for f in s["files"]] + ["", "## 커밋"] + ["- " + c for c in s["commits"]]
    return "\n".join(out + ["", "## 마지막 응답 꼬리", s["tail"]]) + "\n"

def is_logged(sid, texts):
    return any(sid in t or re.search(r"\b%s\b" % sid[:8], t) for t in texts)

def _log(now, result):
    open(os.path.join(ARCHIVE, "scan.log"), "a").write("%s\t%s\n" % (time.strftime("%Y-%m-%dT%H:%M:%S", time.localtime(now)), result))
    return result

def run(project_dir, current_id, now=None):
    now = now or time.time()
    os.makedirs(ARCHIVE, exist_ok=True)
    lock = open(os.path.join(ARCHIVE, ".lock"), "w")
    try:
        fcntl.flock(lock, fcntl.LOCK_EX | fcntl.LOCK_NB)
    except OSError:
        return _log(now, "skipped-lock")
    stamp = os.path.join(ARCHIVE, ".last-scan")
    if os.path.exists(stamp) and now - float(open(stamp).read() or 0) < MIN_INTERVAL:
        return _log(now, "skipped-recent")
    texts = [open(os.path.join(project_dir, p), errors="ignore").read() for p in LOGS if os.path.exists(os.path.join(project_dir, p))]
    rows, written = [], 0
    for path in sorted(glob.glob(os.path.join(sess_dir(project_dir), "*.jsonl"))):
        sid = os.path.basename(path)[:-6]
        try:  # 깨진 세션 파일 하나가 전체 스캔을 죽이지 않게
            s = None if sid == current_id else parse_session(path)
        except OSError:
            continue
        if not s:
            continue
        day = s["ts"][0][:10] if s["ts"] else "0000-00-00"
        dst = os.path.join(ARCHIVE, "%s_%s.md" % (day, sid[:8]))
        if not os.path.exists(dst) or os.path.getmtime(path) > os.path.getmtime(dst):
            open(dst, "w").write(render(s)); written += 1
        first = (s["prompts"][0] if s["prompts"] else "").replace("\t", " ").replace("\n", " ")[:60]
        rows.append("\t".join([day, sid, first, str(len(s["files"])), str(len(s["commits"])), "기재" if is_logged(sid, texts) else "미기재"]))
    open(os.path.join(ARCHIVE, "INDEX.tsv"), "w").write("시작일\t세션\t첫지시\t편집\t커밋\t기재\n" + "\n".join(rows) + "\n")
    open(stamp, "w").write(str(now))
    return _log(now, "scanned %d written %d" % (len(rows), written))

def unlogged(days, now=None):
    cutoff = time.strftime("%Y-%m-%d", time.localtime((now or time.time()) - days * 86400))
    p = os.path.join(ARCHIVE, "INDEX.tsv")
    rows = [l.split("\t") for l in open(p).read().splitlines()[1:] if l] if os.path.exists(p) else []
    return ["%s %s %s" % (r[1][:8], r[0], r[2]) for r in rows if r[5] == "미기재" and r[0] >= cutoff]

if __name__ == "__main__":
    try:
        print("\n".join(unlogged(int(sys.argv[2]))) if sys.argv[1] == "--unlogged" else run(sys.argv[1], sys.argv[2] if len(sys.argv) > 2 else ""))
    except Exception as ex:  # fail-open
        print("archive-error %s" % ex, file=sys.stderr)
    sys.exit(0)
```

- [ ] **Step 4: 통과 확인** — 같은 명령 → `✅ 보존·색인 관문 통과`
- [ ] **Step 5: 저장소 밖 README** — `~/lab/session-archive/README.md` 에 「세션 기록 보존본·색인. 생성 = sop-project `.claude/hooks/session_archive.py`(세션 시작 배너 훅이 호출). 삭제해도 다음 스캔에서 재생성되나 보존 기간(50일)이 지난 원본은 복구 불가 — **지우지 말 것**」
- [ ] **Step 6: G1 — 실데이터 대조**(임시 폴더로 돌려 실제 보존 폴더의 30분 간격을 건드리지 않는다)

```bash
T=$(mktemp -d) && SESSION_ARCHIVE_DIR=$T python3 .claude/hooks/session_archive.py /home/pi/sop-project none && \
python3 - "$T/INDEX.tsv" <<'PY'
import sys
got = {r.split("\t")[1][:8]: r.split("\t")[5] for r in open(sys.argv[1]).read().splitlines()[1:] if r}
yes = "53a7d512 5a9084a0 bbe6055e aa9cec95 0e0e8c17 50891fa5 9a8b3463 65638bb6 5c97a44b 9801dc86 0f9bdbaf c814e9ec f8fcecdc 6d034bf1 92229f89 3e4f101a 75c0669f 11fd399e 730420c8 9eb3cb46 067ebb47 3ee3d7c9 17c2ff79".split()
no = "791095c2 efe6cca3 1461df7a 42619f0d e55e95ca d97f5518 1b03891d d269e6d5 12ed2c28 625ef30b 2c09d49e 35b266bc dffb154d 030ca218 e38a44b3 1b004b69 1e79f4c8 aa6fb348 cf516f07 15f41511 c419d57e 60557982 df612aa6 0a1fab9f".split()
bad = [f"{i} 기대=기재 실제={got.get(i,'없음')}" for i in yes if got.get(i) != "기재"] + [f"{i} 기대=미기재 실제={got.get(i,'없음')}" for i in no if got.get(i) != "미기재"]
print("표 밖 세션:", sorted(set(got) - set(yes) - set(no)))
print("\n".join(bad) if bad else "✅ G1 통과")
PY
```
Expected: `✅ G1 통과`. 불일치가 나오면 **관문 규칙대로 멈춘다** — `grep -n <ID8> docs/*작업로그.md` 로 그 줄이 소급 블록인지 지나가는 언급인지 본다. 지나가는 언급이면 ID8 보조 매칭이 너무 느슨하다는 **설계 문제**이므로 사용자와 정한다.

- [ ] **Step 7: Commit** — `git add .claude/hooks/session_archive.py .claude/hooks/test_session_archive.py && git commit -m "feat(hooks): 세션 보존·색인 스크립트 — G1 통과"`

### Task 2: 배너 개편

**Files:** Modify `.claude/hooks/_banner.py`(전체 교체) · Modify `.claude/hooks/session-worklog-brief.sh` · Create `.claude/hooks/test_banner.py`
**Interfaces — Consumes:** Task 1 CLI 두 형태. **Produces:** 입력 규약 `T/R/N` 유지(호출부 변경 최소)

- [ ] **Step 1: 점검 스크립트** — `test_banner.py`

```python
"""배너 관문 — spec §6.3. 실행: python3 .claude/hooks/test_banner.py"""
import json, os, subprocess, sys
inp = "T\t🚀 세션 시작 점검\nR\t🔄 sop-project\t동기화됨\nR\t⏸ 🔴 인터락\t미복구\nN\t🆔 abc-123\n"
out = subprocess.run([sys.executable, os.path.join(os.path.dirname(os.path.abspath(__file__)), "_banner.py")], input=inp, capture_output=True, text=True).stdout
j = json.loads(out); ctx = j["hookSpecificOutput"]["additionalContext"]; msg = j["systemMessage"]
fails = [f"누락 {s}" for s in ("sop-project", "동기화됨", "인터락", "미복구", "abc-123") if s not in ctx]
fails += ["정렬 표 잔존"] if ("┌" in ctx or "│" in ctx) else []
fails += ["사람 화면이 한 줄 아님"] if "\n" in msg else []
print("❌ " + "; ".join(fails) if fails else "✅ 배너 관문 통과"); sys.exit(1 if fails else 0)
```

- [ ] **Step 2: 실패 확인** — `python3 .claude/hooks/test_banner.py` → `❌ … 정렬 표 잔존; 사람 화면이 한 줄 아님`
- [ ] **Step 3: `_banner.py` 전체 교체**

```python
#!/usr/bin/env python3
# SessionStart 배너 헬퍼 — 입력 T/R/N(탭 구분). 읽는 쪽은 Claude 이므로 정렬 표를 쓰지 않는다(spec 2026-09-13 §6.3).
# systemMessage = 사람 화면 한 줄 요약 · additionalContext = 전체 행
import sys, json
title, rows, notes = None, [], []
for line in sys.stdin:
    p = line.rstrip("\n").split("\t")
    if p[0] == "T":
        title = p[1] if len(p) > 1 else ""
    elif p[0] == "R":
        rows.append(((p[1] if len(p) > 1 else ""), (p[2] if len(p) > 2 else "")))
    elif p[0] == "N":
        notes.append(p[1] if len(p) > 1 else "")
ctx = "\n".join(([title] if title else []) + [("%s %s" % kv).rstrip() for kv in rows] + notes)
n_open = sum(1 for k, _ in rows if k.startswith(("⏸", "▶")))
msg = "%s · 대기 %d건 · 상세는 Claude 컨텍스트" % (title or "세션 시작", n_open)
sys.stdout.write(json.dumps({"systemMessage": msg, "hookSpecificOutput": {"hookEventName": "SessionStart", "additionalContext": ctx}}, ensure_ascii=False) + "\n")
```

- [ ] **Step 4: `session-worklog-brief.sh` 수정** — ①6행 주석 「앞서 도는」 → 「내부에서 순차 호출하는」 ②sync-check 호출 줄 바로 뒤에 추가:

```bash
# 보존·색인(spec 2026-09-13 §6.1) — 배너보다 먼저, 순차. 실패해도 계속.
timeout 20 python3 "$PROJ/.claude/hooks/session_archive.py" "$PROJ" "$SID" >/dev/null 2>&1 || true
UNLOGGED=$(python3 "$PROJ/.claude/hooks/session_archive.py" --unlogged 7 2>/dev/null)
LATEST_DATE=$(awk '/^## /{print $2; exit}' "$LOG" 2>/dev/null)
```
③ 이어하기 행 조립 직전에 `printf 'R%s📌 대기 항목 출처%s작업로그 최신 블록 %s — 그 뒤 세션이 미기재면 낡았을 수 있다. 실제 상태로 대조할 것\n' "$TAB" "$TAB" "${LATEST_DATE:-?}"` ④ 이어하기 `fi` 뒤에:

```bash
  if [ -n "$UNLOGGED" ]; then
    printf 'R%s📭 미기재 세션(7일)%s%s개\n' "$TAB" "$TAB" "$(printf '%s\n' "$UNLOGGED" | grep -c .)"
    printf '%s\n' "$UNLOGGED" | while IFS= read -r u; do printf 'R%s  ·%s%s\n' "$TAB" "$TAB" "$u"; done
  fi
```

- [ ] **Step 5: 통과 확인** — `python3 .claude/hooks/test_banner.py` → `✅ 배너 관문 통과`
- [ ] **Step 6: G2 — 훅 수동 실행**(임시 보존 폴더)

```bash
T=$(mktemp -d); time (echo '{"session_id":"g2-test"}' | CLAUDE_PROJECT_DIR=/home/pi/sop-project SESSION_ARCHIVE_DIR=$T bash .claude/hooks/session-worklog-brief.sh | python3 -c 'import sys,json; j=json.load(sys.stdin); print(j["systemMessage"]); print(j["hookSpecificOutput"]["additionalContext"])')
```
Expected: 한 줄 요약 + `📌 대기 항목 출처` + `📭 미기재 세션(7일)` 행 · real 5초 미만. 실패 시 **관문 규칙대로 되돌린다**.
- [ ] **Step 7: Commit** — 세 파일 경로 지정 add · `"feat(hooks): 배너 개편 — 정렬 표 폐기·출처·미기재 세션 주입 (G2 통과)"`

### Task 3: 권한 금지 목록

**Files:** Modify `.claude/settings.json` · Modify `CLAUDE.md:86`

- [ ] **Step 1: deny 추가**(기존 내용 보존)

```bash
python3 - <<'PY'
import json; p=".claude/settings.json"; d=json.load(open(p))
d["permissions"]["deny"] = ["Bash(git add -A *)", "Bash(git add --all *)", "Bash(git add . *)"]
open(p,"w").write(json.dumps(d, ensure_ascii=False, indent=2) + "\n"); print(json.load(open(p))["permissions"]["deny"])
PY
```
- [ ] **Step 2: CLAUDE.md 86행 교체** → `- ⚠️ **\`git add -A\`·\`--all\`·\`.\` 은 권한 설정(deny)이 막는다** — 2026-08-21 백업 docx 29MB 통째 커밋. 경로를 지정해 add 한다.`
- [ ] **Step 3: Commit** — `git add .claude/settings.json CLAUDE.md` · `"feat(settings): git add 전체 추가 금지 규칙 — 지시문 줄을 장치로"`
- [ ] **Step 4: G3 🤝** — 사용자가 새 세션을 연다 → Claude 가 `git add -A --dry-run` 실행 시도 → **거부 메시지** 확인. 거부 안 되면 관문 규칙대로.

### Task 4: 규칙 문서화

**Files:** Modify `CLAUDE.md` §2 · `docs/claude-code-작업문서.md` §1·§2.7(신설)·§3.5 · `.claude/skills/session-wrap/SKILL.md` §2

- [ ] **Step 1: CLAUDE.md §2** — 「수술적·최소 구현」 불릿 뒤에 4줄: ①역할 분담(구상=사용자 / 계획·구현·검토=Claude / 검증 결과·최종 기능 구현 확인=**공동**, 🔴 **Claude 단독 「완료」 금지**) ②사용자 프롬프트도 틀릴 수 있다 — 사실은 착수 전 대조, 어긋나면 **정정안+근거를 말하고 진행** ③사용자 검토는 파일을 열게 하지 않는다 — 절마다 한 줄(Claude 가 정한 것은 그 자리) + 버린 대안 + 질문 ④배너 ⏸·▶ 는 낡을 수 있다 — 실제 상태로 대조 ⑤「완료」라고 쓸 때 **관문 결과 출처**를 붙이고, 실HW 미검증은 ⏸ 로 적는다(spec §8). 각 줄 끝에 「상세 = CC작업문서 §2.7」.
- [ ] **Step 2: CC작업문서** — §2.7 신설(공동 확인 분담 · 검증 범위 4갈래 · 최종 리뷰 기준에 **사용자 구상 원문**(보존본 인용) 포함 · 멀티에이전트·루프는 **요청 시**, 루프는 최종 확인에서 멈춤 · 계획 문서는 사용자 검토 대상 아님). §1 표에 ③ `session_archive.py` 행(①이 순차 호출, 산출물 저장소 밖). §3.5 에 「2026-09-13 보존·색인 추가 — 요약 원칙과 충돌하지 않는 이유 = spec §6.2」 한 줄.
- [ ] **Step 3: SKILL §2** — 4번을 「미기록이 있으면 **확인 없이 소급 블록을 쓴다**(사실 기재는 판단이 아니다). 원인 해석처럼 판단이 갈리는 것만 확인. `~/lab/session-archive/INDEX.tsv` 의 미기재 세션도 후보로 보고 그 보존본을 근거로 쓴다」로 교체. 5번의 「승인 시」 삭제.
- [ ] **Step 4: 확인** — `grep -c "공동\|정정안\|요약" CLAUDE.md` ≥3 · `wc -l CLAUDE.md` ≤ 200 · `grep -n "확인 없이 소급" .claude/skills/session-wrap/SKILL.md`
- [ ] **Step 5: Commit** — 세 파일 경로 지정 · `"docs(harness): 역할 분담·검증 의무·요약 보고·소급 완화 명문화"`

### Task 5: 공동 관문 · 최종 리뷰

- [ ] **Step 1: G4 🤝** — 사용자가 새 세션을 연다 → Claude 가 자기 컨텍스트에서 `📌 대기 항목 출처`·`📭 미기재 세션` 행을 확인(spec §11.3) → 그 세션에서 `/clear` 2회, 이어서 두 번째 세션을 동시에 연다 → Claude: `tail -5 ~/lab/session-archive/scan.log` → `scanned` 1줄 + `skipped-recent`/`skipped-lock` 만
- [ ] **Step 2: G5 🤝** — 버림 세션에서 사용자 발화 3회 이상 → 터미널 강제 종료 → 31분 뒤 새 세션 → Claude: `ls ~/lab/session-archive/ | grep <버림 ID8>`
- [ ] **Step 3: 최종 리뷰 1회** — `superpowers:requesting-code-review`, 기준 = spec + 이 세션 사용자 발화 원문
- [ ] **Step 4: 7월 결정 재측정** — 이 세션 기록에서 **Task 1 첫 커밋 ~ 최종 리뷰 끝** 구간만 잘라 메인 출력·캐시 읽기·어시스턴트 턴·Agent 호출을 뽑아 `docs/claude-code-작업로그.md` 2026-07-28 표와 같은 형식으로 기록한다. ⚠️ 이 세션은 구현 전 대화가 길어 캐시 읽기가 부풀려진다 — **조건을 병기하고 단일 값으로 결론 내지 않는다**
- [ ] **Step 5: 공동 선언 후 세션마무리** — 관문 5개 결과를 `docs/claude-code-작업로그.md` 에 기록. 미통과 관문이 있으면 ⏸ 로 적고 「완료」 금지
