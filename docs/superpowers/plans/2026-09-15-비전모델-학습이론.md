# 비전 모델 재정립 ① 학습 이론 정립 — 실행 계획

> **실행 방식:** `superpowers:executing-plans` 인라인(CC작업문서 §2.1·§2.2 — 이 작업은 절마다 사용자 확인이 끼어 병렬성이 없다). 체크박스(`- [ ]`)로 추적.

**Goal:** 1차 자료 근거로 YOLO 학습 이론 8절 + 추가 발견을 `dev/ai_model/학습이론.md` 에 정립한다.

**Architecture:** 절 하나 = 태스크 하나. 조사(context7·공식 문서·논문) → 작성 → 대화 요약 → 사용자 확인 → 커밋. 코드 없음.

**Tech Stack:** context7(`/websites/ultralytics`·`/ultralytics/ultralytics`) · WebFetch/WebSearch(Hailo·Roboflow 공식, arXiv)

**Spec:** `docs/superpowers/specs/2026-09-15-비전모델-학습이론-design.md`

## Global Constraints

- 입력 = ESP32 **480×640 세로** · 학습 = Colab CLI(T4) · 라벨링 = Roboflow · 모델 = YOLO · 배포 = Hailo `.hef`
- 🔒 **출처 관문** — 주장마다 1차 출처(URL·논문명). 없으면 `⚠️ 미확인` 표시, 결론 근거 금지. 블로그는 1차 자료가 없을 때만 `(블로그)` 표시
- 🔒 **기억 금지** — Claude 학습 지식으로 채우지 않는다. 조회가 기억과 다르면 조회가 이긴다
- 절마다 **요점 몇 줄 + 「우리 상황 적용」 한두 줄.** 수식·옵션 나열 금지(사용자 요청)
- 조사 중 발견한 학습 관련 조건·데이터 → 맞는 절, 없으면 「추가 발견」 절
- 🔴 **측정 수치는 이 문서·학습이론.md 에 쓰지 않는다** — 우리 값은 통합문서 §10 포인터만
- 🔴 **기존 모델 결과(`console_v1`·`v2`, `tool_v1`~`v4`)는 근거로 쓰지 않는다** — 「과거에 이렇게 했다」 사례로만 인용
- 커밋 = `sop-project`, 경로 지정 add, 서명 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. push 는 마지막 `work-close` 에서 한 번

---

### Task 1: 준비 — 불신 표시 · 문서 골격 · README

**Files:**
- Create: `dev/ai_model/학습이론.md`
- Modify: `docs/통합문서.md` (L931 `### 정확도 (버튼 검출 console_v2)` 아래, L941 `### 정확도 (공구 검출)` 아래, 🔓 표)
- Modify: `README.md` (L149 학습 절차 행 위)

- [ ] **Step 1:** 통합문서 두 정확도 표 제목 바로 아래에 한 줄씩:
  `> ⚠️ **2026-09-15 부터 전면 재검토 대상** — 학습 설계 없이 만든 모델이라 결과를 신뢰하지 않는다(사용자 결정). 재정립 = spec \`2026-09-15-비전모델-학습이론\` ①~④. 수치는 이력으로 보존(R2).`
- [ ] **Step 2:** 🔓 표 `console_v3` 행 위에 행 추가:
  `| 비전 모델 전면 재정립 ①이론 ②데이터 확보 ③평가 체계 ④학습·검증 | 비전 | 기존 결과 불신(2026-09-15). ① 진행 중 — 결과물 \`dev/ai_model/학습이론.md\`. 하위 = \`console_v3\` · \`tool_v5\` 행 | spec \`2026-09-15-비전모델-학습이론\` | 2026-09-15 |`
- [ ] **Step 3:** `학습이론.md` 골격 — 머리(목적·출처 관문·「수치는 §10」·「결정은 통합문서 §7」) + 8절 제목 + `## 추가 발견` + 절마다 `### 출처` 소제목.
- [ ] **Step 4:** README L149 앞에 행: `| 비전 모델 학습 이론(1차 자료 근거·재정립 ①) | [\`dev/ai_model/학습이론.md\`](dev/ai_model/학습이론.md) |`
- [ ] **Step 5 (검증):** `grep -n "전면 재검토 대상" docs/통합문서.md` → 2줄 · `grep -c "^## " dev/ai_model/학습이론.md` → 9 이상
- [ ] **Step 6:** 커밋 `docs(비전재정립): ① 준비 — 기존 결과 불신 표시·학습이론 골격·README`

---

### 절 태스크 공통 절차 (Task 2~9 모두 이 순서 — 각 태스크에 복제된 것으로 읽는다)

1. **조사** — 아래 「조사 질문」마다 1차 자료 조회. context7 은 질문 하나에 한 주제.
2. **작성** — 요점 3~6줄 + 「우리 상황 적용」 1~2줄 + `### 출처`(URL 목록).
3. **관문 점검** — 요점마다 출처 번호가 붙었나. 없으면 `⚠️ 미확인`.
4. **커밋** — `docs(학습이론): N절 <주제> 초안`
5. **턴 끝 상태 출력** — 아래 「상태 점검 명령」을 돌려 출력을 대화에 남긴다(판정 모델은 파일을 못 읽고 대화만 본다).

> ~~사용자 확인 전 다음 절 착수 금지~~ → 2026-09-15 `/goal` 루프로 변경 — 절별 확인 없이 진행, 사용자 검토는 Task 10 에서 한 번.

#### 상태 점검 명령

```bash
cd /home/pi/sop-project && f=dev/ai_model/학습이론.md
for n in 1 2 3 4 5 6 7 8; do
  sec=$(awk -v n="$n" '$0 ~ "^## "n"\\." {p=1; print; next} /^## / {p=0} p' "$f")
  pts=$(echo "$sec" | grep -cE "^[0-9]+\. \*\*|^[0-9]+\. [^h]" )
  src=$(echo "$sec" | awk '/^### 출처/{p=1;next} p' | grep -c "http")
  unc=$(echo "$sec" | grep -c "⚠️ 미확인")
  app=$(echo "$sec" | grep -c "우리 상황 적용")
  chk=$(echo "$sec" | grep -c "✅ 원문 대조")
  crs=$(echo "$sec" | grep -c "✅ 교차 확인")
  echo "절$n 줄수=$(echo "$sec" | wc -l) 출처URL=$src 미확인=$unc 적용=$app 원문대조=$chk 교차확인=$crs"
done
add=$(awk '/^## 추가 발견/{p=1;next} p' "$f")
echo "추가발견 항목=$(echo "$add" | grep -c '^- ') 관점①=$(echo "$add" | grep -c '관점 ①') 관점②=$(echo "$add" | grep -c '관점 ②') 관점③=$(echo "$add" | grep -c '관점 ③') 대조가이드=$(echo "$add" | grep -c '대조한 가이드')"
git status --short "$f"
```

#### `/goal` 완료 조건 (사용자가 입력)

1. 절 1~8 전부 `적용=1` 이상 · `출처URL≥1` · 계획서 그 절의 **조사 질문 전부**가 출처 붙은 요점이거나 `⚠️ 미확인(찾아본 곳)` 으로 처리됨 — 턴 끝에 절별 「질문 ①~⑥ → 요점 번호/미확인」 대응표를 출력
2. 절 1~8 전부 `원문대조=1` — 요점마다 출처 원문을 다시 열어 일치 확인, 불일치는 고친 뒤 절 끝에 `> ✅ 원문 대조 <날짜>` 한 줄
3. 절 1~8 전부 `교차확인=1` (Step 0-b)
4. 「추가 발견」 — `대조가이드=1`(Step 0-c) · 관점 ①②③ 각 1건 이상(Step 0-d, 찾았는데 없으면 「관점 ③: 해당 없음 — 찾아본 곳」으로 1건) · 항목마다 출처·관련 작업 · `git status` 에 학습이론.md 미커밋 변경 없음
5. 45턴이 지나면 미완이어도 멈추고 남은 항목을 보고

### Task 2: 1절 YOLO가 배우는 것
**조사 질문:** ①현행 Ultralytics YOLO 계열(v8·11·26) 검출 구조 개요 ②손실 구성 `box`·`cls`·`dfl` 각각의 역할(YOLO26 에서 dfl 이 L1 로 바뀐 점 포함) ③정답 박스를 예측에 배정하는 방식(task-aligned assigner 등) ④앵커 프리의 의미

### Task 3: 2절 데이터
**조사 질문:** ①클래스당 권장 장수·인스턴스 수(Ultralytics·Roboflow 공식 팁) ②다양성(배경·조명·각도) 권고 ③train/val/test 분할과 **누출**(같은 영상 연속 프레임) ④라벨 품질 — 박스를 물체에 딱 맞게, 빠짐없이 ⑤배경(빈) 이미지 비율 권고
**우리 적용 연결:** 모조↔시연 콘솔 · `tool_v4` 라벨 사고(§10.63 사례)

### Task 4: 3절 입력
**조사 질문:** ①`imgsz` 와 직사각형 입력(`rect`) 처리 ②letterbox vs stretch — 학습·추론 불일치 영향 ③Roboflow 전처리(Resize Stretch/Fit)와 Ultralytics letterbox 의 관계 ④작은 물체 검출 권고(해상도·모델 크기)
**우리 적용 연결:** 480×640 세로 → 640 입력

### Task 5: 4절 증강
**조사 질문:** ①Ultralytics 기본 증강 목록과 기본값(`hsv_h`·`hsv_s`·`hsv_v`·`mosaic`·`fliplr`·`degrees` 등) ②`close_mosaic` ③색이 클래스를 정의할 때 색 증강 위험 ④Roboflow 오프라인 증강과 학습 중 증강의 중복
**우리 적용 연결:** 버튼 = 색 클래스 · 공구 = 형상 클래스

### Task 6: 5절 학습 설정
**조사 질문:** ①전이학습(사전학습 가중치)·`freeze` ②모델 크기 n/s/m 선택 기준 ③`epochs`·`batch`·`lr0`·`optimizer=auto`·`cos_lr`·`warmup` ④`patience` 조기 종료 ⑤오검출(손톱 같은 비슷한 물체) 줄이는 공식 권고 — 배경·하드 네거티브 ⑥T4 에서 `batch`·`amp`·`cache` 제약

### Task 7: 6절 학습 곡선 읽기
**조사 질문:** ①`results.csv` 열(train/val 손실 3종·precision·recall·mAP50·mAP50-95·lr) ②과적합·과소적합을 곡선에서 판별하는 공식 설명 ③`best.pt` 선택 기준(fitness 가중) ④학습 산출물 목록(혼동행렬·PR 곡선·`args.yaml`)

### Task 8: 7절 검증
**조사 질문:** ①mAP50·mAP50-95·precision·recall 정의 ②`conf`·`iou` 임계와 운용점 선택(F1 곡선) ③val 과 별도 test(홀드아웃) 운용 ④클래스별 AP·혼동행렬 해석 ⑤`model.val(split='test')`
**우리 적용 연결:** CLAUDE.md §5 측정 함정(사후 층화·단일 세션·프리라벨 금지)과 대조

### Task 9: 8절 배포(Hailo)
**조사 질문:** ①Hailo Model Zoo 가 지원하는 YOLO 버전·크기(Hailo-8L/8 해당 칩 확인 = 통합문서 §11.1) ②DFC 양자화 흐름·캘리브레이션 이미지 권장 수·`optimization_level` ③ONNX export 옵션(`opset`·`imgsz` 고정) ④양자화 전후 정확도 비교 권고
**우리 적용 연결:** YOLO 버전 선택의 제약

---

### Task 10: 추가 발견 정리 · 최종 점검

- [ ] **Step 0 (원문 대조):** 절 1~8 요점마다 출처 원문 재조회 → 불일치 수정 → 절 끝 `> ✅ 원문 대조 <날짜>`. 커밋.
- [ ] **Step 0-b (교차 확인):** 절마다 핵심 요점에 **독립된 두 번째 출처**(다른 기관·논문). 출처끼리 다르면 요점에 `⚠️ 출처 간 불일치` 표시. 절 끝 `> ✅ 교차 확인 <날짜>`.
- [ ] **Step 0-c (누락 점검):** 공식 학습 가이드 목차(Ultralytics *Model Training Tips*·*Data Collection and Annotation*·*Preprocessing*·*Model Evaluation*·*Hyperparameter Tuning* 가이드, Roboflow 학습 가이드, Hailo DFC 사용자 가이드)를 8절과 대조 → 빠진 주제는 「추가 발견」. 대조한 가이드 목록을 「추가 발견」 머리에 적는다.
- [ ] **Step 0-d (미처 생각 못한 것):** 세 관점으로 찾는다 — ①**실패 사례**: 공식 문서의 흔한 문제·FAQ·GitHub 이슈 중 공식 답변(유지보수자) ②**우리 조건 문헌**: 1인칭(egocentric) 검출 · 손-물체 가림 · 작은 물체 · 세로 입력 · INT8 양자화 정확도 손실 · 비슷한 모양 오검출(하드 네거티브) 논문·공식 자료 ③**거꾸로 질문**: 「시연에서 이 모델이 틀리는 경우」를 나열하고 8절이 각각을 다루는지 점검. 결과는 「추가 발견」에 `(관점 ①/②/③)` 표시. 🔴 GitHub 이슈는 유지보수자 답변만 근거.
- [ ] **Step 1:** 「추가 발견」 절 정리 — 항목마다 출처·관련 작업(②/③/④) 표시.
- [ ] **Step 2 (관문 검증):** `grep -n "미확인" dev/ai_model/학습이론.md` 출력으로 미확인 목록 확정 · 절마다 `### 출처` 가 비어 있지 않은지 확인.
- [ ] **Step 3:** `superpowers:requesting-code-review` — 기준에 사용자 구상 원문(「기억이 아닌 정보 탐색」·「너무 자세하지 않게」·「발견한 조건·데이터 포함」) 인용.
- [ ] **Step 4 (공동 확인):** 전체 요약 보고 → 사용자와 함께 ① 완료 여부 선언. 🔴 Claude 단독 완료 선언 금지.
- [ ] **Step 5:** 🔓 표 재정립 행을 「① 완료 — ② 데이터 확보 설계 다음」으로 갱신, 커밋.

### Task 11: `work-close`

- [ ] 공동 확인 직후 `work-close` 호출 — 작업로그 기록 · push.

## 중단 규칙

- 한 절에서 핵심 조사 질문의 **과반이 1차 출처 없음** → 그 절을 멈추고 사용자에게 출처 범위(블로그 허용 등) 결정 요청.
- 조사 결과가 spec 전제(YOLO·Hailo·Colab)를 흔들면(예: Hailo 가 필요한 YOLO 버전 미지원) → 진행 중단, brainstorming 으로 돌아가 재설계.
