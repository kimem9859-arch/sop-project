# 촬영 도구 정비 + 순수 촬영 성능 기준선 측정 실행 계획

> **상태** — 🛑 **중단(2026-09-22)** — Task 1~5 완료(도구 3종 · 기준선 + 해상도·화질 스윕 5조합×3회 · §12.64 기록). **Task 6(추론 포함 3회 재측정)·Task 7(마무리)은 하지 않았다** — 사용자가 「촬영 성능 개선」을 새 작업으로 돌리기로 했다(2026-09-22). 펌웨어는 **VGA·q15 원복 완료**. 재개하려면 Task 6 부터.
> ⚠️ **아래 체크박스는 실행 중 갱신하지 않는다** — 진행 상태는 이 줄과 `docs/작업로그.md` 가 정본이다(이 저장소 관례).
> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:executing-plans` 인라인 실행. 실HW(ESP32·카메라)가 직렬이라 병렬성이 없다.

**Goal:** 데이터셋 촬영 전용 도구를 만들고, **모델 추론 없이 순수 촬영만 할 때의 성능 기준선**을 단계별로 재어 정본에 남긴다.

**Architecture:** 촬영 도구를 새로 만들되(`capture_dataset.py`), 이미 있는 벤치(`bench_detector.py`)는 **비교 가능하도록 보강만** 한다. 두 도구가 같은 전처리를 쓰도록 왜곡보정 계산을 `frame_orient` 로 모은다. 기준선은 「파이가 어디서 느려지는가」를 단계별로 나눠 재고, ESP32 가 시리얼로 내보내는 송신 통계를 함께 모아 **카메라·전송·파이**를 가른다.

**Tech Stack:** 파이 `python3` · OpenCV · NumPy · pyserial(ESP32 통계 수집) · 기존 `frame_orient`·`config`

**Spec:** `docs/superpowers/specs/2026-09-16-비전모델-데이터확보-design.md` §8 「저장 = … `bench_detector` 는 왜곡보정이 없어 **촬영 도구를 먼저 고친다**」

## Global Constraints

- **전처리 순서는 런타임과 같다** — 수신 → **반전 → 왜곡보정 → 회전** → 사용(`frame_orient` 머리말). 🔴 회전을 먼저 하면 480×640 이 되어 캘리브레이션이 'mismatch' 로 빠지고 **왜곡보정이 조용히 꺼진다**
- **펌웨어는 이번에 건드리지 않는다** — VGA 640×480 · `jpeg_quality=15` 유지. 화질·해상도 변경은 기준선을 본 뒤 별도 결정(2026-09-21 사용자 ⓒ 선택)
- **저장은 무손실 PNG** — JPEG·mp4 금지(B4 파란 스티커는 손실 압축만으로 사라진다)
- **도구는 임계·경로를 `config` 에서 읽는다** — 기본값을 새로 정하지 않는다(도구 기본값이 config 를 안 따라 4번 물렸다)
- **측정 수치를 이 문서에 쓰지 않는다** — 결과 정본은 통합문서 §12
- **ESP32 는 시리얼번호로 식별한다** — 메인 `3C:0F:02:DD:5E:58`. `ttyACM` 번호는 꽂는 순서로 뒤바뀐다(2026-09-21 실제로 뒤집혀 잡혔다)
- **이번 작업에서 펌웨어를 굽지 않는다** — 굽기가 필요해지면 멈추고 사람에게 확인받는다

## 관문 3개

- 🔴 **중단 규칙** — 다음 중 하나라도 나오면 그 자리에서 멈추고 보고한다.
  1. ESP32 IP 가 `10.47.16.x`(폰 핫스팟) — 측정값이 무의미해진다
  2. 실행 로그에 `[캘리브레이션] 해상도 불일치` 가 뜬다 — 왜곡보정이 꺼진 채 재는 것이다
  3. ESP32 시리얼의 `drop` 이 `cap` 의 10% 를 넘는다 — 원인을 가르기 전에는 다음 단계로 가지 않는다
- 🔒 **홀드아웃** — 기준선 측정 회차의 산출물(PNG)은 **학습·평가에 쓰지 않는다.** 측정 전용이며, 끝나면 지운다. 섞이면 「측정용으로 찍은 정지 장면」이 학습 데이터에 들어간다
- 🔒 **검증 관문** — 태스크마다 ①자가시험 통과 ②**실기동 확인**(import 검사로 못 잡는 런타임 오류가 있다) ③기존 도구 회귀(`tool_probe --save-clean` 이 그대로 도는지)

## 파일 구조

| 파일 | 책임 | 변경 |
|---|---|---|
| `Rpi5/Demo/frame_orient.py` | 방향·왜곡 보정의 **단일 출처** | 왜곡보정 함수 추가 |
| `Rpi5/Demo/test/tool_probe.py` | 공구 판정 점검 | 자체 사본 제거, 위 모듈 사용 |
| `Rpi5/Demo/test/bench_detector.py` | **추론 포함** 성능 측정 | 비교 가능하도록 보강 |
| `Rpi5/Demo/test/capture_dataset.py` | **촬영 전용** + 순수 촬영 성능 측정 | 새로 만듦 |

---

### Task 1: 왜곡보정을 `frame_orient` 로 모은다 `[Claude]`

**왜** — 같은 계산이 지금 `camera_thread`(런타임)와 `tool_probe`(도구) 두 곳에 있다. 새 도구가 세 번째 사본을 만들면 **한쪽이 어긋나는 것은 시간 문제**다. `frame_orient` 머리말이 이미 「반전 → (왜곡보정) → 회전」 순서를 규정하고 있어 그 자리가 맞다.

**Files:**
- Modify: `Rpi5/Demo/frame_orient.py`
- Modify: `Rpi5/Demo/test/tool_probe.py:165~180`(자체 `_undistort_map` 제거)

**Interfaces:**
- Produces: `frame_orient.undistort_map(w, h) -> tuple | None` · `frame_orient.undistort(frame, maps) -> frame` · `frame_orient.apply_full(frame, maps) -> frame`

- [ ] **Step 1: 자가시험을 먼저 쓴다** — `frame_orient.py` 끝에 추가

```python
def _selftest() -> int:
    """보정 순서와 맵 해상도 판정이 규약대로인지 확인한다."""
    import numpy as np
    f = np.zeros((480, 640, 3), np.uint8)
    f[0:10, :] = 255                       # 위쪽에 흰 띠 — 반전 여부를 눈금으로 삼는다
    maps = undistort_map(640, 480)
    assert maps is not None, "640×480 캘리브레이션 맵을 만들지 못했다"
    assert undistort_map(800, 600) is None, "해상도가 다르면 None 이어야 한다"
    out = apply_full(f, maps)
    exp = (640, 480) if config.CAMERA_ROTATE_CCW90 else (480, 640)
    assert (out.shape[1], out.shape[0]) == exp, out.shape
    print("selftest OK")
    return 0
```

- [ ] **Step 2: 실행해 실패를 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 -c "import frame_orient; frame_orient._selftest()"`
Expected: FAIL — `AttributeError: module 'frame_orient' has no attribute 'undistort_map'`

- [ ] **Step 3: 구현** — `frame_orient.py` 의 `apply()` 아래에 추가. `import os`·`import numpy as np` 를 머리에 더한다

```python
def undistort_map(w, h):
    """렌즈 왜곡 보정 맵. 캘리브레이션 파일이 없거나 해상도가 다르면 None.

    🔴 None 을 받으면 **보정 없이 진행하지 말고 경고를 띄운다** — 조용히 꺼지는 것이
       이 함정의 본질이다(로그 한 줄만 남고 화면은 멀쩡해 보인다).
    """
    path = config.YOLO_CALIBRATION_PATH
    if not os.path.exists(path):
        return None
    data = np.load(path)
    if "image_size" in data:
        iw, ih = int(data["image_size"][0]), int(data["image_size"][1])
        if (iw, ih) != (w, h):
            return None
    cam_mat, dist = data["camera_matrix"], data["dist_coeffs"]
    new_mat, _ = cv2.getOptimalNewCameraMatrix(cam_mat, dist, (w, h),
                                               config.CALIB_ALPHA, (w, h))
    return cv2.initUndistortRectifyMap(cam_mat, dist, None, new_mat, (w, h), cv2.CV_16SC2)


def undistort(frame, maps):
    """맵이 None 이면 원본을 그대로 돌려준다(호출부가 경고를 책임진다)."""
    if maps is None:
        return frame
    return cv2.remap(frame, maps[0], maps[1], cv2.INTER_LINEAR)


def apply_full(frame, maps):
    """런타임과 같은 전체 순서 — 반전 → 왜곡보정 → 회전."""
    return rotate(undistort(flip(frame), maps))
```

- [ ] **Step 4: 자가시험 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 -c "import frame_orient; frame_orient._selftest()"`
Expected: `selftest OK`

- [ ] **Step 5: `tool_probe` 가 새 함수를 쓰도록 바꾼다** — `_undistort_map` 정의(165~180행)를 지우고, 호출부 `umap = _undistort_map(w0, h0) or False` 를 `umap = frame_orient.undistort_map(w0, h0) or False` 로. 🔑 `or False` 관용구는 **그대로 둔다** — 「한 번 시도했고 없더라」를 뜻하는 기존 표시다

- [ ] **Step 6: 회귀 확인 — `tool_probe` 가 그대로 뜨는지**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 test/tool_probe.py --help`
Expected: 사용법이 출력되고 `--save-clean` 항목이 보인다(import 오류 없음)

- [ ] **Step 7: 커밋**

```bash
cd /home/pi/sop-project/Rpi5
git add Demo/frame_orient.py Demo/test/tool_probe.py
git commit -m "refactor(frame_orient): 왜곡보정을 단일 출처로 — tool_probe 사본 제거"
```

---

### Task 2: `bench_detector` 보강 — 비교 가능하게 `[Claude]`

**왜** — 2026-09-21 검토에서 넷이 나왔다. ①왜곡보정 없음 ②화면 표시 비용이 FPS 에 **항상** 섞이고 `--no-video` 로도 못 끈다 ③수신·버림 장수를 세지 않는다 ④`perf_log.csv` 첫 줄에 `fps=0.00` 이 남는다.
🔴 **②를 고치지 않으면 새 도구와의 비교가 성립하지 않는다** — 새 도구는 측정 시 화면을 끄기 때문이다.

**Files:**
- Modify: `Rpi5/Demo/test/bench_detector.py`

**Interfaces:**
- Consumes: `frame_orient.undistort_map`·`frame_orient.undistort`(Task 1)
- Produces: CLI `--headless`·`--undistort` · 요약에 `수신/처리/버림` 3줄

- [ ] **Step 1: 옵션 두 개 추가**(`add_argument` 묶음 끝, 852행 부근)

```python
    parser.add_argument("--headless", action="store_true",
                        help="화면 표시·오버레이 그리기를 끈다. 🔑 FPS 를 다른 도구와 비교할 때 "
                             "반드시 켠다 — 끄지 않으면 imshow·waitKey(1) 비용이 FPS 에 섞인다")
    parser.add_argument("--undistort", action="store_true",
                        help="런타임과 같은 왜곡보정을 넣는다(기본 꺼짐). "
                             "🔴 켜면 옛 측정값과 직접 비교할 수 없다 — 입력 그림이 달라진다")
```

- [ ] **Step 2: 왜곡보정을 전처리에 끼운다** — `_apply_flip`(271~276행)을 아래로 교체

```python
    # 🔑 맵은 **첫 프레임의 실제 크기**로 만든다. config 에 해상도 상수가 없고
    #    (CAMERA_WIDTH/HEIGHT 는 존재하지 않는다), 센서 설정이 바뀌어도 따라가야 한다.
    umap_box = [None]          # None=아직 시도 안 함 / False=없음 / 맵
    def _apply_flip(f):
        if flip_mode == "v":  f = cv2.flip(f, 0)
        elif flip_mode == "h":  f = cv2.flip(f, 1)
        elif flip_mode == "vh": f = cv2.flip(f, -1)
        # 🔴 반전 → 왜곡보정 → 회전. 런타임(`frame_orient.apply_full`)과 같은 순서다.
        if args.undistort and source == "esp32":
            if umap_box[0] is None:
                h0, w0 = f.shape[:2]
                umap_box[0] = frame_orient.undistort_map(w0, h0) or False
                if umap_box[0] is False:
                    print(f"🔴 [왜곡보정] {w0}×{h0} 용 캘리브레이션 맵 없음 — 보정 없이 진행한다")
            if umap_box[0]:
                f = frame_orient.undistort(f, umap_box[0])
        return frame_orient.rotate(f) if source == "esp32" else f
```

- [ ] **Step 3: 화면 표시를 `--headless` 로 끈다** — 678~692행을 아래로 교체

```python
            # 영상 + 실시간 미리보기
            # 🔑 headless 면 오버레이도 그리지 않는다 — frame.copy() 와 그리기 자체가 비용이다.
            if not args.headless:
                frame_draw = _draw_detections(frame.copy(), tracks, fps, frame_no)
                if fingertip is not None:
                    cv2.circle(frame_draw, fingertip, 10, (255, 0, 255), -1)
                    cv2.circle(frame_draw, fingertip, 12, (255, 255, 255), 2)
                if not args.no_video and video_queue is not None:
                    try:
                        video_queue.put_nowait((frame_draw.copy(), video_path))
                    except Exception:
                        pass  # 큐 가득 찬 경우 드롭
                if frame_no % 2 == 0:
                    cv2.imshow("bench_detector", frame_draw)
                if cv2.waitKey(1) & 0xFF == ord("q"):
                    print("\n[사용자 중단]")
                    break
```

🔴 **`--headless` 는 `--no-video` 를 함축한다** — 그리지 않은 화면은 저장할 수 없다. Step 5 에서 그 조합을 막는다.

- [ ] **Step 4: 수신·버림 장수를 센다** — 수신 스레드(468~476행)와 요약에 카운터를 더한다

```python
    # (상태 변수 옆에 추가)
    recv_count    = [0]     # 소켓에서 실제로 꺼낸 장수
    stale_skipped = [0]     # 처리 전에 새 프레임으로 덮여 버려진 장수

    # (recv_worker 안, latest_raw 대입 직전)
                with raw_lock:
                    if latest_raw[0] is not None:
                        stale_skipped[0] += 1     # 아직 안 쓴 것을 덮어쓴다 = 버림
                    latest_raw[0] = data
                recv_count[0] += 1

    # (본 루프에서 data 를 꺼낸 직후 — 꺼냈으면 비워서 중복 처리를 막는다)
                with raw_lock:
                    data = latest_raw[0]
                    latest_raw[0] = None
```

🔑 **꺼낼 때 `None` 으로 비우는 것이 중복 처리도 같이 막는다** — 신호에 순서표가 없어 같은 프레임을 두 번 집을 수 있는 틈이 있었다(확률은 낮았다).

- [ ] **Step 5: 요약에 세 줄을 더하고, 모순된 옵션 조합을 막는다**

```python
    # (main() 에서 parse_args 직후)
    if args.headless:
        args.no_video = True        # 그리지 않은 화면은 저장할 수 없다

    # (요약의 FPS 세 줄 아래)
    if source == "esp32":
        print(f"수신 프레임 : {recv_count[0]}")
        print(f"처리 프레임 : {frame_no}")
        print(f"버린 프레임 : {stale_skipped[0]}  ← 처리보다 수신이 빨라 덮인 장수")
```

- [ ] **Step 6: `perf_log.csv` 첫 줄의 `fps=0.00` 을 남기지 않는다** — 670행을 아래로

```python
            # 🔑 첫 프레임은 직전 시각이 없어 FPS 를 계산할 수 없다. 0.00 을 적으면
            #    그 CSV 를 평균 내는 새 분석이 값을 낮게 본다(지금 소비자 2곳은 각자 거르고 있다).
            if prev_time is not None or frame_no > 1:
                perf_w.writerow([frame_no, now_str, f"{fps:.2f}", f"{infer_ms:.2f}", len(tracks)])
```

- [ ] **Step 7: 구문·기동 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 -m py_compile test/bench_detector.py && python3 test/bench_detector.py --help | grep -E "headless|undistort"`
Expected: 두 옵션이 도움말에 보인다

- [ ] **Step 8: 커밋**

```bash
cd /home/pi/sop-project/Rpi5
git add Demo/test/bench_detector.py
git commit -m "feat(bench): --headless·--undistort 추가, 수신/버림 계수, perf CSV 첫 행 정정"
```

---

### Task 3: `capture_dataset.py` — 촬영 모드 `[Claude]`

**Files:**
- Create: `Rpi5/Demo/test/capture_dataset.py`

**Interfaces:**
- Consumes: `frame_orient.apply_full`·`undistort_map`(Task 1) · `config.CAMERA_TCP_HOST`·`CAMERA_TCP_PORT`
- Produces: `~/data/capture/<시각>_<장소>_<장면>/f00001.png …` + `session.json`

- [ ] **Step 1: 자가시험을 먼저 쓴다** — 합성 프레임으로 저장 규칙을 확인

```python
def _selftest() -> int:
    """저장 폴더 이름·PNG 무손실·session.json 필수 키를 확인한다."""
    import tempfile, json as _json
    import numpy as np
    with tempfile.TemporaryDirectory() as d:
        sess = Session(Path(d), place="장소1", scene="배경", every=0.0,
                       meta={"frame_size": "VGA", "jpeg_quality": 15})
        img = np.full((640, 480, 3), 7, np.uint8)
        sess.save(img); sess.save(img)
        sess.close(stages={"recv": 2, "decode": 2, "orient": 2, "saved": 2})
        files = sorted(p.name for p in sess.dir.glob("f*.png"))
        assert files == ["f00001.png", "f00002.png"], files
        back = cv2.imread(str(sess.dir / "f00001.png"))
        assert (back == 7).all(), "PNG 가 무손실이 아니다"
        meta = _json.loads((sess.dir / "session.json").read_text(encoding="utf-8"))
        for k in ("place", "scene", "frames", "stages", "frame_size", "jpeg_quality"):
            assert k in meta, k
    print("selftest OK")
    return 0
```

- [ ] **Step 2: 실행해 실패를 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 test/capture_dataset.py --selftest`
Expected: FAIL — `NameError: name 'Session' is not defined`

- [ ] **Step 3: 구현** — 머리말에 담을 것: 왜 전용 도구인가(검출이 필요 없다) · 🔴 전처리 순서 · 🔴 무손실 PNG · 장면 목록의 출처(설계서 §5)

```python
from __future__ import annotations
import argparse, json, os, re, socket, struct, sys, threading, time
from datetime import datetime
from pathlib import Path

import cv2
import numpy as np

_DEMO = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _DEMO)
import config
import frame_orient

# 설계서 §5 촬영 장면 8종 — 화면에 띄워 그 자체가 촬영 지시서가 되게 한다.
SCENES = ["1 콘솔전체", "2 버튼누르기", "3 손지나감", "4 머리움직임",
          "5 빛반사", "6 놓인공구", "7 쥔공구", "8 배경"]
PLACES = ["장소1", "장소2", "장소3"]


class Session:
    """한 회차의 저장 폴더와 메타를 들고 있는다."""

    def __init__(self, root: Path, place: str, scene: str, every: float, meta: dict):
        stamp = datetime.now().strftime("%Y%m%d_%H%M%S")
        self.dir = root / f"{stamp}_{place}_{scene.split()[0]}"
        self.dir.mkdir(parents=True, exist_ok=True)
        self.place, self.scene, self.every = place, scene, every
        self.meta = dict(meta)
        self.n = 0
        self.started = time.time()

    def save(self, img) -> None:
        self.n += 1
        cv2.imwrite(str(self.dir / f"f{self.n:05d}.png"), img,
                    [cv2.IMWRITE_PNG_COMPRESSION, 3])

    def close(self, stages: dict) -> None:
        self.meta.update({
            "place": self.place, "scene": self.scene, "every_sec": self.every,
            "frames": self.n, "seconds": round(time.time() - self.started, 1),
            "stages": stages,
        })
        (self.dir / "session.json").write_text(
            json.dumps(self.meta, ensure_ascii=False, indent=1), encoding="utf-8")
```

- [ ] **Step 4: 수신·미리보기·키 조작을 붙인다**

```python
def _recv_exact(sock, n):
    buf = b""
    while len(buf) < n:
        try:
            chunk = sock.recv(n - len(buf))
        except (socket.timeout, OSError):
            return None
        if not chunk:
            return None
        buf += chunk
    return buf


def _recv_frame(sock):
    head = _recv_exact(sock, 4)
    if head is None:
        return None
    ln = struct.unpack("<I", head)[0]
    if ln == 0 or ln > config.TCP_MAX_FRAME_BYTES:
        return None
    return _recv_exact(sock, ln)


def run(args) -> int:
    host = args.host or config.CAMERA_TCP_HOST
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.setsockopt(socket.IPPROTO_TCP, socket.TCP_NODELAY, 1)
    sock.settimeout(config.TCP_RECV_TIMEOUT_SEC)
    sock.connect((host, config.CAMERA_TCP_PORT))
    # 🔴 폰 핫스팟 고착은 조용한 고장이다 — 영상도 GUI 도 정상으로 보이고 FPS 만 떨어진다.
    if host.startswith("10.47."):
        print(f"🔴 [경고] ESP32 가 폰 핫스팟에 붙어 있다({host}). ESP32 만 전원 재투입할 것.")

    # 🔑 맵은 **첫 프레임의 실제 크기**로 만든다 — config 에 해상도 상수가 없다.
    umap, umap_ready = None, False
    root = Path(os.path.expanduser(args.out))
    place_i, scene_i = 0, 0
    sess, last_save = None, 0.0
    stages = {"recv": 0, "decode": 0, "orient": 0, "saved": 0}
    print("space=녹화 시작/정지  s=장면  p=장소  q=종료")
    while True:
        data = _recv_frame(sock)
        if data is None:
            print("[수신 끊김] 종료한다.")
            break
        stages["recv"] += 1
        frame = cv2.imdecode(np.frombuffer(data, np.uint8), cv2.IMREAD_COLOR)
        if frame is None:
            continue
        stages["decode"] += 1
        if not umap_ready:
            h0, w0 = frame.shape[:2]
            umap, umap_ready = frame_orient.undistort_map(w0, h0), True
            if umap is None:
                print(f"🔴 [왜곡보정] {w0}×{h0} 용 맵 없음 — 런타임과 다른 그림이 된다.")
                if not args.force:
                    print("   --force 를 주면 그래도 진행한다. 지금은 멈춘다.")
                    return 1
        frame = frame_orient.apply_full(frame, umap)
        stages["orient"] += 1

        if sess is not None and (time.time() - last_save) >= args.every:
            sess.save(frame)
            stages["saved"] += 1
            last_save = time.time()

        view = frame.copy()
        state = f"REC {sess.n}" if sess else "대기"
        cv2.putText(view, f"{PLACES[place_i]} | {SCENES[scene_i]} | {state}",
                    (8, 22), cv2.FONT_HERSHEY_SIMPLEX, 0.55, (0, 255, 255), 2)
        cv2.imshow("capture_dataset", view)
        k = cv2.waitKey(1) & 0xFF
        if k == ord("q"):
            break
        if k == ord(" "):
            if sess is None:
                sess = Session(root, PLACES[place_i], SCENES[scene_i], args.every,
                               {"host": host, "frame_size": "VGA",
                                "jpeg_quality": 15, "undistort": umap is not None})
                last_save = 0.0
                print(f"[녹화 시작] {sess.dir}")
            else:
                sess.close(stages); print(f"[녹화 정지] {sess.n}장 → {sess.dir}"); sess = None
        if k == ord("s") and sess is None:
            scene_i = (scene_i + 1) % len(SCENES)
        if k == ord("p") and sess is None:
            place_i = (place_i + 1) % len(PLACES)

    if sess is not None:
        sess.close(stages)
    sock.close(); cv2.destroyAllWindows()
    return 0
```

🔑 **장면·장소는 녹화 중에 못 바꾼다**(`sess is None` 조건) — 한 폴더에 두 조건이 섞이면 나중에 가를 수 없다.

- [ ] **Step 5: CLI 를 붙인다**

```python
def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--host", default=None, help="ESP32 IP(기본: config)")
    ap.add_argument("--out", default="~/data/capture", help="저장 뿌리")
    ap.add_argument("--every", type=float, default=0.2,
                    help="저장 간격(초). 기본 0.2 = 초당 5장 — 시연영상 추출과 같은 밀도")
    ap.add_argument("--force", action="store_true", help="왜곡보정 없이도 진행")
    ap.add_argument("--selftest", action="store_true")
    a = ap.parse_args()
    return _selftest() if a.selftest else run(a)


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 6: 자가시험 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 test/capture_dataset.py --selftest`
Expected: `selftest OK`

- [ ] **Step 7 (🔒 검증 관문): 실기동** — 메인 ESP32 를 켜고 실제로 띄운다

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 test/capture_dataset.py --every 0.5`
확인할 것: ①창이 뜨고 영상이 보인다 ②`space` 로 녹화가 시작·정지된다 ③`s`·`p` 로 장면·장소가 바뀐다 ④폴더에 PNG 와 `session.json` 이 생긴다 ⑤저장된 PNG 를 열어 **480×640 세로**인지 확인
🔴 **여기서 멈추면 안 된다** — import 검사로 못 잡는 런타임 오류가 이 단계에서만 드러난 전례가 있다

- [ ] **Step 8: 커밋**

```bash
cd /home/pi/sop-project/Rpi5
git add Demo/test/capture_dataset.py
git commit -m "feat(test): capture_dataset — 데이터셋 촬영 전용(검출 없음·런타임 동일 보정)"
```

---

### Task 4: `capture_dataset` 에 기준선 측정 모드 `[Claude]`

**왜** — 「어디서 느려지는가」는 단계를 나눠 재야 답이 나온다. 한 덩어리로 재면 「느리다」만 알고 고칠 곳은 모른다.

**Files:**
- Modify: `Rpi5/Demo/test/capture_dataset.py`

**Interfaces:**
- Produces: `--bench <초>` 실행 시 표준출력 요약 + `bench.json`

- [ ] **Step 1: ESP32 시리얼 통계 수집기를 더한다** — 펌웨어가 **5초마다 자동으로** 찍는 줄을 그대로 받아 적는다

```python
_FRAME_RE = re.compile(r"Frame: cap=(\d+) sent=(\d+) drop=(\d+) · ([\d.]+)KB/장 · "
                       r"send avg=(\d+)ms max=(\d+)ms")


def _esp_stats(port: str, stop: threading.Event, out: list) -> None:
    """ESP32 시리얼에서 송신 통계를 모은다. USB 가 꽂혀 있을 때만 쓴다.

    🔑 cap 과 sent 의 차이가 핵심이다 — 잡았는데 못 보냈으면 전송이, 애초에 적게
       잡았으면 카메라·펌웨어가 병목이다.
    """
    try:
        import serial
    except ImportError:
        print("[ESP32] pyserial 없음 — 송신 통계 생략"); return
    try:
        s = serial.Serial(port, 115200, timeout=1)
    except Exception as e:
        print(f"[ESP32] 시리얼 열기 실패({port}): {e} — 송신 통계 생략"); return
    while not stop.is_set():
        line = s.readline().decode("utf-8", "replace")
        m = _FRAME_RE.search(line)
        if m:
            out.append({"cap": int(m.group(1)), "sent": int(m.group(2)),
                        "drop": int(m.group(3)), "kb": float(m.group(4)),
                        "send_avg_ms": int(m.group(5)), "send_max_ms": int(m.group(6))})
    s.close()
```

- [ ] **Step 2: 측정 루프를 더한다** — 저장 유무를 `--no-save` 로 가른다

```python
def run_bench(args) -> int:
    host = args.host or config.CAMERA_TCP_HOST
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.setsockopt(socket.IPPROTO_TCP, socket.TCP_NODELAY, 1)
    sock.settimeout(config.TCP_RECV_TIMEOUT_SEC)
    sock.connect((host, config.CAMERA_TCP_PORT))
    umap, umap_ready = None, False      # 첫 프레임 크기로 만든다(Task 3 과 같은 이유)
    esp, stop = [], threading.Event()
    if args.esp_port:
        threading.Thread(target=_esp_stats, args=(args.esp_port, stop, esp), daemon=True).start()

    out = Path(os.path.expanduser(args.out)) / f"bench_{datetime.now():%Y%m%d_%H%M%S}"
    out.mkdir(parents=True, exist_ok=True)
    t_end = time.time() + args.bench
    n = {"recv": 0, "decode": 0, "orient": 0, "saved": 0}
    ms = {"decode": [], "orient": [], "save": []}
    bytes_total = 0
    t0 = time.time()
    while time.time() < t_end:
        data = _recv_frame(sock)
        if data is None:
            break
        n["recv"] += 1; bytes_total += len(data)
        a = time.perf_counter()
        frame = cv2.imdecode(np.frombuffer(data, np.uint8), cv2.IMREAD_COLOR)
        if frame is None:
            continue
        b = time.perf_counter(); n["decode"] += 1
        if not umap_ready:
            h0, w0 = frame.shape[:2]
            umap, umap_ready = frame_orient.undistort_map(w0, h0), True
            if umap is None:
                print(f"🔴 [왜곡보정] {w0}×{h0} 용 맵 없음 — 보정 없이 잰다(조건에 기록된다)")
        frame = frame_orient.apply_full(frame, umap)
        c = time.perf_counter(); n["orient"] += 1
        ms["decode"].append((b - a) * 1000); ms["orient"].append((c - b) * 1000)
        if not args.no_save:
            cv2.imwrite(str(out / f"f{n['saved']+1:05d}.png"), frame,
                        [cv2.IMWRITE_PNG_COMPRESSION, 3])
            ms["save"].append((time.perf_counter() - c) * 1000); n["saved"] += 1
    stop.set()
    dur = time.time() - t0
    sock.close()

    avg = lambda xs: (sum(xs) / len(xs)) if xs else 0.0
    res = {
        "seconds": round(dur, 1), "no_save": bool(args.no_save),
        "counts": n,
        "fps": {k: round(v / dur, 2) for k, v in n.items()},
        "ms_avg": {k: round(avg(v), 2) for k, v in ms.items()},
        "kbps": round(bytes_total / 1024 / dur, 1),
        "esp32": esp,
        "frame_size": "VGA", "jpeg_quality": 15, "undistort": umap is not None,
    }
    (out / "bench.json").write_text(json.dumps(res, ensure_ascii=False, indent=1),
                                    encoding="utf-8")
    print(json.dumps(res, ensure_ascii=False, indent=1))
    print(f"\n결과 → {out/'bench.json'}")
    return 0
```

- [ ] **Step 3: CLI 에 세 옵션을 더한다**

```python
    ap.add_argument("--bench", type=float, default=0.0, metavar="초",
                    help="지정 시 촬영 대신 성능 측정만 한다(화면 없음)")
    ap.add_argument("--no-save", action="store_true",
                    help="--bench 에서 PNG 저장을 뺀다 — 저장 비용을 분리해 보려고")
    ap.add_argument("--esp-port", default=None, metavar="/dev/ttyACMx",
                    help="ESP32 시리얼 포트. 주면 송신 통계(cap/sent/drop)를 함께 모은다")
```

`main()` 의 분기도 바꾼다: `return _selftest() if a.selftest else (run_bench(a) if a.bench > 0 else run(a))`

- [ ] **Step 4: 짧게 돌려 형태를 확인**(🔒 검증 관문)

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 test/capture_dataset.py --bench 10 --no-save`
Expected: `counts`·`fps`·`ms_avg`·`kbps` 가 채워진 JSON 이 나온다. `recv` 가 0 이면 **중단 규칙 1·3** 을 먼저 확인한다

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5
git add Demo/test/capture_dataset.py
git commit -m "feat(test): capture_dataset --bench — 단계별 촬영 성능 + ESP32 송신 통계"
```

---

### Task 5: 기준선 측정 실행 `[사용자 + Claude]`

**왜** — 기준선이 없으면 나중에 화질을 올렸을 때 「좋아졌다」를 말할 수 없다.

**준비 `[사용자]`** — ①메인 ESP32(`3C:0F:02:DD:5E:58`)를 **USB 로 파이에 연결**(시리얼 통계용) ②콘솔이 보이는 자리에 **고정**(움직이면 회차 간 비교가 안 된다) ③조명 = 일반 실내 형광등

- [ ] **Step 1 `[Claude]`: 포트를 시리얼번호로 확인한다**

```bash
python3 -c "
from serial.tools import list_ports
for p in list_ports.comports():
    if p.serial_number == '3C:0F:02:DD:5E:58': print('메인 =', p.device)
"
```
🔴 `ttyACM` 번호를 믿지 말 것 — 꽂는 순서로 뒤바뀐다

- [ ] **Step 2 `[Claude]`: 저장 포함 60초**

Run: `python3 test/capture_dataset.py --bench 60 --esp-port <위에서 나온 포트>`

- [ ] **Step 3 `[Claude]`: 저장 없이 60초**(저장 비용 분리)

Run: `python3 test/capture_dataset.py --bench 60 --no-save --esp-port <같은 포트>`

- [ ] **Step 4 `[Claude]`: 중단 규칙 점검** — `esp32` 배열의 `drop`/`cap` 비율이 10% 를 넘거나, 연결 IP 가 `10.47.16.x` 면 **멈추고 보고한다**

- [ ] **Step 5 `[Claude]`: 두 회차를 표로 보고한다** — 단계별 fps · 평균 ms · 대역폭 · ESP32 cap/sent/drop. 🔴 **측정값을 이 계획서에 쓰지 않는다**(정본은 통합문서 §12)

---

### Task 6: 추론 포함 비교 측정 `[Claude]`

**왜** — 「모델이 가져가는 몫」은 **같은 조건**으로 재야 나온다. 화면을 한쪽만 켜면 그 차이가 모델 비용으로 잘못 잡힌다.

- [ ] **Step 1: 같은 자리·같은 조명에서 추론 포함 측정**

Run: `python3 test/bench_detector.py --frames 300 --headless --undistort --condition baseline-compare`
🔑 `--headless` 와 `--undistort` 를 **둘 다** 준다 — Task 5 와 조건을 맞추기 위해서다(새 도구는 화면이 없고 왜곡보정을 한다)

- [ ] **Step 2: 손 검출까지 포함한 회차도 잰다**

Run: `python3 test/bench_detector.py --frames 300 --headless --undistort --hand --condition baseline-compare-hoi`

- [ ] **Step 3: 세 회차를 나란히 보고한다** — 순수 촬영(Task 5) / +버튼 추론 / +손 검출. **어느 단계에서 얼마가 빠지는지**를 표로

---

### Task 7: 정본 기록 + 마무리 `[Claude + 사용자]`

- [ ] **Step 1: 측정 절을 쓴다** — `docs/성능검증-저널.md` 에 새 `### 12.N` 절. 제목은 평문, 제목 다음 빈 줄 뒤 `> ✅ **CURRENT-AS-OF 2026-09-21**`. 조건을 반드시 병기 — **펌웨어 VGA·q15 · 유선(USB 연결) · 고정 구도 · 형광등**
- [ ] **Step 2: 통합문서 세 곳을 함께 고친다** — §12 갈래 색인 · `[CURRENT]` 표 · 🔓 미결 표(화질·해상도 상향 판단이 남았다면)
- [ ] **Step 3: 자가 점검 5종을 돌린다**(`work-close` 2.5). 하나라도 ❌ 면 커밋하지 않는다
- [ ] **Step 4 `[Claude + 사용자]`: 공동 확인** — 기준선 수치와 「다음에 무엇을 바꿀지」(jpeg_quality·해상도)를 함께 정한다
- [ ] **Step 5: `work-close` 호출** — 정본 → 작업로그 → push

---

## 이 계획에서 하지 않는 것

- **펌웨어 수정**(`jpeg_quality` 낮추기 · 해상도 올리기) — 기준선을 본 뒤 별도 결정. 해상도를 올리면 **재캘리브레이션(체스보드 20장)** 이 반드시 따라온다
- **재캘리브레이션** — 위와 같은 이유
- **예비 ESP32 활용** — 카메라 달린 확장보드가 없어 지금은 촬영에 쓸 수 없다(펌웨어가 카메라를 필수로 요구해 부팅 루프 중)
- **`bench_detector` 의 중복 프레임 틈** — 확률이 매우 낮아 Task 2 Step 4 의 「꺼낼 때 비우기」로 부수적으로만 닫는다
- **USB 웹캠 경로**(`CAP_V4L2` 미지정) — 런타임도 같은 방식이라 불일치가 아니다. 웹캠이 없어 실측도 못 한다
