# 실제 시연 프로그램 문제 수정 ④-A 코어(카메라 · 공구 판정 · 인터락 · 음성) Implementation Plan

> 상태: ✅ **완료(2026-09-26 · 세션 `baa57676`→`4c6a495c` · 공동 확인 「작업 마무리 진행」 · 최종 리뷰는 ④-B 끝에서 ④ 전체로)** — Rpi5 `2673e6a`..`672603d` · Expected 19/19(실제 저장소) · 다음 = ④-B · 📝 작성(2026-09-26 · 세션 `baa57676`) · 🔑 계획서의 교체·추가 지시와 Run 명령을 **새 복사본에서 Task 0~9 그대로 실행**해 아래 Expected 를 얻었다(저장소 미반영 · 새 복사본 두 번 더 돌려 19/19 일치) · 드라이런이 잡은 계획 결함 = 기동 안내 문구를 바꾸면 `fsm_sim` 출력이 달라져 관문 비교가 깨진다 → 글자 유지 · 시험 도우미가 새 함수만 바꿔 끼우면 고치기 전 기존 인터락 시험이 깨진다 → 옛 함수도 · 다음 = ④-B GUI 계획서(마지막 태스크 = `work-close`)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 카메라 수신·처리의 연결 끊김 연쇄(C12·C19)와 체류 시각의 출처(C16), 공구 워커 경쟁·사망(C13·C14), 인터락 늦은 ACK·포트 안내(C15·M-4·M-5·M-6·M-10), 음성비서 재생 파일·확인 밀림·설명(C20·M-2·M-3)을 고친다.

**Architecture:** 카메라 수신 스레드는 **자기 연결의 소켓과 번호**를 받아 그 연결일 때만 알리고, 프레임을 다 받은 시각을 프레임과 함께 넘긴다 → `roi_signal` 이 그 시각을 싣고 콘솔이 그대로 판정기에 넘긴다. 프레임 처리 오류는 그 프레임만 버린다. 공구 게이트는 잠금 하나로 두 스레드를 직렬화하고 워커 생존을 본다. 인터락은 보내기 직전 입력 버퍼를 비우고, 포트 찾기가 사유를 함께 돌려준다. **판정기(`fsm.py`)는 고치지 않는다.**

**Tech Stack:** Python 3.13 · PyQt6(오프스크린) · 소켓(127.0.0.1 가짜 카메라 · 소켓 쌍) · 가짜 시리얼 · 하위 프로세스(가짜 공구 워커)

**Spec:** `docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md` §9(9.1·9.2·9.4~9.7) — 이 계획서 = ④-A

## Global Constraints

- 코드 저장소 = `/home/pi/sop-project/Rpi5`(별도 git) · 문서 = `/home/pi/sop-project`. 커밋은 **경로 지정 add** 만.
- 커밋 서명 = `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>` + `Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL` (실행 세션이 다르면 그 세션 값).
- 자가 테스트는 **일괄 러너가 없다** — `Rpi5/Demo` 에서 `selftest/test_*.py` 개별 실행. `test_voice_tts` 만 `~/env/tts/.venv/bin/python`. 🔴 **종료 코드는 `rc=$?` 로 먼저 받는다**.
- 🔴 **Hailo 를 쓰는 시험(`test_console_flow` 등)을 다른 GUI·측정 실행과 동시에 돌리지 않는다.**
- 🔴 **고치지 않는 것**(spec §9.6) — `fsm.py` · 측정 도구(`test/`) · 펌웨어(`.ino`) · ACK 순번 · by-id 경로 3초 안 재꽂음(`st_rdev`) · `Speaker.f`.
- 🔴 **실HW 검증은 ⏸** — 인터락(C15·M-4·M-5·M-6·M-10)은 가짜 시리얼로만 · 음성(C20·M-2)은 소켓 쌍으로만 · 카메라(C12·C16·C19)는 127.0.0.1 가짜 서버로만.
- 🔴 **push 하지 않는다** — push 는 ④-B 끝의 `work-close` 에서 한 번.

## 관문

- **🔒 홀드아웃** — 판정 임계·측정 데이터(`test/hoi.db`)를 건드리지 않는다. 판정기를 고치지 않으므로 `fsm_sim --gate` 출력이 **Task 0 기준선과 글자까지 같아야** 한다. 합격선 = 태스크마다 적힌 Expected(복사본 드라이런으로 **구현 전에 고정**).
- **검증 관문** — ①태스크마다 새 테스트가 **고치기 전 실패 → 고친 뒤 통과**(가드는 「전후 통과」 표시 · 설명만 바꾸는 M-3 제외) ②Task 9: 자가 테스트가 Task 0 기준선보다 나빠지지 않음(새 파일 `test_camera_link.py` 한 줄만 늘어남) · `fsm_sim --gate` 기준선과 같음 · `test_imports` 통과.
- **중단 규칙** — ①확인 단계가 Expected 와 다르면 다음 태스크로 넘어가지 않는다 ②이번 변경과 무관한 기존 테스트가 깨지면 멈추고 보고한다 ③같은 수정을 3번 시도해도 안 되면 멈추고 보고한다 ④인터락 시험이 실제 장치(`/dev/ttyACM*`)에 쓰기를 시도하면 즉시 멈춘다(가짜 시리얼 주입 실패) ⑤카메라 시험이 127.0.0.1 밖으로 붙으려 하면 즉시 멈춘다.

## Review Focus

1. **카메라 연결은 살아 있는데 프레임만 멎은 채 처리 루프가 빠져나올 때** — 옛 수신 스레드가 새 연결의 플래그를 세우거나 새 소켓을 같이 읽으면 안 된다 → `test_c19_old_receiver_leaves_new_connection_alone` · `test_c19_close_wakes_blocked_receiver`(Task 1).
2. **프레임 처리 오류가 연달아 날 때(Hailo 일시 오류가 몇 프레임 이어짐)** — 연결은 한 번만 · 로그는 한 줄 · 영상은 이어진다 → `test_c12_frame_error_keeps_connection`(Task 3).
3. **공구 워커가 `ready` 를 남긴 뒤 죽을 때** — 「사용 가능」에서 빠지고 종료 코드·오류 출력이 로그에 남으며 요청 파일이 쌓이지 않는다(`ready` 전에 죽어도 같은 경로) → `test_c14_dead_worker_not_available`(Task 5).
4. **체류 시각** — 카메라가 프레임을 받은 시각이 판정기까지 그대로 간다 · 판정기·시뮬레이터 값은 그대로 → `test_c16_*`(Task 2) · Task 9 관문.
5. **음성 재생 확인이 한도 안에 안 끝난 뒤의 다음 재생** — 옛 채널을 버려 늦은 「재생 완료」가 섞이지 않는다 · 제때 끝나면 채널을 그대로 쓴다 → `test_m2_timeout_drops_channel`(Task 8).

## 계획서 표기

코드 블록 앞의 HTML 주석은 **어디에 어떻게 넣는지**를 뜻한다(렌더링에는 안 보인다):
- `<!-- rep: <경로> -->` 다음 두 블록 = 바꿀 원문(정확히 한 곳) → 새 내용
- `<!-- add: <경로> -->` 다음 한 블록 = 파일 끝 `if __name__ == "__main__":` **바로 앞**에 넣는다
- `<!-- new: <경로> -->` 다음 한 블록 = 새 파일
- Expected 의 「…통과」 줄 끝 실행 시간 `(0.00Xs)` 는 비교하지 않는다.
- 드라이런 도구 = `~/lab/plan-dryrun/plan_dryrun.py`(git 밖 · 이 파이에만) — `python3 plan_dryrun.py <계획서> <복사본 루트> [태스크번호,…]` 로 위 지시와 `Run` 을 순서대로 적용·실행하고 바로 뒤 Expected 와 기계 비교한다. 태스크 실행기 `run_task.py N` 은 안의 `PLAN` 경로를 이 계획서로 바꿔 쓴다. 없으면 지시를 손으로 적용한다(지시가 정확히 한 곳을 가리키므로 결과는 같다).

---

### Task 0: 기준선 기록

**Files:** Create `Rpi5/조사/범위B-20260926/before_selftest.txt` · `before_gate.txt`

- [ ] **Step 1: 자가 테스트 전부 · 관문을 실행해 남긴다**

Run:
```bash
cd /home/pi/sop-project/Rpi5/Demo && D=../조사/범위B-20260926 && mkdir -p $D && \
for t in selftest/test_*.py; do
  py=python3; [ "$(basename $t)" = test_voice_tts.py ] && py=~/env/tts/.venv/bin/python
  timeout 600 $py "$t" >/dev/null 2>&1; rc=$?; echo "$(basename $t) exit=$rc"
done > $D/before_selftest.txt; grep -c "exit=0" $D/before_selftest.txt; grep -v "exit=0" $D/before_selftest.txt; \
python3 test/fsm_sim.py --gate > $D/before_gate.txt 2>&1; echo gate_exit=$?
```
Expected:
```
31
gate_exit=0
```

- [ ] **Step 2: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add 조사/범위B-20260926/before_selftest.txt 조사/범위B-20260926/before_gate.txt && git commit -m "docs(조사): 범위 B·미룬 사소 ④ 기준선 — 자가 테스트·관문 (수정 전)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 1: C19 — 수신 스레드는 자기 연결만 읽고 자기 연결일 때만 알린다

**Files:** Modify `Rpi5/Demo/camera_thread.py` · Create `Rpi5/Demo/selftest/test_camera_link.py`

**Interfaces:**
- Produces: `camera_thread._close_sock(sock)`(shutdown → close · 예외 흡수) · `CameraThread._conn_gen`(int · 연결마다 +1) · `CameraThread._recv_worker(sock, gen)` · `_recv_latest_frame(sock)` · `_recv_exact(sock, length)`. 시험 파일 `test_camera_link.py`(가짜 검출기 `_FAKE_DET` · `check`) — Task 2·3 이 시험을 더한다.
- 🔑 새 연결을 시작할 때 `_latest_raw` 도 비운다 — 끊기기 전 프레임을 새 연결에서 처리하면 R5(재연결 때 관측 버림) 직후 옛 관측이 다시 들어간다.

- [ ] **Step 1: 실패하는 테스트**

<!-- new: Rpi5/Demo/selftest/test_camera_link.py -->
```python
"""카메라 수신 연결 검증(C12·C16·C19) — ESP32·Hailo 없이.

실행: python3 Demo/selftest/test_camera_link.py
🔴 버튼 검출기를 가짜로 바꿔 끼운다 — `camera_thread` 는 import 할 때 검출기(Hailo)를 연다.
   카메라는 127.0.0.1 의 가짜 TCP 서버(4바이트 길이 + JPEG — ESP32 와 같은 모양)로 흉내 낸다.
정본 = 상위 docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md §9.2
"""
import os
import socket
import struct
import sys
import threading
import time
import types

_DEMO_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _DEMO_DIR)
os.environ.setdefault("QT_QPA_PLATFORM", "offscreen")

import cv2
import numpy as np


class _FakeDetector:
    backend_name = "시험용"
    NAMES = {0: "B1", 1: "B2", 2: "B3", 3: "B4", 4: "EMO"}

    def __init__(self):
        self.dets = []
        self.fail_next = 0            # 다음 N번 detect 는 예외(Hailo 일시 오류 흉내 · C12)

    def class_name(self, i):
        return self.NAMES[i]

    def detect(self, frame):
        if self.fail_next:
            self.fail_next -= 1
            raise RuntimeError("HAILO_TIMEOUT(시험)")
        return list(self.dets)

    def close(self):
        pass


_FAKE_DET = _FakeDetector()
_fake_mod = types.ModuleType("detector")
_fake_mod.create_detector = lambda: _FAKE_DET
sys.modules["detector"] = _fake_mod

import config
config.HAND_ENABLED = False
config.TOOL_ENABLED = False

from PyQt6.QtCore import Qt
from PyQt6.QtWidgets import QApplication
_app = QApplication.instance() or QApplication([])

import camera_thread as ct

_fails = []


def check(cond, msg):
    print(("  ✅ " if cond else "  ❌ ") + msg)
    if not cond:
        _fails.append(msg)


# ---------------------------------------------------------------- C19 연결별 수신
def test_c19_old_receiver_leaves_new_connection_alone():
    """C19 — 끝난 연결의 수신 스레드는 새 연결의 플래그를 건드리지 않는다(검토 C19 — 새 연결을 끊었다)."""
    print("\n[C19] 옛 수신 스레드")
    th = ct.CameraThread()
    a, b = socket.socketpair()
    b.close()                                        # 옛 연결 — 곧바로 끊김(recv 가 빈 값)
    th._conn_gen = 2                                 # 그 사이 새 연결(2번)이 붙었다
    th._recv_error = False
    th._raw_event.clear()
    try:
        th._recv_worker(a, 1)                        # 1번 연결의 수신 스레드가 뒤늦게 끝난다
    except TypeError as e:
        check(False, f"수신 스레드가 자기 연결(소켓·번호)을 받지 않는다 — {e}")
        return
    finally:
        a.close()
    check(th._recv_error is False, "새 연결에 「수신 오류」를 세우지 않는다")
    check(not th._raw_event.is_set(), "새 연결의 처리 루프를 깨우지 않는다")


def test_c19_close_wakes_blocked_receiver():
    """C19 — 연결을 끝내면 막혀 있던 수신이 곧바로 깬다(close 만으로는 안 깨 join 3초 뒤에도 살아남았다)."""
    print("\n[C19] 막힌 수신 깨우기")
    close = getattr(ct, "_close_sock", None)
    check(close is not None, "camera_thread._close_sock 이 있다")
    if close is None:
        return
    a, b = socket.socketpair()
    a.settimeout(5)
    woke = threading.Event()

    def rx():
        try:
            a.recv(4)
        except OSError:
            pass
        woke.set()

    threading.Thread(target=rx, daemon=True).start()
    time.sleep(0.1)                                  # recv 에 들어갈 시간
    close(a)
    check(woke.wait(1.0), "1초 안에 깬다")
    b.close()


if __name__ == "__main__":
    for _name, _fn in sorted(globals().items()):
        if _name.startswith("test_"):
            _fn()
    print()
    if _fails:
        print(f"❌ 실패 {len(_fails)}건")
        for m in _fails:
            print(f"   - {m}")
        sys.exit(1)
    print("✅ 카메라 연결 검증 통과")
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_camera_link.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ camera_thread._close_sock 이 있다
  ❌ 수신 스레드가 자기 연결(소켓·번호)을 받지 않는다 — CameraThread._recv_worker() takes 1 positional argument but 3 were given
❌ 실패 2건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
def close_detector():
    """Release detector backend resources (Hailo device, pipeline, etc.)."""
    if _detector is not None:
        _detector.close()
```
```python
def close_detector():
    """Release detector backend resources (Hailo device, pipeline, etc.)."""
    if _detector is not None:
        _detector.close()


def _close_sock(sock):
    """소켓을 닫는다 — 🔴 **먼저 shutdown** 해 다른 스레드의 막힌 recv 를 깨운다(검토 C19).

    close 만으로는 이미 recv 에 들어가 있는 스레드가 깨지 않는다 — 연결은 살아 있고 데이터만 없으면
    수신 스레드가 타임아웃(10초)까지 남아, join(3초) 뒤에도 살아남았다.
    """
    try:
        sock.shutdown(socket.SHUT_RDWR)
    except OSError:
        pass
    try:
        sock.close()
    except OSError:
        pass
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
        self._raw_event    = threading.Event()
        self._recv_error   = False
```
```python
        self._raw_event    = threading.Event()
        self._recv_error   = False
        self._conn_gen     = 0       # 연결 번호 — 수신 스레드는 자기 연결일 때만 알린다(C19)
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
    def _recv_worker(self):
        while self._running:
            data = self._recv_latest_frame()
            if data is None:
                self._recv_error = True
                self._raw_event.set()
                break
            with self._raw_lock:
                self._latest_raw = data
            self._raw_event.set()
```
```python
    def _recv_worker(self, sock, gen):
        """수신 전용 — 최신 프레임을 _latest_raw 에 덮어쓴다.

        🔴 **그 연결의 소켓만** 읽고, 연결 번호가 지금 것일 때만 알린다(검토 C19) — 종전에는
           `self.sock` 을 매번 다시 읽고 플래그를 연결끼리 나눠 써서, join(3초) 뒤에도 살아남은
           옛 수신 스레드가 새 소켓을 같이 읽거나 `_recv_error` 로 **새 연결을 끊었다.**
        """
        while self._running:
            data = self._recv_latest_frame(sock)
            with self._raw_lock:
                if gen != self._conn_gen:
                    return                   # 이미 끝난 연결 — 새 연결을 건드리지 않는다
                if data is None:
                    self._recv_error = True
                else:
                    self._latest_raw = data
                self._raw_event.set()
            if data is None:
                return
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
            self._recv_error = False
            self._raw_event.clear()
            recv_thread = threading.Thread(target=self._recv_worker, daemon=True)
            recv_thread.start()
```
```python
            with self._raw_lock:
                self._conn_gen += 1
                self._recv_error = False
                self._latest_raw = None          # 끊기기 전 프레임을 새 연결에서 쓰지 않는다
                self._raw_event.clear()
            recv_thread = threading.Thread(target=self._recv_worker,
                                           args=(self.sock, self._conn_gen), daemon=True)
            recv_thread.start()
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
            finally:
                if self.sock is not None:
                    try:
                        self.sock.close()
                    except Exception:
                        pass
                    self.sock = None
                recv_thread.join(timeout=3)
```
```python
            finally:
                if self.sock is not None:
                    _close_sock(self.sock)       # shutdown 으로 막힌 수신을 깨운다(C19)
                    self.sock = None
                recv_thread.join(timeout=3)
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
    def _recv_latest_frame(self):
        while True:
            header = self._recv_exact(4)
```
```python
    def _recv_latest_frame(self, sock):
        while True:
            header = self._recv_exact(sock, 4)
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
            data = self._recv_exact(length)
            if data is None:
                return None
            try:
                readable, _, _ = select.select([self.sock], [], [], 0)
```
```python
            data = self._recv_exact(sock, length)
            if data is None:
                return None
            try:
                readable, _, _ = select.select([sock], [], [], 0)
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
    def _recv_exact(self, length):
        data = b''
        while len(data) < length:
            if not self._running:
                return None
            try:
                chunk = self.sock.recv(length - len(data))
```
```python
    def _recv_exact(self, sock, length):
        data = b''
        while len(data) < length:
            if not self._running:
                return None
            try:
                chunk = sock.recv(length - len(data))
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
        self._running = False
        self._raw_event.set()
        if self.sock is not None:
            try:
                self.sock.close()
            except Exception:
                pass
        self.wait()
```
```python
        self._running = False
        self._raw_event.set()
        sock = self.sock
        if sock is not None:
            _close_sock(sock)
        self.wait()
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_camera_link.py 2>&1 | tail -1; python3 selftest/test_recognition.py 2>&1 | tail -1; python3 selftest/test_imports.py >/dev/null 2>&1; echo imports_exit=$?`
Expected:
```
✅ 카메라 연결 검증 통과
✅ 인식 검증 통과
imports_exit=0
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/camera_thread.py Demo/selftest/test_camera_link.py && git commit -m "fix(camera): 수신 스레드는 자기 연결의 소켓만 읽고 자기 연결일 때만 알린다 · 연결을 끝낼 때 shutdown 으로 막힌 수신을 깨운다 (범위 B C19)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 2: C16 — 체류 시각은 카메라가 프레임을 받은 시각

**Files:** Modify `Rpi5/Demo/camera_thread.py` · `Rpi5/Demo/safety_console.py` · Test `Rpi5/Demo/selftest/test_camera_link.py`

**Interfaces:**
- Consumes: Task 1 의 `_recv_worker(sock, gen)`.
- Produces: `CameraThread.roi_signal = pyqtSignal(str, int, float)`(라벨 · 단계 · 프레임을 다 받은 시각 `time.monotonic()`) · `_latest_raw = (bytes, float)` · `CameraThread._process_frame(frame, t=None)`(없으면 지금 시각) · `SafetyConsole._on_roi(roi, level, t)` → `fsm.update_vision(roi, t, level)`.
- 🔑 판정기는 `now` 를 **자기끼리만** 뺀다(`update_vision` 의 체류·갭메우기) — 단조 시계로 바꿔도 다른 시계와 섞이지 않는다. `press_button(…, time.time())` 의 `now` 는 판정기가 쓰지 않는다.

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_camera_link.py -->
```python
# ---------------------------------------------------------------- C16 프레임 시각
def test_c16_roi_carries_frame_time():
    """C16 — 체류 시각은 GUI 가 신호를 받은 때가 아니라 **카메라가 프레임을 받은 때**다(검토 C16·U18)."""
    print("\n[C16] 프레임 시각")
    th = ct.CameraThread()
    got = []
    th.roi_signal.connect(lambda *a: got.append(a))
    try:
        th._process_frame(np.zeros((48, 64, 3), np.uint8), 123.5)
    except TypeError as e:
        check(False, f"프레임 시각을 받지 못한다 — {e}")
        return
    check(got == [("", 0, 123.5)], f"roi_signal = {got} — 받은 시각이 실려야 한다")


def test_c16_console_passes_frame_time():
    """C16 — 콘솔은 받은 시각을 그대로 판정기에 넘긴다(time.time() 으로 바꾸지 않는다)."""
    print("\n[C16] 콘솔 → 판정기")
    import safety_console as sc
    calls = []
    fake = types.SimpleNamespace(
        fsm=types.SimpleNamespace(update_vision=lambda roi, now, level: calls.append((roi, now, level))),
        _last_roi=None, _append_log=lambda m: None)
    try:
        sc.SafetyConsole._on_roi(fake, "B2", 2, 77.25)
    except TypeError as e:
        check(False, f"슬롯이 시각을 받지 못한다 — {e}")
        return
    check(calls == [("B2", 77.25, 2)], f"update_vision 인자 = {calls}")
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_camera_link.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 슬롯이 시각을 받지 못한다 — SafetyConsole._on_roi() takes 3 positional arguments but 4 were given
  ❌ 프레임 시각을 받지 못한다 — CameraThread._process_frame() takes 2 positional arguments but 3 were given
❌ 실패 2건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
    roi_signal                = pyqtSignal(str, int)  # (버튼 ROI 라벨, 단계) — ""·0 = 없음
```
```python
    roi_signal                = pyqtSignal(str, int, float)  # (버튼 ROI 라벨, 단계, 프레임을 받은 시각) — ""·0 = 없음
                                                 # 🔴 @pyqtSlot(str, int, float) 와 짝을 맞출 것(C16)
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
                if data is None:
                    self._recv_error = True
                else:
                    self._latest_raw = data
```
```python
                if data is None:
                    self._recv_error = True
                else:
                    self._latest_raw = (data, time.monotonic())   # C16 — 프레임을 다 받은 시각
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
                    with self._raw_lock:
                        data = self._latest_raw

                    if data is None:
                        continue

```
```python
                    with self._raw_lock:
                        got = self._latest_raw

                    if got is None:
                        continue
                    data, t_frame = got

```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
                    frame = self._process_frame(frame)
```
```python
                    frame = self._process_frame(frame, t_frame)
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
    def _process_frame(self, frame):
        # 🔴 방향 보정은 **반전 → 왜곡보정 → 회전** 순서다(frame_orient 참조).
```
```python
    def _process_frame(self, frame, t=None):
        """프레임 하나를 처리한다. t = 그 프레임을 다 받은 시각(`time.monotonic()` · 없으면 지금).

        🔑 체류 시각은 이 t 다(C16) — GUI 가 신호를 처리한 시각으로 재면 GUI 가 멈췄다 풀릴 때
           밀린 신호가 몰려 체류가 흔들렸고(검토 C16·U18), 벽시계라 시각 보정에도 튀었다.
        """
        if t is None:
            t = time.monotonic()
        # 🔴 방향 보정은 **반전 → 왜곡보정 → 회전** 순서다(frame_orient 참조).
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
        self.roi_signal.emit(roi or "", level or 0)
```
```python
        self.roi_signal.emit(roi or "", level or 0, t)
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
    @pyqtSlot(str, int)   # 🔴 시그널(str, int)과 반드시 일치해야 한다 — 어긋나면 기동 즉시 TypeError
    def _on_roi(self, roi, level):
        """HOI 결과(손끝이 든 버튼 ROI + 구역 단계)를 FSM 비전 틱으로 전달.

        level: 2=박스 안(위험) / 1=링(접근) / 0=밖. 상세 = roi_zones.py
        """
        self.fsm.update_vision(roi or None, time.time(), level or ZONE_INSIDE)
```
```python
    @pyqtSlot(str, int, float)   # 🔴 시그널(str, int, float)과 반드시 일치해야 한다 — 어긋나면 기동 즉시 TypeError
    def _on_roi(self, roi, level, t):
        """HOI 결과(손끝이 든 버튼 ROI + 구역 단계)를 FSM 비전 틱으로 전달.

        level: 2=박스 안(위험) / 1=링(접근) / 0=밖. 상세 = roi_zones.py
        t: 카메라가 그 프레임을 다 받은 시각(`time.monotonic()`) — 🔴 여기서 `time.time()` 으로 바꾸지
           않는다. GUI 가 신호를 처리한 시각은 GUI 가 멈췄다 풀리면 몰려 체류가 흔들렸다(검토 C16·U18).
        """
        self.fsm.update_vision(roi or None, t, level or ZONE_INSIDE)
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_camera_link.py 2>&1 | tail -1; python3 selftest/test_recognition.py 2>&1 | tail -1; python3 selftest/test_imports.py >/dev/null 2>&1; echo imports_exit=$?`
Expected:
```
✅ 카메라 연결 검증 통과
✅ 인식 검증 통과
imports_exit=0
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/camera_thread.py Demo/safety_console.py Demo/selftest/test_camera_link.py && git commit -m "fix(camera): 체류 시각을 카메라가 프레임을 받은 시각(단조 시계)으로 — GUI 가 신호를 처리한 시각이라 GUI 가 멈추면 체류가 흔들리던 것 (범위 B C16·U18)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 3: C12 — 프레임 하나의 처리 오류로 연결을 끊지 않는다

**Files:** Modify `Rpi5/Demo/camera_thread.py` · Test `Rpi5/Demo/selftest/test_camera_link.py`

**Interfaces:**
- Consumes: Task 2 의 `_process_frame(frame, t)` · `t_frame`.
- Produces: `CameraThread._frame_error(e)`(로그 5초에 한 번) · `CameraThread._frame_err_at`(마지막 로그 시각 · 없으면 None). 시험 대역 `_FakeCam`(127.0.0.1 · 붙은 횟수 `conns`).

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_camera_link.py -->
```python
# ---------------------------------------------------------------- C12 프레임 처리 오류
class _FakeCam:
    """ESP32 대역 — 붙는 손님마다 작은 JPEG 를 0.05초 간격으로 보낸다(4바이트 길이 + JPEG). 붙은 횟수를 센다."""

    def __init__(self):
        self.srv = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        self.srv.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
        self.srv.bind(("127.0.0.1", 0))
        self.srv.listen(4)
        self.srv.settimeout(0.2)
        self.port = self.srv.getsockname()[1]
        self.conns = 0
        self._stop = False
        ok, buf = cv2.imencode(".jpg", np.zeros((48, 64, 3), np.uint8))
        self._frame = struct.pack("<I", len(buf)) + buf.tobytes()
        threading.Thread(target=self._accept, daemon=True).start()

    def _accept(self):
        while not self._stop:
            try:
                c, _ = self.srv.accept()
            except OSError:
                continue
            self.conns += 1
            threading.Thread(target=self._send, args=(c,), daemon=True).start()

    def _send(self, c):
        try:
            while not self._stop:
                c.sendall(self._frame)
                time.sleep(0.05)
        except OSError:
            pass
        finally:
            c.close()

    def close(self):
        self._stop = True
        self.srv.close()


def test_c12_frame_error_keeps_connection():
    """C12 — 프레임 처리 오류로 카메라 연결을 끊지 않는다(검토 C12 — 영상이 3초 넘게 끊기고 로그는 네트워크 탓)."""
    print("\n[C12] 프레임 처리 오류")
    cam = _FakeCam()
    ct.CAMERA_TCP_PORT = cam.port
    ct.TCP_RECONNECT_DELAY_SEC = 0.1
    th = ct.CameraThread()
    th.set_host("127.0.0.1")
    logs, frames = [], []
    th.log_signal.connect(logs.append, Qt.ConnectionType.DirectConnection)
    th.change_pixmap_signal.connect(lambda img: frames.append(1), Qt.ConnectionType.DirectConnection)
    _FAKE_DET.fail_next = 3                          # 세 프레임 연달아 처리 오류
    runner = threading.Thread(target=th.run, daemon=True)
    runner.start()
    time.sleep(1.0)
    th._running = False
    th._raw_event.set()
    runner.join(timeout=5)
    cam.close()
    _FAKE_DET.fail_next = 0
    errs = [m for m in logs if "프레임 처리 오류" in m]
    check(cam.conns == 1, "한 번만 붙는다 — 끊고 다시 붙으면 안 된다")
    check(len(frames) >= 3, "오류 뒤에도 영상이 이어진다")
    check(len(errs) == 1, "「프레임 처리 오류」 로그가 한 줄 — 이어진 오류는 묶는다")
    check(not any("수신 오류" in m for m in logs), "「수신 오류」(네트워크 탓)로 적지 않는다")
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_camera_link.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 한 번만 붙는다 — 끊고 다시 붙으면 안 된다
  ❌ 「프레임 처리 오류」 로그가 한 줄 — 이어진 오류는 묶는다
  ❌ 「수신 오류」(네트워크 탓)로 적지 않는다
❌ 실패 3건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
        self._conn_gen     = 0       # 연결 번호 — 수신 스레드는 자기 연결일 때만 알린다(C19)
```
```python
        self._conn_gen     = 0       # 연결 번호 — 수신 스레드는 자기 연결일 때만 알린다(C19)
        self._frame_err_at = None    # 마지막 「프레임 처리 오류」 로그 시각 — 5초에 한 번(C12)
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
    # =========================================================================
    # [스레드 메인 루프]
    # =========================================================================
    def run(self):
```
```python
    def _frame_error(self, e):
        """프레임 처리 오류를 적는다 — 같은 오류가 매 프레임 이어져도 로그는 5초에 한 번(C12)."""
        now = time.monotonic()
        if self._frame_err_at is None or now - self._frame_err_at >= 5.0:
            self._frame_err_at = now
            self.log_signal.emit(f"[카메라] 프레임 처리 오류 — 그 프레임만 건너뛴다(연결 유지): {e!r}")

    # =========================================================================
    # [스레드 메인 루프]
    # =========================================================================
    def run(self):
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
                    frame = self._process_frame(frame, t_frame)
```
```python
                    try:
                        frame = self._process_frame(frame, t_frame)
                    except Exception as e:                   # noqa: BLE001
                        # 🔴 프레임 하나의 처리 오류(Hailo 일시 오류·공구 게이트 경쟁 등)로 연결을 끊지
                        #    않는다 — 종전에는 아래 「수신 오류」로 가서 소켓을 닫고 3초 뒤 다시 붙어,
                        #    영상이 끊기고 로그는 네트워크 탓처럼 보였다(검토 C12). 그 프레임만 버린다.
                        self._frame_error(e)
                        continue
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_camera_link.py 2>&1 | tail -1; python3 selftest/test_recognition.py 2>&1 | tail -1`
Expected:
```
✅ 카메라 연결 검증 통과
✅ 인식 검증 통과
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/camera_thread.py Demo/selftest/test_camera_link.py && git commit -m "fix(camera): 프레임 처리 오류는 그 프레임만 버린다 — 연결을 끊고 3초 뒤 다시 붙으며 로그는 네트워크 탓으로 남던 것 · 로그 5초에 한 번 (범위 B C12)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 4: C13 — 공구 게이트를 두 스레드가 잠금으로 나눠 쓴다

**Files:** Modify `Rpi5/Demo/tool_gate.py` · Test `Rpi5/Demo/selftest/test_tool_gate.py`

**Interfaces:**
- Produces: `ToolGate._lock`(threading.Lock) · `ToolGate._ready()`(잠금 안에서 부른다 — 워커 핸들이 있고 `ready` 파일이 있는가) · `_start_locked()` · `_request_locked(frame, fingertip)` · `_poll_locked()`. 바깥 인터페이스(`start`·`stop`·`available`·`request`·`poll`)는 그대로. 시험 대역 `_Alive`(살아 있는 워커 — `poll()` 이 None) — Task 5 가 기존 시험의 `object()` 대역을 이것으로 바꾼다.
- 🔑 `stop()` 은 핸들을 **잠금 안에서 꺼내 비우고**, 종료는 잠금 밖에서 기다린다 — 카메라 스레드를 최대 2초 막지 않는다.

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_tool_gate.py -->
```python
# ------------------------------------------------------- ③ 두 스레드(C13)
import threading


class _Alive:
    """살아 있는 워커 대역 — poll() 이 None(아직 안 끝남)."""

    def poll(self):
        return None

    def terminate(self):
        pass

    def wait(self, timeout=None):
        return 0

    def kill(self):
        pass


class _RacyPending(dict):
    """poll() 이 `seq in _pending` 을 묻는 순간 다른 스레드가 stop() 을 부른다 — 경쟁 창을 결정적으로 연다."""

    def __init__(self, gate, *a):
        super().__init__(*a)
        self._gate = gate
        self.stopper = None

    def __contains__(self, k):
        if self.stopper is None:
            self.stopper = threading.Thread(target=self._gate.stop)
            self.stopper.start()
            self.stopper.join(timeout=0.3)      # 잠금이 있으면 stop 은 poll 이 끝날 때까지 기다린다
        return super().__contains__(k)


def test_c13_stop_during_poll():
    """C13 — 카메라 스레드의 poll() 도중 GUI 스레드가 stop() 해도 결과가 깨지지 않는다(검토 C13)."""
    print("[8] poll 도중 stop")
    tmp = tempfile.mkdtemp()
    try:
        gate = _gate(tmp)
        gate._proc = _Alive()
        _mark_ready(tmp)
        gate.request(FRAME, (3, 4))                    # seq=1
        _write_resp(tmp, seq=1, dets=[])
        gate._pending = _RacyPending(gate, gate._pending)
        try:
            got = gate.poll()
            check(got is not None and got[1] == (3, 4), "poll 이 그 결과를 끝까지 돌려준다")
        except Exception as e:                         # noqa: BLE001
            check(False, f"poll 이 예외로 끝났다 — {type(e).__name__}: {e}")
        gate._pending.stopper.join(timeout=3)
        check(gate._proc is None, "stop 은 poll 이 끝난 뒤 마저 한다")
    finally:
        shutil.rmtree(tmp, ignore_errors=True)
```

<!-- rep: Rpi5/Demo/selftest/test_tool_gate.py -->
```python
    test_요청파일이_실제로_생긴다()

    elapsed = time.time() - t0
```
```python
    test_요청파일이_실제로_생긴다()
    test_c13_stop_during_poll()

    elapsed = time.time() - t0
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_tool_gate.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ poll 이 그 결과를 끝까지 돌려준다
❌ 실패 1건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/tool_gate.py -->
```python
import shutil
import subprocess
```
```python
import shutil
import subprocess
import threading
```

<!-- rep: Rpi5/Demo/tool_gate.py -->
```python
        self._pending = {}        # seq → 그 요청을 보낼 때의 fingertip
        self._last_seq = 0        # 이미 소비한 응답의 seq
```
```python
        self._pending = {}        # seq → 그 요청을 보낼 때의 fingertip
        self._last_seq = 0        # 이미 소비한 응답의 seq
        # 🔴 GUI 스레드(start·stop)와 카메라 스레드(request·poll)가 함께 만진다 — 상태는 잠금
        #    안에서만(검토 C13). 없으면 stop() 의 `_pending.clear()` 가 poll() 의 검사와 pop 사이에
        #    끼어 KeyError·RuntimeError 가 났고, 그 예외가 카메라를 다시 붙게 했다.
        self._lock = threading.Lock()
```

<!-- rep: Rpi5/Demo/tool_gate.py -->
```python
    def start(self):
        """워커를 띄운다. 이미 떠 있으면 아무 일도 하지 않는다."""
        if self._proc is not None:
            return
```
```python
    def start(self):
        """워커를 띄운다. 이미 떠 있으면 아무 일도 하지 않는다."""
        with self._lock:
            self._start_locked()

    def _start_locked(self):
        if self._proc is not None:
            return
```

<!-- rep: Rpi5/Demo/tool_gate.py -->
```python
    def stop(self):
        """워커를 내린다. 서브 작업이 끝나면 반드시 부른다(CPU 를 계속 먹는다)."""
        if self._proc is None:
            return
        try:
            self._proc.terminate()
            self._proc.wait(timeout=2)
        except Exception:                                    # noqa: BLE001
            try:
                self._proc.kill()
            except Exception:                                # noqa: BLE001
                pass
        self._proc = None
        self._pending.clear()
        self._log("[공구] 추론 워커를 내렸습니다.")

    @property
    def available(self):
        """워커가 떠 있고 모델 로딩까지 끝났는가."""
        return self._proc is not None and os.path.exists(os.path.join(self._dir, "ready"))
```
```python
    def stop(self):
        """워커를 내린다. 서브 작업이 끝나면 반드시 부른다(CPU 를 계속 먹는다).

        🔑 상태는 잠금 안에서 비우고, 종료는 **잠금 밖에서** 기다린다(C13) — 기다리는 동안
           카메라 스레드의 poll() 을 막지 않는다.
        """
        with self._lock:
            proc, self._proc = self._proc, None
            self._pending.clear()
        if proc is None:
            return
        try:
            proc.terminate()
            proc.wait(timeout=2)
        except Exception:                                    # noqa: BLE001
            try:
                proc.kill()
            except Exception:                                # noqa: BLE001
                pass
        self._log("[공구] 추론 워커를 내렸습니다.")

    @property
    def available(self):
        """워커가 떠 있고 모델 로딩까지 끝났는가."""
        with self._lock:
            return self._ready()

    def _ready(self):
        """(잠금 안에서) 워커가 떠 있고 모델 로딩까지 끝났는가."""
        return self._proc is not None and os.path.exists(os.path.join(self._dir, "ready"))
```

<!-- rep: Rpi5/Demo/tool_gate.py -->
```python
        if not self.available:
            return
        self._seq += 1
```
```python
        with self._lock:
            self._request_locked(frame, fingertip)

    def _request_locked(self, frame, fingertip):
        if not self._ready():
            return
        self._seq += 1
```

<!-- rep: Rpi5/Demo/tool_gate.py -->
```python
        if not self.available:
            return None
        path = os.path.join(self._dir, "resp.json")
```
```python
        with self._lock:
            return self._poll_locked()

    def _poll_locked(self):
        if not self._ready():
            return None
        path = os.path.join(self._dir, "resp.json")
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_tool_gate.py 2>&1 | tail -1`
Expected:
```
✅ 공구 게이트 검증 통과
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/tool_gate.py Demo/selftest/test_tool_gate.py && git commit -m "fix(tool_gate): GUI·카메라 두 스레드가 잠금으로 나눠 쓴다 — 서브 작업이 끝나는 순간 poll 과 stop 이 겹쳐 카메라가 다시 붙던 것 · 종료는 잠금 밖에서 기다린다 (범위 B C13)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 5: C14 — 공구 워커가 죽으면 「사용 가능」에서 빠지고 이유를 남긴다

**Files:** Modify `Rpi5/Demo/tool_gate.py` · Test `Rpi5/Demo/selftest/test_tool_gate.py`

**Interfaces:**
- Consumes: Task 4 의 `_ready()` · `_Alive`.
- Produces: `_ready()` 가 워커 생존(`_proc.poll()`)을 본다 — 끝나 있으면 한 번 로그(종료 코드 + 오류 출력 끝 5줄)를 남기고 `_proc = None` · `ToolGate._err_tail(n=5)` · 워커 오류 출력 = 공유 폴더의 `worker.err`.
- 🔴 기존 시험 [2]~[7] 은 워커 대역으로 `object()` 를 넣는다 — `poll()` 이 없어 이제 깨지므로 `_Alive()` 로 바꾼다(Step 1).

- [ ] **Step 1: 실패하는 테스트**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 -c "p='selftest/test_tool_gate.py';s=open(p,encoding='utf-8').read();print(s.count('gate._proc = object()'));open(p,'w',encoding='utf-8').write(s.replace('gate._proc = object()','gate._proc = _Alive()'))"`
Expected: `6`

<!-- add: Rpi5/Demo/selftest/test_tool_gate.py -->
```python
# ------------------------------------------------------- ④ 죽은 워커(C14)
def test_c14_dead_worker_not_available():
    """C14 — 워커가 죽으면 「사용 가능」에서 빠지고, 종료 코드와 오류 출력이 로그에 남는다(검토 C14)."""
    print("[9] 죽은 워커")
    import tool_gate
    tmp = tempfile.mkdtemp()
    script = os.path.join(tempfile.mkdtemp(), "fake_worker.py")
    with open(script, "w", encoding="utf-8") as f:
        f.write("import os, sys\n"
                "open(os.path.join(sys.argv[1], 'ready'), 'w').close()\n"
                "sys.stderr.write('MemoryError: 시험용 워커 사망\\n')\n"
                "sys.exit(3)\n")
    old = tool_gate._WORKER
    tool_gate._WORKER = script
    logs = []
    try:
        gate = ToolGate(shm_dir=tmp, python=sys.executable, model=script, conf=0.65,
                        log=logs.append)
        gate.start()
        end = time.time() + 5
        while gate._proc is not None and gate._proc.poll() is None and time.time() < end:
            time.sleep(0.02)
        check(os.path.exists(os.path.join(tmp, "ready")), "워커가 ready 를 남기고 죽었다(시험 조건)")
        check(gate.available is False, "죽은 워커는 「사용 가능」이 아니다")
        check(any("종료 코드 3" in m for m in logs), "로그에 종료 코드")
        check(any("시험용 워커 사망" in m for m in logs), "로그에 워커 오류 출력")
        gate.request(FRAME, (1, 1))
        check(not any(n.startswith("req_") for n in os.listdir(tmp)), "죽은 워커에게 요청을 쌓지 않는다")
        gate.stop()
    finally:
        tool_gate._WORKER = old
        shutil.rmtree(tmp, ignore_errors=True)
```

<!-- rep: Rpi5/Demo/selftest/test_tool_gate.py -->
```python
    test_c13_stop_during_poll()

    elapsed = time.time() - t0
```
```python
    test_c13_stop_during_poll()
    test_c14_dead_worker_not_available()

    elapsed = time.time() - t0
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_tool_gate.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 죽은 워커는 「사용 가능」이 아니다
  ❌ 로그에 종료 코드
  ❌ 로그에 워커 오류 출력
  ❌ 죽은 워커에게 요청을 쌓지 않는다
❌ 실패 4건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/tool_gate.py -->
```python
        try:
            self._proc = subprocess.Popen(
                [self._python, _WORKER, self._dir, self._model, str(self._conf)],
                stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL,
            )
```
```python
        try:
            # 🔑 오류 출력은 공유 폴더의 파일로 받는다 — 버리면 워커가 죽은 이유를 볼 수 없었다(C14)
            with open(os.path.join(self._dir, "worker.err"), "wb") as err:
                self._proc = subprocess.Popen(
                    [self._python, _WORKER, self._dir, self._model, str(self._conf)],
                    stdout=subprocess.DEVNULL, stderr=err,
                )
```

<!-- rep: Rpi5/Demo/tool_gate.py -->
```python
    def _ready(self):
        """(잠금 안에서) 워커가 떠 있고 모델 로딩까지 끝났는가."""
        return self._proc is not None and os.path.exists(os.path.join(self._dir, "ready"))
```
```python
    def _ready(self):
        """(잠금 안에서) 워커가 **살아 있고** 모델 로딩까지 끝났는가.

        🔴 워커가 끝나 있으면(OOM 등) 한 번 로그를 남기고 내린 것으로 본다(검토 C14) — 종전에는
           `ready` 파일만 봐서 죽은 워커가 「사용 가능」으로 남았고, 요청 JPEG 만 쌓인 채 공구
           게이트가 로그 없이 영영 안 열렸다. 다음 서브 작업의 start() 가 다시 띄운다.
        """
        if self._proc is None:
            return False
        rc = self._proc.poll()
        if rc is not None:
            self._proc = None
            self._pending.clear()
            self._log(f"[공구] 🔴 추론 워커가 끝났습니다(종료 코드 {rc}) — 공구 지참 단계가 "
                      f"자동으로 넘어가지 않습니다.{self._err_tail()}")
            return False
        return os.path.exists(os.path.join(self._dir, "ready"))

    def _err_tail(self, n=5):
        """워커 오류 출력(worker.err)의 끝 n 줄 — 없으면 빈 문자열."""
        try:
            with open(os.path.join(self._dir, "worker.err"), encoding="utf-8", errors="replace") as f:
                lines = [ln.rstrip() for ln in f if ln.strip()]
        except OSError:
            return ""
        return "".join("\n    " + ln for ln in lines[-n:])
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_tool_gate.py 2>&1 | tail -1`
Expected:
```
✅ 공구 게이트 검증 통과
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/tool_gate.py Demo/selftest/test_tool_gate.py && git commit -m "fix(tool_gate): 워커가 죽으면 「사용 가능」에서 빼고 종료 코드·오류 출력을 로그에 — ready 만 봐서 공구 게이트가 로그 없이 영영 안 열리던 것 (범위 B C14)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 6: C15 — 늦게 온 ACK 를 다음 명령의 ACK 로 읽지 않는다

**Files:** Modify `Rpi5/Demo/interlock.py` · Test `Rpi5/Demo/selftest/test_interlock.py`

**Interfaces:**
- Produces: `_write_now` 가 매 전송 시도 직전에 `ser.reset_input_buffer()` 를 부른다. 시험 대역 `_LateAckSerial`(WARN 의 ACK 가 한도 뒤에 도착 · BLOCK 무응답).
- 🔑 펌웨어는 고치지 않는다(ACK 순번 = spec §9.6 하지 않는 것).

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_interlock.py -->
```python
class _LateAckSerial(_FakeSerial):
    """WARN 의 ACK 가 한도(1초)를 넘겨 늦게 오고, BLOCK 에는 응답이 없는 장치(릴레이 고장)."""

    def __init__(self, port):
        super().__init__(port, 115200)
        self._buf = []
        self._late = 0

    def write(self, b):
        self.writes.append(b.decode().strip())
        if self.writes[-1] == "WARN":
            self._late += 1                              # 한도 안에는 안 온다

    def readline(self):
        if self._buf:
            return self._buf.pop(0)
        if self._late:                                   # 기다림이 끝난 **뒤에** 도착
            self._buf += [b"ACK\n"] * self._late
            self._late = 0
        return b""

    def reset_input_buffer(self):
        self._buf.clear()


def test_c15_late_ack_not_taken_for_block():
    """C15 — 늦게 온 WARN 의 ACK 를 BLOCK 의 ACK 로 읽지 않는다(검토 C15 — 차단 확인이 거짓이 됐다)."""
    print("\n[C15] 늦은 ACK")
    tmp = tempfile.mkdtemp()
    dev = os.path.join(tmp, "ttyACM0")
    open(dev, "w").close()
    box = {"p": dev}
    c = _ctl(box)
    faults = []
    try:
        check(_wait(lambda: c.connected), "처음 붙음")
        with c._lock:
            c._ser = _LateAckSerial(dev)
        c._on_fault = faults.append
        c._write_now("WARN")
        c._write_now("BLOCK")
        check(bool(faults), "BLOCK 차단 확인 실패를 알린다(늦은 WARN-ACK 로 속지 않는다)")
        check(not c.connected, "응답 없는 장치를 「연결됨」으로 두지 않는다")
    finally:
        c.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_interlock.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ BLOCK 차단 확인 실패를 알린다(늦은 WARN-ACK 로 속지 않는다)
  ❌ 응답 없는 장치를 「연결됨」으로 두지 않는다
❌ 실패 2건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/interlock.py -->
```python
            for i in range(attempts):
                try:
                    ser.write((cmd + "\n").encode("ascii"))
                    ser.flush()
```
```python
            for i in range(attempts):
                try:
                    # 🔴 보내기 직전마다 받은 것을 비운다(검토 C15) — ACK 에 순번이 없어, 한도(1초)를
                    #    넘겨 늦게 온 ACK 가 남아 있으면 **다음 명령의 ACK 로 읽혔다**(릴레이가 안
                    #    움직였어도 BLOCK 「(ACK)」). 늦은 ACK 는 버리고 이번 명령의 응답만 본다.
                    ser.reset_input_buffer()
                    ser.write((cmd + "\n").encode("ascii"))
                    ser.flush()
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_interlock.py 2>&1 | tail -1`
Expected:
```
✅ 인터락 검증 통과
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/interlock.py Demo/selftest/test_interlock.py && git commit -m "fix(interlock): 보내기 직전마다 입력 버퍼를 비운다 — 늦게 온 ACK 가 다음 명령(BLOCK)의 ACK 로 읽혀 차단 확인이 거짓이 되던 것 (범위 B C15)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 7: M-4·M-5·M-6·M-10 — 인터락 포트 안내와 포기 규칙

**Files:** Modify `Rpi5/Demo/config.py` · `Rpi5/Demo/interlock.py` · Test `Rpi5/Demo/selftest/test_interlock.py`

**Interfaces:**
- Produces: `config.find_interlock_port()` → `(경로, None)` 또는 `(None, 사유)`(사유 = 「인터록 장치를 못 찾았다 (ESP32 는 인터록이 아니다)」 · 「인터록 후보가 여럿이라 고를 수 없다: […] — SOP_INTERLOCK_PORT=/dev/ttyACMx 로 지정하라」 · 「포트 해석 실패: …」 · 강제 지정은 그대로 돌려준다) · `config.resolve_interlock_port(quiet=False)` = 기동 때 안내를 찍는 감싸개(돌려주는 값 · **안내 글자 모두 종전과 같다** — `fsm_sim` 출력이 이 줄을 담아 관문 비교에 들어간다) · `InterlockController._absent` = 못 찾은 **사유 문자열** 또는 None(안내는 사유가 바뀔 때만) · 지정 경로가 **없으면 못 찾음**(`_fixed_port`·`SOP_INTERLOCK_PORT` 공통). 시험 대역 `_FakeSerial.refuse`(있지만 못 여는 경로).
- 🔴 시험 도우미 `_ctl` 은 `config.find_interlock_port` 도 바꿔 끼운다(인터락이 부르는 함수가 바뀐다 — 고치기 전에도 기존 시험이 돌도록 옛 함수도 그대로 바꿔 끼운다).
- M-10 은 **가드**(전후 통과) — 「찾았지만 열리지 않는 장치 → 몇 번 뒤 포기 · 알림」은 이미 그렇게 돈다. 시험만 없었다.

- [ ] **Step 1: 실패하는 테스트**

<!-- rep: Rpi5/Demo/selftest/test_interlock.py -->
```python
        if not os.path.exists(port):
            raise OSError(f"no such port {port}")
```
```python
        if not os.path.exists(port):
            raise OSError(f"no such port {port}")
        if port in _FakeSerial.refuse:
            raise OSError(f"permission denied {port}")     # 있지만 못 여는 장치(M-4·M-10)
```

<!-- rep: Rpi5/Demo/selftest/test_interlock.py -->
```python
class _FakeSerial:
    opened = []
```
```python
class _FakeSerial:
    opened = []
    refuse = set()
```

<!-- rep: Rpi5/Demo/selftest/test_interlock.py -->
```python
    config.resolve_interlock_port = lambda quiet=False: port_box["p"]
```
```python
    config.resolve_interlock_port = lambda quiet=False: port_box["p"]      # 고치기 전 경로
    config.find_interlock_port = lambda: ((port_box["p"], None) if port_box["p"]
                                          else (None, "장치를 못 찾았다(시험)"))
```

<!-- add: Rpi5/Demo/selftest/test_interlock.py -->
```python
_REAL_FIND = getattr(config, "find_interlock_port", None)   # _ctl 이 바꿔 끼우기 전 원본(M-6)


def test_m4_open_failure_logs_tried_port():
    """M-4 — 열기 실패 로그는 **시도한** 포트를 찍는다(③ 리뷰 M-4 — 옛 값을 찍었다)."""
    print("\n[M-4] 실패 로그의 포트")
    tmp = tempfile.mkdtemp()
    dev = os.path.join(tmp, "ttyACM1")
    open(dev, "w").close()
    _FakeSerial.refuse.add(dev)
    config.resolve_interlock_port = lambda quiet=False: dev      # 고치기 전 경로
    config.find_interlock_port = lambda: (dev, None)
    config.INTERLOCK_PORT = None                                   # 기동 때는 못 찾았다
    logs = []
    c = interlock.InterlockController(enabled=True, log=logs.append)
    try:
        fails = [m for m in logs if "연결 실패" in m]
        check(bool(fails) and f"연결 실패({dev})" in fails[0], "연결 실패 로그에 시도한 포트가 찍힌다")
    finally:
        _FakeSerial.refuse.discard(dev)
        c.close()


def test_m5_fixed_port_missing_is_absent():
    """M-5 — 지정한 포트가 없으면 「못 찾음」 — 포기하지 않고, 나중에 생기면 붙는다(③ 리뷰 M-5)."""
    print("\n[M-5] 지정 포트 없음")
    tmp = tempfile.mkdtemp()
    dev = os.path.join(tmp, "ttyACM9")
    logs = []
    c = interlock.InterlockController(port=dev, enabled=True, log=logs.append)
    try:
        time.sleep(0.4)                                    # 재시도 한도(2회)를 넘길 만큼
        check(not c.gave_up, "지정 포트가 없는 동안에는 포기하지 않는다")
        check(any("지정한 포트가 없다" in m for m in logs), "로그 = 「지정한 포트가 없다」")
        open(dev, "w").close()
        check(_wait(lambda: c.connected), "나중에 생기면 붙는다")
    finally:
        c.close()


def test_m6_many_candidates_says_so():
    """M-6 — 후보가 여럿이면 「꽂히면 붙는다」가 아니라 「여럿이라 고를 수 없다」고 안내한다(③ 리뷰 M-6)."""
    print("\n[M-6] 후보 여럿")
    check(_REAL_FIND is not None, "config.find_interlock_port 가 있다(사유를 함께 돌려준다)")
    if _REAL_FIND is None:
        return
    import types
    from serial.tools import list_ports
    fake = [types.SimpleNamespace(device="/dev/시험A", vid=0x2341, pid=0x0069),
            types.SimpleNamespace(device="/dev/시험B", vid=0x1a86, pid=0x7523)]
    old, old_env = list_ports.comports, os.environ.pop("SOP_INTERLOCK_PORT", None)
    try:
        list_ports.comports = lambda: fake
        port, why = _REAL_FIND()
        check(port is None, "고르지 않는다")
        check("여럿" in (why or "") and "SOP_INTERLOCK_PORT" in (why or ""),
              "사유 = 후보가 여럿 · 직접 지정 안내")
        list_ports.comports = lambda: fake[:1]
        check(_REAL_FIND() == ("/dev/시험A", None), "하나면 그것")
        list_ports.comports = lambda: []
        port, why = _REAL_FIND()
        check(port is None and "못 찾았다" in (why or ""), "없으면 「못 찾았다」")
    finally:
        list_ports.comports = old
        if old_env is not None:
            os.environ["SOP_INTERLOCK_PORT"] = old_env


def test_m10_found_but_unopenable_gives_up():
    """M-10(가드) — 찾았지만 열리지 않는 장치는 몇 번 뒤 포기하고 알린다(③ Review Focus 1 의 나머지 절반)."""
    print("\n[M-10] 못 여는 장치 → 포기")
    tmp = tempfile.mkdtemp()
    dev = os.path.join(tmp, "ttyACM2")
    open(dev, "w").close()
    _FakeSerial.refuse.add(dev)
    config.resolve_interlock_port = lambda quiet=False: dev
    config.find_interlock_port = lambda: (dev, None)
    gave = []
    c = interlock.InterlockController(enabled=True, log=lambda m: None, on_give_up=gave.append)
    try:
        check(_wait(lambda: c.gave_up), "포기한다")
        check(gave == [config.CONNECT_MAX_TRIES], "포기 알림(시도 횟수와 함께)")
        check(not c.connected, "미연결")
    finally:
        _FakeSerial.refuse.discard(dev)
        c.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_interlock.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 연결 실패 로그에 시도한 포트가 찍힌다
  ❌ 지정 포트가 없는 동안에는 포기하지 않는다
  ❌ 로그 = 「지정한 포트가 없다」
  ❌ 나중에 생기면 붙는다
  ❌ config.find_interlock_port 가 있다(사유를 함께 돌려준다)
❌ 실패 5건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/config.py -->
```python
def resolve_interlock_port(quiet=False):
    """인터록 Arduino 의 경로. 확신할 수 없으면 None.

    🔑 인터락 컨트롤러가 **연결할 때마다** 다시 부른다(I1·I2) — 켠 뒤에 꽂거나, 다시 꽂혀
       번호(ttyACM0 → 1)가 바뀌어도 찾는다. quiet=True 면 안내를 찍지 않는다(재시도 루프용).
    """
    say = (lambda m: None) if quiet else print
    forced = os.environ.get("SOP_INTERLOCK_PORT")
    if forced:
        say(f"[config] 인터록 포트 강제 지정: {forced}")
        return forced
    try:
        from serial.tools import list_ports
        from serial_ports import ESP32_S3          # 상수를 복제하지 않는다
        cands = [p.device for p in list_ports.comports()
                 if p.vid is not None and (p.vid, p.pid) != ESP32_S3]
        if len(cands) == 1:
            return cands[0]
        if len(cands) > 1:
            say(f"[config] ⚠️ 인터록 후보가 여럿이라 고를 수 없다: {cands}")
        else:
            say("[config] ⚠️ 인터록 장치를 못 찾았다 (ESP32 는 인터록이 아니다)")
    except Exception as e:
        say(f"[config] ⚠️ 포트 해석 실패: {e}")
    say("[config] → 찾을 때까지 연결하지 않는다(꽂히면 자동으로 붙는다). "
        "강제하려면 SOP_INTERLOCK_PORT=/dev/ttyACMx")
    return None
```
```python
def find_interlock_port():
    """인터록 Arduino 의 경로와 못 찾은 사유 — `(경로, None)` 또는 `(None, 사유)`.

    🔑 인터락 컨트롤러가 **연결할 때마다** 다시 부른다(I1·I2) — 켠 뒤에 꽂거나, 다시 꽂혀
       번호(ttyACM0 → 1)가 바뀌어도 찾는다.
    🔴 사유를 함께 돌려준다(③ 리뷰 M-6) — 후보가 여럿인데 「못 찾았다 — 꽂히면 붙는다」로 안내하면
       꽂아도 소용없는 상황을 가린다.
    강제 지정(`SOP_INTERLOCK_PORT`)은 그대로 돌려준다 — 경로가 있는지는 여는 쪽(interlock)이 본다.
    """
    forced = os.environ.get("SOP_INTERLOCK_PORT")
    if forced:
        return forced, None
    try:
        from serial.tools import list_ports
        from serial_ports import ESP32_S3          # 상수를 복제하지 않는다
        cands = [p.device for p in list_ports.comports()
                 if p.vid is not None and (p.vid, p.pid) != ESP32_S3]
    except Exception as e:
        return None, f"포트 해석 실패: {e}"
    if len(cands) == 1:
        return cands[0], None
    if len(cands) > 1:
        return None, (f"인터록 후보가 여럿이라 고를 수 없다: {cands} — "
                      f"SOP_INTERLOCK_PORT=/dev/ttyACMx 로 지정하라")
    return None, "인터록 장치를 못 찾았다 (ESP32 는 인터록이 아니다)"


def resolve_interlock_port(quiet=False):
    """기동 때 한 번 — 인터록 경로(확신할 수 없으면 None)를 돌려주고 안내를 찍는다.

    🔑 안내 글자는 종전과 같다 — `fsm_sim` 등 config 를 불러오는 도구의 출력이 이 줄을 담는다.
    """
    say = (lambda m: None) if quiet else print
    port, why = find_interlock_port()
    forced = os.environ.get("SOP_INTERLOCK_PORT")
    if forced:
        say(f"[config] 인터록 포트 강제 지정: {forced}")
    elif port is None:
        say(f"[config] ⚠️ {why}")
        say("[config] → 찾을 때까지 연결하지 않는다(꽂히면 자동으로 붙는다). "
            "강제하려면 SOP_INTERLOCK_PORT=/dev/ttyACMx")
    return port
```

<!-- rep: Rpi5/Demo/interlock.py -->
```python
        self._absent  = False     # 장치를 못 찾은 상태 — 안내는 한 번만 · 이 동안은 포기하지 않는다(I1)
```
```python
        self._absent  = None      # 못 찾은 사유(찾았으면 None) — 안내는 사유가 바뀔 때만 · 이 동안은 포기하지 않는다(I1)
```

<!-- rep: Rpi5/Demo/interlock.py -->
```python
        port = self._fixed_port or config.resolve_interlock_port(quiet=True)
        if port is None:
            if not self._absent:
                self._log("[인터락] 장치를 못 찾았다 — 꽂히면 자동으로 붙는다")
                self._absent = True
            return False
        self._absent = False
```
```python
        port, why = ((self._fixed_port, None) if self._fixed_port
                     else config.find_interlock_port())
        if port is not None and not os.path.exists(port):
            # 🔴 지정한 경로(SOP_INTERLOCK_PORT · port=)가 없으면 「못 엶」이 아니라 「못 찾음」이다
            #    (③ 리뷰 M-5) — 못 엶으로 세면 약 15초 뒤 포기해, 켠 뒤에 꽂아도 안 붙었다.
            port, why = None, f"지정한 포트가 없다({port})"
        if port is None:
            if self._absent != why:                  # 🔑 안내는 사유가 바뀔 때만 — 쌓이지 않게(M-6)
                self._log(f"[인터락] {why} — 계속 찾는다(꽂히거나 고르게 되면 붙는다)")
                self._absent = why
            return False
        self._absent = None
```

<!-- rep: Rpi5/Demo/interlock.py -->
```python
                self._log(f"[인터락] 연결 실패({self._port}): {e} — fallback")
```
```python
                self._log(f"[인터락] 연결 실패({port}): {e} — fallback")   # 시도한 포트(M-4)
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_interlock.py 2>&1 | tail -1; python3 selftest/test_precheck.py 2>&1 | tail -1; python3 selftest/test_imports.py >/dev/null 2>&1; echo imports_exit=$?`
Expected:
```
✅ 인터락 검증 통과
✅ 점검 검증 통과
imports_exit=0
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/config.py Demo/interlock.py Demo/selftest/test_interlock.py && git commit -m "fix(interlock): 포트 찾기가 사유를 함께 돌려준다(후보 여럿 안내) · 지정 포트가 없으면 못 찾음(포기하지 않음) · 실패 로그는 시도한 포트 · 못 여는 장치 포기 시험 (③ 리뷰 M-4·M-5·M-6·M-10)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 8: C20·M-2·M-3 — 음성비서 재생

**Files:** Modify `Rpi5/Demo/voice_assistant.py` · Test `Rpi5/Demo/selftest/test_voice_speaker.py`

**Interfaces:**
- Produces: `Speaker.play(key, alog=None)` — 파일을 못 읽으면 로그 후 `False`(예외 없음) · `Speaker._drain(wait=15.0)` — 「재생 완료」·「FAIL」 없이 끝나면 로그 후 `reset()`(명령 채널 버림 · 다음 `send` 가 새로 붙는다) · `open_audio_log` 설명만 사실대로(M-3).
- 🔑 M-3 사실 확인(2026-09-26 · Python 3.13) — `wave` 로 프레임을 쓰던 프로세스를 SIGTERM 으로 끝내도 파일 길이가 온전했다(쓴 프레임 수 = 읽은 프레임 수).

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_voice_speaker.py -->
```python
def test_c20_missing_wav_does_not_kill():
    """C20 — 재생 파일이 없으면 그 재생만 실패 — 음성비서가 죽지 않는다(검토 C20)."""
    print("\n[C20] wav 없음")
    spk, b = _speaker_pair()
    old = va.WAV_DIR
    va.WAV_DIR = tempfile.mkdtemp()
    try:
        ok = spk.play("없는소리")
        check(ok is False, "그 재생만 실패로 돌려준다")
    except Exception as e:                           # noqa: BLE001
        check(False, f"예외로 끝났다 — {type(e).__name__}")
    finally:
        va.WAV_DIR = old
        b.close()


def test_m2_timeout_drops_channel():
    """M-2 — 한도 안에 「재생 완료」가 없으면 명령 채널을 버린다 — 늦은 응답이 다음 재생에 섞이지 않게(③ 리뷰 M-2)."""
    print("\n[M-2] 재생 확인 한도 초과")
    spk, b = _speaker_pair()
    _send_later(b, ["[적재] ok\n"])
    out = spk._drain(wait=0.5)
    check(out == ["[적재] ok"], "한도 안에 받은 응답은 돌려준다")
    check(spk.s is None, "명령 채널을 버린다(다음 전송이 새로 붙는다)")
    b.close()
    spk2, b2 = _speaker_pair()
    _send_later(b2, ["[재생 완료]\n"])
    spk2._drain(wait=3.0)
    check(spk2.s is not None, "제때 끝나면 채널을 그대로 쓴다")
    b2.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_voice_speaker.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 예외로 끝났다 — FileNotFoundError
  ❌ 명령 채널을 버린다(다음 전송이 새로 붙는다)
❌ 실패 2건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/voice_assistant.py -->
```python
    def play(self, key, alog=None):
        path = os.path.join(WAV_DIR, f"{key}.wav")
        if alog:
            alog.played(key, path)
        body, sec = wav_payload(path)
```
```python
    def play(self, key, alog=None):
        path = os.path.join(WAV_DIR, f"{key}.wav")
        try:
            body, sec = wav_payload(path)
        except (OSError, EOFError, wave.Error) as e:
            # 🔴 그 재생만 실패로 적는다 — 종전에는 try 밖이라 wav 가 없으면(`*.wav` 는 git 밖 —
            #    새 클론·sop-pi-2) 첫 답변에서 **데몬 전체가 죽었다**(검토 C20).
            log(f"🔴 재생 파일을 못 읽었다 → {key} · {e}")
            return False
        if alog:
            alog.played(key, path)
```

<!-- rep: Rpi5/Demo/voice_assistant.py -->
```python
        out = []
        end = time.time() + wait
        self.s.settimeout(1.0)
```
```python
        out = []
        done = False
        end = time.time() + wait
        self.s.settimeout(1.0)
```

<!-- rep: Rpi5/Demo/voice_assistant.py -->
```python
                if "재생 완료" in t or "FAIL" in t:
                    break
        self.s.settimeout(30)
        return out
```
```python
                if "재생 완료" in t or "FAIL" in t:
                    done = True
                    break
        if not done:
            # 🔴 끝 응답 없이 끝났으면 명령 채널을 버린다(③ 리뷰 M-2) — 남겨 두면 한도 뒤에 늦게 온
            #    「[재생 완료]」가 **다음 재생의 확인으로 곧바로 읽혀** 그 재생 중에 다시 듣고, 이후
            #    재생 확인이 한 칸씩 밀렸다. 다음 전송이 새로 붙는다(_ensure).
            log(f"⚠️ 재생 확인이 {wait:.0f}초 안에 끝나지 않았다 — 명령 채널을 다시 붙인다")
            self.reset()
            return out
        self.s.settimeout(30)
        return out
```

<!-- rep: Rpi5/Demo/voice_assistant.py -->
```python
    """오디오 기록을 열고 **프로그램이 끝날 때 닫도록** 등록한다(V4).

    🔴 닫지 않으면 마이크_전체.wav 의 머리(길이)가 안 써져 파일이 깨질 수 있다. run() 은
       빠져나가는 길이 여러 개(리허설 종료·Ctrl+C)라 atexit 에 맡긴다.
    """
```
```python
    """오디오 기록을 열고 **프로그램이 끝날 때 닫도록** 등록한다(V4 — 파일 핸들 정리).

    🔑 닫지 않아도 파일은 깨지지 않는다 — 파이썬 `wave` 는 쓸 때마다 머리(길이)를 고쳐 써서,
       SIGTERM 으로 끝나도 마이크_전체.wav 는 온전하다(③ 리뷰 M-3 · 2026-09-26 Python 3.13 확인).
    ⚠️ atexit 은 정상 종료·Ctrl+C 에서만 돈다 — `voice/record_voice_demo.py` 는 `terminate()`
       (SIGTERM)로 끝내므로 그 경로에서는 돌지 않는다(위 이유로 파일은 그래도 온전하다).
    """
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_voice_speaker.py 2>&1 | tail -1`
Expected:
```
✅ 음성 명령 채널 검증 통과
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/voice_assistant.py Demo/selftest/test_voice_speaker.py && git commit -m "fix(voice): 재생 파일을 못 읽으면 그 재생만 실패 · 재생 확인이 한도 안에 안 끝나면 명령 채널을 다시 붙인다 · 녹음 파일 닫기 설명을 사실대로 (범위 B C20 · ③ 리뷰 M-2·M-3)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 9: 검증 관문

**Files:** Create `Rpi5/조사/범위B-20260926/after_A_selftest.txt` · `after_A_gate.txt`

- [ ] **Step 1: 자가 테스트 전부 · 관문**

Run:
```bash
cd /home/pi/sop-project/Rpi5/Demo && D=../조사/범위B-20260926 && \
for t in selftest/test_*.py; do
  py=python3; [ "$(basename $t)" = test_voice_tts.py ] && py=~/env/tts/.venv/bin/python
  timeout 600 $py "$t" >/dev/null 2>&1; rc=$?; echo "$(basename $t) exit=$rc"
done > $D/after_A_selftest.txt; diff $D/before_selftest.txt $D/after_A_selftest.txt; \
python3 test/fsm_sim.py --gate > $D/after_A_gate.txt 2>&1; echo gate_exit=$?; diff $D/before_gate.txt $D/after_A_gate.txt && echo gate_same
```
Expected:
```
3a4
> test_camera_link.py exit=0
gate_exit=0
gate_same
```
- diff 가 새 파일 한 줄 말고 더 있으면 → 중단 규칙 ②. · `gate_exit` ≠ 0 또는 `gate_same` 없음 → 멈춘다(판정기를 고치지 않았는데 값이 바뀌었다).

- [ ] **Step 2: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add 조사/범위B-20260926/after_A_selftest.txt 조사/범위B-20260926/after_A_gate.txt && git commit -m "docs(조사): 범위 B·미룬 사소 ④-A 결과 — 자가 테스트·관문 (수정 후 · 관문 기준선과 같음)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 10: 정본·문서 반영

🔴 통합문서를 열면 `.claude/rules/정본편집.md` 가 붙는다 — 그 규칙을 따른다(현행만 본문 · 폐기는 `docs/변경이력.md` · 미구현은 장 끝 ⏸).

**Files:** Modify `docs/통합문서.md`(§7.4 · §8.1) · `Rpi5/함수목록.md`(§3 새 함수 · 머리 개수) · `Rpi5/조사/README.md` · spec·이 계획서 머리

- [ ] **Step 1: 통합문서 현행 반영**
  - §7.4 표 아래 「타이머 근거」 인용 블록 끝에 한 줄 — **체류·갭메우기 시각 = 카메라가 프레임을 다 받은 시각(단조 시계)** · GUI 가 신호를 처리한 시각이 아니다(GUI 가 멈췄다 풀려도 체류가 흔들리지 않는다) · 판정 입력이 바뀌었으므로 실제 시연 프로그램에서 잰 체류 관련 값은 「수정 전 값」.
  - §7.4 「⏸ 이 장에서 아직 안 된 것」의 「카메라 재연결 직후 첫 프레임이 옛 체류에 이어 붙어 곧바로 경고」 줄 — ③ R5 로 이미 해결됐다(통합문서 §6.3 의 재연결 서술) → 줄을 지운다.
  - §8.1 「고장에 안전하게」 — ①첫 줄의 「못 찾은 동안은 포기하지 않고(안내 로그 한 줄)」 뒤에 「**지정한 포트(`SOP_INTERLOCK_PORT`)가 없어도 못 찾음으로 본다** · 후보가 여럿이면 「여럿이라 고를 수 없다 — 직접 지정」이라고 안내한다(안내는 사유가 바뀔 때만)」 ②`BLOCK` ACK 줄 끝에 「**보내기 직전마다 받은 것을 비운다** — 한도를 넘겨 늦게 온 ACK 를 다음 명령의 확인으로 읽지 않는다」.
- [ ] **Step 2: 함수목록** — §3 새 행(표 형식 그대로 · 줄 = 넣을 때 기준 · 모듈 제목의 함수 수와 머리의 「함수·메서드 N」 도 갱신 · 🔴 `조사/함수목록-생성/build.py` 로 다시 만들지 않는다): `camera_thread._close_sock` · `CameraThread._frame_error` · `ToolGate._start_locked`·`_ready`·`_err_tail`·`_request_locked`·`_poll_locked` · `config.find_interlock_port`. 설명 고칠 행: `CameraThread._recv_worker`(「자기 연결의 소켓만 읽고 자기 연결일 때만 알린다 · 받은 시각을 함께 둔다」) · `resolve_interlock_port`(「기동 때 안내를 찍는 감싸개 — 연결할 때마다 부르는 것은 `find_interlock_port`」 · 부르는 곳에서 `interlock…_open` 을 뺀다) · §1 흐름도의 `_on_roi` 줄 「(roi, 시각, 단계)」 → 「(roi, 프레임을 받은 시각, 단계)」.
- [ ] **Step 3: 조사 README** — 표에 `범위B-20260926/`(④ 기준선·결과) 한 행. (③ 리뷰 M-11 의 빠진 폴더 3개는 ④-B 에서 함께 넣는다 — 같은 표를 두 번 고치지 않도록 ④-B 계획서가 이 행 아래에 더한다.)
- [ ] **Step 4: spec·계획서 머리** — spec 상태 줄 「📝 ④범위 B·미룬 사소(… §9)」 → 「④-A 구현·관문 통과(공동 확인 대기)」 · 이 계획서 상태 줄 → 「구현·관문 통과 — Rpi5 `<Task 0 커밋>`..`<Task 9 커밋>` · 다음 = ④-B」.
- [ ] **Step 5: 커밋** (두 저장소)

```bash
cd /home/pi/sop-project/Rpi5 && git add 함수목록.md 조사/README.md && git commit -m "docs: 범위 B·미룬 사소 ④-A 반영 — 함수목록 새 함수(수신 소켓 닫기·프레임 오류·공구 게이트 잠금·포트 사유) · 조사 README

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
cd /home/pi/sop-project && git add docs/통합문서.md docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md docs/superpowers/plans/2026-09-26-런타임문제수정-4A-코어.md && git commit -m "docs(정본): 범위 B ④-A — §7.4 체류 시각 = 프레임을 받은 시각 · 해결된 ⏸ 정리 · §8.1 지정 포트 없음·후보 여럿 안내·늦은 ACK

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

→ 다음 = **④-B GUI 계획서**(`docs/superpowers/plans/2026-09-26-런타임문제수정-4B-GUI.md`) — 최종 리뷰·공동 확인·`work-close` 는 ④-B 끝에서 ④ 전체를 한 번에.
