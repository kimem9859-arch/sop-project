# 세션 마무리 정리 — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> 이 프로젝트 기본 = **executing-plans 인라인**(CC작업문서 §2.2 — 태스크가 같은 스킬·규칙 파일을 사슬로 고친다).

**Goal:** 마무리 입구를 `work-close` 하나로 모으고 `session-wrap` 을 그 3단계의 내부 절차로 좁히며 낡은 문장을 지금 사실로 고친다.
**Architecture:** 코드 변경 없음 — 스킬 3개 frontmatter·본문, 커맨드 1개 삭제, 규칙(CLAUDE.md)·설명서(CC작업문서) 서술만.
**Tech Stack:** Claude Code 스킬/커맨드(Markdown frontmatter) · bash · git
**Spec:** `docs/superpowers/specs/2026-09-14-세션마무리-정리-design.md`

## Global Constraints

- `session-wrap` **절 번호 불변** — 0단계 자리에 삭제 표시 한 줄만 남긴다
- `session-wrap` frontmatter = `user-invocable: false` · `disable-model-invocation` 은 쓰지 않는다
- 과거 작업로그·spec·plan · `docs(세션마무리)` 커밋 접두어 · `statusline.py` · `session_archive.py` 는 수정하지 않는다
- 절차 본문은 스킬 한 곳 — CLAUDE.md·CC작업문서는 발동·포인터만
- 커밋은 `sop-project` 에 경로 지정(`git add -A` 금지) · 서명 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` · **push 는 Task 4(work-close) 에서 한 번**
- 🔴 관문 미통과면 「완료」라고 쓰지 않는다 · R5 는 새 세션에서 사용자와 공동

## 관문

| 관문 | 위치 | 통과 기준 | 실패 시 |
|---|---|---|---|
| R1 옛 경로 0 | Task 3 | 아래 R1 명령 출력 0줄 | 해당 파일 고친 뒤 재실행 |
| R2 입구 제거 | Task 3 | 커맨드 파일 없음 · frontmatter `user-invocable: false` 1건 | Task 1 되돌아감 |
| R3 낡은 문장 0 | Task 3 | 아래 R3 명령 출력 0줄 | Task 1 되돌아감 |
| R4 기존 테스트 | Task 3 | `test_session_archive.py` 통과 | 원인 규명 전 진행 금지 |
| R5 공동 확인 | Task 4 뒤 새 세션 | "세션 마무리" → `work-close` 가 결말부터 묻는다 · 스킬 목록에 `/세션마무리` 없음 | ⏸ 로 남기고 재설계 |

## 파일

| 파일 | 책임 | 태스크 |
|---|---|---|
| `.claude/commands/세션마무리.md` | 삭제 | 1 |
| `.claude/skills/session-wrap/SKILL.md` | 작업로그 기록 절차(내부) | 1 |
| `.claude/skills/work-close/SKILL.md` | 마무리 입구 | 1 |
| `.claude/skills/session-resume/SKILL.md` | 이어받기 — 기록 포인터 | 1 |
| `CLAUDE.md` §2 | 발동 규칙 | 2 |
| `docs/claude-code-작업문서.md` §1·§2.6·§3.5 | 설명서 | 2 |

---

### Task 1: 스킬·커맨드 — 입구 하나 + `session-wrap` 본문 정리

- [ ] **Step 1: 커맨드 삭제** — `git rm .claude/commands/세션마무리.md`
- [ ] **Step 2: `session-wrap` frontmatter** — `user-invocable: false` 추가 · description = 「`work-close` 3단계에서 부르는 작업로그 기록 절차 — 이번 작업 단위를 4분류(✅⏸▶🔗)로 정리하고 미기록 커밋을 소급 점검해 작업로그에 기록한다. 사용자 입구는 `work-close`.」
- [ ] **Step 3: `session-wrap` 본문(spec §4 표 그대로)**
  - 언제 → 「`work-close` 3단계에서만 불린다」 + 정의 문장 유지
  - 0단계 본문 → `### 0. (2026-09-14 삭제 — 마무리 입구는 work-close)` 한 줄
  - 2.4 끝에 「사용자가 이 대화에서 기록 불필요로 정한 세션은 소급하지 않는다(2026-09-14 1e79f4c8)」
  - 3 「— 그건 SessionEnd 훅이 이미 함」 삭제
  - 6 「CLAUDE.md「작업·커밋 규칙」」 → 「CLAUDE.md §4 커밋」 · 「push는 사용자 요청 시.」 → 「push 는 여기서 하지 않는다 — `work-close` 4단계가 한 번.」
  - 주의 ①②③ spec §4 문구로 교체
- [ ] **Step 4: `work-close`** — `when_to_use` 에 「"세션 마무리"·"세션 정리" 라고 할 때(입구는 이 스킬 하나)」 추가 · 3단계 「(그 스킬의 0단계 안전판은 이 스킬에서 불렸으므로 통과)」 삭제 · 주의 마지막 불릿 → 「brainstorming 없이 한 작업(미계획 작업)의 정본 기록 형식은 아직 없다 — 결과 사실이 있으면 3단계 작업로그에 `- ⏸ ⚠️ **정본 미기록(미계획 작업)**: <사실 한 줄>` 을 남긴다.」
- [ ] **Step 5: `session-resume`** 13줄 「(기록은 `session-wrap`)」 → 「(기록은 `work-close`)」
- [ ] **Step 6: 커밋** — `refactor(skill): 마무리 입구를 work-close 하나로 — session-wrap 내부 절차화 · /세션마무리 삭제 · 낡은 문장 정리`

### Task 2: 규칙·설명서

- [ ] **Step 1: `CLAUDE.md` §2 「세션 마무리」 줄 교체** — 「**⭐ 「세션 마무리」·「작업 마무리」** — 사용자가 `/작업마무리`를 치거나 **"세션 마무리"·"세션 정리"·"작업 마무리"** 라고 하면 `work-close` 를 호출한다(입구는 하나 — 결말 합의가 먼저, 작업로그 기록은 그 안의 `session-wrap`). 정의 = 하루의 끝이 아니라 **한 작업 단위(A작업)가 논리적으로 끝난 시점**(하루에 여러 번 가능). 절차 정본은 그 스킬이다.」
- [ ] **Step 2: CC작업문서** — §1 `session-wrap` 행 = 「스킬 `session-wrap`(사용자 입구 없음) | `work-close` 3단계의 작업로그 기록 절차 — 4분류 · 소급 점검. 🆕 2026-09-14 `/세션마무리` 삭제 · `user-invocable: false`(작업: `docs/superpowers/plans/2026-09-14-세션마무리-정리.md`)」 · `work-close` 행 「`session-wrap` 0단계 안전판 · 」 삭제 · §2.6 마무리 줄 「세션 마무리만은 `/세션마무리` 또는 … 로 기록.」 → 「"세션 마무리"·"세션 정리" 도 같은 입구다. 작업로그는 4분류(✅⏸▶🔗)로」 · 「절차 정본 = 스킬 `session-wrap`」 → 「절차 정본 = `work-close`(작업로그 부분은 `session-wrap`)」 · §3.5 첫 불릿 「(스킬 `session-wrap`·`/세션마무리`)」 → 「(`work-close` 3단계 `session-wrap`)」 · 「세션 마무리 단독 호출도 0단계가 정본 기록 누락을 막는다.」 삭제
- [ ] **Step 3: 커밋** — `docs(규칙): 마무리 입구 하나 — "세션 마무리"도 work-close · CC작업문서 서술 정리`

### Task 3: 관문 R1~R4 + 최종 리뷰

- [ ] **Step 1: R1**
  `grep -rnE '/세션마무리|session-wrap' CLAUDE.md README.md .claude docs/claude-code-작업문서.md | grep -vE 'work-close|내부|user-invocable|3단계|statusline|test_session_archive|session_archive.py'` → 0줄(남으면 한 줄씩 사람 판정해 계획서 밖 이력인지 확인)
- [ ] **Step 2: R2** — `test ! -e .claude/commands/세션마무리.md && grep -c '^user-invocable: false' .claude/skills/session-wrap/SKILL.md` → `1`
- [ ] **Step 3: R3** — `grep -nE 'SessionEnd 훅이 이미|현재 상태|작업·커밋 규칙|push는 사용자 요청 시|쌓인다' .claude/skills/session-wrap/SKILL.md` → 0줄
- [ ] **Step 4: R4** — `python3 .claude/hooks/test_session_archive.py` → 통과
- [ ] **Step 5: 최종 리뷰** — `superpowers:requesting-code-review`(기준 = spec + 사용자 구상 「work-close를 새로 만들었으니 기존 세션마무리의 낡은 규칙·스킬 정리」 · A안) · 중요 이상 반영 후 R1~R4 재실행

### Task 4: work-close 실행

- [ ] 공동 확인(R1~R4 결과 보고 · R5 는 새 세션 ⏸) 뒤 `work-close` — 정본 = CC작업문서 §1(Task 2 에서 반영됨 — 2단계는 spec·plan 경로 확인만) · 작업로그 ⏸ 「새 세션 R5」 · push 한 번
