# Demo 구조 재편 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `Rpi5/Demo/` 최상위에 섞여 있는 문서 4개와 자체점검 4개를 각각 하위 폴더로 옮겨 최상위 파일을 27개에서 19개로 줄인다.

**Architecture:** 파일 이동은 전부 `git mv`로 해 내용 변경을 0으로 만든다. 옮기는 자체점검 4개만 `sys.path`가 부모(`Demo/`)를 가리키도록 고치고, 런타임 코드 13개·실행 스크립트 3개·`test/` 도구 8개는 한 줄도 건드리지 않는다. 이동으로 어긋나는 문서 참조는 같은 작업 단위 안에서 갱신한다.

**Tech Stack:** Python 3.11 (`Rpi5/.venv`), git. 새 의존성 없음.

## Global Constraints

- 설계 정본: `docs/superpowers/specs/2026-07-26-Demo-구조재편-design.md`
- **런타임 코드는 수정 금지** — `config.py`·`camera_thread.py`·`detector.py`·`fsm.py`·`roi_zones.py`·`hand_tracker.py`·`hailo_device.py`·`interlock.py`·`gpio_input.py`·`main.py`·`safety_console.py`·`recipe.py`·`check_model.py`
- **실행 스크립트 수정 금지** — `run_demo.sh`·`run_scenario.sh`·`run_bench_test.sh`
- **`test/` 폴더 수정 금지** — 측정·분석 도구 8개 전부
- 파일 이동은 반드시 `git mv` (git이 rename으로 인식해야 함)
- 파이썬 실행은 `Rpi5/Demo/` 에서 `../.venv/bin/python`
- 기준선 = 자체점검 **25개 통과** (`test_fsm` 14 · `test_recipe` 5 · `test_hoi_sim` 4 · `test_imports` 2)
- 커밋 서명 끝에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`
- push 하지 않는다 (사용자 요청 시에만)

## File Structure

**Task 1이 만드는 것**
- `Rpi5/Demo/selftest/test_fsm.py` — FSM 전이 단위 테스트 (이동)
- `Rpi5/Demo/selftest/test_hoi_sim.py` — HOI→FSM 통합 시뮬레이션 (이동)
- `Rpi5/Demo/selftest/test_recipe.py` — 레시피 로더 테스트 (이동)
- `Rpi5/Demo/selftest/test_imports.py` — 시연 경로 스모크 테스트 (이동)

**Task 2가 만드는 것**
- `Rpi5/Demo/docs/TESTING_FSM.md` — 테스트 절차 (이동 + 실행법 갱신)
- `Rpi5/Demo/docs/labeling_guide.md` — 라벨링 기준 (이동)
- `Rpi5/Demo/docs/dataset_pipeline.md` — 데이터 파이프라인 (이동)
- `Rpi5/Demo/docs/augmentation_plan.md` — 증강 스펙 (이동)
- `sop-project/CLAUDE.md:220-222` — 위 3개 문서의 포인터 경로 갱신 (수정)

---

### Task 1: 자체점검 4개를 `selftest/` 로 이동

**Files:**
- Create: `Rpi5/Demo/selftest/` (폴더)
- Move: `Rpi5/Demo/test_fsm.py` → `Rpi5/Demo/selftest/test_fsm.py`
- Move: `Rpi5/Demo/test_hoi_sim.py` → `Rpi5/Demo/selftest/test_hoi_sim.py`
- Move: `Rpi5/Demo/test_recipe.py` → `Rpi5/Demo/selftest/test_recipe.py`
- Move: `Rpi5/Demo/test_imports.py` → `Rpi5/Demo/selftest/test_imports.py`
- Modify: 위 4개 파일의 `sys.path` 블록과 docstring 실행법

**Interfaces:**
- Consumes: `Demo/` 최상위의 `fsm`·`roi_zones`·`recipe`·`camera_thread`·`safety_console`·`main` 모듈 (변경 없음)
- Produces: `Demo/selftest/` 경로. Task 2의 `TESTING_FSM.md` 실행법이 이 경로를 참조한다.

- [ ] **Step 1: 기준선 확인 — 이동 전 25개가 통과하는지 기록**

```bash
cd /home/pi/sop-project/Rpi5/Demo
../.venv/bin/python test_fsm.py     | tail -1
../.venv/bin/python test_recipe.py  | tail -1
../.venv/bin/python test_hoi_sim.py | tail -1
../.venv/bin/python test_imports.py | tail -1
```

Expected:
```
14/14 passed
5/5 passed
4/4 passed
2/2 passed
```

합계 25. 여기서 다른 숫자가 나오면 **중단하고 보고할 것** — 이동 전부터 깨져 있다는 뜻이라 이 계획의 전제가 무너진다.

- [ ] **Step 2: `git mv` 로 4개 이동**

```bash
cd /home/pi/sop-project/Rpi5
mkdir -p Demo/selftest
git mv Demo/test_fsm.py      Demo/selftest/test_fsm.py
git mv Demo/test_hoi_sim.py  Demo/selftest/test_hoi_sim.py
git mv Demo/test_recipe.py   Demo/selftest/test_recipe.py
git mv Demo/test_imports.py  Demo/selftest/test_imports.py
git status --short
```

Expected: 4줄 전부 `R ` (rename)로 표시.

- [ ] **Step 3: 이동만으로 깨지는지 확인 (실패해야 정상)**

```bash
cd /home/pi/sop-project/Rpi5/Demo
../.venv/bin/python selftest/test_fsm.py 2>&1 | tail -3
```

Expected: `ModuleNotFoundError: No module named 'fsm'`

`sys.path`가 아직 `selftest/` 자신을 가리키고 있어 부모의 `fsm.py`를 못 찾는다. 이 실패를 확인하는 것이 다음 단계가 진짜 고침인지 보증한다.

- [ ] **Step 4: `test_fsm.py` 경로 수정**

`Rpi5/Demo/selftest/test_fsm.py` 4번째 줄(docstring 안 실행법)을 바꾼다:

```
실행: python -m pytest Demo/selftest/test_fsm.py -q   또는   python Demo/selftest/test_fsm.py
```

10번째 줄을 다음으로 교체한다:

```python
_DEMO_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _DEMO_DIR)
```

(`test/bench_detector.py:56` 이 쓰는 것과 같은 관용구다.)

- [ ] **Step 5: `test_hoi_sim.py` 경로 수정**

`Rpi5/Demo/selftest/test_hoi_sim.py` 7번째 줄(docstring):

```
실행: python Demo/selftest/test_hoi_sim.py
```

14번째 줄을 교체:

```python
_DEMO_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _DEMO_DIR)
```

- [ ] **Step 6: `test_recipe.py` 경로 수정**

`Rpi5/Demo/selftest/test_recipe.py` 3번째 줄(docstring):

```
실행: python Demo/selftest/test_recipe.py
```

11번째 줄을 교체:

```python
_DEMO_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _DEMO_DIR)
```

- [ ] **Step 7: `test_imports.py` 경로 수정 (두 곳)**

이 파일은 고칠 곳이 **두 군데**다. `safety_console.py` 를 소스로 읽어 AST 파싱하기 때문에 `sys.path` 말고 파일 경로가 하나 더 있다. 한쪽만 고치면 `FileNotFoundError` 가 난다.

⚠️ **줄 번호로 찾지 말고 내용으로 찾을 것** — 첫 번째 교체가 1줄을 2줄로 늘려 뒤쪽 줄 번호가 밀린다.

첫 번째, `sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))` 를 찾아 교체:

```python
_DEMO_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _DEMO_DIR)
```

두 번째, `test_camera_thread_exports` 함수 안의 `src = os.path.join(...)` 줄을 찾아 교체:

```python
    src = os.path.join(_DEMO_DIR, "safety_console.py")
```

교체 후 이 파일에 `os.path.dirname(os.path.abspath(__file__))` 라는 표현이 **하나도 남지 않아야** 한다. 확인:

```bash
cd /home/pi/sop-project/Rpi5/Demo
grep -n "dirname" selftest/test_imports.py
```

Expected: `_DEMO_DIR = ...` 한 줄만 나온다.

- [ ] **Step 8: 4개 전부 통과 확인**

```bash
cd /home/pi/sop-project/Rpi5/Demo
../.venv/bin/python selftest/test_fsm.py     | tail -1
../.venv/bin/python selftest/test_recipe.py  | tail -1
../.venv/bin/python selftest/test_hoi_sim.py | tail -1
../.venv/bin/python selftest/test_imports.py | tail -1
```

Expected: Step 1과 **똑같이** `14/14` · `5/5` · `4/4` · `2/2`.

숫자가 하나라도 다르면 중단하고 보고할 것.

- [ ] **Step 9: `test/` 측정 도구가 여전히 부모 모듈을 찾는지 확인**

```bash
cd /home/pi/sop-project/Rpi5/Demo
../.venv/bin/python -c "
import sys, os
sys.path.insert(0, os.path.abspath('test'))
sys.path.insert(0, os.path.abspath('.'))
import config, roi_zones
print('config·roi_zones import OK')
print('HEF:', os.path.basename(config.HEF_MODEL_PATH), os.path.exists(config.HEF_MODEL_PATH))
"
```

Expected:
```
config·roi_zones import OK
HEF: console_v2.hef True
```

`test/` 폴더는 건드리지 않았으므로 통과해야 정상이다. 실패하면 이동이 의도치 않게 최상위를 건드렸다는 뜻이다.

- [ ] **Step 10: rename 인식 + 내용 변경 범위 확인**

```bash
cd /home/pi/sop-project/Rpi5
git status --short
git diff --stat
```

Expected: 4개 전부 `R` 로 표시되고, `git diff --stat` 의 변경 줄은 **파일당 3~5줄**(docstring 1줄 + sys.path 2줄, `test_imports.py`만 +1줄)에 그친다. 그보다 크면 의도치 않은 수정이 섞인 것이다.

- [ ] **Step 11: 커밋**

```bash
cd /home/pi/sop-project/Rpi5
git add -A Demo/selftest
git commit -m "$(cat <<'EOF'
refactor(구조): 자체점검 4개를 Demo/selftest/ 로 분리

Demo 최상위의 혼재를 푸는 첫 단계. PyQt6·카메라 없이 도는 자체점검
4개를 selftest/ 로 옮겼다. 폴더명이 tests/ 가 아닌 이유는 기존 test/
(측정·분석 도구)와 한 글자 차이라 사람도 셸 탭 완성도 혼동하기 때문.

옮기면서 고친 것은 sys.path 뿐 — test/ 도구들이 쓰는 _DEMO_DIR 관용구를
그대로 따랐다. test_imports.py 는 safety_console.py 를 소스로 읽어 AST
파싱하므로 경로가 두 곳이라 둘 다 고쳤다.

런타임 코드 13개·실행 스크립트 3개·test/ 도구 8개는 무변경.

검증: 25개 전부 통과(test_fsm 14·test_recipe 5·test_hoi_sim 4·
test_imports 2) — 이동 전 기준선과 동일.

설계: docs/superpowers/specs/2026-07-26-Demo-구조재편-design.md

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
git log --oneline -1
```

---

### Task 2: 문서 4개를 `docs/` 로 이동하고 참조를 갱신

**Files:**
- Create: `Rpi5/Demo/docs/` (폴더)
- Move: `Rpi5/Demo/TESTING_FSM.md` → `Rpi5/Demo/docs/TESTING_FSM.md`
- Move: `Rpi5/Demo/labeling_guide.md` → `Rpi5/Demo/docs/labeling_guide.md`
- Move: `Rpi5/Demo/dataset_pipeline.md` → `Rpi5/Demo/docs/dataset_pipeline.md`
- Move: `Rpi5/Demo/augmentation_plan.md` → `Rpi5/Demo/docs/augmentation_plan.md`
- Modify: `Rpi5/Demo/docs/TESTING_FSM.md` (실행법 블록 + 파일 표)
- Modify: `sop-project/CLAUDE.md:220-222` (포인터 3개)

**Interfaces:**
- Consumes: Task 1이 만든 `Demo/selftest/` 경로 — `TESTING_FSM.md` 의 실행법이 이 경로를 적는다.
- Produces: 없음 (문서만)

- [ ] **Step 1: `git mv` 로 4개 이동**

```bash
cd /home/pi/sop-project/Rpi5
mkdir -p Demo/docs
git mv Demo/TESTING_FSM.md       Demo/docs/TESTING_FSM.md
git mv Demo/labeling_guide.md    Demo/docs/labeling_guide.md
git mv Demo/dataset_pipeline.md  Demo/docs/dataset_pipeline.md
git mv Demo/augmentation_plan.md Demo/docs/augmentation_plan.md
git status --short
```

Expected: 4줄 전부 `R `.

- [ ] **Step 2: `TESTING_FSM.md` 의 실행법 블록 갱신**

`Rpi5/Demo/docs/TESTING_FSM.md` 의 52~58번째 줄 코드블록을 다음으로 교체한다:

````
```bash
cd Demo
../.venv/bin/python selftest/test_fsm.py     # FSM 전이 14종 → "14/14 passed"
../.venv/bin/python selftest/test_recipe.py  # 레시피 5종    → "5/5 passed"
../.venv/bin/python selftest/test_hoi_sim.py # 인식→판정 4종 → "4/4 passed"
../.venv/bin/python selftest/test_imports.py # 시연 경로 2종 → "2/2 passed"
../.venv/bin/python recipe.py                # 레시피 내용 출력 확인
```
````

세 가지가 함께 바뀐다:
1. 경로에 `selftest/` 추가 (`recipe.py` 는 최상위에 남으므로 그대로)
2. **`test_fsm` 을 "11종/11/11" 로 적어둔 것이 실제와 다르다 — 14가 맞다.** 지금 이 줄을 고치는 김에 바로잡는다
3. 목록에 없던 `test_imports.py` 추가 (2026-07-22 신설분이 문서에 반영 안 돼 있었다)

- [ ] **Step 3: `TESTING_FSM.md` 의 파일 표 갱신**

같은 파일 32번째 줄:

```
| `test_fsm.py` / `test_hoi_sim.py` / `test_recipe.py` | 단위·통합 테스트 | 신규 |
```

를 다음으로 교체:

```
| `selftest/` (`test_fsm`·`test_hoi_sim`·`test_recipe`·`test_imports`) | 자체점검 — HW 없이 도는 테스트 | 신규 |
```

- [ ] **Step 4: 실행법이 실제로 도는지 문서대로 따라해 확인**

```bash
cd /home/pi/sop-project/Rpi5/Demo
../.venv/bin/python selftest/test_fsm.py     | tail -1
../.venv/bin/python selftest/test_recipe.py  | tail -1
../.venv/bin/python selftest/test_hoi_sim.py | tail -1
../.venv/bin/python selftest/test_imports.py | tail -1
../.venv/bin/python recipe.py                | tail -2
```

Expected: `14/14` · `5/5` · `4/4` · `2/2` 그리고 `recipe.py` 가 레시피 내용을 출력.

문서에 적은 명령이 실제로 도는지 확인하는 단계다. 안 돌면 문서가 거짓말이 된다.

- [ ] **Step 5: Rpi5 커밋**

```bash
cd /home/pi/sop-project/Rpi5
git add -A Demo/docs
git commit -m "$(cat <<'EOF'
refactor(구조): 문서 4개를 Demo/docs/ 로 분리

TESTING_FSM·labeling_guide·dataset_pipeline·augmentation_plan 을
Demo/docs/ 로 옮겼다. 이로써 Demo 최상위 파일이 27 → 19개가 된다.

TESTING_FSM.md 는 이동하면서 본문도 갱신 — 실행법에 selftest/ 경로를
반영하고, 함께 stale 2건을 바로잡았다: test_fsm 이 "11종/11/11" 로
적혀 있었으나 실제는 14, 그리고 2026-07-22 신설된 test_imports.py 가
목록에 빠져 있었다.

검증: 문서에 적은 명령을 그대로 따라 실행해 25개 통과 확인.

설계: docs/superpowers/specs/2026-07-26-Demo-구조재편-design.md

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
git log --oneline -1
```

- [ ] **Step 6: 루트 `CLAUDE.md` 포인터 3개 갱신**

`sop-project/CLAUDE.md` 220~222번째 줄에서 경로만 바꾼다 (설명 문구는 그대로).

- 220줄: `Rpi5/Demo/labeling_guide.md` → `Rpi5/Demo/docs/labeling_guide.md`
- 221줄: `Rpi5/Demo/dataset_pipeline.md` → `Rpi5/Demo/docs/dataset_pipeline.md`
- 222줄: `Rpi5/Demo/augmentation_plan.md` → `Rpi5/Demo/docs/augmentation_plan.md`

- [ ] **Step 7: 깨진 경로가 남아 있는지 전수 확인**

```bash
cd /home/pi/sop-project
grep -rn "Demo/labeling_guide\|Demo/dataset_pipeline\|Demo/augmentation_plan\|Demo/TESTING_FSM" \
  CLAUDE.md Rpi5/CLAUDE.md README.md dev/ 2>/dev/null | grep -v "Demo/docs/"
echo "--- 위에 아무것도 안 나오면 정상 ---"
```

Expected: 아무 줄도 안 나옴.

`docs/작업로그.md` 와 `docs/타임라인.md` 는 **의도적으로 제외**했다 — 시간순 저널이라 그 시점의 사실을 보존하는 것이 맞다(설계 §5).

- [ ] **Step 8: 최상위 파일 수 확인**

```bash
cd /home/pi/sop-project/Rpi5/Demo
ls *.py *.md *.sh *.npz *.png *.json 2>/dev/null | wc -l
```

Expected: `19`

- [ ] **Step 9: sop-project 커밋**

```bash
cd /home/pi/sop-project
git add CLAUDE.md
git commit -m "$(cat <<'EOF'
docs(경로): Demo 문서 3개의 포인터를 Demo/docs/ 로 갱신

Rpi5 쪽에서 labeling_guide·dataset_pipeline·augmentation_plan 을
Demo/docs/ 로 옮긴 것에 맞춰 CLAUDE.md 포인터를 따라 고쳤다.
"폴더·경로 바꾸면 README·CLAUDE 같이 갱신" 규칙 이행.

작업로그·타임라인의 과거 언급은 고치지 않았다 — 시간순 저널이라
그 시점의 사실을 보존하는 것이 맞다.

설계: docs/superpowers/specs/2026-07-26-Demo-구조재편-design.md

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
git log --oneline -1
git status --short
```

Expected: 커밋 후 두 repo 모두 작업트리 깨끗.

---

## 완료 조건

전부 만족해야 끝난 것이다.

1. `Rpi5/Demo/` 최상위 파일 **19개** (숨김 제외)
2. 자체점검 **25개 통과** — 이동 전 기준선과 동일
3. `git log` 에 rename 3커밋 (Rpi5 2 · sop-project 1), 작업트리 깨끗
4. `TESTING_FSM.md` 에 적힌 명령을 그대로 따라 실행해 통과
5. `Demo/labeling_guide` 류의 옛 경로가 CLAUDE.md·README·dev/ 어디에도 없음
6. 런타임 코드·실행 스크립트·`test/` 도구는 `git diff` 에 **한 줄도 나타나지 않음**

## 롤백

```bash
cd /home/pi/sop-project/Rpi5 && git revert --no-edit HEAD HEAD~1
cd /home/pi/sop-project    && git revert --no-edit HEAD
```

런타임은 애초에 영향권 밖이라 롤백 없이도 시연 경로는 계속 돈다.
