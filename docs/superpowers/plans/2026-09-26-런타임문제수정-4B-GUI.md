# 실제 시연 프로그램 문제 수정 ④-B GUI Implementation Plan

> 상태: ✅ **구현·관문 통과(2026-09-26 · 세션 `baa57676`→`4c6a495c` 이어짐 · 공동 확인 대기)** — Rpi5 `5365156`..`32a4304` · Expected 18/18(실제 저장소) · 📝 작성(2026-09-26 · 세션 `baa57676`) · 🔑 계획서의 교체·추가 지시와 Run 명령을 **④-A 가 적용된 새 복사본에서 Task 1~9 그대로 실행**해 아래 Expected 를 얻었다(저장소 미반영) · 드라이런이 잡은 계획 결함 = 발광 시험이 임시 부모 위젯을 붙들지 않아 먼저 지워져 `test_overlay.py` 전체가 멈췄다 → 부모를 변수에

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 화면 어둡게 하기(U13)·테마 전환 뒤 옛 색(U14)·게이지 번쩍임(U15)·종료 정리(U16)·레시피 검사(U17)·겹치는 알림 창(U20)·차단 중 EMO 알림(M-9)을 고치고, ④ 전체를 최종 리뷰·공동 확인·마무리한다.

**Architecture:** 스크림은 호출부가 켜고 끄지 않고 **한 곳에서 상태로 계산**한다. 테마가 바뀌면 각 패널이 **마지막으로 그린 내용**을 새 색으로 다시 그린다(단계 행 · 쌓인 알림 · 녹화 줄 · 맥박 · 발광). 알림 창은 종류마다 하나를 재사용한다. 레시피 검사는 GUI 가 직접 읽는 키를 보장한다.

**Tech Stack:** Python 3.13 · PyQt6(오프스크린) · 실제 `SafetyConsole`(GPIO·인터락 끔 — `make_console`)

**Spec:** `docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md` §9(9.1·9.3~9.7) — 이 계획서 = ④-B · 앞 계획서 = `plans/2026-09-26-런타임문제수정-4A-코어.md`

## Global Constraints

- 코드 저장소 = `/home/pi/sop-project/Rpi5`(별도 git) · 문서 = `/home/pi/sop-project`. 커밋은 **경로 지정 add** 만.
- 커밋 서명 = `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>` + `Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL` (실행 세션이 다르면 그 세션 값).
- 자가 테스트는 **일괄 러너가 없다** — `Rpi5/Demo` 에서 `selftest/test_*.py` 개별 실행. `test_voice_tts` 만 `~/env/tts/.venv/bin/python`. 🔴 **종료 코드는 `rc=$?` 로 먼저 받는다**.
- 🔴 **Hailo 를 쓰는 시험(`test_console_flow` 등)을 다른 GUI·측정 실행과 동시에 돌리지 않는다.**
- 🔴 **고치지 않는 것** — `fsm.py` · 측정 도구 · 펌웨어 · C18(§9.5 — 정본에 동작만 적는다) · U19(§9.1 — 도달 경로 없음).
- 🔴 **시험이 테마·발광 설정을 바꾸면 끝에서 되돌린다**(`theme.set_theme("dark")` · `theme.GLOW_BORDER`) — 같은 파일의 뒤 시험이 기본값을 전제한다.
- 🔴 **push 는 Task 11 의 `work-close` 에서 한 번.**

## 관문

- **🔒 홀드아웃** — 판정 임계·측정 데이터를 건드리지 않는다. 판정기를 고치지 않으므로 `fsm_sim --gate` 출력이 ④ 기준선(`조사/범위B-20260926/before_gate.txt`)과 **글자까지 같아야** 한다. 합격선 = 태스크마다 적힌 Expected(복사본 드라이런으로 **구현 전에 고정**).
- **검증 관문** — ①태스크마다 새 테스트가 **고치기 전 실패 → 고친 뒤 통과** ②Task 9: 자가 테스트가 ④-A 결과(`after_A_selftest.txt`)와 같음(새 시험 파일 없음) · `fsm_sim --gate` 기준선과 같음.
- **중단 규칙** — ①확인 단계가 Expected 와 다르면 다음 태스크로 넘어가지 않는다 ②이번 변경과 무관한 기존 테스트가 깨지면 멈추고 보고한다 ③같은 수정을 3번 시도해도 안 되면 멈추고 보고한다 ④콘솔 시험이 GPIO·인터락을 켠 채 만들어지면(실제 릴레이에 명령) 즉시 멈춘다.

## Review Focus

1. **결과창이 뜬 채로 배너가 생기거나 사라질 때** — 스크림은 결과창·열린 창·배너 중 하나라도 있으면 켜진다(계산 한 곳) → `test_u13_dim_follows_state`(Task 1)가 메뉴·차단 조합을 고정 · 결과창은 기존 `test_scenario_runs_to_end` 가 지킨다.
2. **애니메이션을 끈 설정(`SOP_UI_ANIM=0`)에서 테마 전환** — 맥박이 멈춰 있으므로 다시 시작하지 않고 `_paint` 가 새 색을 칠한다 → `test_banner_both_themes`(기존) · `test_u14_block_pulse_follows_theme`(Task 3, 켠 쪽).
3. **알림 창을 확인(OK)으로 닫은 뒤 다시 거부될 때** — 같은 창이 다시 뜬다(숨김 → 표시) → `test_u20_refusal_popups_do_not_stack`(Task 7).
4. **서브 작업이 경고로 멈춰 있다가 취소될 때의 게이지** — 숨길 때 진행률이 0 이 되고, 멈춤 표시(G1)는 그대로 → `test_u15_gauge_starts_empty_next_time`(Task 4) · `test_gauge_shows_paused`(기존).
5. **레시피의 EMO 이름이 빠진 옛 레시피** — config 값으로 채워 그대로 돈다 → `test_u17_emo_button_filled_and_fixed`(Task 6).

## 계획서 표기

④-A 계획서와 같다 — `<!-- rep: <경로> -->`(원문 → 새 내용 · 정확히 한 곳) · `<!-- add: <경로> -->`(`if __name__ == "__main__":` 바로 앞) · `<!-- new: <경로> -->` · 「…통과」 줄 끝 `(0.00Xs)` 는 비교하지 않는다 · 드라이런 = `python3 ~/lab/plan-dryrun/plan_dryrun.py <계획서> <복사본 루트> [태스크,…]`.

---

### Task 1: U13 — 스크림은 상태에서 계산한다

**Files:** Modify `Rpi5/Demo/safety_console.py` · Test `Rpi5/Demo/selftest/test_console_flow.py`

**Interfaces:**
- Produces: `SafetyConsole._refresh_dim()` — `_dim_others(열린 창이 있음 or 배너가 떠 있음)`. 🔴 종전 `_dim_others(True|False|self._any_sheet_open())` 호출 12곳을 **모두** 이것으로 바꾼다(Step 3 의 Run 이 개수를 확인한다).

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def test_u13_dim_follows_state():
    """U13 — 어둡게 하기는 상태에서 계산한다 — 메뉴를 연 채 손이 들락거려도, 차단 중 메뉴를 열었다 닫아도 유지된다(리뷰 U13)."""
    print("\n[U13] 어둡게 하기")
    win = make_console()
    win._on_cta()
    win._toggle_menu(True)
    t0 = time.time()
    win.fsm.update_vision("B1", t0)                  # 손 진입 → MONITOR
    win.fsm.update_vision(None, t0 + 5)              # 이탈 → PROCESS_RUN
    check(not win.scrim.isHidden(), "메뉴가 열린 동안 손이 들락거려도 어둡게 한다")
    win._toggle_menu(False)
    check(win.scrim.isHidden(), "메뉴를 닫으면 걷힌다")
    key(win, "3")                                    # 위반 BLOCK
    win._toggle_menu(True)
    win._toggle_menu(False)
    check(not win.scrim.isHidden(), "차단 중 메뉴를 열었다 닫아도 배너 뒤 어둡게 하기가 남는다")
    win.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 메뉴가 열린 동안 손이 들락거려도 어둡게 한다
  ❌ 차단 중 메뉴를 열었다 닫아도 배너 뒤 어둡게 하기가 남는다
❌ 실패 2건
```

- [ ] **Step 3: 구현**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 -c "import re;p='safety_console.py';s=open(p,encoding='utf-8').read();pat=re.compile(r'self\._dim_others\((?:True|False|self\._any_sheet_open\(\))\)');print(len(pat.findall(s)));open(p,'w',encoding='utf-8').write(pat.sub('self._refresh_dim()',s))"`
Expected: `12`

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
    def _dim_others(self, dimmed):
        """열린 패널·경고가 주인공이 되도록 뒤를 어둡게 한다 (design §4.7·§4.5).
```
```python
    def _refresh_dim(self):
        """스크림을 **지금 상태에서 계산**해 켜고 끈다 — 열린 창이 있거나 배너(경고·공구 경고·차단)가
        떠 있으면 어둡게(리뷰 U13).

        🔴 호출부가 켜짐·꺼짐을 넘기지 않는다 — 종전에는 메뉴를 연 채 손이 들락거리면
           (MONITOR↔PROCESS_RUN) 상태 분기가 스크림을 껐고, 차단 중 메뉴를 열었다 닫으면 배너 뒤
           어둡게 하기가 사라졌다. `_sync_cta_visibility` 와 같은 원칙(기억하지 말고 계산).
        """
        self._dim_others(self._any_sheet_open() or self.alert.mode is not None)

    def _dim_others(self, dimmed):
        """열린 패널·경고가 주인공이 되도록 뒤를 어둡게 한다 (design §4.7·§4.5).
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | tail -1`
Expected:
```
✅ 글라스 UI 흐름 통과
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/safety_console.py Demo/selftest/test_console_flow.py && git commit -m "fix(gui): 스크림을 상태에서 계산한다 — 메뉴를 연 채 손이 들락거리거나 차단 중 메뉴를 열었다 닫으면 어둡게 하기가 사라지던 것 (범위 B U13)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 2: U14 ①②⑤ — 테마를 바꾸면 마지막으로 그린 글자를 새 색으로

**Files:** Modify `Rpi5/Demo/overlay.py`(StatusPanel) · `Rpi5/Demo/overlay_menu.py`(NotifyPanel · RecordPanel) · Test `Rpi5/Demo/selftest/test_overlay.py` · `Rpi5/Demo/selftest/test_overlay_menu.py`

**Interfaces:**
- Produces: `StatusPanel._last_view`(`update_view` 인자 튜플 · 처음 None) · `StatusPanel.apply_theme()`(판 + 마지막 상태 다시 그리기 — 같은 단계라 애니메이션 없음) · `NotifyPanel._paint(lbl, token, weight, extra="")` · `NotifyPanel._painted`(칠한 라벨 목록) · `RecordPanel._rec_path`·`_rec_elapsed`(마지막 `set_state` 값).

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_overlay.py -->
```python
def test_u14_status_rows_follow_theme():
    """U14 ① — 테마를 바꾸면 공정 단계 행도 새 색으로 다시 칠한다(다음 전이까지 옛 색으로 남던 것)."""
    print("\n[U14] 공정 단계 행 테마")
    theme.set_theme("dark")
    try:
        host = QWidget()
        p = StatusPanel(STEPS, host)
        p.update_view("PROCESS RUN", 2)
        theme.set_theme("light")
        p.apply_theme()
        check(theme.C("current") in p._rows[1].styleSheet(), "현재 단계 행이 화이트 테마 색")
        check(theme.C("done") in p._rows[0].styleSheet(), "완료 행이 화이트 테마 색")
    finally:
        theme.set_theme("dark")
```

<!-- add: Rpi5/Demo/selftest/test_overlay_menu.py -->
```python
def test_u14_notify_rows_follow_theme():
    """U14 ② — 이미 쌓인 알림도 테마를 바꾸면 새 색으로(다크 글자가 흰 바탕에 묻히던 것)."""
    print("\n[U14] 쌓인 알림 테마")
    from PyQt6.QtWidgets import QLabel
    theme.set_theme("dark")
    try:
        host = QWidget()
        n = NotifyPanel(host)
        n.push("work", "옛 알림", "부가 설명")
        theme.set_theme("light")
        n.apply_theme()
        title = [lb for lb in n._rows[-1].findChildren(QLabel) if lb.text() == "옛 알림"][0]
        check(theme.C("text") in title.styleSheet(), "제목 글자가 화이트 테마 색")
    finally:
        theme.set_theme("dark")


def test_u14_record_panel_keeps_file_on_theme():
    """U14 ⑤ — 녹화 중 테마를 바꿔도 파일 이름·경과가 지워지지 않는다."""
    print("\n[U14] 녹화 패널 테마")
    from overlay_menu import RecordPanel
    try:
        host = QWidget()
        p = RecordPanel(host)
        p.set_state(True, "/tmp/시연_01.mp4", 12)
        theme.set_theme("light")
        p.apply_theme()
        check(p._note.text() == "시연_01.mp4", f"파일 이름 줄 = {p._note.text()!r}")
        check("12초" in p._state.text(), f"경과 = {p._state.text()!r}")
    finally:
        theme.set_theme("dark")
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_overlay.py 2>&1 | grep -E "^  ❌|^❌"; python3 selftest/test_overlay_menu.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 현재 단계 행이 화이트 테마 색
  ❌ 완료 행이 화이트 테마 색
❌ 실패 2건
  ❌ 제목 글자가 화이트 테마 색
  ❌ 파일 이름 줄 = ''
  ❌ 경과 = '● 녹화 중 — 0초'
❌ 실패 3건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/overlay.py -->
```python
        self._steps = list(steps)
        self._prev_step = None          # 애니메이션 트리거용 직전 expected_step
```
```python
        self._steps = list(steps)
        self._prev_step = None          # 애니메이션 트리거용 직전 expected_step
        self._last_view = None          # 마지막 update_view 인자 — 테마 전환 때 다시 그린다(U14)
```

<!-- rep: Rpi5/Demo/overlay.py -->
```python
        """state_value = FSM State.value 문자열. sub_running 이면 현재 단계를 '진행 중'으로."""
        started = state_value != "IDLE"
```
```python
        """state_value = FSM State.value 문자열. sub_running 이면 현재 단계를 '진행 중'으로."""
        self._last_view = (state_value, expected_step, sub_running)
        started = state_value != "IDLE"
```

<!-- rep: Rpi5/Demo/overlay.py -->
```python
    def relayout(self, parent_rect):
        p = _POS["status"]
```
```python
    def apply_theme(self):
        """판과 함께 **마지막으로 그린 상태를 새 색으로** 다시 그린다(리뷰 U14 ①).

        색은 update_view 가 칠하므로, 판만 다시 칠하면 다음 FSM 전이까지(IDLE 이면 작업 시작
        전까지) 공정 단계 행이 옛 색으로 남았다. 같은 단계로 다시 그리므로 애니메이션은 없다.
        """
        super().apply_theme()
        if self._last_view is not None:
            self.update_view(*self._last_view)

    def relayout(self, parent_rect):
        p = _POS["status"]
```

<!-- rep: Rpi5/Demo/overlay_menu.py -->
```python
        lay.addWidget(self._scroll)

        self._rows = []
```
```python
        lay.addWidget(self._scroll)

        self._rows = []
        self._painted = []                # (라벨, 색 토큰, 굵기, 덧붙인 QSS) — 테마 전환 때 다시 칠한다(U14)
```

<!-- rep: Rpi5/Demo/overlay_menu.py -->
```python
        icon.setStyleSheet(theme.text_qss(token, 600) + "padding: 0;")
```
```python
        self._paint(icon, token, 600, "padding: 0;")
```

<!-- rep: Rpi5/Demo/overlay_menu.py -->
```python
        t = QLabel(title)
        t.setFont(config.font("body", 600))
        t.setStyleSheet(theme.text_qss("text", 600))
```
```python
        t = QLabel(title)
        t.setFont(config.font("body", 600))
        self._paint(t, "text", 600)
```

<!-- rep: Rpi5/Demo/overlay_menu.py -->
```python
            s = QLabel(sub)
            s.setFont(config.font("small"))
            s.setStyleSheet(theme.text_qss("label", 500))
```
```python
            s = QLabel(sub)
            s.setFont(config.font("small"))
            self._paint(s, "label", 500)
```

<!-- rep: Rpi5/Demo/overlay_menu.py -->
```python
    def _scroll_to_latest(self):
```
```python
    def _paint(self, lbl, token, weight, extra=""):
        """글자색을 칠하고 기억한다 — 테마가 바뀌면 apply_theme 이 다시 칠한다(리뷰 U14 ②)."""
        lbl.setStyleSheet(theme.text_qss(token, weight) + extra)
        self._painted.append((lbl, token, weight, extra))

    def _scroll_to_latest(self):
```

<!-- rep: Rpi5/Demo/overlay_menu.py -->
```python
    def apply_theme(self):
        super().apply_theme()
        self._title.setStyleSheet(theme.text_qss("label", 700))

    def relayout(self, parent_rect):
        p = _NOTIFY_POS
```
```python
    def apply_theme(self):
        super().apply_theme()
        self._title.setStyleSheet(theme.text_qss("label", 700))
        # 🔴 이미 쌓인 알림도 새 색으로 — 다크 글자(#e6e8ea)가 화이트 시트 위에 남아 거의 안
        #    보였다(리뷰 U14 ②).
        for lbl, token, weight, extra in self._painted:
            lbl.setStyleSheet(theme.text_qss(token, weight) + extra)

    def relayout(self, parent_rect):
        p = _NOTIFY_POS
```

<!-- rep: Rpi5/Demo/overlay_menu.py -->
```python
    def set_state(self, recording, path="", elapsed=0):
        self._recording = recording
```
```python
    def set_state(self, recording, path="", elapsed=0):
        self._recording = recording
        self._rec_path, self._rec_elapsed = path, elapsed   # 테마 전환 때 그대로 다시 그린다(U14 ⑤)
```

<!-- rep: Rpi5/Demo/overlay_menu.py -->
```python
        self.set_state(getattr(self, "_recording", False))
```
```python
        # 🔴 경로·경과도 넘긴다 — 빠뜨리면 녹화 중 테마를 바꿀 때 파일 이름 줄이 지워지고 경과가
        #    0초로 돌아갔다(리뷰 U14 ⑤).
        self.set_state(getattr(self, "_recording", False),
                       getattr(self, "_rec_path", ""), getattr(self, "_rec_elapsed", 0))
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_overlay.py 2>&1 | tail -1; python3 selftest/test_overlay_menu.py 2>&1 | tail -1; python3 selftest/test_console_flow.py 2>&1 | tail -1`
Expected:
```
✅ 오버레이 스모크 통과
✅ 메뉴·알림·설정 스모크 통과
✅ 글라스 UI 흐름 통과
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/overlay.py Demo/overlay_menu.py Demo/selftest/test_overlay.py Demo/selftest/test_overlay_menu.py && git commit -m "fix(overlay): 테마를 바꾸면 공정 단계 행·쌓인 알림·녹화 줄을 새 색으로 다시 그린다 — 옛 색이 남고 녹화 파일 이름이 지워지던 것 (범위 B U14 ①②⑤)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 3: U14 ③④ — 차단 맥박·발광 색도 테마를 따른다

**Files:** Modify `Rpi5/Demo/anim.py`(Pulse) · `Rpi5/Demo/overlay.py`(AlertBanner · GlowFrame) · `Rpi5/Demo/safety_console.py`(`_apply_theme`) · Test `Rpi5/Demo/selftest/test_overlay.py` · `Rpi5/Demo/selftest/test_console_flow.py`

**Interfaces:**
- Produces: `anim.Pulse(widget, base_qss_fn, color_hex, selector=None)` — `color_hex` 에 **색을 돌려주는 함수**도 받는다(시작할 때 읽는다) · `AlertBanner.apply_theme()` 이 뛰고 있는 맥박을 **다시 시작**한다 · `GlowFrame.apply_theme()`(지금 단계로 `set_level`) · 콘솔 `_apply_theme` 대상에 `self.glow`.

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_overlay.py -->
```python
def test_u14_block_pulse_follows_theme():
    """U14 ③ — 차단 맥박 색도 테마를 따른다(생성 때 다크 빨강으로 굳던 것)."""
    print("\n[U14] 차단 맥박 테마")
    from PyQt6.QtGui import QColor
    from PyQt6.QtTest import QTest
    config.UI_ANIMATION = True
    theme.set_theme("dark")
    try:
        host = QWidget()
        b = AlertBanner(host)
        b.show_block()
        theme.set_theme("light")
        b.apply_theme()
        QTest.qWait(150)                                  # 맥박이 몇 번 칠할 시간
        c = QColor(theme.C("danger"))
        check(f"rgba({c.red()},{c.green()},{c.blue()}," in b.styleSheet(),
              "맥박 테두리가 화이트 테마 빨강")
        b.hide_all()
    finally:
        config.UI_ANIMATION = False
        theme.set_theme("dark")


def test_u14_glow_follows_theme():
    """U14 ④ — 켜 둔 발광 테두리도 테마를 바꾸면 새 색으로."""
    print("\n[U14] 발광 테마")
    old = theme.GLOW_BORDER
    theme.GLOW_BORDER = True
    theme.set_theme("dark")
    try:
        host = QWidget()                                  # 🔴 부모를 붙들어 둔다 — 임시 부모는 곧바로 지워진다
        g = GlowFrame(host)
        g.set_level("block")
        theme.set_theme("light")
        try:
            g.apply_theme()
        except AttributeError as e:
            check(False, f"발광이 테마를 받지 못한다 — {e}")
            return
        check(theme.C("danger") in g.styleSheet(), "테두리가 화이트 테마 빨강")
    finally:
        theme.GLOW_BORDER = old
        theme.set_theme("dark")
```

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def test_u14_console_theme_repaints_glow():
    """U14 ④ — 콘솔의 테마 전환이 발광 테두리까지 다시 칠한다."""
    print("\n[U14] 콘솔 테마 → 발광")
    old = theme.GLOW_BORDER
    theme.GLOW_BORDER = True
    win = make_console()
    try:
        win._on_cta()
        key(win, "3")                                    # 위반 BLOCK → 발광
        win._on_theme_changed("light")
        check(theme.C("danger") in win.glow.styleSheet(), "발광 테두리가 화이트 테마 빨강")
    finally:
        theme.GLOW_BORDER = old
        theme.set_theme("dark")
        win.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_overlay.py 2>&1 | grep -E "^  ❌|^❌"; python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 맥박 테두리가 화이트 테마 빨강
  ❌ 발광이 테마를 받지 못한다 — 'GlowFrame' object has no attribute 'apply_theme'
❌ 실패 2건
  ❌ 발광 테두리가 화이트 테마 빨강
❌ 실패 1건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/anim.py -->
```python
    base_qss_fn: 맥박이 덧씌울 바탕 QSS 를 그때그때 만들어 주는 함수(테마 전환 대응).
    """

    def __init__(self, widget, base_qss_fn, color_hex, selector=None):
```
```python
    base_qss_fn: 맥박이 덧씌울 바탕 QSS 를 그때그때 만들어 주는 함수(테마 전환 대응).
    color_hex: 색(#RRGGBB) 또는 그때그때 색을 주는 함수 — 시작할 때 읽는다(테마 전환 대응 · 리뷰 U14 ③).
    """

    def __init__(self, widget, base_qss_fn, color_hex, selector=None):
```

<!-- rep: Rpi5/Demo/anim.py -->
```python
        base = self._base()
        c = QColor(self._c)
```
```python
        base = self._base()
        c = QColor(self._c() if callable(self._c) else self._c)
```

<!-- rep: Rpi5/Demo/overlay.py -->
```python
            theme.C("danger"), selector=_ALERT_SEL)
```
```python
            lambda: theme.C("danger"), selector=_ALERT_SEL)   # 색도 그때그때(U14 ③)
```

<!-- rep: Rpi5/Demo/overlay.py -->
```python
        self.setAttribute(Qt.WidgetAttribute.WA_StyledBackground, True)
        if self._mode is not None and self._last_paint is not None:
            self._paint(*self._last_paint)
```
```python
        self.setAttribute(Qt.WidgetAttribute.WA_StyledBackground, True)
        if self._mode is not None and self._last_paint is not None:
            self._paint(*self._last_paint)
            if self._pulse.active():
                # 🔴 뛰는 맥박은 시작할 때의 바탕·색을 쥐고 매 틱 덮어쓴다 — 새 색으로 다시
                #    시작한다(리뷰 U14 ③ — 테마를 바꿔도 다크 빨강 맥박이 남았다).
                self._pulse.stop()
                self._pulse.start()
```

<!-- rep: Rpi5/Demo/overlay.py -->
```python
    @property
    def level(self):
        return self._level
```
```python
    def apply_theme(self):
        """테마가 바뀌면 지금 단계를 새 색으로 다시 그린다(리뷰 U14 ④ — 켜 둔 테두리가 옛 색으로 남던 것)."""
        self.set_level(self._level)

    @property
    def level(self):
        return self._level
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        for w in (self.status_panel, self.gauge_panel, self.alert,
```
```python
        for w in (self.status_panel, self.gauge_panel, self.alert, self.glow,
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_overlay.py 2>&1 | tail -1; python3 selftest/test_console_flow.py 2>&1 | tail -1; python3 selftest/test_anim.py 2>&1 | tail -1`
Expected:
```
✅ 오버레이 스모크 통과
✅ 글라스 UI 흐름 통과
✅ 애니메이션 관문 통과
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/anim.py Demo/overlay.py Demo/safety_console.py Demo/selftest/test_overlay.py Demo/selftest/test_console_flow.py && git commit -m "fix(overlay): 차단 맥박·발광 테두리 색도 테마를 따른다 — 생성 때 색으로 굳어 테마를 바꿔도 다크 빨강이 남던 것 (범위 B U14 ③④)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 4: U15 — 게이지를 숨길 때 진행률을 비운다

**Files:** Modify `Rpi5/Demo/overlay.py`(GaugePanel) · Test `Rpi5/Demo/selftest/test_overlay.py`

**Interfaces:**
- Produces: `GaugePanel.update_view(None)`(또는 끝난 서브) → `_shown_progress = _target_progress = 0.0` · 진행 중이던 채움 보간 정지 · 채움 폭 0.

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_overlay.py -->
```python
def test_u15_gauge_starts_empty_next_time():
    """U15 — 서브 작업이 끝나면 게이지 진행률을 비운다(다음 게이지가 가득 찼다 줄어드는 번쩍임)."""
    print("\n[U15] 게이지 진행률")
    host = QWidget()
    g = GaugePanel(host)
    s = SubTask(WAIT, now=0.0)
    s.tick(now=999.0)                                     # 다 찼다
    g.update_view(s)
    check(g._shown_progress == 1.0, "끝난 서브는 가득 찬 게이지(시험 조건)")
    g.update_view(None)                                   # 서브 종료
    check(g._shown_progress == 0.0 and g._target_progress == 0.0, "숨길 때 진행률을 0으로")
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_overlay.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 숨길 때 진행률을 0으로
❌ 실패 1건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/overlay.py -->
```python
        if sub is None or not sub.is_active:
            self._stop_pulse()
            self.hide()
            return
```
```python
        if sub is None or not sub.is_active:
            self._stop_pulse()
            # 🔴 진행률도 비운다(리뷰 U15) — 남기면 다음 서브 게이지가 가득 찬 채 시작해 0으로
            #    줄어드는 보간이 번쩍였다.
            if self._gauge_anim is not None:
                self._gauge_anim.stop()
                self._gauge_anim = None
            self._shown_progress = self._target_progress = 0.0
            self._fill.setGeometry(0, 0, 0, 8)
            self.hide()
            return
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_overlay.py 2>&1 | tail -1; python3 selftest/test_anim.py 2>&1 | tail -1`
Expected:
```
✅ 오버레이 스모크 통과
✅ 애니메이션 관문 통과
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/overlay.py Demo/selftest/test_overlay.py && git commit -m "fix(overlay): 게이지를 숨길 때 진행률을 비운다 — 다음 서브 게이지가 가득 찼다 줄어들며 번쩍이던 것 (범위 B U15)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 5: U16 — 종료할 때 공구 워커를 내리고 상태 파일을 지운다

**Files:** Modify `Rpi5/Demo/safety_console.py`(`closeEvent`) · Test `Rpi5/Demo/selftest/test_console_flow.py`

**Interfaces:**
- Produces: `closeEvent` 가 맨 앞에서 `_end_tool_scan()` · `_state_pub.clear()` 를 부른다.

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def test_u16_close_stops_tool_worker_and_state():
    """U16 — 창을 닫으면 공구 워커를 내리고 음성비서용 상태 파일을 지운다(유령 상태 파일·워커가 남던 것)."""
    print("\n[U16] 종료 정리")
    win = make_console()
    calls = []
    win._state_pub.clear = lambda: calls.append("상태 파일 지움")
    real_scan = win.camera_thread.set_tool_scan
    win.camera_thread.set_tool_scan = lambda on: (calls.append(f"공구 스캔 {on}"), real_scan(on))
    win.close()
    check("상태 파일 지움" in calls, "상태 파일을 지운다")
    check("공구 스캔 False" in calls, "공구 스캔(워커)을 내린다")
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 상태 파일을 지운다
  ❌ 공구 스캔(워커)을 내린다
❌ 실패 2건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
    def closeEvent(self, event):
        self._stop_demo_capture()
```
```python
    def closeEvent(self, event):
        # 🔴 공구 워커를 내리고 상태 파일을 지운다(리뷰 U16) — 빠뜨리면 서브 작업 중 종료할 때 워커가
        #    ppid 검사에만 기대 남고, 음성비서가 읽는 상태 파일이 유령으로 남았다.
        self._end_tool_scan()
        self._state_pub.clear()
        self._stop_demo_capture()
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | tail -1`
Expected:
```
✅ 글라스 UI 흐름 통과
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/safety_console.py Demo/selftest/test_console_flow.py && git commit -m "fix(gui): 종료할 때 공구 워커를 내리고 상태 파일을 지운다 — 유령 상태 파일과 워커가 남던 것 (범위 B U16)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 6: U17 — 레시피 검사가 쓰는 쪽의 가정과 맞는다

**Files:** Modify `Rpi5/Demo/recipe.py`(`_validate`) · Test `Rpi5/Demo/selftest/test_recipe.py`

**Interfaces:**
- Produces: `_validate` — `process_name` 이 비면 `RecipeError` · `emo_button` 은 없으면 `config.FSM_EMO_BUTTON` 으로 채우고, 다르면 `RecipeError` · 공정 버튼과 겹침 검사는 그대로.
- 🔑 지금 `recipe.json` 은 두 키가 다 있고 `emo_button = "EMO"` — 그대로 통과한다.

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_recipe.py -->
```python
def test_u17_emo_button_filled_and_fixed():
    """U17 — emo_button 은 없으면 config 값으로 채우고, 다른 이름이면 거부한다(물리 EMO 가 오답 버튼이 되던 것)."""
    import config
    data = {k: v for k, v in GOOD.items() if k != "emo_button"}
    with tempfile.TemporaryDirectory() as tmp:
        r = load_recipe(_write(tmp, data))
        assert r["emo_button"] == config.FSM_EMO_BUTTON, r.get("emo_button")
        try:
            load_recipe(_write(tmp, {**GOOD, "emo_button": "STOP"}))
            assert False, "EMO 이름이 다른 레시피가 통과했다"
        except RecipeError:
            pass
    print("  PASS  emo_button 없음 → config 값 · 다른 이름 → RecipeError")


def test_u17_process_name_required():
    """U17 — 공정 이름이 없으면 검사에서 거부한다(통과한 뒤 GUI 가 KeyError 로 죽던 것)."""
    data = {k: v for k, v in GOOD.items() if k != "process_name"}
    with tempfile.TemporaryDirectory() as tmp:
        try:
            load_recipe(_write(tmp, data))
            assert False, "process_name 없는 레시피가 통과했다"
        except RecipeError:
            pass
    print("  PASS  process_name 없음 → RecipeError")
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_recipe.py 2>&1 | tail -1`
Expected:
```
KeyError: 'emo_button'
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/recipe.py -->
```python
    if data.get("emo_button") in buttons:
        raise RecipeError(f"emo_button({data.get('emo_button')})이 공정 버튼과 겹칩니다.")
```
```python
    # 🔑 쓰는 쪽(GUI)이 직접 읽는 키는 여기서 보장한다(리뷰 U17) — 검사를 통과했는데 기동 중
    #    KeyError 가 나면 안 된다.
    if not data.get("process_name"):
        raise RecipeError("process_name 이 비어 있습니다 (화면·기록에 쓰는 공정 이름)")
    # 🔴 EMO 이름은 config 값과 같아야 한다 — GPIO·키보드가 그 이름으로 눌림을 보내고 버튼 모델의
    #    클래스 이름도 같다. 레시피만 다른 이름을 쓰면 물리 EMO 가 「오답 버튼」으로 처리돼 위반
    #    차단이 됐다. 없으면 채운다(P7 체류 임계와 같은 방식).
    emo = data.setdefault("emo_button", config.FSM_EMO_BUTTON)
    if emo != config.FSM_EMO_BUTTON:
        raise RecipeError(f"emo_button({emo!r})은 {config.FSM_EMO_BUTTON!r} 이어야 합니다 "
                          f"(GPIO·키보드·버튼 모델이 이 이름을 씁니다)")
    if emo in buttons:
        raise RecipeError(f"emo_button({emo})이 공정 버튼과 겹칩니다.")
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_recipe.py 2>&1 | tail -1; python3 selftest/test_fsm.py 2>&1 | tail -1`
Expected:
```
14/14 passed
39/39 passed
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/recipe.py Demo/selftest/test_recipe.py && git commit -m "fix(recipe): 검사가 GUI 가 직접 읽는 키를 보장한다 — process_name 없으면 거부 · emo_button 은 채우고 config 이름과 다르면 거부 (범위 B U17)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 7: U20 — 알림 창은 종류마다 하나

**Files:** Modify `Rpi5/Demo/safety_console.py` · Test `Rpi5/Demo/selftest/test_console_flow.py`

**Interfaces:**
- Produces: `SafetyConsole._popups`(dict · 종류 → QMessageBox) · `SafetyConsole._popup(key, icon, title, text)` — 없으면 만들고, 있으면 글자를 바꿔 다시 띄우고 앞으로 올린다(비모달 · OK 로 닫으면 숨김 → 다음에 다시 뜬다). 종류 = `release_refused`(해제 거부) · `reset_refused`(초기화 거부) · `interlock_fault`(인터락 폴트).

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def test_u20_refusal_popups_do_not_stack():
    """U20 — 거부·폴트 창은 종류마다 하나 — 여러 번 눌러도 겹겹이 쌓이지 않는다(시연 화면을 가리던 것)."""
    print("\n[U20] 알림 창 쌓임")
    from PyQt6.QtWidgets import QMessageBox
    win = make_console()
    win._on_cta()
    key(win, "E")                                    # EMO 차단
    win.gpio_input.emo_active = lambda: True         # EMO 를 누른 채
    for _ in range(3):
        win._release_block()
    for _ in range(2):
        win._on_interlock_fault("시험 폴트")
    shown = sorted(b.windowTitle() for b in win.findChildren(QMessageBox) if not b.isHidden())
    check(shown == ["인터락 폴트", "해제 거부"], f"떠 있는 창 = {shown}")
    win.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 떠 있는 창 = ['인터락 폴트', '인터락 폴트', '해제 거부', '해제 거부', '해제 거부']
❌ 실패 1건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        self._tool_override = None   # 설정 메뉴에서 바꾼 지정 공구(세션 한정)
```
```python
        self._tool_override = None   # 설정 메뉴에서 바꾼 지정 공구(세션 한정)
        self._popups = {}            # 종류 → 알림 창 — 종류마다 하나만(U20 · _popup)
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
            box = QMessageBox(self)
            box.setIcon(QMessageBox.Icon.Warning)
            box.setWindowTitle("해제 거부")
            box.setText("EMO가 아직 복귀되지 않았습니다.\n"
                        "비상정지 버튼을 돌려 복귀(또는 EMO 배선 점검) 후 다시 시도하세요.")
            box.setStandardButtons(QMessageBox.StandardButton.Ok)
            box.setModal(False)
            box.show()
            return
```
```python
            self._popup("release_refused", QMessageBox.Icon.Warning, "해제 거부",
                        "EMO가 아직 복귀되지 않았습니다.\n"
                        "비상정지 버튼을 돌려 복귀(또는 EMO 배선 점검) 후 다시 시도하세요.")
            return
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
            box = QMessageBox(self)
            box.setIcon(QMessageBox.Icon.Warning)
            box.setWindowTitle("초기화 거부")
            box.setText("EMO가 아직 복귀되지 않았습니다.\n"
                        "비상정지 버튼을 돌려 복귀(또는 EMO 배선 점검) 후 다시 시도하세요.")
            box.setStandardButtons(QMessageBox.StandardButton.Ok)
            box.setModal(False)
            box.show()
            return
```
```python
            self._popup("reset_refused", QMessageBox.Icon.Warning, "초기화 거부",
                        "EMO가 아직 복귀되지 않았습니다.\n"
                        "비상정지 버튼을 돌려 복귀(또는 EMO 배선 점검) 후 다시 시도하세요.")
            return
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        self._append_log(f"[인터록] 🚨 폴트: {msg}")
        box = QMessageBox(self)
        box.setIcon(QMessageBox.Icon.Critical)
        box.setWindowTitle("인터락 폴트")
        box.setText("물리 차단이 확인되지 않았습니다!\n"
                    f"{msg}\n\n릴레이·배선·Arduino 전원을 점검하세요.")
        box.setStandardButtons(QMessageBox.StandardButton.Ok)
        box.setModal(False)
        box.show()
```
```python
        self._append_log(f"[인터록] 🚨 폴트: {msg}")
        self._popup("interlock_fault", QMessageBox.Icon.Critical, "인터락 폴트",
                    "물리 차단이 확인되지 않았습니다!\n"
                    f"{msg}\n\n릴레이·배선·Arduino 전원을 점검하세요.")

    def _popup(self, key, icon, title, text):
        """종류(key)마다 알림 창 하나 — 이미 있으면 글자만 바꿔 다시 띄우고 앞으로 올린다(리뷰 U20).

        🔴 누를 때마다 새 비모달 창을 만들면, EMO 를 복귀하지 않은 채 「차단 해제」를 여러 번 누를 때
           창이 겹겹이 쌓여 시연 화면을 가렸다.
        """
        box = self._popups.get(key)
        if box is None:
            box = QMessageBox(self)
            box.setStandardButtons(QMessageBox.StandardButton.Ok)
            box.setModal(False)
            self._popups[key] = box
        box.setIcon(icon)
        box.setWindowTitle(title)
        box.setText(text)
        box.show()
        box.raise_()
        return box
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | tail -1`
Expected:
```
✅ 글라스 UI 흐름 통과
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/safety_console.py Demo/selftest/test_console_flow.py && git commit -m "fix(gui): 해제 거부·초기화 거부·인터락 폴트 창은 종류마다 하나 — 누를 때마다 겹겹이 쌓여 시연 화면을 가리던 것 (범위 B U20)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 8: M-9 — 위반 차단 중 EMO 도 알림 목록에 남긴다

**Files:** Modify `Rpi5/Demo/safety_console.py`(`_commit_button`) · Test `Rpi5/Demo/selftest/test_console_flow.py`

**Interfaces:**
- Produces: 위반 차단 중 EMO 분기가 일반 EMO 경로와 같은 알림 `_notify("danger", "비상정지", "전기 입력 차단됨")` 을 낸다.

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def test_m9_emo_during_violation_block_notifies():
    """M-9 — 위반 차단 중 EMO 도 알림 목록에 「비상정지」를 남긴다(일반 EMO 경로와 같게 · ③ 리뷰 M-9)."""
    print("\n[M-9] 차단 중 EMO 알림")
    win = make_console()
    win._on_cta()
    key(win, "3")                                    # 위반 BLOCK
    n0 = notify_titles(win).count("비상정지")
    key(win, "E")
    check(notify_titles(win).count("비상정지") == n0 + 1, "알림 목록에 「비상정지」가 하나 는다")
    win.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 알림 목록에 「비상정지」가 하나 는다
❌ 실패 1건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
            self._show_block_banner(emo=True)
            self._publish_state()
```
```python
            self._show_block_banner(emo=True)
            self._notify("danger", "비상정지", "전기 입력 차단됨")   # 일반 EMO 경로와 같게(③ 리뷰 M-9)
            self._publish_state()
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | tail -1`
Expected:
```
✅ 글라스 UI 흐름 통과
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/safety_console.py Demo/selftest/test_console_flow.py && git commit -m "fix(gui): 위반 차단 중 EMO 도 알림 목록에 「비상정지」를 남긴다 (③ 리뷰 M-9)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 9: 검증 관문

**Files:** Create `Rpi5/조사/범위B-20260926/after_B_selftest.txt` · `after_B_gate.txt`

- [ ] **Step 1: 자가 테스트 전부 · 관문**

Run:
```bash
cd /home/pi/sop-project/Rpi5/Demo && D=../조사/범위B-20260926 && \
for t in selftest/test_*.py; do
  py=python3; [ "$(basename $t)" = test_voice_tts.py ] && py=~/env/tts/.venv/bin/python
  timeout 600 $py "$t" >/dev/null 2>&1; rc=$?; echo "$(basename $t) exit=$rc"
done > $D/after_B_selftest.txt; diff $D/after_A_selftest.txt $D/after_B_selftest.txt && echo selftest_same; grep -c "exit=0" $D/after_B_selftest.txt; \
python3 test/fsm_sim.py --gate > $D/after_B_gate.txt 2>&1; echo gate_exit=$?; diff $D/before_gate.txt $D/after_B_gate.txt && echo gate_same
```
Expected:
```
selftest_same
32
gate_exit=0
gate_same
```
- `selftest_same` 이 없으면 → 중단 규칙 ②. · `gate_exit` ≠ 0 또는 `gate_same` 없음 → 멈춘다.

- [ ] **Step 2: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add 조사/범위B-20260926/after_B_selftest.txt 조사/범위B-20260926/after_B_gate.txt && git commit -m "docs(조사): 범위 B·미룬 사소 ④-B 결과 — 자가 테스트·관문 (수정 후 · 관문 기준선과 같음)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 10: 정본·문서 반영

🔴 통합문서를 열면 `.claude/rules/정본편집.md` 가 붙는다 — 그 규칙을 따른다(현행만 본문 · 폐기는 `docs/변경이력.md` · 미구현은 장 끝 ⏸).

**Files:** Modify `docs/통합문서.md`(§8.1 · 🔓 행) · `Rpi5/함수목록.md` · `Rpi5/조사/README.md` · spec·이 계획서 머리

- [ ] **Step 1: 통합문서**
  - §8.1 「신호가 가는 길」의 EMO 줄 뒤 — **C18**: 「차단 중 오답 버튼을 **누른 채** 해제하면 전기가 다시 들어오는 순간 새 눌림으로 잡혀 곧바로 다시 차단된다 — 실제 장비라면 누르고 있던 오답 동작이 그 순간 실행되므로 **의도된 안전 동작**이다(설계 §9.5).」
  - 🔓 「실제 시연 프로그램 문제 수정」 행 — 제목에 「④ 범위 B·미룬 사소 구현(공동 확인 대기)」을 더하고 본문 끝에 한 줄: 「④(2026-09-26) — 범위 B 고칠 것 13(C12~C16·C19·C20·U13~U17·U20) · 고치지 않음 2(C18 안전 쪽 · U19 도달 경로 없음) · ③ 리뷰 미룬 사소 8 · 계획서 `plans/2026-09-26-런타임문제수정-4A-코어.md`·`4B-GUI.md` · ⏸ 실HW(Arduino — C15·M-5·M-6 · 서브 ESP32 — C20·M-2)」.
- [ ] **Step 2: 함수목록** — §3 새 행(④-A 와 같은 방식 · 모듈 제목 함수 수 · 머리 「함수·메서드 N」): `SafetyConsole._refresh_dim` · `SafetyConsole._popup` · `StatusPanel.apply_theme` · `GlowFrame.apply_theme` · `NotifyPanel._paint`. 머리 괄호의 「④-A 8개」 → 「④ 13개」.
- [ ] **Step 3: 조사 README**(③ 리뷰 M-11) — 표에 빠진 폴더 3개 행: `판정수정-20260925/`(판정 수정 ① 기준선·결과·규칙별 미리보기) · `GUI수정-20260925/`(GUI 수정 ② 기준선·결과) · `인식인터락음성-20260925/`(인식·인터락·음성 ③ 기준선·결과) — 쓰는 곳 = 설계서 `specs/2026-09-25-런타임-문제수정-design.md` · 저널 §12.74·§12.75.
- [ ] **Step 4: spec·계획서 머리** — spec 상태 줄 「④-A 코어 구현·관문 통과(공동 확인 대기 …) · ④-B GUI 대기」 → 「④-A·④-B 구현·관문 통과(공동 확인 대기)」 · 이 계획서 상태 줄 → 「구현·관문 통과 — Rpi5 `<Task 1 BASE>`..`<Task 9 커밋>`」.
- [ ] **Step 5: 커밋** (두 저장소)

```bash
cd /home/pi/sop-project/Rpi5 && git add 함수목록.md 조사/README.md && git commit -m "docs: 범위 B·미룬 사소 ④-B 반영 — 함수목록 새 함수(스크림 계산·알림 창·테마 다시 그리기) · 조사 README 빠진 폴더 3개 (③ 리뷰 M-11)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
cd /home/pi/sop-project && git add docs/통합문서.md docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md docs/superpowers/plans/2026-09-26-런타임문제수정-4B-GUI.md && git commit -m "docs(정본): 범위 B ④-B — §8.1 차단 중 누른 채 해제하면 다시 차단(의도된 안전 동작 C18) · 🔓 행 ④ 구현

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 11: 최종 리뷰(④ 전체) · 공동 확인 · 마무리

- [ ] **Step 1: 최종 리뷰** — `superpowers:requesting-code-review` · 새 맥락 리뷰어 1회(가장 강한 모델) · 범위 = Rpi5 ④ 기준선 커밋(`2673e6a`)의 부모 `9b31d0e`..HEAD + 문서 `bc7c92e`..HEAD. 리뷰 기준에 넣을 것: spec §9 · 두 계획서의 Review Focus 원문 · 두 ledger 의 `Ruling:` 줄 · **사용자 구상 원문**(보존본 `~/lab/session-archive/` 의 이 세션 — 「범위 B(낮음 17건) · 미룬 사소 항목 8건 진행하자」 · 설계 승인 「진행해줘」 · 계획 승인 「시작」).
- [ ] **Step 2: 반영** — Critical·Important 는 한 번에 고친다(각각 실패 시험 먼저 → 통과 → 자가 테스트 전부) · Minor 는 ledger 와 보고에 「미룬 사소」로.
- [ ] **Step 3: 공동 확인** — 사용자에게 고정 형식으로 보고(무엇이 달라졌나 · 판단 기록 · 미룬 사소 · ⏸ 실HW)하고 확인받는다. 🔴 Claude 단독으로 「완료」를 선언하지 않는다.
- [ ] **Step 4: `work-close`** — 공동 확인 뒤. push 는 여기서 한 번(두 저장소).
