# 글라스 오디오 다리 (V1.5) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 파이와 ESP32 사이에 wav 를 양방향으로 실어 날라, 막혀 있는 **명료도 판정**(§10.49)과 **STT 재측정**(§10.50)을 풀어 준다.

**Architecture:** 전송을 아두이노 `Stream` 으로 추상화해 유선(USB CDC)↔무선(`WiFiClient`) 교체가 변수 하나가 되게 한다. 프레이밍은 「헤더 한 줄 + 원시 int16 PCM + 체크섬」이고, 오디오는 **받아놓고 재생**한다(버퍼 언더런 원천 회피 = 무선 대비책). 포트는 번호가 아니라 **USB 신원**으로 해석한다.

**Tech Stack:** Arduino ESP32 코어 3.3.10 (`ESP_I2S`) · XIAO ESP32S3 Sense · Python 3.13 + pyserial 3.5 · arduino-cli

**Spec:** [`docs/superpowers/specs/2026-09-01-글라스-오디오다리-design.md`](../specs/2026-09-01-글라스-오디오다리-design.md)

## Global Constraints

- 🔴 **굽는 FQBN 은 `esp32:esp32:XIAO_ESP32S3:PSRAM=opi`** — 기본값이 `PSRAM=disabled` 라 빠뜨리면 `ps_malloc` 이 NULL 을 낸다.
- 🔴 **포트를 코드에 박지 않는다.** 모든 포트는 `Demo/serial_ports.py` 로 해석한다.
- 🔴 **체크섬 불일치 = 중단.** 잘린 전송도 그럴듯한 소리를 내므로, 검증 없이 얻은 소리는 어떤 판정에도 쓰지 않는다.
- **마이크는 16000Hz 고정**(설계 §8.2). 스피커만 헤더의 레이트를 따라 다시 연다.
- **V0 명령(`r`·`p`·`1~5`·`i`)을 없애지 않는다** — V0 검증 절차가 살아 있어야 한다.
- 스케치 이름 `mic_speaker_test` **유지**(통합문서·상위 spec 이 이 이름으로 인용 중).
- 커밋 서명: `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. **코드는 `Rpi5` 저장소, 문서는 `sop-project`.**

## 파일 구조

| 파일 | 책임 |
|---|---|
| `Rpi5/Demo/serial_ports.py` (신규) | **USB 신원 → 장치 경로.** 이것만 한다 |
| `Rpi5/arduino/mic_speaker_test/mic_speaker_test.ino` (수정) | 펌웨어 — PSRAM 버퍼 · `Stream` 이음매 · `W/P/R/D` |
| `Rpi5/arduino/mic_speaker_test/esp32_audio.py` (신규) | 파이 쪽 짝 — 프로토콜 인코딩·디코딩·wav 입출력 |
| `Rpi5/Demo/config.py` (수정) | `INTERLOCK_PORT` 해석(폴백 유지) |

---

### Task 1: USB 신원으로 포트 해석

**Files:**
- Create: `Rpi5/Demo/serial_ports.py`
- Test: `Rpi5/Demo/serial_ports.py` 의 `__main__` 자체 점검

**Interfaces:**
- Produces: `resolve(vid: int, pid: int | None = None, serial_number: str | None = None) -> str | None`
- Produces: `ESP32_S3 = (0x303a, 0x1001)`
- Produces: `describe() -> list[str]` — 붙어 있는 시리얼 장치를 사람이 읽을 형태로

- [ ] **Step 1: 지금 붙어 있는 장치의 신원을 눈으로 확인한다**

Run:
```bash
python3 -c "from serial.tools import list_ports; [print(f'{p.device} vid={p.vid:04x} pid={p.pid:04x} sn={p.serial_number}') for p in list_ports.comports() if p.vid]"
```
Expected: `/dev/ttyACM0 vid=303a pid=1001 sn=3C:0F:02:DD:5E:40`

- [ ] **Step 2: `serial_ports.py` 를 쓴다**

```python
"""USB 신원으로 시리얼 장치를 찾는다 — 포트 번호(ttyACM0/1)를 쓰지 않기 위해.

🔴 왜 번호를 안 쓰나: 꽂는 순서·개수에 따라 ttyACM0 과 ttyACM1 이 뒤바뀐다.
   2026-09-01 실제로 config.py 의 INTERLOCK_PORT="/dev/ttyACM0" 이 ESP32 를
   가리키고 있었다(인터록 Arduino 미연결 상태에서 ESP32 가 0번을 차지).
   앞으로 인터록·디스플레이 터치·오디오가 두 포트를 나눠 쓰므로 번호는 못 믿는다.
"""
from serial.tools import list_ports

# (vid, pid) — XIAO ESP32S3 의 네이티브 USB(USB JTAG/serial). ROM 페리페럴이라
# 앱 펌웨어가 죽어도 이 신원은 사라지지 않는다.
ESP32_S3 = (0x303a, 0x1001)


def resolve(vid, pid=None, serial_number=None):
    """조건에 맞는 첫 장치 경로를 준다. 없으면 None."""
    for p in list_ports.comports():
        if p.vid != vid:
            continue
        if pid is not None and p.pid != pid:
            continue
        if serial_number is not None and p.serial_number != serial_number:
            continue
        return p.device
    return None


def describe():
    """붙어 있는 시리얼 장치를 사람이 읽을 줄로. 진단·오류 메시지용."""
    out = []
    for p in list_ports.comports():
        if p.vid is None:
            continue
        out.append(f"{p.device}  vid={p.vid:04x} pid={p.pid:04x}  {p.product or '?'}  sn={p.serial_number}")
    return out


if __name__ == "__main__":
    import sys
    # --path : 경로만 한 줄로 (셸에서 $(...) 로 받으려고). 못 찾으면 종료코드 1.
    if "--path" in sys.argv:
        path = resolve(*ESP32_S3)
        if not path:
            print("ESP32-S3 를 못 찾았다", file=sys.stderr)
            sys.exit(1)
        print(path)
        sys.exit(0)
    print("붙어 있는 USB 시리얼 장치:")
    for line in describe() or ["  (없음)"]:
        print("  " + line)
    print()
    print(f"ESP32-S3 → {resolve(*ESP32_S3) or '못 찾음'}")
```

- [ ] **Step 3: 자체 점검을 돌린다**

Run: `python3 Rpi5/Demo/serial_ports.py`
Expected: 장치 목록에 `/dev/ttyACM0 vid=303a pid=1001` 이 나오고 마지막 줄이 `ESP32-S3 → /dev/ttyACM0`

- [ ] **Step 4: 못 찾는 경우도 확인한다** (오탐 방지)

Run: `python3 -c "import sys; sys.path.insert(0,'Rpi5/Demo'); import serial_ports as s; print(s.resolve(0x2341))"`
Expected: `None` — 인터록 Arduino 는 지금 안 꽂혀 있다. **여기서 경로가 나오면 매칭이 틀린 것이다.**

- [ ] **Step 5: 커밋** (Rpi5 저장소)

```bash
cd Rpi5 && git add Demo/serial_ports.py && git commit -m "feat(serial): USB 신원으로 포트 해석 — ttyACM 번호 의존 제거

꽂는 순서에 따라 ttyACM0/1 이 뒤바뀌어 config.py 의 INTERLOCK_PORT 가
ESP32 를 가리키는 일이 실제로 있었다. vid/pid/시리얼로 장치를 고른다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: 펌웨어 — PSRAM 버퍼 + `Stream` 이음매 + `W/P/R/D`

**Files:**
- Modify: `Rpi5/arduino/mic_speaker_test/mic_speaker_test.ino`

**Interfaces:**
- Produces (시리얼 프로토콜, Task 3 이 소비):
  - `W <샘플수> <레이트>\n` + `int16LE × 샘플수` + `uint32LE 체크섬` → 응답 `[적재] n=… rate=… sum=… ok`
  - `P\n` → `[재생 완료]`
  - `R <초>\n` → `[녹음 완료] <샘플수>샘플 · DC … · RMS … · peak …`
  - `D\n` → `D <샘플수> <레이트> <체크섬>\n` + `int16LE × 샘플수`
- Produces: 체크섬 = 모든 샘플을 `uint16` 으로 본 값의 **32비트 합**(오버플로 허용)

- [ ] **Step 1: 버퍼를 PSRAM 으로 옮기고 늘린다**

`static int16_t rec[N_SAMPLE];` 를 없애고 상단 상수·전역을 이렇게 바꾼다:

```cpp
static const uint32_t RATE       = 16000;        // 🔴 마이크 고정 (설계 §8.2)
static const uint32_t MAX_SEC    = 10;
static const uint32_t MAX_RATE   = 24000;
static const size_t   MAX_SAMPLE = MAX_SEC * MAX_RATE;   // 240,000 샘플 = 480KB

// 🔴 PSRAM 에 잡는다 — 내부 SRAM 에는 480KB 가 안 들어간다.
//    굽기 FQBN 에 PSRAM=opi 가 없으면 여기서 NULL 이 난다(기본값이 disabled).
static int16_t *rec     = nullptr;
static size_t   nRec    = 0;            // 지금 담겨 있는 샘플 수
static uint32_t recRate = RATE;         // 담겨 있는 것의 샘플레이트
static bool     hasRec  = false;
static uint32_t spkRate = RATE;         // 스피커 I2S 가 지금 열려 있는 레이트
```

`record()`·`play()` 안의 `N_SAMPLE` 을 전부 `nRec` 로 바꾸고, `play()` 의 루프 조건도 `i < nRec` 로 한다.

- [ ] **Step 2: `setup()` 에서 PSRAM 을 잡고, 실패를 소리 내어 알린다**

`Serial.begin(115200); delay(1500);` 바로 뒤에 넣는다:

```cpp
  rec = (int16_t *)ps_malloc(MAX_SAMPLE * sizeof(int16_t));
  if (rec == nullptr) {
    // 🔴 여기서 멈추는 이유: 조용히 실패하면 "녹음이 안 된다"로 오진한다.
    Serial.println("[FAIL] PSRAM 할당 실패 — PSRAM=opi 로 굽지 않았다.");
    Serial.println("       arduino-cli compile --fqbn esp32:esp32:XIAO_ESP32S3:PSRAM=opi ...");
    while (true) delay(1000);
  }
  Serial.printf("[OK] PSRAM 버퍼 %u샘플 (%uKB · 최대 %u초)\n",
                (unsigned)MAX_SAMPLE, (unsigned)(MAX_SAMPLE * 2 / 1024), (unsigned)MAX_SEC);
```

- [ ] **Step 3: 전송 이음매와 도우미를 넣는다**

`I2SClass spk;` 아래에 넣는다:

```cpp
// ── 전송 계층 ────────────────────────────────────────────────
// 🔑 Serial(USB CDC)과 WiFiClient 는 둘 다 Stream 을 상속한다.
//    그래서 아래 코드는 유선인지 무선인지 모른 채로 동작하고,
//    무선 전환은 이 포인터 하나를 바꾸는 일이 된다(설계 §4.1).
static Stream *link = &Serial;

static uint32_t checksum(const int16_t *p, size_t n) {
  uint32_t s = 0;
  for (size_t i = 0; i < n; i++) s += (uint16_t)p[i];   // 오버플로 허용
  return s;
}

// 개행까지 한 줄을 읽는다. 타임아웃이면 빈 문자열.
static String readLine(uint32_t ms = 2000) {
  String s;
  uint32_t t0 = millis();
  while (millis() - t0 < ms) {
    while (link->available()) {
      char c = link->read();
      if (c == '\n') return s;
      if (c != '\r') s += c;
      t0 = millis();
    }
    delay(1);
  }
  return s;
}

// 정확히 len 바이트를 채운다. 🔴 여기서 블로킹으로 읽기 때문에
//    오디오 안에 'r'·'p' 바이트가 섞여도 명령으로 오해되지 않는다.
static bool readExact(uint8_t *dst, size_t len, uint32_t ms = 10000) {
  size_t got = 0;
  uint32_t t0 = millis();
  while (got < len && millis() - t0 < ms) {
    int n = link->readBytes((char *)dst + got, len - got);
    if (n > 0) { got += n; t0 = millis(); }
  }
  return got == len;
}

// 스피커 I2S 를 원하는 레이트로 (필요할 때만) 다시 연다.
static bool setSpkRate(uint32_t r) {
  if (r == spkRate) return true;
  spk.end();
  spk.setPins(SPK_BCLK, SPK_LRC, SPK_DIN);
  if (!spk.begin(I2S_MODE_STD, r, I2S_DATA_BIT_WIDTH_16BIT, I2S_SLOT_MODE_STEREO)) {
    Serial.printf("[FAIL] 스피커를 %luHz 로 열지 못했다.\n", (unsigned long)r);
    return false;
  }
  spkRate = r;
  return true;
}
```

- [ ] **Step 4: `W`(적재) 명령을 넣는다**

```cpp
// "W <샘플수> <레이트>" + int16LE 페이로드 + uint32LE 체크섬
static void cmdWrite(const String &args) {
  long n = 0, r = 0;
  if (sscanf(args.c_str(), "%ld %ld", &n, &r) != 2) {
    Serial.println("[FAIL] W 인자를 못 읽었다. 형식: W <샘플수> <레이트>");
    return;
  }
  if (n <= 0 || (size_t)n > MAX_SAMPLE) {
    Serial.printf("[FAIL] 샘플수 %ld — 1~%u 범위를 벗어났다.\n", n, (unsigned)MAX_SAMPLE);
    return;
  }
  if (r < 8000 || r > (long)MAX_RATE) {
    Serial.printf("[FAIL] 레이트 %ld — 8000~%u 범위를 벗어났다.\n", r, (unsigned)MAX_RATE);
    return;
  }
  if (!readExact((uint8_t *)rec, (size_t)n * sizeof(int16_t))) {
    Serial.println("[FAIL] 페이로드가 도중에 끊겼다.");
    hasRec = false;
    return;
  }
  uint8_t cs[4];
  if (!readExact(cs, 4, 2000)) {
    Serial.println("[FAIL] 체크섬 4바이트가 안 왔다.");
    hasRec = false;
    return;
  }
  uint32_t want = (uint32_t)cs[0] | ((uint32_t)cs[1] << 8)
                | ((uint32_t)cs[2] << 16) | ((uint32_t)cs[3] << 24);
  uint32_t got = checksum(rec, (size_t)n);
  if (want != got) {
    // 🔴 여기서 멈추는 이유: 잘린 전송도 그럴듯한 소리를 낸다.
    //    그 소리로 판정하면 "STT 가 나쁘다"로 오진한다(설계 §4.2).
    Serial.printf("[FAIL] 체크섬 불일치 — 보낸 값 %lu · 받은 값 %lu\n",
                  (unsigned long)want, (unsigned long)got);
    hasRec = false;
    return;
  }
  nRec = (size_t)n;
  recRate = (uint32_t)r;
  hasRec = true;
  // 적재된 것은 이미 정규화 대상이 아니므로 peak 를 그대로 재둔다.
  lastPeak = 0;
  for (size_t i = 0; i < nRec; i++) if (abs(rec[i]) > lastPeak) lastPeak = abs(rec[i]);
  Serial.printf("[적재] n=%u rate=%lu sum=%lu ok\n",
                (unsigned)nRec, (unsigned long)recRate, (unsigned long)got);
}
```

- [ ] **Step 5: `D`(덤프) 명령을 넣는다**

```cpp
// "D" → "D <샘플수> <레이트> <체크섬>" + int16LE 페이로드
static void cmdDump() {
  if (!hasRec) { Serial.println("[FAIL] 담긴 것이 없다."); return; }
  uint32_t cs = checksum(rec, nRec);
  link->printf("D %u %lu %lu\n", (unsigned)nRec, (unsigned long)recRate, (unsigned long)cs);
  link->write((uint8_t *)rec, nRec * sizeof(int16_t));
  link->flush();
}
```

- [ ] **Step 6: `play()` 가 적재된 레이트를 따르게 하고, `R`(길이 지정 녹음)을 넣는다**

`play()` 맨 앞(`if (!hasRec)` 뒤)에 넣는다:
```cpp
  if (!setSpkRate(recRate)) return;
```

`record()` 의 시그니처를 `void record(uint32_t sec)` 로 바꾸고, 머리 부분을 이렇게 만든다:

```cpp
void record(uint32_t sec) {
  if (sec < 1) sec = 1;
  if (sec > MAX_SEC) sec = MAX_SEC;
  size_t want_n = (size_t)RATE * sec;

  // 🔴 앞서 22050Hz 를 재생했다면 신호음이 엉뚱한 음높이로 난다 — 되돌린다.
  if (!setSpkRate(RATE)) return;

  Serial.printf("[녹음] 신호음 3번 뒤 %lu초간 녹음합니다.\n", (unsigned long)sec);
  for (int i = 0; i < 3; i++) { beep(660, 120); delay(380); }
  beep(1320, 200);
  delay(150);

  size_t got = 0;
  while (got < want_n) {
    size_t want = (want_n - got) * sizeof(int16_t);
    size_t n = mic.readBytes((char *)(rec + got), want);
    if (n == 0) break;
    got += n / sizeof(int16_t);
  }
  if (got < want_n / 2) {
    Serial.printf("[FAIL] 샘플이 부족하다(%u) — 마이크를 못 읽었다.\n", (unsigned)got);
    hasRec = false;
    return;
  }
```

이 아래 DC 제거·통계 계산 블록은 **기존 코드를 그대로 두되** `got` 을 계속 쓴다. 블록 끝의
`hasRec = true;` 옆에 두 줄을 더한다:

```cpp
  nRec    = got;      // 🔑 이제 길이가 가변이다
  recRate = RATE;     // 마이크는 항상 16kHz
```

- [ ] **Step 7: `loop()` 를 한 줄 명령까지 받게 고친다**

```cpp
void loop() {
  if (!link->available()) { delay(20); return; }
  char c = link->read();
  if (c == 'W' || c == 'R') {                 // 인자가 있는 명령
    String args = readLine();
    if (c == 'W') cmdWrite(args);
    else          record((uint32_t)args.toInt());
  } else if (c == 'D') {
    readLine(50);                              // 남은 개행 버림
    cmdDump();
  } else if (c == 'P') {
    readLine(50);
    play();
  } else if (c == 'r') {
    record(3);
    if (hasRec) { delay(200); play(); }
  } else if (c == 'p') {
    play();
  } else if (c >= '1' && c <= '5') {
    volIdx = c - '1';
    Serial.printf("[음량] 목표 진폭 %d (단계 %d/5)\n", TARGET[volIdx], volIdx + 1);
  } else if (c == 'i') {
    if (hasRec) Serial.printf("[통계] n %u · rate %lu · DC %d · RMS %d · peak %d\n",
                              (unsigned)nRec, (unsigned long)recRate, lastDC, lastRms, lastPeak);
    else        Serial.println("[통계] 담긴 것 없음");
  }
}
```

- [ ] **Step 8: 머리 주석의 포트 오정보를 고친다**

기존:
```
 * 굽기 (🔴 ttyACM0 은 Arduino 인터록):
 *   arduino-cli upload -p /dev/ttyACM1 --fqbn esp32:esp32:XIAO_ESP32S3 arduino/mic_speaker_test
```
바꿀 내용:
```
 * 굽기 — 🔴 포트 번호를 믿지 말 것. 꽂는 순서에 따라 ttyACM0/1 이 뒤바뀐다.
 *   PORT=$(python3 Demo/serial_ports.py --path)
 *   arduino-cli compile --fqbn esp32:esp32:XIAO_ESP32S3:PSRAM=opi arduino/mic_speaker_test
 *   arduino-cli upload -p "$PORT" --fqbn esp32:esp32:XIAO_ESP32S3:PSRAM=opi arduino/mic_speaker_test
 *   🔴 PSRAM=opi 필수 — 빠뜨리면 ps_malloc 이 NULL 을 낸다(기본값 disabled).
```
그리고 `serial_ports.py` 에 `--path` 인자를 더한다(경로만 한 줄 출력, 못 찾으면 종료코드 1).

- [ ] **Step 9: 컴파일만 해서 문법·링크를 확인한다** (아직 굽지 않는다)

Run:
```bash
~/bin/arduino-cli compile --fqbn esp32:esp32:XIAO_ESP32S3:PSRAM=opi Rpi5/arduino/mic_speaker_test
```
Expected: `Sketch uses ... bytes` 로 끝나고 오류 0

- [ ] **Step 10: 커밋** (Rpi5 저장소)

```bash
cd Rpi5 && git add arduino/mic_speaker_test/mic_speaker_test.ino Demo/serial_ports.py && git commit -m "feat(audio): 파이↔ESP32 wav 전송 — PSRAM 버퍼·Stream 이음매·W/P/R/D

명료도 판정과 STT 재측정이 둘 다 「오디오가 오갈 길이 없다」로 막혀
있었다. 전송을 Stream 으로 추상화해 무선 전환이 포인터 교체가 되게 했다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: 파이 쪽 도구 `esp32_audio.py`

**Files:**
- Create: `Rpi5/arduino/mic_speaker_test/esp32_audio.py`

**Interfaces:**
- Consumes: Task 1 의 `resolve`·`ESP32_S3` · Task 2 의 시리얼 프로토콜
- Produces: CLI `play <wav>` / `rec <초> <out.wav>` / `tone <Hz> <초> <out.wav>`
- Produces: `class Link` (추상) · `SerialLink(path)` · `TcpLink(host, port)`

- [ ] **Step 1: 도구를 쓴다**

```python
#!/usr/bin/env python3
"""파이 ↔ ESP32 오디오 다리 — 펌웨어 mic_speaker_test 의 짝.

🔴 이 파일과 펌웨어는 프로토콜을 공유한다. 한쪽만 고치면 조용히 어긋난다.

  play <wav>            wav 를 ESP32 로 보내 FQ 스피커로 재생
  rec <초> <out.wav>    ESP32 로 녹음시키고 회수해 wav 로 저장
  tone <Hz> <초> <wav>  검증용 순음 wav 생성 (ESP32 불필요)
"""
import argparse, os, socket, struct, sys, time, wave

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "..", "Demo"))
from serial_ports import ESP32_S3, describe, resolve   # noqa: E402


class Link:
    """전송 계층 — 펌웨어의 Stream& 에 대응한다(설계 §4.1).

    유선(SerialLink)과 무선(TcpLink)이 같은 얼굴을 하므로, 아래 프로토콜
    코드는 어느 쪽인지 모른 채 동작한다.
    """
    def write(self, b): raise NotImplementedError
    def read(self, n): raise NotImplementedError
    def readline(self): raise NotImplementedError
    def close(self): pass


class SerialLink(Link):
    def __init__(self, path, baud=115200, timeout=15):
        import serial
        self.s = serial.Serial(path, baud, timeout=timeout)
        time.sleep(0.3)
        self.s.reset_input_buffer()

    def write(self, b): self.s.write(b); self.s.flush()

    def read(self, n):
        out = b""
        while len(out) < n:
            chunk = self.s.read(n - len(out))
            if not chunk:
                raise IOError(f"읽기 중단 — {len(out)}/{n} 바이트만 받았다")
            out += chunk
        return out

    def readline(self): return self.s.readline().decode("utf-8", "replace").strip()

    def close(self): self.s.close()


class TcpLink(Link):
    """무선 이행용. 🔴 아직 펌웨어 쪽 WiFiClient 결선이 없다(설계 §7)."""
    def __init__(self, host, port, timeout=15):
        self.s = socket.create_connection((host, port), timeout)
        self.s.settimeout(timeout)
        self.f = self.s.makefile("rb")

    def write(self, b): self.s.sendall(b)

    def read(self, n):
        out = self.f.read(n)
        if out is None or len(out) < n:
            raise IOError(f"읽기 중단 — {len(out or b'')}/{n} 바이트만 받았다")
        return out

    def readline(self): return self.f.readline().decode("utf-8", "replace").strip()

    def close(self): self.s.close()


def checksum(samples):
    """펌웨어의 checksum() 과 같아야 한다 — uint16 으로 본 값의 32비트 합."""
    return sum(s & 0xFFFF for s in samples) & 0xFFFFFFFF


def read_wav(path):
    with wave.open(path) as w:
        if w.getnchannels() != 1:
            raise SystemExit(f"🔴 모노만 된다 — {path} 는 {w.getnchannels()}채널")
        if w.getsampwidth() != 2:
            raise SystemExit(f"🔴 16비트만 된다 — {path} 는 {w.getsampwidth()*8}비트")
        rate = w.getframerate()
        raw = w.readframes(w.getnframes())
    return list(struct.unpack(f"<{len(raw)//2}h", raw)), rate


def write_wav(path, samples, rate):
    with wave.open(path, "w") as w:
        w.setnchannels(1); w.setsampwidth(2); w.setframerate(rate)
        w.writeframes(struct.pack(f"<{len(samples)}h", *samples))


def wait_for(link, marker, timeout=30):
    """marker 로 시작하는 줄이 나올 때까지 로그를 흘려보며 기다린다."""
    t0 = time.time()
    while time.time() - t0 < timeout:
        line = link.readline()
        if not line:
            continue
        print("  " + line)
        if line.startswith(marker):
            return line
        if line.startswith("[FAIL]"):
            raise SystemExit(f"🔴 ESP32 가 실패를 보고했다: {line}")
    raise SystemExit(f"🔴 '{marker}' 응답이 {timeout}초 안에 안 왔다")


def cmd_play(link, path):
    samples, rate = read_wav(path)
    cs = checksum(samples)
    print(f"▶ 보냄: {os.path.basename(path)} — {len(samples)}샘플 · {rate}Hz · sum={cs}")
    link.write(f"W {len(samples)} {rate}\n".encode())
    link.write(struct.pack(f"<{len(samples)}h", *samples))
    link.write(struct.pack("<I", cs))
    wait_for(link, "[적재]")
    link.write(b"P\n")
    wait_for(link, "[재생 완료]", timeout=60)
    print("✅ 재생 완료")


def cmd_rec(link, sec, out):
    print(f"▶ {sec}초 녹음 — 신호음 뒤에 말하세요")
    link.write(f"R {sec}\n".encode())
    wait_for(link, "[녹음 완료]", timeout=sec + 30)
    link.write(b"D\n")
    header = ""
    while not header.startswith("D "):
        header = link.readline()
        if not header:
            raise SystemExit("🔴 덤프 헤더가 안 왔다")
        if header.startswith("[FAIL]"):
            raise SystemExit(f"🔴 {header}")
    _, n, rate, want = header.split()
    n, rate, want = int(n), int(rate), int(want)
    raw = link.read(n * 2)
    samples = list(struct.unpack(f"<{n}h", raw))
    got = checksum(samples)
    if got != want:
        # 🔴 여기서 멈춘다 — 잘린 녹음으로 STT 를 재면 마이크를 오해한다.
        raise SystemExit(f"🔴 체크섬 불일치 — ESP32 {want} · 파이 {got}. 측정하지 말 것.")
    write_wav(out, samples, rate)
    peak = max(abs(s) for s in samples) if samples else 0
    rms = int((sum(s * s for s in samples) / len(samples)) ** 0.5) if samples else 0
    print(f"✅ {out} — {n}샘플 · {rate}Hz · {n/rate:.2f}초 · RMS {rms} · peak {peak} · 체크섬 일치")


def cmd_tone(hz, sec, out, rate=16000):
    import math
    n = int(rate * sec)
    samples = [int(math.sin(2 * math.pi * hz * i / rate) * 12000) for i in range(n)]
    write_wav(out, samples, rate)
    print(f"✅ {out} — {hz}Hz 순음 {sec}초 · {rate}Hz")


def main():
    ap = argparse.ArgumentParser(description=__doc__,
                                 formatter_class=argparse.RawDescriptionHelpFormatter)
    ap.add_argument("--port", help="시리얼 경로 (기본: USB 신원으로 자동 탐지)")
    ap.add_argument("--tcp", help="무선용 host:port (미구현 — 설계 §7)")
    sub = ap.add_subparsers(dest="cmd", required=True)
    p = sub.add_parser("play");  p.add_argument("wav")
    r = sub.add_parser("rec");   r.add_argument("sec", type=int); r.add_argument("out")
    t = sub.add_parser("tone");  t.add_argument("hz", type=int); t.add_argument("sec", type=float); t.add_argument("out")
    a = ap.parse_args()

    if a.cmd == "tone":
        cmd_tone(a.hz, a.sec, a.out); return

    if a.tcp:
        host, port = a.tcp.split(":")
        link = TcpLink(host, int(port))
    else:
        path = a.port or resolve(*ESP32_S3)
        if not path:
            print("🔴 ESP32 를 못 찾았다. 붙어 있는 장치:", file=sys.stderr)
            for line in describe() or ["  (없음)"]:
                print("  " + line, file=sys.stderr)
            sys.exit(1)
        print(f"🔌 {path}")
        link = SerialLink(path)

    try:
        if a.cmd == "play": cmd_play(link, a.wav)
        else:               cmd_rec(link, a.sec, a.out)
    finally:
        link.close()


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: ESP32 없이 되는 부분부터 확인한다** (순음 생성 + 체크섬 왕복)

Run:
```bash
cd Rpi5/arduino/mic_speaker_test && python3 esp32_audio.py tone 1000 2 /tmp/tone1k.wav && \
python3 -c "
import sys; sys.path.insert(0,'.')
from esp32_audio import read_wav, write_wav, checksum
s, r = read_wav('/tmp/tone1k.wav')
print('샘플', len(s), '레이트', r, '체크섬', checksum(s))
write_wav('/tmp/rt.wav', s, r)
s2, r2 = read_wav('/tmp/rt.wav')
assert (s, r) == (s2, r2), '왕복에서 값이 바뀌었다'
print('✅ wav 왕복 무손실')
"
```
Expected: `샘플 32000 레이트 16000 체크섬 …` 그리고 `✅ wav 왕복 무손실`

- [ ] **Step 3: 인자 오류 처리를 확인한다** (스테레오 거부)

Run:
```bash
cd Rpi5/arduino/mic_speaker_test && python3 -c "
import wave, struct
with wave.open('/tmp/st.wav','w') as w:
    w.setnchannels(2); w.setsampwidth(2); w.setframerate(16000)
    w.writeframes(struct.pack('<200h', *([0]*200)))
" && python3 esp32_audio.py play /tmp/st.wav; echo "종료코드 $?"
```
Expected: `🔴 모노만 된다 — /tmp/st.wav 는 2채널` · 종료코드 1

- [ ] **Step 4: 커밋** (Rpi5 저장소)

```bash
cd Rpi5 && git add arduino/mic_speaker_test/esp32_audio.py && git commit -m "feat(audio): 파이 쪽 오디오 다리 도구 — play/rec/tone

펌웨어와 프로토콜을 공유하므로 같은 폴더에 둔다(camera_stream 전례).
Link 추상화로 유선(SerialLink)·무선(TcpLink)이 같은 얼굴을 한다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: 🔴 관문 — 굽고 왕복을 검증한다

**Files:** (코드 변경 없음 — 실행과 판정만)

**Interfaces:**
- Consumes: Task 1·2·3 전부

- [ ] **Step 1: 굽기 전 현재 상태를 기록해 둔다** (되돌릴 근거)

Run: `python3 Rpi5/Demo/serial_ports.py`
Expected: `ESP32-S3 → /dev/ttyACM0` (경로가 무엇이든 기록해 둔다)

- [ ] **Step 2: 굽는다**

Run:
```bash
PORT=$(python3 Rpi5/Demo/serial_ports.py --path) && \
~/bin/arduino-cli upload -p "$PORT" --fqbn esp32:esp32:XIAO_ESP32S3:PSRAM=opi Rpi5/arduino/mic_speaker_test
```
Expected: `Hard resetting via RTS pin...` 로 끝난다.
🔴 **실패하면 여기서 멈추고 사용자에게 알린다** — BOOT 버튼이 필요할 수 있고 그건 사람이 해야 한다.

- [ ] **Step 3: PSRAM 관문 — 부팅 로그를 본다**

Run:
```bash
python3 -c "
import sys; sys.path.insert(0,'Rpi5/Demo')
import serial, time
from serial_ports import ESP32_S3, resolve
s = serial.Serial(resolve(*ESP32_S3), 115200, timeout=0.5)
s.dtr = False; s.rts = True; time.sleep(0.1); s.rts = False
t = time.time(); buf = b''
while time.time() - t < 6: buf += s.read(4096)
print(buf.decode('utf-8','replace'))
"
```
Expected: `[OK] PSRAM 버퍼 240000샘플 (468KB · 최대 10초)` 가 보인다.
🔴 `[FAIL] PSRAM 할당 실패` 가 나오면 **`PSRAM=opi` 를 빠뜨린 것** — Step 2 로 돌아간다.

- [ ] **Step 4: 왕복 관문 — 녹음 → 덤프 → wav**

Run:
```bash
cd Rpi5/arduino/mic_speaker_test && python3 esp32_audio.py rec 3 /tmp/rt_mic.wav
```
Expected: `[녹음 완료] 48000샘플 …` 에 이어 `✅ /tmp/rt_mic.wav — 48000샘플 · 16000Hz · 3.00초 · RMS … · peak … · 체크섬 일치`
🔴 **체크섬 불일치가 나오면 여기서 멈춘다** — 이후 어떤 소리도 판정에 쓰지 않는다.

- [ ] **Step 5: ESP32 가 보고한 값과 파이가 계산한 값을 대조한다**

`[녹음 완료]` 줄의 `RMS`·`peak` 와 `✅` 줄의 `RMS`·`peak` 가 **같아야 한다.**
다르면 DC 제거 시점이 어긋난 것이므로 Task 2 Step 6 을 다시 본다.

- [ ] **Step 6: 적재 관문 — 순음을 보내 본다** (소리는 내일 확인)

Run:
```bash
cd Rpi5/arduino/mic_speaker_test && python3 esp32_audio.py tone 1000 2 /tmp/tone1k.wav && \
python3 esp32_audio.py play /tmp/tone1k.wav
```
Expected: `[적재] n=32000 rate=16000 sum=… ok` → `[재생 완료]` → `✅ 재생 완료`
⚠️ **소리가 1kHz 로 들리는지는 내일 사람이 확인한다.** 지금은 프로토콜이 통했다는 것까지만 판정한다.

- [ ] **Step 7: 22050Hz 적재 관문** — 레이트 재초기화가 실제로 되는지

Run:
```bash
cd Rpi5/arduino/mic_speaker_test && python3 esp32_audio.py play ~/lab/tts/kss_short.wav
```
Expected: `[적재] n=… rate=22050 sum=… ok` → `[재생 완료]`
🔴 `[FAIL] 스피커를 22050Hz 로 열지 못했다` 가 나오면 **설계 §4.3 의 되돌림**(파이에서 16kHz 로 리샘플)으로 간다.

- [ ] **Step 8: 결과를 사용자에게 보고한다** — 통과/실패와 내일 남은 것

---

### Task 5: `config.py` 인터록 포트 — 폴백을 지키며 고친다

**Files:**
- Modify: `Rpi5/Demo/config.py:235`

**Interfaces:**
- Consumes: Task 1 의 `resolve`

- [ ] **Step 1: 고친다**

`INTERLOCK_PORT = "/dev/ttyACM0"` 를 바꾼다:

```python
# 🔴 포트 번호를 박지 않는다 — 꽂는 순서에 따라 ttyACM0/1 이 뒤바뀐다.
#    2026-09-01 에 이 값이 실제로 ESP32 를 가리키고 있었다(인터록 미연결 상태).
#    ⚠️ 인터록 Arduino 의 USB 신원은 아직 확인하지 못했다(보드가 없어서).
#       그래서 지금은 「ESP32 가 아닌 ACM 장치」를 고르고, 못 고르면 옛 기본값으로
#       폴백한다. 검증하지 못한 것을 기본 경로로 만들지 않는다.
def _resolve_interlock_port(default="/dev/ttyACM0"):
    try:
        from serial.tools import list_ports
        from serial_ports import ESP32_S3          # 상수를 복제하지 않는다
        cands = [p.device for p in list_ports.comports()
                 if p.vid is not None and (p.vid, p.pid) != ESP32_S3]
        if len(cands) == 1:
            return cands[0]
        if len(cands) > 1:
            print(f"[config] ⚠️ 인터록 후보가 여럿이다: {cands} → {default} 사용")
        else:
            print(f"[config] ⚠️ 인터록 장치를 못 찾았다 → {default} 사용")
    except Exception as e:
        print(f"[config] ⚠️ 포트 해석 실패({e}) → {default} 사용")
    return default

INTERLOCK_PORT = _resolve_interlock_port()   # Arduino UNO R4 USB 시리얼
```

- [ ] **Step 2: 지금 상태(ESP32만 연결)에서 무엇을 고르는지 확인한다**

Run: `python3 -c "import sys; sys.path.insert(0,'Rpi5/Demo'); import config; print(config.INTERLOCK_PORT)"`
Expected: `[config] ⚠️ 인터록 장치를 못 찾았다 → /dev/ttyACM0 사용` 뒤에 `/dev/ttyACM0`
🔑 **ESP32 를 인터록으로 집지 않는다는 것**이 이 확인의 목적이다. 경고가 떠야 정상이다.

- [ ] **Step 3: `run_demo.sh` 실기동 확인** (🔴 CLAUDE.md §5 — import 검사로 못 잡는 오류가 있다)

Run: `cd Rpi5/Demo && timeout 25 bash run_demo.sh 2>&1 | tail -30`
Expected: GUI 가 기동하고 즉시 죽지 않는다. `INTERLOCK` 관련 예외가 없다.
⚠️ 인터록 보드가 없으므로 **연결 실패 경고는 정상**이다. 죽지만 않으면 통과.

- [ ] **Step 4: 커밋** (Rpi5 저장소)

```bash
cd Rpi5 && git add Demo/config.py && git commit -m "fix(config): 인터락 포트를 USB 신원으로 해석 — ESP32 오인 방지

INTERLOCK_PORT=/dev/ttyACM0 이 실제로 ESP32 를 가리키고 있었다.
인터록 Arduino 의 신원은 미확인이라 폴백 + 경고로 둔다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## 내일(HW 앞)에서 할 것 — 이 계획의 범위 밖

1. **마이크 청취** ← 첫 작업. 폰 녹음본·ESP32 PDM 녹음본을 FQ 스피커로(귀 옆 5cm)
2. **합성음 청취** — `kss_*.wav` 4개. §10.49 ⛔ 명료도를 닫는다
3. **STT 재측정** — ESP32+폰 나란히 녹음 → `dev/tts/stt_bench.py`
4. 결과를 **통합문서 §10.49·§10.50 과 `[CURRENT]` 표에 함께 기입**(둘 중 하나만 고치지 않는다)

## 중단 규칙

- **PSRAM 할당 실패** → 굽기 옵션 문제. 고쳐서 다시 굽는다.
- **체크섬 불일치** → 전송 경로 결함. **어떤 소리도 판정에 쓰지 않는다.**
- **22050Hz 재초기화 실패** → 설계 §4.3 의 되돌림(파이 리샘플)으로 간다. 그때는 *"뭉갬의 원인에 리샘플이 섞였다"* 를 §10.49 에 **명시**한다.
- **굽기 실패로 보드가 안 올라옴** → 사람 개입(BOOT 버튼)이 필요하다. 멈추고 보고한다.
