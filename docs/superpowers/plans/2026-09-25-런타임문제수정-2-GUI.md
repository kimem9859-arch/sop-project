# 실제 시연 프로그램 문제 수정 ② GUI 상태 일치 Implementation Plan

> 상태: ✅ 완료(2026-09-25 · 세션 `56d1f2c1` · 공동 확인) · Task 0~11 · 최종 리뷰 반영(Important 3 + 박스 여백) · 사용자 결정으로 G8 가장자리 테두리 제외·경고/차단 박스 가운데 통일 · 미룬 사소 4건 · 🔑 계획서의 교체·추가 지시와 Run 명령을 **새 복사본에서 Task 0~9 그대로 실행**해 아래 Expected 를 얻었다(저장소 미반영)

> ⚠️ **정정(2026-09-25)** — 자가 테스트 기록 명령이 `$(basename)` 뒤에 `$?` 를 읽어 모든 종료 코드를 0 으로 적었다(관문이 실패를 못 잡음). 아래 명령은 고친 것(`rc=$?`)이다. 이 결함으로 남은 기록은 무효 — 올바른 재실행 결과로 바꿨다(①: `Rpi5/조사/판정수정-20260925/selftest_기록정정.txt` · ②: `Rpi5/조사/GUI수정-20260925/` 두 파일).

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 화면(GUI)·서브 작업·판정기가 같은 상태를 가리키게 한다 — 경고 = 서브 작업 일시정지 · 차단 = 취소(G1)를 중심으로 G2~G12 를 고친다.

**Architecture:** `SubTask` 에 일시정지·재개를 넣고, `SafetyConsole._on_fsm_state` 가 판정기 상태 변화에 맞춰 서브 작업을 멈추고·잇고·버린다. 버튼 입력(`_press_button`)은 차단 중 무시(G3)·경고 중 정답 = 해제 + 평소대로(D3)를 따른다. 나머지는 각자 제자리(배너 우선순위·문구·설정 창·녹화)에서 고친다.

**Tech Stack:** Python 3.13 · PyQt6(오프스크린 시험) · ffmpeg 는 시험에서 띄우지 않는다

**Spec:** `docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md` §1(D3·D5)·§2.2·§3 — 이 계획서 = ②묶음

## Global Constraints

- 코드 저장소 = `/home/pi/sop-project/Rpi5`(별도 git) · 문서 = `/home/pi/sop-project`. 커밋은 **경로 지정 add** 만.
- 커밋 서명 = `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>` + `Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL` (실행 세션이 다르면 그 세션 값).
- 자가 테스트는 **일괄 러너가 없다** — `Rpi5/Demo` 에서 `selftest/test_*.py` 개별 실행. `test_voice_tts` 만 `~/env/tts/.venv/bin/python`.
- 🔴 **`fsm.py` 는 고치지 않는다**(①묶음에서 끝남). 판정 규칙은 그대로이고 GUI 가 판정기 상태를 **따른다**.
- 🔴 **측정 수치를 계획서에 쓰지 않는다.** ②는 판정 규칙을 바꾸지 않아 `fsm_sim` 값이 변하지 않는다 — Task 9 에서 관문만 다시 확인한다.
- 🔴 **G8 은 화면 모습이 바뀐다**(지금까지 시연 영상에 없던 붉은·주황 테두리와 배너 판). 공동 확인 때 **사진으로** 보인다(Task 7).
- 🔴 **push 는 사용자 결정 전까지 하지 않는다**(2026-09-25 사용자 「푸시 진행하지 말고」 — 다른 세션 커밋이 섞여 있다).

## 관문

- **🔒 홀드아웃** — 이 묶음은 판정 규칙·임계·측정 데이터를 건드리지 않는다. 합격선 = 태스크마다 적힌 Expected(계획서를 복사본에서 미리 실행해 **고정**) — 실행 중에 기대값을 고쳐 통과시키지 않는다.
- **검증 관문** — ①태스크마다 새 테스트가 **고치기 전 실패 → 고친 뒤 통과**(가드는 「전후 통과」 표시) ②Task 9: 자가 테스트가 Task 0 기준선과 같음(새 파일 `test_demo_recorder.py` 한 줄만 늘어남) · `test_imports` 통과 · `fsm_sim --gate` PASS.
- **중단 규칙** — ①확인 단계가 Expected 와 다르면 다음 태스크로 넘어가지 않는다 ②이번 변경과 무관한 기존 테스트가 깨지면 멈추고 보고한다(기대값을 고쳐 통과시키지 않는다) ③같은 수정을 3번 시도해도 안 되면 멈추고 보고한다 ④G8 사진을 사용자가 거부하면 G8 커밋만 되돌린다(`git revert`) — 나머지는 유지.

## Review Focus

1. **경고↔해제가 빠르게 반복될 때 게이지(「일시정지」↔진행) 깜빡임과 멈춘 시간 누적** — 사람은 경고 동안의 시간만 빠지기를 기대한다 → `test_resume_continues_from_remaining`(Task 1)이 누적 계산을 고정한다. 화면 깜빡임은 ⏸ 실기동.
2. **공구 단계에서 경고 중 요구 공구를 쥐고, 해제한 뒤** — 쥔 것은 유지되고 남은 시간이 차면 진행해야 한다 → `test_paused_never_advances`(Task 1)가 「멈춘 동안 진행 금지 · 풀리면 곧바로 진행 가능」을 고정한다.
3. **경고로 멈춰 있을 때 EMO** — 서브 작업은 취소되고 해제 뒤 「작업 시작」 전 대기여야 한다 → `test_g1_emo_during_warning_cancels_and_idles`(Task 2).
4. **실제 ffmpeg 한쪽이 죽는 경우** — 시험은 가짜 파이프만 쓴다 → `test_one_pipe_dies_other_continues`(Task 8)가 흐름을 고정하고, 실촬영 확인은 ⏸.
5. **G8 테두리가 실제 화면 크기·전체화면에서 영상 영역과 맞는지** — 오프스크린 1280×720 사진으로만 확인한다 → ⏸ 실기동에서 눈으로.

## 계획서 표기

코드 블록 앞의 HTML 주석은 **어디에 어떻게 넣는지**를 뜻한다(렌더링에는 안 보인다):
- `<!-- rep: <경로> -->` 다음 두 블록 = 바꿀 원문(정확히 한 곳) → 새 내용
- `<!-- add: <경로> -->` 다음 한 블록 = 파일 끝 `if __name__ == "__main__":` **바로 앞**에 넣는다
- `<!-- new: <경로> -->` 다음 한 블록 = 새 파일
- Expected 의 「…통과」 줄 끝 실행 시간 `(0.00Xs)` 는 비교하지 않는다.

---

### Task 0: 기준선 기록

**Files:** Create `Rpi5/조사/GUI수정-20260925/before_selftest.txt`

- [ ] **Step 1: 자가 테스트 전부를 개별 실행해 결과를 남긴다**

Run:
```bash
cd /home/pi/sop-project/Rpi5/Demo && mkdir -p ../조사/GUI수정-20260925 && \
for t in selftest/test_*.py; do
  py=python3; [ "$(basename $t)" = test_voice_tts.py ] && py=~/env/tts/.venv/bin/python
  timeout 600 $py "$t" >/dev/null 2>&1; rc=$?; echo "$(basename $t) exit=$rc"
done > ../조사/GUI수정-20260925/before_selftest.txt; grep -c "exit=0" ../조사/GUI수정-20260925/before_selftest.txt
```
Expected: `27`

- [ ] **Step 2: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add 조사/GUI수정-20260925/before_selftest.txt && git commit -m "docs(조사): GUI 수정 ② 기준선 — 자가 테스트 (수정 전)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 1: 서브 작업 일시정지 · 게이지 「일시정지」 표시

**Files:** Modify `Rpi5/Demo/sub_task.py` · `Rpi5/Demo/overlay.py`(`GaugePanel.update_view`) · Test `selftest/test_sub_task.py` · `selftest/test_overlay.py`

**Interfaces:**
- Produces: `SubTask.pause(now=None)` · `SubTask.resume(now=None)` · `SubTask.paused -> bool`. 멈춘 동안 `elapsed_sec` 가 늘지 않고 `can_advance` 는 False. Task 2 가 쓴다.

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_sub_task.py -->
```python
# ----------------------------------------------------- 일시정지 (설계 2026-09-25 D5 · G1)
def test_pause_freezes_time():
    """경고로 멈춘 동안은 시간이 흐르지 않는다."""
    print("\n[일시정지] 멈춘 동안 시간 정지")
    s = SubTask(WAIT, now=0.0)
    s.tick(now=10.0)
    s.pause(now=10.0)
    s.tick(now=25.0)
    check(s.paused, "pause → paused")
    check(abs(s.elapsed_sec - 10.0) < 1e-9, f"멈춘 뒤 15초가 지나도 경과 10초 — {s.elapsed_sec}")


def test_resume_continues_from_remaining():
    """풀리면 남은 시간부터 잇는다 — 멈춘 시간은 두 번 멈춰도 합쳐서 뺀다."""
    print("\n[일시정지] 이어서")
    s = SubTask(WAIT, now=0.0)
    s.tick(now=10.0); s.pause(now=10.0); s.tick(now=25.0)
    s.resume(now=25.0)
    s.tick(now=30.0)
    check(not s.paused, "resume → paused 아님")
    check(abs(s.elapsed_sec - 15.0) < 1e-9, f"멈춘 15초는 빼고 15초 — {s.elapsed_sec}")
    s.pause(now=30.0); s.resume(now=34.0); s.tick(now=40.0)
    check(abs(s.elapsed_sec - 21.0) < 1e-9, f"두 번째 멈춤 4초도 뺀다 — {s.elapsed_sec}")
    s.tick(now=50.0)
    check(s.time_done and s.can_advance, "남은 시간이 차면 진행 가능")


def test_paused_never_advances():
    """🔴 멈춘 동안은 시간·공구가 다 차 있어도 넘어가지 않는다."""
    print("\n[일시정지] 진행 금지")
    s = SubTask(TOOL, now=0.0)
    s.tick(now=31.0)                 # 시간은 이미 찼다
    s.pause(now=31.0)
    s.set_tool(s.want_tool)          # 멈춘 동안 요구 공구를 쥔다
    check(not s.can_advance, "멈춰 있으면 can_advance=False")
    s.resume(now=40.0)
    check(s.can_advance, "풀리면 곧바로 진행 가능")
```

<!-- rep: Rpi5/Demo/selftest/test_sub_task.py -->
```python
    test_real_clock_default()
```
```python
    test_real_clock_default()
    test_pause_freezes_time()
    test_resume_continues_from_remaining()
    test_paused_never_advances()
```

<!-- add: Rpi5/Demo/selftest/test_overlay.py -->
```python
def test_gauge_shows_paused():
    """G1 — 경고로 멈춘 서브 작업은 게이지에 「일시정지」가 뜬다(설계 2026-09-25 D5)."""
    print("\n[G1] 게이지 일시정지 표시")
    host = QWidget()
    g = GaugePanel(host)
    s = SubTask(WAIT, now=0.0)
    s.tick(now=12.0)
    s.pause(now=12.0)
    g.update_view(s)
    check(g._time.text().startswith("일시정지"), f"시간 칸 = '{g._time.text()}'")
    s.resume(now=20.0)
    g.update_view(s)
    check(not g._time.text().startswith("일시정지"), f"풀리면 = '{g._time.text()}'")
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_sub_task.py 2>&1 | tail -1; python3 selftest/test_overlay.py 2>&1 | tail -1`
Expected:
```
AttributeError: 'SubTask' object has no attribute 'pause'
AttributeError: 'SubTask' object has no attribute 'pause'
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/sub_task.py -->
```python
        self._tool = None
```
```python
        self._tool = None
        # 일시정지(설계 2026-09-25 D5 · G1) — 경고가 뜨면 멈추고, 풀리면 남은 시간부터 잇는다.
        self._paused_at = None       # 멈춘 시각. None 이면 진행 중
        self._paused_total = 0.0     # 지금까지 멈춰 있던 시간의 합
```

<!-- rep: Rpi5/Demo/sub_task.py -->
```python
        if not self._spec:
            return 0.0
        return max(0.0, self._now - self._start)
```
```python
        if not self._spec:
            return 0.0
        end = self._paused_at if self._paused_at is not None else self._now
        return max(0.0, end - self._start - self._paused_total)
```

<!-- rep: Rpi5/Demo/sub_task.py -->
```python
    @property
    def time_done(self):
        return self.progress >= 1.0
```
```python
    @property
    def time_done(self):
        return self.progress >= 1.0

    @property
    def paused(self):
        """경고로 멈춰 있는가 — 멈춘 동안은 시간이 흐르지 않고 진행하지도 않는다."""
        return self._paused_at is not None
```

<!-- rep: Rpi5/Demo/sub_task.py -->
```python
        if not self._spec:
            return True
        if not self.time_done:
            return False
```
```python
        if not self._spec:
            return True
        if self.paused:
            return False             # 🔴 멈춘 동안은 공구를 쥐어도 넘어가지 않는다(G1)
        if not self.time_done:
            return False
```

<!-- rep: Rpi5/Demo/sub_task.py -->
```python
    def tick(self, now=None):
        """현재 시각을 갱신한다. GUI 타이머가 주기적으로 부른다."""
        self._now = time.time() if now is None else now
```
```python
    def tick(self, now=None):
        """현재 시각을 갱신한다. GUI 타이머가 주기적으로 부른다."""
        self._now = time.time() if now is None else now

    def pause(self, now=None):
        """경고가 뜨면 멈춘다(설계 D5 · G1). 이미 멈춰 있으면 아무 일도 없다."""
        if self._spec and self._paused_at is None:
            self._paused_at = time.time() if now is None else now

    def resume(self, now=None):
        """경고가 풀리면 **남은 시간부터** 잇는다 — 멈춘 동안은 경과에 넣지 않는다."""
        if self._paused_at is None:
            return
        now = time.time() if now is None else now
        self._paused_total += max(0.0, now - self._paused_at)
        self._paused_at = None
        self._now = now
```

<!-- rep: Rpi5/Demo/overlay.py -->
```python
        remain_token = "done" if sub.time_done else "current"
        self._time.setText("완료" if sub.time_done
                           else f"{int(sub.elapsed_sec)}/{int(sub.total_sec)}s")
        self._time.setStyleSheet(theme.text_qss(remain_token, 700))
```
```python
        if sub.paused:
            # 경고로 멈춘 동안 — 시간이 흐르지 않는다는 것을 글자로 알린다(설계 D5 · G1)
            remain_token = "warn"
            self._time.setText(f"일시정지 {int(sub.elapsed_sec)}/{int(sub.total_sec)}s")
        else:
            remain_token = "done" if sub.time_done else "current"
            self._time.setText("완료" if sub.time_done
                               else f"{int(sub.elapsed_sec)}/{int(sub.total_sec)}s")
        self._time.setStyleSheet(theme.text_qss(remain_token, 700))
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_sub_task.py 2>&1 | tail -1; python3 selftest/test_overlay.py 2>&1 | tail -1; python3 selftest/test_console_flow.py 2>&1 | tail -1`
Expected:
```
✅ 서브 작업 검증 통과
✅ 오버레이 스모크 통과
✅ 글라스 UI 흐름 통과
```
(첫 줄 끝의 실행 시간 `(0.00Xs)` 은 비교하지 않는다)

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/sub_task.py Demo/overlay.py Demo/selftest/test_sub_task.py Demo/selftest/test_overlay.py && git commit -m "feat(sub_task): 서브 작업 일시정지·재개 · 게이지 「일시정지」 표시 (GUI 수정 G1 준비)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 2: G1·G3·D3 — 서브 작업이 판정기 상태를 따른다

**Files:** Modify `Rpi5/Demo/safety_console.py`(`_press_button` · `_on_fsm_state` · 새 `_emo_button`·`_cancel_sub`) · `Rpi5/Demo/session_stats.py`(`sub_started`) · Test `selftest/test_console_flow.py`

**Interfaces:**
- Consumes: `SubTask.pause/resume/paused`(Task 1)
- Produces: `SafetyConsole._emo_button() -> str` · `SafetyConsole._cancel_sub(why: str)` — Task 3 이 `_emo_button` 을 쓴다. 테스트 helper `dwell_warning(win, roi)` · `finish_sub(win)` — Task 3~6 이 쓴다.

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
# ============================================================================
# ②GUI 상태 일치 (2026-09-25) — 설계 docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md §3
# 재현 원본 = Rpi5/조사/런타임-검토-20260924/flow_probe.py (리뷰 U#)
# ============================================================================
def dwell_warning(win, roi="B3"):
    """오답 ROI 에 레시피 체류 임계를 넘겨 머문다 — 0.1초 간격 프레임으로 WARNING 을 만든다."""
    t0 = time.time()
    for i in range(6):
        win.fsm.update_vision(roi, t0 + i * 0.1)


def finish_sub(win):
    """서브 작업의 시간·공구 조건을 채우고 타이머 틱 한 번을 흉내 낸다."""
    if win._sub is None:
        return
    win._sub.tick(now=time.time() + 999)
    if win._sub.needs_tool:
        win._sub.set_tool(win._sub.want_tool)
    win._update_sub_view()


def test_g1_warning_pauses_sub():
    """G1 — 경고가 뜨면 서브 작업이 멈추고, 풀면 남은 시간부터 잇는다(리뷰 U10)."""
    print("\n[G1] 경고 = 서브 작업 일시정지")
    win = make_console()
    win._on_cta()
    key(win, "1")                                    # B1 → 10초 서브 시작
    dwell_warning(win, "B3")
    check(win.fsm.state == State.WARNING, f"B3 머묾 → WARNING({win.fsm.state.value})")
    check(win._sub is not None and win._sub.paused, "서브 작업이 멈춘다")
    if win._sub is not None:
        win._sub.tick(now=time.time() + 999)         # 멈춘 동안 시간이 흘러도
        win._update_sub_view()
    check(win._sub is not None and win.fsm.expected_step == 1,
          "🔴 멈춘 동안은 게이지가 차지 않아 진행하지 않는다")
    check(win.gauge_panel._time.text().startswith("일시정지"),
          f"게이지 = '{win.gauge_panel._time.text()}'")
    win._on_alert_release()                          # 경고 해제
    check(win._sub is not None and not win._sub.paused, "해제하면 이어서 돈다")
    finish_sub(win)
    check(win.fsm.expected_step == 2, f"남은 시간이 차면 진행 → {win.fsm.expected_step}")
    win.close()


def test_g1_block_cancels_sub():
    """G1 — 차단이 걸리면 서브 작업을 버린다. 해제 뒤 그 버튼부터 다시."""
    print("\n[G1] 차단 = 서브 작업 취소")
    win = make_console()
    win._on_cta()
    key(win, "1")                                    # B1 서브
    key(win, "3")                                    # 대기 중 오답 → BLOCK
    check(win.fsm.state == State.BLOCK, f"BLOCK({win.fsm.state.value})")
    check(win._sub is None, "서브 작업이 취소된다")
    win._on_alert_release()
    check(win.fsm.state == State.PROCESS_RUN and win.fsm.expected_step == 1,
          f"해제 뒤 1단계 그대로({win.fsm.state.value}·{win.fsm.expected_step})")
    check(win._sub is None, "취소된 서브가 뒤늦게 확정되지 않는다")
    key(win, "1")                                    # 그 버튼부터 다시
    check(win._sub is not None and win._sub.is_active and not win._sub.paused,
          "B1 을 다시 누르면 새로 시작")
    win.close()


def test_g1_emo_during_sub_no_fake_block():
    """G1 — EMO 로 멈춘 뒤 대기 중이던 눌림이 뒤늦게 확정돼 가짜 차단이 나지 않는다(U1 재현 A)."""
    print("\n[G1] EMO 뒤 가짜 차단 없음")
    win = make_console()
    win._on_cta()
    key(win, "1"); finish_sub(win)
    key(win, "2"); finish_sub(win)
    key(win, "3")                                    # B3 서브 시작
    key(win, "E")                                    # 비상정지
    check(win._sub is None, "EMO 차단 → 서브 취소")
    win._on_alert_release()                          # EMO 해제 → 작업 시작 전 대기(P5)
    check(win.fsm.state == State.IDLE, f"IDLE({win.fsm.state.value})")
    check(win._stats._violations == [], f"위반 기록 없음 — {win._stats._violations}")
    win.close()


def test_g1_emo_during_warning_cancels_and_idles():
    """G1 — 경고로 멈춰 있을 때 EMO → 서브 취소 · 해제하면 「작업 시작」 전 대기."""
    print("\n[G1] 경고 중 EMO")
    win = make_console()
    win._on_cta()
    key(win, "1")
    dwell_warning(win, "B3")
    key(win, "E")
    check(win._sub is None, "멈춰 있던 서브가 취소된다")
    win._on_alert_release()
    check(win.fsm.state == State.IDLE and win._sub is None,
          f"해제 → IDLE·서브 없음({win.fsm.state.value})")
    win.close()


def test_g1_wrong_press_during_warning_cancels_paused_sub():
    """G1 — 경고 중 오답을 눌러 차단으로 넘어가면 멈춰 있던 서브도 버린다."""
    print("\n[G1] 경고 → 차단이면 취소")
    win = make_console()
    win._on_cta()
    key(win, "1")
    dwell_warning(win, "B3")
    key(win, "3")                                    # 경고 중 오답 눌림 → BLOCK
    check(win.fsm.state == State.BLOCK, f"BLOCK({win.fsm.state.value})")
    check(win._sub is None, "멈춰 있던 서브도 취소")
    win.close()


def test_g1_retry_after_block_keeps_one_tool_record():
    """G1 — 공구 단계에서 차단 뒤 다시 하면 결과창 공구 줄은 하나다(취소한 시도와 이어 쓴다).

    ⚠️ 고치기 전 코드에서는 통과한다 — 그때는 차단해도 서브가 취소되지 않아 다시 시작할
       일이 없었다. G1(취소)을 넣으면 실패하고(Step 4), 집계를 고쳐 통과시킨다(Step 5).
    """
    print("\n[G1] 차단 뒤 재시도 — 공구 기록 하나")
    win = make_console()
    win._on_cta()
    key(win, "1"); finish_sub(win)
    key(win, "2")                                    # 공구 서브
    key(win, "3")                                    # 오답 → BLOCK → 취소
    win._on_alert_release()
    key(win, "2"); finish_sub(win)                   # 다시 해서 완료
    out = win._stats.finish()
    check([t["button"] for t in out["tools"]] == ["B2"],
          f"공구 기록 = {[t['button'] for t in out['tools']]}")
    win.close()


def test_d3_correct_press_during_warning_resumes_sub():
    """D3 — 경고 중 정답(멈춘 서브의 버튼) = 경고 해제 + 이어서. 단계를 건너뛰지 않는다."""
    print("\n[D3] 경고 중 정답 — 서브 이어서")
    win = make_console()
    win._on_cta()
    key(win, "1")
    dwell_warning(win, "B3")
    key(win, "1")                                    # 경고 중 정답
    check(win.fsm.state != State.WARNING, f"경고 해제({win.fsm.state.value})")
    check(win._sub is not None and not win._sub.paused, "서브가 이어서 돈다")
    check(win.fsm.expected_step == 1, "🔴 남은 대기를 건너뛰지 않는다 — 아직 1단계")
    win.close()


def test_d3_correct_press_during_warning_starts_sub():
    """D3 — 버튼을 누르기 전 경고가 떴을 때 정답을 누르면 경고 해제 + 서브 시작."""
    print("\n[D3] 경고 중 정답 — 서브 시작")
    win = make_console()
    win._on_cta()
    dwell_warning(win, "B3")                         # B1 을 누르기 전에 경고
    check(win.fsm.state == State.WARNING, "WARNING")
    key(win, "1")
    check(win.fsm.state != State.WARNING, f"경고 해제({win.fsm.state.value})")
    check(win._sub is not None and win._sub.is_active and not win._sub.paused,
          "서브 작업이 새로 시작된다")
    win.close()


def test_d3_correct_press_during_warning_no_sub_step():
    """D3 가드(전후 통과) — 서브가 없는 4단계에서 경고 중 정답 = 해제 + 완료(①묶음 P6)."""
    print("\n[D3] 경고 중 정답 — 4단계 완료")
    win = make_console()
    win._on_cta()
    for k in ("1", "2", "3"):
        key(win, k); finish_sub(win)
    dwell_warning(win, "B1")
    check(win.fsm.state == State.WARNING, "4단계에서 WARNING")
    key(win, "4")
    check(win.fsm.state == State.IDLE, f"공정 완료({win.fsm.state.value})")
    check(not win.result_panel.isHidden(), "결과창이 뜬다")
    win.close()


def test_g3_press_ignored_during_block():
    """G3 — 차단 중 정답 버튼은 화면에서도 무시된다 · EMO 는 통과한다(리뷰 U4 재현 A3)."""
    print("\n[G3] 차단 중 입력 무시")
    win = make_console()
    win._on_cta()
    key(win, "3")                                    # 1단계에서 B3 → BLOCK
    key(win, "1")                                    # 차단 중 정답
    check(win._sub is None, "서브 작업이 시작되지 않는다")
    win._on_alert_release()
    check(win._sub is None and win.fsm.expected_step == 1,
          "해제 뒤에도 그 눌림은 인정되지 않는다")
    key(win, "3")                                    # 다시 차단
    key(win, "E")                                    # 차단 중 EMO
    check(win.fsm._emo_active, "EMO 는 차단 중에도 판정기에 닿는다")
    win.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 경고 해제(WARNING)
  ❌ 경고 해제(WARNING)
  ❌ 서브 작업이 취소된다
  ❌ 취소된 서브가 뒤늦게 확정되지 않는다
  ❌ EMO 차단 → 서브 취소
  ❌ 멈춰 있던 서브가 취소된다
  ❌ 서브 작업이 멈춘다
  ❌ 🔴 멈춘 동안은 게이지가 차지 않아 진행하지 않는다
  ❌ 게이지 = '완료'
  ❌ 해제하면 이어서 돈다
  ❌ 멈춰 있던 서브도 취소
  ❌ 서브 작업이 시작되지 않는다
  ❌ 해제 뒤에도 그 눌림은 인정되지 않는다
❌ 실패 13건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        self._last_button = button
        # 🔴 눌림 시각은 **여기서** 찍는다 — _commit_button 이 아니다. 서브 작업이
```
```python
        # 🔴 차단 중에는 판정기처럼 화면도 버튼을 받지 않는다 — EMO 만 통과한다(G3).
        #    받으면 차단 중에 서브 작업이 시작돼, 해제가 먼저 끝나면 그 눌림이 단계
        #    완료로 인정됐다(리뷰 U4 · 인터락이 GND 를 못 끊는 fallback·키보드에서).
        if self.fsm.state == State.BLOCK and button != self._emo_button():
            self._append_log(f"[버튼] {button} 눌림 — 차단 중이라 무시")
            return
        self._last_button = button
        # 🔴 눌림 시각은 **여기서** 찍는다 — _commit_button 이 아니다. 서브 작업이
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
            if button == self._sub_button:
                self._append_log(f"[서브] {button} 재입력 무시 — {self._sub.label} 진행 중")
                return
```
```python
            if button == self._sub_button:
                if self.fsm.state == State.WARNING:
                    # 경고 중 정답 = 경고 해제 + 멈춘 서브 작업을 이어서(설계 §2.2 · D3).
                    # 🔴 단계를 곧바로 완료하지 않는다 — 남은 대기를 건너뛰는 우회로다.
                    self._append_log(f"[서브] {button} — 경고 해제, {self._sub.label} 이어서")
                    self.fsm.release_warning()      # → _on_fsm_state 가 서브를 재개한다
                    return
                self._append_log(f"[서브] {button} 재입력 무시 — {self._sub.label} 진행 중")
                return
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        spec = self._sub_spec_for(button)
        if spec is not None and self.fsm.state != State.IDLE and button == self.fsm.correct_roi:
            self._begin_sub(button, spec)
            return
```
```python
        spec = self._sub_spec_for(button)
        if spec is not None and self.fsm.state != State.IDLE and button == self.fsm.correct_roi:
            if self.fsm.state == State.WARNING:
                # 경고 중 정답 = 경고 해제 + 평소처럼 서브 작업 시작(설계 §2.2 · D3)
                self._append_log(f"[FSM] 경고 중 정답 {button} — 경고 해제")
                self.fsm.release_warning()
            self._begin_sub(button, spec)
            return
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
    def _step_button(self, order):
        for s in (self._recipe or {}).get("steps", []):
            if s.get("order") == order:
                return s.get("button", "")
        return ""
```
```python
    def _step_button(self, order):
        for s in (self._recipe or {}).get("steps", []):
            if s.get("order") == order:
                return s.get("button", "")
        return ""

    def _emo_button(self):
        """비상정지 버튼 이름.

        🔴 `.get(...)` 기본값은 키가 **없을 때만** 적용된다 — emo_button 키가 있는데 값이
           null(None)이면 여전히 None 이 돌아와 fsm.py 의 config 폴백과 어긋난다.
           `or` 로 None 도 폴백시킨다.
        """
        return (self._recipe or {}).get("emo_button") or config.FSM_EMO_BUTTON
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        if self._tool_state is not None:
            self._tool_state = None
        self.camera_thread.set_tool_scan(False)
```
```python
        if self._tool_state is not None:
            self._tool_state = None
        self.camera_thread.set_tool_scan(False)

    def _cancel_sub(self, why):
        """진행 중인 서브 작업을 버린다 — 🔴 눌림을 FSM 에 전달하지 않는다(설계 D5 · 차단 = 취소).

        해제한 뒤에는 그 버튼부터 다시 누른다.
        """
        if self._sub is not None and self._sub.is_active:
            self._append_log(f"[서브] {self._sub.label} 취소 — {why}")
        self._sub_timer.stop()
        self._end_tool_scan()
        self._sub = None
        self._sub_button = None
        self.gauge_panel.update_view(None)
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
    def _on_fsm_state(self, old, new):
        self.status_panel.update_view(new.value, self.fsm.expected_step)
        self._append_log(f"[FSM] {old.value} → {new.value}")
        self._publish_state()
```
```python
    def _on_fsm_state(self, old, new):
        self.status_panel.update_view(new.value, self.fsm.expected_step)
        self._append_log(f"[FSM] {old.value} → {new.value}")
        self._publish_state()

        # 서브 작업은 판정기 상태를 따른다(설계 2026-09-25 D5 · G1) — 경고 = 일시정지 ·
        # 차단 = 취소. 🔴 따로 돌게 두면 EMO 해제 뒤 대기 중이던 눌림이 뒤늦게 확정돼
        # 가짜 차단이 나고(리뷰 U1), 경고 중 게이지가 차서 진행이 조용히 사라진다(U10).
        sub = self._sub
        if sub is not None and sub.is_active:
            if new == State.BLOCK:
                self._cancel_sub("차단")
            elif new == State.WARNING:
                sub.pause()
                self._append_log(f"[서브] {sub.label} 일시정지 — 경고 중")
                self.gauge_panel.update_view(sub)
            elif old == State.WARNING and sub.paused:
                sub.resume()
                self._append_log(f"[서브] {sub.label} 이어서 — 경고 해제")
                self.gauge_panel.update_view(sub)
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
            # 🔴 .get(...) 기본값은 키가 "없을 때"만 적용된다 — emo_button 키가
            #    있는데 값이 null(None)이면 여전히 None 이 돌아와 fsm.py 의
            #    config 폴백과 어긋난다. `or` 로 None 도 폴백시킨다.
            emo = (self._recipe or {}).get("emo_button") or config.FSM_EMO_BUTTON
```
```python
            emo = self._emo_button()
```

- [ ] **Step 4: 화면만 고친 상태 — 재시도 공구 기록이 두 줄이 되는 것을 본다**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 공구 기록 = ['B2', 'B2']
❌ 실패 1건
```

- [ ] **Step 5: 집계 — 취소한 시도와 이어 쓴다**

<!-- rep: Rpi5/Demo/session_stats.py -->
```python
        spec = spec or {}
        if not spec.get("tool"):
            return
        self._tool_names.update(spec.get("tool_names") or {})
```
```python
        spec = spec or {}
        if not spec.get("tool"):
            return
        self._tool_names.update(spec.get("tool_names") or {})
        # 🔑 차단으로 취소된 서브를 같은 버튼으로 다시 시작하면 **이어 쓴다**(설계 D5 · G1) —
        #    새로 만들면 결과창에 같은 단계 공구 줄이 두 번 찍힌다. 오답 횟수는 남기고
        #    쥔 시각은 이번 시도부터 다시 잰다.
        if self._tools and self._tools[-1]["button"] == button:
            self._tools[-1].update({"want": spec.get("tool"), "_start": _now(now),
                                    "grasp_sec": None})
            return
```

- [ ] **Step 6: 통과 확인 + 회귀**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | tail -1; python3 selftest/test_session_stats.py 2>&1 | tail -1; python3 selftest/test_imports.py 2>&1 | tail -1`
Expected:
```
✅ 글라스 UI 흐름 통과
✅ 결과 집계 검증 통과
3/3 passed
```

- [ ] **Step 7: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/safety_console.py Demo/session_stats.py Demo/selftest/test_console_flow.py && git commit -m "feat(gui): 서브 작업이 판정기 상태를 따른다 — 경고 = 일시정지 · 차단 = 취소 · 차단 중 입력 무시 · 경고 중 정답 = 해제 + 평소대로 (GUI 수정 G1·G3·D3)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 3: G4·G5 — EMO 해제 뒤처리 · EMO 차단 문구

**Files:** Modify `Rpi5/Demo/safety_console.py`(`_on_fsm_state` BLOCK 분기 · `_release_block`) · Test `selftest/test_console_flow.py`

**Interfaces:**
- Consumes: `_emo_button()`(Task 2) · `finish_sub`(Task 2 테스트 helper)
- Produces: 테스트 helper `notify_titles(win) -> list[str]`

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def notify_titles(win):
    """알림 패널에 쌓인 글자 전부(제목·부제)."""
    from PyQt6.QtWidgets import QLabel
    return [lbl.text() for row in win.notify_panel._rows for lbl in row.findChildren(QLabel)]


def test_g4_emo_release_ends_work():
    """G4 — EMO 해제 → 「작업 시작」 전 대기. 진행 중이던 작업은 알림 하나로 끝나고 집계가 비워진다."""
    print("\n[G4] EMO 해제 뒤처리")
    win = make_console()
    win._on_cta()
    key(win, "1"); finish_sub(win)                   # 1단계 완료
    key(win, "E")
    win._on_alert_release()
    check(win.fsm.state == State.IDLE, f"IDLE({win.fsm.state.value})")
    check(not win.btn_cta.isHidden(), "「작업 시작」 버튼이 다시 보인다")
    check(win.result_panel.isHidden(), "결과창은 뜨지 않는다")
    check(not win._stats.running and win._stats._steps == [], "집계가 비워진다")
    check(any("비상정지로 작업 중단" in t for t in notify_titles(win)),
          "「비상정지로 작업 중단」 알림")
    win._on_cta()                                    # 새로 시작
    for k in ("1", "2", "3", "4"):
        key(win, k)
        finish_sub(win)
    out = win._stats.finish()
    check(len(out["steps"]) == 4, f"결과 단계 수 = 이번 회차 4 — {len(out['steps'])}")
    win.close()


def test_g5_emo_banner_text():
    """G5 — EMO 차단은 「순서 위반」이 아니라 「비상정지」 문구다 · 위반 차단은 그대로."""
    print("\n[G5] EMO 차단 문구")
    win = make_console()
    win._on_cta()
    key(win, "E")
    check("비상정지" in win.alert._line1.text(), f"EMO 차단 = '{win.alert._line1.text()}'")
    win.close()
    win = make_console()
    win._on_cta()
    key(win, "3")
    check("순서 위반" in win.alert._line1.text(), f"위반 차단 = '{win.alert._line1.text()}'")
    win.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 집계가 비워진다
  ❌ 「비상정지로 작업 중단」 알림
  ❌ EMO 차단 = '순서 위반이 계속되어 인터락이 작동했습니다'
❌ 실패 3건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
            emo = self._emo_button()
            if self._last_button != emo:
                self._stats.violation(self.fsm.correct_roi, self._last_button or "?", "block")
            self.glow.set_level("block")
            self.alert.show_block()
            self._dim_others(True)
            self._notify("danger", "전기 입력 차단됨",
                         f"{self.fsm.expected_step}단계 {self.fsm.correct_roi}")
```
```python
            emo = self._emo_button()
            self.glow.set_level("block")
            if self._last_button != emo:
                self._stats.violation(self.fsm.correct_roi, self._last_button or "?", "block")
                self.alert.show_block()
                self._notify("danger", "전기 입력 차단됨",
                             f"{self.fsm.expected_step}단계 {self.fsm.correct_roi}")
            else:
                # 🔴 EMO 차단을 「순서 위반」이라 적지 않는다(G5) — 해제하려면 EMO 부터
                #    복귀해야 한다는 것을 문구가 알려야 한다.
                self.alert.show_block("비상정지로 차단됐습니다 — EMO 를 복귀한 뒤 해제하세요")
                self._notify("danger", "비상정지", "전기 입력 차단됨")
            self._dim_others(True)
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
            box.show()
            return
        self.fsm.release_block()
```
```python
            box.show()
            return
        was_running = self._stats.running
        self.fsm.release_block()
        if self.fsm.state == State.IDLE:
            # EMO 차단 해제 → 「작업 시작」 전 대기(P5 · 설계 D4). 진행 중이던 작업은 여기서
            # 끝난다 — 결과창 없이 알림 하나로 마무리하고 집계를 비운다(G4).
            self._stats.reset()
            self._append_log("[FSM] 비상정지 해제 — 「작업 시작」 전 대기")
            if was_running:
                self._notify("danger", "비상정지로 작업 중단",
                             "「작업 시작」을 눌러 1단계부터 다시 합니다")
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | tail -1`
Expected: `✅ 글라스 UI 흐름 통과`

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/safety_console.py Demo/selftest/test_console_flow.py && git commit -m "feat(gui): EMO 해제 뒤 작업 중단 알림·집계 비움 · EMO 차단 문구 분리 (GUI 수정 G4·G5)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 4: G2·G11 — 경고창 우선순위 · 같은 오답 공구는 한 번

**Files:** Modify `Rpi5/Demo/safety_console.py`(`__init__` · `_begin_sub` · `_on_tool` · `_update_sub_view`) · Test `selftest/test_console_flow.py`

**Interfaces:**
- Consumes: `dwell_warning`·`finish_sub`(Task 2)
- Produces: `SafetyConsole._wrong_tool_noted`(str|None — 이미 알리고 센 오답 공구)

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def test_g2_tool_banner_does_not_cover_warning():
    """G2 — 「다른 공구」 경고가 순서 경고를 덮지 않는다. 해제 버튼이 남는다(리뷰 U3 재현 C2)."""
    print("\n[G2] 경고창 우선순위 — 순서 경고")
    win = make_console()
    win._on_cta()
    key(win, "1"); finish_sub(win)
    key(win, "2")                                    # 공구 서브
    win._sub.set_tool("driver"); win._tick_sub()     # 다른 공구
    check(win.alert.mode == "tool", "공구 경고")
    dwell_warning(win, "B3")
    win._tick_sub()                                  # 200ms 뒤
    check(win.alert.mode == "order", f"순서 경고가 남는다({win.alert.mode})")
    check(not win.alert._release.isHidden(), "「경고 해제」 버튼이 보인다")
    win._on_alert_release()
    win._tick_sub()
    check(win.alert.mode == "tool", f"해제 뒤 공구 경고가 다시 뜬다({win.alert.mode})")
    win.close()


def test_g2_block_not_covered_and_stays():
    """G2 가드(Task 2 뒤 통과) — 차단 배너는 공구 경고에 덮이지 않는다(리뷰 U2 재현 C)."""
    print("\n[G2] 경고창 우선순위 — 차단")
    win = make_console()
    win._on_cta()
    key(win, "1"); finish_sub(win)
    key(win, "2")
    win._sub.set_tool("driver"); win._tick_sub()
    key(win, "3")                                    # 오답 → BLOCK
    win._tick_sub()
    check(win.alert.mode == "block", f"차단 배너 유지({win.alert.mode})")
    check(not win.alert._release.isHidden(), "「차단 해제」 버튼이 보인다")
    win.close()


def test_g11_wrong_tool_counted_once():
    """G11 — 검출이 깜빡여도 같은 오답 공구는 한 번만 알리고 센다 · 내려놓고 다시 쥐면 다시 센다(U12)."""
    print("\n[G11] 다른 공구 횟수")
    win = make_console()
    win._on_cta()
    key(win, "1"); finish_sub(win)
    key(win, "2")
    want = win._sub.want_tool
    other = "driver" if want != "driver" else "pliers"
    box_other = (other, 0.9, 300, 100, 400, 200)
    n0 = win.notify_panel.count
    win.camera_thread.tool_signal.emit([box_other], (350, 150))   # 다른 공구를 쥠
    win.camera_thread.tool_signal.emit([box_other], None)         # 손이 잠깐 안 보임
    win.camera_thread.tool_signal.emit([box_other], (350, 150))   # 다시 보임
    check(win.notify_panel.count - n0 == 1, f"알림 1건 — {win.notify_panel.count - n0}")
    win.camera_thread.tool_signal.emit([box_other], (10, 10))     # 빈손으로 보임 = 내려놓음
    win.camera_thread.tool_signal.emit([box_other], (350, 150))   # 다시 쥠
    out = win._stats.finish()
    check(out["tools"][-1]["wrong"].get(other) == 2,
          f"내려놓고 다시 쥐면 2회 — {out['tools'][-1]['wrong']}")
    win.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 알림 1건 — 2
  ❌ 내려놓고 다시 쥐면 2회 — {'driver': 3}
  ❌ 순서 경고가 남는다(tool)
  ❌ 「경고 해제」 버튼이 보인다
❌ 실패 4건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        self._tool_override = None   # 설정 메뉴에서 바꾼 지정 공구(세션 한정)
```
```python
        self._tool_override = None   # 설정 메뉴에서 바꾼 지정 공구(세션 한정)
        self._wrong_tool_noted = None  # 이미 알리고 센 오답 공구(G11) — 같은 공구는 한 번
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
    def _begin_sub(self, button, spec):
        self._sub = SubTask(spec)
        self._sub_button = button
```
```python
    def _begin_sub(self, button, spec):
        self._sub = SubTask(spec)
        self._sub_button = button
        self._wrong_tool_noted = None
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        self._sub.set_tool(tool)
```
```python
        self._sub.set_tool(tool)
        # 🔑 손이 보이는데 쥔 공구가 없으면 「내려놓음」으로 본다 — 같은 오답 공구를 다시
        #    쥐면 다시 센다(G11). 손이 안 보일 때(None)는 깜빡임이라 기억을 지우지 않는다.
        if fingertip is not None and tool is None:
            self._wrong_tool_noted = None
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        # 공구 오선택 — 🔴 해제 버튼 없이, 올바른 공구로 바꾸면 스스로 풀린다
        if sub.wrong_tool:
            if self.alert.mode != "tool":
                self.alert.show_wrong_tool(sub.wrong_tool_name, sub.want_tool_name)
                self.glow.set_level("warn")
                self._dim_others(True)
                self._notify("warn", "다른 공구입니다",
                             f"{sub.wrong_tool_name} → {sub.want_tool_name} 필요")
                self._stats.tool_grasped(sub.wrong_tool, False)
                self._relayout()
```
```python
        # 공구 오선택 — 🔴 해제 버튼 없이, 올바른 공구로 바꾸면 스스로 풀린다
        if sub.wrong_tool:
            # 🔴 순서 경고·차단 배너를 덮지 않는다 — 우선순위 차단 > 순서 경고 > 공구
            #    경고(G2). 덮으면 해제 버튼이 사라지고 배너 없이 갇혔다(리뷰 U2·U3).
            if self.alert.mode is None:
                self.alert.show_wrong_tool(sub.wrong_tool_name, sub.want_tool_name)
                self.glow.set_level("warn")
                self._dim_others(True)
                self._relayout()
            # 🔴 같은 오답 공구는 한 번만 알리고 센다(G11) — 손이 잠깐 안 보일 때마다
            #    배너가 다시 떠도 결과창 횟수는 「집은 횟수」여야 한다(리뷰 U12).
            #    기억은 공구가 바뀌거나 빈손이 보일 때(_on_tool) 지운다.
            if sub.wrong_tool != self._wrong_tool_noted:
                self._wrong_tool_noted = sub.wrong_tool
                self._notify("warn", "다른 공구입니다",
                             f"{sub.wrong_tool_name} → {sub.want_tool_name} 필요")
                self._stats.tool_grasped(sub.wrong_tool, False)
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | tail -1`
Expected: `✅ 글라스 UI 흐름 통과`

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/safety_console.py Demo/selftest/test_console_flow.py && git commit -m "feat(gui): 공구 경고는 순서 경고·차단을 덮지 않는다 · 같은 오답 공구는 한 번만 알리고 센다 (GUI 수정 G2·G11)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 5: G9 — 시연용 `t` 키 유지

**Files:** Modify `Rpi5/Demo/tool_state.py` · `Rpi5/Demo/safety_console.py`(`_sim_tool_grasped`) · Test `selftest/test_tool_state.py` · `selftest/test_console_flow.py`

**Interfaces:**
- Produces: `ToolState.force_grasped()` — 판정을 「쥠」으로 확정한다(이후 `update()` 는 요구 공구를 돌려준다).

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_tool_state.py -->
```python
def test_force_grasped_sticks():
    """G9 — 시연용 확정은 이후 스캔(손·공구 없음)에도 유지된다."""
    print("\n[G9] 시연용 확정 유지")
    s = ToolState("wrench")
    s.force_grasped()
    check(s.phase == "grasped", "grasped")
    check(s.update([], None) == "wrench", "손이 안 보여도 wrench")
    check(s.update([], (10, 10)) == "wrench", "빈손이어도 wrench")
```

<!-- rep: Rpi5/Demo/selftest/test_tool_state.py -->
```python
    test_쥔공구_클래스는_부재를_근거로_쓰지_않는다()
```
```python
    test_쥔공구_클래스는_부재를_근거로_쓰지_않는다()
    test_force_grasped_sticks()
```

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def test_g9_sim_tool_key_survives_next_scan():
    """G9 — `t` 로 쥔 처리는 다음 공구 검출 결과에 덮이지 않는다(리뷰 U6 재현 D)."""
    print("\n[G9] t 키 유지")
    win = make_console()
    win._on_cta()
    key(win, "1"); finish_sub(win)
    key(win, "2")
    key(win, "t")
    win.camera_thread.tool_signal.emit([], (100, 100))            # 다음 스캔: 공구 미검출
    check(win._sub is not None and win._sub.tool_ok, "t 로 쥔 처리가 유지된다")
    win.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_tool_state.py 2>&1 | tail -1; python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
AttributeError: 'ToolState' object has no attribute 'force_grasped'
  ❌ t 로 쥔 처리가 유지된다
❌ 실패 1건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/tool_state.py -->
```python
    @property
    def want_tool(self):
        return self._want
```
```python
    @property
    def want_tool(self):
        return self._want

    def force_grasped(self):
        """시연용 `t` — 요구 공구를 쥔 것으로 **확정**한다(판정 우회 · G9).

        확정은 유지되므로(`update` 첫 줄) 다음 스캔 결과가 덮어쓰지 못한다 — 종전에는
        SubTask 만 바꿔 약 1초 뒤 스캔이 「찾기」로 되돌렸다(리뷰 U6).
        """
        self._phase = "grasped"
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        sub.set_tool(sub.want_tool)
        self._stats.tool_grasped(sub.want_tool, True)
```
```python
        if self._tool_state is not None:
            self._tool_state.force_grasped()     # 🔴 판정기도 확정해야 다음 스캔이 안 덮는다(G9)
        sub.set_tool(sub.want_tool)
        self._stats.tool_grasped(sub.want_tool, True)
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_tool_state.py 2>&1 | tail -1; python3 selftest/test_console_flow.py 2>&1 | tail -1`
Expected:
```
✅ 공구 판정 검증 통과
✅ 글라스 UI 흐름 통과
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/tool_state.py Demo/safety_console.py Demo/selftest/test_tool_state.py Demo/selftest/test_console_flow.py && git commit -m "fix(gui): 시연용 t 키 — 공구 판정기도 확정해 다음 스캔이 덮지 않는다 (GUI 수정 G9)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 6: G6·G7 — 설정 창 공구 단계 제목 · 음성비서 공구

**Files:** Modify `Rpi5/Demo/overlay_menu.py`(`SettingsPanel`) · `Rpi5/Demo/safety_console.py`(`_wire_tool_settings` · `_publish_state`) · Test `selftest/test_overlay_menu.py` · `selftest/test_console_flow.py`

**Interfaces:**
- Produces: `SettingsPanel.set_tools(tools, current_tool, tool_names=None, step_order=None)`

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_overlay_menu.py -->
```python
def test_settings_tool_caption_from_step():
    """G6 — 지정 공구 제목이 레시피의 공구 단계 번호를 따른다."""
    print("\n[G6] 지정 공구 제목")
    p = SettingsPanel()
    p.set_tools(["a", "b"], "a", None, step_order=2)
    check(p._tool_caption.text() == "2단계 지정 공구", f"'{p._tool_caption.text()}'")
```

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def test_g6_g7_tool_settings_follow_recipe():
    """G6 — 설정 창 제목 = 레시피 공구 단계 · G7 — 음성비서 상태에 바꾼 공구가 실린다."""
    print("\n[G6·G7] 공구 설정")
    win = make_console()
    check(win.settings_panel._tool_caption.text() == "2단계 지정 공구",
          f"설정 창 제목 '{win.settings_panel._tool_caption.text()}'")
    pubs = []
    win._state_pub.publish = pubs.append
    win._tool_override = "driver"
    win._on_cta()
    key(win, "1"); finish_sub(win)                   # 2단계로
    sub = pubs[-1]["서브작업"]
    check(sub["tool"] == "driver", f"공개된 공구 = {sub['tool']}")
    win.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_overlay_menu.py 2>&1 | tail -1; python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
TypeError: SettingsPanel.set_tools() got an unexpected keyword argument 'step_order'
  ❌ 설정 창 제목 '4단계 지정 공구'
  ❌ 공개된 공구 = wrench
❌ 실패 2건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/overlay_menu.py -->
```python
        self._tool_caption = QLabel("4단계 지정 공구")
```
```python
        self._tool_caption = QLabel("지정 공구")      # 단계 번호는 set_tools 가 레시피에서 붙인다(G6)
```

<!-- rep: Rpi5/Demo/overlay_menu.py -->
```python
    def set_tools(self, tools, current_tool, tool_names=None):
        """레시피가 준 선택지로 라디오를 만든다 — 목록을 코드에 박지 않는다."""
        names = tool_names or {}
```
```python
    def set_tools(self, tools, current_tool, tool_names=None, step_order=None):
        """레시피가 준 선택지로 라디오를 만든다 — 목록을 코드에 박지 않는다.

        step_order = 공구를 쓰는 단계 번호 — 제목에 쓴다(G6 · 종전엔 「4단계」가 박혀 있었다).
        """
        names = tool_names or {}
        self._tool_caption.setText(f"{step_order}단계 지정 공구" if step_order else "지정 공구")
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
                self.settings_panel.set_tools(
                    sub.get("tools", []), sub.get("tool"), sub.get("tool_names"))
```
```python
                self.settings_panel.set_tools(
                    sub.get("tools", []), sub.get("tool"), sub.get("tool_names"),
                    step_order=s.get("order"))
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
                if s.get("order") == cur and s.get("sub"):
                    spec = s["sub"]
```
```python
                if s.get("order") == cur and s.get("sub"):
                    # 🔴 설정에서 바꾼 지정 공구를 반영한다 — 판정(_sub_spec_for)과 같은 값이어야
                    #    음성 안내가 실제 판정 공구와 맞다(G7).
                    spec = self._sub_spec_for(s.get("button")) or s["sub"]
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_overlay_menu.py 2>&1 | tail -1; python3 selftest/test_console_flow.py 2>&1 | tail -1`
Expected:
```
✅ 메뉴·알림·설정 스모크 통과
✅ 글라스 UI 흐름 통과
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/overlay_menu.py Demo/safety_console.py Demo/selftest/test_overlay_menu.py Demo/selftest/test_console_flow.py && git commit -m "fix(gui): 설정 창 공구 단계 제목을 레시피에서 · 음성비서 상태에 설정한 공구 반영 (GUI 수정 G6·G7)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 7: G8 — 발광 테두리·배너 판을 실제로 그린다

**Files:** Modify `Rpi5/Demo/overlay.py`(`GlowFrame.__init__` · `AlertBanner`) · Test `selftest/test_overlay.py` · Create `Rpi5/조사/GUI수정-20260925/g8_screenshot.py` · `g8_warning.png` · `g8_block.png`

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_overlay.py -->
```python
def _px(root, x, y):
    return root.grab().toImage().pixelColor(x, y).name()


def test_g8_glow_and_banner_paint():
    """G8 — 발광 테두리와 배너 판이 실제로 그려진다(픽셀 · 리뷰 U7·U8)."""
    print("\n[G8] 발광·배너 그리기")
    old_bg = getattr(theme, "PANEL_BACKGROUND", True)
    theme.PANEL_BACKGROUND = False                   # 기본값 — 이때 안 그려졌다
    try:
        root = QWidget(); root.resize(800, 600); root.setStyleSheet("background:#808080;")
        glow = GlowFrame(root); glow.relayout(root.rect())
        alert = AlertBanner(root)
        alert.apply_theme()                          # _init_ui → _apply_theme 와 같은 순서
        glow.set_level("block")
        alert.show_block()
        alert.setGeometry(QRect(200, 200, 400, 150))
        root.show(); _app.processEvents()
        x = glow.geometry().left() + 3
        check(_px(root, x, 300) == theme.C("danger").lower(),
              f"테두리 자리 = {_px(root, x, 300)} (기대 {theme.C('danger')})")
        check(_px(root, 210, 280) != "#808080",
              f"배너 판 = {_px(root, 210, 280)} (배경 회색 그대로면 안 그려진 것)")
        alert.apply_theme()                          # 떠 있는 동안 테마 재적용
        _app.processEvents()
        check(_px(root, 210, 280) != "#808080", "테마를 다시 칠해도 판이 남는다")
        root.close()
    finally:
        theme.PANEL_BACKGROUND = old_bg
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_overlay.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 테두리 자리 = #808080 (기대 #ff5252)
  ❌ 배너 판 = #808080 (배경 회색 그대로면 안 그려진 것)
❌ 실패 2건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/overlay.py -->
```python
    def __init__(self, parent=None):
        super().__init__(parent)
        self.setAttribute(Qt.WidgetAttribute.WA_TransparentForMouseEvents, True)
        self._level = None
        self.hide()
```
```python
    def __init__(self, parent=None):
        super().__init__(parent)
        self.setAttribute(Qt.WidgetAttribute.WA_TransparentForMouseEvents, True)
        # 🔴 파이썬 하위 클래스 QWidget 은 이 속성 없이는 QSS 테두리를 **안 그린다**
        #    (리뷰 U7 — 픽셀로 확인: 테두리 자리가 배경 그대로였다 · _Panel.apply_theme 과 같은 원인).
        self.setAttribute(Qt.WidgetAttribute.WA_StyledBackground, True)
        self._level = None
        self.hide()
```

<!-- rep: Rpi5/Demo/overlay.py -->
```python
        self._shown_mode = None         # 화면에 그려져 있는 mode (등장 트리거 비교용)
```
```python
        self._shown_mode = None         # 화면에 그려져 있는 mode (등장 트리거 비교용)
        self._last_paint = None         # 마지막 _paint 인자 — 테마를 다시 칠할 때 쓴다(G8)
```

<!-- rep: Rpi5/Demo/overlay.py -->
```python
        prev_mode = self._shown_mode
        self._shown_mode = self._mode
```
```python
        prev_mode = self._shown_mode
        self._shown_mode = self._mode
        self._last_paint = (token, mark, title, line1, line2, release_text, indent2)
```

<!-- rep: Rpi5/Demo/overlay.py -->
```python
    @property
    def mode(self):
        return self._mode
```
```python
    @property
    def mode(self):
        return self._mode

    def apply_theme(self):
        """🔴 배너는 판·테두리가 곧 경고 표시다 — 「공정 단계 패널 배경」 설정과 무관하게 늘 그린다(G8).

        기본 `_Panel.apply_theme` 은 PANEL_BACKGROUND(기본 끔)에 따라 배경 그리기를 꺼서
        배너 판·색 테두리·차단 맥박이 한 번도 안 그려졌다(리뷰 U8). 떠 있는 동안 테마를
        바꾸면 지금 모드로 다시 칠한다 — 일반 패널 QSS 로 덮이면 색 테두리가 사라졌다.
        """
        self.setAttribute(Qt.WidgetAttribute.WA_StyledBackground, True)
        if self._mode is not None and self._last_paint is not None:
            self._paint(*self._last_paint)
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_overlay.py 2>&1 | tail -1; python3 selftest/test_console_flow.py 2>&1 | tail -1`
Expected:
```
✅ 오버레이 스모크 통과
✅ 글라스 UI 흐름 통과
```

- [ ] **Step 5: 사용자에게 보일 사진을 만든다**

<!-- new: Rpi5/조사/GUI수정-20260925/g8_screenshot.py -->
```python
"""G8 사진 — 경고·차단 때 화면(오프스크린 1280×720). 공동 확인에서 사용자에게 보인다.

실행: cd Rpi5/Demo && python3 ../조사/GUI수정-20260925/g8_screenshot.py
"""
import os
import sys
import time

os.environ.setdefault("QT_QPA_PLATFORM", "offscreen")
HERE = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, os.path.join(HERE, "..", "..", "Demo"))

import config
config.RECORDING_ENABLED = False
config.CAMERA_TCP_HOST = "127.0.0.1"
config.TCP_RECV_TIMEOUT_SEC = 0.3
config.TCP_RECONNECT_DELAY_SEC = 0.1
config.GPIO_INPUT_ENABLED = False
config.INTERLOCK_ENABLED = False
config.UI_ANIMATION = False

from PyQt6.QtWidgets import QApplication
app = QApplication.instance() or QApplication([])
from safety_console import SafetyConsole

win = SafetyConsole()
win.resize(1280, 720)
win._relayout()
win.show(); app.processEvents()
win._on_cta()
win._press_button("B1")
t0 = time.time()
for i in range(6):
    win.fsm.update_vision("B3", t0 + i * 0.1)        # 오답 머묾 → 경고
win._relayout(); app.processEvents()
win.grab().save(os.path.join(HERE, "g8_warning.png"))
win._press_button("B3")                              # 경고 중 오답 → 차단
win._relayout(); app.processEvents()
win.grab().save(os.path.join(HERE, "g8_block.png"))
print("saved", sorted(f for f in os.listdir(HERE) if f.endswith(".png")))
win.close()
```

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 ../조사/GUI수정-20260925/g8_screenshot.py 2>&1 | tail -1`
Expected: `saved ['g8_block.png', 'g8_warning.png']`

두 사진을 `Read` 로 열어 붉은(차단)·주황(경고) 테두리와 배너 판이 보이는지 **눈으로** 확인한다. 안 보이면 멈춘다(중단 규칙 ①).

- [ ] **Step 6: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/overlay.py Demo/selftest/test_overlay.py 조사/GUI수정-20260925/g8_screenshot.py 조사/GUI수정-20260925/g8_warning.png 조사/GUI수정-20260925/g8_block.png && git commit -m "fix(overlay): 경고·차단 발광 테두리와 배너 판을 실제로 그린다 — 화면 모습이 바뀜 (GUI 수정 G8)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 8: G10·G12 — 시연 녹화 1인칭 크기 · 한쪽이 죽으면 알림

**Files:** Modify `Rpi5/Demo/safety_console.py`(`__init__` 촬영 블록 · `_start_demo_capture` · `_update_conn_bar`) · `Rpi5/Demo/demo_recorder.py` · `Rpi5/Demo/demo_ffmpeg.py` · Create `selftest/test_demo_recorder.py` · Test `selftest/test_console_flow.py`

**Interfaces:**
- Produces: `DemoRecorder.take_dead() -> list[str]`(멈춘 1인칭 이름 — 가져가면 비워진다) · `demo_recorder.FPV_NAMES = ("오버레이 켬", "오버레이 끔")` · `FfmpegSet.fpv_pipes` 는 즉시 죽은 자리를 **None** 으로 남긴다.

- [ ] **Step 1: 실패하는 테스트**

<!-- new: Rpi5/Demo/selftest/test_demo_recorder.py -->
```python
"""시연 녹화 1인칭 파이프 검증 — 한쪽이 죽어도 다른 쪽은 계속 민다(G12).

실행: python3 Demo/selftest/test_demo_recorder.py
⚠️ ffmpeg 를 띄우지 않는다 — FfmpegSet 을 가짜로 바꾼다.
"""
import os
import sys
import tempfile
import threading
import time

_DEMO_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _DEMO_DIR)

import numpy as np

import config
from demo_recorder import DemoRecorder

_fails = []


def check(cond, msg):
    print(("  ✅ " if cond else "  ❌ ") + msg)
    if not cond:
        _fails.append(msg)


class _Good:
    def __init__(self):
        self.writes = 0

    def write(self, b):
        self.writes += 1


class _Broken:
    def write(self, b):
        raise BrokenPipeError("ffmpeg 가 죽었다")


class _FakeFf:
    """FfmpegSet 대역 — start 는 주어진 파이프 목록을 그대로 쓴다."""

    def __init__(self, pipes):
        self.fpv_pipes = pipes

    def start(self, screen_rect, fpv_size, paths):
        return []

    def stop(self):
        pass


def _recorder(pipes, tmp):
    rec = DemoRecorder(tmp, "20260925_000000", "시험", "켬")
    rec._ff = _FakeFf(pipes)
    img = np.zeros((8, 6, 3), np.uint8)
    rec.submit_camera(img, img)
    return rec


def test_one_pipe_dies_other_continues():
    """G12 — 오버레이 켬 쪽이 죽어도 끔 쪽은 계속 받는다."""
    print("\n[G12] 한쪽이 죽어도 다른 쪽은 계속")
    old_fps = config.DEMO_CAPTURE_FPS
    config.DEMO_CAPTURE_FPS = 100                    # 짧게 돌린다
    try:
        with tempfile.TemporaryDirectory() as tmp:
            good = _Good()
            rec = _recorder([_Broken(), good], tmp)
            rec._fpv_size = (6, 8)
            rec._running = True
            t = threading.Thread(target=rec._feed, daemon=True)
            t.start(); time.sleep(0.2); rec._running = False; t.join(timeout=2)
            check(good.writes > 3, f"끔 쪽이 계속 받는다 — {good.writes}회")
            check(getattr(rec, "take_dead", lambda: None)() == ["오버레이 켬"],
                  "멈춘 쪽이 알림 대상에 오른다")
            check(getattr(rec, "take_dead", lambda: None)() == [], "한 번 가져가면 비워진다")
    finally:
        config.DEMO_CAPTURE_FPS = old_fps


def test_immediate_death_is_reported():
    """G12 — 시작하자마자 죽은 쪽(None 자리)도 알림 대상에 오른다."""
    print("\n[G12] 즉시 종료도 알림")
    old = config.demo_wants
    config.demo_wants = lambda what: False           # 피더 스레드를 띄우지 않는다
    try:
        with tempfile.TemporaryDirectory() as tmp:
            rec = _recorder([None, _Good()], tmp)
            rec.start((0, 0, 100, 100), (0, 0, 10, 10))
            check(getattr(rec, "take_dead", lambda: None)() == ["오버레이 켬"],
                  "시작 때 죽은 쪽이 오른다")
            rec.stop()
    finally:
        config.demo_wants = old


if __name__ == "__main__":
    for _name, _fn in sorted(globals().items()):
        if _name.startswith("test_"):
            _fn()
    print()
    if _fails:
        print(f"❌ 실패 {len(_fails)}건")
        for m in _fails:
            print(f"   - {m}")
        sys.exit(1)
    print("✅ 시연 녹화 파이프 검증 통과")
```

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def test_g10_frame_sink_attached_at_start():
    """G10 — 시연 촬영이면 카메라 sink 를 기동 때 붙여 첫 프레임이 촬영 시작 전에 담긴다(U11)."""
    print("\n[G10] 1인칭 녹화 크기 — sink 선연결")
    old = (config.DEMO_CAPTURE, config.DEMO_CAPTURE_START_TIMEOUT)
    config.DEMO_CAPTURE = True
    config.DEMO_CAPTURE_START_TIMEOUT = 3600         # 시험 중 촬영이 실제로 시작되지 않게
    try:
        win = make_console()
        check(win.camera_thread._frame_sink is not None, "기동 직후 sink 가 붙어 있다")
        win._demo_done = True                        # 닫을 때 촬영을 켜지 않게
        win.close()
    finally:
        config.DEMO_CAPTURE, config.DEMO_CAPTURE_START_TIMEOUT = old


def test_g12_dead_fpv_stream_notifies():
    """G12 — 1인칭 녹화 한쪽이 멈추면 알림이 뜬다."""
    print("\n[G12] 녹화 중단 알림")
    win = make_console()

    class _FakeDemo:
        def take_dead(self):
            return ["오버레이 켬"]

    win._demo, win._demo_on = _FakeDemo(), True
    n0 = win.notify_panel.count
    win._update_conn_bar()
    check(win.notify_panel.count == n0 + 1, f"알림 +1 — {win.notify_panel.count - n0}")
    win._demo, win._demo_on = None, False
    win.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_demo_recorder.py 2>&1 | grep -E "^  ❌|^❌"; python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 시작 때 죽은 쪽이 오른다
  ❌ 끔 쪽이 계속 받는다 — 0회
  ❌ 멈춘 쪽이 알림 대상에 오른다
  ❌ 한 번 가져가면 비워진다
❌ 실패 4건
  ❌ 기동 직후 sink 가 붙어 있다
  ❌ 알림 +1 — 0
❌ 실패 2건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/demo_recorder.py -->
```python
from demo_ffmpeg import FfmpegSet
```
```python
from demo_ffmpeg import FfmpegSet

# 1인칭 두 벌의 이름 — FfmpegSet.fpv_pipes 순서(fpv_on, fpv_off)와 같다.
FPV_NAMES = ("오버레이 켬", "오버레이 끔")
```

<!-- rep: Rpi5/Demo/demo_recorder.py -->
```python
        self._pushed = 0
        self._awake_at = 0.0
```
```python
        self._pushed = 0
        self._awake_at = 0.0
        self._dead = []              # 멈춘 1인칭 이름 — GUI 가 take_dead() 로 가져가 알린다(G12)

    def _mark_dead(self, name):
        with self._lock:
            self._dead.append(name)

    def take_dead(self):
        """멈춘 1인칭 이름들 — 가져가면 비워진다(GUI 스레드가 부른다)."""
        with self._lock:
            out, self._dead = self._dead, []
        return out
```

<!-- rep: Rpi5/Demo/demo_recorder.py -->
```python
        self._started = time.time()
        self._running = True
```
```python
        # 시작하자마자 죽은 1인칭은 None 자리로 남는다(demo_ffmpeg) — 알림 대상에 올린다(G12).
        for name, pipe in zip(FPV_NAMES, self._ff.fpv_pipes):
            if pipe is None:
                self._mark_dead(name)
        self._started = time.time()
        self._running = True
```

<!-- rep: Rpi5/Demo/demo_recorder.py -->
```python
            if latest is not None and len(pipes) == 2:
                for stream, img in zip(pipes, latest):
                    try:
                        if (img.shape[1], img.shape[0]) != self._fpv_size:
                            img = cv2.resize(img, self._fpv_size)
                        stream.write(img.tobytes())
                    except (BrokenPipeError, ValueError, OSError):
                        self._running = False
                        break
                self._pushed += 1
```
```python
            if latest is not None and len(pipes) == 2:
                for i, (stream, img) in enumerate(zip(pipes, latest)):
                    if stream is None:
                        continue                  # 이미 멈춘 쪽
                    try:
                        if (img.shape[1], img.shape[0]) != self._fpv_size:
                            img = cv2.resize(img, self._fpv_size)
                        stream.write(img.tobytes())
                    except (BrokenPipeError, ValueError, OSError):
                        # 🔴 이 쪽만 버린다 — 종전에는 하나가 죽으면 둘 다 조용히 멈췄다(G12)
                        pipes[i] = None
                        self._mark_dead(FPV_NAMES[i])
                if all(p is None for p in pipes):
                    self._running = False
                    break
                self._pushed += 1
```

<!-- rep: Rpi5/Demo/demo_ffmpeg.py -->
```python
                self._procs.remove(p)
                if p.stdin in self.fpv_pipes:
                    self.fpv_pipes.remove(p.stdin)
```
```python
                self._procs.remove(p)
                if p.stdin in self.fpv_pipes:
                    # 🔴 빼지 말고 자리를 비운다 — 빼면 [켬, 끔] 짝이 어긋나 남은 쪽에도
                    #    아무것도 안 밀렸다(G12). 받는 쪽(demo_recorder)이 None 을 건너뛴다.
                    self.fpv_pipes[self.fpv_pipes.index(p.stdin)] = None
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
            self._demo = DemoRecorder(self._demo_dir, stamp,
                                      config.DEMO_SCENARIO, config.DEMO_OVERLAY)
            self.camera_thread.set_draw_boxes(config.DEMO_OVERLAY == "켬")
```
```python
            self._demo = DemoRecorder(self._demo_dir, stamp,
                                      config.DEMO_SCENARIO, config.DEMO_OVERLAY)
            # 🔴 sink 를 **지금** 붙인다 — 촬영 시작(첫 프레임 도착 시)에 붙이면 그 순간
            #    `_latest` 가 비어 1인칭 크기가 늘 폴백 480×640 이 됐다(리뷰 U11 · G10).
            #    카메라 스레드는 화면 신호보다 먼저 sink 를 부르므로 첫 프레임이 이미 담긴다.
            self.camera_thread.set_frame_sink(self._demo.submit_camera)
            self.camera_thread.set_draw_boxes(config.DEMO_OVERLAY == "켬")
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        # 🔴 sink 를 먼저 붙인다 — start() 가 첫 프레임 크기를 보고 ffmpeg 입력 규격을 정한다.
        self.camera_thread.set_frame_sink(self._demo.submit_camera)
        problems = self._demo.start(
```
```python
        # 🔑 sink 는 __init__ 에서 이미 붙였다 — start() 가 첫 프레임 크기를 보고 ffmpeg
        #    입력 규격을 정한다(G10).
        problems = self._demo.start(
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        if config.SHOW_FPS:
            self.fps_label.setText("" if fps is None else f"{fps:.1f} fps")
```
```python
        if config.SHOW_FPS:
            self.fps_label.setText("" if fps is None else f"{fps:.1f} fps")

        # 시연 녹화 1인칭 두 벌 중 멈춘 것 — 🔴 조용히 멈추지 않게 알린다(G12).
        if self._demo is not None and self._demo_on:
            for name in self._demo.take_dead():
                self._append_log(f"[시연촬영] ⚠️ 1인칭 「{name}」 녹화가 멈췄습니다 — 다른 쪽은 계속합니다")
                self._notify("danger", "시연 녹화 일부 중단", f"1인칭 {name}")
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_demo_recorder.py 2>&1 | tail -1; python3 selftest/test_console_flow.py 2>&1 | tail -1; python3 selftest/test_imports.py 2>&1 | tail -1`
Expected:
```
✅ 시연 녹화 파이프 검증 통과
✅ 글라스 UI 흐름 통과
3/3 passed
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/safety_console.py Demo/demo_recorder.py Demo/demo_ffmpeg.py Demo/selftest/test_demo_recorder.py Demo/selftest/test_console_flow.py && git commit -m "fix(demo): 1인칭 녹화 크기는 첫 프레임에서 · 한쪽이 멈춰도 다른 쪽은 계속하고 알린다 (GUI 수정 G10·G12)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 9: 검증 관문

**Files:** Create `Rpi5/조사/GUI수정-20260925/after_selftest.txt` · `after_gate.txt`

- [ ] **Step 1: 자가 테스트 전부 · 관문**

Run:
```bash
cd /home/pi/sop-project/Rpi5/Demo && D=../조사/GUI수정-20260925 && \
for t in selftest/test_*.py; do
  py=python3; [ "$(basename $t)" = test_voice_tts.py ] && py=~/env/tts/.venv/bin/python
  timeout 600 $py "$t" >/dev/null 2>&1; rc=$?; echo "$(basename $t) exit=$rc"
done > $D/after_selftest.txt; diff $D/before_selftest.txt $D/after_selftest.txt; \
python3 test/fsm_sim.py --gate > $D/after_gate.txt 2>&1; echo gate_exit=$?
```
Expected:
```
4a5
> test_demo_recorder.py exit=0
gate_exit=0
```
- diff 가 새 파일 한 줄 말고 더 있으면 → 중단 규칙 ②. · `gate_exit` ≠ 0 → 멈춘다.

- [ ] **Step 2: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add 조사/GUI수정-20260925/after_selftest.txt 조사/GUI수정-20260925/after_gate.txt && git commit -m "docs(조사): GUI 수정 ② 결과 — 자가 테스트·관문 (수정 후)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 10: 정본·문서 반영

🔴 통합문서를 열면 `.claude/rules/정본편집.md` 가 붙는다 — 그 규칙을 따른다(현행만 본문 · 폐기는 `docs/변경이력.md` · 미구현은 장 끝 ⏸).

**Files:** Modify `docs/통합문서.md`(§7 장 끝 ⏸ 첫 항목 · §8 GUI 동작 · 🔓 행) · `docs/변경이력.md` · `Rpi5/함수목록.md` §4.1(2·4·5·10) · `docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md` 머리 · 이 계획서 머리

- [ ] **Step 1: §7 장 끝 ⏸ 「서브 작업이 있는 단계에서 경고 중 정답 버튼」 항목을 지우고**, §8 의 GUI 동작 서술 자리에 현행을 적는다 — 경고 = 서브 작업 일시정지(게이지 「일시정지」) · 차단 = 취소(그 버튼부터 다시) · 차단 중 버튼 입력 무시(EMO 제외) · 경고 중 정답 = 해제 + 평소대로 · 배너 우선순위(차단 > 순서 경고 > 공구 경고) · EMO 차단 문구 · EMO 해제 = 알림 「비상정지로 작업 중단」. 자리는 `grep -n "서브 작업\|배너\|해제 버튼" docs/통합문서.md` 로 §8 안에서 찾는다.
- [ ] **Step 2: 변경이력** — 「서브 작업은 경고·차단 중에도 돈다」(암묵 동작이었다 — 리뷰 U1·U10 근거)가 폐기됐다는 한 줄을 §12 제어·피드백·GUI 기재 절에.
- [ ] **Step 3: 🔓 행** — 「①판정 완료 · ②③ 대기」 → 「①② 완료 · ③ 대기」, 계획서 경로 추가 · ⏸ 실시연 금지 문구를 「③ 전에는 실시연 가능 여부를 사용자와 정한다」로(①·② 중간 상태가 풀렸다).
- [ ] **Step 4: 함수목록 §4.1** — 2(EMO 배너)·4(설정 창 4단계)·5(음성비서 공구)·10(녹화 한쪽 멈춤)에 「✅ 해결 — GUI 수정 ②(2026-09-25)」.
- [ ] **Step 5: spec·계획서 머리** — 상태 줄 갱신(공동 확인 전이면 「구현·관문 통과(공동 확인 대기)」).
- [ ] **Step 6: 커밋** (두 저장소)

```bash
cd /home/pi/sop-project/Rpi5 && git add 함수목록.md && git commit -m "docs(함수목록): GUI 수정 ② 반영 — §4.1-2·4·5·10 해결

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
cd /home/pi/sop-project && git add docs/통합문서.md docs/변경이력.md docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md docs/superpowers/plans/2026-09-25-런타임문제수정-2-GUI.md && git commit -m "docs(정본): GUI 수정 ② — 경고 = 서브 일시정지 · 차단 = 취소 · 배너 우선순위 · EMO 문구·해제 뒤처리

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 11: 최종 리뷰 · 공동 확인 · 마무리

- [ ] **Step 1: 최종 리뷰 1회** — `superpowers:requesting-code-review`, 새 맥락 리뷰어(가장 성능 좋은 모델). 기준에 사용자 원문: G1 「경고는 일시정지, 차단은 작업 취소로 했으면해」 · D3 맞물림 승인. Review Focus 를 그대로 넘긴다.
- [ ] **Step 2: 공동 확인** — 태스크마다 한 줄 · **G8 사진 두 장**(`g8_warning.png`·`g8_block.png`)을 보인다 · ⏸ 남은 것(실기동 화면 · 실촬영 녹화). 🔴 Claude 단독 「완료」 선언 금지.
- [ ] **Step 3: `work-close`** — push 는 사용자 결정에 따른다(Global Constraints).
