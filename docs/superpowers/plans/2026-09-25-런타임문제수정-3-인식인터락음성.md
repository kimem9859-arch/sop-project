# 실제 시연 프로그램 문제 수정 ③ 인식 · 인터락 · 음성 Implementation Plan

> 상태: ✅ **완료(2026-09-25 · 세션 `ac3b92f1` · 공동 확인 「마무리 · push 안 함」)** · 최종 리뷰 반영(ACK 기반 연결 · EMO 배너 안내 · 배너 연결 추종 · 랜드마크 그리기 순서) · R2 유지(설계 D10 · 저널 §12.75) · ⏸ 실HW(Arduino · 서브 ESP32 실재생) — Task 0~12 실제 저장소에 같은 도구로 적용 · Expected 전부 일치 · Rpi5 `f3e5bcc`..`2045882` · 📝 작성(2026-09-25 · 세션 `56d1f2c1` 초안 → `ac3b92f1` 드라이런·보완) · 🔑 계획서의 교체·추가 지시와 Run 명령을 **새 복사본에서 Task 0~12 그대로 실행**해 아래 Expected 를 얻었다(저장소 미반영) · 드라이런이 잡은 계획 결함 = `test_hoi_sim` 회귀(R2 트랙 계약) · 설계서와 어긋난 2곳(R2 「오래된 트랙 우선」 · I1 「계속 찾는다」) · 미룬 사소 7건 누락 → Task 2·5·11 에 반영

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 손·버튼 인식의 헛판정 원인(R1~R3)과 카메라 재연결 직후 경고(R5), 인터락이 안 붙거나 어긋나는 문제(I1~I3), 음성비서 재생 확인 고장(V1)과 EMO 안내(V2)·설명·녹음 파일(V3·V4)을 고친다.

**Architecture:** 인식은 `hand_tracker`(손 하나 고르기)·`camera_thread`(두 프레임 확정 트랙 · 박스 그리기 전 손 검출 · 재연결 신호)에서, 판정기는 재연결 때 관측만 지우는 `forget_observation()` 하나를 더한다. 인터락은 포트를 **연결할 때마다 다시 찾고**, 장치 노드가 사라지면 끊김으로 보며, 재연결 동기화는 **그 시점의 최신 요청**을 보낸다. 음성은 소켓에서 직접 줄을 읽어 무음 타임아웃을 견딘다.

**Tech Stack:** Python 3.13 · PyQt6(오프스크린) · pyserial(시험에서는 가짜) · 소켓 쌍(시험용 ESP32 대역)

**Spec:** `docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md` §1(D7·D8·D9)·§4 — 이 계획서 = ③묶음

## Global Constraints

- 코드 저장소 = `/home/pi/sop-project/Rpi5`(별도 git) · 문서 = `/home/pi/sop-project`. 커밋은 **경로 지정 add** 만.
- 커밋 서명 = `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>` + `Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL` (실행 세션이 다르면 그 세션 값).
- 자가 테스트는 **일괄 러너가 없다** — `Rpi5/Demo` 에서 `selftest/test_*.py` 개별 실행. `test_voice_tts` 만 `~/env/tts/.venv/bin/python`. 🔴 **종료 코드는 `rc=$?` 로 먼저 받는다**(②에서 `$(basename)` 뒤 `$?` 가 0 으로 덮였다).
- 🔴 **Hailo 를 쓰는 시험(`test_console_flow` 등)을 다른 GUI·측정 실행과 동시에 돌리지 않는다** — 장치를 나눠 쓸 수 없다(리뷰 ② 참고).
- 🔴 **R4 는 코드를 고치지 않는다**(spec D7) · 호출어 **동작은 바꾸지 않는다**(D8 — 설명만).
- 🔴 **측정 도구(`test/bench_detector.py`)의 추적 함수 복사본은 고치지 않는다** — ⏸ 측정 도구 정합 작업에서 실제 시연과 맞춘다. 이번 R2 로 둘의 차이가 하나 더 생긴다(Task 13 에 기록).
- **미룬 사소 7건**(①·② 최종 리뷰 — 작업로그 「→ ③ 때」)은 Task 11(코드·주석·재현 스크립트)과 Task 13(함수목록)에서 닫는다.
- 🔴 **실HW 검증은 ⏸** — 인터락(I1~I3)은 Arduino 가 없어 가짜 시리얼로만 · 음성(V1)은 소켓 쌍으로만. 실재생 확인은 공동 확인 때 사용자와 정한다.
- 🔴 **push 는 사용자 결정 전까지 하지 않는다**(2026-09-25 「푸시 진행하지 말고」).

## 관문

- **🔒 홀드아웃** — 판정 임계·측정 데이터를 건드리지 않는다. `fsm_sim` 은 판정기만 쓰므로 값이 바뀌지 않아야 한다(R5 의 `forget_observation` 은 시뮬레이터가 부르지 않는다). 합격선 = 태스크마다 적힌 Expected(복사본 드라이런으로 **구현 전에 고정**).
- **검증 관문** — ①태스크마다 새 테스트가 **고치기 전 실패 → 고친 뒤 통과**(가드는 「전후 통과」 표시) ②Task 12: 자가 테스트가 Task 0 기준선보다 나빠지지 않음(새 파일 3개만 늘어남) · `fsm_sim --gate` PASS · `test_imports` 통과.
- **중단 규칙** — ①확인 단계가 Expected 와 다르면 다음 태스크로 넘어가지 않는다 ②이번 변경과 무관한 기존 테스트가 깨지면 멈추고 보고한다 ③같은 수정을 3번 시도해도 안 되면 멈추고 보고한다 ④인터락 시험이 실제 장치(`/dev/ttyACM*`)에 쓰기를 시도하면 즉시 멈춘다(가짜 시리얼 주입 실패).

## Review Focus

1. **인터락 장치 없이 시연할 때** — 「연결 포기」 알림 없이 조용히 계속 찾고(로그 「장치를 못 찾았다」 한 줄), 차단되면 배너 둘째 줄에 「화면에서만 차단 중」이 보여야 한다(전기가 끊겼다고 믿게 하지 않는다). 찾은 장치가 **열리지 않을 때만** 종전처럼 몇 번 뒤 포기한다 → `test_i1_device_plugged_after_start`(Task 5) · `test_i1_block_banner_says_screen_only`(Task 7).
2. **재연결 직후 손가락이 방금 완료한 버튼 위에 그대로 있는 경우** — 방금 버튼 예외는 유지돼야 한다 → `test_r5_forget_keeps_just_done`(Task 4).
3. **버튼이 화면에 처음 들어온 첫 프레임** — 두 프레임 확정 규칙 때문에 한 프레임(약 0.07초) 늦게 판정에 들어간다. 사람은 알아채지 못해야 한다 → `test_r2_new_track_confirmed_after_two_frames`(Task 2)가 「한 프레임 늦음」을 고정한다.
4. **음성 재생이 15초 넘게 길어지는 경우** — `_drain` 의 기다림 한도(15초)에 걸려 「확인 안 됨」이 될 수 있다 · 지금 고정 wav 는 모두 짧다 → 시험 없음(⏸ 실재생에서 본다).
5. **두 손이 모두 기준 미달이거나 flags 모양이 (N,1)** — 손 없음으로 보거나 올바른 손을 골라야 한다 → `test_r1_pick_hand_uses_same_hand`(Task 1).

## 계획서 표기

코드 블록 앞의 HTML 주석은 **어디에 어떻게 넣는지**를 뜻한다(렌더링에는 안 보인다):
- `<!-- rep: <경로> -->` 다음 두 블록 = 바꿀 원문(정확히 한 곳) → 새 내용
- `<!-- add: <경로> -->` 다음 한 블록 = 파일 끝 `if __name__ == "__main__":` **바로 앞**에 넣는다
- `<!-- new: <경로> -->` 다음 한 블록 = 새 파일
- Expected 의 「…통과」 줄 끝 실행 시간 `(0.00Xs)` 는 비교하지 않는다.
- 블록의 언어 표시(`python`·`text`)는 파일 종류일 뿐 적용 방식은 같다(Task 11 의 `.txt` 머리 한 줄).
- 드라이런 도구 = `.superpowers/sdd/2026-09-25-런타임문제수정-3-인식인터락음성/plan_dryrun.py`(git 밖 · 이 파이에만 · ② 도구에 `text` 블록 허용만 더함) — `python3 plan_dryrun.py <계획서> <복사본 루트> [태스크번호,…]` 로 위 지시와 `Run` 을 순서대로 적용·실행한다. 없으면 지시를 손으로 적용한다(지시가 정확히 한 곳을 가리키므로 결과는 같다).

---

### Task 0: 기준선 기록

**Files:** Create `Rpi5/조사/인식인터락음성-20260925/before_selftest.txt`

- [ ] **Step 1: 자가 테스트 전부를 개별 실행해 결과를 남긴다**

Run:
```bash
cd /home/pi/sop-project/Rpi5/Demo && mkdir -p ../조사/인식인터락음성-20260925 && \
for t in selftest/test_*.py; do
  py=python3; [ "$(basename $t)" = test_voice_tts.py ] && py=~/env/tts/.venv/bin/python
  timeout 600 $py "$t" >/dev/null 2>&1; rc=$?; echo "$(basename $t) exit=$rc"
done > ../조사/인식인터락음성-20260925/before_selftest.txt; grep -c "exit=0" ../조사/인식인터락음성-20260925/before_selftest.txt; grep -v "exit=0" ../조사/인식인터락음성-20260925/before_selftest.txt
```
Expected: `28` (실패 줄 없음)

- [ ] **Step 2: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add 조사/인식인터락음성-20260925/before_selftest.txt && git commit -m "docs(조사): 인식·인터락·음성 수정 ③ 기준선 — 자가 테스트 (수정 전)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 1: R1 — 신뢰도와 좌표를 같은 손에서

**Files:** Modify `Rpi5/Demo/hand_tracker.py` · Create `Rpi5/Demo/selftest/test_recognition.py`

**Interfaces:**
- Produces: `hand_tracker.pick_hand(flags, lms, min_score)` — 신뢰도 1위 손의 랜드마크 또는 None. 시험 파일 `test_recognition.py`(가짜 검출기 주입 · `check`) — Task 2·3·4 가 시험을 더한다.

- [ ] **Step 1: 실패하는 테스트**

<!-- new: Rpi5/Demo/selftest/test_recognition.py -->
```python
"""인식 쪽 수정 검증(R1~R3·R5) — Hailo·카메라 없이.

실행: python3 Demo/selftest/test_recognition.py
🔴 버튼 검출기를 가짜로 바꿔 끼운다 — `camera_thread` 는 import 할 때 검출기(Hailo)를 연다.
정본 = 상위 docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md §4.1
"""
import os
import sys
import types

_DEMO_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _DEMO_DIR)
os.environ.setdefault("QT_QPA_PLATFORM", "offscreen")

import numpy as np


class _FakeDetector:
    backend_name = "시험용"
    NAMES = {0: "B1", 1: "B2", 2: "B3", 3: "B4", 4: "EMO"}

    def __init__(self):
        self.dets = []

    def class_name(self, i):
        return self.NAMES[i]

    def detect(self, frame):
        return list(self.dets)


_FAKE_DET = _FakeDetector()
_fake_mod = types.ModuleType("detector")
_fake_mod.create_detector = lambda: _FAKE_DET
sys.modules["detector"] = _fake_mod

import config
config.HAND_ENABLED = False
config.TOOL_ENABLED = False

from PyQt6.QtWidgets import QApplication
_app = QApplication.instance() or QApplication([])

import camera_thread as ct

_fails = []


def check(cond, msg):
    print(("  ✅ " if cond else "  ❌ ") + msg)
    if not cond:
        _fails.append(msg)


# ---------------------------------------------------------------- R1 손 고르기
def test_r1_pick_hand_uses_same_hand():
    """R1 — 신뢰도와 좌표를 같은 손에서 가져온다(검토 C10)."""
    print("\n[R1] 손 고르기")
    from hand_tracker import pick_hand
    lm_a, lm_b = ["0번 손"], ["1번 손"]
    check(pick_hand([0.1, 0.9], [lm_a, lm_b], 0.5) is lm_b, "신뢰도 높은 1번 손의 좌표를 쓴다")
    check(pick_hand([[0.9], [0.1]], [lm_a, lm_b], 0.5) is lm_a, "flags 모양이 (N,1) 이어도")
    check(pick_hand([0.1, 0.3], [lm_a, lm_b], 0.5) is None, "둘 다 기준 미달이면 손 없음")
    check(pick_hand(None, [lm_a], 0.5) is None, "flags 없음 → 손 없음")
    check(pick_hand([0.9], [], 0.5) is None, "랜드마크 없음 → 손 없음")


if __name__ == "__main__":
    for _name, _fn in sorted(globals().items()):
        if _name.startswith("test_"):
            _fn()
    print()
    if _fails:
        print(f"❌ 실패 {len(_fails)}건")
        for m in _fails:
            print(f"   - {m}")
        sys.exit(1)
    print("✅ 인식 검증 통과")
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_recognition.py 2>&1 | tail -1`
Expected:
```
ImportError: cannot import name 'pick_hand' from 'hand_tracker' (/home/pi/sop-project/Rpi5/Demo/hand_tracker.py)
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/hand_tracker.py -->
```python
            if len(lms) == 0 or flags is None:
                return None
            if float(np.max(flags)) < config.HAND_MIN_SCORE:
                return None
            hand = lms[0]
```
```python
            hand = pick_hand(flags, lms, config.HAND_MIN_SCORE)
            if hand is None:
                return None
```

<!-- rep: Rpi5/Demo/hand_tracker.py -->
```python
TIP = 8          # MediaPipe 손 랜드마크 인덱스 8 = 검지 끝. 접촉 판정 기준점(§7.2)
```
```python
TIP = 8          # MediaPipe 손 랜드마크 인덱스 8 = 검지 끝. 접촉 판정 기준점(§7.2)


def pick_hand(flags, lms, min_score):
    """여러 손 중 **신뢰도가 가장 높은 손 하나**의 랜드마크. 기준 미달·손 없음이면 None.

    🔴 신뢰도와 좌표는 **같은 손**에서 가져온다(R1 · 검토 C10) — 종전에는 신뢰도는 모든 손
       중 최댓값, 좌표는 0번 손(팜 점수 1위)이라, 1번 손만 좋으면 엉터리 0번 손끝이 판정에
       들어갔다. 두 손을 함께 판정하지는 않는다(설계 R1).
    """
    import numpy as np
    if flags is None or len(lms) == 0:
        return None
    f = np.asarray(flags, dtype=float).reshape(-1)[:len(lms)]
    if f.size == 0:
        return None
    i = int(np.argmax(f))
    if f[i] < min_score:
        return None
    return lms[i]
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_recognition.py 2>&1 | tail -1; python3 selftest/test_hand_group.py > /dev/null 2>&1; echo hand_group_exit=$?`
Expected:
```
✅ 인식 검증 통과
hand_group_exit=0
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/hand_tracker.py Demo/selftest/test_recognition.py && git commit -m "fix(hand): 신뢰도와 손끝 좌표를 같은 손에서 — 신뢰도 1위 손 하나를 고른다 (인식 수정 R1)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 2: R2 — 새 버튼 트랙은 두 프레임 연속 봐야 확정

**Files:** Modify `Rpi5/Demo/config.py` · `Rpi5/Demo/camera_thread.py`(`_update_tracks` · `_one_per_class` · `_labeled_boxes` · `_draw_yolo` · 검출 목록 내보내기) · Test `selftest/test_recognition.py` · `selftest/test_hoi_sim.py`(가짜 트랙에 `confirmed`)

**Interfaces:**
- Produces: 트랙 dict 에 `hits`(int) · `confirmed` 가 False 인 **미확정 트랙**이 한 프레임 동안 존재할 수 있다 — 판정(`_labeled_boxes`)·그리기·검출 목록은 **확정 트랙만** 쓴다. `config.YOLO_CONFIRM_HITS = 2`. 같은 클래스 하나 남기기 순서 = **지금 보임(miss) → 오래됨(hits) → 점수**(설계 R2 「오래된 트랙 우선」).
- 🔴 트랙을 손으로 만들어 판정 함수에 넘기는 곳은 `confirmed` 키가 있어야 한다 — 저장소 안에서는 `selftest/test_hoi_sim.py` 하나뿐이다(드라이런에서 `KeyError: 'confirmed'` 로 발견 · `grep -rn "roi_at_point\|zone_at_point\|_labeled_boxes"` 전수).

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_recognition.py -->
```python
# ---------------------------------------------------------------- R2 두 프레임 확정
def _det(cls, score, box):
    return (cls, score, *box)


def test_r2_single_frame_fake_not_used():
    """R2 — 한 프레임짜리 오분류(B3 자리에 가짜 EMO)는 판정 박스가 되지 않는다(검토 C11ⓐ)."""
    print("\n[R2] 한 프레임 가짜")
    tr = []
    box = (100, 100, 140, 140)
    for _ in range(2):
        ct._update_tracks(tr, [_det(2, 0.9, box)])                       # B3 확정
    ct._update_tracks(tr, [_det(2, 0.9, box), _det(4, 0.71, (105, 105, 130, 130))])
    labels = [b[0] for b in ct._labeled_boxes(tr)]
    check(labels == ["B3"], f"판정 박스 = {labels}")
    roi, _level = ct.zone_at_point(115, 115, tr, ring=0)
    check(roi == "B3", f"손끝 ROI = {roi} — 가짜 EMO 가 끼어들면 안 된다")


def test_r2_established_not_displaced():
    """R2 — 자리 잡은 트랙은 한 프레임짜리 같은 클래스 가짜에 밀려나지 않는다(검토 C11ⓑ)."""
    print("\n[R2] 자리 잡은 트랙 유지")
    tr = []
    real = (200, 200, 240, 240)
    for _ in range(2):
        ct._update_tracks(tr, [_det(3, 0.9, real)])
    ct._update_tracks(tr, [_det(3, 0.55, real), _det(3, 0.66, (400, 50, 440, 90))])
    boxes = [b[1:] for b in ct._labeled_boxes(tr)]
    check(boxes == [real], f"B4 박스 = {boxes} — 손에 가려 점수가 낮아진 진짜 자리여야 한다")


def test_r2_established_beats_persistent_fake():
    """R2 — 둘 다 보이면 먼저 자리 잡은 트랙이 남는다 — 여러 프레임 이어진 가짜도 점수로 못 이긴다(설계 R2)."""
    print("\n[R2] 오래된 트랙 우선")
    tr = []
    real = (200, 200, 240, 240)
    for _ in range(3):
        ct._update_tracks(tr, [_det(3, 0.9, real)])
    for _ in range(3):
        ct._update_tracks(tr, [_det(3, 0.55, real), _det(3, 0.9, (400, 50, 440, 90))])
    boxes = [b[1:] for b in ct._labeled_boxes(tr)]
    check(boxes == [real], f"B4 박스 = {boxes} — 세 프레임 이어진 점수 높은 가짜에도 진짜 자리가 남는다")


def test_r2_new_track_confirmed_after_two_frames():
    """R2 — 새 버튼은 두 프레임 연속 보이면 판정에 들어간다(한 프레임 늦음 · Review Focus 3)."""
    print("\n[R2] 두 프레임 확정")
    tr = []
    box = (10, 10, 50, 50)
    ct._update_tracks(tr, [_det(0, 0.9, box)])
    check(ct._labeled_boxes(tr) == [], "첫 프레임 — 아직 판정에 안 쓴다")
    ct._update_tracks(tr, [_det(0, 0.9, box)])
    check([b[0] for b in ct._labeled_boxes(tr)] == ["B1"], "두 번째 프레임 — 판정에 쓴다")


def test_r2_moved_box_follows_after_two_frames():
    """R2 — 박스가 크게 튀면 두 번째 프레임부터 새 자리를 쓴다(그 전엔 옛 자리)."""
    print("\n[R2] 튄 박스")
    tr = []
    a, b = (10, 10, 50, 50), (300, 300, 340, 340)
    for _ in range(2):
        ct._update_tracks(tr, [_det(0, 0.9, a)])
    ct._update_tracks(tr, [_det(0, 0.9, b)])
    check([x[1:] for x in ct._labeled_boxes(tr)] == [a], "튄 첫 프레임 — 아직 옛 자리")
    ct._update_tracks(tr, [_det(0, 0.9, b)])
    check([x[1:] for x in ct._labeled_boxes(tr)] == [b], "두 번째 프레임 — 새 자리")
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_recognition.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ B4 박스 = [(400, 50, 440, 90)] — 세 프레임 이어진 점수 높은 가짜에도 진짜 자리가 남는다
  ❌ B4 박스 = [(400, 50, 440, 90)] — 손에 가려 점수가 낮아진 진짜 자리여야 한다
  ❌ 튄 첫 프레임 — 아직 옛 자리
  ❌ 첫 프레임 — 아직 판정에 안 쓴다
  ❌ 판정 박스 = ['B3', 'EMO']
  ❌ 손끝 ROI = EMO — 가짜 EMO 가 끼어들면 안 된다
❌ 실패 6건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/config.py -->
```python
YOLO_MAX_MISS   = 5
```
```python
YOLO_MAX_MISS   = 5
# 🔑 새 트랙은 이만큼 **연속** 보여야 판정·표시에 쓴다(인식 수정 R2) — 한 프레임짜리 오분류
#    (B3 자리의 가짜 EMO 등)가 YOLO_MAX_MISS 프레임 동안 살아 체류·경고를 만들던 것.
#    대가: 새 버튼이 한 프레임(약 0.07초) 늦게 판정에 들어간다.
YOLO_CONFIRM_HITS = 2
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
    YOLO_CONF_HIGH, YOLO_IOU_MATCH, YOLO_MAX_MISS,
)
```
```python
    YOLO_CONF_HIGH, YOLO_IOU_MATCH, YOLO_MAX_MISS, YOLO_CONFIRM_HITS,
)
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
        if best_i >= 0:
            d = detections[best_i]
            t['box'] = (d[2], d[3], d[4], d[5])
            t['score'] = d[1]
            t['miss'] = 0
            if d[1] >= YOLO_CONF_HIGH:
                t['confirmed'] = True
            used[best_i] = True
        else:
            t['miss'] += 1
    for i, d in enumerate(detections):
        if used[i]:
            continue
        if d[1] >= YOLO_CONF_HIGH:
            tracks.append({
                'cls': d[0], 'box': (d[2], d[3], d[4], d[5]),
                'score': d[1], 'miss': 0, 'confirmed': True,
            })
    tracks[:] = [t for t in tracks if t['miss'] <= YOLO_MAX_MISS and t['confirmed']]
    tracks[:] = _one_per_class(tracks)
    return tracks
```
```python
        if best_i >= 0:
            d = detections[best_i]
            t['box'] = (d[2], d[3], d[4], d[5])
            t['score'] = d[1]
            t['miss'] = 0
            t['hits'] = t.get('hits', 1) + 1
            # 🔑 새 트랙은 **연속 YOLO_CONFIRM_HITS 프레임** 봐야 확정한다(R2)
            if d[1] >= YOLO_CONF_HIGH and t['hits'] >= YOLO_CONFIRM_HITS:
                t['confirmed'] = True
            used[best_i] = True
        else:
            t['miss'] += 1
    for i, d in enumerate(detections):
        if used[i]:
            continue
        if d[1] >= YOLO_CONF_HIGH:
            tracks.append({
                'cls': d[0], 'box': (d[2], d[3], d[4], d[5]),
                'score': d[1], 'miss': 0, 'hits': 1,
                'confirmed': YOLO_CONFIRM_HITS <= 1,
            })
    # 확정 트랙은 YOLO_MAX_MISS 까지 가림을 견디고, 미확정 트랙은 한 번이라도 놓치면 버린다
    tracks[:] = [t for t in tracks
                 if (t['confirmed'] and t['miss'] <= YOLO_MAX_MISS)
                 or (not t['confirmed'] and t['miss'] == 0)]
    tracks[:] = _one_per_class(tracks)
    return tracks
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
    best = {}
    for t in tracks:
        prev = best.get(t['cls'])
        if prev is None or (t['miss'], -t['score']) < (prev['miss'], -prev['score']):
            best[t['cls']] = t
    return [t for t in tracks if best[t['cls']] is t]
```
```python
    # 🔑 미확정 트랙은 판정·표시에 안 쓰이므로 여기서 겨루지 않는다 — 확정되는 두 번째
    #    프레임에 겨룬다(R2). 겨루는 순서 = 지금 보임(miss) → **오래됨(hits)** → 점수.
    #    점수가 오래됨보다 앞이면, 손가락에 가려 점수가 떨어진 진짜 트랙이 다른 곳의
    #    같은 클래스 가짜에 밀려 지워졌다(검토 C11ⓑ · 설계 R2 「오래된 트랙 우선」).
    def rank(t):
        return (t['miss'], -t.get('hits', 1), -t['score'])

    best = {}
    for t in tracks:
        if not t['confirmed']:
            continue
        prev = best.get(t['cls'])
        if prev is None or rank(t) < rank(prev):
            best[t['cls']] = t
    return [t for t in tracks if not t['confirmed'] or best[t['cls']] is t]
```

<!-- rep: Rpi5/Demo/selftest/test_hoi_sim.py -->
```python
TRACKS = [
    {"cls": 1, "box": (50, 400, 150, 500)},    # B1
    {"cls": 2, "box": (200, 400, 300, 500)},   # B2
    {"cls": 3, "box": (350, 400, 450, 500)},   # B3
    {"cls": 4, "box": (500, 400, 600, 500)},   # B4
]
```
```python
# 🔑 이미 자리 잡은(확정) 트랙이다 — 판정은 확정 트랙만 쓴다(인식 수정 R2)
TRACKS = [
    {"cls": 1, "box": (50, 400, 150, 500), "confirmed": True},    # B1
    {"cls": 2, "box": (200, 400, 300, 500), "confirmed": True},   # B2
    {"cls": 3, "box": (350, 400, 450, 500), "confirmed": True},   # B3
    {"cls": 4, "box": (500, 400, 600, 500), "confirmed": True},   # B4
]
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
    if _detector is None:
        return []
    return [(_detector.class_name(t['cls']), *t['box']) for t in tracks]
```
```python
    if _detector is None:
        return []
    # 🔴 확정 트랙만 — 미확정(한 프레임) 트랙은 판정에 쓰지 않는다(R2)
    return [(_detector.class_name(t['cls']), *t['box']) for t in tracks if t['confirmed']]
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
    def _draw_yolo(self, frame, tracks):
        for t in tracks:
            cls_id = t['cls']
```
```python
    def _draw_yolo(self, frame, tracks):
        for t in tracks:
            if not t['confirmed']:
                continue                 # 미확정 트랙은 그리지 않는다(R2)
            cls_id = t['cls']
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
            self.yolo_detections_signal.emit([
                (_detector.class_name(t['cls']), t['score'], *t['box']) for t in tracks
            ])
```
```python
            self.yolo_detections_signal.emit([
                (_detector.class_name(t['cls']), t['score'], *t['box'])
                for t in tracks if t['confirmed']          # 미확정 트랙은 내보내지 않는다(R2)
            ])
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_recognition.py 2>&1 | tail -1; python3 selftest/test_hoi_sim.py > /dev/null 2>&1; echo hoi_sim_exit=$?; python3 selftest/test_imports.py 2>&1 | tail -1`
Expected:
```
✅ 인식 검증 통과
hoi_sim_exit=0
3/3 passed
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/config.py Demo/camera_thread.py Demo/selftest/test_recognition.py Demo/selftest/test_hoi_sim.py && git commit -m "fix(camera): 새 버튼 트랙은 두 프레임 연속 봐야 확정 · 같은 클래스는 오래된 트랙 우선 — 한 프레임 오분류가 판정·자리 잡은 트랙을 흔들던 것 (인식 수정 R2)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 3: R3 — 손 검출은 박스를 그리기 전 프레임으로

**Files:** Modify `Rpi5/Demo/camera_thread.py`(`_process_frame`) · Test `selftest/test_recognition.py`

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_recognition.py -->
```python
# ---------------------------------------------------------------- R3 손 검출 입력
class _RecordingHand:
    """손 검출 대역 — 받은 프레임을 복사해 둔다."""
    available = True
    reason = ""

    def __init__(self):
        self.seen = []

    def detect(self, frame, draw_on=None):
        self.seen.append(frame.copy())
        return None


def _thread():
    th = ct.CameraThread()
    th._hand = _RecordingHand()
    return th


def test_r3_hand_sees_frame_before_boxes():
    """R3 — 손 모델 입력에 버튼 박스 선이 없다(함수목록 §4.1-1 — 표시 설정이 손 입력을 바꾸던 것)."""
    print("\n[R3] 손 검출 입력")
    th = _thread()
    th.set_draw_boxes(True)
    _FAKE_DET.dets = [(0, 0.9, 20, 20, 60, 60)]
    img = np.zeros((120, 160, 3), np.uint8)
    for _ in range(2):                                   # 두 번째 프레임에 B1 확정 → 그림
        th._process_frame(img.copy())
    seen = th._hand.seen[-1]
    check(int(seen.max()) == 0, f"손 모델 입력에 박스 선이 없다 — 최댓값 {int(seen.max())}")
    _FAKE_DET.dets = []
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_recognition.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 손 모델 입력에 박스 선이 없다 — 최댓값 255
❌ 실패 1건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
        if DETECTOR_AVAILABLE:
            dets = _detector.detect(frame)
            with self._lock:
                self._tracks = _update_tracks(self._tracks, dets)
                tracks = self._tracks
            if draw_overlay:
                frame = self._draw_yolo(frame, tracks)
            self.yolo_detections_signal.emit([
                (_detector.class_name(t['cls']), t['score'], *t['box'])
                for t in tracks if t['confirmed']          # 미확정 트랙은 내보내지 않는다(R2)
            ])

        # 손 검출 → 검지 끝. 랜드마크 표시는 hand_tracker 가 frame 에 직접 그린다.
        # 🔴 draw_on=None 이어도 검출은 그대로 한다 — 반환값(손끝)은 ROI 판정에 쓴다.
        fingertip = self._hand.detect(frame, draw_on=frame if draw_overlay else None)
```
```python
        tracks = []
        if DETECTOR_AVAILABLE:
            dets = _detector.detect(frame)
            with self._lock:
                self._tracks = _update_tracks(self._tracks, dets)
                tracks = self._tracks

        # 손 검출 → 검지 끝. 🔴 버튼 박스를 **그리기 전** 프레임으로 한다(R3) — 그린 뒤에 하면
        #    화면 표시·촬영 여부가 손 모델 입력을 바꿨다(함수목록 §4.1-1). 랜드마크는
        #    hand_tracker 가 추론 뒤 frame 에 직접 그린다(그 위에 박스가 그려진다).
        # 🔴 draw_on=None 이어도 검출은 그대로 한다 — 반환값(손끝)은 ROI 판정에 쓴다.
        fingertip = self._hand.detect(frame, draw_on=frame if draw_overlay else None)

        if DETECTOR_AVAILABLE:
            if draw_overlay:
                frame = self._draw_yolo(frame, tracks)
            self.yolo_detections_signal.emit([
                (_detector.class_name(t['cls']), t['score'], *t['box'])
                for t in tracks if t['confirmed']          # 미확정 트랙은 내보내지 않는다(R2)
            ])
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_recognition.py 2>&1 | tail -1; python3 selftest/test_console_flow.py 2>&1 | tail -1`
Expected:
```
✅ 인식 검증 통과
✅ 글라스 UI 흐름 통과
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/camera_thread.py Demo/selftest/test_recognition.py && git commit -m "fix(camera): 손 검출을 버튼 박스를 그리기 전 프레임으로 — 표시·촬영 설정이 손 모델 입력을 바꾸던 것 (인식 수정 R3)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 4: R5 — 카메라 재연결 신호로 끊기기 전 관측을 버린다

**Files:** Modify `Rpi5/Demo/camera_thread.py`(신호 · `_on_connected` · `run`) · `Rpi5/Demo/fsm.py`(`forget_observation`) · `Rpi5/Demo/safety_console.py`(연결 · `_on_stream_reset`) · Test `selftest/test_recognition.py` · `selftest/test_fsm.py` · `selftest/test_console_flow.py`

**Interfaces:**
- Produces: `CameraThread.stream_reset_signal`(인자 없음) · `CameraThread._on_connected()` · `SafetyFSM.forget_observation()` — 체류·관측(`_last_roi`·`_last_seen`·창)을 지우고 **`_just_done` 은 유지**.

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_recognition.py -->
```python
# ---------------------------------------------------------------- R5 재연결
def test_r5_reconnect_clears_tracks_and_signals():
    """R5 — (재)연결되면 끊기기 전 트랙을 버리고 GUI 에 알린다(검토 C4 — 옛 트랙이 남았다)."""
    print("\n[R5] 재연결")
    th = _thread()
    th._tracks = [{'cls': 2, 'box': (1, 1, 5, 5), 'score': 0.9, 'miss': 3, 'hits': 5, 'confirmed': True}]
    got = []
    th.stream_reset_signal.connect(lambda: got.append(1))
    th._on_connected()
    check(th._tracks == [], "옛 트랙이 비워진다")
    check(got == [1], "재연결 신호가 나간다")
```

<!-- add: Rpi5/Demo/selftest/test_fsm.py -->
```python
def test_r5_forget_observation_after_reconnect():
    """R5 — 재연결 뒤 첫 프레임이 끊기기 전 체류에 이어 붙지 않는다(검토 C4)."""
    fsm, _ = make_fsm(threshold=0.3, gap_fill=0.3)
    run(fsm)
    fsm.update_vision("B3", now=0.0)
    fsm.update_vision("B3", now=0.1)
    fsm.forget_observation()                # 카메라 재연결
    fsm.update_vision("B3", now=5.0)        # 재연결 뒤 첫 프레임 — 종전엔 5초 체류로 경고
    assert fsm.state == State.MONITOR
    assert fsm.last_roi == "B3"


def test_r5_forget_keeps_just_done():
    """R5 — 재연결해도 방금 완료한 버튼 예외는 남는다(손가락이 그대로일 수 있다 · Review Focus 2)."""
    fsm, _ = make_fsm(threshold=0.3, gap_fill=0.3)
    run(fsm)
    fsm.press_button("B1", 0.0)
    feed(fsm, "B1", 0.0, 0.2)
    fsm.forget_observation()
    feed(fsm, "B1", 1.0, 1.6)
    assert fsm.state == State.MONITOR
```

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def test_r5_stream_reset_clears_fsm_observation():
    """R5 — 카메라 재연결 신호를 받으면 판정기가 끊기기 전 관측을 지운다."""
    print("\n[R5] 재연결 → 판정기 관측 지움")
    win = make_console()
    win._on_cta()
    t0 = time.time()
    win.fsm.update_vision("B3", t0)
    win.fsm.update_vision("B3", t0 + 0.1)
    signal = getattr(win.camera_thread, "stream_reset_signal", None)
    check(signal is not None, "카메라 스레드에 재연결 신호가 있다")
    if signal is not None:
        signal.emit()
    check(win.fsm._dwell_roi is None and win.fsm.last_roi is None,
          f"체류·관측이 지워진다 — {win.fsm._dwell_roi}·{win.fsm.last_roi}")
    win.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_recognition.py 2>&1 | tail -1; python3 selftest/test_fsm.py 2>&1 | grep -E "^  FAIL|passed"; python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
AttributeError: 'CameraThread' object has no attribute 'stream_reset_signal'
  FAIL  test_r5_forget_keeps_just_done
  FAIL  test_r5_forget_observation_after_reconnect
35/37 passed
  ❌ 카메라 스레드에 재연결 신호가 있다
  ❌ 체류·관측이 지워진다 — B3·B3
❌ 실패 2건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
    hand_signal                = pyqtSignal(bool)      # 이 프레임에서 손이 검출됐는가 (집계 전용)
```
```python
    hand_signal                = pyqtSignal(bool)      # 이 프레임에서 손이 검출됐는가 (집계 전용)
    stream_reset_signal        = pyqtSignal()          # 카메라 (재)연결 — 끊기기 전 관측을 버리라(R5)
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
            self._fail_count = 0          # 붙었으면 카운터를 되돌린다
```
```python
            self._fail_count = 0          # 붙었으면 카운터를 되돌린다
            self._on_connected()
```

<!-- rep: Rpi5/Demo/camera_thread.py -->
```python
    def set_tool_scan(self, on):
```
```python
    def _on_connected(self):
        """(재)연결 직후 — 끊기기 전 트랙을 버리고 GUI 에 알린다(R5 · 설계 D9).

        🔴 옛 트랙(miss ≤ YOLO_MAX_MISS)과 판정기 관측이 남으면, 재연결 뒤 첫 프레임이
           끊기기 전 체류에 이어 붙어 곧바로 경고가 났다(검토 C4). 시간 간격으로 추측하지
           않고(P4 보류 — 녹화의 짧은 끊김에도 반응했다) 연결 사건으로 지운다.
        """
        with self._lock:
            self._tracks = []
        self.stream_reset_signal.emit()

    def set_tool_scan(self, on):
```

<!-- rep: Rpi5/Demo/fsm.py -->
```python
    def release_warning(self):
```
```python
    def forget_observation(self):
        """카메라 (재)연결 — 끊기기 전 관측을 버린다(R5 · 설계 D9).

        체류·마지막 관측·창을 지운다. 🔴 방금 완료한 버튼 예외(_just_done)는 **유지**한다 —
        재연결 사이 손가락이 그 버튼 위에 그대로일 수 있다. 상태 전이는 하지 않는다.
        """
        self._reset_dwell()
        self._last_roi = self._last_level = self._last_seen = None
        self._win.clear()

    def release_warning(self):
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        self.camera_thread.roi_signal.connect(self._on_roi)
```
```python
        self.camera_thread.roi_signal.connect(self._on_roi)
        self.camera_thread.stream_reset_signal.connect(self._on_stream_reset)
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
    def _on_hand(self, seen):
        self._hand_seen = bool(seen)
```
```python
    def _on_hand(self, seen):
        self._hand_seen = bool(seen)

    def _on_stream_reset(self):
        """카메라 (재)연결 — 끊기기 전 손 관측을 판정기에서 지운다(R5)."""
        self.fsm.forget_observation()
        self._append_log("[카메라] 연결 — 끊기기 전 손 관측을 버렸다")
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_recognition.py 2>&1 | tail -1; python3 selftest/test_fsm.py | tail -1; python3 selftest/test_console_flow.py 2>&1 | tail -1; python3 selftest/test_imports.py 2>&1 | tail -1; python3 test/fsm_sim.py --gate 2>&1 | tail -1`
Expected:
```
✅ 인식 검증 통과
37/37 passed
✅ 글라스 UI 흐름 통과
3/3 passed
✅ 통과 — 기준선 산출로 진행
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/camera_thread.py Demo/fsm.py Demo/safety_console.py Demo/selftest/test_recognition.py Demo/selftest/test_fsm.py Demo/selftest/test_console_flow.py && git commit -m "fix(camera): 카메라 재연결 신호로 옛 트랙·판정기 관측을 지운다 — 재연결 직후 첫 프레임 경고 (인식 수정 R5)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 5: I1·I2 — 인터락 포트를 연결할 때마다 다시 찾고, 뽑히면 곧바로 끊김

**Files:** Modify `Rpi5/Demo/config.py` · `Rpi5/Demo/interlock.py` · Create `Rpi5/Demo/selftest/test_interlock.py`

**Interfaces:**
- Produces: `config.resolve_interlock_port(quiet=False)` · `config.INTERLOCK_ENABLED`(기본 True · 끄려면 `SOP_INTERLOCK=0`) · `config.INTERLOCK_PORT`(못 찾으면 None) · `config.INTERLOCK_BOOT_WAIT_SEC = 2.0` · `InterlockController.connected` = 열림 **그리고** 장치 노드가 있음 · `InterlockController._absent`(장치를 못 찾은 상태 — 이 동안은 재시도 한도를 세지 않는다). 시험 파일 `test_interlock.py`(가짜 시리얼 `_FakeSerial`·`_ctl`·`_wait`) — Task 6 이 시험을 더한다.
- 🔑 **포기 규칙(설계 I1 「못 찾아도 계속 찾는다」)** — 종전 재연결 루프는 `CONNECT_MAX_TRIES`(5)번 실패하면 멈췄다. 인터락이 켜진 채 장치 없이 시연하면(지금 상태) 켤 때마다 약 15초 뒤 「인터락 연결 실패」 알림이 뜨고, 그 뒤에 꽂아도 수동 재시도 전까지 안 붙는다. 그래서 **장치를 못 찾은 동안은 세지 않고**, 찾은 장치가 **열리지 않을 때만** 센다(무한 실패 로그 방지라는 종전 이유는 그 경우에만 해당한다 — 못 찾음 안내는 한 번만 남는다).

- [ ] **Step 1: 실패하는 테스트**

<!-- new: Rpi5/Demo/selftest/test_interlock.py -->
```python
"""인터락 연결 관리 검증(I1~I3) — 가짜 시리얼로.

실행: python3 Demo/selftest/test_interlock.py
🔴 실제 Arduino·릴레이에 닿지 않는다 — `interlock.serial` 을 가짜 모듈로 바꾸고, 포트는
   임시 폴더의 빈 파일(뽑힘 = 파일 삭제)로 흉내 낸다.
정본 = 상위 docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md §4.2
"""
import os
import sys
import tempfile
import time

_DEMO_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _DEMO_DIR)

import config
config.INTERLOCK_BOOT_WAIT_SEC = 0.05
config.INTERLOCK_RECONNECT_DELAY_SEC = 0.05
config.CONNECT_MAX_TRIES = 2          # 포기가 빨리 오게 — 장치가 없는 동안에는 포기하지 않아야 한다(I1)
import interlock
from fsm import Feedback

_fails = []


def check(cond, msg):
    print(("  ✅ " if cond else "  ❌ ") + msg)
    if not cond:
        _fails.append(msg)


class _FakeSerial:
    opened = []

    def __init__(self, port, baud, timeout=None, write_timeout=None):
        if not str(port).startswith(tempfile.gettempdir()):
            raise RuntimeError(f"🔴 시험이 실제 장치를 열려고 했다: {port}")
        if not os.path.exists(port):
            raise OSError(f"no such port {port}")
        self.port = port
        self.is_open = True
        self.writes = []
        self._acks = 0
        _FakeSerial.opened.append(self)

    def write(self, b):
        if not os.path.exists(self.port):
            raise OSError("device gone")
        self.writes.append(b.decode().strip())
        self._acks += 1

    def flush(self):
        pass

    def readline(self):
        if self._acks:
            self._acks -= 1
            return b"ACK\n"
        return b""

    def reset_input_buffer(self):
        pass

    def close(self):
        self.is_open = False


class _FakeSerialModule:
    Serial = _FakeSerial


interlock.serial = _FakeSerialModule


def _wait(cond, sec=3.0):
    end = time.time() + sec
    while time.time() < end:
        if cond():
            return True
        time.sleep(0.02)
    return False


def _ctl(port_box):
    """port_box["p"] 를 돌려주는 가짜 포트 찾기로 인터락을 만든다."""
    config.resolve_interlock_port = lambda quiet=False: port_box["p"]
    config.INTERLOCK_PORT = port_box["p"]
    return interlock.InterlockController(enabled=True, log=lambda m: None)


def test_i1_device_plugged_after_start():
    """I1 — 켤 때 장치가 없어도 포기하지 않고 계속 찾다가, 나중에 꽂으면 스스로 붙는다(검토 C7)."""
    print("\n[I1] 나중에 꽂기")
    tmp = tempfile.mkdtemp()
    dev = os.path.join(tmp, "ttyACM0")
    box = {"p": None}
    c = _ctl(box)
    try:
        check(not c.connected, "시작 때 장치 없음 → 미연결")
        time.sleep(0.4)                                   # 재시도 한도(2회)를 넘길 만큼 없는 채로
        check(not c.gave_up, "장치가 없는 동안에는 포기하지 않는다")
        open(dev, "w").close()
        box["p"] = dev                                    # 꽂음
        check(_wait(lambda: c.connected), "꽂으면 재연결 스레드가 붙는다")
        check(_wait(lambda: bool(_FakeSerial.opened)
                    and _FakeSerial.opened[-1].writes[:1] == ["RUN"]),
              "붙자마자 RUN 으로 램프를 맞춘다")
    finally:
        c.close()


def test_i2_unplug_detected_and_new_number():
    """I2 — 뽑히면 쓰기 전이라도 끊김으로 보고, 다른 번호로 다시 꽂혀도 붙는다(검토 C9)."""
    print("\n[I2] 뽑힘·번호 바뀜")
    tmp = tempfile.mkdtemp()
    dev0, dev1 = os.path.join(tmp, "ttyACM0"), os.path.join(tmp, "ttyACM1")
    open(dev0, "w").close()
    box = {"p": dev0}
    c = _ctl(box)
    try:
        check(_wait(lambda: c.connected), "처음 붙음")
        os.unlink(dev0)                                   # 뽑음
        box["p"] = None                                   # 뽑힌 장치는 찾기에도 안 나온다
        check(_wait(lambda: not c.connected), "뽑히면 곧바로 끊김(녹색으로 남지 않는다)")
        time.sleep(0.3)                                   # 없는 동안 재시도 한도를 넘겨도
        open(dev1, "w").close()
        box["p"] = dev1                                   # 다른 번호로 다시 꽂힘
        check(_wait(lambda: c.connected and _FakeSerial.opened[-1].port == dev1),
              "새 번호로 다시 붙는다")
    finally:
        c.close()


if __name__ == "__main__":
    for _name, _fn in sorted(globals().items()):
        if _name.startswith("test_"):
            _fn()
    print()
    if _fails:
        print(f"❌ 실패 {len(_fails)}건")
        for m in _fails:
            print(f"   - {m}")
        sys.exit(1)
    print("✅ 인터락 검증 통과")
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && timeout 120 python3 selftest/test_interlock.py 2>&1 | grep -E "^  ❌|^❌|Error"`
Expected:
```
  ❌ 장치가 없는 동안에는 포기하지 않는다
  ❌ 꽂으면 재연결 스레드가 붙는다
  ❌ 붙자마자 RUN 으로 램프를 맞춘다
  ❌ 뽑히면 곧바로 끊김(녹색으로 남지 않는다)
  ❌ 새 번호로 다시 붙는다
❌ 실패 5건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/config.py -->
```python
def _resolve_interlock_port():
    """인터록 Arduino 의 경로. 확신할 수 없으면 None."""
    forced = os.environ.get("SOP_INTERLOCK_PORT")
    if forced:
        print(f"[config] 인터록 포트 강제 지정: {forced}")
        return forced
```
```python
def resolve_interlock_port(quiet=False):
    """인터록 Arduino 의 경로. 확신할 수 없으면 None.

    🔑 인터락 컨트롤러가 **연결할 때마다** 다시 부른다(I1·I2) — 켠 뒤에 꽂거나, 다시 꽂혀
       번호(ttyACM0 → 1)가 바뀌어도 찾는다. quiet=True 면 안내를 찍지 않는다(재시도 루프용).
    """
    say = (lambda m: None) if quiet else print
    forced = os.environ.get("SOP_INTERLOCK_PORT")
    if forced:
        say(f"[config] 인터록 포트 강제 지정: {forced}")
        return forced
```

<!-- rep: Rpi5/Demo/config.py -->
```python
        if len(cands) == 1:
            return cands[0]
        if len(cands) > 1:
            print(f"[config] ⚠️ 인터록 후보가 여럿이라 고를 수 없다: {cands}")
        else:
            print("[config] ⚠️ 인터록 장치를 못 찾았다 (ESP32 는 인터록이 아니다)")
    except Exception as e:
        print(f"[config] ⚠️ 포트 해석 실패: {e}")
    print("[config] → 인터록을 비활성화한다. 강제하려면 SOP_INTERLOCK_PORT=/dev/ttyACMx")
    return None

_INTERLOCK_PORT_RESOLVED = _resolve_interlock_port()

# 🔴 포트를 못 고르면 인터록 자체를 끈다 — 위 주석의 이유.
INTERLOCK_ENABLED = _INTERLOCK_PORT_RESOLVED is not None
INTERLOCK_PORT    = _INTERLOCK_PORT_RESOLVED or "/dev/ttyACM0"  # 표시용 기본값
```
```python
        if len(cands) == 1:
            return cands[0]
        if len(cands) > 1:
            say(f"[config] ⚠️ 인터록 후보가 여럿이라 고를 수 없다: {cands}")
        else:
            say("[config] ⚠️ 인터록 장치를 못 찾았다 (ESP32 는 인터록이 아니다)")
    except Exception as e:
        say(f"[config] ⚠️ 포트 해석 실패: {e}")
    say("[config] → 찾을 때까지 연결하지 않는다(꽂히면 자동으로 붙는다). "
        "강제하려면 SOP_INTERLOCK_PORT=/dev/ttyACMx")
    return None

_INTERLOCK_PORT_RESOLVED = resolve_interlock_port()

# 🔴 포트를 못 찾아도 인터록을 **끄지 않는다**(I1) — 끄면 나중에 꽂아도 살아나지 않고 차단이
#    조용히 화면에서만 걸렸다. 모르는 장치에는 여전히 연결하지 않는다(위 주석).
#    일부러 끄려면 SOP_INTERLOCK=0.
INTERLOCK_ENABLED = os.environ.get("SOP_INTERLOCK", "1") != "0"
INTERLOCK_PORT    = _INTERLOCK_PORT_RESOLVED     # None = 아직 못 찾음(연결할 때마다 다시 찾는다)
INTERLOCK_BOOT_WAIT_SEC = 2.0        # UNO R4 는 연결 직후 리셋된다 — 부팅을 기다린다
```

<!-- rep: Rpi5/Demo/interlock.py -->
```python
import queue
import threading
import time
```
```python
import os
import queue
import threading
import time
```

<!-- rep: Rpi5/Demo/interlock.py -->
```python
        self._port    = port if port is not None else config.INTERLOCK_PORT
```
```python
        # 🔑 port 를 주면 그것만 쓰고, 안 주면 **연결할 때마다** config 로 다시 찾는다(I1·I2)
        self._fixed_port = port
        self._port    = port if port is not None else config.INTERLOCK_PORT
        self._absent  = False     # 장치를 못 찾은 상태 — 안내는 한 번만 · 이 동안은 포기하지 않는다(I1)
```

<!-- rep: Rpi5/Demo/interlock.py -->
```python
        if serial is None:
            return False
        with self._lock:
            if self._ser is not None and getattr(self._ser, "is_open", False):
                return True
            try:
                self._ser = serial.Serial(
                    self._port, self._baud, timeout=self._timeout,
                    write_timeout=self._timeout)
                # UNO R4 는 연결 직후 리셋 → 부팅 대기. 짧게 비운다.
                time.sleep(2.0)
                self._ser.reset_input_buffer()
                self._log(f"[인터락] 연결됨 — {self._port} @ {self._baud}")
                return True
```
```python
        if serial is None:
            return False
        port = self._fixed_port or config.resolve_interlock_port(quiet=True)
        if port is None:
            if not self._absent:
                self._log("[인터락] 장치를 못 찾았다 — 꽂히면 자동으로 붙는다")
                self._absent = True
            return False
        self._absent = False
        with self._lock:
            if self._ser is not None and getattr(self._ser, "is_open", False):
                return True
            try:
                self._ser = serial.Serial(
                    port, self._baud, timeout=self._timeout,
                    write_timeout=self._timeout)
                self._port = port
                # UNO R4 는 연결 직후 리셋 → 부팅 대기. 짧게 비운다.
                time.sleep(getattr(config, "INTERLOCK_BOOT_WAIT_SEC", 2.0))
                self._ser.reset_input_buffer()
                self._log(f"[인터락] 연결됨 — {self._port} @ {self._baud}")
                return True
```

<!-- rep: Rpi5/Demo/interlock.py -->
```python
        max_tries = getattr(config, "CONNECT_MAX_TRIES", 5)
        while not self._closing:
            if self.connected:
```
```python
        max_tries = getattr(config, "CONNECT_MAX_TRIES", 5)
        while not self._closing:
            if self._ser is not None and not self.connected:
                # 🔴 열려 있다고 믿는 핸들의 장치가 사라졌다(뽑힘) — 버리고 다시 찾는다(I2)
                with self._lock:
                    self._drop()
                self._log("[인터락] 장치가 사라졌다(뽑힘) — 다시 찾는다")
            if self.connected:
```

<!-- rep: Rpi5/Demo/interlock.py -->
```python
        🔴 무한 재시도 금지 — 3초마다 영원히 돌면 "연결 실패" 로그가 계속 쌓인다.
           CONNECT_MAX_TRIES 회 실패하면 멈추고 on_give_up 을 부른다.
```
```python
        🔴 무한 재시도 금지 — 3초마다 영원히 돌면 "연결 실패" 로그가 계속 쌓인다.
           CONNECT_MAX_TRIES 회 실패하면 멈추고 on_give_up 을 부른다.
        🔑 단, 장치를 **못 찾은** 동안은 세지 않고 계속 찾는다(I1) — 못 찾음 안내는 한 번만
           남아 쌓이지 않고, 켠 뒤에 꽂아도 붙어야 한다. 포기는 찾은 장치가 안 열릴 때만.
```

<!-- rep: Rpi5/Demo/interlock.py -->
```python
                else:
                    self._fail_count += 1
                    if self._fail_count >= max_tries:
```
```python
                elif self._absent:
                    pass                   # 🔑 못 찾은 동안은 포기하지 않는다(I1)
                else:
                    self._fail_count += 1
                    if self._fail_count >= max_tries:
```

<!-- rep: Rpi5/Demo/interlock.py -->
```python
        return self._ser is not None and getattr(self._ser, "is_open", False)
```
```python
        ser = self._ser
        if ser is None or not getattr(ser, "is_open", False):
            return False
        # 🔴 열림 표시만 믿지 않는다 — 뽑혀도 쓰기가 실패할 때까지 녹색이었다(검토 C9).
        #    장치 노드가 사라졌으면 끊김이다.
        path = getattr(ser, "port", None) or self._port
        return bool(path) and os.path.exists(path)
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && timeout 120 python3 selftest/test_interlock.py 2>&1 | tail -1; python3 selftest/test_precheck.py > /dev/null 2>&1; echo precheck_exit=$?; python3 selftest/test_imports.py 2>&1 | tail -1`
Expected:
```
✅ 인터락 검증 통과
precheck_exit=0
3/3 passed
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/config.py Demo/interlock.py Demo/selftest/test_interlock.py && git commit -m "fix(interlock): 포트를 연결할 때마다 다시 찾고 장치가 사라지면 곧바로 끊김으로 — 켠 뒤 꽂기·번호 바뀜·뽑힘 녹색 (인터락 수정 I1·I2)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 6: I3 — 재연결 동기화는 그때의 최신 요청을 보낸다

**Files:** Modify `Rpi5/Demo/interlock.py` · Test `selftest/test_interlock.py`

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_interlock.py -->
```python
def test_i3_resync_sends_latest():
    """I3 — 붙는 동안(부팅 대기) 들어온 해제가 옛 BLOCK 에 덮이지 않는다(검토 C8)."""
    print("\n[I3] 재연결 동기화")
    old_wait = config.INTERLOCK_BOOT_WAIT_SEC
    config.INTERLOCK_BOOT_WAIT_SEC = 0.4               # 부팅 대기 중에 명령이 들어오게
    tmp = tempfile.mkdtemp()
    dev = os.path.join(tmp, "ttyACM0")
    box = {"p": None}
    c = _ctl(box)
    try:
        c.set_interlock(True)                          # 미연결 중 BLOCK (보류)
        open(dev, "w").close()
        box["p"] = dev                                 # 꽂음 → 재연결 시작
        time.sleep(0.2)                                # 부팅 대기 중에
        c.set_feedback(Feedback.NONE)                  # 해제 → RUN
        ok = _wait(lambda: len(_FakeSerial.opened[-1].writes) >= 2, 3.0)
        writes = _FakeSerial.opened[-1].writes
        check(ok and writes[-1] == "RUN", f"마지막으로 나간 명령 = RUN — {writes}")
    finally:
        c.close()
        config.INTERLOCK_BOOT_WAIT_SEC = old_wait
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && timeout 120 python3 selftest/test_interlock.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 마지막으로 나간 명령 = RUN — ['RUN', 'BLOCK']
❌ 실패 1건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/interlock.py -->
```python
_FEEDBACK_TO_CMD = {
    "NONE":    "RUN",
    "WARNING": "WARN",
    "BLOCK":   "BLOCK",
}
```
```python
_FEEDBACK_TO_CMD = {
    "NONE":    "RUN",
    "WARNING": "WARN",
    "BLOCK":   "BLOCK",
}

# 재연결 동기화 표시 — 전송 워커가 **꺼낼 때** 그 시점의 최신 요청으로 바꿔 보낸다(I3).
_SYNC = object()
```

<!-- rep: Rpi5/Demo/interlock.py -->
```python
        self._last_cmd = None     # 재연결 시 재전송용 마지막 명령
```
```python
        self._last_cmd = None     # 마지막으로 전송(또는 보류)한 명령 — 중복 제거용
        self._desired = None      # FSM 이 마지막으로 요청한 상태 — 재연결 동기화가 보낸다(I3)
```

<!-- rep: Rpi5/Demo/interlock.py -->
```python
        cmd = self._last_cmd if self._last_cmd is not None else "RUN"
        self._write(cmd, force=True)
```
```python
        # 🔴 지금 _last_cmd 를 읽어 넣으면 안 된다 — 붙는 동안(부팅 대기) 들어온 요청이 아직
        #    처리 전이라, 옛 BLOCK 이 대기열 **맨 뒤**에 들어가 마지막으로 나갔다(검토 C8:
        #    FSM 은 PROCESS_RUN 인데 릴레이는 BLOCK). 표시만 넣고 워커가 꺼낼 때 최신 요청을 보낸다.
        self._write(_SYNC, force=True)
```

<!-- rep: Rpi5/Demo/interlock.py -->
```python
        if not self._enabled:
            return
        self._queue.put((cmd, force))
```
```python
        if not self._enabled:
            return
        if cmd is not _SYNC:
            self._desired = cmd
        self._queue.put((cmd, force))
```

<!-- rep: Rpi5/Demo/interlock.py -->
```python
            cmd, force = item
            try:
```
```python
            cmd, force = item
            if cmd is _SYNC:
                cmd = self._desired or "RUN"     # 첫 연결이면 RUN(Pi 가 정한 정상)
            try:
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && timeout 120 python3 selftest/test_interlock.py 2>&1 | tail -1`
Expected: `✅ 인터락 검증 통과`

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/interlock.py Demo/selftest/test_interlock.py && git commit -m "fix(interlock): 재연결 동기화는 꺼내는 시점의 최신 요청을 보낸다 — 옛 BLOCK 이 마지막에 나가던 경쟁 (인터락 수정 I3)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 7: I1 — 인터락 없이 차단되면 배너에 「화면에서만 차단 중」

**Files:** Modify `Rpi5/Demo/safety_console.py`(`_show_block_banner` 새로 · `_on_fsm_state` BLOCK 분기) · `Rpi5/Demo/interlock.py`(미연결 BLOCK 알람) · Test `selftest/test_console_flow.py`

**Interfaces:**
- Produces: `SafetyConsole._show_block_banner(emo)` — 차단 배너 문구를 한곳에서 고른다(EMO 문구 · 「화면에서만」 둘째 줄). Task 11 의 「위반 차단 중 EMO」가 다시 쓴다.

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def test_i1_block_banner_says_screen_only():
    """I1 — 인터락이 안 붙은 채 차단되면 배너가 「화면에서만 차단 중」이라고 알린다(검토 C7)."""
    print("\n[I1] 인터락 없는 차단")
    win = make_console()                             # 시험은 인터락을 끈다 → 미연결
    win._on_cta()
    key(win, "3")                                    # 오답 → BLOCK
    check("화면에서만" in win.alert._line2.text(), f"위반 차단 둘째 줄 = {win.alert._line2.text()!r}")
    win.close()
    win = make_console()
    win._on_cta()
    key(win, "E")
    check("화면에서만" in win.alert._line2.text(), f"EMO 차단 둘째 줄 = {win.alert._line2.text()!r}")
    win.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 위반 차단 둘째 줄 = '\xa0'
  ❌ EMO 차단 둘째 줄 = '— EMO 를 복귀한 뒤 「차단 해제」를 누르세요'
❌ 실패 2건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
            emo = self._emo_button()
            self.glow.set_level("block")
            if self._last_button != emo:
                self._stats.violation(self.fsm.correct_roi, self._last_button or "?", "block")
                self.alert.show_block()
```
```python
            emo = self._emo_button()
            self.glow.set_level("block")
            if self._last_button != emo:
                self._stats.violation(self.fsm.correct_roi, self._last_button or "?", "block")
                self._show_block_banner(emo=False)
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
                self.alert.show_block("비상정지로 차단됐습니다",
                                      "— EMO 를 복귀한 뒤 「차단 해제」를 누르세요")
```
```python
                self._show_block_banner(emo=True)
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
    def _cancel_sub(self, why):
```
```python
    def _show_block_banner(self, emo):
        """차단 배너 문구 — EMO 차단은 「비상정지」(G5), 위반 차단은 기본 문구.

        🔴 인터락이 안 붙어 있으면 전기가 끊기지 않았다 — 둘째 줄에 「화면에서만 차단 중」을
           적어 끊겼다고 믿게 두지 않는다(I1 · 검토 C7).
        """
        itl_hint = "" if self.interlock.connected else "⚠ 인터락 미연결 — 화면에서만 차단 중"
        if emo:
            self.alert.show_block("비상정지로 차단됐습니다",
                                  itl_hint or "— EMO 를 복귀한 뒤 「차단 해제」를 누르세요")
        else:
            self.alert.show_block(hint=itl_hint)

    def _cancel_sub(self, why):
```

<!-- rep: Rpi5/Demo/interlock.py -->
```python
            if ser is None or not getattr(ser, "is_open", False):
                self._log(f"[인터락] (미연결) 명령 보류: {cmd}")
                if cmd == "BLOCK":
                    self._fault("BLOCK 송신 불가(시리얼 미연결) — 릴레이 차단 미확인")
                return
```
```python
            if ser is None or not getattr(ser, "is_open", False):
                # 🔑 미연결 BLOCK 은 알람 창을 띄우지 않는다 — 인터락이 꺼져 있던 종전에는 이
                #    경로에 오지 않았고, 이제는 차단 배너가 「화면에서만 차단 중」을 알린다(I1).
                #    붙어 있다가 쓰기·ACK 가 실패한 경우는 아래에서 그대로 알람한다.
                self._log(f"[인터락] (미연결) 명령 보류: {cmd}")
                return
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | tail -1; timeout 120 python3 selftest/test_interlock.py 2>&1 | tail -1; python3 selftest/test_overlay.py 2>&1 | tail -1`
Expected:
```
✅ 글라스 UI 흐름 통과
✅ 인터락 검증 통과
✅ 오버레이 스모크 통과
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/safety_console.py Demo/interlock.py Demo/selftest/test_console_flow.py && git commit -m "feat(gui): 인터락 없이 차단되면 배너에 「화면에서만 차단 중」 · 미연결 BLOCK 은 알람 창 대신 배너로 (인터락 수정 I1)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 8: V1 — 재생 확인이 첫 무음 뒤에도 살아 있게

**Files:** Modify `Rpi5/Demo/voice_assistant.py`(`Speaker`) · Create `Rpi5/Demo/selftest/test_voice_speaker.py`

**Interfaces:**
- Produces: `Speaker._readline() -> bytes | None`(None = 타임아웃 · b"" = 닫힘) · 시험 파일 `test_voice_speaker.py` — Task 10 이 V4 시험을 더한다.

- [ ] **Step 1: 실패하는 테스트**

<!-- new: Rpi5/Demo/selftest/test_voice_speaker.py -->
```python
"""음성비서 명령 채널 검증(V1·V4) — ESP32 없이 소켓 쌍으로.

실행: python3 Demo/selftest/test_voice_speaker.py
정본 = 상위 docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md §4.3
"""
import os
import socket
import sys
import tempfile
import threading
import time

_DEMO_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _DEMO_DIR)

import voice_assistant as va

_fails = []


def check(cond, msg):
    print(("  ✅ " if cond else "  ❌ ") + msg)
    if not cond:
        _fails.append(msg)


def _speaker_pair():
    a, b = socket.socketpair()
    spk = va.Speaker("시험")
    spk.s = a
    spk.f = a.makefile("rb")          # 종전 코드가 읽던 통로(고친 뒤에는 쓰지 않는다)
    return spk, b


def _send_later(b, parts):
    def go():
        for p in parts:
            if isinstance(p, float):
                time.sleep(p)
            else:
                b.sendall(p.encode())
    threading.Thread(target=go, daemon=True).start()


def test_v1_silence_during_playback_is_waited():
    """V1 — 재생 중 1초 넘는 무음 뒤의 「재생 완료」를 받고, 다음 재생도 확인된다(검토 C6)."""
    print("\n[V1] 재생 확인")
    spk, b = _speaker_pair()
    _send_later(b, ["[준비]\n", "[적재] ok\n", 1.4, "[재생 완료]\n"])
    out = spk._drain(wait=5.0)
    check(any("재생 완료" in x for x in out), f"첫 재생 확인 — {out}")
    _send_later(b, ["[적재] ok\n", 0.2, "[재생 완료]\n"])
    out2 = spk._drain(wait=5.0)
    check(any("재생 완료" in x for x in out2), f"두 번째 재생도 확인 — {out2}")
    b.close()


if __name__ == "__main__":
    for _name, _fn in sorted(globals().items()):
        if _name.startswith("test_"):
            _fn()
    print()
    if _fails:
        print(f"❌ 실패 {len(_fails)}건")
        for m in _fails:
            print(f"   - {m}")
        sys.exit(1)
    print("✅ 음성 명령 채널 검증 통과")
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && timeout 60 python3 selftest/test_voice_speaker.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 첫 재생 확인 — ['[준비]', '[적재] ok']
  ❌ 두 번째 재생도 확인 — []
❌ 실패 2건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/voice_assistant.py -->
```python
    def __init__(self, ip):
        self.ip = ip
        self.s = None
        self.f = None
```
```python
    def __init__(self, ip):
        self.ip = ip
        self.s = None
        self.f = None                 # 쓰지 않는다(V1) — 옛 시험·호출부 호환용으로만 남긴다
        self._rbuf = b""              # 소켓에서 받은 아직 줄이 안 된 바이트
```

<!-- rep: Rpi5/Demo/voice_assistant.py -->
```python
            self.s.settimeout(30)
            self.f = self.s.makefile("rb")
```
```python
            self.s.settimeout(30)
            self._rbuf = b""
```

<!-- rep: Rpi5/Demo/voice_assistant.py -->
```python
        while time.time() < end:
            try:
                line = self.f.readline()
            except socket.timeout:
                # 🔴 재생 중에는 펌웨어가 몇 초간 아무것도 안 보낸다 — 여기서
                #    포기하면 「확인되지 않았다」로 잘못 판정한다(2026-09-07 에
                #    실제로 그랬다. 소리는 났는데 로그만 실패로 남았다).
                continue
            except (OSError, AttributeError):
                break
            if not line:
                break
```
```python
        while time.time() < end:
            try:
                line = self._readline()
            except (OSError, AttributeError):
                break
            if line is None:
                # 🔴 재생 중에는 펌웨어가 몇 초간 아무것도 안 보낸다 — 여기서
                #    포기하면 「확인되지 않았다」로 잘못 판정한다(2026-09-07 에
                #    실제로 그랬다. 소리는 났는데 로그만 실패로 남았다).
                continue
            if not line:
                break
```

<!-- rep: Rpi5/Demo/voice_assistant.py -->
```python
    def send(self, payload, expect=False):
```
```python
    def _readline(self):
        """한 줄(b"...\\n")을 소켓에서 직접 읽는다. 타임아웃이면 None · 닫혔으면 b"".

        🔴 `socket.makefile()` 을 쓰지 않는다 — 그 객체는 타임아웃이 **한 번** 나면 이후 모든
           읽기가 OSError("cannot read from timed out object") 라, 첫 재생의 무음 뒤로 재생
           확인이 전부 실패했다(검토 C6 · 성능검증 §10.61 「원인 미규명」의 원인). 그 사이
           메인 루프가 재생 중에 마이크를 다시 들었다.
        """
        while b"\n" not in self._rbuf:
            try:
                chunk = self.s.recv(4096)
            except socket.timeout:
                return None
            if not chunk:
                line, self._rbuf = self._rbuf, b""
                return line
            self._rbuf += chunk
        line, _, self._rbuf = self._rbuf.partition(b"\n")
        return line + b"\n"

    def send(self, payload, expect=False):
```

<!-- rep: Rpi5/Demo/voice_assistant.py -->
```python
        """링크가 끊겼을 때 명령 채널도 버린다 — 다음 send 에서 다시 붙는다."""
        if self.s is not None:
```
```python
        """링크가 끊겼을 때 명령 채널도 버린다 — 다음 send 에서 다시 붙는다."""
        self._rbuf = b""
        if self.s is not None:
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && timeout 60 python3 selftest/test_voice_speaker.py 2>&1 | tail -1; ~/env/tts/.venv/bin/python selftest/test_voice_tts.py > /dev/null 2>&1; echo voice_tts_exit=$?`
Expected:
```
✅ 음성 명령 채널 검증 통과
voice_tts_exit=0
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/voice_assistant.py Demo/selftest/test_voice_speaker.py && git commit -m "fix(voice): 명령 채널을 소켓에서 직접 줄 단위로 읽는다 — 첫 무음 타임아웃 뒤 재생 확인이 모두 실패하던 것 (음성 수정 V1)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 9: V2 — EMO 로 멈추면 음성비서도 「비상정지」라고 말한다

**Files:** Modify `Rpi5/Demo/fsm.py`(`emo_active`) · `Rpi5/Demo/safety_console.py`(`_publish_state`) · `Rpi5/Demo/voice_card.py`(`build_card`) · Test `selftest/test_fsm.py` · `selftest/test_voice_card.py` · `selftest/test_console_flow.py`

**Interfaces:**
- Produces: `SafetyFSM.emo_active -> bool` · 상태 파일 키 `"비상정지"`(bool)

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_fsm.py -->
```python
def test_v2_emo_active_property():
    """V2 — EMO 로 걸린 차단인지 밖에서 읽을 수 있다(음성비서 상태 공개용)."""
    fsm, _ = make_fsm()
    run(fsm)
    assert fsm.emo_active is False
    fsm.press_button("EMO")
    assert fsm.emo_active is True
    fsm.release_block()
    assert fsm.emo_active is False
```

<!-- rep: Rpi5/Demo/selftest/test_voice_card.py -->
```python
def main():
    tmp = tempfile.mkdtemp(prefix="sop_card_test_")
```
```python
def test_v2_emo_block_card():
    """V2 — EMO 로 멈춘 차단은 「순서를 어겨」가 아니라 「비상정지」라고 쓴다(검토 C17)."""
    print("── V2 EMO 차단 카드")
    card = build_card(dict(LIVE, 상태="BLOCK", 비상정지=True), [], False)
    check("비상정지" in card and "순서를 어겨" not in card, f"EMO 차단 카드: {card}")
    card2 = build_card(dict(LIVE, 상태="BLOCK", 비상정지=False), [], False)
    check("순서를 어겨" in card2, "위반 차단은 그대로")


def main():
    test_v2_emo_block_card()
    tmp = tempfile.mkdtemp(prefix="sop_card_test_")
```

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def test_v2_emo_published_to_voice():
    """V2 — EMO 로 멈추면 음성비서 상태에 「비상정지」가 실린다."""
    print("\n[V2] EMO 상태 공개")
    win = make_console()
    pubs = []
    win._state_pub.publish = pubs.append
    win._on_cta()
    key(win, "E")
    check(bool(pubs) and pubs[-1].get("비상정지") is True,
          f"비상정지 = {pubs[-1].get('비상정지') if pubs else '공개 없음'}")
    win.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_fsm.py 2>&1 | grep -E "^  FAIL|passed"; python3 selftest/test_voice_card.py 2>&1 | grep -E "❌"; python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  FAIL  test_v2_emo_active_property
37/38 passed
  ❌ EMO 차단 카드: [사실]
❌ 실패 1건
  ❌ 비상정지 = None
❌ 실패 1건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/fsm.py -->
```python
    _emo_active = False
```
```python
    _emo_active = False

    @property
    def emo_active(self):
        """EMO 로 걸린 차단인가(해제 전까지) — 음성비서 상태 공개가 읽는다(V2)."""
        return self._emo_active
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
                "상태": self.fsm.state.value,
```
```python
                "상태": self.fsm.state.value,
                "비상정지": self.fsm.emo_active,     # EMO 차단을 순서 위반과 가른다(V2)
```

<!-- rep: Rpi5/Demo/voice_card.py -->
```python
        if st == "BLOCK":
            L.append("상태: 🔴 차단 중 — 순서를 어겨 전기 입력이 끊겼다")
```
```python
        if st == "BLOCK" and state.get("비상정지"):
            # 🔴 EMO 는 순서 위반이 아니다 — 화면 문구(G5)와 같은 말을 해야 한다(V2)
            L.append("상태: 🔴 비상정지(EMO)로 멈춤 — 순서 위반이 아니다. "
                     "EMO 를 복귀한 뒤 「차단 해제」를 눌러야 한다")
        elif st == "BLOCK":
            L.append("상태: 🔴 차단 중 — 순서를 어겨 전기 입력이 끊겼다")
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_fsm.py | tail -1; python3 selftest/test_voice_card.py 2>&1 | tail -1; python3 selftest/test_console_flow.py 2>&1 | tail -1; python3 selftest/test_state_publisher.py > /dev/null 2>&1; echo state_pub_exit=$?`
Expected:
```
38/38 passed
✅ 전부 통과
✅ 글라스 UI 흐름 통과
state_pub_exit=0
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/fsm.py Demo/safety_console.py Demo/voice_card.py Demo/selftest/test_fsm.py Demo/selftest/test_voice_card.py Demo/selftest/test_console_flow.py && git commit -m "feat(voice): EMO 차단을 상태 파일에 싣고 음성 카드가 「비상정지」라고 쓴다 (음성 수정 V2)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 10: V3·V4 — 호출어 설명을 동작에 맞추고, 녹음 파일을 종료 때 닫는다

**Files:** Modify `Rpi5/Demo/voice_lib.py`(`is_wake` 설명) · `Rpi5/Demo/voice_assistant.py`(`open_audio_log` · `run`) · Test `selftest/test_voice_lib.py` · `selftest/test_voice_speaker.py`

- [ ] **Step 1: 테스트**

<!-- rep: Rpi5/Demo/selftest/test_voice_lib.py -->
```python
print()
if _fails:
    print(f"🔴 실패 {len(_fails)}건")
```
```python
# ---- V3 가드(전후 통과) — 호출어는 발화 어디에 있어도 잡는다(설명을 이 동작에 맞췄다)
check(is_wake("아 저기 가디언 지금 몇 단계야"), "V3 — 군말 뒤의 호출어도 잡는다")
check(not is_wake("지금 몇 단계야"), "V3 — 호출어가 없으면 아니다")

print()
if _fails:
    print(f"🔴 실패 {len(_fails)}건")
```

<!-- add: Rpi5/Demo/selftest/test_voice_speaker.py -->
```python
def test_v4_audio_log_closed_at_exit():
    """V4 — 오디오 기록(마이크_전체.wav)을 프로그램이 끝날 때 닫도록 등록한다(함수목록 §4.1-8)."""
    print("\n[V4] 녹음 파일 닫기")
    reg = []
    atx = getattr(va, "atexit", None)
    old = atx.register if atx else None
    if atx:
        atx.register = lambda fn, *a, **k: reg.append(fn)
    try:
        opener = getattr(va, "open_audio_log", None)
        check(opener is not None, "open_audio_log 가 있다")
        if opener is not None:
            alog = opener(tempfile.mkdtemp())
            check(any(getattr(f, "__self__", None) is alog and f.__name__ == "close" for f in reg),
                  "종료 때 alog.close 가 불리도록 등록된다")
            alog.close()
    finally:
        if atx:
            atx.register = old
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_voice_lib.py 2>&1 | tail -1; timeout 60 python3 selftest/test_voice_speaker.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
✅ 전부 통과
  ❌ open_audio_log 가 있다
❌ 실패 1건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/voice_lib.py -->
```python
    """호출어가 들어 있는가 — 앞 두 글자만 보는 접두 매칭."""
```
```python
    """호출어가 들어 있는가 — 발화 **어디에든** 호출어 조각(WAKE_PREFIXES)이 있으면 호출이다.

    🔑 앞머리만 보지 않는다 — STT 가 앞에 군말(「아」·「저기」)을 붙여도 잡는다. 종전 설명
       「앞 두 글자 접두 매칭」은 동작과 달랐다(V3 · 설명을 동작에 맞춤). 동작을 바꾸려면
       오호출 실측이 먼저다(설계 D8).
    """
```

<!-- rep: Rpi5/Demo/voice_assistant.py -->
```python
import argparse
import array
```
```python
import argparse
import array
import atexit
```

<!-- rep: Rpi5/Demo/voice_assistant.py -->
```python
def esp_ip():
```
```python
def open_audio_log(path):
    """오디오 기록을 열고 **프로그램이 끝날 때 닫도록** 등록한다(V4).

    🔴 닫지 않으면 마이크_전체.wav 의 머리(길이)가 안 써져 파일이 깨질 수 있다. run() 은
       빠져나가는 길이 여러 개(리허설 종료·Ctrl+C)라 atexit 에 맡긴다.
    """
    alog = AudioLog(path)
    atexit.register(alog.close)
    return alog


def esp_ip():
```

<!-- rep: Rpi5/Demo/voice_assistant.py -->
```python
    alog = AudioLog(AUDIO_DIR)
```
```python
    alog = open_audio_log(AUDIO_DIR)
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_voice_lib.py 2>&1 | tail -1; timeout 60 python3 selftest/test_voice_speaker.py 2>&1 | tail -1`
Expected:
```
✅ 전부 통과
✅ 음성 명령 채널 검증 통과
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/voice_lib.py Demo/voice_assistant.py Demo/selftest/test_voice_lib.py Demo/selftest/test_voice_speaker.py && git commit -m "fix(voice): 호출어 설명을 동작에 맞춤 · 녹음 파일을 종료 때 닫는다 (음성 수정 V3·V4)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 11: 미룬 사소 7건 — ①·② 최종 리뷰

**Files:** Modify `Rpi5/Demo/safety_console.py`(`_on_fsm_state` 재개 조건 · `_commit_button`) · `Rpi5/Demo/fsm.py`(머리 경로 · `load_recipe` 설명) · `Rpi5/Demo/session_stats.py`(`sub_started` 설명) · Create `Rpi5/조사/판정수정-20260925/current_metrics.py` · Modify 같은 폴더 `after_current_metrics.txt`·`preview_variants.txt`(머리 한 줄) · Test `selftest/test_console_flow.py` · `selftest/test_fsm.py`

| # | 출처 | 무엇 | 처리 |
|---|---|---|---|
| 1 | ② 리뷰 4 | 경고로 멈춘 서브가 있을 때 작업 초기화 → 곧 버려질 작업에 「이어서 — 경고 해제」 로그 | 재개 조건에 `new != State.IDLE` |
| 2 | ② 리뷰 5 | 위반 차단 중 EMO → 전이가 없어 배너가 「순서 위반」 문구로 남고 음성 상태도 옛것 | `_commit_button` 이 배너(Task 7 `_show_block_banner`)·상태 공개를 다시 한다 |
| 3 | ① 리뷰 7 | `_just_done` 이 작업 초기화 때만 지워지고 새 작업 시작(`load_recipe`)·마지막 단계 완료 때는 남는다 · 초기화 시험 없음 | **동작 유지**(설계 P2 「손이 떠날 때까지」와 같다 · 실사용 빈도 낮음) — 의도를 `load_recipe` 설명에 적고, 초기화 시험을 가드로 추가 |
| 4 | ① 리뷰 9 | `after_current_metrics.txt`·`preview_variants.txt` 덧붙임(P2만·P3만)을 만든 명령이 저장소에 없다 | 당시 명령(세션 `56d1f2c1` 기록에서 원문 확보)을 `current_metrics.py` 로 · 두 파일 머리에 재현 명령 한 줄 |
| 5 | ① 리뷰 11 | `fsm.py` 머리의 설계서 경로가 `docs/…` (Rpi5 는 별도 저장소 — 다른 파일은 「상위 docs/…」) | 「상위」 한 단어 |
| 6 | ② 리뷰 9 | 공구 기록 이어 쓰기 조건(「마지막 기록과 같은 버튼」)이 「취소됐다」를 묵시적으로 가정 | 전제를 설명에 적는다(코드 변경 없음) |
| 7 | ② 리뷰 7 | 함수목록에 ②의 새 함수가 없다 | Task 13 Step 4(③ 새 함수와 함께) |

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def test_minor_reset_during_warning_no_resume_log():
    """사소 1(② 리뷰 4) — 경고로 멈춘 서브가 있을 때 작업 초기화하면 「이어서」가 찍히지 않는다."""
    print("\n[사소 1] 경고 중 작업 초기화")
    win = make_console()
    win._on_cta()
    key(win, "1")                                    # B1 → 서브 시작
    dwell_warning(win, "B3")                         # 경고 → 서브 일시정지
    n0 = len(win.log_browser.toPlainText().splitlines())
    win._reset_work()
    tail = win.log_browser.toPlainText().splitlines()[n0:]
    bad = [ln for ln in tail if "이어서 — 경고 해제" in ln]
    check(not bad, f"초기화 뒤 「이어서」 로그 {len(bad)}줄")
    check(win.fsm.state == State.IDLE and win._sub is None, "초기화는 그대로 된다")
    win.close()


def test_minor_emo_during_violation_block():
    """사소 2(② 리뷰 5) — 위반 차단 중 EMO 를 누르면 배너·음성 상태가 「비상정지」로 바뀐다(G5·V2)."""
    print("\n[사소 2] 위반 차단 중 EMO")
    win = make_console()
    pubs = []
    win._state_pub.publish = pubs.append
    win._on_cta()
    key(win, "3")                                    # 오답 → 위반 BLOCK
    key(win, "E")                                    # 차단 중 EMO
    check("비상정지" in win.alert._line1.text(), f"배너 = '{win.alert._line1.text()}'")
    check(bool(pubs) and pubs[-1].get("비상정지") is True,
          f"음성 상태 비상정지 = {pubs[-1].get('비상정지') if pubs else '공개 없음'}")
    win.close()
```

<!-- add: Rpi5/Demo/selftest/test_fsm.py -->
```python
def test_reset_clears_just_done():
    """가드(전후 통과) — 사소 3(① 리뷰 7): 작업 초기화는 방금 완료한 버튼 예외도 지운다(시험이 없었다)."""
    fsm, _ = make_fsm()
    run(fsm)
    fsm.press_button("B1")
    assert fsm._just_done == "B1"
    fsm.reset()
    assert fsm._just_done is None
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"; python3 selftest/test_fsm.py | tail -1`
Expected:
```
  ❌ 배너 = '순서 위반이 계속되어 인터락이 작동했습니다'
  ❌ 음성 상태 비상정지 = False
  ❌ 초기화 뒤 「이어서」 로그 1줄
❌ 실패 3건
39/39 passed
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
            elif old == State.WARNING and sub.paused:
                sub.resume()
```
```python
            elif old == State.WARNING and sub.paused and new != State.IDLE:
                # IDLE(작업 초기화·EMO 해제)은 아래 IDLE 분기가 서브를 정리한다 — 여기서 이으면
                # 곧 버려질 작업에 「이어서」가 찍혔다(② 리뷰 4)
                sub.resume()
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        before = self.fsm.expected_step
        self.fsm.press_button(button, time.time())
```
```python
        before = self.fsm.expected_step
        was_block = self.fsm.state == State.BLOCK
        self.fsm.press_button(button, time.time())
        if was_block and self.fsm.state == State.BLOCK and button == self._emo_button():
            # 🔴 위반 차단 중 EMO — 판정기가 이미 BLOCK 이라 상태가 바뀌지 않아 _on_fsm_state 가
            #    불리지 않는다. 그대로 두면 배너가 「순서 위반」 문구로 남고 음성비서도 위반
            #    차단이라고 말한다(② 리뷰 5 · G5·V2).
            self._show_block_banner(emo=True)
            self._publish_state()
```

<!-- rep: Rpi5/Demo/fsm.py -->
```python
(2026-09-25 규칙 변경 — 설계 docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md D4).
```
```python
(2026-09-25 규칙 변경 — 설계 상위 docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md D4).
```

<!-- rep: Rpi5/Demo/fsm.py -->
```python
    def load_recipe(self):
        """IDLE → READY → PROCESS RUN. 레시피(공정 매뉴얼) 로드 완료."""
```
```python
    def load_recipe(self):
        """IDLE → READY → PROCESS RUN. 레시피(공정 매뉴얼) 로드 완료.

        🔑 방금 완료한 버튼 예외(_just_done)는 여기서 지우지 않는다 — 마지막 단계를 누른 손이
           그 버튼 위에 남은 채 새 작업이 시작되면, 손이 한 번 떠날 때까지 그 버튼 체류는
           경고하지 않는다(P2 「손이 떠날 때까지」와 같은 규칙 · ① 리뷰 7). 작업 초기화
           (reset)는 지운다.
        """
```

<!-- rep: Rpi5/Demo/session_stats.py -->
```python
        #    새로 만들면 결과창에 같은 단계 공구 줄이 두 번 찍힌다. 오답 횟수는 남기고
        #    쥔 시각은 이번 시도부터 다시 잰다.
```
```python
        #    새로 만들면 결과창에 같은 단계 공구 줄이 두 번 찍힌다. 오답 횟수는 남기고
        #    쥔 시각은 이번 시도부터 다시 잰다.
        #    ⚠️ 조건은 「취소됐다」가 아니라 「마지막 기록과 같은 버튼」이다 — 같은 버튼의 공구
        #    서브가 다시 시작되는 길이 차단 취소 뒤 재시도뿐이라 지금은 같다(EMO·작업 시작은
        #    집계를 비우고, 레시피 단계 버튼은 서로 다르다). 한 레시피에 같은 버튼 단계가
        #    둘 생기면 취소 표시를 따로 둬야 한다(② 리뷰 9).
```

<!-- new: Rpi5/조사/판정수정-20260925/current_metrics.py -->
```python
"""§12.31 정의로 판정기 지표를 잰다 — after_current_metrics.txt 와 preview_variants.txt 덧붙임
(P2만·P3만)을 다시 만든다.

실행: cd Rpi5/Demo && python3 ../조사/판정수정-20260925/current_metrics.py
조건 = fsm_sim 시뮬레이션 정책 4종 · hoi.db 22세션(2026-07 모조 콘솔 VGA · 매 프레임 PNG 저장 중 9.1fps 촬영).
2026-09-25 판정 수정 ① 때 한 번 돌리고 저장하지 않은 명령을 그대로 옮겼다(① 최종 리뷰 9 —
재현 명령이 저장소에 없었다 · 원문 = 세션 56d1f2c1 기록).
🔴 「수정 후」 줄은 **지금 코드**의 fsm.py 를 잰다 — ① 뒤 판정기가 바뀌면 값이 달라진다.
   「수정 전」·「P2만」·「P3만」은 preview_variants.py 의 재구현(make_cls)이다.
"""
import sys

sys.path[:0] = ['test', '.']
import fsm_sim
import hoi_metrics

PV = "../조사/판정수정-20260925/preview_variants.py"
con = hoi_metrics.connect()


def summary(label):
    rows = fsm_sim.run_all(con)
    norm = [r for r in rows if r['violations'] == 0]
    fa = sum(r['false_alarms'] for r in norm)
    mins = sum(r['duration_sec'] for r in norm) / 60
    tb = sum(r['blocked'] for r in rows)
    tv = sum(r['violations'] for r in rows)
    ex = fsm_sim.run_all(con, exclude_cliff=True)
    eb = sum(r['blocked'] for r in ex)
    ev = sum(r['violations'] for r in ex)
    print(f"{label}: E2E 오경보(정상 세션 {len(norm)}개) {fa}건/{mins:.1f}분 = {fa/mins:.2f}회/분"
          f" · 사전 차단 {tb}/{tv} = {tb/tv*100:.1f}% · 절벽 제외 {eb}/{ev} = {eb/ev*100:.1f}%")


summary("수정 후(실제 코드)")
# preview_variants.py 의 규칙 재구현(make_cls)만 빌린다 — 실행 루프 앞까지
src = open(PV, encoding="utf-8").read().split("\nfor name, flags")[0]
ns = {}
exec(compile(src, "preview_variants", "exec"), ns)
fsm_sim.SafetyFSM = ns["make_cls"](0, 0, 0, 0)
summary("수정 전(재구성)")
for name, flags in [("P2만", (0, 0, 1, 0)), ("P3만", (0, 0, 0, 1))]:
    fsm_sim.SafetyFSM = ns["make_cls"](*flags)
    rows = fsm_sim.run_all(con)
    fa = sum(r['false_alarms'] for r in rows)
    tb = sum(r['blocked'] for r in rows)
    tv = sum(r['violations'] for r in rows)
    print(f"{name:<10} 오경보 {fa:>4}건 · 사전 차단 {tb}/{tv}")     # preview_variants.txt 덧붙임과 같은 모양
```

<!-- rep: Rpi5/조사/판정수정-20260925/after_current_metrics.txt -->
```text
# §12.31 정의(정상 세션 = 위반 0 세션 · 사전 차단 = 위반 눌림 전 WARNING)로 계산 — 2026-09-25
```
```text
# §12.31 정의(정상 세션 = 위반 0 세션 · 사전 차단 = 위반 눌림 전 WARNING)로 계산 — 2026-09-25
# 재현: cd Rpi5/Demo && python3 ../조사/판정수정-20260925/current_metrics.py (첫 두 줄)
```

<!-- rep: Rpi5/조사/판정수정-20260925/preview_variants.txt -->
```text
# 덧붙임(2026-09-25, 구현 후 분해) — 같은 스크립트 make_cls 로 P2·P3 단독
```
```text
# 덧붙임(2026-09-25, 구현 후 분해) — 같은 스크립트 make_cls 로 P2·P3 단독 · 재현 = current_metrics.py (끝 두 줄)
```

- [ ] **Step 4: 통과 확인** — 재현 스크립트 출력이 저장된 줄과 **글자까지 같아야** 한다(수치는 계획서에 적지 않는다 — `rules/계획서작성.md`)

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | tail -1; python3 selftest/test_fsm.py | tail -1; python3 selftest/test_session_stats.py > /dev/null 2>&1; echo session_stats_exit=$?; D=../조사/판정수정-20260925; out=$(python3 $D/current_metrics.py 2>&1 | grep -v "^\[config\]"); diff <(sed -n 4,5p $D/after_current_metrics.txt) <(echo "$out" | sed -n 1,2p) && echo 재현_일치_전후; diff <(sed -n 10,11p $D/preview_variants.txt) <(echo "$out" | sed -n 3,4p) && echo 재현_일치_분해`
Expected:
```
✅ 글라스 UI 흐름 통과
39/39 passed
session_stats_exit=0
재현_일치_전후
재현_일치_분해
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/safety_console.py Demo/fsm.py Demo/session_stats.py Demo/selftest/test_console_flow.py Demo/selftest/test_fsm.py 조사/판정수정-20260925/current_metrics.py 조사/판정수정-20260925/after_current_metrics.txt 조사/판정수정-20260925/preview_variants.txt && git commit -m "fix: 미룬 사소 항목 — 경고 중 초기화 「이어서」 로그 · 위반 차단 중 EMO 배너·음성 상태 · _just_done 수명 의도 · ① 지표 재현 스크립트 · 설명 2곳 (①·② 최종 리뷰)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 12: 검증 관문

**Files:** Create `Rpi5/조사/인식인터락음성-20260925/after_selftest.txt` · `after_gate.txt`

- [ ] **Step 1: 자가 테스트 전부 · 관문**

Run:
```bash
cd /home/pi/sop-project/Rpi5/Demo && D=../조사/인식인터락음성-20260925 && \
for t in selftest/test_*.py; do
  py=python3; [ "$(basename $t)" = test_voice_tts.py ] && py=~/env/tts/.venv/bin/python
  timeout 600 $py "$t" >/dev/null 2>&1; rc=$?; echo "$(basename $t) exit=$rc"
done > $D/after_selftest.txt; diff $D/before_selftest.txt $D/after_selftest.txt; \
python3 test/fsm_sim.py --gate > $D/after_gate.txt 2>&1; echo gate_exit=$?
```
Expected:
```
14a15
> test_interlock.py exit=0
18a20
> test_recognition.py exit=0
27a30
> test_voice_speaker.py exit=0
gate_exit=0
```
- diff 가 새 파일 세 줄 말고 더 있으면 → 중단 규칙 ②. · `gate_exit` ≠ 0 → 멈춘다.

- [ ] **Step 2: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add 조사/인식인터락음성-20260925/after_selftest.txt 조사/인식인터락음성-20260925/after_gate.txt && git commit -m "docs(조사): 인식·인터락·음성 수정 ③ 결과 — 자가 테스트·관문 (수정 후)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 13: 정본·문서 반영

🔴 통합문서를 열면 `.claude/rules/정본편집.md` 가 붙는다 — 그 규칙을 따른다(현행만 본문 · 폐기는 `docs/변경이력.md` · 미구현은 장 끝 ⏸).

**Files:** Modify `docs/통합문서.md` · `docs/변경이력.md` · `Rpi5/함수목록.md`(§3 새 함수 · §4.1-1·7·8) · `Rpi5/CLAUDE.md`(`camera_thread` 줄) · `Rpi5/조사/README.md` · `Rpi5/조사/오탐검토-오버레이.py`(머리) · spec·계획서 머리

- [ ] **Step 1: 통합문서 현행 반영** — 자리는 `grep -n "YOLO_MAX_MISS\|가림 대응\|인터락\|INTERLOCK\|재생 확인\|호출어" docs/통합문서.md` 로 해당 장(§6 인식 · §8 인터락 · §9 음성) 안에서 찾는다:
  - §6 — 새 버튼 트랙은 **두 프레임 연속** 봐야 판정에 쓴다(한 프레임 늦음) · 같은 클래스가 둘이면 **지금 보이는 것 → 오래된 것 → 점수** 순으로 하나 · 손 검출은 **박스를 그리기 전** 프레임 · 손이 여럿이면 **신뢰도 1위 손 하나** · 카메라 **(재)연결 때 옛 트랙·판정기 관측을 버린다**(방금 완료한 버튼 예외는 유지).
  - §8 — 인터락은 **포트를 못 찾아도 꺼지지 않고** 연결할 때마다 다시 찾는다(`SOP_INTERLOCK=0` 으로만 끈다) · 못 찾은 동안은 포기하지 않는다(찾은 장치가 열리지 않을 때만 몇 번 뒤 포기 · 알림) · 장치가 사라지면 곧바로 끊김(빨강) · 재연결 동기화는 최신 요청 · 인터락 없이 차단되면 배너 둘째 줄 「⚠ 인터락 미연결 — 화면에서만 차단 중」(알람 창은 띄우지 않는다).
  - §9 — 재생 확인이 무음 뒤에도 유지(재생이 끝나야 다시 듣는다) · EMO 로 멈추면 음성도 「비상정지」 · 호출어는 발화 어디에 있어도 잡는다.
- [ ] **Step 2: 변경이력** — ①「인터락 포트를 못 찾으면 인터락을 끈다(INTERLOCK_ENABLED = 포트 찾음)」 ②「미연결 BLOCK 은 알람 창」 ③(통합문서에 적혀 있었다면) 「같은 클래스 트랙은 점수 높은 쪽」 을 해당 주제 절에(§8 인터락 자리가 없으면 「12. 제어·피드백·GUI 기재」).
- [ ] **Step 3: 🔓 표** — ①「실제 시연 프로그램 문제 수정」 행: 「①·②GUI 완료 · ③ 대기」 → 「①②③ 구현 · ③ 공동 확인 대기」 + 계획서 경로 · ⏸ 실HW(Arduino 인터락 · ESP32 실재생) ②「손 검출 입력에 YOLO 박스」 행: R3 로 **해결** — 행을 지우고 근거 절(§12.73-(5))에 결론 한 줄 ③「측정 도구와 실제 시연 프로그램의 불일치」 행: 「R2(두 프레임 확정)로 `bench_detector` 추적 복사본과 차이 하나 더」 덧붙임.
- [ ] **Step 4: 함수목록** — ①§4.1 1(손 검출 입력)·7(호출어 설명)·8(녹음 파일)에 「✅ 해결 — 인식·음성 수정 ③(2026-09-25)」 ②§3 새 함수 행(표 형식 그대로 · 줄 = 넣을 때 기준 · 모듈 제목 개수와 머리의 함수 수도 갱신 · 🔴 `조사/함수목록-생성/build.py` 로 다시 만들지 않는다 — 손으로 고친 §4 가 덮인다): ②의 `SubTask.pause`·`resume`·`paused` · `SafetyConsole._cancel_sub`·`_emo_button` · `ToolState.force_grasped` · `DemoRecorder.take_dead` / ③의 `hand_tracker.pick_hand` · `CameraThread._on_connected` · `SafetyFSM.forget_observation`·`emo_active` · `SafetyConsole._on_stream_reset`·`_show_block_banner` · `Speaker._readline` · `voice_assistant.open_audio_log` · 이름 바뀜 `config._resolve_interlock_port` → `resolve_interlock_port` · `DemoRecorder.submit_camera` 의 부르는 곳(G10 뒤 — `grep -n submit_camera Demo/*.py` 로 확인) ③`Rpi5/CLAUDE.md` 의 `_update_tracks()` 설명에 「새 트랙은 두 프레임 확정(YOLO_CONFIRM_HITS) · 같은 클래스는 오래된 트랙 우선」.
- [ ] **Step 4-1: 오탐 검수 사진 스크립트** — `조사/오탐검토-오버레이.py` 는 실제 시연의 `_update_tracks` 를 부른다. R2 뒤에 다시 만들면 **한 프레임짜리 새 박스(검수 대상인 오탐 후보)가 빠져** 저장된 사진과 달라진다 — 스크립트 머리와 `조사/README.md` 의 「똑같이 다시 만들어진다(40장 바이트 비교 일치)」 줄에 「R2(2026-09-25) 전 추적 기준 · 지금 다시 만들면 한 프레임 박스가 빠진다」를 적는다.
- [ ] **Step 5: spec·계획서 머리** — 상태 줄(공동 확인 전이면 「구현·관문 통과(공동 확인 대기)」).
- [ ] **Step 6: 커밋** (두 저장소)

```bash
cd /home/pi/sop-project/Rpi5 && git add 함수목록.md CLAUDE.md 조사/README.md 조사/오탐검토-오버레이.py && git commit -m "docs: 인식·인터락·음성 수정 ③ 반영 — 함수목록 §4.1-1·7·8 해결·②③ 새 함수 · 두 프레임 확정 트랙 · 오탐 검수 스크립트 R2 주의

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
cd /home/pi/sop-project && git add docs/통합문서.md docs/변경이력.md docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md docs/superpowers/plans/2026-09-25-런타임문제수정-3-인식인터락음성.md && git commit -m "docs(정본): 인식·인터락·음성 수정 ③ — §6 두 프레임 확정·손 입력·재연결 · §8 인터락 재탐색·화면에서만 차단 · §9 재생 확인·EMO 안내 · 🔓 표

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 14: 최종 리뷰 · 공동 확인 · 마무리

- [ ] **Step 1: 최종 리뷰 1회** — `superpowers:requesting-code-review`, 새 맥락 리뷰어(가장 성능 좋은 모델). Review Focus 를 그대로 넘긴다.
- [ ] **Step 2: 공동 확인** — 태스크마다 한 줄 · ⏸ 남은 것(Arduino 인터락 실기동 · ESP32 실재생 · 실기동 인식) · 실재생 확인(서브 ESP32)을 지금 할지 사용자에게 묻는다. 🔴 Claude 단독 「완료」 선언 금지.
- [ ] **Step 3: `work-close`** — push 는 사용자 결정에 따른다(Global Constraints).
