# 글라스 UI 애니메이션 구현 계획

> **상태(2026-08-24 확인)** — ✅ **구현·관문 통과 완료** (`Rpi5` `324142b`…`bc2de12`). `test_anim.py` 13검사 + 기존 selftest 9종 PASS, `run_demo.sh` 실기동 정상.
> ⏸ **G6(애니메이션 ON/OFF FPS 전후 비교) 미실시** — 카메라 부재로 측정 불가. 계측 수단(`fps.py`)은 마련됨. 하락 10% 초과면 기본 OFF 규칙이 대기 중이다.
> ⚠️ **아래 체크박스는 실행 당시 갱신되지 않았다** — 진행 상태는 이 줄과 `docs/작업로그.md` 2026-08-16 블록이 정본이다.

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:executing-plans` 로 태스크 단위 실행. 체크박스(`- [ ]`)로 추적한다.

**Goal:** 오버레이가 상태를 「툭 바꿔 표시」하던 것을 「움직여 표시」하도록 바꾼다(4종).

**Architecture:** `anim.py` 를 신설해 도우미·지속시간 상수·ON/OFF 게이트를 한 곳에 모으고, `overlay.py` 의 각 위젯은 **직전 상태와 달라졌을 때만** 그 도우미를 부른다. 표시 정보·배치·색은 바꾸지 않는다.

**Tech Stack:** PyQt6 6.11 / `QVariantAnimation` + QSS. 🔴 `QGraphicsEffect` 계열 금지.

**Spec:** `docs/superpowers/specs/2026-08-16-ui-애니메이션-design.md`

## Global Constraints

- 🔴 **`QGraphicsEffect`(DropShadow·Opacity) 금지** — 페인터 충돌로 화면이 안 그려지고 클릭도 안 먹었다(CPU 60%, `overlay.py:73`). 투명도는 QSS 색 알파를 `QVariantAnimation` 으로 굴려서 낸다.
- 🔴 **테두리 두께를 애니메이션하지 않는다** — 레이아웃이 밀려 글자가 흔들린다. 색 알파만.
- 🔴 **`_sub_timer` 가 200ms 주기로 갱신 함수를 반복 호출한다**(`safety_console.py:224`). 갱신 함수 안에서 무조건 애니메이션을 시작하면 초당 5번 재시작된다. **직전 상태 비교 게이트가 필수.**
- 색은 `theme.C(token)`, 폰트는 `config.font()`. 새 파일에 색 코드를 직접 쓰지 않는다.
- 배치는 부모 크기 % (`overlay.place`). 고정 픽셀 금지.
- 테스트는 pytest 가 아니다 — `selftest/*.py` 관례(`check(cond, msg)` + `sys.exit(1)`), 실행은 `python3 Demo/selftest/test_X.py`, `QT_QPA_PLATFORM=offscreen`.
- 코드는 `Rpi5` repo 에 커밋한다(문서는 `sop-project`). 커밋 서명 = `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.

---

### Task 1: `anim.py` — 도우미·상수·게이트

**Files:**
- Create: `Rpi5/Demo/anim.py`
- Modify: `Rpi5/Demo/config.py` (UI 폰트 블록 아래에 `[UI 애니메이션]` 블록 추가)
- Test: `Rpi5/Demo/selftest/test_anim.py`

**Interfaces (뒤 태스크가 쓰는 것):**
- `anim.enabled() -> bool`
- `anim.tween(parent, ms, on_step, curve=None, on_done=None, loops=1) -> QVariantAnimation|None` — 0.0→1.0 보간. 꺼져 있으면 `on_step(1.0)`·`on_done()` 을 즉시 부르고 `None`
- `anim.flash(widget, color_hex, ms=D_FLASH)` — 원래 QSS 뒤에 배경 알파를 덧붙였다 걷어낸다
- `anim.slide_in(widget, target_rect, ms=D_SLIDE, dy=SLIDE_DY)` — 꺼져 있으면 `target_rect` 에 즉시 놓는다
- `anim.busy(widget) -> bool` — 이 위젯에 진행 중인 위치 애니메이션이 있는가(relayout 회피용)
- `anim.Pulse(widget, base_qss_fn, color_hex)` — `.start()` / `.stop()` / `.active()`
- 상수: `D_FLASH=520` `D_SLIDE=260` `D_BAR=300` `D_GAUGE=200` `D_PULSE=620` `SLIDE_DY=40`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`Rpi5/Demo/selftest/test_anim.py` (관례는 `selftest/test_overlay.py` 와 동일 — 머리말·`check`·`__main__` 블록을 그대로 따른다).

파일 머리에 필요한 것: `sys.path` 에 Demo 추가 · `os.environ.setdefault("QT_QPA_PLATFORM", "offscreen")` · `from PyQt6.QtCore import QRect` · `from PyQt6.QtWidgets import QApplication, QWidget, QLabel` · `import config, theme, anim` · `from overlay import StatusPanel, GaugePanel, AlertBanner` · `_app = QApplication.instance() or QApplication([])` · `_w = QWidget()` · `SCREEN = QRect(0, 0, 1920, 1080)`:

```python
def test_gate_off_applies_final_state():
    """🔴 관문 ① — 꺼져 있으면 애니메이션 없이 최종 상태가 즉시 적용된다."""
    config.UI_ANIMATION = False
    seen = []
    a = anim.tween(_w, 300, lambda t: seen.append(float(t)))
    check(a is None, "꺼짐: 애니메이션 객체를 만들지 않는다")
    check(seen == [1.0], f"꺼짐: 최종값 1.0 만 한 번 적용된다 (실제 {seen})")

    lbl = QLabel(_w)
    lbl.setStyleSheet("color: red;")
    anim.slide_in(lbl, QRect(10, 20, 30, 40))
    check(lbl.geometry() == QRect(10, 20, 30, 40), "꺼짐: slide_in 이 목표 위치에 즉시 놓는다")
    config.UI_ANIMATION = True


def test_pulse_stops():
    """🔴 관문 ③ — 맥박은 멈춘다. 안 멈추면 QSS 재적용으로 CPU 를 태운다."""
    p = anim.Pulse(_w, lambda: "", theme.C("danger"))
    p.start()
    check(p.active(), "start 후 active")
    p.stop()
    check(not p.active(), "stop 후 정지")
    p.stop()
    check(not p.active(), "두 번 stop 해도 예외 없이 정지 상태")


def test_busy_flag():
    """relayout 회피의 근거 — 슬라이드 중에는 busy 가 True."""
    lbl = QLabel(_w)
    anim.slide_in(lbl, QRect(0, 0, 10, 10))
    check(anim.busy(lbl), "슬라이드 중 busy")
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd ~/sop-project/Rpi5/Demo && python3 selftest/test_anim.py`
Expected: FAIL — `ModuleNotFoundError: No module named 'anim'`

- [ ] **Step 3: `config.py` 에 스위치를 넣는다**

`[UI 폰트]` 블록 아래에 둔다(파일 앞부분의 UI 관련 설정끼리 모은다):

```python
# =============================================================================
# [UI 애니메이션] — 정본: 상위 docs/superpowers/specs/2026-08-16-ui-애니메이션-design.md
# =============================================================================
# 🔴 성능은 아직 미판정이다(카메라 부재로 FPS 측정을 시나리오 테스트로 미뤘다).
#    문제가 보이면 SOP_UI_ANIM=0 으로 끈다 — 코드를 고칠 필요 없다.
UI_ANIMATION = os.environ.get("SOP_UI_ANIM", "1") != "0"

# 실측 FPS 를 화면 우하단에 표시할지. 기본은 로그에만 남긴다(시연 화면을 더럽히지 않는다).
SHOW_FPS = os.environ.get("SOP_SHOW_FPS", "0") == "1"
```

- [ ] **Step 4: `anim.py` 를 쓴다**

```python
"""오버레이 애니메이션 도우미 — 정본: 상위 specs/2026-08-16-ui-애니메이션-design.md

🔴 QGraphicsEffect(DropShadow·Opacity)를 쓰지 않는다 — overlay.py `_glow()` 참조.
   페인터 충돌로 화면이 안 그려지고 클릭도 안 먹었다(CPU 60%).
   투명도는 QSS 색의 알파를 QVariantAnimation 으로 굴려서 낸다.
🔴 테두리 **두께**를 애니메이션하지 않는다 — 레이아웃이 밀려 글자가 흔들린다.

끄는 법: config.UI_ANIMATION = False (환경변수 SOP_UI_ANIM=0).
   꺼지면 **여기서** 최종 상태를 즉시 적용한다. 호출부에 if 를 흩지 않는다 —
   분기가 위젯마다 퍼지면 끄는 경로를 검증할 수 없다.
"""

from PyQt6.QtCore import QVariantAnimation, QEasingCurve
from PyQt6.QtGui import QColor

import config

# 지속시간(ms) — 값의 단일 출처. 실기동에서 눈으로 보고 조정한다.
D_FLASH = 520      # 줄 반짝
D_SLIDE = 260      # 배너 등장
D_BAR   = 300      # 현재 단계 막대
D_GAUGE = 200      # 게이지 따라가기 (_sub_timer 주기와 같게 — 끊기지 않는다)
D_PULSE = 620      # 차단 맥박 반주기
SLIDE_DY = 40      # 배너가 아래에서 올라오는 거리(px)

_BUSY = "_anim_busy"


def enabled():
    return bool(getattr(config, "UI_ANIMATION", True))


def tween(parent, ms, on_step, curve=QEasingCurve.Type.OutCubic, on_done=None, loops=1):
    """0.0 → 1.0 보간. 꺼져 있으면 최종값만 적용하고 None 을 준다."""
    if not enabled():
        on_step(1.0)
        if on_done:
            on_done()
        return None
    a = QVariantAnimation(parent)
    a.setStartValue(0.0)
    a.setEndValue(1.0)
    a.setDuration(ms)
    a.setEasingCurve(curve)
    a.setLoopCount(loops)
    a.valueChanged.connect(on_step)
    if on_done:
        a.finished.connect(on_done)
    a.start(QVariantAnimation.DeletionPolicy.DeleteWhenStopped)
    return a


def flash(widget, color_hex, ms=D_FLASH):
    """한 줄을 잠깐 반짝. 원래 QSS 를 기억했다가 그대로 되돌린다."""
    if not enabled():
        return
    base = widget.styleSheet()

    def step(t):
        c = QColor(color_hex)
        alpha = 0.45 * (1.0 - float(t))
        widget.setStyleSheet(
            base + f"background-color: rgba({c.red()},{c.green()},{c.blue()},{alpha:.3f});")

    tween(widget, ms, step, curve=QEasingCurve.Type.OutQuad,
          on_done=lambda: widget.setStyleSheet(base))


def slide_in(widget, target, ms=D_SLIDE, dy=SLIDE_DY):
    """아래(dy>0)에서 미끄러져 목표 사각형에 앉는다. geometry 만 움직인다."""
    if not enabled():
        widget.setGeometry(target)
        widget.show()
        return
    widget.setGeometry(target.translated(0, dy))
    widget.show()
    setattr(widget, _BUSY, True)
    tween(widget, ms,
          lambda t: widget.setGeometry(target.translated(0, int(dy * (1 - float(t))))),
          on_done=lambda: setattr(widget, _BUSY, False))


def busy(widget):
    """위치 애니메이션이 진행 중인가 — relayout 이 geometry 를 덮어쓰지 않게 확인한다."""
    return bool(getattr(widget, _BUSY, False))


class Pulse:
    """테두리 색이 천천히 맥박한다(차단 배너).

    🔴 반드시 stop() 으로 멈춘다 — 살아남으면 QSS 를 계속 재적용해 CPU 를 태운다.
    base_qss_fn: 맥박이 덧씌울 바탕 QSS 를 그때그때 만들어 주는 함수(테마 전환 대응).
    """

    def __init__(self, widget, base_qss_fn, color_hex):
        self._w = widget
        self._base = base_qss_fn
        self._c = color_hex
        self._anim = None

    def start(self):
        if self._anim is not None:
            return                      # 이미 뛰고 있으면 재시작하지 않는다
        base = self._base()
        c = QColor(self._c)

        def step(t):
            a = 0.30 + 0.70 * float(t)
            self._w.setStyleSheet(
                base + f"border: 1px solid rgba({c.red()},{c.green()},{c.blue()},{a:.2f});")

        if not enabled():
            step(1.0)
            return
        self._anim = tween(self._w, D_PULSE, step,
                           curve=QEasingCurve.Type.InOutSine, loops=-1)

    def stop(self):
        if self._anim is not None:
            self._anim.stop()
            self._anim = None

    def active(self):
        return self._anim is not None
```

- [ ] **Step 5: 테스트가 통과하는지 확인한다**

Run: `cd ~/sop-project/Rpi5/Demo && python3 selftest/test_anim.py`
Expected: PASS (`✅` 만 출력, exit 0)

- [ ] **Step 6: 커밋**

```bash
cd ~/sop-project/Rpi5 && git add Demo/anim.py Demo/config.py Demo/selftest/test_anim.py
git commit -m "feat(ui): 애니메이션 도우미 anim.py 신설 — 게이트·상수 단일 출처"
```

---

### Task 2: `GaugePanel` — 게이지 따라가기 + 공구 상태 번짐

**Files:**
- Modify: `Rpi5/Demo/overlay.py` (`GaugePanel.__init__`·`update_view`)
- Test: `Rpi5/Demo/selftest/test_anim.py` (추가)

**Interfaces:**
- Consumes: `anim.tween`·`anim.flash`·`anim.D_GAUGE`
- Produces: `GaugePanel._shown_progress`(표시 중인 채움 비율) — 테스트가 읽는다

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
def test_gauge_no_restart_on_same_value():
    """🔴 관문 ② — _sub_timer 가 200ms 마다 같은 값을 다시 넣어도 재시작하지 않는다."""
    g = GaugePanel(_w)
    sub = _FakeSub(progress=0.5, tool_ok=False, wrong_tool=None)
    g.update_view(sub)
    first = g._gauge_anim
    for _ in range(5):
        g.update_view(sub)                      # 같은 값 반복 — 실제 런타임의 5Hz 호출
    check(g._gauge_anim is first, "같은 값 반복 호출은 애니메이션을 재시작하지 않는다")


def test_gauge_flashes_only_on_tool_change():
    """공구 상태가 바뀐 순간에만 번진다."""
    g = GaugePanel(_w)
    sub = _FakeSub(progress=0.3, tool_ok=False, wrong_tool=None, needs_tool=True)
    g.update_view(sub)
    check(g._prev_tool == (False, None), "직전 공구 상태를 기억한다")
    sub.tool_ok = True
    g.update_view(sub)
    check(g._prev_tool == (True, None), "바뀐 값으로 갱신된다")
```

`_FakeSub` 는 `GaugePanel.update_view` 가 읽는 속성만 갖는 테스트용 더미다(`selftest/test_anim.py` 안에 둔다):

```python
class _FakeSub:
    def __init__(self, progress, tool_ok, wrong_tool, needs_tool=False):
        self.is_active, self.label = True, "테스트"
        self.total_sec, self.elapsed_sec = 30, 30 * progress
        self.progress, self.time_done = progress, progress >= 1.0
        self.needs_tool, self.want_tool_name = needs_tool, "렌치"
        self.tool_ok, self.wrong_tool = tool_ok, wrong_tool
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python3 selftest/test_anim.py`
Expected: FAIL — `AttributeError: 'GaugePanel' object has no attribute '_gauge_anim'`

- [ ] **Step 3: `GaugePanel.__init__` 에 직전 상태를 넣는다**

`self._needs_tool = False` 아래에 이어 쓴다:

```python
        # 애니메이션용 직전 상태 — 🔴 _sub_timer 가 200ms 마다 같은 값을 다시 넣는다.
        #    비교 없이 애니메이션을 걸면 초당 5번 재시작돼 끝나지 않는다.
        self._shown_progress = 0.0      # 지금 화면에 그려진 채움 비율
        self._target_progress = 0.0
        self._gauge_anim = None
        self._prev_tool = None          # (tool_ok, wrong_tool)
```

- [ ] **Step 4: `update_view` 의 채움 계산을 보간으로 바꾼다**

기존 3줄

```python
        w = int(self._track.width() * sub.progress)
        self._fill.setGeometry(0, 0, w, 8)
```

를 아래로 바꾼다(`self._track.setStyleSheet(theme.gauge_qss())` 다음 자리 그대로):

```python
        # 목표만 갱신하고, 그리는 것은 보간에 맡긴다. 같은 목표면 아무 일도 하지 않는다.
        if abs(sub.progress - self._target_progress) > 1e-6:
            start = self._shown_progress
            self._target_progress = sub.progress

            def _step(t, s=start, e=sub.progress):
                self._shown_progress = s + (e - s) * float(t)
                self._fill.setGeometry(
                    0, 0, int(self._track.width() * self._shown_progress), 8)

            self._gauge_anim = anim.tween(self, anim.D_GAUGE, _step,
                                          curve=QEasingCurve.Type.Linear)
        else:
            self._fill.setGeometry(
                0, 0, int(self._track.width() * self._shown_progress), 8)
```

`overlay.py` 머리의 import 에 `anim` 과 `QEasingCurve` 를 더한다:

```python
from PyQt6.QtCore import Qt, QRect, QEasingCurve, pyqtSignal
import anim
```

- [ ] **Step 5: 공구 상태가 바뀐 순간에만 번지게 한다**

`self._tool_state.setStyleSheet(theme.text_qss(token, 600))` **다음 줄**에 붙인다:

```python
            # 바뀐 순간에만 번진다 — 200ms 주기 반복 호출에서 매번 번지면 안 된다.
            now_tool = (sub.tool_ok, sub.wrong_tool)
            if self._prev_tool is not None and now_tool != self._prev_tool:
                anim.flash(self._tool_state, theme.C(token))
            self._prev_tool = now_tool
```

- [ ] **Step 6: 테스트 통과 + 기존 오버레이 테스트도 깨지지 않았는지 확인한다**

Run: `python3 selftest/test_anim.py && python3 selftest/test_overlay.py`
Expected: 둘 다 PASS

- [ ] **Step 7: 커밋**

```bash
cd ~/sop-project/Rpi5 && git add Demo/overlay.py Demo/selftest/test_anim.py
git commit -m "feat(ui): 게이지 채움 보간 + 공구 상태 변화 시 번짐"
```

---

### Task 3: `StatusPanel` — 단계 전환

**Files:**
- Modify: `Rpi5/Demo/overlay.py` (`StatusPanel.__init__`·`update_view`)
- Test: `Rpi5/Demo/selftest/test_anim.py` (추가)

**Interfaces:**
- Consumes: `anim.flash`·`anim.tween`·`anim.D_BAR`
- Produces: `StatusPanel._prev_step`(직전 `expected_step`)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
def test_status_step_change_gate():
    s = StatusPanel(STEPS, _w)
    s.update_view("PROCESS_RUN", 1)
    check(s._prev_step == 1, "직전 단계를 기억한다")
    for _ in range(3):
        s.update_view("PROCESS_RUN", 1)         # 같은 단계 반복
    check(s._prev_step == 1, "같은 단계 반복은 상태를 바꾸지 않는다")
    s.update_view("PROCESS_RUN", 2)
    check(s._prev_step == 2, "단계가 바뀌면 갱신된다")
```

`STEPS` 는 `selftest/test_overlay.py` 와 같은 4단계 리스트를 이 파일에도 둔다(테스트끼리 의존시키지 않는다).

- [ ] **Step 2: 실패를 확인한다**

Run: `python3 selftest/test_anim.py`
Expected: FAIL — `AttributeError: ... '_prev_step'`

- [ ] **Step 3: `__init__` 에 직전 단계를 넣는다**

`self._steps = list(steps)` 다음 줄:

```python
        self._prev_step = None          # 애니메이션 트리거용 직전 expected_step
```

- [ ] **Step 4: `update_view` 끝에 전환 연출을 붙인다**

행 그리기 `for` 루프가 끝난 **뒤**, 함수 마지막에 추가한다:

```python
        # 단계가 바뀐 순간에만 — 끝난 줄이 완료색으로 반짝하고,
        # 새 현재 줄의 왼쪽 막대가 자라며 자리 잡는다.
        if started and self._prev_step is not None and expected_step != self._prev_step:
            old = self._prev_step - 1
            if 0 <= old < len(self._rows):
                anim.flash(self._rows[old], theme.C("done"))
            idx = expected_step - 1
            if 0 <= idx < len(self._rows):
                row = self._rows[idx]
                base = row.styleSheet()
                color = theme.C(cur_token)
                anim.tween(
                    row, anim.D_BAR,
                    lambda t, r=row, b=base, c=color: r.setStyleSheet(
                        b + f"border-left: {max(1, int(_ROW_BAR * float(t)))}px solid {c};"),
                    on_done=lambda r=row, b=base: r.setStyleSheet(b))
        self._prev_step = expected_step
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `python3 selftest/test_anim.py && python3 selftest/test_overlay.py`
Expected: 둘 다 PASS

- [ ] **Step 6: 커밋**

```bash
cd ~/sop-project/Rpi5 && git add Demo/overlay.py Demo/selftest/test_anim.py
git commit -m "feat(ui): 단계 전환 연출 — 완료 줄 반짝 + 현재 막대 자람"
```

---

### Task 4: `AlertBanner` — 슬라이드 등장 · 차단 맥박 · relayout 회피

**Files:**
- Modify: `Rpi5/Demo/overlay.py` (`AlertBanner.__init__`·`_paint`·`hide_all`·`relayout`)
- Test: `Rpi5/Demo/selftest/test_anim.py` (추가)

**Interfaces:**
- Consumes: `anim.slide_in`·`anim.busy`·`anim.Pulse`
- Produces: `AlertBanner._pulse`(Pulse 인스턴스)·`AlertBanner._pending_rect`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
def test_alert_pulse_lifecycle():
    """🔴 차단에서만 맥박하고, 벗어나면 반드시 멈춘다."""
    a = AlertBanner(_w)
    a.show_order_violation("B2", "펌프/퍼지")
    check(not a._pulse.active(), "경고에서는 맥박하지 않는다")
    a.show_block()
    check(a._pulse.active(), "차단에서 맥박한다")
    a.show_order_violation("B2", "펌프/퍼지")
    check(not a._pulse.active(), "차단을 벗어나면 멈춘다")
    a.show_block()
    a.hide_all()
    check(not a._pulse.active(), "숨기면 멈춘다")


def test_alert_entrance_happens_at_relayout():
    """등장은 위치를 정하는 relayout 이 실행한다 — _paint 는 예약만."""
    a = AlertBanner(_w)
    a.show_order_violation("B2", "펌프/퍼지")
    check(a._needs_entrance, "_paint 는 등장을 예약만 한다")
    a.relayout(SCREEN)
    check(not a._needs_entrance, "relayout 이 등장을 실행하고 예약을 지운다")


def test_alert_relayout_skips_while_sliding():
    """슬라이드 중 relayout 이 geometry 를 덮어쓰면 위치가 튄다."""
    a = AlertBanner(_w)
    a.show_block()
    anim.slide_in(a, QRect(0, 0, 100, 50))
    before = a.geometry()
    a.relayout(SCREEN)
    check(a.geometry() == before, "슬라이드 중에는 relayout 이 geometry 를 건드리지 않는다")
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python3 selftest/test_anim.py`
Expected: FAIL — `AttributeError: ... '_pulse'`

- [ ] **Step 3: `__init__` 에 맥박·보류 사각형을 넣는다**

`self._mode = None` 다음 줄:

```python
        # 차단 맥박 — 🔴 mode 를 벗어나거나 숨길 때 반드시 stop() 한다.
        self._pulse = anim.Pulse(
            self, lambda: theme.panel_qss("sheet", padding="14px 18px"),
            theme.C("danger"))
        self._shown_mode = None         # 화면에 그려져 있는 mode (등장 트리거 비교용)
        self._needs_entrance = False    # 다음 relayout 에서 미끄러져 들어올 것
```

- [ ] **Step 4: `_paint` 에 등장 예약·맥박을 건다**

🔴 **등장을 여기서 시작하면 안 된다.** 호출 순서가 `show_block()` → `_paint()` → (호출부의) `_relayout()` 이라, `_paint` 시점에는 **배너가 앉을 최종 위치가 아직 정해지지 않았다.** 목업은 `relayout` 을 먼저 부르고 슬라이드를 걸었기에 이 문제가 없었다. 그래서 여기서는 **예약만** 하고, 위치를 정하는 `relayout` 이 실제 등장을 실행한다(Step 6).

`_paint` 의 마지막 두 줄

```python
        self.raise_()
        self.show()
```

를 아래로 바꾼다:

```python
        self.raise_()
        # mode 가 바뀐 순간에만 등장을 예약한다 — 같은 mode 로 다시 그려질 때는
        # 이미 자리에 있으므로 움직이지 않는다(공구 배너는 200ms 주기로 다시 온다).
        if self._mode != prev_mode:
            self._pulse.stop()
            self._needs_entrance = True
            if self._mode == "block":
                self._pulse.start()
        self.show()
```

`_paint` 머리에서 이전 모드를 붙잡아 둔다 — `def _paint(self, token, ...)` 바로 다음 줄에:

```python
        prev_mode = getattr(self, "_shown_mode", None)
        self._shown_mode = self._mode
```

(🔴 `self._mode` 는 `show_*` 가 `_paint` 를 부르기 **전에** 이미 새 값으로 바뀌어 있다. 그래서 별도의 `_shown_mode` 로 비교한다.)

- [ ] **Step 5: `hide_all` 에서 멈춘다**

```python
    def hide_all(self):
        self._mode = None
        self._shown_mode = None
        self._needs_entrance = False
        self._pulse.stop()              # 🔴 살아남으면 QSS 재적용으로 CPU 를 태운다
        self.hide()
```

- [ ] **Step 6: `relayout` 이 슬라이드를 방해하지 않게 한다**

```python
    def relayout(self, parent_rect):
        # 🔴 슬라이드 중에는 geometry 를 건드리지 않는다 — _update_sub_view 가 200ms
        #    마다 relayout 을 부르므로, 그대로 두면 등장 중에 위치가 튄다.
        if anim.busy(self):
            return
        if self._mode == "block":
            place(self, parent_rect, width=_BLOCK_POS["width"])       # 중앙을 가린다
        else:
            place(self, parent_rect, width=_BANNER_POS["width"],
                  bottom=_BANNER_POS["bottom"])
        # 위치가 정해진 **지금** 등장한다(_paint 시점에는 목표 위치를 몰랐다).
        if self._needs_entrance:
            self._needs_entrance = False
            anim.slide_in(self, self.geometry())
```

- [ ] **Step 7: 테스트 통과 확인**

Run: `python3 selftest/test_anim.py && python3 selftest/test_overlay.py && python3 selftest/test_console_flow.py`
Expected: 셋 다 PASS

- [ ] **Step 8: 커밋**

```bash
cd ~/sop-project/Rpi5 && git add Demo/overlay.py Demo/selftest/test_anim.py
git commit -m "feat(ui): 경고 배너 슬라이드 등장 + 차단 테두리 맥박(정지 보장)"
```

---

### Task 5: 실측 FPS 계측 — 코드만

**Files:**
- Modify: `Rpi5/Demo/safety_console.py` (`_note_frame`·`_update_conn_bar` 부근)
- Test: `Rpi5/Demo/selftest/test_anim.py` (추가 — 순수 계산 함수만 검사)

**Interfaces:**
- Produces: `fps_meter.FpsMeter` 는 만들지 않는다. `SafetyConsole._fps_note(now)` / `_fps_value()` 두 메서드로 충분하다(YAGNI).

- [ ] **Step 1: 실패하는 테스트를 쓴다**

계산부만 떼어 검사한다 — GUI 전체를 띄우지 않는다:

```python
def test_fps_from_intervals():
    """도착 간격 목록에서 FPS 를 낸다. 카메라가 없으면 None."""
    from safety_console import fps_from_intervals
    check(fps_from_intervals([]) is None, "표본 없음 → None (카메라 없을 때)")
    check(abs(fps_from_intervals([0.1] * 10) - 10.0) < 0.01, "0.1초 간격 → 10.0fps")
    check(abs(fps_from_intervals([0.1, 0.1, 5.0]) - 10.0) < 0.01,
          "중앙값이라 멈춤 구간 하나에 흔들리지 않는다")
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python3 selftest/test_anim.py`
Expected: FAIL — `ImportError: cannot import name 'fps_from_intervals'`

- [ ] **Step 3: 계산 함수를 `safety_console.py` 모듈 수준에 넣는다**

`class SafetyConsole` **위**, import 들 아래에 둔다:

```python
def fps_from_intervals(intervals):
    """프레임 도착 간격(초) 목록 → 실측 FPS. 표본이 없으면 None.

    🔴 평균이 아니라 **중앙값**이다 — 재연결·정지 구간의 큰 간격 하나가
       평균을 통째로 무너뜨린다(측정이 아니라 사고를 재는 꼴이 된다).
    """
    vals = sorted(v for v in intervals if v > 0)
    if not vals:
        return None
    mid = vals[len(vals) // 2] if len(vals) % 2 else \
        (vals[len(vals) // 2 - 1] + vals[len(vals) // 2]) / 2
    return 1.0 / mid if mid > 0 else None
```

- [ ] **Step 4: `_note_frame` 에서 간격을 모은다**

`_note_frame` 첫머리(기존 `self._last_frame_time` 갱신 근처)에 넣는다:

```python
        now = time.time()
        if self._last_frame_time:
            self._fps_intervals.append(now - self._last_frame_time)
            if len(self._fps_intervals) > 60:        # 최근 60프레임만
                self._fps_intervals.pop(0)
```

`__init__` 에 `self._fps_intervals = []` 를 더한다(`self._last_frame_time = 0.0` 옆).

- [ ] **Step 5: 1초 주기로 로그·표시한다**

`_update_conn_bar` 끝에 붙인다(이 타이머는 이미 1초 주기로 돈다 — 타이머를 새로 만들지 않는다):

```python
        fps = fps_from_intervals(self._fps_intervals)
        if fps is not None:
            self._fps_log_at = getattr(self, "_fps_log_at", 0.0)
            if time.time() - self._fps_log_at >= 10:
                self._fps_log_at = time.time()
                anim_state = "on" if config.UI_ANIMATION else "off"
                self._log(f"[FPS] {fps:.1f} (애니메이션 {anim_state})")
        if config.SHOW_FPS:
            self.fps_label.setText("" if fps is None else f"{fps:.1f} fps")
```

`_init_ui` 에서 `config.SHOW_FPS` 일 때만 라벨을 만든다(꺼져 있으면 위젯 자체가 없다):

```python
        self.fps_label = QLabel("", self._root)
        self.fps_label.setFont(config.font("small", 700))
        self.fps_label.setStyleSheet(theme.text_qss("label", 700))
        self.fps_label.setVisible(config.SHOW_FPS)
```

`_relayout` 에 배치 한 줄을 더한다(연결 상태바 바로 위):

```python
        if config.SHOW_FPS:
            place(self.fps_label, self._root.rect(), right=0.03, bottom=0.10)
```

- [ ] **Step 6: 테스트 + import 관문**

Run: `python3 selftest/test_anim.py && python3 selftest/test_imports.py`
Expected: 둘 다 PASS (🔴 `test_imports.py` 는 GUI 진입점의 import 이름 실재를 대조한다 — `safety_console` 을 고쳤으니 반드시 돌린다)

- [ ] **Step 7: 커밋**

```bash
cd ~/sop-project/Rpi5 && git add Demo/safety_console.py Demo/selftest/test_anim.py
git commit -m "feat(ui): 실측 FPS 계측(중앙값) — 로그 10초 주기 · SOP_SHOW_FPS=1 이면 화면 표시"
```

---

### Task 6: 실기동 관문 · 문서 마무리

**Files:**
- Modify: `docs/superpowers/specs/2026-08-03-uiux-글라스-design.md` (포인터 한 줄)
- Modify: `Rpi5/CLAUDE.md` (애니메이션 함정 한 줄)

- [ ] **Step 1: 🔴 실기동 관문 — 실제로 띄운다**

Run: `cd ~/sop-project/Rpi5/Demo && ./run_demo.sh`
확인할 것: ①GUI 가 기동 즉시 죽지 않는다 ②콘솔에 `QPainter::begin` 경고가 **한 줄도** 없다 ③단계 전환·게이지·경고 배너가 의도대로 움직인다 ④`SOP_UI_ANIM=0 ./run_demo.sh` 로 끄면 애니메이션 없이 정상 동작한다.

🔴 이 관문을 건너뛰지 않는다 — `@pyqtSlot` 시그니처 불일치처럼 **import 검사로 못 잡는 런타임 오류**로 GUI 가 기동 즉시 죽은 적이 있다.

- [ ] **Step 2: 문제가 보이면 지속시간 상수만 조정한다**

`anim.py` 의 `D_*` 값만 고친다(사용자 결정: "완성물 확인 후 값 재수정"). 구조는 건드리지 않는다.

- [ ] **Step 3: 상위 UI design 문서에 포인터를 단다**

`2026-08-03-uiux-글라스-design.md` 머리말에 한 줄 — 사본을 만들지 않는다:

```markdown
> 전환 애니메이션의 정본은 `2026-08-16-ui-애니메이션-design.md` 다(이 문서는 배치·색·패널만 정한다).
```

- [ ] **Step 4: `Rpi5/CLAUDE.md` 에 함정 한 줄을 남긴다**

`safety_console.py` 절에 덧붙인다:

```markdown
- 🆕 **`anim.py`(2026-08-16)** — 오버레이 전환 애니메이션. 🔴 **갱신 함수는 200ms 주기로 반복 호출된다** — 애니메이션은 반드시 「직전 상태와 달라졌을 때만」 건다. 끄기 = `SOP_UI_ANIM=0`. **성능(FPS)은 미판정** — 계측 코드만 있고 측정은 카메라 복귀 후 시나리오 테스트에서.
```

- [ ] **Step 5: 커밋 (repo 가 둘이다)**

```bash
cd ~/sop-project/Rpi5 && git add CLAUDE.md && \
  git commit -m "docs: anim.py 함정 — 200ms 반복 호출 게이트 · 성능 미판정"
cd ~/sop-project && git add docs/superpowers/specs/2026-08-03-uiux-글라스-design.md && \
  git commit -m "docs(설계): 애니메이션 정본 포인터 추가"
```

---

## 관문 요약 (CC작업문서 §2.4)

- **검증 관문** — `selftest/test_anim.py` 3종(게이트 OFF 즉시 적용 · 반복 호출 무재시작 · 맥박 정지)이 통과하지 못하면 그 상태로 진행하지 않는다.
- **실기동 관문** — Task 6 Step 1. `QPainter::begin` 경고가 한 줄이라도 보이면 **즉시 중단**하고 원인을 찾는다(그 경고가 곧 화면이 안 그려지는 사고다).
- **중단 규칙(유예)** — 시나리오 테스트 실측에서 FPS 하락 10% 초과면 `UI_ANIMATION` 기본값을 False 로 돌리고 원인을 재분석한다.
- 🔴 **성능 주장 금지** — 측정 전까지 "FPS 영향 없음"을 어떤 형태로도 쓰지 않는다.
