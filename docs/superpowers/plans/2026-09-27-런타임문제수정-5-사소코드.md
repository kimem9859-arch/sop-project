# 실제 시연 프로그램 문제 수정 ⑤ 종합 리뷰 사소 코드 4건 Implementation Plan

> 상태: 📝 작성(2026-09-27 · 세션 `14c83934`) · 🔑 드라이런 = 계획서의 교체·추가 지시와 Run 명령을 **Rpi5 사본(추적 파일 + `.camera_ip` · 공구 모델·음성 wav 링크 · `git init`)에서 Task 1~6 그대로 실행**해 아래 Expected 를 얻었다 — **11/11 일치**(2026-09-27 · 저장소 미반영)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans(인라인 — 작업갈래 스킬) to implement this plan task-by-task.

**Goal:** 종합 리뷰 사소 코드 12건 중 시연에 보이는 4건 — 작업 전 EMO 때 「작업 시작」 비침(B-M2) · 초기화 뒤 거짓 「비상정지로 작업 중단」(A-M2) · 해제 성공 뒤 거부 창 남음(A-M8) · 음성 상태 파일 어긋남(A-M3) — 을 고치고, ⑤를 최종 리뷰·공동 확인·마무리한다.

**Architecture:** 전부 `safety_console.py` 한 파일. 「작업 시작」 표시는 모든 전이에서 상태로 계산하고, 작업 초기화는 집계까지 비우며, 차단을 벗어나면 EMO 미복귀 거부 창을 닫는다. 음성 상태 파일은 「완료 결과가 없고, 판정기 IDLE 이거나 작업 중이 아니면 비운다」로 바꾸고 완료 결과를 콘솔이 다음 시작·초기화까지 들고 있는다.

**Tech Stack:** Python 3.13 · PyQt6(오프스크린) · 실제 `SafetyConsole`(GPIO·인터락 끔 — `make_console`) · 자가 테스트 일괄 러너 · 리뷰 재현 도구 `~/lab/runtime-review-harness/`

**Spec:** `docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md` §10(10.1~10.7) — 목록·원문 = `Rpi5/조사/종합리뷰-20260926/요약.md` · `리뷰_A_교차맞물림.md`(A-M#) · `리뷰_B_실제조건실행.md`(B-M#)

## Global Constraints

- 코드 저장소 = `/home/pi/sop-project/Rpi5`(별도 git) · 문서 = `/home/pi/sop-project`. 커밋은 **경로 지정 add** 만.
- 커밋 서명 = `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>` + `Claude-Session: https://claude.ai/code/session_01EjsfDNyCnnb2QidihGHfKW` (실행 세션이 다르면 그 세션 값).
- 자가 테스트 = **일괄 러너** `python3 selftest/run_all.py`(`Rpi5/Demo` 에서) — 마지막 줄 `SELFTEST N pass / N fail / N skip (T s)` · 종료 코드 0 = 실패 없음. 🔴 종료 코드는 `rc=$?` 로 파이프 없이 받는다.
- 🔴 **Hailo·GUI 를 쓰는 다른 실행(데모·측정·다른 세션의 시험)과 동시에 돌리지 않는다.**
- 🔴 **고치지 않는 것** — `fsm.py` · `voice_card.py` · `voice_assistant.py` · `voice_lib.py` · `interlock.py` · `gpio_input.py` · `session_stats.py`(Task 1 의 홀드아웃) · 나머지 사소 8건(A-M1·A-M5·A-M6·A-M7·B-M1·B-M3·B-M4·B-M5 — spec §10.1).
- 🔴 **인터락 폴트 창(`interlock_fault`)은 닫지 않는다**(spec §10.3).
- 🔴 **push 는 Task 8 의 `work-close` 에서 한 번.**

## 관문

- **🔒 홀드아웃** — 위 「고치지 않는 것」 7개 파일의 sha256 을 Task 1 에서 적고, Task 6 에서 **한 글자도 바뀌지 않았음**(`sha256sum -c`)을 확인한다. 합격선 = 태스크마다 적힌 Expected(사본 드라이런으로 **구현 전에 고정**).
- **검증 관문** — ①Task 1: 러너 기준선 `33 pass / 0 fail` ②Task 2~5: 새 시험이 **고치기 전 실패 → 고친 뒤 통과** · 러너 종료 코드 0 ③Task 6: 러너 `33 pass / 0 fail` · 홀드아웃 같음 · 재현 도구 `emo_idle_cta`(「작업 시작」 안 보임)·`emo_states` 종료 0 · 문제 0.
- **중단 규칙** — ①확인 단계가 Expected 와 다르면 다음 태스크로 넘어가지 않는다 ②이번 변경과 무관한 기존 시험이 깨지면 멈추고 보고한다 ③같은 수정을 3번 시도해도 안 되면 멈추고 보고한다 ④콘솔 시험이 GPIO·인터락을 켠 채 만들어지면(실제 릴레이에 명령) 즉시 멈춘다 ⑤재현 도구가 실제 장치(시리얼·GPIO·Hailo)를 열려 하면(`실제 장치를 열려고 했다` 예외) 즉시 멈춘다.

## Review Focus

1. **결과창이 뜬 채 EMO 를 누르고 풀 때의 「작업 시작」** — 결과창 뒤로 비치지 않아야 한다(결과창도 시트) → `test_review_bm2_cta_hidden_while_emo_before_start`(Task 2) 뒷부분.
2. **작업 중 EMO 를 풀 때의 음성 상태 파일** — 지워져야 한다(판정기 전이가 집계 비우기보다 먼저라 `running` 만 보면 「진행 중」이 남는다 — IDLE 조건이 막는 경우) → `test_review_am3_voice_state_file`(Task 5) ④.
3. **완주 뒤 「작업 초기화」** — 결과도 버리고 파일이 지워져야 한다 → 같은 시험 ③.
4. **거부 창이 닫힌 뒤 다시 거부될 때** — 같은 창이 다시 뜬다(숨김 → 표시 · U20 재사용) → `test_review_am8_refusal_popups_close_when_block_ends`(Task 4) ①의 끝.
5. **「작업 시작」을 누르는 순간의 음성 상태** — 레시피 적재 중 전이가 파일을 잠깐 비워도 끝은 「진행 중 · 결과 없음」이어야 한다 → Task 5 시험 ②의 끝 · 기존 `test_v2_emo_published_to_voice`(작업 중 EMO = 비상정지 공개).

## 계획서 표기

④ 계획서와 같다 — `<!-- rep: <경로> -->`(원문 → 새 내용 · 정확히 한 곳) · `<!-- add: <경로> -->`(`if __name__ == "__main__":` 바로 앞) · 러너 마지막 줄 끝 ` (T s)` 는 비교하지 않는다 · 드라이런 = `python3 ~/lab/plan-dryrun/plan_dryrun.py <계획서> <사본 루트> [태스크,…]` · 사본 = `git archive` + `git init`(Task 6 재현 도구가 `git ls-files` 를 쓴다).

---

### Task 1: 기준선 — 러너 · 홀드아웃

**Files:** Create `Rpi5/조사/사소코드-20260927/before_selftest.txt` · `holdout.sha256`

- [ ] **Step 1: 기준선을 적는다**

Run:
```bash
cd /home/pi/sop-project/Rpi5/Demo && D=../조사/사소코드-20260927 && mkdir -p $D && \
python3 selftest/run_all.py > $D/before_selftest.txt 2>&1; echo rc=$?; tail -1 $D/before_selftest.txt; \
sha256sum fsm.py voice_card.py voice_assistant.py voice_lib.py interlock.py gpio_input.py session_stats.py > $D/holdout.sha256; wc -l < $D/holdout.sha256
```
Expected:
```
rc=0
SELFTEST 33 pass / 0 fail / 0 skip
7
```
- `rc` ≠ 0 이면 멈춘다(기준선이 깨진 채 시작하지 않는다).

- [ ] **Step 2: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add 조사/사소코드-20260927/before_selftest.txt 조사/사소코드-20260927/holdout.sha256 && git commit -m "docs(조사): 종합 리뷰 사소 코드 ⑤ 기준선 — 자가 테스트 일괄 러너 33 pass · 홀드아웃 파일 7개 sha256

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01EjsfDNyCnnb2QidihGHfKW"
```

---

### Task 2: B-M2 — 「작업 시작」은 모든 전이에서 상태로 계산한다

**Files:** Modify `Rpi5/Demo/safety_console.py`(`_on_fsm_state` 끝) · Test `Rpi5/Demo/selftest/test_console_flow.py`

**Interfaces:**
- Produces: `_on_fsm_state` 가 상태와 무관하게 `_sync_cta_visibility()` → `_relayout()` 순으로 부른다. IDLE 분기의 주석은 「IDLE 로 돌아오면 서브 작업을 정리하고 「작업 시작」 문구로 되돌린다」 — 🔴 Task 4 가 이 주석 줄을 기준으로 삽입한다.

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def test_review_bm2_cta_hidden_while_emo_before_start():
    """종합 리뷰 B-M2 — 작업 전 EMO 로 차단되면 「작업 시작」을 감춘다(차단 배너 판 뒤로 비치던 것) · 해제하면 돌아온다.
    결과창이 뜬 채 EMO 를 누르고 풀어도 결과창 뒤로 비치지 않는다."""
    print("\n[종합 B-M2] 작업 전 EMO 와 「작업 시작」")
    win = make_console()
    check(not win.btn_cta.isHidden(), "작업 전 — 「작업 시작」 보임")
    key(win, "E")
    check(win.fsm.state == State.BLOCK, f"EMO 차단({win.fsm.state.value})")
    check(win.btn_cta.isHidden(), "차단 중에는 「작업 시작」을 감춘다")
    win.gpio_input.emo_active = lambda: False        # EMO 복귀
    win._release_block()
    check(win.fsm.state == State.IDLE, f"해제 → 대기({win.fsm.state.value})")
    check(not win.btn_cta.isHidden(), "해제하면 「작업 시작」이 돌아온다")
    win.close()
    win = make_console()
    win._on_cta()
    for k in ("1", "2", "3", "4"):
        key(win, k)
        finish_sub(win)
    check(not win.result_panel.isHidden(), "완주 — 결과창")
    key(win, "E")
    win.gpio_input.emo_active = lambda: False
    win._release_block()
    check(win.btn_cta.isHidden(), "결과창이 뜬 채 EMO 를 누르고 풀어도 「작업 시작」이 결과창 뒤로 비치지 않는다")
    win.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 차단 중에는 「작업 시작」을 감춘다
❌ 실패 1건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        # IDLE 로 돌아오면 다시 「작업 시작」을 띄운다
        if new == State.IDLE:
            self._sub_timer.stop()
            self._end_tool_scan()
            self._sub = None
            self._sub_button = None
            self.gauge_panel.update_view(None)
            self.btn_cta.setText("▶  작업 시작")
            # 🔴 show() 를 직접 부르지 않는다 — 결과창이 떠 있으면 그 뒤로 비친다.
            #    "기억하지 말고 상태에서 그때그때 계산한다"(_sync_cta_visibility).
            self._sync_cta_visibility()

        self._relayout()
```
```python
        # IDLE 로 돌아오면 서브 작업을 정리하고 「작업 시작」 문구로 되돌린다
        if new == State.IDLE:
            self._sub_timer.stop()
            self._end_tool_scan()
            self._sub = None
            self._sub_button = None
            self.gauge_panel.update_view(None)
            self.btn_cta.setText("▶  작업 시작")

        # 「작업 시작」 버튼은 **모든 전이에서** 상태로 다시 계산한다 — IDLE 로 올 때만 부르면 작업 전
        # EMO(IDLE → BLOCK)에 버튼이 남아 차단 배너 판 뒤로 비쳤다(종합 리뷰 B-M2).
        # 🔴 show() 를 직접 부르지 않는다 — 결과창이 떠 있으면 그 뒤로 비친다.
        #    "기억하지 말고 상태에서 그때그때 계산한다"(_sync_cta_visibility).
        self._sync_cta_visibility()
        self._relayout()
```

- [ ] **Step 4: 통과 확인 · 러너**

Run:
```bash
cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | tail -1; \
o=$(mktemp); python3 selftest/run_all.py > $o 2>&1; echo rc=$?; tail -1 $o; rm -f $o
```
Expected:
```
✅ 글라스 UI 흐름 통과
rc=0
SELFTEST 33 pass / 0 fail / 0 skip
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/safety_console.py Demo/selftest/test_console_flow.py && git commit -m "fix(gui): 「작업 시작」 버튼을 모든 상태 전이에서 다시 계산한다 — 작업 전 EMO 로 차단돼도 버튼이 남아 차단 배너 판 뒤로 비치던 것 (종합 리뷰 사소 B-M2)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01EjsfDNyCnnb2QidihGHfKW"
```

---

### Task 3: A-M2 — 작업 초기화는 집계도 비운다

**Files:** Modify `Rpi5/Demo/safety_console.py`(`_reset_work` · `_publish_state` 머리 주석) · Test `Rpi5/Demo/selftest/test_console_flow.py`

**Interfaces:**
- Produces: `_reset_work` 는 `self._stats.reset()` 을 `self.fsm.reset()` **바로 앞**에서 부른다 — 🔴 Task 5 가 이 두 줄을 기준으로 삽입한다. `_publish_state` 머리 주석은 두 줄(「🔴 「작업 시작 전」의 정본은 … 종합 리뷰 A-M2).」) — 🔴 Task 5 가 이 두 줄과 바로 뒤 `if` 를 바꾼다.

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def test_review_am2_reset_then_emo_release_no_false_stop_notice():
    """종합 리뷰 A-M2 — 작업 초기화 뒤 EMO → 해제해도 「비상정지로 작업 중단」이 뜨지 않는다(진행 중인 작업이 없다).
    초기화가 판정기만 되돌리고 집계는 두어 running 이 켜진 채 남던 것."""
    print("\n[종합 A-M2] 초기화 뒤 EMO 해제 알림")
    win = make_console()
    win._on_cta()
    key(win, "1")
    finish_sub(win)                                  # 1단계 완료 — 진행 중
    win._reset_work()
    check(not win._stats.running, "초기화하면 집계가 꺼진다")
    key(win, "E")
    win.gpio_input.emo_active = lambda: False        # EMO 복귀
    win._release_block()
    check(win.fsm.state == State.IDLE, f"해제 → 대기({win.fsm.state.value})")
    check(not any("비상정지로 작업 중단" in t for t in notify_titles(win)),
          "진행 중인 작업이 없으니 「비상정지로 작업 중단」 알림이 없다")
    win.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 초기화하면 집계가 꺼진다
  ❌ 진행 중인 작업이 없으니 「비상정지로 작업 중단」 알림이 없다
❌ 실패 2건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        self._append_log("[FSM] 작업 초기화 — 「작업 시작」 전 상태로 되돌립니다")
        self.fsm.reset()
```
```python
        self._append_log("[FSM] 작업 초기화 — 「작업 시작」 전 상태로 되돌립니다")
        # 🔴 집계도 비운다 — 판정기만 되돌리면 `running` 이 켜진 채 남아, 초기화 뒤 EMO 를 풀 때
        #    진행 중인 작업이 없는데 「비상정지로 작업 중단」이 떴다(종합 리뷰 A-M2). 완주 때
        #    `finish()` 가 running 을 끄는 것과 같다.
        self._stats.reset()
        self.fsm.reset()
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
            # 🔴 「작업 시작 전」의 정본은 **FSM 이 IDLE 인가**다. `_stats.running` 을
            #    쓰면 안 된다 — 「작업 초기화」는 `fsm.reset()` 만 부르고 `_stats` 는
            #    그대로 두므로, 초기화 뒤에도 running 이 True 로 남아 세션이 살아 있는
            #    것처럼 보인다. `fsm.reset()` 의 정의가 곧 「작업 시작 직전」이다.
```
```python
            # 🔴 「작업 시작 전」의 정본은 **FSM 이 IDLE 인가**다. `fsm.reset()` 의 정의가 곧
            #    「작업 시작 직전」이다(「작업 초기화」는 집계도 비운다 — 종합 리뷰 A-M2).
```

- [ ] **Step 4: 통과 확인 · 러너**

Run:
```bash
cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | tail -1; \
o=$(mktemp); python3 selftest/run_all.py > $o 2>&1; echo rc=$?; tail -1 $o; rm -f $o
```
Expected:
```
✅ 글라스 UI 흐름 통과
rc=0
SELFTEST 33 pass / 0 fail / 0 skip
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/safety_console.py Demo/selftest/test_console_flow.py && git commit -m "fix(gui): 작업 초기화가 집계도 비운다 — 초기화 뒤 EMO 를 풀면 진행 중인 작업이 없는데 「비상정지로 작업 중단」이 뜨던 것 (종합 리뷰 사소 A-M2)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01EjsfDNyCnnb2QidihGHfKW"
```

---

### Task 4: A-M8 — 차단이 풀리면 EMO 미복귀 거부 창을 닫는다

**Files:** Modify `Rpi5/Demo/safety_console.py`(`_on_fsm_state`) · Test `Rpi5/Demo/selftest/test_console_flow.py`

**Interfaces:**
- Consumes: Task 2 의 IDLE 분기 주석 줄 「# IDLE 로 돌아오면 서브 작업을 정리하고 「작업 시작」 문구로 되돌린다」 · `self._popups`(U20 — 종류 → `QMessageBox`, `_popup(key, …)` 이 `show()` 로 다시 띄운다).
- Produces: `old == State.BLOCK` 인 전이마다 `_popups` 의 `release_refused`·`reset_refused` 를 `hide()`. `interlock_fault` 는 건드리지 않는다.

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def test_review_am8_refusal_popups_close_when_block_ends():
    """종합 리뷰 A-M8 — 차단이 풀리면(해제·초기화 성공) 「해제 거부」·「초기화 거부」 창을 닫는다 — 「EMO가 아직 복귀되지
    않았습니다」가 남아 옛 말을 하던 것. 인터락 폴트 창은 닫지 않는다(설계 §10.3). 닫힌 창은 다시 거부되면 다시 뜬다."""
    print("\n[종합 A-M8] 거부 창 닫기")
    import safety_console
    from PyQt6.QtWidgets import QMessageBox

    def shown(w):
        return sorted(b.windowTitle() for b in w.findChildren(QMessageBox) if not b.isHidden())

    win = make_console()                             # ① 차단 해제로 풀림
    win._on_cta()
    key(win, "E")
    win.gpio_input.emo_active = lambda: True         # EMO 를 누른 채
    win._release_block()
    win._on_reset_clicked()
    win._on_interlock_fault("시험 폴트")
    check(shown(win) == ["인터락 폴트", "초기화 거부", "해제 거부"], f"거부 창 = {shown(win)}")
    win.gpio_input.emo_active = lambda: False        # EMO 복귀
    win._release_block()
    check(win.fsm.state == State.IDLE, f"해제 → 대기({win.fsm.state.value})")
    check(shown(win) == ["인터락 폴트"], f"해제 뒤 남은 창 = {shown(win)}")
    key(win, "E")
    win.gpio_input.emo_active = lambda: True
    win._release_block()
    check("해제 거부" in shown(win), "다시 거부되면 「해제 거부」 창이 다시 뜬다")
    win.close()

    win = make_console()                             # ② 작업 초기화로 풀림
    win._on_cta()
    key(win, "E")
    win.gpio_input.emo_active = lambda: True
    win._on_reset_clicked()
    win.gpio_input.emo_active = lambda: False
    old = safety_console.QMessageBox.question
    safety_console.QMessageBox.question = lambda *a, **k: QMessageBox.StandardButton.Yes
    try:
        win._on_reset_clicked()
    finally:
        safety_console.QMessageBox.question = old
    check(win.fsm.state == State.IDLE, f"초기화 → 대기({win.fsm.state.value})")
    check(shown(win) == [], f"초기화 뒤 남은 창 = {shown(win)}")
    win.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 해제 뒤 남은 창 = ['인터락 폴트', '초기화 거부', '해제 거부']
  ❌ 초기화 뒤 남은 창 = ['초기화 거부']
❌ 실패 2건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        # IDLE 로 돌아오면 서브 작업을 정리하고 「작업 시작」 문구로 되돌린다
        if new == State.IDLE:
```
```python
        # 차단이 풀리면(해제·초기화 성공) 「EMO 미복귀」 거부 창은 옛 말이 된다 — 닫는다(종합 리뷰 A-M8).
        # 🔴 인터락 폴트 창은 닫지 않는다 — 「차단 미확인」은 지난 사실이고 릴레이·배선·Arduino 점검
        #    안내는 여전히 유효하다. 안전 경보는 사람이 보고 OK 로 닫는다(설계 §10.3).
        if old == State.BLOCK:
            for k in ("release_refused", "reset_refused"):
                box = self._popups.get(k)
                if box is not None:
                    box.hide()

        # IDLE 로 돌아오면 서브 작업을 정리하고 「작업 시작」 문구로 되돌린다
        if new == State.IDLE:
```

- [ ] **Step 4: 통과 확인 · 러너**

Run:
```bash
cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | tail -1; \
o=$(mktemp); python3 selftest/run_all.py > $o 2>&1; echo rc=$?; tail -1 $o; rm -f $o
```
Expected:
```
✅ 글라스 UI 흐름 통과
rc=0
SELFTEST 33 pass / 0 fail / 0 skip
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/safety_console.py Demo/selftest/test_console_flow.py && git commit -m "fix(gui): 차단이 풀리면 「해제 거부」·「초기화 거부」 창을 닫는다 — 해제에 성공해도 「EMO가 아직 복귀되지 않았습니다」가 남던 것 · 인터락 폴트 창은 그대로 (종합 리뷰 사소 A-M8)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01EjsfDNyCnnb2QidihGHfKW"
```

---

### Task 5: A-M3 — 음성 상태 파일: 작업 전 EMO 는 비우고, 완료 결과는 다음 시작까지

**Files:** Modify `Rpi5/Demo/safety_console.py`(`__init__` · `_on_start_process` · `_show_result` · `_publish_state` · `_reset_work`) · Test `Rpi5/Demo/selftest/test_console_flow.py`

**Interfaces:**
- Consumes: Task 3 의 `_reset_work` 두 줄(`self._stats.reset()` → `self.fsm.reset()`) · `_publish_state` 머리 주석 두 줄 · `SessionStats.running`(「작업 시작」부터 완주 `finish()`·초기화·EMO 해제까지 True — Task 3 뒤).
- Produces: `SafetyConsole._last_result`(완료 결과 dict 또는 None) · `_publish_state()` — **인자 없음**(종전 `result=None` 인자 제거 · 부르는 곳은 `_show_result` 하나였다).

- [ ] **Step 1: 실패하는 테스트**

<!-- add: Rpi5/Demo/selftest/test_console_flow.py -->
```python
def test_review_am3_voice_state_file():
    """종합 리뷰 A-M3 — 음성 상태 파일: 작업 전 EMO 는 「진행 중」으로 공개하지 않는다(파일을 비운다) · 완료 결과는
    EMO·해제 뒤에도 다음 「작업 시작」 전까지 남는다 — 「1단계 진행 중 · 비상정지」로 덮이고 해제하면 지워지던 것."""
    print("\n[종합 A-M3] 음성 상태 파일")

    def watch(w):
        ev = []
        w._state_pub.publish = lambda d: ev.append(d)
        w._state_pub.clear = lambda: ev.append(None)
        return lambda: ev[-1] if ev else None        # 지금 파일 내용(None = 없음)

    win = make_console()                             # ① 작업 전 EMO
    now = watch(win)
    key(win, "E")
    check(win.fsm.state == State.BLOCK, f"작업 전 EMO 차단({win.fsm.state.value})")
    check(now() is None, "작업 전 EMO 는 공개하지 않는다(파일 없음 = 「작업 시작 전」)")
    win.close()

    win = make_console()                             # ② 완주 → EMO → 해제 → 다음 회차
    now = watch(win)
    win._on_cta()
    for k in ("1", "2", "3", "4"):
        key(win, k)
        finish_sub(win)
    check(bool(now() and now().get("결과")), "완주 — 결과 공개")
    key(win, "E")
    check(bool(now() and now().get("결과")), "완료 뒤 EMO — 결과가 남는다")
    check(bool(now()) and now().get("비상정지") is True, "완료 뒤 EMO — 비상정지를 싣는다")
    win.gpio_input.emo_active = lambda: False        # EMO 복귀
    win._release_block()
    check(bool(now() and now().get("결과")), "완료 뒤 EMO 해제 — 결과가 남는다")
    win._on_cta()
    check(bool(now()) and now().get("결과") is None and now().get("세션") is True,
          "다음 「작업 시작」 — 결과를 버리고 진행 중으로 공개")
    win.close()

    win = make_console()                             # ③ 완주 → 작업 초기화
    now = watch(win)
    win._on_cta()
    for k in ("1", "2", "3", "4"):
        key(win, k)
        finish_sub(win)
    win._reset_work()
    check(now() is None, "완주 뒤 작업 초기화 — 결과도 버리고 파일을 비운다")
    win.close()

    win = make_console()                             # ④ 작업 중 EMO → 해제
    now = watch(win)
    win._on_cta()
    key(win, "1")
    finish_sub(win)
    key(win, "E")
    check(bool(now()) and now().get("비상정지") is True, "작업 중 EMO — 진행 중 · 비상정지 공개")
    win.gpio_input.emo_active = lambda: False
    win._release_block()
    check(now() is None, "작업 중 EMO 해제 — 파일을 비운다(작업이 끝났다)")
    win.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | grep -E "^  ❌|^❌"`
Expected:
```
  ❌ 작업 전 EMO 는 공개하지 않는다(파일 없음 = 「작업 시작 전」)
  ❌ 완료 뒤 EMO — 결과가 남는다
  ❌ 완료 뒤 EMO 해제 — 결과가 남는다
❌ 실패 3건
```

- [ ] **Step 3: 구현**

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        self._popups = {}            # 종류 → 알림 창 — 종류마다 하나만(U20 · _popup)
```
```python
        self._popups = {}            # 종류 → 알림 창 — 종류마다 하나만(U20 · _popup)
        self._last_result = None     # 완료 결과 — 다음 「작업 시작」·「작업 초기화」까지 음성 상태에 싣는다(A-M3)
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
    def _on_start_process(self):
        self.fsm.load_recipe()
```
```python
    def _on_start_process(self):
        self._last_result = None     # 앞 회차 완료 결과는 여기서 버린다(A-M3)
        self.fsm.load_recipe()
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        data = self._stats.finish()
        self._publish_state(result=data)
```
```python
        data = self._stats.finish()
        self._last_result = data
        self._publish_state()
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
    def _publish_state(self, result=None):
```
```python
    def _publish_state(self):
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
            # 🔴 「작업 시작 전」의 정본은 **FSM 이 IDLE 인가**다. `fsm.reset()` 의 정의가 곧
            #    「작업 시작 직전」이다(「작업 초기화」는 집계도 비운다 — 종합 리뷰 A-M2).
            if self.fsm.state == State.IDLE and result is None:
                self._state_pub.clear()
                return
```
```python
            # 🔴 「작업 전」 = 완료 결과가 없고, 판정기가 IDLE 이거나 작업 중이 아니다(`_stats.running`
            #    꺼짐). 판정기 IDLE 만 보면 작업 전 EMO(IDLE → BLOCK)가 「진행 중 · 1단계」로 공개됐다
            #    (종합 리뷰 A-M3). running 만 보면 EMO 해제 순간을 놓친다 — 판정기 전이(→ 이 공개)가
            #    집계 비우기보다 먼저라 그때 running 이 아직 켜져 있다. running 은 「작업 시작」부터
            #    완주·초기화·EMO 해제까지다.
            # 🔑 완료 결과는 다음 「작업 시작」·「작업 초기화」까지 싣는다 — 완료 뒤 EMO·해제에도
            #    「작업 결과 어때?」에 답해야 한다.
            result = self._last_result
            if result is None and (self.fsm.state == State.IDLE or not self._stats.running):
                self._state_pub.clear()
                return
```

<!-- rep: Rpi5/Demo/safety_console.py -->
```python
        self._stats.reset()
        self.fsm.reset()
```
```python
        self._stats.reset()
        self._last_result = None     # 완료 결과도 버린다 — 「작업 시작」 전 상태(A-M3)
        self.fsm.reset()
```

- [ ] **Step 4: 통과 확인 · 러너 · 남은 `result=` 호출 없음**

Run:
```bash
cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py 2>&1 | tail -1; \
grep -c "_publish_state(result" safety_console.py; \
o=$(mktemp); python3 selftest/run_all.py > $o 2>&1; echo rc=$?; tail -1 $o; rm -f $o
```
Expected:
```
✅ 글라스 UI 흐름 통과
0
rc=0
SELFTEST 33 pass / 0 fail / 0 skip
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/safety_console.py Demo/selftest/test_console_flow.py && git commit -m "fix(gui·voice): 음성 상태 파일 — 작업 전 EMO 는 「진행 중 · 1단계」로 공개하지 않고, 완료 결과는 EMO·해제 뒤에도 다음 「작업 시작」·「작업 초기화」까지 싣는다 (종합 리뷰 사소 A-M3)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01EjsfDNyCnnb2QidihGHfKW"
```

---

### Task 6: 검증 관문 — 러너 · 홀드아웃 · 실제 조건 재현

**Files:** Create `Rpi5/조사/사소코드-20260927/after_selftest.txt` · `after_harness.txt`

- [ ] **Step 1: 러너 · 홀드아웃**

Run:
```bash
cd /home/pi/sop-project/Rpi5/Demo && D=../조사/사소코드-20260927 && \
python3 selftest/run_all.py > $D/after_selftest.txt 2>&1; echo rc=$?; tail -1 $D/after_selftest.txt; \
sha256sum -c --quiet $D/holdout.sha256 && echo holdout_same
```
Expected:
```
rc=0
SELFTEST 33 pass / 0 fail / 0 skip
holdout_same
```
- `holdout_same` 이 없으면 멈춘다(고치지 않기로 한 파일이 바뀌었다).

- [ ] **Step 2: 실제 조건 재현(리뷰 재현 도구 · 추적 파일 사본)**

🔴 재현 도구는 git 밖의 검토하지 않은 코드다 — 작성 세션(`14c83934`)이 읽고 확인한 것: GPIO·인터락·녹화를 끄고 검출기·손·공구 게이트를 가짜로 바꾼다 · 인터락은 `H_ITL=1` 일 때만 가짜 시리얼이며 공유 폴더 밖 경로를 열려 하면 예외 · 상태 파일은 `H_SHM` 아래. 이 두 시나리오는 `H_ITL`·실제 게이트·ffmpeg 를 쓰지 않는다.

Run:
```bash
H=$(mktemp -d) && cd /home/pi/sop-project/Rpi5 && git ls-files -z | tar --null -T - -cf - | tar -x -C $H && cd ~/lab/runtime-review-harness && \
for s in emo_idle_cta emo_states; do H_SHM=$H/shm timeout 300 python3 h.py $H/Demo $s > $H/$s.txt 2>&1; echo "$s rc=$?"; grep -o "CTA 보임=[A-Za-z]*\|RESULT .*" $H/$s.txt; done | tee /home/pi/sop-project/Rpi5/조사/사소코드-20260927/after_harness.txt; rm -rf $H
```
Expected:
```
emo_idle_cta rc=0
CTA 보임=False
RESULT scen=emo_idle_cta problems=0 rc=0
emo_states rc=0
RESULT scen=emo_states problems=0 rc=0
```
- 수정 전(작성 세션 드라이런 전 사본)은 `CTA 보임=True` 였다.

- [ ] **Step 3: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add 조사/사소코드-20260927/after_selftest.txt 조사/사소코드-20260927/after_harness.txt && git commit -m "docs(조사): 종합 리뷰 사소 코드 ⑤ 결과 — 러너 33 pass · 홀드아웃 7개 그대로 · 재현 도구 emo_idle_cta(작업 시작 안 보임)·emo_states 문제 0

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01EjsfDNyCnnb2QidihGHfKW"
```

---

### Task 7: 정본·문서 반영

🔴 통합문서를 열면 `.claude/rules/정본편집.md` 가 붙는다 — 그 규칙을 따른다(현행만 본문 · 폐기는 `docs/변경이력.md` · 미구현은 장 끝 ⏸).

**Files:** Modify `docs/통합문서.md`(§8.2 창 줄 · §9.4 음성 줄 · 🔓 행) · `Rpi5/함수목록.md` · `Rpi5/조사/README.md` · spec·이 계획서 머리

- [ ] **Step 1: 통합문서**
  - §8.2 「해제 거부 · 초기화 거부 · 인터락 폴트 창은 종류마다 하나」 줄 끝에: 「차단이 풀리면(해제·초기화) 해제 거부·초기화 거부 창은 저절로 닫힌다 — 인터락 폴트 창은 사람이 OK 로 닫는다(「차단 미확인」은 지난 사실이고 점검 안내는 유효하다).」
  - §9.4 「EMO 로 멈추면 「비상정지」라고 답한다」 줄 뒤에 한 줄: 「**작업 시작 전에 누른 EMO 는 「작업 시작 전」으로 답한다**(진행 중인 작업이 없다) · **완료 결과는 다음 「작업 시작」·「작업 초기화」 전까지 답한다**(완료 뒤 EMO 를 눌렀다 풀어도) · ⏸ 실재생 확인 전.」
  - 🔓 「실제 시연 프로그램 문제 수정」 행 — 제목에 「⑤ 종합 리뷰 사소 코드 4건 구현(공동 확인 대기)」을 더하고 본문 끝에 한 줄: 「⑤(2026-09-27) — 종합 리뷰 사소 코드 12 중 시연에 보이는 4건: 작업 전 EMO 때 「작업 시작」이 차단 배너 뒤로 비치지 않음(B-M2) · 초기화 뒤 EMO 해제에 거짓 「비상정지로 작업 중단」 없음(A-M2) · 차단이 풀리면 거부 창 닫힘 · 인터락 폴트 창은 그대로(A-M8) · 음성 상태 파일 — 작업 전 EMO 는 「작업 시작 전」 · 완료 결과는 다음 시작까지(A-M3) · 나머지 8건 목록 유지 · 계획서 `plans/2026-09-27-런타임문제수정-5-사소코드.md` · ⏸ 실HW(실기동 화면 · 서브 ESP32 음성).」
- [ ] **Step 2: 함수목록** — 설명만 고친다(새 함수 없음 · 함수 수 그대로):
  - `SafetyConsole._reset_work` → 「작업 상태·집계·완료 결과·서브 작업·경고를 모두 지워 시작 전으로 돌린다. (⑤ A-M2·A-M3)」
  - `SafetyConsole._publish_state` → 「음성비서가 읽을 현재 작업 상태를 파일로 내보낸다 — 완료 결과가 없고 대기이거나 작업 중이 아니면 지운다 · 완료 결과는 다음 시작·초기화까지 싣는다. (⑤ A-M3)」
  - `SafetyConsole._on_fsm_state` → 「FSM 상태가 바뀌면 상태판·경고·차단 표시와 집계를 맞춘다 — 「작업 시작」 표시를 다시 계산하고, 차단이 풀리면 거부 창을 닫는다. (⑤ B-M2·A-M8)」
  - `SafetyConsole._show_result` → 「작업이 끝나면 결과를 집계해 결과창을 띄우고, 결과를 다음 시작까지 음성 상태에 남긴다.」
  - `SafetyConsole._on_start_process` → 「작업을 시작해 FSM·결과 집계를 첫 단계로 맞추고 앞 회차 결과를 버린다.」
- [ ] **Step 3: 조사 README** — 표에 행: `사소코드-20260927/` — 종합 리뷰 사소 코드 ⑤ 기준선·결과(러너 · 홀드아웃 sha256 · 재현 도구 두 장면) · 쓰는 곳 = 설계서 §10 · 이 계획서.
- [ ] **Step 4: spec·계획서 머리** — spec 상태 줄 「🔄 ⑤ 종합 리뷰 사소 코드 4건 설계(2026-09-27 · §10)」 → 「🔄 ⑤ 종합 리뷰 사소 코드 4건 구현·관문 통과(2026-09-27 · §10 · 공동 확인 대기)」 · 이 계획서 상태 줄 → 「구현·관문 통과 — Rpi5 `<Task 1 커밋>`..`<Task 7 커밋>` · Expected <일치 수>/<전체>」.
- [ ] **Step 5: 커밋** (두 저장소)

```bash
cd /home/pi/sop-project/Rpi5 && git add 함수목록.md 조사/README.md && git commit -m "docs: 종합 리뷰 사소 코드 ⑤ 반영 — 함수목록 바뀐 함수 설명 5개(작업 시작 표시·거부 창·초기화 집계·음성 상태 결과) · 조사 README 사소코드-20260927

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01EjsfDNyCnnb2QidihGHfKW"
cd /home/pi/sop-project && git add docs/통합문서.md docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md docs/superpowers/plans/2026-09-27-런타임문제수정-5-사소코드.md && git commit -m "docs(정본): 종합 리뷰 사소 코드 ⑤ — §8.2 차단이 풀리면 거부 창 닫힘(인터락 폴트 창은 OK 로) · §9.4 작업 전 EMO = 「작업 시작 전」·완료 결과는 다음 시작까지 · 🔓 행 ⑤ 구현

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01EjsfDNyCnnb2QidihGHfKW"
```

---

### Task 8: 최종 리뷰 · 공동 확인 · 마무리

- [ ] **Step 1: 최종 리뷰** — `superpowers:requesting-code-review` · 새 맥락 리뷰어 1회(가장 강한 모델) · 범위 = Rpi5 `5ddedc3..HEAD` + 문서 `9c4c13e~1..HEAD`. 리뷰 기준에 넣을 것: spec §10 · 이 계획서의 Review Focus 원문 · 리뷰 원문의 해당 항목(A-M2·A-M3·A-M8 = `리뷰_A_교차맞물림.md` · B-M2 = `리뷰_B_실제조건실행.md`) · **사용자 구상 원문**(보존본 `~/lab/session-archive/` 의 세션 `14c83934` — 「종합 리뷰 사소 코드 12건(작은 코드 문제, 목록만 있음)에 대해서 확인해줘」 · 「수정 절차에 자가테스트일괄러너는 필요없는거야?」 · 4건 진행 「진핸」 · 설계 승인 「승인」 · 계획 진행 「진행」).
- [ ] **Step 2: 반영** — Critical·Important 는 한 번에 고친다(각각 실패 시험 먼저 → 통과 → 러너 종료 코드 0) · Minor 는 보고에 「미룬 사소」로.
- [ ] **Step 3: 공동 확인** — 사용자에게 고정 형식으로 보고(무엇이 달라졌나 · 판단 기록 · 미룬 사소 · ⏸ 실HW)하고 확인받는다. 🔴 Claude 단독으로 「완료」를 선언하지 않는다.
- [ ] **Step 4: `work-close`** — 공동 확인 뒤. push 는 여기서 한 번(두 저장소).
