# 실제 시연 프로그램 문제 수정 ① 판정 Implementation Plan

> 상태: ✅ 완료(2026-09-25 · 세션 `56d1f2c1` · 공동 확인) · Task 0~6 · 최종 리뷰 반영(Important 4 + 재등급 4) · 미룬 사소 3건 = ③ R5 계획서 · 결과 = 저널 §12.74

> ⚠️ **정정(2026-09-25)** — 자가 테스트 기록 명령이 `$(basename)` 뒤에 `$?` 를 읽어 모든 종료 코드를 0 으로 적었다(관문이 실패를 못 잡음). 아래 명령은 고친 것(`rc=$?`)이다. 이 결함으로 남은 기록은 무효 — 올바른 재실행 결과로 바꿨다(①: `Rpi5/조사/판정수정-20260925/selftest_기록정정.txt` · ②: `Rpi5/조사/GUI수정-20260925/` 두 파일).

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 판정기(`fsm.py`)가 방금 누른 버튼·EMO 근처 손 때문에 헛경고를 내지 않게 하고(P2·P3), 규칙 변경 2건(P5 EMO 해제 → IDLE · P6 경고 중 정답 → 완료)과 체류 임계값 정리(P7)를 넣는다.

**Architecture:** 체류(오답 머묾) 판정에서 빼는 ROI 를 「정답」 하나에서 「정답 · 방금 완료한 버튼(`_just_done`) · EMO」 셋으로 늘린다. `_just_done` 은 손이 그 ROI 를 떠나면(다른 ROI 관측 · 갭메우기 만료) 풀린다. 체류 셈법 자체와 관측 기록(`_last_roi`·`_last_seen`)은 **바꾸지 않는다**(P1 보류 · P4 → ③ R5 — spec D9).

**Tech Stack:** Python 3.13 (순수 로직 · Qt·HW 없음)

**Spec:** `docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md` §1(D1~D9)·§2 — 이 계획서 = ①묶음. ②GUI·③인식·인터락·음성은 따로 계획서를 쓴다.

## Global Constraints

- 코드 저장소 = `/home/pi/sop-project/Rpi5`(별도 git) · 문서 = `/home/pi/sop-project`. 커밋은 **경로 지정 add** 만(`git add -A`·`--all`·`.` 금지).
- 커밋 서명 = `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>` + `Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL` (실행 세션이 다르면 그 세션 값).
- 자가 테스트는 **일괄 러너가 없다** — `Rpi5/Demo` 에서 `selftest/test_*.py` 를 개별 실행한다. `test_voice_tts` 만 `~/env/tts/.venv/bin/python`.
- 🔴 **값을 바꾸지 않는다**(spec D6) — `FSM_GAP_FILL_SEC`·`FSM_DWELL_THRESHOLD_SEC`·`recipe.json` 의 `dwell_threshold_sec` 은 그대로다.
- 🔴 **P1·P4 를 넣지 않는다**(spec D9) — 체류 블록의 셈법(`now - self._dwell_start`)과 갭메우기 처리는 건드리지 않는다.
- 🔴 **측정 도구에 판정 규칙을 다시 쓰지 않는다** — `test/fsm_sim.py` 는 실제 `SafetyFSM` 을 import 한다. 이 계획서는 `fsm_sim.py` 를 **고치지 않는다.** (`조사/판정수정-20260925/preview_variants.py` 는 일회성 분석이지 측정 도구가 아니다.)
- 🔴 **측정 수치를 계획서·spec 에 쓰지 않는다** — 전후 값은 `Rpi5/조사/판정수정-20260925/` 원출력에 두고, 정본 기록은 Task 5 에서 한다.
- 🔴 **이 묶음은 `safety_console.py` 를 고치지 않는다**(GUI = ②묶음). ①만 적용된 상태는 **중간 상태**다 — 서브 작업이 있는 단계(B1~B3)에서 경고 중 정답을 누르면 GUI 가 서브를 시작하고, 서브가 경고 중에 끝나면 P6 로 단계가 완료된다(②의 G1 이 막는다). **①·② 사이에는 실시연하지 않는다.**

## 관문

- **🔒 홀드아웃** — 이번 묶음은 임계·값을 하나도 바꾸지 않는다. `fsm_sim` 전 세션(이름에 `holdout` 이 붙은 세션 포함)은 **평가 전용**이다. 결과를 보고 규칙·테스트 기대값을 고치지 않는다. 합격선(= 미리보기 「P2+P3만」 줄)은 **구현 전에 고정**됐다(`preview_variants.txt`, 커밋 `docs(조사): 판정 수정 ① 규칙별 미리보기`).
- **검증 관문** — ①태스크마다 새 테스트가 **고치기 전 실패 → 고친 뒤 통과**(가드 테스트는 「전후 통과」라고 표시함) ②Task 4: 자가 테스트가 Task 0 기준선과 같음 · `fsm_sim --gate` PASS · **오경보·사전 차단 합계 = 미리보기 「P2+P3만」 줄** · 거울 값 불변.
- **중단 규칙** — ①어느 태스크든 확인 단계가 기대와 다르면 다음 태스크로 넘어가지 않는다 ②**이번 규칙 변경과 무관한** 기존 테스트가 깨지면 멈추고 보고한다(기대값을 고쳐 통과시키지 않는다) ③`fsm_sim --gate` FAIL → 어떤 수치도 쓰지 않고 멈춘다 ④Task 4 합계가 미리보기와 **다르면** 멈춘다 — 구현이 설계와 다르다는 뜻이다(값·규칙 조정 금지) ⑤같은 수정을 3번 시도해도 안 되면 멈추고 보고한다.

## Review Focus

1. **서브 대기(10초) 뒤 자동 진행 순간, 손가락이 방금 버튼에 있는데 손 검출이 0.3초 넘게 끊겼다 다시 잡히는 경우** — 판정기는 「안 보임」과 「다른 곳」을 구별하지 못해 P2 예외가 끝나고 0.3초 뒤 경고가 날 수 있다. 사람은 경고가 없기를 기대한다 → 정의된 동작을 `test_p2_exception_ends_when_hand_leaves`(Task 1)가 고정한다. **한계로 기록**하고 실HW 에서 빈도를 본다(⏸).
2. **가짜 EMO 박스가 오답 버튼 자리에 잡히는 경우**(검토 C11) — P3 로 경고가 안 나 진짜 위반 머묾을 놓칠 수 있다. 사람은 오답 버튼 머묾에 경고를 기대한다 → ③묶음 R2(트래킹)가 다룬다. 여기서는 `test_p3_emo_dwell_does_not_warn`(Task 1)가 「EMO 머묾 = 경고 없음」만 고정한다.
3. ⏸ **2프레임 스침 뒤 갭메우기 0.3초 동안 머묾이 계속 쌓여 나는 경고**(검토 C1 · P1 보류) — 사람은 경고가 없기를 기대한다. **보류 항목이라 동작을 고정하는 테스트를 두지 않는다**(고정하면 나중 수정이 테스트를 깨야 한다). 남은 헛경고의 원인 후보로 Task 5 기록에 적는다.
4. ⏸ **카메라 재연결 직후 첫 프레임이 오답 박스**(검토 C4) — 이 묶음에서는 그대로 즉시 경고가 날 수 있다 → ③ R5 계획서가 고정한다.
5. **레시피에서 `dwell_threshold_sec` 을 지운 경우** — GUI 가 죽지 않고 config 값으로 돈다 → `test_missing_dwell_filled_from_config`(Task 3).

---

### Task 0: 기준선 기록

**Files:**
- Create: `Rpi5/조사/판정수정-20260925/before_selftest.txt` · `before_fsm_sim.txt` · `before_gate.txt` · `before_false_alarms.txt`

- [ ] **Step 1: 자가 테스트 전부를 개별 실행해 결과를 남긴다**

```bash
cd /home/pi/sop-project/Rpi5/Demo && \
for t in selftest/test_*.py; do
  py=python3; [ "$(basename $t)" = test_voice_tts.py ] && py=~/env/tts/.venv/bin/python
  timeout 600 $py "$t" >/dev/null 2>&1; rc=$?; echo "$(basename $t) exit=$rc"
done | tee ../조사/판정수정-20260925/before_selftest.txt
```
Expected: 한 줄에 테스트 하나씩 `exit=0` 또는 실패 코드. **실패가 있으면 그대로 기록한다**(이미 있던 실패 = Task 4 비교 기준).

- [ ] **Step 2: 판정 시뮬레이터 결과를 남긴다**

```bash
cd /home/pi/sop-project/Rpi5/Demo && D=../조사/판정수정-20260925 && \
python3 test/fsm_sim.py > $D/before_fsm_sim.txt 2>&1; \
python3 test/fsm_sim.py --gate > $D/before_gate.txt 2>&1; echo gate_exit=$?; \
python3 -c "
import sys; sys.path[:0] = ['test', '.']
import fsm_sim, hoi_metrics
rows = fsm_sim.run_all(hoi_metrics.connect())
fa = sum(r['false_alarms'] for r in rows); mins = sum(r['duration_sec'] for r in rows) / 60
tb = sum(r['blocked'] for r in rows); tv = sum(r['violations'] for r in rows)
print(f'오경보 합계 {fa}건 / {mins:.1f}분 = {fa/mins:.2f}/분 · 사전 차단 {tb}/{tv}')
" 2>/dev/null | grep 오경보 > $D/before_false_alarms.txt; cat $D/before_false_alarms.txt; grep 현재 $D/preview_variants.txt
```
Expected: `gate_exit=0` · `before_false_alarms.txt` 의 건수·사전 차단이 `preview_variants.txt` 「현재(재구성)」 줄과 같다.

- [ ] **Step 3: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add 조사/판정수정-20260925/before_selftest.txt 조사/판정수정-20260925/before_fsm_sim.txt 조사/판정수정-20260925/before_gate.txt 조사/판정수정-20260925/before_false_alarms.txt && git commit -m "docs(조사): 판정 수정 ① 기준선 — 자가 테스트·fsm_sim·관문·오경보 합계 (수정 전)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 1: P2·P3 — 방금 완료한 버튼·EMO 는 체류 판정 제외

**Files:**
- Modify: `Rpi5/Demo/fsm.py` (`__init__` · `update_vision` · `_step_complete` · `reset`)
- Test: `Rpi5/Demo/selftest/test_fsm.py` (파일 끝 `if __name__ == "__main__":` 앞에 추가)

**Interfaces:**
- Produces: `feed(fsm, roi, t0, t1, dt=0.1, **kw)`(테스트 helper · Task 2 가 쓴다) · `DT15` · `SafetyFSM._just_done`(str|None — 판정기 내부, ②묶음은 읽지 않는다)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
# ============================================================================
# 판정 수정 ① (2026-09-25) — 설계 docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md §2
# 재현 원본 = Rpi5/조사/런타임-검토-20260924/fsm_sim_check.py (검토 C2·C3·C5)
# ============================================================================
DT15 = 1 / 15      # 실제 카메라 약 15fps


def feed(fsm, roi, t0, t1, dt=0.1, **kw):
    """t0~t1 을 dt 간격 프레임으로 먹인다(양 끝 포함) — 실제 카메라처럼 촘촘한 관측."""
    n = max(1, int(round((t1 - t0) / dt)))
    for i in range(n + 1):
        fsm.update_vision(roi, t0 + (t1 - t0) * i / n, **kw)
    return t1


def test_p2_lingering_on_just_completed_button():
    """P2 — 방금 완료한 버튼 위에 손가락을 둬도 경고가 없다(검토 C2 재현 2)."""
    fsm, _ = make_fsm(threshold=0.3, gap_fill=0.3)
    run(fsm)
    feed(fsm, "B1", 0.0, 0.3)
    fsm.press_button("B1", 0.3)             # 1단계 완료 → 기대 B2
    assert fsm.correct_roi == "B2"
    feed(fsm, "B1", 0.3, 1.0)               # 0.7초 머묾 — 종전엔 0.3초 뒤 경고
    assert fsm.state == State.MONITOR


def test_p2_exception_ends_when_hand_leaves():
    """P2 — 손이 떠난 뒤(갭메우기 만료) 다시 오면 그때는 오답으로 센다.

    ⚠️ 판정기는 「안 보임」과 「다른 곳」을 구별하지 못한다 — 검출이 0.3초 넘게 끊겨도
       떠난 것으로 본다(계획서 Review Focus 1 · 한계).
    """
    fsm, _ = make_fsm(threshold=0.3, gap_fill=0.3)
    run(fsm)
    fsm.press_button("B1", 0.0)
    feed(fsm, "B1", 0.0, 0.5)
    feed(fsm, None, 0.6, 1.2)               # 갭메우기보다 오래 안 보임 = 떠남
    assert fsm.state == State.PROCESS_RUN
    feed(fsm, "B1", 1.3, 1.7)               # 돌아와 0.4초 머묾
    assert fsm.state == State.WARNING


def test_p2_exception_ends_on_other_roi():
    """P2 가드(전후 통과) — 다른 버튼으로 옮겼다 돌아오면 예외가 끝난다."""
    fsm, _ = make_fsm(threshold=0.3, gap_fill=0.3)
    run(fsm)
    fsm.press_button("B1", 0.0)
    fsm.update_vision("B1", now=0.0)
    fsm.update_vision("B2", now=0.05)       # 정답 B2 로 옮김
    feed(fsm, "B1", 0.1, 0.5)               # 다시 B1 에 0.4초
    assert fsm.state == State.WARNING


def test_p2_real_press_still_blocks():
    """P2 가드(전후 통과) — 예외는 체류에만. 방금 완료한 버튼을 실제로 다시 누르면 차단."""
    fsm, _ = make_fsm(threshold=0.3, gap_fill=0.3)
    run(fsm)
    fsm.press_button("B1", 0.0)
    feed(fsm, "B1", 0.0, 0.5)
    fsm.press_button("B1", 0.5)
    assert fsm.state == State.BLOCK


def test_p3_emo_dwell_does_not_warn():
    """P3 — EMO 박스에 머물러도 순서 경고가 없다(설계 §5.1 · 검토 C3 재현 4)."""
    fsm, _ = make_fsm(threshold=0.3, gap_fill=0.3)
    run(fsm)
    feed(fsm, "EMO", 0.0, 1.0, dt=DT15)
    assert fsm.state == State.MONITOR
    fsm.press_button("EMO", 1.0)            # 누르면 지금처럼 즉시 차단
    assert fsm.state == State.BLOCK
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_fsm.py 2>&1 | grep -E "^  FAIL|passed"`
Expected (미리 실행으로 확인함):
```
  FAIL  test_p2_exception_ends_when_hand_leaves
  FAIL  test_p2_lingering_on_just_completed_button
  FAIL  test_p3_emo_dwell_does_not_warn
27/30 passed
```

- [ ] **Step 3: 구현** — `fsm.py`

`__init__` 의 `self._dwell_start  = None  # 체류 시작 시각` 바로 아래:
```python
        # 방금 완료한 버튼 (P2 · 2026-09-25) — 손이 그 ROI 를 떠날 때까지 체류 판정에서 뺀다.
        # 단계가 넘어가는 순간 손끝이 아직 그 버튼 위라 곧바로 오답 체류가 되던 것(검토 C2).
        self._just_done    = None
```

`update_vision` 의 `# --- ③ 상태 게이트` 줄 **바로 앞**에:
```python
        # P2 — 방금 완료한 버튼 예외는 손이 그 ROI 를 떠나면(다른 ROI · 진짜 이탈) 끝난다.
        #      상태와 무관한 사실이라 게이트보다 먼저 본다.
        if self._just_done is not None and roi != self._just_done:
            self._just_done = None

```

`update_vision` 의
```python
        if roi == self.correct_roi:
            # 정답 ROI 진입: 눌림 대기 (진전은 press_button에서). 오답 타이머 해제.
            self._reset_dwell()
            return
```
→
```python
        if roi in (self.correct_roi, self._just_done, self._emo):
            # 정답 ROI: 눌림 대기 (진전은 press_button 에서). 오답 타이머 해제.
            # 방금 완료한 버튼(P2)·EMO(P3)도 체류 판정 대상이 아니다 — EMO 는 설계 §5.1
            # 「위반 판정 대상 아님」이고, **누르면** press_button 이 즉시 BLOCK 한다(검토 C3).
            self._reset_dwell()
            return
```

`_step_complete` docstring 다음 줄 `self._reset_dwell()` 바로 아래:
```python
        self._just_done = self.correct_roi     # P2 — 기대단계를 올리기 전에 기억한다
```

`reset` 의 `self._reset_dwell()` 바로 아래:
```python
        self._just_done = None
```

- [ ] **Step 4: 통과 확인 + 회귀**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_fsm.py | tail -1 && python3 selftest/test_recipe.py | tail -1 && python3 selftest/test_console_flow.py 2>&1 | tail -1 && python3 test/fsm_sim.py --gate 2>&1 | tail -1`
Expected: `30/30 passed` · `10/10 passed` · `✅ 글라스 UI 흐름 통과` · `✅ 통과 — 기준선 산출로 진행`

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/fsm.py Demo/selftest/test_fsm.py && git commit -m "fix(fsm): 방금 완료한 버튼·EMO 는 체류 판정 제외 (판정 수정 P2·P3)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 2: P5·P6 — 규칙 변경 2건

**Files:**
- Modify: `Rpi5/Demo/fsm.py` (모듈 docstring · `press_button` · `release_block`)
- Modify: `Rpi5/Demo/selftest/test_fsm.py` (`test_emo_blocks_immediately_and_resets_on_release` 기대값 = **규칙 변경** · 새 테스트 4개를 Task 1 블록 뒤에)

**Interfaces:**
- Consumes: `feed`(Task 1)
- Produces: `release_block()` — EMO 차단이면 `reset()` 과 같은 결과(IDLE · 기대 1 · 인터락 해제). `press_button(정답)` — WARNING 에서도 단계 완료. ②묶음 G1·G4 가 이 동작 위에 선다.

- [ ] **Step 1: 테스트**

`test_emo_blocks_immediately_and_resets_on_release` 의
```python
    fsm.release_block()                     # EMO 해제 → READY + 기대=1 리셋
    assert fsm.state == State.PROCESS_RUN
    assert fsm.expected_step == 1            # 시퀀스 전체 재시작
```
→
```python
    fsm.release_block()                     # EMO 해제 → 「작업 시작」 전 대기 (P5 · 규칙 변경 2026-09-25)
    assert fsm.state == State.IDLE
    assert fsm.expected_step == 1            # 시퀀스 전체 재시작
    assert log["interlock"][-1] is False     # 인터락 해제
    assert fsm._emo_active is False
```

새 테스트(Task 1 블록 뒤):
```python
def test_p5_emo_before_start_does_not_start_work():
    """P5 — 작업 시작 전(IDLE) EMO 를 풀어도 작업이 저절로 시작되지 않는다(검토 C5 재현 5)."""
    fsm, _ = make_fsm()
    fsm.press_button("EMO")
    assert fsm.state == State.BLOCK
    fsm.release_block()
    assert fsm.state == State.IDLE


def test_p5_violation_block_release_unchanged():
    """P5 가드(전후 통과) — 위반 차단 해제는 지금처럼 READY → PROCESS_RUN · 기대단계 유지."""
    fsm, _ = make_fsm()
    run(fsm)
    fsm.press_button("B3")                  # 오답 눌림 → BLOCK
    fsm.release_block()
    assert fsm.state == State.PROCESS_RUN
    assert fsm.expected_step == 1


def test_p6_correct_press_during_warning_completes_step():
    """P6 — 경고 중 정답 버튼 → 경고 해제 + 단계 완료(설계 D3 — 판정기 쪽)."""
    fsm, log = make_fsm(threshold=0.3, gap_fill=0.3)
    run(fsm)
    feed(fsm, "B2", 0.0, 0.4)
    assert fsm.state == State.WARNING
    fsm.press_button("B1", 0.5)
    assert fsm.expected_step == 2
    assert fsm.state == State.PROCESS_RUN
    assert log["feedback"][-1] == Feedback.NONE


def test_p6_correct_press_during_warning_on_last_step():
    """P6 — 마지막 단계에서도 경고 중 정답이면 공정 완료(IDLE)."""
    fsm, _ = make_fsm(threshold=0.3, gap_fill=0.3)
    run(fsm)
    for b in ("B1", "B2", "B3"):
        fsm.press_button(b)
    feed(fsm, "B1", 1.0, 1.4)               # 기대 B4 인데 B1 에 머묾 → 경고
    assert fsm.state == State.WARNING
    fsm.press_button("B4", 1.5)
    assert fsm.state == State.IDLE
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_fsm.py 2>&1 | grep -E "^  FAIL|passed"`
Expected (미리 실행으로 확인함):
```
  FAIL  test_emo_blocks_immediately_and_resets_on_release
  FAIL  test_p5_emo_before_start_does_not_start_work
  FAIL  test_p6_correct_press_during_warning_completes_step
  FAIL  test_p6_correct_press_during_warning_on_last_step
30/34 passed
```

- [ ] **Step 3: 구현** — `fsm.py`

모듈 docstring 의
```
오답. **오답은 단계를 절대 진전시키지 않는다.** 위반으로 BLOCK된 뒤 해제해도
기대단계는 유지되며, EMO 해제만 기대단계를 1로 리셋한다.
```
→
```
오답. **오답은 단계를 절대 진전시키지 않는다.** 위반으로 BLOCK된 뒤 해제해도
기대단계는 유지되며, EMO 해제는 「작업 시작」 전 대기(IDLE · 기대단계 1)로 돌아간다
(2026-09-25 규칙 변경 — 설계 docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md D4).
```

`press_button` 의
```python
        if button == self.correct_roi:
            # 정답 버튼 눌림 → Step Complete
            if self.state in (State.MONITOR, State.PROCESS_RUN):
                self._step_complete()
            return
```
→
```python
        if button == self.correct_roi:
            # 정답 버튼 눌림 → Step Complete. **WARNING 중 정답도 완료한다** — 경고 해제 +
            # 단계 완료(P6 · 규칙 변경 2026-09-25 D3). 장비는 이미 그 단계를 수행했으므로
            # 판정 단계를 어긋나게 두지 않는다. 서브 작업이 있는 단계는 GUI 가 먼저 받는다.
            if self.state in (State.MONITOR, State.PROCESS_RUN, State.WARNING):
                self._step_complete()
            return
```

`release_block` 전체를
```python
    def release_block(self):
        """BLOCK 해제 버튼.

        - 위반 BLOCK → READY 복귀 · 기대단계 유지(§7.3-7).
        - EMO BLOCK → **「작업 시작」 전 대기(IDLE) · 기대단계 1**(P5 · 규칙 변경 2026-09-25 D4).
          언제 눌렀든 같다. 종전 「READY 복귀 + 1단계」는 작업 시작 전·완료 뒤 EMO 를 풀면
          「작업 시작」 없이 작업이 도는 구멍이 있었다(검토 C5).
        """
        if self.state != State.BLOCK:
            return
        if self._emo_active:
            self.reset()                # IDLE · 기대 1 · EMO 플래그 정리 · 인터락 해제(_goto)
            return
        self._goto(State.READY)
        self._goto(State.PROCESS_RUN)
```

- [ ] **Step 4: 통과 확인 + 회귀**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_fsm.py | tail -1 && python3 selftest/test_recipe.py | tail -1 && python3 selftest/test_console_flow.py 2>&1 | tail -1 && python3 test/fsm_sim.py --gate 2>&1 | tail -1`
Expected: `34/34 passed` · `10/10 passed` · `✅ 글라스 UI 흐름 통과` · `✅ 통과 — 기준선 산출로 진행`

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/fsm.py Demo/selftest/test_fsm.py && git commit -m "feat(fsm): 규칙 변경 — EMO 해제는 작업 시작 전 대기 · 경고 중 정답은 해제+완료 (판정 수정 P5·P6)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 3: P7 — 체류 임계값 누락 시 config 값 · 두 값 일치 검사

**Files:**
- Modify: `Rpi5/Demo/recipe.py` (import · `_validate`)
- Test: `Rpi5/Demo/selftest/test_recipe.py` (파일 끝 `if __name__ == "__main__":` 앞에 추가)

- [ ] **Step 1: 테스트**

```python
# ----------------------------------------------------- 체류 임계값 (판정 수정 P7 · 2026-09-25)
def test_missing_dwell_filled_from_config():
    """🔴 dwell_threshold_sec 가 없으면 config 값으로 채운다 — 종전엔 숨은 기본 1.0(폐기값)으로
    통과시키고 GUI 는 키를 직접 읽어 KeyError 가 났다(함수목록 §4.1-9)."""
    import config
    data = {k: v for k, v in GOOD.items() if k != "dwell_threshold_sec"}
    with tempfile.TemporaryDirectory() as tmp:
        r = load_recipe(_write(tmp, data))
    assert r["dwell_threshold_sec"] == config.FSM_DWELL_THRESHOLD_SEC
    print("  PASS  체류 임계 누락 → config 값으로 채움")


def test_real_recipe_dwell_matches_config():
    """🔴 가드(전후 통과) — 런타임(recipe.json)과 측정 도구(config)의 체류 임계가 같다.
    도구가 config 를 안 따라 네 번 물렸다 — 값이 갈라지면 여기서 잡는다."""
    import config
    assert load_recipe()["dwell_threshold_sec"] == config.FSM_DWELL_THRESHOLD_SEC, \
        "recipe.json 과 config.FSM_DWELL_THRESHOLD_SEC 가 다르다 — 측정이 런타임을 대표하지 못한다"
    print("  PASS  recipe.json 체류 임계 == config")
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_recipe.py 2>&1 | tail -1`
Expected: `KeyError: 'dwell_threshold_sec'` (러너가 첫 실패에서 멈춘다)

- [ ] **Step 3: 구현** — `recipe.py`

`import os` 아래:
```python

import config
```

`_validate` 의
```python
    thr = data.get("dwell_threshold_sec", 1.0)
```
→
```python
    # 🔑 없으면 config 값을 **채워 넣는다**(P7 · 2026-09-25) — 호출부(GUI)가 키를 직접 읽어도
    #    KeyError 가 나지 않고, 종전의 숨은 기본 1.0(폐기값)이 사라진다. 측정 도구는 config 를
    #    읽으므로 두 값이 같아야 한다 — selftest/test_recipe.py 가 검사한다.
    thr = data.setdefault("dwell_threshold_sec", config.FSM_DWELL_THRESHOLD_SEC)
```

- [ ] **Step 4: 통과 확인 + 회귀**

Run: `cd /home/pi/sop-project/Rpi5/Demo && python3 selftest/test_recipe.py | tail -1 && python3 selftest/test_imports.py 2>&1 | tail -1 && python3 selftest/test_console_flow.py 2>&1 | tail -1`
Expected: `12/12 passed` · `test_imports` 가 Task 0 기준선과 같은 결과 · `✅ 글라스 UI 흐름 통과`

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add Demo/recipe.py Demo/selftest/test_recipe.py && git commit -m "fix(recipe): 체류 임계 누락 시 config 값으로 채움 · recipe.json 과 config 일치 검사 (판정 수정 P7)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 4: 검증 관문 — 자가 테스트 전부 · 관문 · 미리보기 대조

**Files:**
- Create: `Rpi5/조사/판정수정-20260925/after_selftest.txt` · `after_fsm_sim.txt` · `after_gate.txt` · `after_false_alarms.txt`

- [ ] **Step 1: 수정 후 결과를 남기고 기준선·미리보기와 대조한다**

```bash
cd /home/pi/sop-project/Rpi5/Demo && D=../조사/판정수정-20260925 && \
for t in selftest/test_*.py; do
  py=python3; [ "$(basename $t)" = test_voice_tts.py ] && py=~/env/tts/.venv/bin/python
  timeout 600 $py "$t" >/dev/null 2>&1; rc=$?; echo "$(basename $t) exit=$rc"
done > $D/after_selftest.txt; \
diff $D/before_selftest.txt $D/after_selftest.txt && echo "자가 테스트 기준선과 같음"; \
python3 test/fsm_sim.py > $D/after_fsm_sim.txt 2>&1; \
python3 test/fsm_sim.py --gate > $D/after_gate.txt 2>&1; echo gate_exit=$?; \
python3 -c "
import sys; sys.path[:0] = ['test', '.']
import fsm_sim, hoi_metrics
rows = fsm_sim.run_all(hoi_metrics.connect())
fa = sum(r['false_alarms'] for r in rows); mins = sum(r['duration_sec'] for r in rows) / 60
tb = sum(r['blocked'] for r in rows); tv = sum(r['violations'] for r in rows)
print(f'오경보 합계 {fa}건 / {mins:.1f}분 = {fa/mins:.2f}/분 · 사전 차단 {tb}/{tv}')
" 2>/dev/null | grep 오경보 > $D/after_false_alarms.txt; \
python3 -c "
import re
p = open('$D/preview_variants.txt', encoding='utf-8').read()
a = open('$D/after_false_alarms.txt', encoding='utf-8').read()
pl = [l for l in p.splitlines() if l.startswith('P2+P3만')][0]
exp = re.search(r'(\d+)건.*사전 차단 (\d+/\d+)', pl).groups()
got = re.search(r'오경보 합계 (\d+)건.*사전 차단 (\d+/\d+)', a).groups()
print('미리보기', exp, '· 실제', got, '→', '일치' if exp == got else '불일치')
"; \
diff <(grep 통합 $D/before_fsm_sim.txt | grep -o '거울 *[0-9.]*% ([0-9/]*)') <(grep 통합 $D/after_fsm_sim.txt | grep -o '거울 *[0-9.]*% ([0-9/]*)') && echo "거울 불변"
```
Expected: `자가 테스트 기준선과 같음` · `gate_exit=0` · `… → 일치` · `거울 불변`.
- diff 가 있으면 → 새로 실패한 테스트가 이번 변경 때문인지 확인 → 중단 규칙 ②.
- `gate_exit` ≠ 0 → 중단 규칙 ③. · `불일치` → 중단 규칙 ④. · 거울이 바뀌었으면 관측 기록을 건드린 것이다 → 멈춘다.

- [ ] **Step 2: 커밋**

```bash
cd /home/pi/sop-project/Rpi5 && git add 조사/판정수정-20260925/after_selftest.txt 조사/판정수정-20260925/after_fsm_sim.txt 조사/판정수정-20260925/after_gate.txt 조사/판정수정-20260925/after_false_alarms.txt && git commit -m "docs(조사): 판정 수정 ① 결과 — 자가 테스트·fsm_sim·관문·오경보 합계 (수정 후)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```

---

### Task 5: 정본·문서 반영

🔴 통합문서·저널을 열면 `.claude/rules/정본편집.md`·`수치인용.md` 가 붙는다 — **그 규칙을 따른다.** 쓸 곳은 `.claude/rules/문서배치.md` 표대로다(측정 절 본문 = 성능검증-저널 · 확정값 = 통합문서 §12 `[CURRENT]` · 미결 = 🔓 표).

**Files:**
- Modify: `docs/통합문서.md` — §5 표 공통 행(EMO) · §5 「이상」 흐름 · §7.2 EMO 공통 경로 메모 · §7.3-5·6·7 · 🔓 「실제 시연 프로그램 문제 수정」 행 · (해당 행이 있으면) §12 `[CURRENT]`
- Modify: `docs/성능검증-저널.md` — 판정 갈래 끝에 새 절(번호는 파일에서 다음 번호를 확인)
- Modify: `Rpi5/CLAUDE.md` (FSM 줄 「EMO→즉시 BLOCK(해제 시 기대단계=1 리셋…)」)
- Modify: `Rpi5/함수목록.md` §4.1 — 6번(WARNING 중 정답 무시)·9번(체류 임계 두 곳)에 「✅ 해결 — 판정 수정 ①(2026-09-25)」
- Modify: `docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md` 머리 상태 줄

- [ ] **Step 1: §5·§7 규칙 반영** — 바꾸는 뜻(문장은 정본편집 규칙대로):
  - EMO 해제 → **「작업 시작」 전 대기(IDLE) · 기대단계 1**(언제 눌렀든). 종전 「READY 복귀 + 1단계」는 폐기 이력으로(`docs/변경이력.md` 가 폐기 사실의 자리).
  - §7.3-6 WARNING 세 갈래 — 해제 버튼 → MONITOR / 오답 버튼 → BLOCK / **정답 버튼 → 해제 + 단계 완료**(서브 작업이 있는 단계는 ②묶음 G1 뒤 「해제 + 평소대로」).
  - §7.3-5 — **방금 완료한 버튼**(손이 떠날 때까지)과 **EMO** 는 체류 판정 대상이 아님.
  - §7.4 값은 **바꾸지 않는다**(D6).

- [ ] **Step 2: 저널 새 절** — 전후 합계(`before_`/`after_false_alarms.txt`)와 규칙별 미리보기(`preview_variants.txt`)를 **조건과 함께**: fsm_sim 시뮬레이션 정책 4종 · 데이터 = 2026-07 모조 콘솔 VGA 22세션 · 「규칙 변경 후 값」으로 따로 두고 예전 `[CURRENT]` 값을 덮어쓰지 않음 · 관문 PASS 출처(`after_gate.txt`) · **P1 보류 이유**(누르는 순간 가림 구간) · 남은 헛경고 후보(Review Focus 3).

- [ ] **Step 3: 🔓 행 갱신** — 「🛑 설계 중단」 → 「①판정 구현(2026-09-25) · ②③ 대기 · P1 보류(D9)」, 설계서·계획서 경로.

- [ ] **Step 4: `Rpi5/CLAUDE.md`·`함수목록.md`·spec 머리 갱신**

- [ ] **Step 5: 커밋** (두 저장소)

```bash
cd /home/pi/sop-project/Rpi5 && git add CLAUDE.md 함수목록.md && git commit -m "docs: 판정 수정 ① 반영 — EMO 해제는 작업 시작 전 대기 · 함수목록 §4.1-6·9 해결

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
cd /home/pi/sop-project && git add docs/통합문서.md docs/성능검증-저널.md docs/superpowers/specs/2026-09-25-런타임-문제수정-design.md && git commit -m "docs(정본): 판정 수정 ① — §7 규칙 변경(EMO 해제 → IDLE · 경고 중 정답 → 완료 · 방금 버튼·EMO 체류 제외) · 저널 전후 결과

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JDs5qRwEXjDBfduqnbPXNL"
```
(`docs/변경이력.md` 를 고쳤으면 두 번째 add 에 함께 넣는다.)

---

### Task 6: 최종 리뷰 · 공동 확인 · 마무리

- [ ] **Step 1: 최종 리뷰 1회** — `superpowers:requesting-code-review`. 대상 = Task 1~3 의 Rpi5 커밋 범위. 리뷰 기준에 **사용자 결정 원문**을 넣는다: P5·P6(`~/lab/session-archive/2026-09-23_a38f8f67.md` · 2026-09-24 16:47~16:54) · D3 맞물림·D9(P1 보류·P4 이동) 승인(세션 `56d1f2c1`). 지적은 `superpowers:receiving-code-review` 로 검증한 뒤 반영한다.

- [ ] **Step 2: 공동 확인** — 사용자에게 고정 형식으로 보고한다: 태스크마다 한 줄 · 전후 대조(Task 4 Step 1 출력) · ⏸ 남은 것(실HW 흐름 · Review Focus 1~4 · ①·② 사이 실시연 금지). 🔴 **Claude 단독으로 「완료」를 선언하지 않는다** — 관문 결과 출처(`after_gate.txt` · 자가 테스트 diff · 미리보기 일치)를 붙인다.

- [ ] **Step 3: `work-close`** — 결말(✅ 또는 ⏩ ②묶음으로 이어감)을 사용자와 확인한 뒤 실행한다. push 는 여기서 한 번.
