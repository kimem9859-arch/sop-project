# 통합문서 구조 재편 — 실행 계획서

> **실행자에게:** 이 계획은 태스크 단위로 실행한다. 각 태스크 끝에 **검증 명령 + 기대 출력**과 **커밋**이 있다. 태스크 사이에서 사용자 검토를 받는다(CLAUDE.md §2 역할 분담 — Claude 단독 「완료」 선언 금지).

**목표:** 통합문서(`docs/통합문서.md`)를 기능 / 비기능 / 구현 수단 3묶음·16장으로 재편하고, 참조 1,100여 곳을 깨짐 없이 옮기며, 앞으로 새 사실을 붙일 자리(R12)를 각 장에 만든다.

**방법:** ①내용 이동·삭제를 **옛 번호 체계 그대로** 먼저 끝내고 → ②장을 재배열하며 새 번호를 부여 → ③참조를 2단계 기계 치환(보호 목록 제외) → ④신설 2개 장 집필 → ⑤R12 자리 → ⑥바깥 문서 갱신·자가 점검.

**대상 파일:** `docs/통합문서.md`(6,475줄) 본체 · 바깥 길잡이 문서(README·CLAUDE.md×2·스킬·훅·Rpi5 문서)

**설계(spec):** `docs/superpowers/specs/2026-09-18-정본-구조재편-design.md`

---

## 전역 제약

- 🔴 **측정 절(옛 §10.x) 본문은 번호 외에 건드리지 않는다.** 문장·수치·판정 표현 일절 수정 금지.
- 🔴 **소절 안 번호는 유지한다** — `§10.31` → `§12.31`(31 은 그대로). 작업로그·`[CURRENT]` 표·갈래 색인이 그 번호로 가리킨다.
- 🔴 **경위는 본문 밖**(문서 규칙 R9) — 이동 중 발견되는 「왜 이렇게 바뀌었나」 서술은 걷어내고 커밋 메시지에 남긴다.
- 🔴 **사실은 한 곳에만**(R5) — 재편 뒤 부품 = §14.1 · 핀·채널 = §15 · 측정 = §12 `[CURRENT]` · 설계값 = 해당 절.
- 🔴 **추측 금지** — 신설 장에서 확인 못 한 것은 `⏸ 미확인` 으로 남긴다.
- 이동만 하는 장은 **이번에 문체를 바꾸지 않는다**(장별 검토 때).
- 커밋 서명: `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` + `Claude-Session:` 줄. push 는 `work-close` 에서 한 번.

---

## ⚠️ 설계에 없던 함정 3건 (조사로 발견 · 2026-09-18)

계획서를 쓰기 전 실제 문서를 조사해 아래 셋을 찾았다. 설계 §4 「기계 치환」을 **그대로 하면 문서가 망가진다.**

### 함정 A — 통합문서 안의 `§N` 이 전부 통합문서를 가리키지 않는다 🔴 최대 위험

`설계 §8.2` · `CLAUDE.md §5` · `창판정-design §5.4` · `결선도_초안.md §2~4` · `세운콘솔-촬영-design §10` · `LLM관문.md §2` · `점검 문서 §1.5` 처럼 **다른 문서의 절**을 가리키는 참조가 최소 40곳 섞여 있다. 기계 치환하면 `CLAUDE.md §5` 가 `CLAUDE.md §13` 이 되는 식으로 **없는 절을 가리키게 된다.**

→ **대응 = 보호 목록(Task 3 Step 1~3).** 치환 전에 외부 참조를 `§!` 토큰으로 봉인하고, 치환이 끝난 뒤 되돌린다.

### 함정 B — 참조 치환 범위가 설계의 「60곳」보다 훨씬 넓다

| 부류 | 파일 | § 참조 수 | 이번에 고치나 |
|---|---|---|---|
| 정본 본체 | `docs/통합문서.md` | 1,166 | ✅ 고친다 |
| 살아 있는 길잡이 | `README.md` 28 · `CLAUDE.md` 19 · `Rpi5/CLAUDE.md` 38 · `docs/claude-code-작업문서.md` 12 · `.claude/` 스킬·훅 39 · `Rpi5/Demo/` 문서 75 | 약 211 | ✅ 고친다 |
| 역사 기록 | `docs/작업로그.md` 843 · `docs/claude-code-작업로그.md` 97 · `docs/정본검토-기록.md` 273 · `docs/미기록작업-점검-20260911.md` 128 · `docs/superpowers/` specs·plans 1,000여 | 약 2,300 | ❌ 고치지 않는다 |

**🟦 Claude가 정한 것 D1 — 역사 기록은 치환하지 않고, 대신 §16 부록에 「옛↔새 장번호 대조표」를 둔다.**
이유: 작업로그·설계 문서는 **그때 무엇을 했는지의 기록**이고, 과거 커밋 서명을 고치지 않는 규칙(CLAUDE.md §4)과 같은 계열이다. 2,300곳을 고치면 위험만 커지고 얻는 것은 길잡이 하나인데, 그 길잡이는 대조표 한 장으로 대신할 수 있다.
버린 대안: (가) 전부 치환 — 역사 왜곡 + 함정 A 위험이 2배. (나) 아무 대조표도 안 둠 — 작업로그의 `§10.47` 이 영영 미아가 된다.

### 함정 C — 이미 깨진 참조가 있고, 장이 통째로 갈라진다

- 조사 중 `§5.4` `§5.5` `§5.7` `§7.5` 처럼 **존재하지 않는 소절**을 가리키는 표기를 찾았는데, 전부 외부 문서 참조였다(함정 A 의 실제 사례). 「깨짐 0」 판정은 **외부 참조를 제외한 뒤** 재야 한다.
- 옛 §4·§5·§6·§7·§9·§13 은 **한 장이 두 장으로 갈라진다.** 소절 번호가 붙은 참조는 기계 치환되지만, **맨 참조(`§5` 13곳 · `§6` 12곳 · `§7` 12곳 · `§8` 23곳 · `§9` 19곳 · `§4` 7곳 · `§13` 20곳 · `§10` 54곳)는 문맥을 봐야** 어디로 갈지 정해진다 → Task 3 Step 6 에서 한 건씩 판정.

---

## 옛 → 새 대조표 (정본)

이 표가 Task 2·3 의 기준이다. **소절 규칙이 장 규칙보다 먼저** 적용된다.

### 장 단위

| 옛 | 새 | 묶음 | 비고 |
|---|---|---|---|
| §1 §2 §3 | §1 §2 §3 | 머리 | 그대로 |
| §4.1 | §4.1 | Ⅰ 기능 | 기능 요구사항 |
| §4.3 | §4.2 | Ⅰ | 시스템 요구사항 |
| §4.4 | §4.3 | Ⅰ | 사용자 요구사항 |
| §4.2 | **§11** | Ⅱ 비기능 | 비기능 요구사항(목표) — 장으로 승격 |
| §5.1 | §13.1 | Ⅲ 구현 수단 | 시스템 구성 블록도 |
| §5.2 | §13.2 | Ⅲ | 통신 경로 |
| §5.3 | **분해** | — | 부품→§14.1 · 버튼 배치도→§5.4 · 배치 경위·폐기 배치도→**삭제** |
| §6.1 | §5.1 | Ⅰ | 4단계 공정 + 비상정지 |
| §6.1.1 | §5.1.1 | Ⅰ | 서브 작업 |
| §6.2 | §5.2 | Ⅰ | 정상/이상 흐름 |
| §6.3 (사용자 흐름) | §5.3 | Ⅰ | 착용→기동→작업→피드백 |
| §6.3 「운용 조작」 소제목 | **§8.2·§8.3** | Ⅰ | 작업 초기화·결과창 → 신설 §8 |
| §7.1 | §6.1 | Ⅰ | 스택 구성 |
| §7.2 | §6.2 | Ⅰ | HOI 알고리즘 |
| §7.3 | §6.3 | Ⅰ | 상세 설명 |
| §7.4 | **§10** | Ⅰ | 검토 중/확장 → 범위와 향후 확장 |
| §8 (다리 절) | **§7 에 흡수** | Ⅰ | 장 소멸 |
| §9.1 | §7.1 | Ⅰ | 상태 정의 |
| §9.2 | §7.2 | Ⅰ | 상태 전이도 |
| §9.3 | §7.3 | Ⅰ | 전이 시나리오 |
| §9.4 | §7.4 | Ⅰ | 임계값 정본 |
| §9.5 | **§10** | Ⅰ | 확장 분리 항목 |
| — | **§8** 🆕 | Ⅰ | 제어·피드백 — 인터락·타워램프·GUI |
| — | **§9** 🆕 | Ⅰ | 음성비서 |
| §13.1 §13.2 §13.3 | §10.1 §10.2 §10.3 | Ⅰ | 향후 확장 |
| §10.N | §12.N | Ⅱ | **N 유지** (성능 검증 데이터) |
| §11.1 §11.2 §11.3 | §14.1 §14.2 §14.3 | Ⅲ | 하드웨어·환경 |
| §12 | §15 | Ⅲ | 회로도·핀맵 |
| §14.N | §16.N | 부록 | 부록 |

### 묶음 머리

```
## Ⅰ. 기능        (§4 앞)
## Ⅱ. 비기능      (§11 앞)
## Ⅲ. 구현 수단   (§13 앞)
## 부록           (§16 앞)
```

### 기계 치환용 sed 규칙 (2단계 · 소절 먼저 · 긴 번호 먼저)

```bash
# 1단계: 옛 → 임시 표식 §@N  (표식에 @ 가 있어 뒤 규칙에 다시 안 걸린다)
SED1='
s/§4\.2/§@11/g; s/§4\.3/§@4.2/g; s/§4\.4/§@4.3/g;
s/§5\.1/§@13.1/g; s/§5\.2/§@13.2/g;
s/§6\.1\.1/§@5.1.1/g; s/§6\.1/§@5.1/g; s/§6\.2/§@5.2/g;
s/§7\.1/§@6.1/g; s/§7\.2/§@6.2/g; s/§7\.3/§@6.3/g; s/§7\.4/§@10/g;
s/§9\.1/§@7.1/g; s/§9\.2/§@7.2/g; s/§9\.3/§@7.3/g; s/§9\.4/§@7.4/g; s/§9\.5/§@10/g;
s/§10\./§@12./g; s/§11\./§@14./g; s/§12\./§@15./g; s/§13\.1/§@10.1/g; s/§13\.2/§@10.2/g; s/§13\.3/§@10.3/g; s/§14\./§@16./g;
s/§14/§@16/g; s/§13/§@10/g; s/§12/§@15/g; s/§11/§@14/g; s/§10/§@12/g;
s/§9/§@7/g; s/§8/§@7/g; s/§7/§@6/g; s/§6/§@5/g;
'
# 🔴 §5(맨) · §5.3 · §6.3 · §4(맨) 는 규칙에 없다 — Task 3 Step 6 에서 한 건씩 수동 판정
# 2단계: 표식 → 새 번호
SED2='s/§@/§/g'
```

---

## Task 0: 실행 준비 — 백업과 조사표

**파일:** Create `/tmp/claude-1000/.../재편-작업표.md`(스크래치) · 수정 없음

- [ ] **Step 1: 시작점 백업**

```bash
cd /home/pi/sop-project
git rev-parse HEAD > /tmp/재편-시작커밋.txt && cat /tmp/재편-시작커밋.txt
cp docs/통합문서.md /tmp/통합문서-재편전.md
wc -l /tmp/통합문서-재편전.md
```
기대: 6475 줄.

- [ ] **Step 2: 기준 지표 3개를 적어 둔다 (재편 뒤 비교용)**

```bash
cd /home/pi/sop-project
echo "총 § 참조: $(grep -o '§[0-9]\+' docs/통합문서.md | wc -l)"
echo "'###' 소절 수: $(grep -c '^### ' docs/통합문서.md)"
echo "'## 10.' 측정 절 수: $(grep -c '^### 10\.' docs/통합문서.md)"
```
기대: 참조 1166 · 측정 절 수를 기록해 둔다(재편 뒤 **같아야 한다**).

- [ ] **Step 3: 커밋 없음** — 조사만. 다음 태스크로.

---

## Task 1: 삭제·흡수 3건 (옛 번호 그대로)

> 번호를 바꾸기 **전에** 내용 이동을 끝낸다. 옛 번호 체계 안에서 하면 참조가 아직 일관되어 검토가 쉽다.

**파일:** Modify `docs/통합문서.md`
- 옛 §8 (617–642) · 옛 §9 머리(643) · 옛 §5.3 (348–412) · 옛 §7.4 (607–616 부근) · 옛 §9.5 (724–732 부근) · 옛 §13 (6338–6374)

- [ ] **Step 1: 옛 §8 다리 절을 옛 §9 앞머리로 흡수**

옛 §8 전체(`## 8. 〈연결〉…` ~ 다음 `---` 까지)를 잘라 **옛 §9 의 `### 9.1` 바로 앞**에 `### 9.0 인식 → 판정 → 제어 연결`(임시 번호)로 붙인다. Mermaid 블록과 「연결 규칙 요약」 불릿은 **그대로** 옮긴다.

걷어낼 것(R9 · 설계 §3 「내용이 §7·§9·§6.1 재요약」):
- 첫 인용구 `> AI 파이프라인(§7, 독립 A)과 FSM(§9, 독립 B)을 잇는 **다리** 섹션이다…` → 삭제(장 구조 설명이지 사실이 아니다)
- 불릿 중 **§7·§9 본문을 되풀이하는 2줄**(「인식부(§7) 는 … 이벤트만 만든다」 / 「판정부(§9 FSM) 가 … 의미를 부여한다」)은 남긴다 — 그림 판독에 필요하다.
- 마지막 불릿 「이 분리 덕분에 … 독립적으로 교체·개선할 수 있다」 → 남긴다(설계 의도 = 사실).

- [ ] **Step 2: 옛 §5.3 을 세 갈래로 분해**

`### 5.3 구성 요소 요약 (블록도 판독용)` 안을 읽고 나눈다.
1. **부품 표·부품명·수량** → 옛 §11.1 로 옮긴다. 🔴 §11.1 에 이미 있는 부품이면 **옮기지 말고 삭제**한다(R5 사실은 한 곳에만). 옮기기 전 `grep -n '부품명' docs/통합문서.md` 로 중복을 확인한다.
2. **버튼 배치도 + 「학습 분포와 맞춘 결정 근거」** → 옛 §6 끝에 `### 6.4 버튼 배치`(임시)로 옮긴다. 근거 문장은 **사실이므로 유지**(설계 §7).
3. **배치 변경 경위 표 · 폐기된 배치도** → **삭제.** 지운 내용의 요지 한 줄을 커밋 메시지에 남긴다(R9).

- [ ] **Step 3: 옛 §7.4 · 옛 §9.5 를 옛 §13 으로 합류**

- `### 7.4 🔄 검토 중 / 확장 분리` 의 항목들 → 옛 §13 끝에 `### 13.4 비전 파이프라인에서 분리한 항목` 으로.
- `### 9.5 §13 확장으로 분리된 FSM 요소` → 옛 §13 끝에 `### 13.5 FSM 에서 분리한 항목` 으로.
- 🔴 옛 §13.1 「핵심 → 확장으로 이동한 항목」과 **내용이 겹치면 합친다** — 같은 항목을 두 번 적지 않는다.
- 두 소절 자리에는 **아무것도 남기지 않는다**(「§13 참조」 같은 껍데기 금지 — R5).

- [ ] **Step 4: 검증 — 내용 유실 0 확인**

```bash
cd /home/pi/sop-project
# 장 머리에서 §8 이 사라졌는가
grep -n '^## 8\.' docs/통합문서.md   # 기대: 출력 없음
# 삭제한 소절이 사라졌는가
grep -n '^### 5\.3\|^### 7\.4\|^### 9\.5' docs/통합문서.md   # 기대: 출력 없음
# Mermaid 블록 수가 유지됐는가 (흡수는 이동이지 삭제가 아니다)
grep -c '```mermaid' docs/통합문서.md   # 기대: 재편 전과 동일
diff <(grep -o '§10\.[0-9]\+' /tmp/통합문서-재편전.md | sort | uniq -c) \
     <(grep -o '§10\.[0-9]\+' docs/통합문서.md | sort | uniq -c)   # 기대: 차이 없음
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project
git add docs/통합문서.md
git commit -m "$(cat <<'EOF'
docs(정본): 구조 재편 1/7 — 삭제·흡수 3건 (옛 번호 유지)

- 옛 §8 다리 절 → 옛 §9 앞머리로 흡수 (장 구조 설명 인용구는 R9 로 걷어냄)
- 옛 §5.3 분해: 부품 → §11.1 · 버튼 배치도+결정근거 → §6.4 · 배치 변경 경위·폐기 배치도 삭제
- 옛 §7.4 · §9.5 확장 목록 → 옛 §13 한 곳으로 (겹치는 항목 합침)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RepH4diWx23QZmPtxKFaCr
EOF
)"
```

---

## Task 2: 장 재배열 + 새 번호 + 묶음 머리

**파일:** Modify `docs/통합문서.md`

- [ ] **Step 1: 장 순서를 새 배열로 옮긴다**

블록(각 `## N.` 머리부터 다음 `## ` 머리 직전까지)을 통째로 옮겨 아래 순서로 만든다. **본문 문장은 건드리지 않는다.**

```
§1 §2 §3
## Ⅰ. 기능
§4(옛 4.1+4.3+4.4)  §5(옛 6+6.4)  §6(옛 7)  §7(옛 9+흡수한 다리)  §8🆕(빈 장)  §9🆕(빈 장)  §10(옛 13)
## Ⅱ. 비기능
§11(옛 4.2)  §12(옛 10 전체 — 갈래 색인·[CURRENT]·🔓 미결·갈래 ①~⑥ 포함)
## Ⅲ. 구현 수단
§13(옛 5.1+5.2)  §14(옛 11)  §15(옛 12)
## 부록
§16(옛 14)
```

🔴 옛 §10 은 `## 🧭 갈래 색인` · `## 🎯 현재 확정값` · `## 📂 갈래 ①~⑥` 을 **같은 `##` 수준으로 달고 있다** — §12 로 옮길 때 **이 6개 블록을 함께** 옮긴다(733–6155 전체).

이번에는 `## 8. 제어·피드백 — 인터락·타워램프·GUI` 와 `## 9. 음성비서` 를 **머리만** 만들고 본문은 `⏸ 집필 예정(Task 4·5)` 한 줄로 둔다.

- [ ] **Step 2: 장 머리와 소절 번호를 새 번호로 고친다**

각 `## N. 제목` 과 `### N.M` 을 대조표대로 고친다. 🔴 **§12(옛 §10)의 `### 10.N` 은 `### 12.N` 으로만 바꾸고 N 은 유지.**

```bash
cd /home/pi/sop-project
sed -i -E 's/^### 10\.([0-9]+)/### 12.\1/' docs/통합문서.md
grep -c '^### 12\.' docs/통합문서.md   # 기대: Task 0 Step 2 에서 적어 둔 측정 절 수와 동일
```

- [ ] **Step 3: 묶음 머리 4개를 삽입**

`## Ⅰ. 기능` / `## Ⅱ. 비기능` / `## Ⅲ. 구현 수단` / `## 부록` 을 해당 자리에 넣고, 각 머리 아래 **한 줄 설명**을 단다.

```markdown
## Ⅰ. 기능

> 이 시스템이 **무엇을 하는가.** 요구사항 → 시연 시나리오 → 인식 → 판정 → 제어·피드백 → 음성비서 → 범위.
```
(나머지 셋도 같은 형식으로 한 줄씩. 🔴 수치·부품명은 쓰지 않는다 — 포인터만.)

- [ ] **Step 4: 검증 — 번호가 1~16 로 빠짐없이 한 번씩**

```bash
cd /home/pi/sop-project
grep -n '^## [0-9]\+\.' docs/통합문서.md | sed 's/:.*## /  /'   # 기대: 1..16 오름차순, 중복 없음
grep -n '^## [ⅠⅡⅢ]\.\|^## 부록' docs/통합문서.md   # 기대: 4줄
# 줄 수가 크게 변하지 않았는가 (이동이지 삭제가 아니다)
wc -l docs/통합문서.md
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project
git add docs/통합문서.md
git commit -m "$(cat <<'EOF'
docs(정본): 구조 재편 2/7 — 장 재배열·새 번호·묶음 머리 (참조는 아직 옛 번호)

기능 Ⅰ(§4~§10) / 비기능 Ⅱ(§11~§12) / 구현 수단 Ⅲ(§13~§15) / 부록(§16)
§8·§9 는 머리만 — 본문은 Task 4·5 에서 집필
§12(옛 §10) 소절 안 번호는 유지 (12.N ← 10.N)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RepH4diWx23QZmPtxKFaCr
EOF
)"
```

---

## Task 3: 참조 치환 + 깨짐 0 확인 🔴 가장 위험한 태스크

- [ ] **Step 1: 외부 문서 참조 후보를 뽑는다 (함정 A)**

```bash
cd /home/pi/sop-project
grep -n '§[0-9]' docs/통합문서.md \
  | grep -E '(설계|design|초안|계획|plan|spec|점검 문서|보고서|관문|CLAUDE\.md|README|\.md`|\.md\))[^§]{0,60}§[0-9]' \
  > /tmp/외부참조-후보.txt
wc -l /tmp/외부참조-후보.txt   # 기대: 40~60줄 안팎
```

- [ ] **Step 2: 후보를 한 줄씩 읽어 「외부/내부」를 가른다**

🔴 **눈으로 전부 본다.** `CLAUDE.md §5` `설계 §8.2` `창판정-design §5.4` `결선도_초안.md §2~4` `LLM관문.md §2` 는 **외부**. `(§9.4 각주가…)` 처럼 문서명 없이 쓰인 것은 **내부**. 판단이 안 서면 그 줄의 앞뒤 3줄을 읽는다(`sed -n 'X,Yp'`).

외부로 판정한 것을 `줄번호<TAB>원문표기` 형식으로 `/tmp/보호목록.tsv` 에 적는다.

- [ ] **Step 3: 외부 참조를 봉인**

```bash
cd /home/pi/sop-project
while IFS=$'\t' read -r ln ref; do
  esc=$(printf '%s' "$ref" | sed 's/[.[\*^$\/]/\\&/g')
  sed -i "${ln}s/${esc}/§!${ref#§}/g" docs/통합문서.md
done < /tmp/보호목록.tsv
grep -c '§!' docs/통합문서.md   # 기대: 보호목록 줄 수와 일치
```

- [ ] **Step 4: 기계 치환 1단계 (옛 → `§@N`)**

위 「기계 치환용 sed 규칙」의 `SED1` 을 적용한다.

```bash
cd /home/pi/sop-project
sed -i -E "$SED1" docs/통합문서.md
grep -o '§[0-9]\+' docs/통합문서.md | sort | uniq -c   # 기대: §4 §5 만 남음(수동 대상)
```

- [ ] **Step 5: 기계 치환 2단계 (`§@N` → `§N`)**

```bash
cd /home/pi/sop-project
sed -i 's/§@/§/g' docs/통합문서.md
grep -c '§@' docs/통합문서.md   # 기대: 0
```

- [ ] **Step 6: 남은 맨 참조를 한 건씩 판정** (`§5`·`§5.3`·`§6.3`·`§4`)

```bash
cd /home/pi/sop-project
grep -n '§5\b\|§5\.3\|§6\.3\|§4\b' docs/통합문서.md | sed -E 's/^(.{160}).*/\1…/'
```

판정 기준:
- `§5`(옛) → 문맥이 **블록도·통신 경로**면 `§13`, **부품**이면 `§14.1`, **버튼 배치**면 `§5.4`
- `§5.3`(옛) → **색↔단계·버튼 배치**면 `§5.4`, **부품 목록**이면 `§14.1`
- `§6.3`(옛) → **사용자 흐름**이면 `§5.3`, **운용 조작·결과창**이면 `§8`
- `§4`(옛) → **기능 요구사항(FR)**이면 `§4`, **NFR**이면 `§11`

🔴 `§4`·`§5` 는 **새 번호로도 존재하므로**, 이미 치환된 것인지 아직 안 된 것인지 구별해야 한다. 구별법 = Step 4 이후 남은 `§4`·`§5` 는 **전부 옛 번호**다(새 §4·§5 를 가리키는 참조는 이 시점에 없다 — 옛 문서에 그런 참조가 있었을 리 없다). 한 번에 치환하지 말고 **한 줄씩** 고친다.

- [ ] **Step 7: 봉인 해제**

```bash
cd /home/pi/sop-project
sed -i 's/§!/§/g' docs/통합문서.md
grep -c '§!' docs/통합문서.md   # 기대: 0
```

- [ ] **Step 8: 깨짐 0 확인** 🔴 관문

```bash
cd /home/pi/sop-project
# (1) 총 참조 수가 보존됐는가
echo "치환 후 참조: $(grep -o '§[0-9]\+' docs/통합문서.md | wc -l)"   # 기대: Task 0 과 동일(1166)
# (2) 없는 장을 가리키는 참조 — 17 이상
grep -o '§[0-9]\+' docs/통합문서.md | sed 's/§//' | sort -n -u | tail -3   # 기대: 최대 16
# (3) 없는 소절을 가리키는 참조 (외부 참조 제외한 뒤 눈으로 확인)
for r in $(grep -o '§[0-9]\+\.[0-9]\+' docs/통합문서.md | sort -u); do
  n=${r#§}; grep -q "^### ${n}" docs/통합문서.md || echo "깨짐 후보: $r"
done
```
🔴 (3)의 「깨짐 후보」는 **전부 외부 문서 참조여야 한다.** 하나라도 통합문서 자신을 가리키면 Step 6 으로 돌아간다.

- [ ] **Step 9: 커밋**

```bash
cd /home/pi/sop-project
git add docs/통합문서.md
git commit -m "$(cat <<'EOF'
docs(정본): 구조 재편 3/7 — 참조 치환 1,166곳 · 깨짐 0

2단계 치환(옛→§@N→새) · 외부 문서 참조 N곳은 봉인 후 원복
맨 참조(§4·§5·§5.3·§6.3)는 문맥 판정으로 한 건씩 치환
§12 소절 안 번호 유지 확인

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RepH4diWx23QZmPtxKFaCr
EOF
)"
```

---

## Task 4: 신설 §8 집필 — 제어·피드백 (인터락·타워램프·GUI)

**파일:** Modify `docs/통합문서.md` (§8 장)
**사실 출처:** 옛 §8 제어부 서술(이미 §7 로 흡수됨 — 그림은 §7 에 두고 §8 은 **제어 경로와 화면**을 쓴다) · 옛 §6.3 「운용 조작」 · `Rpi5/Demo/` 코드 · §12 갈래(인터락 측정 절) · §15 핀·채널

- [ ] **Step 1: 사실 수집 — 추측 금지**

```bash
cd /home/pi/sop-project
ls Rpi5/Demo/
grep -rn 'towerlamp\|tower_lamp\|타워램프' Rpi5/Demo/*.py | head -20
grep -rn 'interlock' Rpi5/Demo/*.py Rpi5/arduino/console_interlock/*.ino | head -20
grep -n 'GPIO_\|RELAY\|CH5' Rpi5/Demo/config.py | head -30
```
확인할 것: ①인터락 차단 경로(Pi → Arduino → 릴레이 → 무엇) ②타워램프 색·울림 조건 ③GUI 해제 버튼·키보드 우회 키 ④결과창·초기화의 실제 동작. **코드에서 확인 못 한 것은 `⏸ 미확인`.**

- [ ] **Step 2: §8 을 §2·§3 작성 방법으로 쓴다**

형식(설계 §5): **굵은 주장 한 줄** → `####` 묶음 → 「→」 요점 → 출처는 절 끝.

```markdown
## 8. 제어·피드백 — 인터락·타워램프·GUI

> 판정(§7)이 나온 **뒤**에 일어나는 일을 한곳에 모았다. 경고는 시청각, 차단은 전기다.

### 8.1 제어 경로 — 경고와 차단은 다른 층이다

**경고는 소리와 화면으로 나가고, 차단은 전기를 끊는다.** 둘은 같은 판정에서 갈라지지만 경로가 다르다.

#### 경고 경로 (WARNING)
→ …

#### 차단 경로 (BLOCK)
→ RPi5 → Arduino(Serial) → 릴레이 → 부하. 채널 배정 = §15 「핀·채널 배정」 표(정본).

*(측정·실동작 상태 = §12 `[CURRENT]` · 🔓 미결)*

### 8.2 작업 화면 (GUI) — 작업자가 보는 것

### 8.3 운용 조작 — 작업 흐름 밖에서 사람이 부르는 것
(옛 §6.3 의 ① ↺ 작업 초기화 · ② 작업 완료 결과창 원문을 여기로. 🔴 「만들지 않기로 결정한 것」 3줄은 반드시 함께 옮긴다.)

### 8.4 키보드 우회

### ⏸ 이 장에서 아직 안 된 것
```

🔴 **수치는 쓰지 않는다** — 응답시간·차단 지연 등은 `§12 참조` 포인터만(R5).

- [ ] **Step 3: 검증**

```bash
cd /home/pi/sop-project
A=$(grep -n '^## 8\.' docs/통합문서.md | cut -d: -f1); B=$(grep -n '^## 9\.' docs/통합문서.md | cut -d: -f1)
sed -n "${A},${B}p" docs/통합문서.md          # 눈으로 읽는다
# 수치가 새로 들어가지 않았는가 (ms·fps·% 는 §12 에만)
sed -n "${A},${B}p" docs/통합문서.md | grep -n '[0-9]\+\s*\(ms\|fps\|%\|초\)'   # 기대: 출력 없음(있으면 §12 포인터로 바꾼다)
```

- [ ] **Step 4: 커밋**

```bash
cd /home/pi/sop-project
git add docs/통합문서.md
git commit -m "$(cat <<'EOF'
docs(정본): 구조 재편 4/7 — 신설 §8 제어·피드백(인터락·타워램프·GUI)

옛 §8 제어부 + 옛 §6.3 운용 조작·결과창·키보드 우회를 한 장으로
수치는 §12 포인터 · 코드로 확인 못 한 것은 ⏸ 미확인

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RepH4diWx23QZmPtxKFaCr
EOF
)"
```

---

## Task 5: 신설 §9 집필 — 음성비서

**파일:** Modify `docs/통합문서.md` (§9 장)
**사실 출처:** `Rpi5/Demo/voice/` 문서 · `docs/superpowers/specs/2026-08-13-음성비서-design.md` · `specs/`·`plans/` 음성비서 문서 · §12 갈래⑥(옛 §10.45·46·49·50·51·52·53·55·59·61·62) · 부품 = §14.3

- [ ] **Step 1: 사실 수집**

```bash
cd /home/pi/sop-project
ls Rpi5/Demo/voice/ && head -40 Rpi5/Demo/voice/시연절차.md
sed -n '/^## 📂 갈래 ⑥/,/^## 11\.\|^## 14\./p' docs/통합문서.md | grep -n '^### \|^> ✅ CURRENT' | head -40
grep -n '음성\|TTS\|STT\|핫워드\|호출어' docs/통합문서.md | grep -n 'CURRENT' | head
```

- [ ] **Step 2: §9 집필** — ✅ **중간 수준 확정**(사용자 2026-09-18)

```markdown
## 9. 음성비서

> 작업자가 손을 쓰지 못하는 상황에서 **묻고 듣는** 경로. 갈래 A(규칙 기반)와 B(LLM)로 나뉜다.

### 9.1 무엇을 하는 기능인가
**작업자가 「지금 뭐 하지?」라고 물으면 현재 단계와 다음 조작을 말로 답한다.**
→ 입력: 호출어 → STT → (A: 규칙 / B: LLM) → TTS → 글라스 스피커
*(측정 = §12 갈래⑥ · 부품 = §14.3)*

### 9.2 갈래 A — 규칙 기반
### 9.3 갈래 B — LLM
### 9.4 처리 경로 (호출어 · STT · 생성 · TTS)
### 9.5 관문 상태
| 관문 | 상태 | 근거 |
(🔴 결과만. 수치는 §12 포인터)

### ⏸ 이 장에서 아직 안 된 것
```

🔴 **수치 금지** — 지연·CER·정확도는 전부 `§12.N 참조`.

- [ ] **Step 3: 검증** — Task 4 Step 3 과 같은 방식(§9 구간에 `ms`·`%`·`CER` 수치가 없어야 한다).

- [ ] **Step 4: 커밋**

```bash
cd /home/pi/sop-project
git add docs/통합문서.md
git commit -m "$(cat <<'EOF'
docs(정본): 구조 재편 5/7 — 신설 §9 음성비서 (기능 설명 신규)

갈래 A(규칙)/B(LLM) · 처리 경로 · 관문 상태
사실 출처 = Rpi5/Demo/voice · §12 갈래⑥ · 부품 §14.3 포인터
수치는 전부 §12 참조 · 확인 못 한 것은 ⏸ 미확인

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RepH4diWx23QZmPtxKFaCr
EOF
)"
```

---

## Task 6: R12 자리 만들기 — 각 장 끝 `⏸ 이 장에서 아직 안 된 것`

> 문서 규칙 R12 자체는 이미 정본에 있다(커밋 `4df7914`). 이번 태스크는 **R12 가 말하는 자리를 실제로 만드는 것**이다.

**파일:** Modify `docs/통합문서.md`

- [ ] **Step 1: R12 본문을 다시 읽어 자리 이름을 정확히 맞춘다**

```bash
cd /home/pi/sop-project
grep -n 'R12' docs/통합문서.md | head
sed -n "$(grep -n '\*\*R12' docs/통합문서.md | head -1 | cut -d: -f1),+12p" docs/통합문서.md
```

- [ ] **Step 2: 기능 장 §5~§10 끝에 같은 이름의 절을 단다**

```markdown
### ⏸ 이 장에서 아직 안 된 것

- (없음)
```
채울 내용이 있으면 옮긴다 — 재편 과정에서 본문 안에 섞여 있던 「검토 중」·「미구현」 표기를 여기로 모은다. 🔴 **확장으로 확정된 것은 §10 으로** 보낸다(R12 ③).

- [ ] **Step 3: §12 는 손대지 않는다** — 기존 규약(갈래 끝 새 절 · `[CURRENT]` · 🔓 미결) 유지(R12 ④).

- [ ] **Step 4: 검증**

```bash
cd /home/pi/sop-project
grep -c '^### ⏸ 이 장에서 아직 안 된 것' docs/통합문서.md   # 기대: 6 (§5~§10)
# 각 장에 하나씩 있는가
awk '/^## [0-9]+\./{ch=$2} /^### ⏸ 이 장에서/{print ch}' docs/통합문서.md   # 기대: 5. 6. 7. 8. 9. 10.
```

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project
git add docs/통합문서.md
git commit -m "$(cat <<'EOF'
docs(정본): 구조 재편 6/7 — 기능 장 §5~§10 에 R12 「⏸ 이 장에서 아직 안 된 것」 자리

본문에 섞여 있던 검토 중·미구현 표기를 그 절로 모음
확장 확정 항목은 §10 으로 이동

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RepH4diWx23QZmPtxKFaCr
EOF
)"
```

---

## Task 7: 목차 · 대조표 · 바깥 문서 · 자가 점검

**파일:** Modify `docs/통합문서.md`(목차·§16) · `README.md` · `CLAUDE.md` · `Rpi5/CLAUDE.md` · `docs/claude-code-작업문서.md` · `.claude/skills/*/SKILL.md` · `.claude/hooks/*` · `Rpi5/Demo/docs/*.md` · `Rpi5/Demo/voice/*.md`

- [ ] **Step 1: 목차를 새 구조로 다시 쓴다**

`## 목차`(38줄 부근)를 묶음 머리 4개 + 장 16개 + §12 갈래 색인 포인터로 고친다. 실제 장 머리와 기계 대조:

```bash
cd /home/pi/sop-project
grep '^## ' docs/통합문서.md | head -30
```

- [ ] **Step 2: §16 부록에 「옛↔새 장번호 대조표」를 넣는다** (함정 B · 결정 D1)

```markdown
### 16.N 옛↔새 장번호 대조표 (2026-09-18 구조 재편)

> 🔴 **작업로그·설계 문서(`docs/superpowers/`)·정본검토 기록의 `§N` 표기는 재편 전 번호다** — 고치지 않았다(그때의 기록이므로). 옛 번호를 만나면 이 표로 옮겨 읽는다.

| 옛 | 새 |
|---|---|
(위 「옛 → 새 대조표」 장 단위 표를 그대로)
```

- [ ] **Step 3: 바깥 길잡이 문서의 참조를 치환**

같은 `SED1`·`SED2` 를 쓰되, **파일마다 먼저 외부 참조를 눈으로 확인**한다(CLAUDE.md 안의 `CLAUDE.md §3`·`§5` 는 자기 자신 참조 → **고치면 안 된다**).

```bash
cd /home/pi/sop-project
for f in README.md CLAUDE.md docs/claude-code-작업문서.md Rpi5/CLAUDE.md \
         .claude/skills/work-close/SKILL.md .claude/skills/session-wrap/SKILL.md \
         .claude/skills/session-resume/SKILL.md \
         Rpi5/Demo/docs/TESTING_FSM.md Rpi5/Demo/docs/augmentation_plan.md \
         Rpi5/Demo/docs/dataset_pipeline.md Rpi5/Demo/voice/LLM관문.md Rpi5/Demo/voice/시연절차.md \
         Rpi5/Demo/dataset/cleanroom_eval_20260720/ALLOCATION.md; do
  echo "=== $f"; grep -n '§[0-9]' "$f" | sed -E 's/^(.{140}).*/\1…/'
done
```
🔴 `CLAUDE.md §N`·`README §N`·`결선도 §N` 처럼 **다른 문서를 가리키는 것은 건너뛴다.** 통합문서를 가리키는 것만 고친다.

- [ ] **Step 4: 훅 파일은 코드다 — 문자열만 확인**

```bash
cd /home/pi/sop-project
grep -n '§[0-9]' .claude/hooks/*.py .claude/hooks/*.sh
```
통합문서 장 번호를 **하드코딩**한 곳이 있으면 고치고, 테스트를 돌린다:
```bash
cd /home/pi/sop-project && python3 -m pytest .claude/hooks/ -q 2>/dev/null || python3 .claude/hooks/test_banner.py && python3 .claude/hooks/test_session_archive.py
```

- [ ] **Step 5: 자가 점검 — 관문 6개** 🔴 이 출력을 사용자에게 그대로 보고한다

```bash
cd /home/pi/sop-project
echo "① 장 번호 1..16 한 번씩"; grep -o '^## [0-9]\+\.' docs/통합문서.md | sort -V | uniq -c
echo "② 묶음 머리 4개"; grep -c '^## [ⅠⅡⅢ]\.\|^## 부록' docs/통합문서.md
echo "③ 참조 총수(재편 전 1166)"; grep -o '§[0-9]\+' docs/통합문서.md | wc -l
echo "④ 17 이상 참조(0이어야)"; grep -o '§[0-9]\+' docs/통합문서.md | sed 's/§//' | sort -n | awk '$1>16' | wc -l
echo "⑤ 측정 절 수(재편 전과 동일해야)"; grep -c '^### 12\.' docs/통합문서.md
echo "⑥ R12 자리 6개"; grep -c '^### ⏸ 이 장에서 아직 안 된 것' docs/통합문서.md
echo "⑦ 잔여 표식(0이어야)"; grep -c '§@\|§!' docs/통합문서.md
```

- [ ] **Step 6: 커밋**

```bash
cd /home/pi/sop-project
git add docs/통합문서.md README.md CLAUDE.md docs/claude-code-작업문서.md .claude/skills .claude/hooks
git commit -m "$(cat <<'EOF'
docs(정본): 구조 재편 7/7 — 목차·옛↔새 대조표(§16)·바깥 문서 참조·자가 점검

목차 재작성 · §16 에 옛↔새 장번호 대조표(역사 기록은 옛 번호 유지)
README·CLAUDE.md·스킬·훅의 통합문서 참조 치환(다른 문서 참조는 제외)
자가 점검 7항 통과

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RepH4diWx23QZmPtxKFaCr
EOF
)"
cd /home/pi/sop-project/Rpi5 && git add CLAUDE.md Demo/docs Demo/voice Demo/dataset && git commit -m "$(cat <<'EOF'
docs: 통합문서 구조 재편에 따른 §N 참조 치환

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RepH4diWx23QZmPtxKFaCr
EOF
)"
```

- [ ] **Step 7: 공동 확인 → `work-close`**

자가 점검 출력과 새 목차를 사용자에게 보고하고 **확인을 받은 뒤** `work-close` 로 마무리한다. 🔴 Claude 단독 완료 선언 금지(CLAUDE.md §2).

---

## 🟦 미결 2 — 사용자 결정 필요

설계 §7 이 남긴 두 가지다. **Task 5 전까지** 정하면 된다.

**미결 ① — ✅ 결정됨(사용자 2026-09-18): 중간**
- **중간(채택)** — 기능 설명 + 갈래 A/B + 처리 경로 + 관문 상태 표(위 Step 2 골격). 수치는 전부 §12 포인터.
- 얕게: 기능 한 문단 + §12 갈래⑥ 포인터만
- 깊게: 위 + 프롬프트 설계·핫워드·모델 선정 근거까지

**미결 ②(Task 2 에 영향 · 예고) — 옛 §6.2 흐름도와 옛 §9.2 상태도를 합치나?**
- 재편 뒤 새 §5.2(흐름)와 새 §7.2(상태도)로 **다른 장**에 놓인다. 합치면 한 장이 다른 장 그림을 품는다.
- **기본안 = 합치지 않는다**(각 장의 자기 그림으로 둔다). 장별 검토 때 다시 본다.
