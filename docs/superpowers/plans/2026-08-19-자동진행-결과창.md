# 자동 진행 · 결과창 · 오버레이 표시 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 「다음 단계 진행」 버튼 없이 조건 충족 시 자동으로 단계가 넘어가고, 마지막 단계 후 작업 결과 안내창이 뜨며, 탐지 박스를 끄고 켜고 색으로 구분하고, 공구 문구가 눈에 띄게 만든다.

**Architecture:** FSM(`fsm.py`)과 진행 조건(`sub_task.can_advance`)은 **그대로 둔다.** 지금 「버튼 클릭」이 쥐고 있던 방아쇠를 「조건 충족 순간」으로 옮기는 것이 전부다. 집계(`session_stats.py`)와 결과 UI(`overlay_result.py`)는 새 파일로 분리한다 — `safety_console.py` 가 이미 1,495줄이라 여기에 더 넣지 않는다.

**Tech Stack:** Python 3 · PyQt6 · OpenCV(cv2) · 라즈베리파이 5

**Spec:** `docs/superpowers/specs/2026-08-19-자동진행-결과창-design.md`

## Global Constraints

- **repo 가 둘이다.** 코드(`Rpi5/Demo/**`)는 **`Rpi5` repo** 에 커밋한다(`cd ~/sop-project/Rpi5`). 문서(`docs/**`)만 `sop-project` 에 커밋한다.
- 커밋 끝에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` 를 붙인다. push 는 사용자가 요청할 때만 한다.
- **테스트에 pytest 를 쓰지 않는다.** 이 프로젝트의 selftest 는 `check(cond, msg)` 헬퍼를 쓰는 **단독 실행 스크립트**다. 실행: `python3 selftest/test_x.py`, 실패 시 `sys.exit(1)`. 기존 파일(`selftest/test_sub_task.py`)의 형식을 그대로 따른다.
- 모든 selftest 는 `Demo/` 디렉터리에서 실행한다: `cd ~/sop-project/Rpi5/Demo`.
- **시간을 인자로 주입한다.** `now=None` 이면 실시간. 테스트가 10초를 실제로 기다리면 안 된다.
- **`fsm.py` · `sub_task.py` · `tool_state.py` · `tool_gate.py` · `interlock.py` · `detector.py` 를 수정하지 않는다.**
- 색 값은 spec §5.1 표가 정본이다. 임의로 바꾸지 않는다.
- 애니메이션은 `config.UI_ANIMATION`(`SOP_UI_ANIM=0`)으로 통째로 꺼져야 한다.
- 🔴 **크기·두께를 애니메이션하지 않는다** — 레이아웃이 밀려 글자가 흔들린다(`anim.py` 규칙).

---

## 파일 구조

| 파일 | 책임 | 상태 |
| --- | --- | --- |
| `Demo/session_stats.py` | 한 번의 작업에서 일어난 일을 모은다. Qt 비의존 | **신규** |
| `Demo/overlay_result.py` | 결과 안내창 UI (`ResultPanel`) | **신규** |
| `Demo/recipe.json` | 대기 시간 30 → 10 | 수정 |
| `Demo/safety_console.py` | 자동 진행 · CTA 단순화 · 결과창 호출 · 집계 배선 · 설정 배선 | 수정 |
| `Demo/camera_thread.py` | `hand_signal` · `set_draw_boxes` · 클래스별 박스 색 | 수정 |
| `Demo/config.py` | `SHOW_DETECT_BOXES` · `DETECT_BOX_COLORS` · `TOOL_BOX_COLORS` | 수정 |
| `Demo/overlay.py` | `GaugePanel` 공구 칸 = 검정 판 + 맥박 | 수정 |
| `Demo/overlay_menu.py` | `SettingsPanel` 에 「탐지 박스 표시」 | 수정 |
| `Demo/theme.py` | `tool_plate` · `tool_plate_text` 토큰 | 수정 |
| `Demo/anim.py` | `TextPulse` | 수정 |
| `Demo/selftest/test_session_stats.py` | 집계 검증 | **신규** |
| `Demo/selftest/test_box_colors.py` | 색표·BGR 변환 검증 | **신규** |
| `Demo/selftest/test_recipe.py` · `test_console_flow.py` · `test_overlay.py` · `test_anim.py` · `test_imports.py` | 보강 | 수정 |

**작업 순서의 이유**: 위험이 낮고 서로 독립인 것(레시피 → 자동 진행)을 먼저 끝내 흐름을 살려 놓고, 표시(색 → 토글 → 판 → 맥박)를 얹은 뒤, 마지막에 집계·결과창을 붙인다. 결과창은 앞의 모든 것이 돌아야 내용이 채워진다.

---

### Task 1: 대기 시간 30초 → 10초

**Files:**
- Modify: `Rpi5/Demo/recipe.json`
- Test: `Rpi5/Demo/selftest/test_recipe.py`

**Interfaces:**
- Consumes: 없음 (첫 작업)
- Produces: `recipe.json` 의 B1·B2·B3 `sub.sec == 10`. Task 2 의 시나리오 테스트가 이 값을 전제한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`selftest/test_recipe.py` 맨 아래 `if __name__` 블록 **위**에 함수를 추가한다:

```python
def test_wait_seconds_are_ten():
    """🔴 서브 작업 대기는 10초다 (2026-08-19 설계 §1-②).

    30초는 시연이 늘어져 못 쓴다. 값이 되돌아가면 여기서 잡는다.
    """
    print("\n[N] 대기 시간")
    r = load_recipe()
    for s in r["steps"]:
        sub = s.get("sub")
        if not sub:
            continue
        check(sub["sec"] == 10,
              f"{s['button']} 대기 {sub['sec']}초 — 10초여야 한다")
```

그리고 `if __name__ == "__main__":` 블록의 함수 호출 목록에 `test_wait_seconds_are_ten()` 을 추가한다.

- [ ] **Step 2: 실패를 확인한다**

```bash
cd ~/sop-project/Rpi5/Demo && python3 selftest/test_recipe.py
```
Expected: FAIL — `B1 대기 30초 — 10초여야 한다`

- [ ] **Step 3: 레시피를 고친다**

`recipe.json` 의 `"sec": 30` 세 곳(B1·B2·B3)을 `"sec": 10` 으로 바꾼다. 다른 값은 건드리지 않는다.

- [ ] **Step 4: 통과를 확인한다**

```bash
cd ~/sop-project/Rpi5/Demo && python3 selftest/test_recipe.py
```
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
cd ~/sop-project/Rpi5
git add Demo/recipe.json Demo/selftest/test_recipe.py
git commit -m "$(cat <<'EOF'
feat(레시피): 서브 작업 대기 30초 → 10초

시연이 늘어지지 않게 줄인다. 값이 되돌아가면 test_recipe 가 잡는다.
설계 = sop-project/docs/superpowers/specs/2026-08-19-자동진행-결과창-design.md §1

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: 자동 진행 — 「다음 단계 진행」 버튼 제거

**Files:**
- Modify: `Rpi5/Demo/safety_console.py` (`_update_sub_view` · `_on_cta` · `_sync_cta_visibility`)
- Test: `Rpi5/Demo/selftest/test_console_flow.py`

**Interfaces:**
- Consumes: Task 1 의 `sub.sec == 10`
- Produces:
  - `_update_sub_view()` — 조건 충족 시 스스로 `_finish_sub()` 를 부르고 리턴한다
  - `_on_cta()` — 「작업 시작」 전용. 서브작업 분기가 없다
  - `_sync_cta_visibility()` — `show = (self.fsm.state == State.IDLE)`
  - Task 8 이 `_sync_cta_visibility()` 에 결과창 조건을 얹는다

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`selftest/test_console_flow.py` 에 함수 둘을 추가한다:

```python
def test_auto_advance_without_button():
    """🔴 대기가 차면 **버튼 없이** 다음 단계로 간다 (설계 §2.1)."""
    print("\n[3-c] 자동 진행")
    win = make_console()
    win._on_cta()                                   # 작업 시작
    before = win.fsm.expected_step
    key(win, "1")                                   # B1 물리 버튼
    check(win.fsm.expected_step == before,
          f"누른 직후엔 기대단계 {before} 유지 — 서브 작업 중")
    win._sub.tick(now=time.time() + 999)            # 대기 시간을 채운다
    win._update_sub_view()                          # 타이머가 부르는 것과 같다
    check(win._sub is None, "조건 충족 → 서브 작업이 스스로 끝난다")
    check(win.fsm.expected_step == before + 1,
          f"버튼 없이 다음 단계 → {win.fsm.expected_step}")
    check(win.btn_cta.isHidden(), "「다음 단계 진행」 버튼은 뜨지 않는다")
    win.close()


def test_tool_step_waits_until_grasped():
    """🔴 B2 는 시간만 차서는 넘어가지 않는다 — 쥘 때까지 대기 (설계 §2.4)."""
    print("\n[3-d] 공구 대기")
    win = make_console()
    win._on_cta()
    key(win, "1")
    win._sub.tick(now=time.time() + 999)
    win._update_sub_view()                          # B1 자동 통과

    key(win, "2")                                   # B2 — 공구 요구
    before = win.fsm.expected_step
    win._sub.tick(now=time.time() + 999)
    win._update_sub_view()
    check(win._sub is not None and win._sub.is_active,
          "시간이 차도 공구가 없으면 안 넘어간다")
    check(win.fsm.expected_step == before, f"기대단계 {before} 유지")

    win._sub.set_tool(win._sub.want_tool)           # 렌치를 쥔다
    win._update_sub_view()
    check(win.fsm.expected_step == before + 1, "쥐는 순간 자동 진행")
    win.close()
```

`if __name__ == "__main__":` 블록의 호출 목록에 두 함수를 추가한다.

- [ ] **Step 2: 실패를 확인한다**

```bash
cd ~/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py
```
Expected: FAIL — `조건 충족 → 서브 작업이 스스로 끝난다` 가 실패한다(지금은 버튼을 눌러야 끝난다).

- [ ] **Step 3: `_update_sub_view()` 끝을 자동 진행으로 바꾼다**

`safety_console.py` 의 `_update_sub_view()` 끝부분을 **이 세 줄**

```python
        if sub.can_advance:
            self.btn_cta.setText("▶  다음 단계 진행")
        self._sync_cta_visibility()
```

에서 **이렇게** 바꾼다:

```python
        # 조건(시간 AND 공구)을 다 채우면 **버튼 없이 스스로** 진행한다.
        # 🔑 _finish_sub() 가 self._sub = None 을 먼저 하므로 _tick_sub() 의
        #    이른 리턴에 걸려 재진입하지 않는다. 별도 플래그를 두지 않는다.
        if sub.can_advance:
            self._finish_sub()
            return
        self._sync_cta_visibility()
```

- [ ] **Step 4: `_on_cta()` 를 「작업 시작」 전용으로 줄인다**

```python
    def _on_cta(self):
        """화면 정중앙 버튼 — **「작업 시작」 전용**이다.

        🔴 「다음 단계 진행」은 2026-08-19 에 사라졌다 — 조건을 채우면
           _update_sub_view() 가 스스로 진행한다(설계 §2.1).
        """
        self._on_start_process()
        self.btn_cta.hide()
```

- [ ] **Step 5: `_sync_cta_visibility()` 의 서브작업 분기를 없앤다**

`if self._sub is not None and self._sub.is_active:` ~ `show = self.fsm.state == State.IDLE` 의 if/else 를 **한 줄**로 바꾼다:

```python
        # 🔑 서브 작업 분기는 없다 — 진행은 자동이라 띄울 버튼이 없다(설계 §2.2).
        show = self.fsm.state == State.IDLE
        self.btn_cta.setVisible(show)
```

docstring 의 "게이지가 덜 찼을 때" 예시는 더 이상 맞지 않으므로 "작업이 진행 중일 때" 로 고친다.

- [ ] **Step 6: 기존 시나리오 테스트를 새 흐름에 맞춘다**

`test_scenario_runs_to_end()` 의 서브작업 블록에서 `win._finish_sub()` 줄을 지운다 — `_update_sub_view()` 가 이미 끝냈으므로 두 번 부를 필요가 없다. 그 위의 `check(win._sub.can_advance, ...)` 도 지운다(`_update_sub_view()` 뒤에는 `_sub` 가 None 이라 참조하면 터진다). 결과는 이렇게 된다:

```python
        if win._sub is not None and win._sub.is_active:
            check(win.fsm.expected_step == before,
                  f"{btn} 누름 → 서브 작업 중, 기대단계 {before} 유지")
            win._sub.tick(now=time.time() + 999)    # 시간 채움
            if win._sub.needs_tool:
                win._sub.set_tool(win._sub.want_tool)
            win._update_sub_view()                  # 조건 충족 → 자동 진행
```

`test_tool_signal_drives_gate()` 안의 `win._finish_sub()` 도 같은 이유로 지우고 그 앞줄을 `win._update_sub_view()` 로 바꾼다.

- [ ] **Step 7: 통과를 확인한다**

```bash
cd ~/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py
```
Expected: PASS (전 항목)

- [ ] **Step 8: 커밋**

```bash
cd ~/sop-project/Rpi5
git add Demo/safety_console.py Demo/selftest/test_console_flow.py
git commit -m "$(cat <<'EOF'
feat(ui): 「다음 단계 진행」 버튼 제거 — 조건 충족 시 자동 진행

버튼 클릭이 쥐고 있던 방아쇠를 can_advance 충족 순간으로 옮긴다.
FSM 과 진행 조건(시간 AND 공구)은 그대로다. CTA 는 「작업 시작」 전용이 된다.

⚠️ 위반 감지 창이 무한 → 대기 시간(10초)으로 줄어든다(설계 §2.3).
설계 = sop-project/docs/superpowers/specs/2026-08-19-자동진행-결과창-design.md §2

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: 탐지 박스 클래스별 색상

**Files:**
- Modify: `Rpi5/Demo/config.py` · `Rpi5/Demo/camera_thread.py`
- Test: `Rpi5/Demo/selftest/test_box_colors.py` (신규)

**Interfaces:**
- Consumes: 없음
- Produces:
  - `config.DETECT_BOX_COLORS: dict[str, str]` — 버튼·EMO 클래스명 → `#RRGGBB`
  - `config.TOOL_BOX_COLORS: dict[str, str]` — 공구 키 → `#RRGGBB`
  - `config.DETECT_BOX_FALLBACK: str`
  - `camera_thread.box_bgr(name, tool=False) -> tuple[int,int,int]` — Task 4 도 이 함수를 쓴다

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`selftest/test_box_colors.py` 를 새로 만든다:

```python
"""탐지 박스 색표 검증.

실행: python3 Demo/selftest/test_box_colors.py

정본: 상위 docs/superpowers/specs/2026-08-19-자동진행-결과창-design.md §5

🔴 cv2 는 BGR 이다. 16진수를 뒤집는 곳이 두 군데가 되면 반드시 한쪽이 틀린다 —
   변환은 box_bgr() 한 곳에서만 한다. 이 테스트가 그것을 지킨다.
"""

import os
import sys

_DEMO_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _DEMO_DIR)

import config
from camera_thread import box_bgr

_fails = []


def check(cond, msg):
    print(("  ✅ " if cond else "  ❌ ") + msg)
    if not cond:
        _fails.append(msg)


def test_hex_to_bgr():
    """16진수 → BGR 튜플. RGB 로 뒤집히면 여기서 잡힌다."""
    print("\n[1] BGR 변환")
    check(box_bgr("B1") == (0, 212, 255), f"B1 노랑 #FFD400 → BGR {box_bgr('B1')}")
    check(box_bgr("B4") == (255, 125, 46), f"B4 파랑 #2E7DFF → BGR {box_bgr('B4')}")
    check(box_bgr("B2") == (255, 255, 255), "B2 흰 → (255,255,255)")


def test_every_button_class_has_color():
    """모델이 내는 5클래스가 전부 표에 있는가."""
    print("\n[2] 버튼 5클래스")
    for name in ("B1", "B2", "B3", "B4", "EMO"):
        check(name in config.DETECT_BOX_COLORS, f"{name} 색 있음")


def test_every_recipe_tool_has_color():
    """레시피가 쓰는 공구가 전부 표에 있는가."""
    print("\n[3] 공구")
    from recipe import load_recipe
    for s in load_recipe().get("steps", []):
        for key in (s.get("sub") or {}).get("tools", []):
            check(key in config.TOOL_BOX_COLORS, f"{key} 색 있음")


def test_unknown_falls_back():
    """표에 없는 이름은 죽지 않고 폴백색을 쓴다."""
    print("\n[4] 폴백")
    check(box_bgr("B9") == box_bgr_of_hex(config.DETECT_BOX_FALLBACK),
          "모르는 클래스 → 폴백색")


def box_bgr_of_hex(h):
    h = h.lstrip("#")
    return (int(h[4:6], 16), int(h[2:4], 16), int(h[0:2], 16))


if __name__ == "__main__":
    test_hex_to_bgr()
    test_every_button_class_has_color()
    test_every_recipe_tool_has_color()
    test_unknown_falls_back()
    print()
    if _fails:
        print(f"❌ 실패 {len(_fails)}건")
        for m in _fails:
            print(f"   - {m}")
        sys.exit(1)
    print("✅ 박스 색표 검증 통과")
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd ~/sop-project/Rpi5/Demo && python3 selftest/test_box_colors.py
```
Expected: FAIL — `ImportError: cannot import name 'box_bgr'`

- [ ] **Step 3: `config.py` 에 색표를 넣는다**

`[YOLO 설정]` 절 아래에 추가한다:

```python
# =============================================================================
# [탐지 박스 표시] — 정본: 상위 specs/2026-08-19-자동진행-결과창-design.md §5
# =============================================================================
# 🔴 여기는 **cv2 가 영상에 직접 그리는** 색이다. theme.py(Qt 오버레이 테마)와
#    성격이 다르고 테마 전환을 따르지 않는다 — 영상에는 테마가 없다.
# 버튼 색은 **실물 버튼 색 그대로**다(통합문서 §5.3). 화면과 손 앞의 물건이
# 같은 색이라야 "저 노란 버튼"이 바로 이어진다.
DETECT_BOX_COLORS = {
    "B1":  "#FFD400",   # 노랑
    "B2":  "#FFFFFF",   # 흰
    "B3":  "#FF69B4",   # 핑크
    "B4":  "#2E7DFF",   # 파랑 (파랑 원 스티커, 통합문서 §10.12)
    "EMO": "#FF3B30",   # 빨강 (비상정지)
}
# 공구는 위 5색과 겹치지 않게 고른다 — 겹치면 버튼과 헷갈린다.
TOOL_BOX_COLORS = {
    "driver": "#00E5FF",   # 시안
    "wrench": "#B36BFF",   # 보라
    "pliers": "#7CFF4F",   # 라임
}
DETECT_BOX_FALLBACK = "#00FF00"   # 표에 없는 클래스 — 죽지 않고 초록으로 그린다
```

- [ ] **Step 4: `camera_thread.py` 에 `box_bgr()` 를 넣고 그리는 곳을 고친다**

모듈 최상단(클래스 정의 앞)에 추가한다:

```python
def box_bgr(name, tool=False):
    """클래스 이름 → cv2 색(BGR).

    🔴 16진수를 뒤집는 곳은 **여기 하나뿐이다.** 두 군데가 되면 반드시 한쪽이
       틀린다(cv2 는 RGB 가 아니라 BGR 이다).
    색표 정본 = config.DETECT_BOX_COLORS · TOOL_BOX_COLORS (설계 §5.1)
    """
    table = config.TOOL_BOX_COLORS if tool else config.DETECT_BOX_COLORS
    h = table.get(name, config.DETECT_BOX_FALLBACK).lstrip("#")
    return (int(h[4:6], 16), int(h[2:4], 16), int(h[0:2], 16))
```

`_draw_yolo()` 의 두 줄을 바꾼다:

```python
    def _draw_yolo(self, frame, tracks):
        for t in tracks:
            cls_id = t['cls']
            x1, y1, x2, y2 = t['box']
            name = _detector.class_name(cls_id)
            color = box_bgr(name)
            label = f"{name} {t['score']:.2f}"
            cv2.rectangle(frame, (x1, y1), (x2, y2), color, 2)
            cv2.putText(frame, label, (x1, y1 - 6),
                        cv2.FONT_HERSHEY_SIMPLEX, 0.5, color, 2)
        return frame
```

`_draw_tools()` 의 `(0, 165, 255)` 두 곳을 `box_bgr(name, tool=True)` 로 바꾸고, 주석
"🔴 버튼(초록)과 헷갈리지 않게 **주황**으로 그린다." 를 다음으로 고친다:

```python
        🔴 공구는 **종류별 색**이고 버튼 5색과 겹치지 않는다(설계 §5.1).
```

`UsbCameraThread._process_frame()` 안의 `(0, 255, 0)` 두 곳도 같은 방식으로 바꾼다:

```python
            for t in tracks:
                x1, y1, x2, y2 = t['box']
                name = _detector.class_name(t['cls'])
                color = box_bgr(name)
                label = f"{name} {t['score']:.2f}"
                cv2.rectangle(frame, (x1, y1), (x2, y2), color, 2)
                cv2.putText(frame, label, (x1, y1 - 6),
                            cv2.FONT_HERSHEY_SIMPLEX, 0.5, color, 2)
```

- [ ] **Step 5: 통과를 확인한다**

```bash
cd ~/sop-project/Rpi5/Demo && python3 selftest/test_box_colors.py && python3 selftest/test_imports.py
```
Expected: 둘 다 PASS

- [ ] **Step 6: 커밋**

```bash
cd ~/sop-project/Rpi5
git add Demo/config.py Demo/camera_thread.py Demo/selftest/test_box_colors.py
git commit -m "$(cat <<'EOF'
feat(ui): 탐지 박스를 클래스별 색으로 — 버튼은 실물 색, 공구는 종류별

전부 초록 한 색이라 무엇이 무엇인지 구분되지 않았다.
BGR 변환은 box_bgr() 한 곳에서만 한다 — 두 곳이 되면 한쪽이 틀린다.
설계 = sop-project/docs/superpowers/specs/2026-08-19-자동진행-결과창-design.md §5

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: 탐지 박스 표시 켜기·끄기

**Files:**
- Modify: `Rpi5/Demo/config.py` · `Rpi5/Demo/camera_thread.py` · `Rpi5/Demo/overlay_menu.py` · `Rpi5/Demo/safety_console.py`
- Test: `Rpi5/Demo/selftest/test_console_flow.py`

**Interfaces:**
- Consumes: Task 3 의 `box_bgr()`
- Produces:
  - `config.SHOW_DETECT_BOXES: bool`
  - `CameraThread.set_draw_boxes(on: bool)` · `CameraThread.draw_boxes() -> bool` (`UsbCameraThread` 에도 같은 이름으로)
  - `SettingsPanel.boxes_changed = pyqtSignal(bool)` · `SettingsPanel._box_buttons: dict[bool, QRadioButton]`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`selftest/test_console_flow.py` 에 추가한다:

```python
def test_detect_box_toggle():
    """🔴 설정에서 탐지 박스를 끄면 두 카메라 스레드에 모두 전달되는가.

    🔑 표시만 끄는 것이다 — 끈 상태에서도 순서 위반 판정은 그대로 돈다(설계 §4.2).
    """
    print("\n[N] 탐지 박스 토글")
    win = make_console()
    check(win.camera_thread.draw_boxes() is True, "기본은 켜짐")

    win.settings_panel._box_buttons[False].setChecked(True)
    check(win.camera_thread.draw_boxes() is False, "끄면 ESP32 스레드에 전달")
    check(win.usb_camera_thread.draw_boxes() is False, "끄면 USB 스레드에도 전달")

    # 끈 상태에서도 판정은 살아 있다
    win._on_cta()
    before = win.fsm.expected_step
    key(win, "1")
    check(win._sub is not None and win._sub.is_active,
          "박스를 꺼도 서브 작업이 시작된다 — 판정은 표시와 무관하다")
    check(win.fsm.expected_step == before, "기대단계 유지")

    win.settings_panel._box_buttons[True].setChecked(True)
    check(win.camera_thread.draw_boxes() is True, "다시 켜진다")
    win.close()
```

`if __name__` 블록에 호출을 추가한다.

- [ ] **Step 2: 실패를 확인한다**

```bash
cd ~/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py
```
Expected: FAIL — `AttributeError: 'CameraThread' object has no attribute 'draw_boxes'`

- [ ] **Step 3: `config.py` 에 초기값을 넣는다**

Task 3 에서 만든 `[탐지 박스 표시]` 절 맨 위에 추가한다:

```python
# 탐지 박스를 화면에 그릴지. 🔴 **표시만** 끈다 — 검출·판정은 그대로 돈다.
SHOW_DETECT_BOXES = True
```

- [ ] **Step 4: `camera_thread.py` 에 스위치를 단다**

`CameraThread.__init__` 에 `self._draw_boxes = config.SHOW_DETECT_BOXES` 를 추가하고, `set_active()` 옆에 메서드 둘을 넣는다:

```python
    def set_draw_boxes(self, on):
        """탐지 박스·손 랜드마크를 그릴지. 🔴 **표시만** 바뀐다 — 검출·판정은 그대로."""
        with self._lock:
            self._draw_boxes = bool(on)

    def draw_boxes(self):
        with self._lock:
            return self._draw_boxes
```

`CameraThread._process_frame()` 에서 세 곳을 게이트한다:

```python
        with self._lock:
            is_active = self._is_active
            draw = self._draw_boxes
        ...
        if DETECTOR_AVAILABLE:
            dets = _detector.detect(frame)
            with self._lock:
                self._tracks = _update_tracks(self._tracks, dets)
                tracks = self._tracks
            if draw:
                frame = self._draw_yolo(frame, tracks)
            self.yolo_detections_signal.emit([...])          # 그대로

        # 🔴 draw_on=None 이어도 검출은 그대로 한다 — 반환값(손끝)은 ROI 판정에 쓴다.
        fingertip = self._hand.detect(frame, draw_on=frame if draw else None)
        ...
            if draw:
                frame = self._draw_tools(frame)
```

`UsbCameraThread` 에도 `self._draw_boxes` · `set_draw_boxes()` · `draw_boxes()` 를 같은 내용으로 넣고, `_process_frame()` 의 `cv2.rectangle`/`cv2.putText` 루프와 `self._hand.detect(...)` 를 같은 방식으로 게이트한다.

- [ ] **Step 5: `overlay_menu.py` 의 `SettingsPanel` 에 항목을 넣는다**

클래스 상단 시그널 선언에 추가한다:

```python
    boxes_changed = pyqtSignal(bool)
```

`__init__` 의 「공정 단계 패널 배경」 라디오 블록 **아래**, `lay.addStretch()` **위**에 넣는다:

```python
        self._box_caption = QLabel("탐지 박스 표시")
        self._box_caption.setFont(config.font("small", 700))
        lay.addWidget(self._box_caption)

        self._box_group = QButtonGroup(self)
        self._box_buttons = {}
        for on, text in ((True, "있음  (기본)"), (False, "없음  (영상만)")):
            rb = QRadioButton(text)
            rb.setFont(config.font("body", 600))
            rb.setCursor(Qt.CursorShape.PointingHandCursor)
            rb.setChecked(on == config.SHOW_DETECT_BOXES)
            rb.toggled.connect(lambda chk, v=on: chk and self.boxes_changed.emit(v))
            self._box_group.addButton(rb)
            self._box_buttons[on] = rb
            lay.addWidget(rb)

        # 🔴 이 문장을 빼면 "박스를 끄면 감시도 꺼지나?"를 반드시 묻게 된다(설계 §4.2).
        self._box_note = QLabel("ⓘ 표시만 끕니다 — 순서 위반 감지는 그대로 작동합니다")
        self._box_note.setFont(config.font("small"))
        self._box_note.setWordWrap(True)
        lay.addWidget(self._box_note)
```

`apply_theme()` 의 캡션 루프에 `self._box_caption` 을 추가하고, 라디오 QSS 를 먹이는 루프의 리스트에 `list(self._box_group.buttons())` 를 더한다. 안내문에 색을 준다:

```python
        self._box_note.setStyleSheet(theme.text_qss("label", 600))
```

- [ ] **Step 6: `safety_console.py` 에서 배선한다**

`settings_panel` 시그널 연결부에 추가한다:

```python
        self.settings_panel.boxes_changed.connect(self._on_boxes_changed)
```

`_on_panel_bg_changed()` 아래에 핸들러를 넣는다:

```python
    def _on_boxes_changed(self, on):
        """탐지 박스 표시 on/off — 🔴 **두 카메라 스레드 모두**에 전달한다.

        한쪽만 바꾸면 CCTV 로 전환했을 때 설정이 안 먹은 것처럼 보인다.
        """
        self.camera_thread.set_draw_boxes(on)
        self.usb_camera_thread.set_draw_boxes(on)
        self._append_log(f"[설정] 탐지 박스 표시 → {'있음' if on else '없음'}")
```

- [ ] **Step 7: 통과를 확인한다**

```bash
cd ~/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py && python3 selftest/test_overlay_menu.py
```
Expected: 둘 다 PASS

- [ ] **Step 8: 커밋**

```bash
cd ~/sop-project/Rpi5
git add Demo/config.py Demo/camera_thread.py Demo/overlay_menu.py Demo/safety_console.py Demo/selftest/test_console_flow.py
git commit -m "$(cat <<'EOF'
feat(ui): 탐지 박스 표시 켜기·끄기 — 설정 패널

버튼 박스·공구 박스·손 랜드마크를 함께 끈다(영상을 가리지 않고 보려는 목적).
🔴 표시만 끈다 — 검출·판정은 그대로 돈다. 그 문장을 설정 화면에도 적었다.
설계 = sop-project/docs/superpowers/specs/2026-08-19-자동진행-결과창-design.md §4

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: 공구 문구 검정 판

**Files:**
- Modify: `Rpi5/Demo/theme.py` · `Rpi5/Demo/overlay.py`
- Test: `Rpi5/Demo/selftest/test_overlay.py`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `theme.C("tool_plate")` → `"rgba(0, 0, 0, 0.50)"` · `theme.C("tool_plate_text")` → `"#ffffff"` (두 테마 공통)
  - `GaugePanel._tool_box` 가 판을 입는다. Task 6 이 이 위에 맥박을 얹는다

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`selftest/test_overlay.py` 에 추가한다:

```python
def test_tool_plate_does_not_clip_text():
    """🔴 공구 칸에 판을 깔아도 글자가 잘리지 않는가 (설계 §6.2).

    목업에서 실제로 잘렸다 — QSS padding 은 sizeHint 와 어긋난다.
    여백은 레이아웃 contentsMargins 로 줘야 한다.
    """
    print("\n[N] 공구 판")
    from sub_task import SubTask
    spec = {"type": "wait_tool", "sec": 10, "label": "N2 퍼지", "tool": "wrench",
            "tools": ["driver", "wrench", "pliers"],
            "tool_names": {"driver": "드라이버", "wrench": "렌치", "pliers": "플라이어"}}
    for name in ("dark", "light"):
        theme.set_theme(name)
        p = GaugePanel()
        p.apply_theme()
        p.update_view(SubTask(spec, now=0.0))
        p.adjustSize()
        need = (p._tool_text.sizeHint().height()
                + p._tool_state.sizeHint().height() + 16)   # 위아래 여백 8+8
        got = p._tool_box.sizeHint().height()
        check(got >= need, f"{name}: 공구 칸 높이 {got} ≥ 글자+여백 {need}")
        check("rgba(0, 0, 0, 0.50)" in p._tool_box.styleSheet(),
              f"{name}: 판 알파 0.50")
    theme.set_theme("dark")
```

`if __name__` 블록에 호출을 추가한다. 파일 상단에 `from overlay import GaugePanel` 이 없으면 추가한다.

- [ ] **Step 2: 실패를 확인한다**

```bash
cd ~/sop-project/Rpi5/Demo && QT_QPA_PLATFORM=offscreen python3 selftest/test_overlay.py
```
Expected: FAIL — `판 알파 0.50` (스타일시트가 비어 있다)

- [ ] **Step 3: `theme.py` 에 토큰을 넣는다**

`THEMES["dark"]` 와 `THEMES["light"]` **양쪽 모두**에 같은 값으로 넣는다:

```python
        # 공구 문구 판 — 🔴 다크·화이트 **공통**이다. 대비 상대가 테마가 아니라
        #    **영상**이고, 영상에는 테마가 없다(설계 §6.3).
        "tool_plate":      "rgba(0, 0, 0, 0.50)",
        "tool_plate_text": "#ffffff",
```

- [ ] **Step 4: `overlay.GaugePanel` 에 판을 입힌다**

`__init__` 의 `tb.setContentsMargins(0, 8, 0, 0)` 를 다음으로 바꾼다:

```python
        # 🔴 여백을 QSS padding 으로 주지 않는다 — sizeHint 와 어긋나 글자가 잘린다
        #    (목업에서 실제로 잘렸다, 설계 §6.2).
        tb.setContentsMargins(10, 8, 10, 8)
```

그리고 `_tool_box` 생성 직후에 배경을 켠다:

```python
        self._tool_box = QWidget()
        # 🔴 맨 QWidget 은 이 속성 없이는 스타일시트 배경을 안 그린다(2026-08-04 확인).
        self._tool_box.setAttribute(Qt.WidgetAttribute.WA_StyledBackground, True)
```

`update_view()` 의 `if sub.needs_tool:` 블록 맨 앞에 판 스타일을 넣는다:

```python
        if sub.needs_tool:
            # 공구 문구 판 — 밝은 배경(노란 책상) 위에서 글자가 묻히는 것을 막는다.
            self._tool_box.setStyleSheet(
                f"background-color: {theme.C('tool_plate')}; border-radius: 6px;")
```

요구 문구의 색을 판 위 흰색으로 바꾼다 — `theme.text_qss("text", 800)` 을 다음으로:

```python
            self._tool_text.setStyleSheet(
                f"color: {theme.C('tool_plate_text')}; font-weight: 800;"
                f" border: none; background: transparent;")
```

- [ ] **Step 5: 스타일을 먼저, 배치를 나중에**

🔴 판이 붙으면 패널 높이가 늘어난다. `safety_console._update_sub_view()` 는 이미
`self.gauge_panel.update_view(sub)` 를 부른 **뒤** `self._relayout()` 을 부른다 —
순서가 맞다. 순서를 바꾸지 않는다. 이 사실을 `update_view()` 끝의 `self.show()` 위에
주석으로 남긴다:

```python
        # 🔴 호출부는 update_view() **뒤에** relayout 한다 — 판이 붙으면 높이가
        #    늘어나므로 순서가 반대면 판이 글자를 자른다(설계 §6.2).
        self.show()
```

- [ ] **Step 6: 통과를 확인한다**

```bash
cd ~/sop-project/Rpi5/Demo && QT_QPA_PLATFORM=offscreen python3 selftest/test_overlay.py && QT_QPA_PLATFORM=offscreen python3 selftest/test_theme.py
```
Expected: 둘 다 PASS
🔴 `test_theme.py` 가 새 토큰의 테마 간 대비를 문제 삼으면 **검사에 예외를 뚫지 말고**, 그 검사에서 `tool_plate*` 를 「영상 대비용 공통 토큰」으로 분류하고 설계 §6.3 을 주석으로 참조한다.

- [ ] **Step 7: 커밋**

```bash
cd ~/sop-project/Rpi5
git add Demo/theme.py Demo/overlay.py Demo/selftest/test_overlay.py
git commit -m "$(cat <<'EOF'
feat(ui): 공구 문구에 검정 판(50%) — 밝은 배경에서 묻히던 것

노란 책상 위에서 밝은 글자도 노랑도 묻혔다. 색만 바꿔서는 배경이 바뀌면 다시 묻힌다.
🔴 여백은 QSS padding 이 아니라 레이아웃 contentsMargins 로 준다 — 글자가 잘린다.
설계 = sop-project/docs/superpowers/specs/2026-08-19-자동진행-결과창-design.md §6

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: 공구 문구 맥박

**Files:**
- Modify: `Rpi5/Demo/anim.py` · `Rpi5/Demo/overlay.py`
- Test: `Rpi5/Demo/selftest/test_anim.py`

**Interfaces:**
- Consumes: Task 5 의 판·토큰
- Produces: `anim.TextPulse(widget, color_fn, floor=0.40, ms=D_TEXT_PULSE)` — `.start()` · `.stop()` · `.active()`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`selftest/test_anim.py` 에 추가한다:

```python
def test_text_pulse():
    """🔴 글자 맥박 — 시작·정지가 되고, 꺼져 있으면 최종값만 먹는가."""
    print("\n[N] 글자 맥박")
    from PyQt6.QtWidgets import QLabel
    lbl = QLabel("렌치가 필요합니다")

    config.UI_ANIMATION = True
    p = anim.TextPulse(lbl, lambda: "#ffffff")
    p.start()
    check(p.active(), "start() → 돈다")
    p.start()
    check(p.active(), "두 번 불러도 재시작하지 않는다")
    p.stop()
    check(not p.active(), "stop() → 멈춘다")

    config.UI_ANIMATION = False
    p2 = anim.TextPulse(lbl, lambda: "#ffffff")
    p2.start()
    check(not p2.active(), "애니메이션 OFF → 애니메이션을 만들지 않는다")
    check("255,255,255" in lbl.styleSheet().replace(" ", ""),
          "OFF 여도 최종 색은 적용된다")
    config.UI_ANIMATION = True
```

`if __name__` 블록에 호출을 추가한다.

- [ ] **Step 2: 실패를 확인한다**

```bash
cd ~/sop-project/Rpi5/Demo && QT_QPA_PLATFORM=offscreen python3 selftest/test_anim.py
```
Expected: FAIL — `module 'anim' has no attribute 'TextPulse'`

- [ ] **Step 3: `anim.py` 에 `TextPulse` 를 넣는다**

지속시간 상수 옆에 추가한다:

```python
D_TEXT_PULSE = 1600   # 글자 맥박 한 바퀴(ms) — 실기동 목업에서 정한 값
```

파일 끝에 클래스를 추가한다:

```python
class TextPulse:
    """글자색 알파가 천천히 오르내린다(공구 문구).

    🔴 반드시 stop() 으로 멈춘다 — 살아남으면 QSS 를 계속 재적용해 CPU 를 태운다.
    🔴 크기·두께가 아니라 **색만** 흔든다 — 굵기를 흔들면 레이아웃이 밀린다.
    color_fn: 그때그때 색(#RRGGBB)을 주는 함수(테마 전환 대응).
    """

    def __init__(self, widget, color_fn, weight=800, floor=0.40, ms=D_TEXT_PULSE):
        self._w = widget
        self._color = color_fn
        self._weight = weight
        self._floor = floor
        self._ms = ms
        self._anim = None

    def _apply(self, a):
        c = QColor(self._color())
        self._w.setStyleSheet(
            f"color: rgba({c.red()},{c.green()},{c.blue()},{a:.3f});"
            f"font-weight: {self._weight}; border: none; background: transparent;")

    def start(self):
        if self._anim is not None:
            return                      # 이미 뛰고 있으면 재시작하지 않는다
        if not enabled():
            self._apply(1.0)
            return
        floor = self._floor

        def step(t):
            # 0→1 을 삼각파로 접어 한 바퀴에 밝아졌다 어두워진다
            u = 1.0 - abs(2.0 * float(t) - 1.0)
            self._apply(floor + (1.0 - floor) * u)

        self._anim = tween(self._w, self._ms, step,
                           curve=QEasingCurve.Type.InOutSine, loops=-1)

    def stop(self):
        if self._anim is not None:
            self._anim.stop()
            self._anim = None
        self._apply(1.0)               # 멈추면 또렷한 상태로 남는다

    def active(self):
        return self._anim is not None
```

- [ ] **Step 4: `overlay.GaugePanel` 에 맥박을 붙인다**

`__init__` 에 상태를 둔다:

```python
        # 공구 문구 맥박 — 🔴 **상태가 바뀔 때만** start/stop 한다.
        #    _sub_timer 가 200ms 마다 update_view 를 부르므로, 비교 없이 걸면
        #    초당 5번 재시작돼 끝나지 않는 애니메이션이 된다.
        self._pulse_text = anim.TextPulse(
            self._tool_text, lambda: theme.C("tool_plate_text"), weight=800)
        self._pulse_state = anim.TextPulse(
            self._tool_state, lambda: theme.C("current"), weight=700)
        self._pulsing = False
```

`update_view()` 의 `if sub.needs_tool:` 블록 끝(기존 `anim.flash(...)` 뒤)에 넣는다:

```python
            # 「중」 강도 — 상태줄 + 요구 문구가 함께 맥박한다(🔧 아이콘은 제외).
            # 조건을 채웠거나(쥠) 오답 경고 중이면 멈춘다.
            want_pulse = not (sub.tool_ok or sub.wrong_tool)
            if want_pulse != self._pulsing:
                if want_pulse:
                    self._pulse_text.start()
                    self._pulse_state.start()
                else:
                    self._pulse_text.stop()
                    self._pulse_state.stop()
                self._pulsing = want_pulse
```

🔴 맥박이 도는 동안 `_tool_text`·`_tool_state` 의 `setStyleSheet` 를 `update_view` 가 매번 덮어쓰면 깜빡임이 죽는다. `want_pulse` 가 참일 때는 그 두 줄의 `setStyleSheet` 를 건너뛴다:

```python
            if not want_pulse:
                self._tool_text.setStyleSheet(...)      # 기존 그대로
                self._tool_state.setStyleSheet(theme.text_qss(token, 600))
```

`else:` 가지(공구가 필요 없는 단계)와 `sub is None` 이른 리턴 **양쪽 모두**에서 맥박을 멈춘다 — 안 멈추면 다음 단계까지 살아남는다:

```python
        if sub is None or not sub.is_active:
            self._stop_pulse()
            self.hide()
            return
```

```python
    def _stop_pulse(self):
        """🔴 서브 작업이 끝나는 **모든 경로**에서 부른다 — 안 멈추면 CPU 를 먹는다."""
        if self._pulsing:
            self._pulse_text.stop()
            self._pulse_state.stop()
            self._pulsing = False
```

`else:`(공구 불필요) 가지에서도 `self._stop_pulse()` 를 부른다.

- [ ] **Step 5: 통과를 확인한다**

```bash
cd ~/sop-project/Rpi5/Demo && QT_QPA_PLATFORM=offscreen python3 selftest/test_anim.py && QT_QPA_PLATFORM=offscreen python3 selftest/test_overlay.py && python3 selftest/test_console_flow.py
```
Expected: 전부 PASS

- [ ] **Step 6: 커밋**

```bash
cd ~/sop-project/Rpi5
git add Demo/anim.py Demo/overlay.py Demo/selftest/test_anim.py
git commit -m "$(cat <<'EOF'
feat(ui): 공구 문구 맥박 — 서브 작업 진행이 보이지 않던 것

상태줄 + 요구 문구가 1.6초에 한 번 밝아졌다 어두워진다(「중」 강도, 알파 0.40↔1.00).
쥐거나 오답 경고 중이면 멈춘다. 🔴 상태가 바뀔 때만 start/stop — 200ms 주기 재시작 금지.
설계 = sop-project/docs/superpowers/specs/2026-08-19-자동진행-결과창-design.md §6.4

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: 집계 — `session_stats.py`

**Files:**
- Create: `Rpi5/Demo/session_stats.py`
- Modify: `Rpi5/Demo/camera_thread.py` (`hand_signal`)
- Test: `Rpi5/Demo/selftest/test_session_stats.py` (신규)

**Interfaces:**
- Consumes: 없음 (Qt 비의존 단독 모듈)
- Produces — Task 8 이 그대로 쓴다:
  - `SessionStats()` · `.start(recipe_name: str, step_count: int, now=None)`
  - `.button_pressed(button: str, expected: str, ok: bool, now=None)`
  - `.step_done(order: int, button: str, name: str, now=None)`
  - `.sub_started(button: str, spec: dict, now=None)` · `.sub_done(button: str, now=None)`
  - `.tool_grasped(key: str, ok: bool, now=None)`
  - `.violation(expected: str, actual: str, level: str, now=None)` — `level` 은 `"warn"` 또는 `"block"`
  - `.interlock(engaged: bool, now=None)`
  - `.frame(names: list[str])` — 이 프레임에서 검출된 이름들(`"손"` 포함)
  - `.finish(now=None) -> dict` — 아래 스키마
  - `CameraThread.hand_signal = pyqtSignal(bool)` (`UsbCameraThread` 에도)

`finish()` 가 주는 dict:

```python
{
  "recipe": str, "started_at": float, "finished_at": float, "total_sec": float,
  "ok": bool,                                   # 위반이 하나도 없었는가
  "steps":      [{"order": int, "button": str, "name": str,
                  "pressed_at": float, "done_at": float, "sec": float}],
  "violations": [{"at": float, "expected": str, "actual": str, "level": str}],
  "interlocks": [{"at": float, "released_at": float or None}],
  "tools":      [{"button": str, "want": str, "grasp_sec": float or None,
                  "wrong": [str]}],
  "frames": int,
  "detections": {name: {"frames": int, "score_sum": float}},
}
```

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`selftest/test_session_stats.py` 를 새로 만든다:

```python
"""작업 결과 집계 검증.

실행: python3 Demo/selftest/test_session_stats.py

정본: 상위 docs/superpowers/specs/2026-08-19-자동진행-결과창-design.md §3

⚠️ 시간을 인자로 주입하므로 **10초를 실제로 기다리지 않는다.**
🔴 오탐지(false positive)는 집계하지 않는다 — 정답 라벨 없이는 셀 수 없다(§3.4).
"""

import os
import sys
import time

_DEMO_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _DEMO_DIR)

from session_stats import SessionStats

_fails = []


def check(cond, msg):
    print(("  ✅ " if cond else "  ❌ ") + msg)
    if not cond:
        _fails.append(msg)


def test_clean_run():
    """위반 없이 4단계를 마치면 ok=True 이고 순서·시간이 남는다."""
    print("\n[1] 완주")
    s = SessionStats()
    s.start("PECVD 정비(PM) 시퀀스", 4, now=100.0)
    for i, (btn, name) in enumerate([("B1", "클린·가스차단"), ("B2", "펌프/퍼지"),
                                     ("B3", "전극 냉각"), ("B4", "챔버 벤트")]):
        t = 100.0 + i * 12
        s.button_pressed(btn, btn, True, now=t)
        s.step_done(i + 1, btn, name, now=t + 10)
    out = s.finish(now=150.0)
    check(out["ok"] is True, "위반 없음 → ok=True")
    check([x["button"] for x in out["steps"]] == ["B1", "B2", "B3", "B4"],
          f"누른 순서 {[x['button'] for x in out['steps']]}")
    check(abs(out["total_sec"] - 50.0) < 1e-6, f"총 시간 {out['total_sec']}s")
    check(abs(out["steps"][0]["sec"] - 10.0) < 1e-6,
          f"1단계 소요 {out['steps'][0]['sec']}s")


def test_violation_and_interlock():
    """순서 위반과 인터락이 시각과 함께 남는다."""
    print("\n[2] 위반·인터락")
    s = SessionStats()
    s.start("t", 4, now=0.0)
    s.violation("B2", "B4", "block", now=5.0)
    s.interlock(True, now=5.0)
    s.interlock(False, now=9.0)
    out = s.finish(now=10.0)
    check(out["ok"] is False, "위반이 있으면 ok=False")
    check(len(out["violations"]) == 1, "위반 1건")
    v = out["violations"][0]
    check(v["expected"] == "B2" and v["actual"] == "B4" and v["level"] == "block",
          f"기대 {v['expected']} → 실제 {v['actual']} ({v['level']})")
    check(len(out["interlocks"]) == 1 and out["interlocks"][0]["released_at"] == 9.0,
          "인터락 1건 · 해제 시각 기록")


def test_tool_subtask():
    """공구를 쥐기까지 걸린 시간과 오답 공구가 남는다."""
    print("\n[3] 공구")
    s = SessionStats()
    s.start("t", 4, now=0.0)
    s.sub_started("B2", {"type": "wait_tool", "sec": 10, "tool": "wrench"}, now=0.0)
    s.tool_grasped("driver", False, now=3.0)      # 다른 공구를 집었다
    s.tool_grasped("wrench", True, now=7.5)       # 요구 공구를 쥐었다
    s.sub_done("B2", now=7.5)
    out = s.finish(now=10.0)
    t = out["tools"][0]
    check(t["want"] == "wrench", f"요구 공구 {t['want']}")
    check(abs(t["grasp_sec"] - 7.5) < 1e-6, f"쥐기까지 {t['grasp_sec']}s")
    check(t["wrong"] == ["driver"], f"오답 공구 {t['wrong']}")


def test_detection_counts():
    """검출은 **프레임 수와 신뢰도 합**으로만 남는다 — 비율을 만들지 않는다."""
    print("\n[4] 검출")
    s = SessionStats()
    s.start("t", 4, now=0.0)
    s.frame([("B1", 0.90), ("손", 0.80)])
    s.frame([("B1", 0.80)])
    s.frame([])
    out = s.finish(now=1.0)
    check(out["frames"] == 3, f"전체 프레임 {out['frames']}")
    check(out["detections"]["B1"]["frames"] == 2, "B1 검출 2프레임")
    check(abs(out["detections"]["B1"]["score_sum"] - 1.70) < 1e-6, "신뢰도 합 1.70")
    check(out["detections"]["손"]["frames"] == 1, "손 검출 1프레임")
    check("rate" not in out["detections"]["B1"],
          "🔴 비율(rate)을 만들지 않는다 — 손 없는 프레임이 분모에 섞인다")


def test_no_false_positive_field():
    """🔴 오탐지 항목이 존재하지 않는다 (설계 §3.4)."""
    print("\n[5] 오탐지 없음")
    s = SessionStats()
    s.start("t", 4, now=0.0)
    out = s.finish(now=1.0)
    for bad in ("false_positive", "fp", "오탐", "misdetect"):
        check(bad not in out, f"'{bad}' 항목 없음")


if __name__ == "__main__":
    t0 = time.time()
    test_clean_run()
    test_violation_and_interlock()
    test_tool_subtask()
    test_detection_counts()
    test_no_false_positive_field()
    elapsed = time.time() - t0
    print()
    if _fails:
        print(f"❌ 실패 {len(_fails)}건")
        for m in _fails:
            print(f"   - {m}")
        sys.exit(1)
    if elapsed > 1.0:
        print(f"❌ 느림 {elapsed:.1f}s — 실시간을 기다리고 있다(시간 주입이 안 됨)")
        sys.exit(1)
    print(f"✅ 결과 집계 검증 통과 ({elapsed:.3f}s)")
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd ~/sop-project/Rpi5/Demo && python3 selftest/test_session_stats.py
```
Expected: FAIL — `ModuleNotFoundError: No module named 'session_stats'`

- [ ] **Step 3: `session_stats.py` 를 만든다**

```python
"""한 번의 작업에서 일어난 일을 모은다 — 작업 완료 결과창의 재료.

정본: 상위 docs/superpowers/specs/2026-08-19-자동진행-결과창-design.md §3

⚠️ Qt 에 의존하지 않는다 — GUI 없이 시험할 수 있어야 한다.
⚠️ 시간을 **인자로 받는다**(`now`). 생략하면 실시간을 쓴다(런타임 편의).
   fsm.py · sub_task.py 와 같은 규약이다.

🔴 오탐지(false positive)를 집계하지 않는다. 오탐 판정에는 정답 라벨이 필요하고
   실시간 데모 중에 그것을 알 방법이 없다 — 숫자를 만들면 근거 없는 수치가 된다.
🔴 검출을 **비율로 만들지 않는다.** "프레임 단위 손 검출률을 성능 지표로 쓰지 말 것
   — 손이 없는 프레임이 분모에 섞여 값이 오염된다"(CLAUDE.md §5). 프레임 수와
   신뢰도 합만 남기고, 나누는 것은 하지 않는다.
"""

import time


def _now(now):
    return time.time() if now is None else now


class SessionStats:
    def __init__(self):
        self.reset()

    def reset(self):
        self._recipe = ""
        self._step_count = 0
        self._started = None
        self._steps = []           # {order, button, name, pressed_at, done_at, sec}
        self._pending = {}         # button -> pressed_at
        self._violations = []
        self._interlocks = []
        self._tools = []           # {button, want, start, grasp_sec, wrong[]}
        self._frames = 0
        self._dets = {}            # name -> {frames, score_sum}

    # ------------------------------------------------------------------ 시작
    def start(self, recipe_name, step_count, now=None):
        self.reset()
        self._recipe = recipe_name
        self._step_count = step_count
        self._started = _now(now)

    @property
    def running(self):
        return self._started is not None

    # ------------------------------------------------------------------ 단계
    def button_pressed(self, button, expected, ok, now=None):
        if ok:
            self._pending[button] = _now(now)

    def step_done(self, order, button, name, now=None):
        t = _now(now)
        pressed = self._pending.pop(button, t)
        self._steps.append({"order": order, "button": button, "name": name,
                            "pressed_at": pressed, "done_at": t,
                            "sec": t - pressed})

    # ------------------------------------------------------------------ 서브
    def sub_started(self, button, spec, now=None):
        self._tools.append({"button": button, "want": (spec or {}).get("tool"),
                            "start": _now(now), "grasp_sec": None, "wrong": []})

    def sub_done(self, button, now=None):
        pass          # 쥔 시각은 tool_grasped 가 이미 기록한다

    def tool_grasped(self, key, ok, now=None):
        if not self._tools:
            return
        cur = self._tools[-1]
        if ok:
            if cur["grasp_sec"] is None:
                cur["grasp_sec"] = _now(now) - cur["start"]
        elif key and key not in cur["wrong"]:
            cur["wrong"].append(key)

    # ------------------------------------------------------------------ 위반
    def violation(self, expected, actual, level, now=None):
        self._violations.append({"at": _now(now), "expected": expected,
                                 "actual": actual, "level": level})

    def interlock(self, engaged, now=None):
        t = _now(now)
        if engaged:
            self._interlocks.append({"at": t, "released_at": None})
        elif self._interlocks and self._interlocks[-1]["released_at"] is None:
            self._interlocks[-1]["released_at"] = t

    # ------------------------------------------------------------------ 검출
    def frame(self, names):
        """프레임 1장의 검출 목록. names = [(이름, 신뢰도), ...] 또는 [이름, ...]"""
        self._frames += 1
        for item in names:
            name, score = item if isinstance(item, (tuple, list)) else (item, 0.0)
            d = self._dets.setdefault(name, {"frames": 0, "score_sum": 0.0})
            d["frames"] += 1
            d["score_sum"] += float(score)

    # ------------------------------------------------------------------ 마감
    def finish(self, now=None):
        t = _now(now)
        started = self._started if self._started is not None else t
        return {
            "recipe": self._recipe,
            "started_at": started,
            "finished_at": t,
            "total_sec": t - started,
            "ok": not self._violations,
            "steps": list(self._steps),
            "violations": list(self._violations),
            "interlocks": list(self._interlocks),
            "tools": list(self._tools),
            "frames": self._frames,
            "detections": {k: dict(v) for k, v in self._dets.items()},
        }
```

- [ ] **Step 4: 통과를 확인한다**

```bash
cd ~/sop-project/Rpi5/Demo && python3 selftest/test_session_stats.py
```
Expected: PASS

- [ ] **Step 5: `camera_thread.py` 에 `hand_signal` 을 단다**

`CameraThread` 의 시그널 선언부에 추가한다:

```python
    hand_signal = pyqtSignal(bool)      # 이 프레임에서 손이 검출됐는가 (집계 전용)
```

`_process_frame()` 의 `fingertip = self._hand.detect(...)` 바로 아래에 한 줄을 넣는다:

```python
        # 🔴 집계 전용이다 — 판정에 쓰지 않는다. roi_signal 은 ROI 라벨만 주므로
        #    「ROI 밖의 손」과 「손 없음」이 구별되지 않는다(설계 §3.6).
        self.hand_signal.emit(fingertip is not None)
```

`UsbCameraThread` 에도 같은 시그널과 emit 을 넣는다.

- [ ] **Step 6: import 검사를 통과시킨다**

`selftest/test_imports.py` 의 모듈 목록에 `session_stats` 를 추가한다.

```bash
cd ~/sop-project/Rpi5/Demo && python3 selftest/test_imports.py && python3 selftest/test_console_flow.py
```
Expected: 둘 다 PASS

- [ ] **Step 7: 커밋**

```bash
cd ~/sop-project/Rpi5
git add Demo/session_stats.py Demo/camera_thread.py Demo/selftest/test_session_stats.py Demo/selftest/test_imports.py
git commit -m "$(cat <<'EOF'
feat(집계): session_stats — 한 작업에서 일어난 일을 모은다

순서·소요시간·순서위반·인터락·공구·검출 프레임을 모은다. Qt 비의존, 시간 주입.
🔴 오탐지는 집계하지 않는다(정답 라벨 없이는 셀 수 없다). 검출을 비율로 만들지 않는다.
손 검출 집계를 위해 camera_thread 에 hand_signal 을 더한다 — 판정에는 쓰지 않는다.
설계 = sop-project/docs/superpowers/specs/2026-08-19-자동진행-결과창-design.md §3

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: 결과 안내창

**Files:**
- Create: `Rpi5/Demo/overlay_result.py`
- Modify: `Rpi5/Demo/safety_console.py`
- Test: `Rpi5/Demo/selftest/test_console_flow.py`

**Interfaces:**
- Consumes: Task 7 의 `SessionStats.finish()` dict · Task 2 의 `_sync_cta_visibility()`
- Produces:
  - `ResultPanel(parent)` — `.show_result(data: dict)` · `.relayout(parent_rect)` · `.apply_theme()` · `closed = pyqtSignal()`
  - `SafetyConsole.result_panel` · `SafetyConsole._stats`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`selftest/test_console_flow.py` 에 추가한다:

```python
def test_result_panel_on_completion():
    """🔴 4단계를 마치면 결과창이 뜨고, 그 동안 「작업 시작」이 비치지 않는가."""
    print("\n[N] 결과창")
    win = make_console()
    win._on_cta()
    for step in ("1", "2", "3", "4"):
        key(win, step)
        if win._sub is not None and win._sub.is_active:
            win._sub.tick(now=time.time() + 999)
            if win._sub.needs_tool:
                win._sub.set_tool(win._sub.want_tool)
            win._update_sub_view()
    check(not win.result_panel.isHidden(), "완주 → 결과창이 뜬다")
    check(win.btn_cta.isHidden(), "🔴 결과창 뒤로 「작업 시작」이 비치지 않는다")
    win.result_panel.closed.emit()
    check(win.result_panel.isHidden(), "닫으면 사라진다")
    check(not win.btn_cta.isHidden(), "닫으면 「작업 시작」이 돌아온다")
    win.close()


def test_reset_does_not_show_result():
    """🔴 작업 초기화로는 결과창이 뜨지 않는다 — 완주가 아니다(설계 §3.1)."""
    print("\n[N+1] 초기화")
    win = make_console()
    win._on_cta()
    key(win, "1")
    win._reset_work()
    check(win.result_panel.isHidden(), "초기화로는 결과창이 뜨지 않는다")
    win.close()
```

`if __name__` 블록에 두 함수를 추가한다.
⚠️ `_reset_work()` 가 확인 다이얼로그 **뒤에** 실제로 되돌리는 함수다(`safety_console.py:1215`). `_on_reset_clicked()` 는 다이얼로그를 띄우므로 테스트에서 부르지 않는다.

- [ ] **Step 2: 실패를 확인한다**

```bash
cd ~/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py
```
Expected: FAIL — `AttributeError: 'SafetyConsole' object has no attribute 'result_panel'`

- [ ] **Step 3: `overlay_result.py` 를 만든다**

```python
"""작업 완료 결과 안내창 — 정본: 상위 specs/2026-08-19-자동진행-결과창-design.md §3

무엇을 보이나: 판정 · 누른 순서 · 소요시간 · 순서위반 · 인터락 · 공구 · 검출.

🔴 **오탐지 건수를 보이지 않는다** — 정답 라벨 없이는 셀 수 없다(§3.4).
🔴 **검출을 비율(%)로 보이지 않는다** — 손 없는 프레임이 분모에 섞인다.
   「검출 프레임 / 전체 프레임」 두 수를 그대로 적는다.
🔴 화면 맨 아래에 **관측치이지 모델 성능이 아니라는 문장**을 반드시 적는다.
"""

import time

from PyQt6.QtCore import Qt, pyqtSignal
from PyQt6.QtWidgets import (QLabel, QVBoxLayout, QScrollArea, QWidget,
                             QHBoxLayout)

import config
import theme
from overlay import place
from overlay_menu import _Sheet

_RESULT_W = 0.52          # 화면 폭 대비 — 표가 들어가야 해 설정(0.30)보다 넓다


def _hhmmss(sec):
    m, s = divmod(int(sec), 60)
    return f"{m}분 {s}초" if m else f"{s}초"


def _stamp(ts):
    return time.strftime("%Y.%m.%d %H:%M:%S", time.localtime(ts))


class ResultPanel(_Sheet):
    """작업 완료 결과. 내용이 길어 스크롤한다."""

    closed = pyqtSignal()

    def __init__(self, parent=None):
        super().__init__(parent)
        lay = QVBoxLayout(self)
        lay.setContentsMargins(0, 0, 0, 0)
        lay.setSpacing(8)

        head = QHBoxLayout()
        self._title = QLabel("작업 완료")
        self._title.setFont(config.font("title", 800))
        head.addWidget(self._title)
        head.addStretch()
        head.addWidget(self._make_close_button())
        lay.addLayout(head)

        self._verdict = QLabel("")
        self._verdict.setFont(config.font("body", 800))
        lay.addWidget(self._verdict)

        self._scroll = QScrollArea()
        self._scroll.setWidgetResizable(True)
        self._scroll.setFrameShape(QScrollArea.Shape.NoFrame)
        self._body = QWidget()
        self._body_lay = QVBoxLayout(self._body)
        self._body_lay.setContentsMargins(0, 0, 0, 0)
        self._body_lay.setSpacing(10)
        self._scroll.setWidget(self._body)
        lay.addWidget(self._scroll, 1)

        # 🔴 이 문장을 빼지 않는다 — 1회 관측치가 성능으로 읽히면 안 된다(§3.4).
        self._note = QLabel("이 값은 이번 1회 시연에서 관측된 수치이며 모델 성능이 아닙니다.")
        self._note.setFont(config.font("small"))
        self._note.setWordWrap(True)
        lay.addWidget(self._note)

        self.hide()

    # ------------------------------------------------------------------ 표시
    def show_result(self, d):
        for i in reversed(range(self._body_lay.count())):
            w = self._body_lay.itemAt(i).widget()
            if w:
                w.setParent(None)

        ok = d.get("ok", True)
        self._verdict.setText("✅ 완주 성공" if ok else "⚠ 위반이 있었습니다")
        self._verdict.setStyleSheet(theme.text_qss("done" if ok else "warn", 800))

        order = " → ".join(s["button"] for s in d["steps"])
        self._section("누른 순서", [order or "—"])
        self._section("소요 시간", [f"총 {_hhmmss(d['total_sec'])}"] + [
            f"{s['order']}. {s['button']} {s['name']} — {_hhmmss(s['sec'])}"
            for s in d["steps"]])

        vs = d["violations"]
        self._section(f"순서 위반 {len(vs)}건", [
            f"{i}. {_stamp(v['at'])} — 기대 {v['expected']} → 실제 {v['actual']}"
            f" ({'차단' if v['level'] == 'block' else '경고'})"
            for i, v in enumerate(vs, 1)] or ["없음"])

        ils = d["interlocks"]
        self._section(f"인터락 작동 {len(ils)}건", [
            f"{i}. {_stamp(x['at'])} 작동"
            + (f" → {_stamp(x['released_at'])} 해제" if x["released_at"] else " (해제 안 됨)")
            for i, x in enumerate(ils, 1)] or ["없음"])

        rows = []
        for t in d["tools"]:
            got = f"{t['grasp_sec']:.1f}초 만에 쥠" if t["grasp_sec"] is not None else "쥐지 않음"
            rows.append(f"{t['button']} — 요구 {t['want']} · {got}")
            if t["wrong"]:
                rows.append(f"    다른 공구 {len(t['wrong'])}회: {', '.join(t['wrong'])}")
        self._section("공구 서브 작업", rows or ["없음"])

        det = d["detections"]
        frames = d["frames"]
        rows = [f"전체 {frames} 프레임"]
        for name in sorted(det):
            v = det[name]
            avg = v["score_sum"] / v["frames"] if v["frames"] else 0.0
            rows.append(f"{name} — 검출 {v['frames']} 프레임 · 평균 신뢰도 {avg:.2f}")
        self._section("AI 검출", rows)

        self.apply_theme()
        self.show()
        self.raise_()

    def _section(self, title, lines):
        cap = QLabel(title)
        cap.setFont(config.font("small", 700))
        cap.setStyleSheet(theme.text_qss("label", 700))
        self._body_lay.addWidget(cap)
        for line in lines:
            lbl = QLabel(line)
            lbl.setFont(config.font("body", 600))
            lbl.setWordWrap(True)
            lbl.setStyleSheet(theme.text_qss("text", 600))
            self._body_lay.addWidget(lbl)

    def apply_theme(self):
        super().apply_theme()
        self._title.setStyleSheet(theme.text_qss("text", 800))
        self._note.setStyleSheet(theme.text_qss("todo", 600))
        self._scroll.setStyleSheet("background: transparent; border: none;")
        self._body.setStyleSheet("background: transparent;")

    def relayout(self, parent_rect):
        place(self, parent_rect, width=_RESULT_W, top=0.10, height=0.72)
```

- [ ] **Step 4: `safety_console.py` 에 배선한다**

import 에 추가한다:

```python
from session_stats import SessionStats
from overlay_result import ResultPanel
```

`settings_panel` 을 만드는 곳 근처에 패널과 집계기를 만든다:

```python
        self.result_panel = ResultPanel(central)
        self.result_panel.closed.connect(self._close_result)
        self._stats = SessionStats()
```

`_sheets()` 의 튜플에 `self.result_panel` 을 추가한다. `_relayout()` 의 `raise_()` 루프와 `w.relayout(r)` 목록, `_apply_theme()` 의 위젯 목록에도 추가한다.

집계 배선 — 각 지점에 한 줄씩 넣는다:

| 함수 | 넣을 줄 |
| --- | --- |
| `_on_start_process()` 끝 | `self._stats.start((self._recipe or {}).get("process_name", "기본 시퀀스"), self.fsm.step_count)` |
| `_commit_button()` 의 `fsm.press_button` 앞 | `self._stats.button_pressed(button, self.fsm.correct_roi, button == self.fsm.correct_roi)` |
| `_commit_button()` 의 단계 진행 분기 안 | `self._stats.step_done(before, button, self._step_name(before))` |
| `_begin_sub()` 끝 | `self._stats.sub_started(button, spec)` |
| `_finish_sub()` 의 `_commit_button` 앞 | `self._stats.sub_done(button)` |
| `_on_interlock()` 첫 줄 | `self._stats.interlock(engaged)` |
| `_on_state_change()` 의 `WARNING` 분기 | `self._stats.violation(self.fsm.correct_roi, self._last_button or "?", "warn")` |
| `_on_state_change()` 의 `BLOCK` 분기 | `self._stats.violation(self.fsm.correct_roi, self._last_button or "?", "block")` |

`_last_button` 은 `_press_button()` 첫 줄에서 `self._last_button = button` 으로 기억한다(`__init__` 에서 `self._last_button = None` 로 초기화).

공구 집계는 `_on_tool()` 의 마디 전이 로그 옆에 넣는다:

```python
            if self._tool_state.phase == "grasped":
                self._stats.tool_grasped(self._tool_state.want_tool, True)
```

오답 공구는 `_update_sub_view()` 의 `if sub.wrong_tool:` 분기 안(경고를 처음 띄우는 곳)에서:

```python
                self._stats.tool_grasped(sub.wrong_tool, False)
```

검출 집계 — `_on_yolo_detections()` 끝과 `_on_tool()`, 그리고 새 `hand_signal` 핸들러를 합쳐 **프레임 1장에 한 번** 넣는다. 가장 단순하고 틀릴 여지가 없는 방법은 `_on_yolo_detections` 를 프레임의 기준으로 삼는 것이다:

```python
    def _on_yolo_detections(self, detections):
        current = set(d[0] for d in detections)
        self._seen_buttons = current
        if self._stats.running:
            names = [(d[0], d[1]) for d in detections]
            if self._hand_seen:
                names.append(("손", 1.0))
            names += [(n, s) for n, s, *_ in self._tool_dets_for_stats]
            self._stats.frame(names)
```

`self._hand_seen` 과 `self._tool_dets_for_stats` 는 `__init__` 에서 `False` / `[]` 로 초기화하고, 각각 아래에서 갱신한다:

```python
    def _on_hand(self, seen):
        self._hand_seen = bool(seen)
```

`_on_tool(self, dets, fingertip)` 의 이른 리턴(`if self._sub is None ...`) **앞**에 한 줄을 넣는다 — 서브 작업이 없어도 검출은 집계해야 한다:

```python
        # dets = [(이름, 신뢰도, x1, y1, x2, y2), ...]
        self._tool_dets_for_stats = [(d[0], d[1]) for d in dets]
```

그러면 위 `_on_yolo_detections` 의 `names += [(n, s) for n, s, *_ in self._tool_dets_for_stats]` 는 이미 (이름, 신뢰도) 쌍이므로 다음으로 단순해진다:

```python
            names += list(self._tool_dets_for_stats)
```

`camera_thread.hand_signal.connect(self._on_hand)` 를 두 스레드 모두에 연결한다.

완주 판정과 결과창 표시 — `_commit_button()` 끝에 넣는다:

```python
        # 마지막 단계의 정답 눌림 → 공정 완료. 작업 초기화는 이 경로를 타지 않는다.
        if before == self.fsm.step_count and self.fsm.state == State.IDLE:
            self._show_result()
```

```python
    def _show_result(self):
        data = self._stats.finish()
        self._close_sheets()
        self.result_panel.relayout(self._root.rect())
        self.result_panel.show_result(data)
        self._dim_others(True)
        self._sync_cta_visibility()
        self._append_log(f"[결과] 작업 완료 — {'위반 없음' if data['ok'] else '위반 있음'}"
                         f" · 총 {data['total_sec']:.0f}초")

    def _close_result(self):
        self.result_panel.hide()
        self._dim_others(self._any_sheet_open())
        self._sync_cta_visibility()
```

- [ ] **Step 5: 🔴 IDLE 진입 시 CTA 를 직접 show() 하던 함정을 고친다**

`_on_state_change()` 의 `if new == State.IDLE:` 블록에서

```python
            self.btn_cta.setText("▶  작업 시작")
            self.btn_cta.show()
```

를 다음으로 바꾼다:

```python
            self.btn_cta.setText("▶  작업 시작")
            # 🔴 show() 를 직접 부르지 않는다 — 결과창이 떠 있으면 그 뒤로 비친다.
            #    "기억하지 말고 상태에서 그때그때 계산한다"(_sync_cta_visibility).
            self._sync_cta_visibility()
```

`_sync_cta_visibility()` 는 `_any_sheet_open()` 을 보고, `result_panel` 이 `_sheets()` 에 들어갔으므로 자동으로 숨는다.

⚠️ 순서 주의: `_commit_button()` 안에서 `fsm.press_button()` → `_on_state_change(IDLE)` 이 **먼저** 돌고 그 다음 `_show_result()` 가 돈다. `_show_result()` 가 마지막에 `_sync_cta_visibility()` 를 다시 부르므로 결과가 맞는다.

- [ ] **Step 6: 통과를 확인한다**

```bash
cd ~/sop-project/Rpi5/Demo && python3 selftest/test_console_flow.py && python3 selftest/test_imports.py && QT_QPA_PLATFORM=offscreen python3 selftest/test_overlay.py
```
Expected: 전부 PASS

- [ ] **Step 7: 커밋**

```bash
cd ~/sop-project/Rpi5
git add Demo/overlay_result.py Demo/safety_console.py Demo/selftest/test_console_flow.py
git commit -m "$(cat <<'EOF'
feat(ui): 작업 완료 결과 안내창

판정·누른 순서·소요시간·순서위반·인터락·공구·검출을 마지막 단계 후에 보여준다.
🔴 오탐지 건수를 보이지 않고 검출을 비율로 만들지 않는다 — 근거 없는 수치가 된다.
🔴 IDLE 진입 시 btn_cta.show() 를 직접 부르던 것을 _sync_cta_visibility 로 고쳤다
   — 결과창 뒤로 「작업 시작」이 비쳤다.
설계 = sop-project/docs/superpowers/specs/2026-08-19-자동진행-결과창-design.md §3

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: 실기동 확인

**Files:** 없음 (확인만). 문제가 나오면 해당 Task 로 돌아간다.

**Interfaces:**
- Consumes: Task 1~8 전부
- Produces: 확인 결과 (문서 갱신은 Task 10)

- [ ] **Step 1: 전체 selftest 를 돌린다**

```bash
cd ~/sop-project/Rpi5/Demo
for f in selftest/test_*.py; do echo "=== $f"; QT_QPA_PLATFORM=offscreen python3 "$f" || echo "❌ $f"; done
```
Expected: 전부 통과

- [ ] **Step 2: 🔴 실제로 띄운다 (생략 금지)**

```bash
cd ~/sop-project/Rpi5/Demo && DISPLAY=:0 ./run_demo.sh
```

**import 검사로 못 잡는 런타임 오류가 있다** — `@pyqtSlot` 시그니처 불일치로 GUI 가 기동 즉시 죽은 전례가 있다(CLAUDE.md §5). 창이 뜨는 것까지 눈으로 본다.

⚠️ SSH 로 접속했다면 창은 **파이에 붙은 모니터**에 뜬다. 원격 화면에는 안 보인다.

- [ ] **Step 3: 7가지를 눈으로 확인한다**

1. B1 → 10초 후 **자동으로** B2 로 넘어가는가
2. B2 에서 렌치를 안 쥐면 **멈춰 있는가**, 쥐면 넘어가는가
3. B4 후 결과창이 뜨고, 내용이 화면 로그와 **일치하는가**
4. 대기 중 다른 버튼을 누르면 여전히 **차단되는가** (위반 감지가 살아 있는가)
5. 탐지 박스를 끄면 사라지고, **끈 상태에서도 순서 위반이 잡히는가**
6. 공구 칸 판이 글자를 **자르지 않는가** — 다크·화이트 **양쪽**에서
7. 결과창이 열린 동안 「작업 시작」 버튼이 **비쳐 나오지 않는가**

- [ ] **Step 4: 애니메이션을 끈 상태로도 한 번 띄운다**

```bash
cd ~/sop-project/Rpi5/Demo && SOP_UI_ANIM=0 DISPLAY=:0 ./run_demo.sh
```
Expected: 맥박이 멈춘 채(또렷한 상태) 나머지가 그대로 돈다. 진행도 정상.

- [ ] **Step 5: 결과를 기록한다**

확인 항목 7가지의 결과를 다음 Task 에서 작업로그에 적을 수 있게 메모해 둔다. **안 된 것을 「대체로 된다」로 적지 않는다.**

---

### Task 10: 문서 갱신

**Files:**
- Modify: `sop-project/docs/작업로그.md`
- Modify: `sop-project/docs/superpowers/specs/2026-08-19-자동진행-결과창-design.md` (상태 줄)

**Interfaces:**
- Consumes: Task 9 의 확인 결과
- Produces: 없음 (마지막 작업)

- [ ] **Step 1: spec 의 상태 줄을 바꾼다**

`- **상태**: 설계 확정, 구현 전` → `- **상태**: 구현 완료 (2026-08-19)`
실기동에서 확인하지 못한 항목이 있으면 그 줄에 함께 적는다.

- [ ] **Step 2: 작업로그에 항목을 더한다**

`docs/작업로그.md` 의 형식을 따라 **무엇을 했고 무엇이 확인됐는지**만 적는다.
🔴 **수치를 적지 않는다** — 측정값의 정본은 통합문서 §10 이다.
🔴 확인하지 못한 것은 `⏸` 로 남긴다. 「대체로 된다」로 적지 않는다.

- [ ] **Step 3: 커밋**

```bash
cd ~/sop-project
git add docs/작업로그.md docs/superpowers/specs/2026-08-19-자동진행-결과창-design.md
git commit -m "$(cat <<'EOF'
docs(작업로그): 자동 진행·결과창·오버레이 표시 구현 완료

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## 되돌리는 법

문제가 생기면 Task 단위로 되돌린다 — 각 Task 가 한 커밋이다.

| 증상 | 되돌릴 것 |
| --- | --- |
| 단계가 제멋대로 넘어간다 | Task 2 |
| 맥박 때문에 화면이 느리다 | `SOP_UI_ANIM=0` (코드를 고칠 필요 없다) |
| 공구 문구가 잘린다 | Task 5 — 여백을 QSS padding 으로 주지 않았는지 본다 |
| 박스 색이 이상하다 | Task 3 — `box_bgr()` 말고 다른 곳에서 뒤집고 있는지 본다 |
| 결과창 뒤로 버튼이 비친다 | Task 8 Step 5 |
