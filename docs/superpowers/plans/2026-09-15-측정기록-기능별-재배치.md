# 통합문서 §10 기능별 재배치 · 절 구성 통일 — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> 이 프로젝트 기본 = **executing-plans 인라인**(CC작업문서 §2.2 — 모든 태스크가 통합문서 한 파일을 사슬로 고친다).

**Goal:** §10 절을 6갈래로 모으고 절 안 하위절을 9블록 순서로 정리한다 — 원문 한 글자도 바꾸지 않고.
**Architecture:** 파서가 §10 을 절 → 개요 · `####` 하위절 덩어리로 자르고, 사람이 확인한 **배정표**(하위절 → 블록)를 입력으로 **조립기**가 줄 덩어리를 옮긴다. 관문 스크립트가 전후 문서를 기계 대조한다. 도구는 저장소 밖 `~/lab/section10-regroup/`(세션을 넘어 남고 저장소에 안 섞임).
**Tech Stack:** Python 3 표준 라이브러리 · git
**Spec:** `docs/superpowers/specs/2026-09-15-측정기록-기능별-재배치-design.md`

## Global Constraints

- 🔴 **원문 무변경** — 추가 줄은 `## 📂 갈래 N 이름` · `**▸ 블록명**` · 빈 줄 · 「🧱 절 구성 규약」 절뿐. 편집기로 원문 줄을 고치지 않는다(조립기만 쓴다)
- `§10.N` 번호·제목 줄·마커 줄 불변 · 개요(첫 `####` 앞) 이동 없음 · 하위절은 통째 이동(`#####` 포함)
- 갈래 6개·순서·절 목록 = spec §3.2 표 그대로 · 블록 9개·순서 = spec §3.3 표 그대로
- 빈 블록 라벨 없음 · 블록이 하나뿐인 절은 라벨 없음
- 예외 변경 = §10 머리 「🧭 갈래 색인」 표(행 순서 · 실콘솔 행 합침)만
- 기준본 = 착수 시점 `docs/통합문서.md` 를 `~/lab/section10-regroup/before.md` 로 복사(대조용 — 저장소에 사본 금지)
- 커밋 = `sop-project` 경로 지정 · 서명 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` · push 는 Task 6(work-close) 한 번
- 🔴 관문 미통과면 반영·「완료」 금지

## 관문

| 관문 | 위치 | 통과 기준 | 실패 시 |
|---|---|---|---|
| P1 줄 보존 | Task 3 | 추가 줄 제외 다중집합 == 기준본(빈 줄 개수 제외) | 결과 파일 버림 · 원인 규명 |
| P2 절 경계 | Task 3 | 줄마다 소속 §10.N 이 전후 같음 | 〃 |
| P3 절 머리 | Task 3 | 절 첫 두 줄(제목·마커) + 개요 줄 순서 == 기준본 | 〃 |
| P4 블록 안 순서 | Task 3 | 같은 블록 하위절 상대 순서 == 기준본 | 〃 |
| P5 위치 참조 | Task 3 | 배정표의 참조 29곳 위/아래 관계 유지 | 배정표 수정 → Task 2 재확인 |
| P6 §10 밖 | Task 3·4 | §10 바깥 바이트 동일 · 머리는 색인 표·규약 절 외 동일 | 〃 |
| P7 도구·인용 | Task 4 | `work-close` 2.5 통과 · 세 저장소 `§10.N` 인용 번호 전부 실재 | 원인 규명 |
| P8 사용자 확인 | Task 2 · Task 5 | 배정표 확인 답 · 표본 3절 전후 대조 보고 | 멈춤 |

## 파일

| 파일 | 책임 | 태스크 |
|---|---|---|
| `~/lab/section10-regroup/parse.py` | 절·하위절 자르기 · 자동 분류 · 위치 참조 탐지 → `assign.tsv` | 1 |
| `~/lab/section10-regroup/assign.tsv` | 배정표: `절 · 하위절 시작줄 · 제목 · 블록 · 근거 · 참조` | 1·2 |
| `~/lab/section10-regroup/build.py` | 기준본 + 배정표 → 재배치본 | 3 |
| `~/lab/section10-regroup/gates.py` | P1~P6 대조 · 결과 `✅ 통과` / `❌ 실패 N건` exit 1 | 3 |
| `docs/통합문서.md` §10 | 재배치 · 색인 표 · 절 구성 규약 | 3·4 |
| `CLAUDE.md` §3 · `.claude/skills/work-close/SKILL.md` 2.3 | 새 절 규칙 1줄 · 포인터 | 4 |

---

### Task 1: 파서 · 배정표 초안

- [ ] **Step 1: 기준본 고정** — `mkdir -p ~/lab/section10-regroup && cp docs/통합문서.md ~/lab/section10-regroup/before.md && git rev-parse HEAD > ~/lab/section10-regroup/base-commit`
- [ ] **Step 2: `parse.py`** — `sections(lines) -> list[Section]`: `^## 10\. ` ~ `^## 11\. ` 안에서 `^### 10\.(\d+) ` 로 절을 자르고, 절마다 `head`(제목~첫 `^#### ` 앞) · `subs`(각 `^#### ` 부터 다음 `^#### `/절 끝 앞까지, 줄 번호 보존). `classify(title) -> block`: 키워드 사전(조건·방법·설정→조건·방법 / 결과·측정·재현·관문→결과 / 판정·결론·원인·해석·의미→판정·원인 / 한계·말할 수→한계 / 정정·물린→정정 / 다음·미결·후속→다음·미결 / 없음→고유), 두 블록 키워드가 겹치면 `애매`. `refs(sub) -> list`: `(위|아래)(의)? ?(표|절|값|결과|목록|항목|측정|조건|\(\d+\)|[①-⑩])` 등장 줄과 추정 대상 하위절.
- [ ] **Step 3: 자체 시험** — 파서가 63절 · 하위절 386개 · 참조 29곳을 세는지(2026-09-14 실측값), 모든 줄이 정확히 한 덩어리(head 또는 sub)에 속하는지 `assert` 로 확인
- [ ] **Step 4: `assign.tsv` 생성** — 블록 · 근거(맞은 키워드) · 참조 대상 열

### Task 2: 배정표 사용자 확인 (🛑 멈춤)

- [ ] **Step 1: 요약 보고** — 블록별 개수 · **고유 구성 목록 · 애매 목록 · 위치 참조 29곳의 제한 결과**만 표로(전체 386행은 요청 시). CC작업문서 §2.7 형식(Claude 가 정한 배정은 그 자리에 ✋)
- [ ] **Step 2: 사용자 답 반영** — 바뀐 행만 `assign.tsv` 수정 · 확인 답을 `~/lab/section10-regroup/confirm.txt` 에 원문 인용

### Task 3: 조립 · 관문 P1~P6

- [ ] **Step 1: `build.py`** — 절마다 `head` 그대로 → 블록 순서(조건·방법 → 결과 → 판정·원인 → 한계 → 정정 → 다음·미결 → 고유)로 하위절을 원래 상대 순서대로 배치 · 비지 않은 블록이 2개 이상일 때만 블록 앞에 빈 줄 + `**▸ 블록명**` + 빈 줄. 절 순서 = spec §3.2 갈래 순 · 갈래 앞에 `## 📂 갈래 N 이름` + 빈 줄. §10 머리와 §11 이후는 기준본 그대로
- [ ] **Step 2: `gates.py` 먼저 기준본 대 기준본으로 돌려 통과**(도구 자체 점검) → 일부러 한 글자 바꾼 사본으로 **P1 실패** 확인
- [ ] **Step 3: 재배치본 생성 → `gates.py` P1~P6** — 통과 시에만 `docs/통합문서.md` 로 복사
- [ ] **Step 4: 갈래 색인 표** — 행 순서를 6갈래 순으로, 「실콘솔」 행을 버튼 검출 행 절 목록에 합침(현행 값·미결 칸은 버튼 검출 행 그대로). 이 표 변경만 편집 · P6 재실행
- [ ] **Step 5: 커밋** — `docs(통합문서): §10 기능별 재배치 · 절 구성 9블록 — 원문 무변경(관문 P1~P6)`

### Task 4: 새 절 규칙 · 관문 P7

- [ ] **Step 1: §10 머리 「🔖 유효성 마커 규약」 다음에 「🧱 절 구성 규약」 추가** — 새 절은 해당 갈래 끝 · 9블록 순서 · 라벨 형식 · 빈 블록·단일 블록 라벨 없음 · 번호는 이어서 부여(spec §3.5)
- [ ] **Step 2: `CLAUDE.md` §3 §10 불릿 1줄** — 「새 절은 해당 갈래 끝에 넣고 §10 머리 절 구성 규약 순서로 쓴다」 · **`work-close` 2.3** 「측정값 → §10 새 절」에 `(절 구성 규약 — §10 머리)` 포인터
- [ ] **Step 3: P7** — `work-close` 2.5 스크립트 · `grep -rhoE "§ ?10\.[0-9]+"` 세 저장소 번호 집합 ⊆ 통합문서 절 번호 집합 · P6 재실행(머리 변경 = 색인 표 + 규약 절만)
- [ ] **Step 4: 커밋** — `docs(규칙): §10 절 구성 규약 — 새 절은 갈래 끝 · 9블록 순서`

### Task 5: 표본 대조 · 최종 리뷰

- [ ] **Step 1: 표본 3절 전후 대조 보고(P8)** — 초기 짧은 절 1 · 참조 제한이 걸린 절 1 · 가장 큰 절 1: 전 하위절 순서 → 후 블록·순서 표 + P1 결과
- [ ] **Step 2: `superpowers:requesting-code-review`** — 기준 = spec + 사용자 제약 원문 「기존 구성에서 공통구성으로 변경 될 시 내용은 절대로 변경되서는 안되 기존 내용을 유지하여야만해」 · Critical/Important 반영 후 P1~P7 재실행
- [ ] **Step 3: 공동 확인**

### Task 6: work-close

- [ ] 공동 확인 뒤 `work-close` — 정본 = 통합문서 §10(재배치 자체가 결과 — 갈래 색인 머리에 작업 요약 2~3줄 + spec·plan 경로) · spec 상태 줄 · 작업로그 · push 한 번
