# 변경이력 문서 신설 — 실행 계획서

> **실행자에게:** 태스크 단위로 실행한다. 각 태스크 끝에 **검증 명령 + 기대 출력**과 **커밋**이 있다. 마지막에 사용자 공동 확인을 받고 `work-close` 로 닫는다(CLAUDE.md §2 — Claude 단독 「완료」 선언 금지).

**목표:** 폐기된 사실만 담는 `docs/변경이력.md` 를 만들고, 통합문서·README·`work-close` 의 규칙을 그 문서를 가리키게 고친다.

**방법:** ①문서를 만들고 2026-09-18 구조 재편에서 지운 것으로 첫 채움 → ②규칙 4곳을 **한 커밋으로** 고쳐 문서 간 모순이 생기는 순간이 없게 한다 → ③자가 점검 → 공동 확인.

**대상 파일:** `docs/변경이력.md`(신설) · `docs/통합문서.md`(R2·R3·R12) · `README.md` · `.claude/skills/work-close/SKILL.md`

**설계(spec):** `docs/superpowers/specs/2026-09-18-변경이력-문서-design.md`

---

## 전역 제약

- 🔴 **이 문서에는 폐기된 사실만 쓴다.** 현행 값은 적지 않고 `§N 참조` 포인터만 둔다. 「8개 → 5개」가 아니라 **「8개였다」**.
- 🔴 **§12 를 다루지 않는다** — 승계 사슬(`§12.6 → §12.21`)·갈래 색인을 복제하지 않는다. 첫머리 경계 한 줄과 근거 열의 `§12.N` 포인터만 허용. 판정 기준 = **「이 줄이 §12 안에 있는가」**.
- 🔴 **경위는 옮기지 않는다**(R9) — 「왜」 열은 **한 줄**, 상세는 근거(커밋 해시·§12.N)로 넘긴다.
- 🔴 **주제 절은 맨 뒤에 덧붙이기만** — 지운 번호는 재사용 금지.
- 각 주제 절 머리에 **`> 현행 = §N`** 한 줄 필수.
- 열 5개 고정 — 시점 · 폐기된 사실 · 폐기 시점 · 왜 · 근거.
- 커밋 서명: `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. push 는 `work-close` 에서 한 번.

---

## Task 0: 첫 채움 원문 확보 (커밋 없음)

**파일:** 수정 없음 — 읽기만

- [ ] **Step 1: 지운 원문을 꺼낸다**

```bash
cd /home/pi/sop-project
# 2026-09-18 구조 재편 직전본에서 옛 §5.3 을 통째로 꺼낸다
git show d61650b^:docs/통합문서.md | sed -n '/^### 5\.3 구성 요소 요약/,/^---$/p' > /tmp/구5.3.txt
wc -l /tmp/구5.3.txt        # 기대: 60줄 안팎
grep -n '📜 \*\*이력\*\*\|⛔ 구 배치도\|이전 배치 (2026-08-06\|배치 통일 경위' /tmp/구5.3.txt
```
기대: 네 항목이 모두 잡힌다.

- [ ] **Step 2: 디스플레이 오기 정정 원문을 꺼낸다**

```bash
cd /home/pi/sop-project
git show 65534cb | grep -E '^-.*터치 디스플레이|^-.*출력 전용' | cut -c1-200
```
기대: `- **터치 디스플레이** ✅: … **출력 전용**(입력 아님), 단 해제 버튼 UI는 여기 배치` 가 보인다.

- [ ] **Step 3: 커밋 없음** — 다음 태스크로.

---

## Task 1: `docs/변경이력.md` 신설 + 첫 채움

**파일:**
- Create: `docs/변경이력.md`

- [ ] **Step 1: 파일을 아래 내용 그대로 만든다**

```markdown
# 변경이력 — 통합문서의 사실이 어떻게 바뀌어 왔나

> 🔴 **여기에는 폐기된 사실만 있다.** 지금 맞는 것은 [`통합문서.md`](통합문서.md) 가 정본이다. 이 문서에 현행 값은 적지 않는다 — 각 절 머리의 `> 현행 = §N` 을 따라간다.
> 🔴 **측정 수치는 여기 없다** — §12 절의 `⛔ SUPERSEDED-BY` 마커와 갈래 색인이 정본이다. 승계 사슬·색인을 여기 복제하지 않는다.
> **정렬은 주제순**이다. 그날 무엇을 했나(시간순) = [`작업로그.md`](작업로그.md) · 작업 전 설계·버린 대안 = [`superpowers/specs/`](superpowers/specs/).
>
> 🆕 **쓰는 법** — 통합문서의 현행 사실을 덮어쓸 때 **지우기 전에** 여기로 옮긴다(`work-close` 2.4). 주제 절은 **맨 뒤에 덧붙이기만** 하고 지운 번호는 재사용하지 않는다. 열은 5개 고정이며 「왜」는 한 줄, 상세는 근거로 넘긴다(R9).
> 설계 = `docs/superpowers/specs/2026-09-18-변경이력-문서-design.md`

---

## 1. 버튼 구성·수량

> 현행 = 통합문서 §14.1(수량·색) · §5.4(배치)

| 시점 | 폐기된 사실 | 폐기 시점 | 왜 | 근거 |
| --- | --- | --- | --- | --- |
| 2026-06-09 | 아케이드 버튼 **8개**(4색 ×2개씩) — 좌 2×2 사용(B1~B4) / 우 2×2 미사용(확장용) | 2026-08-06 | 실콘솔은 확장 2×2 를 두지 않기로 확정했다 | `65534cb` |

🔴 **이 줄이 2026-08-29 오정정의 원인이었다** — 실물은 2026-08-06 에 5개로 확정됐는데 통합문서 4곳이 3주간 8개인 채였고, 제출문서 작업이 그 낡은 서술을 근거로 「8개」라고 잘못 정정했다.

## 2. 버튼 배치

> 현행 = 통합문서 §5.4 「⭐ 버튼 배치도 — 실콘솔 전면부 포맥스판」

| 시점 | 폐기된 사실 | 폐기 시점 | 왜 | 근거 |
| --- | --- | --- | --- | --- |
| 2026-06-09 | 배치도 **Z자** — 상단열 `[B1 노랑][B2 흰]` · 하단열 `[B3 핑크][B4 검정]` · EMO 는 2×2 한가운데 | 2026-08-10 | 이 배치도가 **모조 콘솔 실물과 어긋나 있었다**(실물·학습 데이터가 정본) | §12.32 |
| 2026-08-06 | 포맥스판 제작 당시 배치 — 위 Z자를 그대로 따르고 EMO 만 최하단 중앙 | 2026-08-10 | `console_v2` 학습 분포와 **세 자리가 어긋나** B3→B1 오분류가 났다 | §12.33 |
| 2026-06-09 | EMO 위치 = 2×2 **한가운데** | 2026-08-06 | 포맥스판에 그 자리 구멍이 없고, 가운데 두면 B3 로 뻗는 손이 EMO 박스에 걸쳐 오경보가 난다 | §12.32 |

## 3. 터치 디스플레이 입력

> 현행 = 통합문서 §14.1

| 시점 | 폐기된 사실 | 폐기 시점 | 왜 | 근거 |
| --- | --- | --- | --- | --- |
| 2026-06 | 제우스 Z10 = **출력 전용**(입력 아님), 해제 버튼 UI 만 여기 배치 | 2026-08-29 | 오기였다 — 경고·차단 해제를 작업자가 화면 터치로 한다 | `65534cb` |
```

- [ ] **Step 2: 검증 — 전역 제약을 지켰는가**

```bash
cd /home/pi/sop-project
echo "① 현행 값이 새어 들어갔나 (0이어야):"
grep -cE '5개|총 5|반시계 U자|출력 \+ 터치' docs/변경이력.md
echo "② §12 승계 사슬이 있나 (0이어야):"
grep -cE '§12\.[0-9]+ *(→|~) *§12\.[0-9]+' docs/변경이력.md
echo "③ 갈래 색인 복제가 있나 (0이어야):"
grep -cE '갈래 ①|갈래 ②|갈래 색인' docs/변경이력.md
echo "④ 주제 절마다 「현행 =」 이 있나 (절 수와 같아야):"
echo "   절 $(grep -c '^## [0-9]' docs/변경이력.md) · 현행줄 $(grep -c '^> 현행 = ' docs/변경이력.md)"
echo "⑤ 표 열이 5개인가:"
grep -c '^| --- | --- | --- | --- | --- |' docs/변경이력.md
```
기대: ① 0 · ② 0 · ③ 0 · ④ 절 3 · 현행줄 3 · ⑤ 3

🔴 ①이 0 이 아니면 **현행 값을 옮겨 적은 것**이다 — 그 줄을 「폐기된 사실」로만 고쳐 쓴다.

- [ ] **Step 3: 커밋**

```bash
cd /home/pi/sop-project
git add docs/변경이력.md
git commit -m "$(cat <<'EOF'
docs(변경이력): 문서 신설 — 폐기된 사실만 담는다 · 첫 채움 3주제 5건

2026-09-18 구조 재편에서 R5·R9 근거로 지운 것이 갈 자리를 만든다.
버튼 수량(8개) · 버튼 배치(Z자·EMO 한가운데·포맥스판 제작 당시) ·
터치 디스플레이(출력 전용 오기).

현행 값은 적지 않고 각 절 머리의 「현행 = §N」 포인터로 넘긴다 —
통합문서와 겹치는 값이 하나도 없게 해 R5 를 형태로 강제한다.
§12 측정은 다루지 않는다(⛔ SUPERSEDED-BY 마커가 정본).

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: 규칙·경계 반영 (한 커밋)

> 🔴 **한 커밋으로 묶는다** — R2 만 고치고 README 가 옛말이면 그 사이 상태가 서로 모순이다.

**파일:**
- Modify: `docs/통합문서.md` (R2 · R3 · R12)
- Modify: `README.md` (문서별 역할·경계 표)
- Modify: `.claude/skills/work-close/SKILL.md` (2.3 · 2.4)

- [ ] **Step 1: R2 를 교체한다**

찾을 줄:
```
> - **R2 이력 보존** — 지우지 않고 `⛔` 또는 취소선+정정으로 무효화한다. 분량은 제약이 아니다. 단, **장을 다시 짤 때 새 구조에서 쓰이지 않는 폐기 내용은 지운다** — 폐기 사실은 커밋 메시지·작업로그에 남는다(R9).
```
바꿀 줄:
```
> - **R2 폐기는 옮긴다** — 폐기된 사실은 [`변경이력.md`](변경이력.md) 로 옮기고 이 문서 본문에는 **현행만** 둔다. 옛 서술을 현행으로 읽은 사고가 실제로 났다(2026-08-29 버튼 수). **옮기지 않고 그냥 지우지 않는다.** 🔴 **예외 = §12** — 측정 절은 본문에 남기고 머리에 `⛔ SUPERSEDED-BY §12.N` 마커를 단다(R6). §12 의 이력을 `변경이력.md` 에 복제하지 않는다.
```

- [ ] **Step 2: R3 를 교체한다**

찾을 줄:
```
> - **R3 쪼개지 않는다** — 문서를 나누거나 사본을 만들지 않는다. grep 대상이 나뉘면 한쪽만 읽고 놓친다.
```
바꿀 줄:
```
> - **R3 쪼개는 축은 하나뿐** — 문서를 주제로 나누거나 사본을 만들지 않는다(grep 대상이 나뉘면 한쪽만 읽고 놓친다). **나누는 축은 「현행 / 폐기」 하나다** — 현행 = 이 문서 · 폐기 = [`변경이력.md`](변경이력.md). 같은 사실을 양쪽에 두지 않는다.
```

- [ ] **Step 3: R12 의 기능 장 규약에 갈 곳을 적는다**

R12 줄에서 찾을 조각:
```
기능 장은 **현행 동작만** 담고 바뀌면 그 자리를 고쳐 쓴다(이력·측정 = §12 · 경위 = 커밋·작업로그 R9).
```
바꿀 조각:
```
기능 장은 **현행 동작만** 담고 바뀌면 그 자리를 고쳐 쓴다(**덮어쓴 옛 사실은 `변경이력.md` 로 옮긴다** R2 · 측정 = §12 · 경위 = 커밋·작업로그 R9).
```

- [ ] **Step 4: README 역할표에 한 줄 넣는다**

`| 설계·사양·SOP·하드웨어 | docs/통합문서.md |` 줄 **바로 다음**에:
```
| **폐기된 사실** — 예전엔 어땠나 | `docs/변경이력.md` (측정 수치는 제외 — §12 `⛔` 마커가 정본) |
```

- [ ] **Step 5: `work-close` 에 옮기기를 넣는다**

2.3 「결과 사실의 제자리」 문단 끝에 한 문장을 잇는다:
```
🔴 **현행 사실을 덮어쓸 때는 지우기 전에 옛 사실을 `docs/변경이력.md` 주제 절로 옮긴다**(R2 — §12 측정 절은 제외, 그쪽은 `⛔` 마커가 담당).
```

2.4 「완료 전환」 표에 행을 하나 더한다(`| §12 측정 절 |` 행 **앞**에):
```
| 덮어쓰는 현행 사실(§12 밖) | 옛 사실을 먼저 `docs/변경이력.md` 주제 절로 옮기고, 그다음 본문을 현행으로 덮어쓴다 |
```

- [ ] **Step 6: 검증 — 규칙이 서로 모순되지 않는가**

```bash
cd /home/pi/sop-project
echo "① 옛 R2·R3 문구가 남았나 (0이어야):"
grep -cE 'R2 이력 보존|R3 쪼개지 않는다' docs/통합문서.md
echo "② 새 R2·R3 가 들어갔나 (각 1):"
grep -c 'R2 폐기는 옮긴다' docs/통합문서.md; grep -c 'R3 쪼개는 축은 하나뿐' docs/통합문서.md
echo "③ 변경이력을 가리키는 곳 4군데:"
for f in docs/통합문서.md README.md .claude/skills/work-close/SKILL.md; do echo "   $f : $(grep -c '변경이력' $f)"; done
echo "④ R12 에 갈 곳이 적혔나 (1):"
grep -c '덮어쓴 옛 사실은 `변경이력.md` 로 옮긴다' docs/통합문서.md
echo "⑤ 규칙 개수 R1~R12 그대로 (12):"
grep -oE '\*\*R[0-9]+ ' docs/통합문서.md | sort -u | wc -l
```
기대: ① 0 · ② 1·1 · ③ 통합문서 ≥3 · README 1 · work-close 2 · ④ 1 · ⑤ 12

🔴 ⑤가 12 가 아니면 규칙을 **더하거나 지운 것**이다 — 이번 작업은 **개정**이지 증설이 아니다.

- [ ] **Step 7: 훅·스킬이 깨지지 않았는지**

```bash
cd /home/pi/sop-project
python3 .claude/hooks/test_banner.py && python3 .claude/hooks/test_session_archive.py
```
기대: 두 관문 모두 ✅ 통과.

- [ ] **Step 8: 커밋**

```bash
cd /home/pi/sop-project
git add docs/통합문서.md README.md .claude/skills/work-close/SKILL.md
git commit -m "$(cat <<'EOF'
docs(정본): R2·R3 개정 + R12 보완 — 폐기된 사실은 변경이력.md 로 옮긴다

- R2 「이력 보존(지우지 않고 ⛔)」 → 「폐기는 옮긴다」. 본문에는 현행만 둔다.
  예외 = §12(측정 절은 본문에 남기고 ⛔ SUPERSEDED-BY 마커 — R6).
- R3 「쪼개지 않는다」 → 「쪼개는 축은 하나뿐」. 나누는 축은 현행/폐기 하나이고
  같은 사실을 양쪽에 두지 않는다.
- R12 기능 장 규약에 갈 곳 명시(덮어쓴 옛 사실 → 변경이력.md).
- README 역할표 1줄 · work-close 2.3·2.4 에 「지우기 전에 옮긴다」.

규칙 개수는 R1~R12 그대로 — 증설이 아니라 개정이다.
CLAUDE.md 는 고치지 않는다(§3 이 규칙을 가리키기만 해 자동 반영).

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: 자가 점검 → 공동 확인 → `work-close`

**파일:** 수정 없음

- [ ] **Step 1: 겹침 0 을 기계로 확인한다** 🔴 사용자 지시의 관문

```bash
cd /home/pi/sop-project
python3 - <<'EOF'
import re
ch = open('docs/변경이력.md').read()
tm = open('docs/통합문서.md').read()

# §12 를 다루지 않는가
chain = re.findall(r'§12\.\d+\s*(?:→|~)\s*§12\.\d+', ch)
print('① §12 승계 사슬 :', chain or '✅ 없음')
print('② 갈래 색인 복제 :', re.findall(r'갈래 [①-⑥]|갈래 색인', ch) or '✅ 없음')

# 절 머리 포인터
secs = re.findall(r'(?m)^## \d+\. (.+)$', ch)
ptrs = re.findall(r'(?m)^> 현행 = ', ch)
print('③ 주제 절 %d개 / 현행 포인터 %d개 :' % (len(secs), len(ptrs)),
      '✅ 일치' if len(secs) == len(ptrs) else '❌ 불일치')

# 폐기된 사실이 통합문서에도 남아 있지 않은가 (복제 검사)
DEAD = ['버튼이 **8개**', '우 2×2는 미사용', '우 2×2 미사용',
        '출력 전용', '2×2 한가운데', '제작 당시, 폐기', '배치 통일 경위']
print('④ 폐기 문구가 통합문서에도 있나 :',
      [k for k in DEAD if k in tm] or '✅ 없음')
EOF
```
기대: ① ✅ 없음 · ② ✅ 없음 · ③ ✅ 일치 · ④ ✅ 없음

🔴 ④가 비지 않으면 **같은 폐기 서술이 두 곳에 있다** — 통합문서 쪽을 지운다(R2·R5).

- [ ] **Step 2: 사용자에게 고정 형식으로 보고한다**

파일을 열게 하지 않는다(CLAUDE.md §2). 보고할 것 — 신설 문서의 절 구성과 첫 채움 5건 · 개정된 R2·R3·R12 문안 · 위 관문 출력 · ✋ Claude 가 정한 것.

- [ ] **Step 3: 확인을 받은 뒤 `work-close`**

🔴 Claude 단독 완료 선언 금지. 사용자 확인 뒤 `work-close` 로 정본 기록·작업로그·push 를 한 번에 한다.

---

## 🟦 이 계획이 안 하는 것

- **옮기기를 잊는 것을 기계로 막지 않는다** — 같은 목표의 훅이 세 번 실패했다(CLAUDE.md §3). 절차(`work-close` 2.4)로 돌려보고 실제로 빠뜨리면 그때 hookify 를 검토한다(spec §8·§9).
- **실물 변경 ↔ 정본 갱신의 시차**(2026-08-29 사고의 진짜 원인)는 다루지 않는다 — 별도 과제(spec §9).
- **소급 채움은 장별 검토 §4~ 와 함께** 한다 — 이 계획에서는 첫 채움 3주제만 한다(spec §7).
