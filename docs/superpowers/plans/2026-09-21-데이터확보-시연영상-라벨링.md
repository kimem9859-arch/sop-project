# 비전 모델 재정립 ② 데이터 확보 — 시연영상 2,086장 라벨링 사이클 실행 계획

> **상태(2026-09-21 작성)** — ▶ 착수 전. 설계서 ② 사용자 승인 완료(2026-09-21).
> ⚠️ **아래 체크박스는 실행 중 갱신하지 않는다** — 진행 상태는 이 줄과 `docs/작업로그.md` 가 정본이다(이 저장소 관례).
> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:executing-plans` 인라인 실행. 사람 작업(라벨링·검수)이 중간에 끼어 병렬성이 없다.

**Goal:** 이미 업로드된 시연영상 프레임 2,086장을 **확정 라벨 규칙대로 라벨하고, 검수 관문을 통과시켜 학습에 쓸 수 있는 상태**로 만든다.

**Architecture:** 사람이 그리고(Roboflow 웹) 기계가 훑는다(파이 도구). Claude 는 ①준비물(배분표·워밍업 목록) ②기계 점검 도구 ③결과 보고를 맡고, 그리기와 최종 판정은 사람이 한다. 사람 작업과 기계 작업을 태스크 단위로 갈라 **누가 하는 일인지 제목에 표시**한다.

**Tech Stack:** Roboflow 웹 UI(라벨링) · Roboflow MCP(조회·내보내기) · 파이 `python3` + Pillow/NumPy(점검 도구) · YOLO 형식 export

**Spec:** `docs/superpowers/specs/2026-09-16-비전모델-데이터확보-design.md`

## Global Constraints

- 클래스는 **8종 고정** — 버튼 `B1` `B2` `B3` `B4` `EMO` · 공구 `driver` `wrench` `pliers`. **쥔/놓인 구분 없음**(spec §3.1)
- 라벨 규칙 정본 = Rpi5 `Demo/docs/labeling_guide.md` — 버튼은 동그라미 전체, 공구는 보이는 부분만, 갈라진 조각도 **박스 하나**, 손·장갑은 박스 없음, ⅓ 이하·불확실은 **그리지 않음**(spec §3.2)
- 라벨 프로젝트는 **`eung-min/console_v4` 한 곳** — 기존 `tool-holdout`·`console_v2-mjefr` 와 섞지 않는다(spec §8)
- 배분 단위는 **영상(세션) 통째** — 한 영상을 두 사람이 나눠 그리지 않는다(spec §8)
- 계정은 **2자리뿐**(무료 플랜) — 팀원 1명 초대 + 나머지 한 사람은 사용자 계정 사용(2026-09-21 사용자 결정)
- 분할은 이미 정해져 있다 — train 1,665 / valid 421 / test 0. **재분할·자동 Rebalance 금지**(spec §8)
- 측정 수치는 이 문서에 쓰지 않는다 — 결과는 통합문서 §12 가 정본
- 도구는 **임계·경로를 `config` 나 인자에서 읽는다** — 기본값을 새로 정하지 않는다(CLAUDE.md 함정: 도구 기본값이 config 를 안 따라 4번 물렸다)

---

### Task 1: 설계서 §9 검수 기준 정정 — 면적비는 지금 클래스 구성에서 성립하지 않는다 `[Claude]`

**왜** — spec §9 의 기계 점검 ①은 「**쥔 사진 ÷ 놓인 사진** 면적비」다. 이 값은 `tool_v4` 가 `wrench` 와 `wrench-in-hand` 를 **다른 클래스로** 두었기에 계산됐다(§12.63). 그런데 §3.1 에서 **쥔/놓인 구분을 없앴으므로 분모·분자를 나눌 기준이 사라졌다.** 같은 신호(조각 박스)를 **클래스 안의 이상치**로 바꿔 잡는다.

**Files:**
- Modify: `docs/superpowers/specs/2026-09-16-비전모델-데이터확보-design.md` §9 「기계 점검(전량)」 항목

**Interfaces:**
- Produces: Task 2 의 도구가 구현할 판정 기준 4종(아래 문안 그대로)

- [ ] **Step 1: §9 기계 점검 문안을 아래로 교체**

```markdown
  - **기계 점검(전량)** — 라벨 export 를 `test/label_audit.py` 로 훑는다.
    ①**클래스 이름** — 8종 밖의 이름이 하나라도 있으면 실패(오타·미등록).
    ②**조각 박스 의심** — 클래스 안에서 **면적이 그 클래스 중앙값의 0.5 미만**이거나 **종횡비가 중앙값의 절반 이하로 납작·가늘어진** 박스를 목록으로 뽑는다. 0건이 목표가 아니라 **목록을 사람이 본다**(`tool_v4` 에서 `wrench` 면적비 0.89 로 드러난 것과 같은 신호를, 쥔/놓인 클래스 없이 잡는 방식).
    ③**배경 비율** — 박스 0개 사진 비율을 **보고만 한다.** 업로드분에서 배경을 지우지 않고 학습 버전의 Filter Null 로 맞추므로(§6), 이 값은 **합격 기준이 아니다.**
    ④**클래스별 장수** — 8종 각각의 사진 수·박스 수를 표로 남긴다(§6 의 「판정은 클래스별 장수로」 근거).
```

- [ ] **Step 2: 검증** — `grep -n "쥔 사진 중앙값" docs/superpowers/specs/2026-09-16-비전모델-데이터확보-design.md` 출력이 **0줄**이어야 한다(옛 기준이 남아 있으면 두 기준이 공존한다)

- [ ] **Step 3: 커밋**

```bash
cd /home/pi/sop-project
git add docs/superpowers/specs/2026-09-16-비전모델-데이터확보-design.md
git commit -m "docs(spec): ② 검수 기계 점검 — 쥔/놓인 면적비를 클래스 내 이상치로 교체"
```

---

### Task 2: 라벨 점검 도구 `label_audit.py` `[Claude]`

**Files:**
- Create: `Rpi5/Demo/test/label_audit.py`
- Test: 같은 파일의 `--selftest`(이 저장소에는 pytest 가 없다 — 기존 `test/*.py` 도구들과 같은 방식으로 **합성 입력 자가시험**을 도구 안에 둔다)

**Interfaces:**
- Consumes: Roboflow YOLO export 폴더 — `data.yaml`(키 `names`) + `train/labels/*.txt`·`valid/labels/*.txt`(YOLO 정규화 형식 `cls cx cy w h`)
- Produces: 표준출력 보고 + `--json <경로>` 로 같은 내용을 파일로. 종료 코드 = **0 통과 / 1 실패**(①·③ 위반 시 1)

- [ ] **Step 1: 자가시험을 먼저 쓴다 — 합성 라벨로 기대값을 박아 둔다**

```python
def _selftest() -> int:
    """합성 export 를 만들어 판정 4종이 기대대로 나오는지 확인한다."""
    import tempfile, json as _json
    with tempfile.TemporaryDirectory() as d:
        root = Path(d)
        (root / "train" / "labels").mkdir(parents=True)
        (root / "data.yaml").write_text(
            "names: [B1, B2, B3, B4, EMO, driver, wrench, pliers]\n", encoding="utf-8")
        L = root / "train" / "labels"
        # wrench(6) 정상 박스 4개 — 면적 0.02, 종횡비 4.0(길쭉)
        for i in range(4):
            (L / f"ok{i}.txt").write_text("6 0.5 0.5 0.2828 0.0707\n", encoding="utf-8")
        # wrench 조각 박스 1개 — 면적 0.004(중앙값의 0.2배), 종횡비 1.0
        (L / "frag.txt").write_text("6 0.5 0.5 0.0632 0.0632\n", encoding="utf-8")
        # 배경(박스 0개) 1장
        (L / "bg.txt").write_text("", encoding="utf-8")
        r = audit(root)
    assert r["unknown_classes"] == [], r["unknown_classes"]
    assert [s["file"] for s in r["suspects"]] == ["frag.txt"], r["suspects"]
    assert r["background_ratio"] == 1 / 6, r["background_ratio"]
    assert r["per_class"]["wrench"]["images"] == 5, r["per_class"]
    assert r["per_class"]["wrench"]["boxes"] == 5, r["per_class"]
    print("selftest OK")
    return 0
```

- [ ] **Step 2: 실행해서 실패하는 것을 확인한다**

Run: `python3 Rpi5/Demo/test/label_audit.py --selftest`
Expected: FAIL — `NameError: name 'audit' is not defined`

- [ ] **Step 3: 최소 구현** — 아래 모듈을 같은 파일에 쓰고, **Step 1 의 `_selftest()` 함수는 그대로 둔다**(`main()` 이 `--selftest` 로 부른다)

```python
"""라벨 export 를 훑어 검수 전에 기계가 잡을 수 있는 것을 잡는다 — spec §9 기계 점검.

왜 필요한가:
    `tool_v4` 는 「양 끝을 감싸는 박스 하나」 지침이 라벨 작업자에게 전달되지 않아
    렌치 라벨이 **머리 조각만** 남았고, 그 사실이 **채점 단계에서야** 드러나 한 사이클을
    통째로 버렸다(§12.63). 조각 박스는 같은 클래스의 다른 박스보다 **작고 덜 길쭉하다** —
    사람이 표본으로 훑기 전에 기계가 전량에서 그 후보를 뽑아낸다.

사용법:
    python3 test/label_audit.py <export폴더>            # 보고만
    python3 test/label_audit.py <export폴더> --json r.json
    python3 test/label_audit.py --selftest              # 합성 입력 자가시험

판정:
    ① 8종 밖 클래스 이름이 있다 → **종료 코드 1(실패)**
    ②③④ 조각 박스 의심 목록 · 배경 비율 · 클래스별 장수는 **보고만** 한다 — 사람이 본다.
       🔑 배경 비율을 실패로 삼지 않는 이유 = 업로드분에서 배경을 지우지 않고
          학습 버전의 Filter Null 로 맞추기 때문이다(spec §6).
"""
from __future__ import annotations
import argparse, json, statistics, sys
from pathlib import Path

CLASSES = ["B1", "B2", "B3", "B4", "EMO", "driver", "wrench", "pliers"]


def _read_names(root: Path) -> list[str]:
    txt = (root / "data.yaml").read_text(encoding="utf-8")
    line = next(l for l in txt.splitlines() if l.strip().startswith("names:"))
    return [s.strip().strip("'\"") for s in line.split("[", 1)[1].rsplit("]", 1)[0].split(",")]


def audit(root: Path, bg_max: float = 0.10) -> dict:
    names = _read_names(root)
    unknown = [n for n in names if n not in CLASSES]
    label_files = sorted(p for split in ("train", "valid", "test")
                         for p in (root / split / "labels").glob("*.txt")) \
        if any((root / s).exists() for s in ("train", "valid", "test")) \
        else sorted((root / "labels").glob("*.txt"))
    per_class: dict[str, dict] = {n: {"images": 0, "boxes": 0} for n in names}
    boxes: dict[str, list[tuple[str, float, float]]] = {n: [] for n in names}
    empty = 0
    for f in label_files:
        rows = [r.split() for r in f.read_text(encoding="utf-8").splitlines() if r.strip()]
        if not rows:
            empty += 1
            continue
        seen = set()
        for r in rows:
            n = names[int(r[0])]
            w, h = float(r[3]), float(r[4])
            per_class[n]["boxes"] += 1
            boxes[n].append((f.name, w * h, (w / h) if h else 0.0))
            seen.add(n)
        for n in seen:
            per_class[n]["images"] += 1
    suspects = []
    for n, bs in boxes.items():
        if len(bs) < 4:            # 표본이 너무 적으면 중앙값이 뜻을 갖지 못한다
            continue
        med_a = statistics.median(b[1] for b in bs)
        med_r = statistics.median(b[2] for b in bs)
        for fn, a, r in bs:
            if a < med_a * 0.5 or r < med_r * 0.5:
                suspects.append({"file": fn, "cls": n, "area": a, "ratio": r,
                                 "median_area": med_a, "median_ratio": med_r})
    total = len(label_files)
    return {"names": names, "unknown_classes": unknown, "per_class": per_class,
            "images": total, "background_images": empty,
            "background_ratio": (empty / total) if total else 0.0,
            "background_max": bg_max, "suspects": suspects}


def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("export", nargs="?", type=Path)
    ap.add_argument("--json", type=Path)
    ap.add_argument("--bg-max", type=float, default=0.10)
    ap.add_argument("--selftest", action="store_true")
    a = ap.parse_args()
    if a.selftest:
        return _selftest()
    if not a.export:
        ap.error("export 폴더를 주거나 --selftest 를 쓴다")
    r = audit(a.export, a.bg_max)
    print(f"사진 {r['images']}장 · 배경 {r['background_images']}장 "
          f"({r['background_ratio']:.1%} / 상한 {r['background_max']:.0%})")
    for n, v in r["per_class"].items():
        print(f"  {n:8s} 사진 {v['images']:5d} · 박스 {v['boxes']:5d}")
    print(f"① 클래스 이름 {'❌ ' + str(r['unknown_classes']) if r['unknown_classes'] else '✅ 8종'}")
    print(f"② 조각 박스 의심 {len(r['suspects'])}건 — 사람이 본다")
    for s in r["suspects"][:20]:
        print(f"  {s['cls']:8s} {s['file']} 면적 {s['area']:.4f}(중앙 {s['median_area']:.4f})"
              f" 종횡비 {s['ratio']:.2f}(중앙 {s['median_ratio']:.2f})")
    print(f"③ 배경 비율 {r['background_ratio']:.1%} — 보고만(학습 버전에서 Filter Null 로 맞춘다)")
    if a.json:
        a.json.write_text(json.dumps(r, ensure_ascii=False, indent=1), encoding="utf-8")
    return 1 if r["unknown_classes"] else 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 자가시험 통과 확인**

Run: `python3 Rpi5/Demo/test/label_audit.py --selftest`
Expected: `selftest OK`

- [ ] **Step 5: 커밋**

```bash
cd /home/pi/sop-project/Rpi5
git add Demo/test/label_audit.py
git commit -m "feat(test): label_audit — 라벨 export 기계 점검(클래스·조각 박스·배경 비율)"
```

---

### Task 2b: 깨진 프레임 검출 도구 `frame_health.py` `[Claude]`

**왜** — 깨진 프레임은 **장소2·3 새 촬영에서도 난다.** 2026-09-21 에 임시 스크립트로 찾았으나 남겨 두지 않으면 그때 또 눈으로 찾아야 한다. 🔴 **밝기 지표만으로는 안 된다** — 콘솔 모서리의 밝기 경계가 깨짐으로 잡혔다(`165513_f00029` 오탐).

**Files:**
- Create: `Rpi5/Demo/test/frame_health.py`

**Interfaces:**
- Consumes: 프레임 폴더(들) — PNG/JPG
- Produces: 후보 목록 파일(`--out`) + 표준출력 보고. Task 5 가 그 목록을 훑는다

- [ ] **Step 1: 자가시험을 먼저 쓴다 — 합성 이미지로 두 지표를 확인**

```python
def _selftest() -> int:
    import tempfile
    import numpy as np
    from PIL import Image
    with tempfile.TemporaryDirectory() as d:
        root = Path(d)
        a = np.zeros((64, 64, 3), np.uint8); a[:, :, :] = 90          # 평범한 회색 = 정상
        Image.fromarray(a).save(root / "ok.png")
        b = a.copy(); b[:, :32] = (200, 255, 0)                        # 왼쪽 절반 형광 초록
        Image.fromarray(b).save(root / "tint.png")
        c = a.copy(); c[:, :48] = 250                                  # 왼쪽이 평탄한 흰색
        Image.fromarray(c).save(root / "washed.png")
        r = {x["file"]: x for x in scan([root])}
    assert r["tint.png"]["suspect"], r["tint.png"]
    assert r["washed.png"]["suspect"], r["washed.png"]
    assert not r["ok.png"]["suspect"], r["ok.png"]
    print("selftest OK")
    return 0
```

- [ ] **Step 2: 실행해서 실패를 확인**

Run: `python3 Rpi5/Demo/test/frame_health.py --selftest`
Expected: FAIL — `NameError: name 'scan' is not defined`

- [ ] **Step 3: 구현** — Step 1 의 `_selftest()` 는 파일에 그대로 둔다. 모듈 머리 설명에 담을 것: 세 형태(형광 초록 뭉갬·색 어긋난 띠·흰색 날아감)·🔴 밝기 지표로는 안 갈린다는 이유·🔴 자동 삭제 금지

```python
from __future__ import annotations
import argparse, sys
from pathlib import Path
import numpy as np
from PIL import Image

EXT = {".png", ".jpg", ".jpeg"}


def metrics(path: Path) -> tuple[float, float]:
    rgb = np.asarray(Image.open(path).convert("RGB"), dtype=np.float32) + 1.0
    col = rgb.mean(axis=0)                                  # 열 평균색
    chrom = col / col.sum(axis=1, keepdims=True)            # 밝기를 뺀 색조
    seam = float(np.abs(np.diff(chrom, axis=0)).sum(axis=1).max())
    g = rgb.mean(axis=2)
    h, w = g.shape[0] // 8 * 8, g.shape[1] // 8 * 8
    blk = g[:h, :w].reshape(h // 8, 8, w // 8, 8)
    washed = float(((blk.mean(axis=(1, 3)) > 200) & (blk.std(axis=(1, 3)) < 4)).mean())
    return seam, washed


def scan(roots: list[Path], seam_max: float = 0.02, washed_max: float = 0.25) -> list[dict]:
    rows = []
    for root in roots:
        for p in sorted(root.rglob("*")):
            if p.suffix.lower() not in EXT:
                continue
            s, w = metrics(p)
            rows.append({"file": p.name, "path": str(p), "seam": s, "washed": w,
                         "suspect": bool(s > seam_max or w > washed_max)})
    return rows


def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("roots", nargs="*", type=Path)
    ap.add_argument("--out", type=Path)
    ap.add_argument("--seam", type=float, default=0.02)
    ap.add_argument("--washed", type=float, default=0.25)
    ap.add_argument("--selftest", action="store_true")
    a = ap.parse_args()
    if a.selftest:
        return _selftest()
    if not a.roots:
        ap.error("폴더를 주거나 --selftest 를 쓴다")
    rows = scan(a.roots, a.seam, a.washed)
    bad = [r for r in rows if r["suspect"]]
    print(f"사진 {len(rows)}장 · 깨짐 후보 {len(bad)}장 ({len(bad)/max(len(rows),1):.1%})")
    for r in sorted(bad, key=lambda r: -r["seam"])[:20]:
        print(f"  {r['file']} 색조이음매 {r['seam']:.4f} · 흰뭉갬 {r['washed']:.2f}")
    if a.out:
        a.out.write_text("\n".join(r["file"] for r in bad) + "\n", encoding="utf-8")
        print(f"목록 → {a.out}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 자가시험 통과 확인**

Run: `python3 Rpi5/Demo/test/frame_health.py --selftest`
Expected: `selftest OK`

- [ ] **Step 5: 2026-09-21 임시 스캔과 대조**

Run: `python3 Rpi5/Demo/test/frame_health.py ~/data/demo_frames --out ~/data/demo_frames/corrupt_candidates.txt`
Expected: 후보 **94장 안팎**. 크게 다르면 지표 구현이 어긋난 것이다

- [ ] **Step 6: 커밋**

```bash
cd /home/pi/sop-project/Rpi5
git add Demo/test/frame_health.py
git commit -m "feat(test): frame_health — 깨진 프레임 검출(색조 이음매·흰 뭉갬)"
```

---

### Task 4: 영상 13개 배분표 `[Claude]`

**Files:**
- Create: `~/data/demo_frames/assignment.md`

**Interfaces:**
- Consumes: `upload_console_v4.json`
- Produces: 작업자별 담당 영상 목록 + 장수. Task 7 이 이대로 나눈다

> ✅ **2026-09-21 실행 완료** — 산출물 `~/data/demo_frames/assignment.md`(팀원A 영상 6개 1,000장 / 사용자계정B 영상 7개 1,086장 · 오답 시나리오 2:2). 사람 배정 확정은 사용자 몫.

- [ ] **Step 1: 영상별 장수를 세어 두 몫으로 가른다** — 장수가 비슷하고, **오답 시나리오 영상 4개가 한쪽에 몰리지 않게** 섞는다(한쪽 사람만 위반 장면을 보면 규칙 이해가 갈린다)

```bash
python3 - <<'EOF'
import json, collections
d = json.load(open('/home/pi/data/demo_frames/upload_console_v4.json'))['uploaded']
c = collections.Counter((v['video'], v['scenario']) for v in d.values())
for (v, s), n in sorted(c.items()): print(v, s, n)
EOF
```

- [ ] **Step 2: 배분표를 쓴다** — 표 = 영상 · 시나리오 · 장수 · 담당(`팀원A` / `사용자계정B`). 합계 줄을 넣는다

- [ ] **Step 3: 대화로 보고하고 사용자 확정을 받는다** — 사람 배정은 사용자 몫이다

---

### Task 5: 사진 정리 — 흐린 141장 + 깨짐 후보 94장 `[사용자]`

**왜** — 라벨링 **전에** 빼야 두 번 그리지 않는다. 두 목록을 한 회차에 훑는다(같은 화면에서 같은 동작이다).

**Files:**
- 읽기: `~/data/demo_frames/sharp_low_141.txt` · `~/data/demo_frames/corrupt_candidates.txt`(Task 2b 산출)

- [ ] **Step 1: Roboflow 웹에서 `console_v4` → Images 를 연다**
- [ ] **Step 2: 흐린 141장** — 버튼·공구가 **무엇인지 알아볼 수 없으면** 뺀다. 흔들렸어도 알아볼 수 있으면 **남긴다**(흔들림은 학습에 필요하다 — spec §5)
- [ ] **Step 3: 깨짐 후보 94장** — 초록 뭉갬·색 어긋난 띠·흰색 날아감이면 **제거**한다. 🔴 **자동 삭제하지 않는 이유** = 얼굴 후보 91장이 전부 오탐이었던 전례(spec §9). 멀쩡하면 남긴다
- [ ] **Step 4: 배경(버튼·공구가 없는) 사진은 지우지 않는다** — 학습 버전에서 Filter Null 로 비율을 맞춘다(spec §6)
- [ ] **Step 5: 이 회차에 워밍업 20장도 함께 고른다** — Task 5b 참조(따로 훑지 않기 위해 붙였다)
- [ ] **Step 6: 흐림·깨짐 각각 몇 장을 뺐는지 Claude 에게 알려 준다** — 남은 장수가 다음 단계 기준이 된다

---

### Task 5b: 워밍업 20장 고르기 `[사용자 → Claude]`

**왜** — spec §9 워밍업 관문은 **같은 사진 20장**을 두 사람이 각자 라벨해 대조한다. 규칙이 갈리는 지점(가림·조각·B3↔EMO·쥔 공구)이 들어 있지 않으면 대조가 뜻이 없다.

🔴 **Claude 가 고를 수 없다 — 2026-09-21 에 시도해 실패했다.** 「버튼·공구가 보이는 사진」을 먼저 알아야 하는데 그것이 곧 라벨링이다. 색 지표로 골랐더니 노랑 최상위 = **모니터 화면**, 빨강 최상위 = **빨간 공구함**, 살색 최상위 = **카메라를 가린 몸**이었다(4장 확인, 적중 0). 버튼은 어두운 판 위의 작은 원이라 화면·공구함·피부에 묻힌다. **그래서 Task 5 에 붙인다** — 사람이 썸네일 격자를 넘기며 고르는 것이 빠르고 정확하다.

**Files:**
- Create: `~/data/demo_frames/warmup_20.txt`

**Interfaces:**
- Produces: Roboflow 업로드 파일명 20줄. Task 6 이 이 목록으로 작업한다

- [ ] **Step 1 `[사용자]`: Task 5 로 사진을 훑는 김에 워밍업 후보 20장을 고른다** — 구성

```
버튼이 잘 보이는 사진        8장
쥔 공구가 보이는 사진        5장
손이 버튼을 가리는 사진      4장
박스 0개(배경) 사진          3장
```

- [ ] **Step 2 `[사용자]`: 고른 20장에 Roboflow 태그 `warmup` 을 단다**(Images 에서 다중 선택 → Apply tags). 🔑 태그는 **고를 때 함께** 단다 — 나중에 붙이려면 사진마다 내부 ID 가 필요하다(2026-09-21 실측)
- [ ] **Step 3 `[Claude]`: 태그로 목록을 받아 파일로 저장한다**

```
Roboflow MCP: images_search(project_id="eung-min/console_v4", tag="warmup", fields=["name"])
→ ~/data/demo_frames/warmup_20.txt (한 줄에 파일명 하나)
```

- [ ] **Step 4 `[Claude]`: 구성을 확인해 대화로 보고한다** — 20장을 Read 로 열어 위 구성과 맞는지, 얼굴이 없는지 본다. 어긋나면 사용자에게 교체를 요청한다

---

### Task 6: 워밍업 — 같은 20장을 두 사람이 각자 라벨하고 대조 `[팀원 · 사용자 · Claude]`

**왜** — 계정이 2자리뿐이라 한 계정을 두 사람이 나눠 쓴다. 같은 사진에 두 사람의 라벨을 **동시에** 담을 수 없으므로 **순차로** 한다.

- [ ] **Step 1 `[사용자]`: 팀원 1명을 워크스페이스에 초대한다**(Settings → Members). 자리는 2개뿐이다
- [ ] **Step 2 `[팀원A]`: `warmup_20.txt` 의 20장을 가이드(PDF)대로 라벨한다**
- [ ] **Step 3 `[Claude]`: A 의 라벨을 내보내 보관한다**

```
Roboflow MCP: versions_generate(증강 0 · 전처리 Auto-Orient 만) → versions_export("yolov8")
저장: ~/data/demo_frames/warmup_A/
```

- [ ] **Step 4 `[사용자]`: 그 20장의 라벨을 지운다**(Roboflow UI — 사진은 남기고 박스만)
- [ ] **Step 5 `[작업자B · 사용자 계정]`: 같은 20장을 라벨한다**
- [ ] **Step 6 `[Claude]`: B 의 라벨도 같은 방식으로 내보내 `warmup_B/` 에 둔다**
- [ ] **Step 7 `[Claude]`: 둘을 대조해 보고한다** — 사진마다 ①박스 수 차이 ②클래스 불일치 ③IoU 0.5 미만으로 어긋난 박스. 표로 만들어 대화에 보고

- [ ] **Step 8 `[사용자]`: 판정** — 어긋난 것이 있으면 **사람이 아니라 `labeling_guide.md` 문장을 먼저 고친다**(spec §9). 고쳤으면 PDF 도 다시 만들고 **워밍업을 한 번 더** 돈다

---

### Task 7: 본 라벨링 `[팀원 · 사용자]`

- [ ] **Step 1: 배분표대로 각자 담당 영상만 라벨한다** — 남의 영상에 손대지 않는다(배분 단위가 무너진다)
- [ ] **Step 2: 확신 못 하는 물체만 있는 사진은 `exclude` 태그를 단다**(지우지 않는다 — spec §3.2)
- [ ] **Step 3: 얼굴·이름표가 보이면 그 자리에서 삭제한다**(spec §9 개인정보 관문 ④)
- [ ] **Step 4: 끝나면 Claude 에게 알린다**

---

### Task 8: 기계 점검 `[Claude]`

**Files:**
- 사용: `Rpi5/Demo/test/label_audit.py`(Task 2)

- [ ] **Step 1: 라벨된 데이터를 내보낸다**

```
Roboflow MCP: versions_generate — 전처리 = Auto-Orient 만(Resize 넣지 않는다 · 늘림 문제는 ④ 미결) · 증강 0
              → versions_export("yolov8") → ~/data/demo_frames/export_v4_r1/
```

- [ ] **Step 2: 점검을 돌린다**

Run: `python3 Rpi5/Demo/test/label_audit.py ~/data/demo_frames/export_v4_r1 --json ~/data/demo_frames/audit_r1.json`
Expected: 종료 코드 0(① 클래스 이름 통과). **1 이면 그 자리에서 멈추고** 원인을 사용자에게 보고한다 — 8종 밖 이름은 라벨러가 클래스를 새로 만들었다는 뜻이라 되돌리기 전에는 다음 단계가 무의미하다

- [ ] **Step 3: ② 조각 박스 의심 목록을 사람이 볼 수 있게 보고한다** — 클래스·파일명·면적·종횡비. 상위 20건을 표로

- [ ] **Step 4: ④ 클래스별 장수를 spec §6 기준과 대조해 보고한다** — 「클래스당 1,500장」에 얼마나 못 미치는지를 **숫자로**. 이것이 장소2·3 촬영 규모의 근거가 된다

---

### Task 9: 사람 표본 검수 `[사용자]`

- [ ] **Step 1 `[Claude]`: 표본을 뽑는다** — 작업자 배분분마다 **10%(최소 30장)** 무작위. 목록을 `~/data/demo_frames/review_sample.txt` 로 저장하고 대화에 보고
- [ ] **Step 2 `[사용자]`: 표본을 Roboflow 에서 보고 센다** — ①빠진 박스 ②잘못된 클래스 ③눈에 띄게 어긋난 경계
- [ ] **Step 3 `[사용자]`: 합격선과 대조** — 빠진 박스 **0건** · 잘못된 클래스 **0건** · 경계 어긋남 **≤5%**
- [ ] **Step 4: 불합격이면** — 그 작업자 배분분 **전량 재작업**. 재작업 **전에** `labeling_guide.md` 문장을 보강한다. 그 뒤 Task 7 → 8 → 9 를 다시 돈다

---

### Task 10: 마무리 — 정본 기록 `[Claude + 사용자]`

- [ ] **Step 1 `[사용자]`: Roboflow 에서 라벨된 사진을 Dataset 에 반영한다**(Annotate → 배치 승인)
- [ ] **Step 2 `[Claude]`: 최종 상태를 확인한다** — `projects_get` 으로 `images` / `unannotated` / 클래스별 박스 수
- [ ] **Step 3 `[Claude + 사용자]`: 공동 확인** — 클래스별 장수 · 검수 결과 · 남은 부족분
- [ ] **Step 4 `[Claude]`: `work-close` 호출** — 정본(통합문서 ② 행·§14.4) → 작업로그 → push

---

## 이 계획에서 하지 않는 것 (별도 계획으로)

- **공구 PNG 176장 재활용**(6→3종 합침 · 좌표 이식 · 전량 재검수) — 재료도 절차도 다르다
- **장소2·장소3 촬영**(촬영 도구 왜곡보정 수정이 선행) · Hailo 캘리브레이션 1,024장 확보
- **버튼 라벨 규칙 A/B 실측** — ④ 소관(spec §10)
- 학습·채점 — ③·④ 소관
